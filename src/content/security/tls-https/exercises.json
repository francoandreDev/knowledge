{
  "items": [
    {
      "type": "quiz",
      "id": "tls-purpose-1",
      "poolId": "tls-purpose-pool",
      "level": 1,
      "prompt": "What does TLS actually encrypt?",
      "choices": [
        "The connection between a browser and a server, so eavesdroppers see scrambled bytes instead of readable data",
        "The contents of the site's database on the server's disk",
        "The user's entire computer's hard drive",
        "Only the password field in a login form, leaving everything else visible"
      ],
      "correctIndex": 0,
      "reference": "<div class=\"not-prose flex flex-col gap-2 text-sm\"><div class=\"flex items-center gap-1.5\"><span>🔐</span> <strong>TLS</strong> — encrypts the connection in transit</div><div class=\"flex items-center gap-1.5\"><span>🚫</span> Says nothing about who's on the other end</div></div>",
      "explanation": "TLS encrypts the connection between browser and server in transit — so a network eavesdropper sees scrambled bytes instead of readable data. It's a guarantee about the pipe, not about who's on the other end of it."
    },
    {
      "type": "quiz",
      "id": "tls-purpose-2",
      "poolId": "tls-purpose-pool",
      "level": 1,
      "prompt": "Why does this unit describe TLS's guarantee as being \"about the pipe, not about who's on the other end\"?",
      "choices": [
        "Because TLS encrypts traffic in transit but doesn't verify the trustworthiness of the destination's content or intent",
        "Because TLS doesn't actually encrypt anything at all",
        "Because TLS only works for email, not web traffic",
        "Because \"pipe\" is a literal physical cable TLS protects"
      ],
      "correctIndex": 0,
      "reference": "<div class=\"not-prose flex flex-col gap-2 text-sm\"><div class=\"flex items-center gap-1.5\"><span>🔐</span> <strong>TLS</strong> — encrypts the connection in transit</div><div class=\"flex items-center gap-1.5\"><span>🚫</span> Says nothing about who's on the other end</div></div>",
      "explanation": "The \"pipe\" metaphor captures that TLS secures the data in transit between two endpoints — it makes no claim about whether the destination endpoint is trustworthy, honest, or who it claims to be."
    },
    {
      "type": "quiz",
      "id": "certificate-def-1",
      "poolId": "certificate-def-pool",
      "level": 1,
      "prompt": "How does this unit define a \"certificate\"?",
      "choices": [
        "A password stored on the server",
        "A signed document binding a public key to a domain name, issued by a certificate authority",
        "A list of every user who has ever visited the site",
        "A backup copy of the website's source code"
      ],
      "correctIndex": 1,
      "reference": "<div class=\"not-prose flex flex-col gap-2 text-sm\"><div class=\"flex items-center gap-1.5\"><span>📜</span> <strong>Certificate</strong> — binds a public key to a domain name</div><div class=\"flex items-center gap-1.5\"><span>✍️</span> Signed by a certificate authority</div></div>",
      "explanation": "A certificate is a signed document that binds a public key to a specific domain name — signed by a certificate authority whose signature browsers already trust."
    },
    {
      "type": "quiz",
      "id": "certificate-def-2",
      "poolId": "certificate-def-pool",
      "level": 1,
      "prompt": "What two things does a certificate bind together?",
      "choices": [
        "A username and a password",
        "A public key and a domain name",
        "A browser version and an operating system",
        "An IP address and a MAC address"
      ],
      "correctIndex": 1,
      "reference": "<div class=\"not-prose flex flex-col gap-2 text-sm\"><div class=\"flex items-center gap-1.5\"><span>📜</span> <strong>Certificate</strong> — binds a public key to a domain name</div><div class=\"flex items-center gap-1.5\"><span>✍️</span> Signed by a certificate authority</div></div>",
      "explanation": "A certificate binds a public key to a domain name — this binding, signed by a trusted CA, is what lets a browser confirm it's encrypting traffic to the party that controls that specific domain."
    },
    {
      "type": "quiz",
      "id": "ca-def-1",
      "poolId": "ca-def-pool",
      "level": 1,
      "prompt": "What is a \"certificate authority (CA)\"?",
      "choices": [
        "A government agency that regulates all internet traffic",
        "An organization whose signing key is pre-trusted by browsers and operating systems, forming the basis of the chain of trust",
        "Any website that has purchased a domain name",
        "A browser extension that blocks ads"
      ],
      "correctIndex": 1,
      "reference": "<div class=\"not-prose flex flex-col gap-2 text-sm\"><div class=\"flex items-center gap-1.5\"><span>🏛️</span> <strong>CA</strong> — signing key pre-trusted by browsers/OSes</div><div class=\"flex items-center gap-1.5\"><span>🔗</span> A cert is trusted if it chains back to one</div></div>",
      "explanation": "A certificate authority is an organization whose signing key browsers and operating systems already trust in advance — a certificate is considered trusted if its signature chain leads back to one of these."
    },
    {
      "type": "quiz",
      "id": "ca-def-2",
      "poolId": "ca-def-pool",
      "level": 1,
      "prompt": "Why does a certificate need to chain back to a CA's key rather than just being self-declared by the website?",
      "choices": [
        "Because the CA's key is pre-trusted by browsers, so the chain of signatures gives the browser a reason to trust the certificate without knowing the website in advance",
        "Self-declared certificates are actually just as trustworthy, this is only a formality",
        "Because CAs charge money and this unit says that's the only reason",
        "Because browsers can't process self-declared certificates technically"
      ],
      "correctIndex": 0,
      "reference": "<div class=\"not-prose flex flex-col gap-2 text-sm\"><div class=\"flex items-center gap-1.5\"><span>🏛️</span> <strong>CA</strong> — signing key pre-trusted by browsers/OSes</div><div class=\"flex items-center gap-1.5\"><span>🔗</span> A cert is trusted if it chains back to one</div></div>",
      "explanation": "The whole point of the CA system is that browsers pre-trust a known, limited set of CA keys — a certificate signed by (or chaining to) one of those keys inherits that pre-existing trust, which a self-declared certificate with no such chain wouldn't have."
    },
    {
      "type": "quiz",
      "id": "chain-of-trust-1",
      "poolId": "chain-of-trust-pool",
      "level": 1,
      "prompt": "What is the \"chain of trust\", per this unit?",
      "choices": [
        "A list of every website a user has ever visited",
        "The sequence of signatures from a leaf certificate up through intermediate certificates to a trusted root CA",
        "A password recovery mechanism",
        "The physical cable route data travels through"
      ],
      "correctIndex": 1,
      "reference": "<div class=\"not-prose flex flex-col gap-2 text-sm\"><div class=\"flex items-center gap-1.5\"><span>🔗</span> Leaf → Intermediate → Root</div><div class=\"flex items-center gap-1.5\"><span>✅</span> Each link verified by a signature</div></div>",
      "explanation": "The chain of trust is the sequence of signatures connecting a site's own (leaf) certificate through one or more intermediate certificates up to a root CA the browser already trusts."
    },
    {
      "type": "quiz",
      "id": "chain-of-trust-2",
      "poolId": "chain-of-trust-pool",
      "level": 1,
      "prompt": "In a typical chain of trust, what signs the leaf certificate?",
      "choices": [
        "The website's own server, self-signing its certificate",
        "An intermediate CA certificate, which in turn is signed by a root CA",
        "The end user's browser",
        "Nothing signs the leaf certificate at all"
      ],
      "correctIndex": 1,
      "reference": "<div class=\"not-prose flex flex-col gap-2 text-sm\"><div class=\"flex items-center gap-1.5\"><span>🔗</span> Leaf → Intermediate → Root</div><div class=\"flex items-center gap-1.5\"><span>✅</span> Each link verified by a signature</div></div>",
      "explanation": "In a typical chain, the leaf certificate is signed by an intermediate CA's certificate, which is itself signed by a root CA's key — a browser walks this chain, verifying each signature, until it reaches a root it already trusts."
    },
    {
      "type": "quiz",
      "id": "domain-validation-1",
      "poolId": "domain-validation-pool",
      "level": 1,
      "prompt": "What does \"domain validation (DV)\" confirm, per this unit?",
      "choices": [
        "The requester's full legal identity and business registration",
        "Only that the requester controls the domain — not who they are or what they intend to use it for",
        "That the domain has been in use for at least 10 years",
        "That the site has no security vulnerabilities"
      ],
      "correctIndex": 1,
      "reference": "<div class=\"not-prose flex flex-col gap-2 text-sm\"><div class=\"flex items-center gap-1.5\"><span>⚡</span> <strong>DV</strong> — fastest, most common certificate type</div><div class=\"flex items-center gap-1.5\"><span>✅</span> Confirms domain control only, not identity</div></div>",
      "explanation": "Domain validation confirms only that the requester controls the domain in question — it says nothing about who the requester is, what organization they represent, or their intentions for the site."
    },
    {
      "type": "quiz",
      "id": "domain-validation-2",
      "poolId": "domain-validation-pool",
      "level": 1,
      "prompt": "Domain validation is described as \"the most common, lightest-weight kind of certificate.\" What does \"lightest-weight\" mean here?",
      "choices": [
        "The certificate file itself is physically smaller in bytes than other certificate types",
        "It requires the least amount of verification — just proving domain control, without deeper identity checks",
        "It only works on mobile devices",
        "It expires faster than any other certificate type"
      ],
      "correctIndex": 1,
      "reference": "<div class=\"not-prose flex flex-col gap-2 text-sm\"><div class=\"flex items-center gap-1.5\"><span>⚡</span> <strong>DV</strong> — fastest, most common certificate type</div><div class=\"flex items-center gap-1.5\"><span>✅</span> Confirms domain control only, not identity</div></div>",
      "explanation": "\"Lightest-weight\" refers to the depth of verification required — DV certificates need only proof of domain control, which is why they can be issued quickly and automatically, unlike certificate types that also verify organizational identity."
    },
    {
      "type": "quiz",
      "id": "phishing-scenario-1",
      "poolId": "phishing-scenario-pool",
      "level": 1,
      "prompt": "In the opening scenario, what made the phishing site convincing enough that the user's padlock check didn't catch it?",
      "choices": [
        "The phishing site had no TLS certificate at all, but the browser displayed a padlock anyway due to a bug",
        "The phishing site had a genuinely valid TLS certificate and a genuine padlock — the padlock's check passed truthfully",
        "The user had disabled all security warnings in their browser settings",
        "The phishing site was hosted on the exact same server as the real PayPal"
      ],
      "correctIndex": 1,
      "reference": "<div class=\"not-prose flex flex-col gap-2 text-sm\"><div class=\"flex items-center gap-1.5\"><span>🔒</span> paypal.com — valid cert, padlock shown</div><div class=\"flex items-center gap-1.5\"><span>🔒</span> paypa1-secure-login.com — ALSO valid, ALSO padlock shown</div></div>",
      "explanation": "The phishing site genuinely had a valid certificate and a genuine padlock — the padlock check passed truthfully because it only verifies domain control and encryption, neither of which the phishing site was lacking."
    },
    {
      "type": "quiz",
      "id": "phishing-scenario-2",
      "poolId": "phishing-scenario-pool",
      "level": 1,
      "prompt": "What was the actual domain of the phishing site in the opening scenario?",
      "choices": [
        "paypal.com",
        "paypa1-secure-login.com",
        "secure.paypal.com",
        "The scenario doesn't specify a domain"
      ],
      "correctIndex": 1,
      "reference": "<div class=\"not-prose flex flex-col gap-2 text-sm\"><div class=\"flex items-center gap-1.5\"><span>🔒</span> paypal.com — valid cert, padlock shown</div><div class=\"flex items-center gap-1.5\"><span>🔒</span> paypa1-secure-login.com — ALSO valid, ALSO padlock shown</div></div>",
      "explanation": "The phishing domain was `paypa1-secure-login.com` — a lookalike domain using the numeral \"1\" in place of the letter \"l\", designed to visually resemble a legitimate PayPal domain."
    },
    {
      "type": "quiz",
      "id": "phishing-scenario-3",
      "poolId": "phishing-scenario-pool",
      "level": 1,
      "prompt": "Per the unit's own answer to its opening question, what did the padlock actually promise in the phishing scenario?",
      "choices": [
        "That the site was operated by the real PayPal company",
        "That the connection to whatever domain was in the address bar was encrypted, and a CA verified someone controls that domain",
        "That the site had passed a manual security audit",
        "Nothing — the padlock is purely decorative and checks nothing"
      ],
      "correctIndex": 1,
      "reference": "<div class=\"not-prose flex flex-col gap-2 text-sm\"><div class=\"flex items-center gap-1.5\"><span>🔒</span> paypal.com — valid cert, padlock shown</div><div class=\"flex items-center gap-1.5\"><span>🔒</span> paypa1-secure-login.com — ALSO valid, ALSO padlock shown</div></div>",
      "explanation": "The unit's answer is precise: the padlock promises the connection to the domain in the address bar is encrypted and that a CA verified control of that exact domain — nothing about who that domain actually belongs to in the real world."
    },
    {
      "type": "quiz",
      "id": "chain-checks-1",
      "poolId": "chain-checks-pool",
      "level": 2,
      "prompt": "Per L2's flowchart, how many checks does the browser perform before showing a padlock, and what are they?",
      "choices": [
        "Three checks: domain match, signature validity, and a manual human review",
        "Two checks: does the certificate's domain match the address bar, and does every signature in the chain verify up to a trusted root",
        "One check: whether the site has ever been reported as malicious",
        "Zero checks — the padlock appears automatically for any HTTPS site"
      ],
      "correctIndex": 1,
      "reference": "<div class=\"not-prose flex flex-col gap-2 text-sm\"><div class=\"flex items-center gap-1.5\"><span>1️⃣</span> Domain in cert matches address bar?</div><div class=\"flex items-center gap-1.5\"><span>2️⃣</span> Every signature in the chain valid up to a trusted root?</div></div>",
      "explanation": "L2's flowchart shows exactly two checks: whether the certificate's domain matches the address bar, and whether every signature in the chain verifies up to a trusted root — no human review, no deeper identity check, for the common case."
    },
    {
      "type": "quiz",
      "id": "chain-checks-2",
      "poolId": "chain-checks-pool",
      "level": 2,
      "prompt": "L2 states neither of the browser's two checks asks a certain question. What is it?",
      "choices": [
        "\"Is this organization who it claims to be\" in any deeper sense than \"do they control this specific domain name\"",
        "\"Is the connection encrypted at all\"",
        "\"Does the certificate exist\"",
        "\"Is the domain name spelled correctly according to a dictionary\""
      ],
      "correctIndex": 0,
      "reference": "<div class=\"not-prose flex flex-col gap-2 text-sm\"><div class=\"flex items-center gap-1.5\"><span>1️⃣</span> Domain in cert matches address bar?</div><div class=\"flex items-center gap-1.5\"><span>2️⃣</span> Every signature in the chain valid up to a trusted root?</div></div>",
      "explanation": "L2 is explicit that neither check asks \"is this organization who it claims to be\" beyond domain control — that deeper identity question is simply out of scope for the standard chain-of-trust verification most sites use."
    },
    {
      "type": "quiz",
      "id": "padlock-proves-1",
      "poolId": "padlock-proves-pool",
      "level": 2,
      "prompt": "Per L2's two-column table, which of these is something the padlock DOES prove?",
      "choices": [
        "The site's content is honest and non-malicious",
        "The certificate's domain matches the address bar exactly",
        "The domain is operated by the company the user assumes",
        "The server won't misuse data once it arrives"
      ],
      "correctIndex": 1,
      "reference": "<table class=\"not-prose w-full text-xs\"><tr><td>PROVES</td><td>encrypted in transit; domain matches address bar; CA verified domain control</td></tr><tr><td>DOESN'T PROVE</td><td>content is honest; domain is who you think; real-world identity vetted; server won't misuse data</td></tr></table>",
      "explanation": "L2's left column (what the padlock proves) includes exact domain matching to the address bar — one of only a few things the padlock actually guarantees, distinct from the much larger list of things it does not guarantee."
    },
    {
      "type": "quiz",
      "id": "padlock-proves-2",
      "poolId": "padlock-proves-pool",
      "level": 2,
      "prompt": "Per L2's two-column table, which of these is something the padlock does NOT prove?",
      "choices": [
        "That the connection is encrypted in transit",
        "That data can't be silently read by a network eavesdropper",
        "That the domain is operated by the company or person the user assumes",
        "That a trusted certificate authority verified control of that domain"
      ],
      "correctIndex": 2,
      "reference": "<table class=\"not-prose w-full text-xs\"><tr><td>PROVES</td><td>encrypted in transit; domain matches address bar; CA verified domain control</td></tr><tr><td>DOESN'T PROVE</td><td>content is honest; domain is who you think; real-world identity vetted; server won't misuse data</td></tr></table>",
      "explanation": "\"The domain is operated by the company or person the user assumes\" sits firmly in the right column — the padlock never verifies real-world ownership or brand identity, only domain control and encryption."
    },
    {
      "type": "quiz",
      "id": "padlock-proves-3",
      "poolId": "padlock-proves-pool",
      "level": 2,
      "prompt": "L2 says \"the gap between these two columns is exactly where phishing over HTTPS lives.\" What does this mean?",
      "choices": [
        "Phishing sites can satisfy everything in the \"proves\" column while still doing real harm through the \"doesn't prove\" column",
        "Phishing is impossible once a site has a valid TLS certificate",
        "The two columns are identical, so there's no real gap",
        "Phishing sites can never obtain a valid TLS certificate under any circumstances"
      ],
      "correctIndex": 0,
      "reference": "<table class=\"not-prose w-full text-xs\"><tr><td>PROVES</td><td>encrypted in transit; domain matches address bar; CA verified domain control</td></tr><tr><td>DOESN'T PROVE</td><td>content is honest; domain is who you think; real-world identity vetted; server won't misuse data</td></tr></table>",
      "explanation": "L2's point is that a phishing site can legitimately satisfy every item the padlock actually verifies (encryption, domain match, CA-verified control) while still causing real harm through everything the padlock never claimed to check — that's the exploited gap."
    },
    {
      "type": "quiz",
      "id": "dv-lightweight-1",
      "poolId": "dv-lightweight-pool",
      "level": 2,
      "prompt": "L2 explains why domain validation was \"deliberately designed to be lightweight.\" What was the reasoning?",
      "choices": [
        "Because CAs are legally forbidden from doing deeper checks",
        "Because fast, automated, cheap issuance is what made HTTPS practical for the entire web to adopt at scale",
        "Because lightweight checks are more secure than thorough ones",
        "Because it was an accident of early internet design with no deliberate reasoning"
      ],
      "correctIndex": 1,
      "reference": "<div class=\"not-prose flex flex-col gap-2 text-sm\"><div class=\"flex items-center gap-1.5\"><span>⚡</span> DV: automated, minutes, no human review</div><div class=\"flex items-center gap-1.5\"><span>🏢</span> EV: adds real identity checks, but rare and no longer visually distinguished</div></div>",
      "explanation": "L2 explains that DV's speed and automation is what let HTTPS scale to the entire web — a certificate issued within minutes with no human review, trading deeper identity verification for practicality at massive scale."
    },
    {
      "type": "quiz",
      "id": "dv-lightweight-2",
      "poolId": "dv-lightweight-pool",
      "level": 2,
      "prompt": "What does L2 say about Extended Validation (EV) certificates today?",
      "choices": [
        "They're now the most common certificate type on the web",
        "They add real organizational identity checks but are far less common, and browsers no longer visually distinguish them from DV certificates the way they used to",
        "They've been completely banned by all major browsers",
        "They require no verification at all, unlike DV certificates"
      ],
      "correctIndex": 1,
      "reference": "<div class=\"not-prose flex flex-col gap-2 text-sm\"><div class=\"flex items-center gap-1.5\"><span>⚡</span> DV: automated, minutes, no human review</div><div class=\"flex items-center gap-1.5\"><span>🏢</span> EV: adds real identity checks, but rare and no longer visually distinguished</div></div>",
      "explanation": "L2 notes that EV certificates do add genuine organizational identity verification, but they're far less common than DV certificates, and modern browsers no longer show a distinct visual indicator for them — so most users never see the difference even when it exists."
    },
    {
      "type": "quiz",
      "id": "https-not-trustworthy-1",
      "poolId": "https-not-trustworthy-pool",
      "level": 2,
      "prompt": "L2 lists \"treating HTTPS as a synonym for trustworthy\" as a failure mode. What's the correct distinction?",
      "choices": [
        "HTTPS answers \"is this connection private,\" not \"should I trust what's on the other end\"",
        "HTTPS and trustworthiness are actually the exact same thing, so this isn't really a failure mode",
        "HTTPS has nothing to do with privacy at all",
        "HTTPS only applies to banking websites specifically"
      ],
      "correctIndex": 0,
      "reference": "<div class=\"not-prose flex flex-col gap-2 text-sm\"><div class=\"flex items-center gap-1.5\"><span>⚠️</span> HTTPS = private connection, not vetted destination</div><div class=\"flex items-center gap-1.5\"><span>⚠️</span> Most certs are domain-only, not identity-vetted</div></div>",
      "explanation": "L2's correction is that HTTPS answers a narrow question — is this connection private — and conflating that with \"should I trust the destination\" is exactly the confusion phishing over HTTPS exploits."
    },
    {
      "type": "quiz",
      "id": "https-not-trustworthy-2",
      "poolId": "https-not-trustworthy-pool",
      "level": 2,
      "prompt": "Why does L2 warn against assuming a CA vetted an organization's real-world identity?",
      "choices": [
        "Because for the overwhelming majority of certificates (domain-validated), the CA only checked domain control, not a business registration or physical address",
        "Because CAs never verify anything at all, ever, for any certificate type",
        "Because all certificates require government ID verification by law",
        "Because this concern doesn't actually apply to any real certificates"
      ],
      "correctIndex": 0,
      "reference": "<div class=\"not-prose flex flex-col gap-2 text-sm\"><div class=\"flex items-center gap-1.5\"><span>⚠️</span> HTTPS = private connection, not vetted destination</div><div class=\"flex items-center gap-1.5\"><span>⚠️</span> Most certs are domain-only, not identity-vetted</div></div>",
      "explanation": "For the vast majority of certificates in use (domain-validated), the CA verified only domain control — not a business registration, physical address, or who's actually operating the site, which is exactly the assumption L2 warns against making."
    },
    {
      "type": "quiz",
      "id": "chain-crypto-1",
      "poolId": "chain-crypto-pool",
      "level": 3,
      "prompt": "In L3's `signCert`/`verifyCertSignature` implementation, what cryptographic algorithm is used?",
      "choices": [
        "A simple string comparison with no real cryptography",
        "SHA-256 hashing combined with RSA signing/verification via Node's built-in `crypto` module",
        "A custom, hand-rolled encryption algorithm",
        "Base64 encoding, which L3 treats as equivalent to encryption"
      ],
      "correctIndex": 1,
      "reference": "<div class=\"not-prose flex flex-col gap-2 text-sm\"><div class=\"flex items-center gap-1.5\"><span>1️⃣</span> leafCert.subject === hostname</div><div class=\"flex items-center gap-1.5\"><span>2️⃣</span> intermediateCert signed by rootPublicKey</div><div class=\"flex items-center gap-1.5\"><span>3️⃣</span> leafCert signed by intermediateCert.publicKey</div></div>",
      "explanation": "L3 uses Node's built-in `crypto` module for genuine SHA-256/RSA signing and verification — `crypto.sign(\"sha256\", ...)` and `crypto.verify(\"sha256\", ...)` — not a toy stand-in, so the verification result reflects real cryptographic behavior."
    },
    {
      "type": "quiz",
      "id": "chain-crypto-2",
      "poolId": "chain-crypto-pool",
      "level": 3,
      "prompt": "In L3's `verifyChain` function, what are the exact three checks performed, in order?",
      "choices": [
        "Check the certificate's file size, check the expiration date, check the issuer's country",
        "Hostname matches leaf subject; intermediate is signed by the trusted root; leaf is signed by the intermediate",
        "Only one check: whether the domain has ever been reported as malicious",
        "Check that the site uses HTTPS, check that it has a green padlock, check that it loads quickly"
      ],
      "correctIndex": 1,
      "reference": "<div class=\"not-prose flex flex-col gap-2 text-sm\"><div class=\"flex items-center gap-1.5\"><span>1️⃣</span> leafCert.subject === hostname</div><div class=\"flex items-center gap-1.5\"><span>2️⃣</span> intermediateCert signed by rootPublicKey</div><div class=\"flex items-center gap-1.5\"><span>3️⃣</span> leafCert signed by intermediateCert.publicKey</div></div>",
      "explanation": "L3's `verifyChain` performs exactly three checks in order: hostname match against the leaf's subject, the intermediate's signature verified against the trusted root's public key, and the leaf's signature verified against the intermediate's public key."
    },
    {
      "type": "quiz",
      "id": "https-def-1",
      "poolId": "https-def-pool",
      "level": 2,
      "prompt": "What is HTTPS, per this unit?",
      "choices": [
        "A completely separate protocol from HTTP with no relationship to it",
        "HTTP running over TLS",
        "A faster version of HTTP with no encryption involved",
        "A protocol used only for file downloads, never for regular web browsing"
      ],
      "correctIndex": 1,
      "reference": "<div class=\"not-prose flex flex-col gap-2 text-sm\"><div class=\"flex items-center gap-1.5\"><span>🌐</span> HTTPS = HTTP + TLS</div><div class=\"flex items-center gap-1.5\"><span>🔐</span> TLS supplies the encryption layer underneath</div></div>",
      "explanation": "HTTPS is simply HTTP running over TLS — the same HTTP requests and responses, but wrapped in the encrypted, authenticated connection TLS provides."
    },
    {
      "type": "quiz",
      "id": "https-def-2",
      "poolId": "https-def-pool",
      "level": 2,
      "prompt": "Why does defining HTTPS as \"HTTP over TLS\" matter for understanding what a padlock guarantees?",
      "choices": [
        "It doesn't matter — HTTPS and TLS have nothing to do with each other conceptually",
        "It clarifies that the padlock reflects TLS's guarantees specifically (encryption, domain-bound certificate) layered under ordinary HTTP, not some separate, broader safety system",
        "It means HTTPS requires a completely different set of HTML tags than HTTP",
        "It means HTTPS sites load content twice as fast as HTTP sites by definition"
      ],
      "correctIndex": 1,
      "reference": "<div class=\"not-prose flex flex-col gap-2 text-sm\"><div class=\"flex items-center gap-1.5\"><span>🌐</span> HTTPS = HTTP + TLS</div><div class=\"flex items-center gap-1.5\"><span>🔐</span> TLS supplies the encryption layer underneath</div></div>",
      "explanation": "Understanding HTTPS as HTTP layered over TLS clarifies that the padlock reflects exactly TLS's specific guarantees (encryption, domain-bound certificate verification) — not some separate, broader safety or trustworthiness system with its own independent checks."
    },
    {
      "type": "quiz",
      "id": "hostname-strict-1",
      "poolId": "hostname-strict-pool",
      "level": 2,
      "prompt": "L2 says the domain-match check is \"strict about the exact string in the address bar.\" What does this mean in practice?",
      "choices": [
        "The check ignores minor spelling differences between similar-looking domains",
        "A certificate for `paypal.com` will not satisfy a domain-match check for `paypa1-secure-login.com`, or vice versa — the strings must match exactly (or match a wildcard pattern)",
        "The check only cares about the first letter of the domain",
        "The domain-match check doesn't actually exist and was invented for this unit"
      ],
      "correctIndex": 1,
      "reference": "<div class=\"not-prose flex flex-col gap-2 text-sm\"><div class=\"flex items-center gap-1.5\"><span>🔍</span> Domain-match check: exact string, no fuzzy matching</div><div class=\"flex items-center gap-1.5\"><span>🙋</span> But it can't tell a human that a lookalike string isn't the real brand</div></div>",
      "explanation": "The domain-match check is strict and exact — a certificate issued for one exact domain string won't satisfy a check against a different string, even a visually similar one. What it can't do is judge whether a *different, valid* domain string is a legitimate brand's domain or a deliberate lookalike."
    },
    {
      "type": "quiz",
      "id": "hostname-strict-2",
      "poolId": "hostname-strict-pool",
      "level": 2,
      "prompt": "L2 notes the domain-match check \"can't tell a human that paypa1-secure-login.com isn't paypal.com.\" Why not, given that the check is exact and strict?",
      "choices": [
        "Because the check only verifies that the certificate's domain matches whatever is in the address bar — it has no concept of which domains are legitimate brands versus lookalikes",
        "Because the check is actually broken and never verifies anything correctly",
        "Because paypa1-secure-login.com and paypal.com are technically the same string to a computer",
        "Because browsers don't actually perform this check at all"
      ],
      "correctIndex": 0,
      "reference": "<div class=\"not-prose flex flex-col gap-2 text-sm\"><div class=\"flex items-center gap-1.5\"><span>🔍</span> Domain-match check: exact string, no fuzzy matching</div><div class=\"flex items-center gap-1.5\"><span>🙋</span> But it can't tell a human that a lookalike string isn't the real brand</div></div>",
      "explanation": "The check verifies certificate-to-address-bar string matching only — it has no notion of \"legitimate brand\" versus \"lookalike domain,\" so a completely valid, correctly-matching certificate for a phishing domain passes the check just as truthfully as one for the real brand would."
    },
    {
      "type": "quiz",
      "id": "ca-hierarchy-setup-1",
      "poolId": "ca-hierarchy-setup-pool",
      "level": 3,
      "prompt": "In L3's CA hierarchy setup, how many separate RSA key pairs are generated in total?",
      "choices": [
        "Two — one for the root, one shared by everything else",
        "Four — root, intermediate, the legitimate leaf, and the phishing leaf",
        "One single key pair used for the entire chain",
        "L3 doesn't generate any real key pairs, only mock placeholders"
      ],
      "correctIndex": 1,
      "reference": "<div class=\"not-prose flex flex-col gap-2 text-sm\"><div class=\"flex items-center gap-1.5\"><span>🔑</span> root, intermediate, leafLegit, leafPhish — 4 real key pairs</div><div class=\"flex items-center gap-1.5\"><span>🏛️</span> Same intermediate CA signs both leaf certs</div></div>",
      "explanation": "L3 generates four separate RSA key pairs: `root`, `intermediate`, `leafLegit`, and `leafPhish` — each a genuine key pair produced by `crypto.generateKeyPairSync`."
    },
    {
      "type": "quiz",
      "id": "ca-hierarchy-setup-2",
      "poolId": "ca-hierarchy-setup-pool",
      "level": 3,
      "prompt": "In L3's setup, which entity signs both the legitimate `paypal.com` leaf certificate and the phishing `paypa1-secure-login.com` leaf certificate?",
      "choices": [
        "Two completely different, unrelated intermediate CAs",
        "The exact same intermediate CA signs both",
        "The root CA signs the leaves directly, skipping the intermediate entirely",
        "Neither certificate is actually signed by anything in L3's example"
      ],
      "correctIndex": 1,
      "reference": "<div class=\"not-prose flex flex-col gap-2 text-sm\"><div class=\"flex items-center gap-1.5\"><span>🔑</span> root, intermediate, leafLegit, leafPhish — 4 real key pairs</div><div class=\"flex items-center gap-1.5\"><span>🏛️</span> Same intermediate CA signs both leaf certs</div></div>",
      "explanation": "The same intermediate CA signs both the legitimate and phishing leaf certificates in L3's example — reinforcing that a single CA has no built-in way to distinguish a brand's real domain from a lookalike domain, since both requesters equally proved control of their respective domains."
    },
    {
      "type": "quiz",
      "id": "ca-hierarchy-setup-3",
      "poolId": "ca-hierarchy-setup-pool",
      "level": 3,
      "prompt": "Per L3, why does the CA have \"no way to know, and no obligation to check\" that one domain is impersonating a well-known brand?",
      "choices": [
        "Because the CA only verifies domain control for each requester, and both `paypal.com` and `paypa1-secure-login.com` requesters could each prove control of their own distinct domain",
        "Because CAs are legally required to ignore brand impersonation entirely by international law",
        "Because the CA's software has a known bug that always approves every request",
        "Because L3 says CAs never issue certificates to phishing domains in practice"
      ],
      "correctIndex": 0,
      "reference": "<div class=\"not-prose flex flex-col gap-2 text-sm\"><div class=\"flex items-center gap-1.5\"><span>🔑</span> root, intermediate, leafLegit, leafPhish — 4 real key pairs</div><div class=\"flex items-center gap-1.5\"><span>🏛️</span> Same intermediate CA signs both leaf certs</div></div>",
      "explanation": "L3 explains that domain validation only checks control of the specific domain being requested — since `paypa1-secure-login.com` is a distinct, real domain that its requester genuinely controlled, the CA has no cryptographic basis to distinguish it from any other legitimately-controlled domain, brand impersonation or not."
    },
    {
      "type": "code",
      "id": "verify-chain-logic",
      "poolId": "verify-chain-logic-pool",
      "level": 3,
      "prompt": "Implement `verifyChain(leafCert, intermediateCert, trustedRootId, hostname)` using this simplified mock-signature model: each cert is `{ subject, issuer, signedBy }` where `signedBy` holds the id of whoever actually signed it. Return `{ valid: false, reason: 'hostname mismatch' }` if `leafCert.subject !== hostname`. Return `{ valid: false, reason: 'intermediate not signed by trusted root' }` if `intermediateCert.signedBy !== trustedRootId`. Return `{ valid: false, reason: 'leaf not signed by intermediate' }` if `leafCert.signedBy !== intermediateCert.subject`. Otherwise return `{ valid: true, reason: 'chain verifies to a trusted root, hostname matches' }`.",
      "starterCode": "function verifyChain(leafCert, intermediateCert, trustedRootId, hostname) {\n  // TODO: implement the three checks described above, in order\n}\n",
      "tests": [
        {
          "description": "valid chain passes",
          "expr": "expect(verifyChain({ subject: 'paypal.com', issuer: 'IntermediateCA', signedBy: 'IntermediateCA' }, { subject: 'IntermediateCA', issuer: 'RootCA', signedBy: 'RootCA' }, 'RootCA', 'paypal.com').valid).toBe(true)"
        },
        {
          "description": "rejects a hostname mismatch",
          "expr": "expect(verifyChain({ subject: 'paypal.com', issuer: 'IntermediateCA', signedBy: 'IntermediateCA' }, { subject: 'IntermediateCA', issuer: 'RootCA', signedBy: 'RootCA' }, 'RootCA', 'evil.com').reason).toBe('hostname mismatch')"
        },
        {
          "description": "rejects an intermediate not signed by the trusted root",
          "expr": "expect(verifyChain({ subject: 'paypal.com', issuer: 'IntermediateCA', signedBy: 'IntermediateCA' }, { subject: 'IntermediateCA', issuer: 'RootCA', signedBy: 'UntrustedRoot' }, 'RootCA', 'paypal.com').valid).toBe(false)"
        },
        {
          "description": "rejects a leaf not signed by the intermediate",
          "expr": "expect(verifyChain({ subject: 'paypal.com', issuer: 'IntermediateCA', signedBy: 'SomeOtherCA' }, { subject: 'IntermediateCA', issuer: 'RootCA', signedBy: 'RootCA' }, 'RootCA', 'paypal.com').valid).toBe(false)"
        },
        {
          "description": "a phishing domain with a legitimately-signed chain still validates true",
          "expr": "expect(verifyChain({ subject: 'paypa1-secure-login.com', issuer: 'IntermediateCA', signedBy: 'IntermediateCA' }, { subject: 'IntermediateCA', issuer: 'RootCA', signedBy: 'RootCA' }, 'RootCA', 'paypa1-secure-login.com').valid).toBe(true)"
        }
      ],
      "solution": "function verifyChain(leafCert, intermediateCert, trustedRootId, hostname) {\n  if (leafCert.subject !== hostname) {\n    return { valid: false, reason: 'hostname mismatch' };\n  }\n  if (intermediateCert.signedBy !== trustedRootId) {\n    return { valid: false, reason: 'intermediate not signed by trusted root' };\n  }\n  if (leafCert.signedBy !== intermediateCert.subject) {\n    return { valid: false, reason: 'leaf not signed by intermediate' };\n  }\n  return { valid: true, reason: 'chain verifies to a trusted root, hostname matches' };\n}",
      "explanation": "The last test is the important one: a phishing domain with a completely legitimate, correctly-signed certificate chain still returns `valid: true` — exactly matching L3's real crypto-based demonstration that chain verification succeeding says nothing about whether the domain belongs to who the user thinks it does.",
      "reference": "<div class=\"not-prose flex flex-col gap-2 text-sm\"><div class=\"flex items-center gap-1.5\"><span>1️⃣</span> leafCert.subject === hostname</div><div class=\"flex items-center gap-1.5\"><span>2️⃣</span> intermediateCert signed by rootPublicKey</div><div class=\"flex items-center gap-1.5\"><span>3️⃣</span> leafCert signed by intermediateCert.publicKey</div></div>"
    },
    {
      "type": "code",
      "id": "hostname-matches-exact",
      "poolId": "hostname-matches-pool",
      "level": 3,
      "prompt": "Write `hostnameMatches(certSubject, addressBarHost)` that returns `true` if a certificate's subject matches the browser's address bar hostname. Support an exact match, and a leading wildcard (`*.example.com` matches `mail.example.com` but NOT `example.com` itself, and NOT `sub.mail.example.com` — a wildcard only covers exactly one extra label).",
      "starterCode": "function hostnameMatches(certSubject, addressBarHost) {\n  // TODO: support exact match, and one-level wildcard matching (*.example.com)\n}\n",
      "tests": [
        {
          "description": "exact match",
          "expr": "expect(hostnameMatches('paypal.com', 'paypal.com')).toBe(true)"
        },
        {
          "description": "lookalike domain does not match",
          "expr": "expect(hostnameMatches('paypal.com', 'paypa1.com')).toBe(false)"
        },
        {
          "description": "wildcard matches one subdomain level",
          "expr": "expect(hostnameMatches('*.example.com', 'mail.example.com')).toBe(true)"
        },
        {
          "description": "wildcard does not match the bare domain",
          "expr": "expect(hostnameMatches('*.example.com', 'example.com')).toBe(false)"
        },
        {
          "description": "wildcard does not match two levels of subdomain",
          "expr": "expect(hostnameMatches('*.example.com', 'sub.mail.example.com')).toBe(false)"
        }
      ],
      "solution": "function hostnameMatches(certSubject, addressBarHost) {\n  if (certSubject.startsWith('*.')) {\n    const suffix = certSubject.slice(1);\n    return (\n      addressBarHost.endsWith(suffix) &&\n      addressBarHost.split('.').length === certSubject.split('.').length\n    );\n  }\n  return certSubject === addressBarHost;\n}",
      "explanation": "A wildcard certificate is scoped to exactly one label of subdomain — matching the label count keeps `*.example.com` from silently matching either the bare domain or a deeper nested subdomain, both of which would widen the certificate's actual scope beyond what it was issued for.",
      "reference": "<div class=\"not-prose flex flex-col gap-2 text-sm\"><div class=\"flex items-center gap-1.5\"><span>✅</span> Exact match: subject === hostname</div><div class=\"flex items-center gap-1.5\"><span>⭐</span> Wildcard: matches exactly one extra label, not zero or two+</div></div>"
    },
    {
      "type": "quiz",
      "id": "both-validate-true-1",
      "poolId": "both-validate-true-pool",
      "level": 3,
      "prompt": "L3 verifies both `paypal.com` and `paypa1-secure-login.com`'s certificate chains. What does the result of running both checks show?",
      "choices": [
        "Only the legitimate domain's chain verifies as valid; the phishing domain's chain fails",
        "Both chains verify as `valid: true` — this is the correct, honest result, not a bug",
        "Both chains fail to verify, since phishing domains can never obtain valid certificates",
        "The verifier crashes when given the phishing domain's certificate"
      ],
      "correctIndex": 1,
      "reference": "<div class=\"not-prose flex flex-col gap-2 text-sm\"><div class=\"flex items-center gap-1.5\"><span>✅</span> Both chains verify true — correct, not a bug</div><div class=\"flex items-center gap-1.5\"><span>❓</span> Neither the CA nor verifier was ever asked \"is this the real PayPal\"</div></div>",
      "explanation": "Both chains verify as `valid: true` in L3's demonstration — this is explicitly not a bug in the verifier; it's the correct result for the question the verifier was actually built to answer (domain-to-key binding), which never included \"is this the real PayPal.\""
    },
    {
      "type": "quiz",
      "id": "both-validate-true-2",
      "poolId": "both-validate-true-pool",
      "level": 3,
      "prompt": "Why does L3 insist that both chains validating true is \"not a bug in the verifier\"?",
      "choices": [
        "Because the verifier is broken and needs to be rewritten immediately",
        "Because the chain-of-trust check answered exactly the question it's designed to answer — truthfully — for both domains; \"is this the real PayPal\" was never part of its contract",
        "Because bugs are impossible in cryptographic code by definition",
        "Because the phishing domain was actually registered by PayPal itself in this example"
      ],
      "correctIndex": 1,
      "reference": "<div class=\"not-prose flex flex-col gap-2 text-sm\"><div class=\"flex items-center gap-1.5\"><span>✅</span> Both chains verify true — correct, not a bug</div><div class=\"flex items-center gap-1.5\"><span>❓</span> Neither the CA nor verifier was ever asked \"is this the real PayPal\"</div></div>",
      "explanation": "L3 argues the verifier is functioning exactly as intended — it truthfully answers \"does this certificate chain to a trusted root for this domain,\" and that question was correctly answered yes for both domains, since brand-identity verification was never part of what the function checks."
    },
    {
      "type": "quiz",
      "id": "tampering-detection-1",
      "poolId": "tampering-detection-pool",
      "level": 3,
      "prompt": "In L3's tampering test, what happens when `legitLeafCert`'s `subject` field is changed to `paypal.com-but-tampered` without re-signing?",
      "choices": [
        "The signature still verifies successfully, proving tampering is undetectable",
        "The signature verification returns false, since the signature covers the exact original certificate contents",
        "The tampered certificate automatically gets re-signed by the intermediate CA",
        "Nothing happens — the tampering test isn't actually run in L3"
      ],
      "correctIndex": 1,
      "reference": "<div class=\"not-prose flex flex-col gap-2 text-sm\"><div class=\"flex items-center gap-1.5\"><span>🔨</span> Tampered subject, same old signature</div><div class=\"flex items-center gap-1.5\"><span>❌</span> verifyCertSignature returns false — signature no longer matches contents</div></div>",
      "explanation": "L3 shows that modifying `subject` without re-signing breaks verification — `verifyCertSignature` returns `false`, since the signature was computed over the original contents and no longer matches the altered payload."
    },
    {
      "type": "quiz",
      "id": "tampering-detection-2",
      "poolId": "tampering-detection-pool",
      "level": 3,
      "prompt": "What does the tampering test demonstrate about the limits of what the verifier catches, per L3?",
      "choices": [
        "That the verifier catches every possible kind of certificate misuse, including lookalike domains",
        "That the verifier correctly detects post-issuance tampering, but detecting a lookalike domain string is a separate, non-cryptographic problem it was never built to solve",
        "That tampering can never be detected by any cryptographic system",
        "That the verifier is completely useless and should be discarded"
      ],
      "correctIndex": 1,
      "reference": "<div class=\"not-prose flex flex-col gap-2 text-sm\"><div class=\"flex items-center gap-1.5\"><span>🔨</span> Tampered subject, same old signature</div><div class=\"flex items-center gap-1.5\"><span>❌</span> verifyCertSignature returns false — signature no longer matches contents</div></div>",
      "explanation": "L3 uses the tampering test to show the verifier isn't useless — it correctly catches post-issuance tampering — while making clear that detecting a lookalike domain string is a fundamentally different, lexical (not cryptographic) problem that has to be solved elsewhere."
    },
    {
      "type": "quiz",
      "id": "generalization-1",
      "poolId": "generalization-pool",
      "level": 3,
      "prompt": "L3 asks whether this demonstration means TLS/HTTPS is pointless. What's the answer?",
      "choices": [
        "Yes — TLS provides no real value once phishing over HTTPS is possible",
        "No — the encryption and hostname-match guarantees are real and valuable; without them, anyone on the network path could read or rewrite even the legitimate site's login form",
        "TLS is only useful for sites that don't handle passwords",
        "The unit doesn't actually answer this question"
      ],
      "correctIndex": 1,
      "reference": "<div class=\"not-prose flex flex-col gap-2 text-sm\"><div class=\"flex items-center gap-1.5\"><span>✅</span> Chain-of-trust: necessary, not sufficient</div><div class=\"flex items-center gap-1.5\"><span>🔍</span> Lookalike-domain detection needs a separate, deliberate check</div></div>",
      "explanation": "L3 explicitly rejects \"TLS is pointless\" — the encryption and hostname-match guarantees remain real and valuable, since without them even the legitimate `paypal.com` would be vulnerable to network-level eavesdropping or tampering."
    },
    {
      "type": "quiz",
      "id": "generalization-2",
      "poolId": "generalization-pool",
      "level": 3,
      "prompt": "L3's extend-it question asks whether lookalike-domain detection belongs inside `verifyChain` itself. What consideration does it raise about putting it there?",
      "choices": [
        "It raises no real consideration — the answer is obviously yes with no downsides",
        "Whether that responsibility belongs bundled into the cryptographic chain check or needs to live as a separate system, and what would change about the function's scope if merged in",
        "That `verifyChain` should be deleted entirely and replaced with lookalike detection only",
        "That lookalike detection is technically impossible to implement in any system"
      ],
      "correctIndex": 1,
      "reference": "<div class=\"not-prose flex flex-col gap-2 text-sm\"><div class=\"flex items-center gap-1.5\"><span>✅</span> Chain-of-trust: necessary, not sufficient</div><div class=\"flex items-center gap-1.5\"><span>🔍</span> Lookalike-domain detection needs a separate, deliberate check</div></div>",
      "explanation": "The extend-it question invites weighing whether lookalike detection belongs bundled inside the cryptographic chain-verification function or should live as an independent, separately-scoped check — a genuine design question about where responsibilities should sit, not a settled answer."
    },
    {
      "type": "quiz",
      "id": "generalization-3",
      "poolId": "generalization-pool",
      "level": 3,
      "prompt": "What is L3's core summary of what chain-of-trust verification is, in relation to deciding whether a site is safe to trust with a password?",
      "choices": [
        "It's both necessary and sufficient — nothing else needs to be checked",
        "It's a necessary check, not a sufficient one",
        "It's neither necessary nor sufficient, and provides no real value",
        "It's sufficient but not necessary — a site could be safe without it"
      ],
      "correctIndex": 1,
      "reference": "<div class=\"not-prose flex flex-col gap-2 text-sm\"><div class=\"flex items-center gap-1.5\"><span>✅</span> Chain-of-trust: necessary, not sufficient</div><div class=\"flex items-center gap-1.5\"><span>🔍</span> Lookalike-domain detection needs a separate, deliberate check</div></div>",
      "explanation": "L3's core summary is precise: chain-of-trust verification is a necessary check for deciding whether a site is safe to trust with a password, but it is not sufficient on its own — additional checks (like lookalike-domain detection) are needed to close the gap."
    }
  ]
}
