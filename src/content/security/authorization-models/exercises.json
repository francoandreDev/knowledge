{
  "items": [
    {
      "type": "quiz",
      "id": "authn-vs-authz-1",
      "poolId": "authn-vs-authz-pool",
      "level": 1,
      "prompt": "What's the difference between authentication and authorization?",
      "choices": [
        "They're the same thing, just different terms",
        "Authentication answers 'who are you'; authorization answers 'what are you allowed to do' — a system can get the first completely right while failing the second",
        "Authentication is for admins, authorization is for regular users",
        "Authorization always happens before authentication"
      ],
      "correctIndex": 1,
      "explanation": "These are genuinely separate questions — the invoice endpoint proved a system can correctly authenticate a user while still failing to authorize their specific request.",
      "reference": "<table class=\"mt-1 w-full border-collapse text-xs\"><thead><tr><th class=\"border-b border-slate-300 pb-1 text-left dark:border-slate-600\">Check</th><th class=\"border-b border-slate-300 pb-1 text-left dark:border-slate-600\">Answers</th></tr></thead><tbody><tr><td class=\"py-1 pr-2\">Valid session?</td><td class=\"py-1\">Authentication</td></tr><tr><td class=\"py-1 pr-2\">Owns this resource?</td><td class=\"py-1\">Authorization</td></tr></tbody></table><p class=\"mt-2 text-xs text-slate-500 dark:text-slate-400\"><strong>Why:</strong> the invoice endpoint got authentication right and skipped authorization entirely.</p>",
      "learnMore": "<h5 class=\"mt-0 mb-2 text-sm font-semibold text-slate-700 dark:text-slate-200\">Two separate gates</h5><p class=\"text-sm text-slate-600 dark:text-slate-300\">Treating 'logged in' as sufficient proof of 'allowed to see this' is one of the most common real-world authorization mistakes, precisely because the two checks feel related but are structurally independent.</p><div class=\"mt-3 rounded-md border border-amber-300 bg-amber-50 p-2 dark:border-amber-800 dark:bg-amber-950\"><p class=\"text-xs font-semibold text-amber-700 dark:text-amber-400\">Common mistake</p><p class=\"mt-0.5 text-xs text-amber-800 dark:text-amber-300\">Writing `if (isLoggedIn)` and treating that as a complete access check.</p></div>"
    },
    {
      "type": "quiz",
      "id": "authn-vs-authz-2",
      "poolId": "authn-vs-authz-pool",
      "level": 1,
      "prompt": "In the invoice incident, which check did the endpoint perform correctly?",
      "choices": [
        "Neither check",
        "Only authorization, not authentication",
        "Authentication — there was a genuinely valid, logged-in session",
        "Both checks were performed correctly"
      ],
      "correctIndex": 2,
      "explanation": "The authentication check worked exactly as intended — the request genuinely came from a real, logged-in user. The failure was entirely in the missing authorization check.",
      "reference": "<table class=\"mt-1 w-full border-collapse text-xs\"><thead><tr><th class=\"border-b border-slate-300 pb-1 text-left dark:border-slate-600\">Check</th><th class=\"border-b border-slate-300 pb-1 text-left dark:border-slate-600\">Answers</th></tr></thead><tbody><tr><td class=\"py-1 pr-2\">Valid session?</td><td class=\"py-1\">Authentication</td></tr><tr><td class=\"py-1 pr-2\">Owns this resource?</td><td class=\"py-1\">Authorization</td></tr></tbody></table><p class=\"mt-2 text-xs text-slate-500 dark:text-slate-400\"><strong>Why:</strong> the invoice endpoint got authentication right and skipped authorization entirely.</p>",
      "learnMore": "<h5 class=\"mt-0 mb-2 text-sm font-semibold text-slate-700 dark:text-slate-200\">Two separate gates</h5><p class=\"text-sm text-slate-600 dark:text-slate-300\">Treating 'logged in' as sufficient proof of 'allowed to see this' is one of the most common real-world authorization mistakes, precisely because the two checks feel related but are structurally independent.</p><div class=\"mt-3 rounded-md border border-amber-300 bg-amber-50 p-2 dark:border-amber-800 dark:bg-amber-950\"><p class=\"text-xs font-semibold text-amber-700 dark:text-amber-400\">Common mistake</p><p class=\"mt-0.5 text-xs text-amber-800 dark:text-amber-300\">Writing `if (isLoggedIn)` and treating that as a complete access check.</p></div>"
    },
    {
      "type": "quiz",
      "id": "authn-vs-authz-3",
      "poolId": "authn-vs-authz-pool",
      "level": 1,
      "prompt": "Can a request be fully authenticated but still fail authorization?",
      "choices": [
        "No — authentication implies authorization",
        "Yes — a genuinely logged-in user can still request a resource they aren't authorized to access",
        "Only if the login credentials were stolen",
        "Only for admin accounts"
      ],
      "correctIndex": 1,
      "explanation": "The invoice incident is a direct example — full, valid authentication, complete authorization failure.",
      "reference": "<table class=\"mt-1 w-full border-collapse text-xs\"><thead><tr><th class=\"border-b border-slate-300 pb-1 text-left dark:border-slate-600\">Check</th><th class=\"border-b border-slate-300 pb-1 text-left dark:border-slate-600\">Answers</th></tr></thead><tbody><tr><td class=\"py-1 pr-2\">Valid session?</td><td class=\"py-1\">Authentication</td></tr><tr><td class=\"py-1 pr-2\">Owns this resource?</td><td class=\"py-1\">Authorization</td></tr></tbody></table><p class=\"mt-2 text-xs text-slate-500 dark:text-slate-400\"><strong>Why:</strong> the invoice endpoint got authentication right and skipped authorization entirely.</p>",
      "learnMore": "<h5 class=\"mt-0 mb-2 text-sm font-semibold text-slate-700 dark:text-slate-200\">Two separate gates</h5><p class=\"text-sm text-slate-600 dark:text-slate-300\">Treating 'logged in' as sufficient proof of 'allowed to see this' is one of the most common real-world authorization mistakes, precisely because the two checks feel related but are structurally independent.</p><div class=\"mt-3 rounded-md border border-amber-300 bg-amber-50 p-2 dark:border-amber-800 dark:bg-amber-950\"><p class=\"text-xs font-semibold text-amber-700 dark:text-amber-400\">Common mistake</p><p class=\"mt-0.5 text-xs text-amber-800 dark:text-amber-300\">Writing `if (isLoggedIn)` and treating that as a complete access check.</p></div>"
    },

    {
      "type": "quiz",
      "id": "idor-name-1",
      "poolId": "idor-name-pool",
      "level": 1,
      "prompt": "What is the name for the specific vulnerability class the invoice incident represents?",
      "choices": [
        "Cross-Site Scripting (XSS)",
        "Insecure Direct Object Reference (IDOR)",
        "SQL Injection",
        "Denial of Service"
      ],
      "correctIndex": 1,
      "explanation": "IDOR describes exactly this pattern: an object (here, an invoice) is referenced directly by an ID the client controls, with no check that the requester should have access to that specific object.",
      "reference": "<div class=\"rounded border border-slate-300 bg-slate-50 p-2 text-xs dark:border-slate-700 dark:bg-slate-900\"><p><strong>IDOR</strong> — Insecure Direct Object Reference</p><p class=\"mt-1\">Checking login status but not resource ownership.</p></div><p class=\"mt-2 text-xs text-slate-500 dark:text-slate-400\"><strong>Why:</strong> it's common enough to have a standard name because it's an easy check to accidentally omit.</p>",
      "learnMore": "<h5 class=\"mt-0 mb-2 text-sm font-semibold text-slate-700 dark:text-slate-200\">A common, well-known pattern</h5><p class=\"text-sm text-slate-600 dark:text-slate-300\">IDOR is one of the most frequently reported real-world web vulnerabilities specifically because it's easy to write an endpoint that 'obviously' requires login and never separately verify resource ownership.</p><div class=\"mt-3 rounded-md border border-amber-300 bg-amber-50 p-2 dark:border-amber-800 dark:bg-amber-950\"><p class=\"text-xs font-semibold text-amber-700 dark:text-amber-400\">Common mistake</p><p class=\"mt-0.5 text-xs text-amber-800 dark:text-amber-300\">Assuming a well-tested, working endpoint can't have this bug simply because it requires login.</p></div>"
    },
    {
      "type": "quiz",
      "id": "idor-name-2",
      "poolId": "idor-name-pool",
      "level": 1,
      "prompt": "Why is IDOR common enough to have a standard, well-known name?",
      "choices": [
        "Because it only happens in poorly written code by inexperienced developers",
        "Because it's easy to write an endpoint that requires login and never separately verify that the specific resource belongs to the requester",
        "Because it's a deliberate design choice in most systems",
        "It isn't actually common — this is a misconception"
      ],
      "correctIndex": 1,
      "explanation": "The pattern recurs across many codebases and skill levels precisely because the missing check is easy to overlook — requiring login FEELS like it should be sufficient, which is the trap.",
      "reference": "<div class=\"rounded border border-slate-300 bg-slate-50 p-2 text-xs dark:border-slate-700 dark:bg-slate-900\"><p><strong>IDOR</strong> — Insecure Direct Object Reference</p><p class=\"mt-1\">Checking login status but not resource ownership.</p></div><p class=\"mt-2 text-xs text-slate-500 dark:text-slate-400\"><strong>Why:</strong> it's common enough to have a standard name because it's an easy check to accidentally omit.</p>",
      "learnMore": "<h5 class=\"mt-0 mb-2 text-sm font-semibold text-slate-700 dark:text-slate-200\">A common, well-known pattern</h5><p class=\"text-sm text-slate-600 dark:text-slate-300\">IDOR is one of the most frequently reported real-world web vulnerabilities specifically because it's easy to write an endpoint that 'obviously' requires login and never separately verify resource ownership.</p><div class=\"mt-3 rounded-md border border-amber-300 bg-amber-50 p-2 dark:border-amber-800 dark:bg-amber-950\"><p class=\"text-xs font-semibold text-amber-700 dark:text-amber-400\">Common mistake</p><p class=\"mt-0.5 text-xs text-amber-800 dark:text-amber-300\">Assuming a well-tested, working endpoint can't have this bug simply because it requires login.</p></div>"
    },

    {
      "type": "quiz",
      "id": "authz-model-def-1",
      "poolId": "authz-model-def-pool",
      "level": 1,
      "prompt": "What is an \"authorization model\"?",
      "choices": [
        "A UML diagram of the login system",
        "A systematic answer to 'what determines whether this request should succeed' — like RBAC or ABAC",
        "A password strength requirement",
        "A synonym for encryption"
      ],
      "correctIndex": 1,
      "explanation": "An authorization model is the systematic rule set (like RBAC or ABAC) that determines access decisions — not a diagram, password policy, or encryption scheme.",
      "reference": "<div class=\"rounded border border-slate-300 bg-slate-50 p-2 text-xs dark:border-slate-700 dark:bg-slate-900\"><p><strong>RBAC</strong> — permissions attached to a role</p><p class=\"mt-1\"><strong>ABAC</strong> — permissions computed from user/resource/context attributes</p></div><p class=\"mt-2 text-xs text-slate-500 dark:text-slate-400\"><strong>Why:</strong> two common, complementary approaches to answering 'should this request succeed.'</p>",
      "learnMore": "<h5 class=\"mt-0 mb-2 text-sm font-semibold text-slate-700 dark:text-slate-200\">A systematic rule set</h5><p class=\"text-sm text-slate-600 dark:text-slate-300\">Rather than ad-hoc checks scattered through code, an authorization model gives a consistent, auditable framework for deciding access — which is exactly what the invoice endpoint lacked.</p><div class=\"mt-3 rounded-md border border-amber-300 bg-amber-50 p-2 dark:border-amber-800 dark:bg-amber-950\"><p class=\"text-xs font-semibold text-amber-700 dark:text-amber-400\">Common mistake</p><p class=\"mt-0.5 text-xs text-amber-800 dark:text-amber-300\">Writing ad-hoc, inconsistent access checks per endpoint instead of applying a consistent model.</p></div>"
    },
    {
      "type": "quiz",
      "id": "authz-model-def-2",
      "poolId": "authz-model-def-pool",
      "level": 1,
      "prompt": "What do RBAC and ABAC stand for?",
      "choices": [
        "Remote-Based Access Control and Application-Based Access Control",
        "Role-Based Access Control and Attribute-Based Access Control",
        "Resource-Based Access Control and Auth-Based Access Control",
        "Rule-Based Access Control and Account-Based Access Control"
      ],
      "correctIndex": 1,
      "explanation": "RBAC ties permissions to a role; ABAC computes access from attributes of the user, resource, and context.",
      "reference": "<div class=\"rounded border border-slate-300 bg-slate-50 p-2 text-xs dark:border-slate-700 dark:bg-slate-900\"><p><strong>RBAC</strong> — permissions attached to a role</p><p class=\"mt-1\"><strong>ABAC</strong> — permissions computed from user/resource/context attributes</p></div><p class=\"mt-2 text-xs text-slate-500 dark:text-slate-400\"><strong>Why:</strong> two common, complementary approaches to answering 'should this request succeed.'</p>",
      "learnMore": "<h5 class=\"mt-0 mb-2 text-sm font-semibold text-slate-700 dark:text-slate-200\">A systematic rule set</h5><p class=\"text-sm text-slate-600 dark:text-slate-300\">Rather than ad-hoc checks scattered through code, an authorization model gives a consistent, auditable framework for deciding access — which is exactly what the invoice endpoint lacked.</p><div class=\"mt-3 rounded-md border border-amber-300 bg-amber-50 p-2 dark:border-amber-800 dark:bg-amber-950\"><p class=\"text-xs font-semibold text-amber-700 dark:text-amber-400\">Common mistake</p><p class=\"mt-0.5 text-xs text-amber-800 dark:text-amber-300\">Writing ad-hoc, inconsistent access checks per endpoint instead of applying a consistent model.</p></div>"
    },

    {
      "type": "quiz",
      "id": "rbac-cant-express-ownership-1",
      "poolId": "rbac-cant-express-ownership-pool",
      "level": 1,
      "prompt": "Can a correctly-implemented RBAC check alone fix the invoice IDOR bug?",
      "choices": [
        "Yes — RBAC can express any authorization rule",
        "No — 'is this role allowed to view invoices' is true regardless of WHICH invoice, so RBAC alone can never express per-resource ownership",
        "Yes, but only with a very complex role hierarchy",
        "No, because RBAC doesn't apply to invoices"
      ],
      "correctIndex": 1,
      "explanation": "This is L1/L2's central technical point — RBAC answers a role-level question that's structurally incapable of expressing 'this specific resource belongs to this specific user.'",
      "reference": "<table class=\"mt-1 w-full border-collapse text-xs\"><thead><tr><th class=\"border-b border-slate-300 pb-1 text-left dark:border-slate-600\">Question</th><th class=\"border-b border-slate-300 pb-1 text-left dark:border-slate-600\">Model</th></tr></thead><tbody><tr><td class=\"py-1 pr-2\">Can a billing-viewer view invoices at all?</td><td class=\"py-1\">RBAC</td></tr><tr><td class=\"py-1 pr-2\">Can THIS billing-viewer view THIS invoice?</td><td class=\"py-1\">ABAC-style</td></tr></tbody></table><p class=\"mt-2 text-xs text-slate-500 dark:text-slate-400\"><strong>Why:</strong> roles don't carry per-resource identity — a role is the same regardless of which invoice is requested.</p>",
      "learnMore": "<h5 class=\"mt-0 mb-2 text-sm font-semibold text-slate-700 dark:text-slate-200\">Why role checks structurally can't express ownership</h5><p class=\"text-sm text-slate-600 dark:text-slate-300\">A role is a property of the user alone — it has no way to reference which specific resource is being requested, so no role-only rule can ever encode 'this resource, this user' the way an attribute comparison can.</p><div class=\"mt-3 rounded-md border border-amber-300 bg-amber-50 p-2 dark:border-amber-800 dark:bg-amber-950\"><p class=\"text-xs font-semibold text-amber-700 dark:text-amber-400\">Common mistake</p><p class=\"mt-0.5 text-xs text-amber-800 dark:text-amber-300\">Believing a fine-grained enough role hierarchy could eventually express per-resource ownership — it structurally cannot, no matter how many roles are added.</p></div>"
    },
    {
      "type": "quiz",
      "id": "rbac-cant-express-ownership-2",
      "poolId": "rbac-cant-express-ownership-pool",
      "level": 1,
      "prompt": "What kind of question does RBAC alone answer well?",
      "choices": [
        "\"Does this specific resource belong to this specific user?\"",
        "\"Is this kind of action allowed for this kind of user (role)?\"",
        "\"What is the user's IP address?\"",
        "\"How long has the user been logged in?\""
      ],
      "correctIndex": 1,
      "explanation": "RBAC is well-suited to role-level questions (can a billing-viewer view invoices at all) — it's the per-resource question that requires an ABAC-style comparison instead.",
      "reference": "<table class=\"mt-1 w-full border-collapse text-xs\"><thead><tr><th class=\"border-b border-slate-300 pb-1 text-left dark:border-slate-600\">Question</th><th class=\"border-b border-slate-300 pb-1 text-left dark:border-slate-600\">Model</th></tr></thead><tbody><tr><td class=\"py-1 pr-2\">Can a billing-viewer view invoices at all?</td><td class=\"py-1\">RBAC</td></tr><tr><td class=\"py-1 pr-2\">Can THIS billing-viewer view THIS invoice?</td><td class=\"py-1\">ABAC-style</td></tr></tbody></table><p class=\"mt-2 text-xs text-slate-500 dark:text-slate-400\"><strong>Why:</strong> roles don't carry per-resource identity — a role is the same regardless of which invoice is requested.</p>",
      "learnMore": "<h5 class=\"mt-0 mb-2 text-sm font-semibold text-slate-700 dark:text-slate-200\">Why role checks structurally can't express ownership</h5><p class=\"text-sm text-slate-600 dark:text-slate-300\">A role is a property of the user alone — it has no way to reference which specific resource is being requested, so no role-only rule can ever encode 'this resource, this user' the way an attribute comparison can.</p><div class=\"mt-3 rounded-md border border-amber-300 bg-amber-50 p-2 dark:border-amber-800 dark:bg-amber-950\"><p class=\"text-xs font-semibold text-amber-700 dark:text-amber-400\">Common mistake</p><p class=\"mt-0.5 text-xs text-amber-800 dark:text-amber-300\">Believing a fine-grained enough role hierarchy could eventually express per-resource ownership — it structurally cannot, no matter how many roles are added.</p></div>"
    },
    {
      "type": "quiz",
      "id": "rbac-cant-express-ownership-3",
      "poolId": "rbac-cant-express-ownership-pool",
      "level": 1,
      "prompt": "Why can't a role, by itself, ever reference \"which specific invoice\" is being requested?",
      "choices": [
        "It could, if roles were named more specifically",
        "A role is a property of the user, assigned independent of any specific request — it has no built-in way to reference the resource being accessed in a given request",
        "Roles are actually capable of this, contrary to what this unit claims",
        "Because invoices don't have IDs"
      ],
      "correctIndex": 1,
      "explanation": "A role is fixed to the user regardless of what they're requesting at any given moment — it structurally has no channel for referencing 'this specific invoice ID,' which is exactly why an attribute comparison (ABAC) is needed instead.",
      "reference": "<table class=\"mt-1 w-full border-collapse text-xs\"><thead><tr><th class=\"border-b border-slate-300 pb-1 text-left dark:border-slate-600\">Question</th><th class=\"border-b border-slate-300 pb-1 text-left dark:border-slate-600\">Model</th></tr></thead><tbody><tr><td class=\"py-1 pr-2\">Can a billing-viewer view invoices at all?</td><td class=\"py-1\">RBAC</td></tr><tr><td class=\"py-1 pr-2\">Can THIS billing-viewer view THIS invoice?</td><td class=\"py-1\">ABAC-style</td></tr></tbody></table><p class=\"mt-2 text-xs text-slate-500 dark:text-slate-400\"><strong>Why:</strong> roles don't carry per-resource identity — a role is the same regardless of which invoice is requested.</p>",
      "learnMore": "<h5 class=\"mt-0 mb-2 text-sm font-semibold text-slate-700 dark:text-slate-200\">Why role checks structurally can't express ownership</h5><p class=\"text-sm text-slate-600 dark:text-slate-300\">A role is a property of the user alone — it has no way to reference which specific resource is being requested, so no role-only rule can ever encode 'this resource, this user' the way an attribute comparison can.</p><div class=\"mt-3 rounded-md border border-amber-300 bg-amber-50 p-2 dark:border-amber-800 dark:bg-amber-950\"><p class=\"text-xs font-semibold text-amber-700 dark:text-amber-400\">Common mistake</p><p class=\"mt-0.5 text-xs text-amber-800 dark:text-amber-300\">Believing a fine-grained enough role hierarchy could eventually express per-resource ownership — it structurally cannot, no matter how many roles are added.</p></div>"
    },

    {
      "type": "quiz",
      "id": "practical-question-1",
      "poolId": "practical-question-pool",
      "level": 1,
      "prompt": "What's the practical question L1 recommends asking for any endpoint or action?",
      "choices": [
        "\"Is the code well-tested?\"",
        "\"Is this specific actor allowed to act on this specific resource?\" — not just whether the role is allowed to do this kind of thing",
        "\"Is the endpoint fast enough?\"",
        "\"Does the endpoint have good error messages?\""
      ],
      "correctIndex": 1,
      "explanation": "This question targets exactly the gap that role-checking alone misses — the per-resource, per-actor comparison that IDOR bugs exploit.",
      "reference": "<div class=\"rounded border border-sky-300 bg-sky-50 p-2 text-xs dark:border-sky-800 dark:bg-sky-950\"><p>\"Is this specific actor allowed to act on this specific resource?\"</p></div><p class=\"mt-2 text-xs text-slate-500 dark:text-slate-400\"><strong>Why:</strong> this is where IDOR-class bugs hide, and no amount of correct role-checking catches it on its own.</p>",
      "learnMore": "<h5 class=\"mt-0 mb-2 text-sm font-semibold text-slate-700 dark:text-slate-200\">A repeatable audit habit</h5><p class=\"text-sm text-slate-600 dark:text-slate-300\">Asking this question for every ID-accepting endpoint is a cheap, repeatable habit that catches a whole class of real-world vulnerabilities before they ship.</p><div class=\"mt-3 rounded-md border border-amber-300 bg-amber-50 p-2 dark:border-amber-800 dark:bg-amber-950\"><p class=\"text-xs font-semibold text-amber-700 dark:text-amber-400\">Common mistake</p><p class=\"mt-0.5 text-xs text-amber-800 dark:text-amber-300\">Only asking whether the role is correct, and stopping the audit there.</p></div>"
    },
    {
      "type": "quiz",
      "id": "practical-question-2",
      "poolId": "practical-question-pool",
      "level": 1,
      "prompt": "Why is \"is this role allowed to do this kind of thing\" alone not sufficient as an audit question?",
      "choices": [
        "It is sufficient — no further question is needed",
        "It misses the per-resource dimension entirely, which is exactly where IDOR bugs live",
        "It's too broad a question to be useful at all",
        "This question only applies to admin roles"
      ],
      "correctIndex": 1,
      "explanation": "Stopping at the role-level question is precisely the gap that produced the invoice bug — it needs to be paired with the resource-specific comparison.",
      "reference": "<div class=\"rounded border border-sky-300 bg-sky-50 p-2 text-xs dark:border-sky-800 dark:bg-sky-950\"><p>\"Is this specific actor allowed to act on this specific resource?\"</p></div><p class=\"mt-2 text-xs text-slate-500 dark:text-slate-400\"><strong>Why:</strong> this is where IDOR-class bugs hide, and no amount of correct role-checking catches it on its own.</p>",
      "learnMore": "<h5 class=\"mt-0 mb-2 text-sm font-semibold text-slate-700 dark:text-slate-200\">A repeatable audit habit</h5><p class=\"text-sm text-slate-600 dark:text-slate-300\">Asking this question for every ID-accepting endpoint is a cheap, repeatable habit that catches a whole class of real-world vulnerabilities before they ship.</p><div class=\"mt-3 rounded-md border border-amber-300 bg-amber-50 p-2 dark:border-amber-800 dark:bg-amber-950\"><p class=\"text-xs font-semibold text-amber-700 dark:text-amber-400\">Common mistake</p><p class=\"mt-0.5 text-xs text-amber-800 dark:text-amber-300\">Only asking whether the role is correct, and stopping the audit there.</p></div>"
    },

    {
      "type": "quiz",
      "id": "two-questions-table-1",
      "poolId": "two-questions-table-pool",
      "level": 1,
      "prompt": "Per L1's table, which row did the vulnerable invoice endpoint SKIP entirely?",
      "choices": [
        "Is there a valid, authenticated session?",
        "Does this specific resource belong to this specific actor?",
        "Is the user's password strong enough?",
        "Is the request over HTTPS?"
      ],
      "correctIndex": 1,
      "explanation": "L1's table shows the endpoint got the first two rows right and never reached the third — the ownership comparison.",
      "reference": "<table class=\"mt-1 w-full border-collapse text-xs\"><thead><tr><th class=\"border-b border-slate-300 pb-1 text-left dark:border-slate-600\">Question</th><th class=\"border-b border-slate-300 pb-1 text-left dark:border-slate-600\">Bug if skipped</th></tr></thead><tbody><tr><td class=\"py-1 pr-2\">Valid session?</td><td class=\"py-1\">Anyone can access anything</td></tr><tr><td class=\"py-1 pr-2\">Role allowed for this kind of action?</td><td class=\"py-1\">A role can do things it shouldn't</td></tr><tr><td class=\"py-1 pr-2\">Owns this specific resource?</td><td class=\"py-1\">The invoice bug</td></tr></tbody></table><p class=\"mt-2 text-xs text-slate-500 dark:text-slate-400\"><strong>Why:</strong> the endpoint stopped after the first two checks.</p>",
      "learnMore": "<h5 class=\"mt-0 mb-2 text-sm font-semibold text-slate-700 dark:text-slate-200\">Three rows, one skipped</h5><p class=\"text-sm text-slate-600 dark:text-slate-300\">Mapping a bug to a specific row in a checklist like this makes the gap concrete and auditable, rather than a vague sense that 'something about access control was wrong.'</p><div class=\"mt-3 rounded-md border border-amber-300 bg-amber-50 p-2 dark:border-amber-800 dark:bg-amber-950\"><p class=\"text-xs font-semibold text-amber-700 dark:text-amber-400\">Common mistake</p><p class=\"mt-0.5 text-xs text-amber-800 dark:text-amber-300\">Describing an authorization bug vaguely instead of identifying exactly which check in the chain was missing.</p></div>"
    },

    {
      "type": "quiz",
      "id": "three-checks-order-1",
      "poolId": "three-checks-order-pool",
      "level": 2,
      "prompt": "Per L2's flowchart, in what order does a well-designed system check authentication, role permission, and resource ownership?",
      "choices": [
        "Ownership, then role, then authentication",
        "Authentication first, then role-level (RBAC) permission, then resource-level (ABAC-style) ownership",
        "All three simultaneously with no order",
        "Role permission first, then authentication, then ownership"
      ],
      "correctIndex": 1,
      "explanation": "L2's flowchart shows this exact sequence — each check gates the next, and failing any one stops the request before the next check runs.",
      "reference": "<div class=\"rounded border border-slate-300 bg-slate-50 p-2 text-xs dark:border-slate-700 dark:bg-slate-900\"><p>1. Valid session? (401 if no)</p><p class=\"mt-1\">2. Role allowed for this action? (403 if no)</p><p class=\"mt-1\">3. Resource belongs to this actor? (403 if no)</p></div><p class=\"mt-2 text-xs text-slate-500 dark:text-slate-400\"><strong>Why:</strong> the invoice bug's gap was specifically between steps 2 and 3.</p>",
      "learnMore": "<h5 class=\"mt-0 mb-2 text-sm font-semibold text-slate-700 dark:text-slate-200\">A gated sequence</h5><p class=\"text-sm text-slate-600 dark:text-slate-300\">Each check exists specifically to catch a different failure mode — skipping any one reopens exactly that failure mode, regardless of how well the other checks are implemented.</p><div class=\"mt-3 rounded-md border border-amber-300 bg-amber-50 p-2 dark:border-amber-800 dark:bg-amber-950\"><p class=\"text-xs font-semibold text-amber-700 dark:text-amber-400\">Common mistake</p><p class=\"mt-0.5 text-xs text-amber-800 dark:text-amber-390\">Assuming the checks are redundant with each other rather than each catching a distinct class of bug.</p></div>"
    },
    {
      "type": "quiz",
      "id": "three-checks-order-2",
      "poolId": "three-checks-order-pool",
      "level": 2,
      "prompt": "Exactly where in L2's three-step chain did the invoice endpoint's bug live?",
      "choices": [
        "Between steps 1 and 2 (authentication to role check)",
        "Between steps 2 and 3 (role check to resource ownership check) — it implicitly assumed step 2 and never reached step 3",
        "At step 1 only — authentication itself was broken",
        "The bug wasn't in this chain at all"
      ],
      "correctIndex": 1,
      "explanation": "The endpoint effectively skipped straight from authentication to returning data, implicitly assuming any authenticated user was permitted, without ever performing the resource-ownership comparison.",
      "reference": "<div class=\"rounded border border-slate-300 bg-slate-50 p-2 text-xs dark:border-slate-700 dark:bg-slate-900\"><p>1. Valid session? (401 if no)</p><p class=\"mt-1\">2. Role allowed for this action? (403 if no)</p><p class=\"mt-1\">3. Resource belongs to this actor? (403 if no)</p></div><p class=\"mt-2 text-xs text-slate-500 dark:text-slate-400\"><strong>Why:</strong> the invoice bug's gap was specifically between steps 2 and 3.</p>",
      "learnMore": "<h5 class=\"mt-0 mb-2 text-sm font-semibold text-slate-700 dark:text-slate-200\">A gated sequence</h5><p class=\"text-sm text-slate-600 dark:text-slate-300\">Each check exists specifically to catch a different failure mode — skipping any one reopens exactly that failure mode, regardless of how well the other checks are implemented.</p><div class=\"mt-3 rounded-md border border-amber-300 bg-amber-50 p-2 dark:border-amber-800 dark:bg-amber-950\"><p class=\"text-xs font-semibold text-amber-700 dark:text-amber-400\">Common mistake</p><p class=\"mt-0.5 text-xs text-amber-800 dark:text-amber-390\">Assuming the checks are redundant with each other rather than each catching a distinct class of bug.</p></div>"
    },
    {
      "type": "quiz",
      "id": "three-checks-order-3",
      "poolId": "three-checks-order-pool",
      "level": 2,
      "prompt": "If step 2 (role permission) fails, does the system still proceed to check step 3 (ownership)?",
      "choices": [
        "Yes, always checks all three regardless",
        "No — per L2's flowchart, a failed step denies the request immediately (403), without needing to evaluate the next step",
        "Only for admin roles",
        "The order doesn't matter for the outcome"
      ],
      "correctIndex": 1,
      "explanation": "The flowchart shows each step short-circuits on failure — there's no need to evaluate resource ownership if the role itself isn't even permitted to perform this kind of action.",
      "reference": "<div class=\"rounded border border-slate-300 bg-slate-50 p-2 text-xs dark:border-slate-700 dark:bg-slate-900\"><p>1. Valid session? (401 if no)</p><p class=\"mt-1\">2. Role allowed for this action? (403 if no)</p><p class=\"mt-1\">3. Resource belongs to this actor? (403 if no)</p></div><p class=\"mt-2 text-xs text-slate-500 dark:text-slate-400\"><strong>Why:</strong> the invoice bug's gap was specifically between steps 2 and 3.</p>",
      "learnMore": "<h5 class=\"mt-0 mb-2 text-sm font-semibold text-slate-700 dark:text-slate-200\">A gated sequence</h5><p class=\"text-sm text-slate-600 dark:text-slate-300\">Each check exists specifically to catch a different failure mode — skipping any one reopens exactly that failure mode, regardless of how well the other checks are implemented.</p><div class=\"mt-3 rounded-md border border-amber-300 bg-amber-50 p-2 dark:border-amber-800 dark:bg-amber-950\"><p class=\"text-xs font-semibold text-amber-700 dark:text-amber-400\">Common mistake</p><p class=\"mt-0.5 text-xs text-amber-800 dark:text-amber-390\">Assuming the checks are redundant with each other rather than each catching a distinct class of bug.</p></div>"
    },

    {
      "type": "quiz",
      "id": "rbac-def-1",
      "poolId": "rbac-def-pool",
      "level": 2,
      "prompt": "In L2's RBAC example, does a 'billing-viewer' role have the 'refund-invoice' permission?",
      "choices": [
        "Yes",
        "No — only 'billing-admin' has refund-invoice in the example permission set",
        "It depends on which specific invoice",
        "RBAC doesn't define specific permissions per role"
      ],
      "correctIndex": 1,
      "explanation": "L2's example explicitly lists billing-viewer's permissions as view-invoice and view-billing-history only — refund-invoice belongs to billing-admin.",
      "reference": "<div class=\"rounded border border-slate-300 bg-slate-50 p-2 text-xs dark:border-slate-700 dark:bg-slate-900\"><p>billing-viewer: [view-invoice, view-billing-history]</p><p class=\"mt-1\">billing-admin: [view-invoice, view-billing-history, edit-invoice, refund-invoice]</p></div><p class=\"mt-2 text-xs text-slate-500 dark:text-slate-400\"><strong>Why:</strong> RBAC ties specific permissions to specific role names.</p>",
      "learnMore": "<h5 class=\"mt-0 mb-2 text-sm font-semibold text-slate-700 dark:text-slate-200\">Roles as permission buckets</h5><p class=\"text-sm text-slate-600 dark:text-slate-300\">A role is just a named collection of permissions — checking access under RBAC is checking whether the user's role's permission list includes the required permission.</p><div class=\"mt-3 rounded-md border border-amber-300 bg-amber-50 p-2 dark:border-amber-800 dark:bg-amber-950\"><p class=\"text-xs font-semibold text-amber-700 dark:text-amber-400\">Common mistake</p><p class=\"mt-0.5 text-xs text-amber-800 dark:text-amber-300\">Assuming any authenticated user with SOME role automatically has every permission a more privileged role would have.</p></div>"
    },
    {
      "type": "quiz",
      "id": "rbac-def-2",
      "poolId": "rbac-def-pool",
      "level": 2,
      "prompt": "How does an RBAC check decide whether a request is allowed?",
      "choices": [
        "By comparing the request's IP address to an allowlist",
        "By checking whether the user's assigned role's permission list includes the permission required for the requested action",
        "By checking whether the user has been active in the last 5 minutes",
        "By checking the size of the request payload"
      ],
      "correctIndex": 1,
      "explanation": "This is the core RBAC mechanism — the user's role determines their permission set, and the check verifies the required permission is in that set.",
      "reference": "<div class=\"rounded border border-slate-300 bg-slate-50 p-2 text-xs dark:border-slate-700 dark:bg-slate-900\"><p>billing-viewer: [view-invoice, view-billing-history]</p><p class=\"mt-1\">billing-admin: [view-invoice, view-billing-history, edit-invoice, refund-invoice]</p></div><p class=\"mt-2 text-xs text-slate-500 dark:text-slate-400\"><strong>Why:</strong> RBAC ties specific permissions to specific role names.</p>",
      "learnMore": "<h5 class=\"mt-0 mb-2 text-sm font-semibold text-slate-700 dark:text-slate-200\">Roles as permission buckets</h5><p class=\"text-sm text-slate-600 dark:text-slate-300\">A role is just a named collection of permissions — checking access under RBAC is checking whether the user's role's permission list includes the required permission.</p><div class=\"mt-3 rounded-md border border-amber-300 bg-amber-50 p-2 dark:border-amber-800 dark:bg-amber-950\"><p class=\"text-xs font-semibold text-amber-700 dark:text-amber-400\">Common mistake</p><p class=\"mt-0.5 text-xs text-amber-800 dark:text-amber-300\">Assuming any authenticated user with SOME role automatically has every permission a more privileged role would have.</p></div>"
    },
    {
      "type": "quiz",
      "id": "rbac-def-3",
      "poolId": "rbac-def-pool",
      "level": 2,
      "prompt": "Is RBAC, by itself, a useful and correct tool for any authorization decision?",
      "choices": [
        "No — RBAC is never useful on its own",
        "Yes, for role-level questions ('can this kind of user do this kind of thing at all') — it's only insufficient for resource-specific ownership questions",
        "Yes, and it can handle every authorization scenario without exception",
        "RBAC is only a theoretical concept, never actually implemented"
      ],
      "correctIndex": 1,
      "explanation": "RBAC is genuinely useful and correct for the questions it's designed to answer — the unit's point isn't that RBAC is flawed, but that it answers a different question than resource ownership.",
      "reference": "<div class=\"rounded border border-slate-300 bg-slate-50 p-2 text-xs dark:border-slate-700 dark:bg-slate-900\"><p>billing-viewer: [view-invoice, view-billing-history]</p><p class=\"mt-1\">billing-admin: [view-invoice, view-billing-history, edit-invoice, refund-invoice]</p></div><p class=\"mt-2 text-xs text-slate-500 dark:text-slate-400\"><strong>Why:</strong> RBAC ties specific permissions to specific role names.</p>",
      "learnMore": "<h5 class=\"mt-0 mb-2 text-sm font-semibold text-slate-700 dark:text-slate-200\">Roles as permission buckets</h5><p class=\"text-sm text-slate-600 dark:text-slate-300\">A role is just a named collection of permissions — checking access under RBAC is checking whether the user's role's permission list includes the required permission.</p><div class=\"mt-3 rounded-md border border-amber-300 bg-amber-50 p-2 dark:border-amber-800 dark:bg-amber-950\"><p class=\"text-xs font-semibold text-amber-700 dark:text-amber-400\">Common mistake</p><p class=\"mt-0.5 text-xs text-amber-800 dark:text-amber-300\">Assuming any authenticated user with SOME role automatically has every permission a more privileged role would have.</p></div>"
    },

    {
      "type": "quiz",
      "id": "abac-def-1",
      "poolId": "abac-def-pool",
      "level": 2,
      "prompt": "In L2's ABAC rule (`user.id == invoice.ownerId OR user.role == 'billing-admin'`), what two kinds of values does the comparison use?",
      "choices": [
        "Two attributes of the user only",
        "An attribute of the user and an attribute of the resource being requested",
        "Two attributes of the resource only",
        "The current server timestamp and the request size"
      ],
      "correctIndex": 1,
      "explanation": "ABAC's defining feature is comparing attributes across different entities — here, an attribute of the requesting user (`user.id`) against an attribute of the specific resource (`invoice.ownerId`).",
      "reference": "<div class=\"rounded border border-slate-300 bg-slate-50 p-2 text-xs dark:border-slate-700 dark:bg-slate-900\"><p><code>ALLOW view-invoice IF user.id == invoice.ownerId OR user.role == \"billing-admin\"</code></p></div><p class=\"mt-2 text-xs text-slate-500 dark:text-slate-400\"><strong>Why:</strong> this comparison is exactly the check the vulnerable endpoint was missing.</p>",
      "learnMore": "<h5 class=\"mt-0 mb-2 text-sm font-semibold text-slate-700 dark:text-slate-200\">Cross-entity comparison</h5><p class=\"text-sm text-slate-600 dark:text-slate-300\">ABAC's power comes specifically from comparing attributes ACROSS entities (user vs. resource), which is structurally different from RBAC's single-entity (user's role) lookup.</p><div class=\"mt-3 rounded-md border border-amber-300 bg-amber-50 p-2 dark:border-amber-800 dark:bg-amber-950\"><p class=\"text-xs font-semibold text-amber-700 dark:text-amber-400\">Common mistake</p><p class=\"mt-0.5 text-xs text-amber-800 dark:text-amber-300\">Writing an 'ABAC-style' check that only inspects user attributes, missing the resource-side comparison that makes it meaningful.</p></div>"
    },
    {
      "type": "quiz",
      "id": "abac-def-2",
      "poolId": "abac-def-pool",
      "level": 2,
      "prompt": "Per L2's ABAC rule, would a billing-admin (role='billing-admin') be allowed to view an invoice they don't own?",
      "choices": [
        "No, never",
        "Yes — the rule's OR clause explicitly grants access to any billing-admin regardless of ownership",
        "Only if they also happen to own the invoice",
        "The rule doesn't address this case"
      ],
      "correctIndex": 1,
      "explanation": "The OR clause in the ABAC rule explicitly carves out broader access for billing-admin, independent of the ownership match.",
      "reference": "<div class=\"rounded border border-slate-300 bg-slate-50 p-2 text-xs dark:border-slate-700 dark:bg-slate-900\"><p><code>ALLOW view-invoice IF user.id == invoice.ownerId OR user.role == \"billing-admin\"</code></p></div><p class=\"mt-2 text-xs text-slate-500 dark:text-slate-400\"><strong>Why:</strong> this comparison is exactly the check the vulnerable endpoint was missing.</p>",
      "learnMore": "<h5 class=\"mt-0 mb-2 text-sm font-semibold text-slate-700 dark:text-slate-200\">Cross-entity comparison</h5><p class=\"text-sm text-slate-600 dark:text-slate-300\">ABAC's power comes specifically from comparing attributes ACROSS entities (user vs. resource), which is structurally different from RBAC's single-entity (user's role) lookup.</p><div class=\"mt-3 rounded-md border border-amber-300 bg-amber-50 p-2 dark:border-amber-800 dark:bg-amber-950\"><p class=\"text-xs font-semibold text-amber-700 dark:text-amber-400\">Common mistake</p><p class=\"mt-0.5 text-xs text-amber-800 dark:text-amber-300\">Writing an 'ABAC-style' check that only inspects user attributes, missing the resource-side comparison that makes it meaningful.</p></div>"
    },

    {
      "type": "quiz",
      "id": "combine-both-1",
      "poolId": "combine-both-pool",
      "level": 2,
      "prompt": "Does fixing the invoice bug mean abandoning RBAC entirely in favor of ABAC?",
      "choices": [
        "Yes — ABAC should replace RBAC entirely",
        "No — most real systems combine both, since they answer complementary questions (role-level gating and resource-level ownership)",
        "Yes, RBAC is deprecated in modern systems",
        "No, ABAC should never be used alongside RBAC"
      ],
      "correctIndex": 1,
      "explanation": "L2 is explicit that both checks are needed together — RBAC gates whether the kind of action is possible at all, ABAC-style checks gate the specific resource.",
      "reference": "<div class=\"rounded border border-slate-300 bg-slate-50 p-2 text-xs dark:border-slate-700 dark:bg-slate-900\"><p>RBAC: is this kind of action allowed for this role?</p><p class=\"mt-1\">ABAC-style: does this resource belong to this actor?</p><p class=\"mt-1\">Both required.</p></div><p class=\"mt-2 text-xs text-slate-500 dark:text-slate-400\"><strong>Why:</strong> removing either check reopens a hole.</p>",
      "learnMore": "<h5 class=\"mt-0 mb-2 text-sm font-semibold text-slate-700 dark:text-slate-200\">Complementary, not competing</h5><p class=\"text-sm text-slate-600 dark:text-slate-300\">RBAC and ABAC aren't rival approaches to pick one over the other — they typically compose, each catching a different class of bug the other can't.</p><div class=\"mt-3 rounded-md border border-amber-300 bg-amber-50 p-2 dark:border-amber-800 dark:bg-amber-950\"><p class=\"text-xs font-semibold text-amber-700 dark:text-amber-400\">Common mistake</p><p class=\"mt-0.5 text-xs text-amber-800 dark:text-amber-300\">Treating the choice between RBAC and ABAC as either/or, rather than recognizing most systems need both.</p></div>"
    },
    {
      "type": "quiz",
      "id": "combine-both-2",
      "poolId": "combine-both-pool",
      "level": 2,
      "prompt": "What happens if the fixed invoice endpoint used ONLY the ABAC-style ownership check, with no RBAC gating at all?",
      "choices": [
        "Nothing changes — the two checks are redundant",
        "Ownership logic alone would need to re-derive role-like reasoning (e.g. is this user even a billing-capable role) inside every rule, duplicating what RBAC already handles cleanly",
        "The system would become more secure with less code",
        "ABAC alone is always sufficient regardless of role gating"
      ],
      "correctIndex": 1,
      "explanation": "L2 notes that ABAC alone would require re-deriving role-like logic inside every rule — RBAC exists precisely to handle that cleanly and separately.",
      "reference": "<div class=\"rounded border border-slate-300 bg-slate-50 p-2 text-xs dark:border-slate-700 dark:bg-slate-900\"><p>RBAC: is this kind of action allowed for this role?</p><p class=\"mt-1\">ABAC-style: does this resource belong to this actor?</p><p class=\"mt-1\">Both required.</p></div><p class=\"mt-2 text-xs text-slate-500 dark:text-slate-400\"><strong>Why:</strong> removing either check reopens a hole.</p>",
      "learnMore": "<h5 class=\"mt-0 mb-2 text-sm font-semibold text-slate-700 dark:text-slate-200\">Complementary, not competing</h5><p class=\"text-sm text-slate-600 dark:text-slate-300\">RBAC and ABAC aren't rival approaches to pick one over the other — they typically compose, each catching a different class of bug the other can't.</p><div class=\"mt-3 rounded-md border border-amber-300 bg-amber-50 p-2 dark:border-amber-800 dark:bg-amber-950\"><p class=\"text-xs font-semibold text-amber-700 dark:text-amber-400\">Common mistake</p><p class=\"mt-0.5 text-xs text-amber-800 dark:text-amber-300\">Treating the choice between RBAC and ABAC as either/or, rather than recognizing most systems need both.</p></div>"
    },

    {
      "type": "quiz",
      "id": "audit-question-1",
      "poolId": "audit-question-pool",
      "level": 2,
      "prompt": "Per L2's generalizable lesson, which endpoints are candidates for the same class of bug as the invoice incident?",
      "choices": [
        "Only endpoints that involve money",
        "Any endpoint that takes an ID from the request and uses it to fetch a specific resource, unless an explicit ownership/relationship check runs first",
        "Only endpoints used by billing-viewer role",
        "Only endpoints written by junior engineers"
      ],
      "correctIndex": 1,
      "explanation": "L2 generalizes beyond invoices specifically — any ID-accepting, resource-fetching endpoint is a candidate unless it explicitly checks the relationship between requester and resource.",
      "reference": "<div class=\"rounded border border-slate-300 bg-slate-50 p-2 text-xs dark:border-slate-700 dark:bg-slate-900\"><p>\"For every endpoint that accepts a resource identifier, is there a line of code comparing an attribute of the resource against an attribute of the requester?\"</p></div><p class=\"mt-2 text-xs text-slate-500 dark:text-slate-400\"><strong>Why:</strong> this audit question generalizes the invoice bug to any similar endpoint.</p>",
      "learnMore": "<h5 class=\"mt-0 mb-2 text-sm font-semibold text-slate-700 dark:text-slate-200\">A pattern, not an isolated bug</h5><p class=\"text-sm text-slate-600 dark:text-slate-300\">Fixing only the reported endpoint and not auditing similar ones leaves the same vulnerability class present elsewhere in the codebase — the real fix is a systematic check across every ID-accepting endpoint.</p><div class=\"mt-3 rounded-md border border-amber-300 bg-amber-50 p-2 dark:border-amber-800 dark:bg-amber-950\"><p class=\"text-xs font-semibold text-amber-700 dark:text-amber-400\">Common mistake</p><p class=\"mt-0.5 text-xs text-amber-800 dark:text-amber-390\">Fixing only the one reported endpoint without auditing structurally similar endpoints for the same missing check.</p></div>"
    },
    {
      "type": "quiz",
      "id": "audit-question-2",
      "poolId": "audit-question-pool",
      "level": 2,
      "prompt": "What's the practical audit question L2 proposes for finding IDOR-class bugs across a codebase?",
      "choices": [
        "\"Does this endpoint use HTTPS?\"",
        "\"For every endpoint accepting a resource identifier, is there a line comparing an attribute of the resource against an attribute of the requester?\"",
        "\"How many lines of code does this endpoint have?\"",
        "\"Is this endpoint documented?\""
      ],
      "correctIndex": 1,
      "explanation": "This is L2's stated audit question — a concrete, repeatable check for the exact class of missing comparison that caused the invoice bug.",
      "reference": "<div class=\"rounded border border-slate-300 bg-slate-50 p-2 text-xs dark:border-slate-700 dark:bg-slate-900\"><p>\"For every endpoint that accepts a resource identifier, is there a line of code comparing an attribute of the resource against an attribute of the requester?\"</p></div><p class=\"mt-2 text-xs text-slate-500 dark:text-slate-400\"><strong>Why:</strong> this audit question generalizes the invoice bug to any similar endpoint.</p>",
      "learnMore": "<h5 class=\"mt-0 mb-2 text-sm font-semibold text-slate-700 dark:text-slate-200\">A pattern, not an isolated bug</h5><p class=\"text-sm text-slate-600 dark:text-slate-300\">Fixing only the reported endpoint and not auditing similar ones leaves the same vulnerability class present elsewhere in the codebase — the real fix is a systematic check across every ID-accepting endpoint.</p><div class=\"mt-3 rounded-md border border-amber-300 bg-amber-50 p-2 dark:border-amber-800 dark:bg-amber-950\"><p class=\"text-xs font-semibold text-amber-700 dark:text-amber-400\">Common mistake</p><p class=\"mt-0.5 text-xs text-amber-800 dark:text-amber-390\">Fixing only the one reported endpoint without auditing structurally similar endpoints for the same missing check.</p></div>"
    },

    {
      "type": "quiz",
      "id": "generalizable-1",
      "poolId": "generalizable-pool",
      "level": 2,
      "prompt": "Is the invoice endpoint's bug likely to be a one-off, isolated mistake in an otherwise-safe codebase?",
      "choices": [
        "Yes, almost certainly isolated to that one endpoint",
        "Not necessarily — any similarly-shaped endpoint (accepting an ID, fetching a resource) is a candidate for the same missing check unless verified otherwise",
        "No — this bug class can only ever occur once per codebase",
        "The scenario doesn't allow this question to be answered"
      ],
      "correctIndex": 1,
      "explanation": "L2's whole final point is that this is a pattern worth auditing for broadly, not an isolated incident to patch and forget.",
      "reference": "<div class=\"rounded border border-slate-300 bg-slate-50 p-2 text-xs dark:border-slate-700 dark:bg-slate-900\"><p>\"For every endpoint that accepts a resource identifier, is there a line of code comparing an attribute of the resource against an attribute of the requester?\"</p></div><p class=\"mt-2 text-xs text-slate-500 dark:text-slate-400\"><strong>Why:</strong> this audit question generalizes the invoice bug to any similar endpoint.</p>",
      "learnMore": "<h5 class=\"mt-0 mb-2 text-sm font-semibold text-slate-700 dark:text-slate-200\">A pattern, not an isolated bug</h5><p class=\"text-sm text-slate-600 dark:text-slate-300\">Fixing only the reported endpoint and not auditing similar ones leaves the same vulnerability class present elsewhere in the codebase — the real fix is a systematic check across every ID-accepting endpoint.</p><div class=\"mt-3 rounded-md border border-amber-300 bg-amber-50 p-2 dark:border-amber-800 dark:bg-amber-950\"><p class=\"text-xs font-semibold text-amber-700 dark:text-amber-400\">Common mistake</p><p class=\"mt-0.5 text-xs text-amber-800 dark:text-amber-390\">Fixing only the one reported endpoint without auditing structurally similar endpoints for the same missing check.</p></div>"
    },

    {
      "type": "code",
      "id": "getInvoiceFixed-1",
      "poolId": "getInvoiceFixed-pool",
      "level": 3,
      "prompt": "Implement `getInvoiceFixed(currentUserId, invoiceId, users, invoices)` — throw `'401 Unauthorized'` if the user doesn't exist, `'404 Not Found'` if the invoice doesn't exist, `'403 Forbidden'` if the invoice's `ownerId` doesn't match the user's `id` AND the user's role isn't `'billing-admin'`. Otherwise return the invoice.",
      "starterCode": "function getInvoiceFixed(currentUserId, invoiceId, users, invoices) {\n  // TODO\n}\n",
      "tests": [
        {
          "description": "owner can access their own invoice",
          "expr": "expect(getInvoiceFixed(7, 48212, {7:{id:7,role:'billing-viewer'}}, {48212:{id:48212,ownerId:7}})).toEqual({id:48212,ownerId:7})"
        },
        {
          "description": "non-owner is forbidden",
          "expr": "expect(() => getInvoiceFixed(42, 48212, {42:{id:42,role:'billing-viewer'},7:{id:7,role:'billing-viewer'}}, {48212:{id:48212,ownerId:7}})).toThrow()"
        },
        {
          "description": "billing-admin can access any invoice",
          "expr": "expect(getInvoiceFixed(1, 48212, {1:{id:1,role:'billing-admin'}}, {48212:{id:48212,ownerId:7}})).toEqual({id:48212,ownerId:7})"
        }
      ],
      "solution": "function getInvoiceFixed(currentUserId, invoiceId, users, invoices) {\n  const user = users[currentUserId];\n  if (!user) throw new Error('401 Unauthorized');\n  const invoice = invoices[invoiceId];\n  if (!invoice) throw new Error('404 Not Found');\n  const isOwner = invoice.ownerId === user.id;\n  const isAdmin = user.role === 'billing-admin';\n  if (!isOwner && !isAdmin) throw new Error('403 Forbidden');\n  return invoice;\n}",
      "explanation": "This is the exact fix from L3 — a single added ownership/admin check that reproduces the correct 403 behavior for the IDOR incident while still allowing legitimate owner and admin access."
    },
    {
      "type": "code",
      "id": "getInvoiceFixed-2",
      "poolId": "getInvoiceFixed-pool",
      "level": 3,
      "prompt": "Implement `getInvoiceFixed(currentUserId, invoiceId, users, invoices)` (same rules). Verify it throws `404 Not Found` before ever reaching the ownership check, for a nonexistent invoice.",
      "starterCode": "function getInvoiceFixed(currentUserId, invoiceId, users, invoices) {\n  // TODO\n}\n",
      "tests": [
        {
          "description": "nonexistent invoice throws 404, regardless of user",
          "expr": "expect(() => getInvoiceFixed(7, 99999, {7:{id:7,role:'billing-viewer'}}, {48212:{id:48212,ownerId:7}})).toThrow()"
        },
        {
          "description": "nonexistent user throws before even checking the invoice",
          "expr": "expect(() => getInvoiceFixed(999, 48212, {7:{id:7,role:'billing-viewer'}}, {48212:{id:48212,ownerId:7}})).toThrow()"
        }
      ],
      "solution": "function getInvoiceFixed(currentUserId, invoiceId, users, invoices) {\n  const user = users[currentUserId];\n  if (!user) throw new Error('401 Unauthorized');\n  const invoice = invoices[invoiceId];\n  if (!invoice) throw new Error('404 Not Found');\n  const isOwner = invoice.ownerId === user.id;\n  const isAdmin = user.role === 'billing-admin';\n  if (!isOwner && !isAdmin) throw new Error('403 Forbidden');\n  return invoice;\n}",
      "explanation": "This confirms the check ordering from L2's flowchart — authentication, then resource existence, then ownership — each guard runs before the next, matching the gated sequence the unit describes."
    },
    {
      "type": "code",
      "id": "getInvoiceFixed-3",
      "poolId": "getInvoiceFixed-pool",
      "level": 3,
      "prompt": "Implement `getInvoiceFixed(currentUserId, invoiceId, users, invoices)` (same rules). Confirm it exactly reproduces L1's incident: user 42 (billing-viewer) requesting invoice 48212 owned by user 7 must be forbidden.",
      "starterCode": "function getInvoiceFixed(currentUserId, invoiceId, users, invoices) {\n  // TODO\n}\n",
      "tests": [
        {
          "description": "reproduces the exact IDOR incident being blocked",
          "expr": "expect(() => getInvoiceFixed(42, 48212, {42:{id:42,role:'billing-viewer'}}, {48212:{id:48212,ownerId:7}})).toThrow()"
        },
        {
          "description": "user 42 CAN access their own invoice 48213",
          "expr": "expect(getInvoiceFixed(42, 48213, {42:{id:42,role:'billing-viewer'}}, {48213:{id:48213,ownerId:42}})).toEqual({id:48213,ownerId:42})"
        }
      ],
      "solution": "function getInvoiceFixed(currentUserId, invoiceId, users, invoices) {\n  const user = users[currentUserId];\n  if (!user) throw new Error('401 Unauthorized');\n  const invoice = invoices[invoiceId];\n  if (!invoice) throw new Error('404 Not Found');\n  const isOwner = invoice.ownerId === user.id;\n  const isAdmin = user.role === 'billing-admin';\n  if (!isOwner && !isAdmin) throw new Error('403 Forbidden');\n  return invoice;\n}",
      "explanation": "Reproducing the exact incident numbers from L1/L3 and confirming the fix blocks it — while still allowing the user's own legitimate invoice — verifies the fix addresses the reported bug without breaking normal access."
    },

    {
      "type": "code",
      "id": "rbacAllows-1",
      "poolId": "rbacAllows-pool",
      "level": 3,
      "prompt": "Implement `rbacAllows(role, permission)` using this permission map: `'billing-viewer'` → `['view-invoice', 'view-billing-history']`, `'billing-admin'` → `['view-invoice', 'view-billing-history', 'edit-invoice', 'refund-invoice']`. Return whether the role's list includes the permission.",
      "starterCode": "function rbacAllows(role, permission) {\n  // TODO\n}\n",
      "tests": [
        {
          "description": "billing-viewer can view-invoice",
          "expr": "expect(rbacAllows('billing-viewer', 'view-invoice')).toBe(true)"
        },
        {
          "description": "billing-viewer cannot refund-invoice",
          "expr": "expect(rbacAllows('billing-viewer', 'refund-invoice')).toBe(false)"
        },
        {
          "description": "billing-admin can refund-invoice",
          "expr": "expect(rbacAllows('billing-admin', 'refund-invoice')).toBe(true)"
        },
        {
          "description": "unknown role has no permissions",
          "expr": "expect(rbacAllows('guest', 'view-invoice')).toBe(false)"
        }
      ],
      "solution": "function rbacAllows(role, permission) {\n  const ROLE_PERMISSIONS = {\n    'billing-viewer': ['view-invoice', 'view-billing-history'],\n    'billing-admin': ['view-invoice', 'view-billing-history', 'edit-invoice', 'refund-invoice'],\n  };\n  return (ROLE_PERMISSIONS[role] || []).includes(permission);\n}",
      "explanation": "This is L3's role-level check — it correctly answers 'is this KIND of action allowed for this role,' the question RBAC is well-suited for, matching the unit's permission table exactly."
    },
    {
      "type": "code",
      "id": "rbacAllows-2",
      "poolId": "rbacAllows-pool",
      "level": 3,
      "prompt": "Implement `rbacAllows(role, permission)` (same permission map). Verify it returns the same `true` result for `view-invoice` regardless of which specific invoice would be involved — demonstrating RBAC's role-level-only scope.",
      "starterCode": "function rbacAllows(role, permission) {\n  // TODO\n}\n",
      "tests": [
        {
          "description": "billing-viewer allowed for view-invoice, with no invoice ID input at all",
          "expr": "expect(rbacAllows('billing-viewer', 'view-invoice')).toBe(true)"
        },
        {
          "description": "billing-viewer can view-billing-history",
          "expr": "expect(rbacAllows('billing-viewer', 'view-billing-history')).toBe(true)"
        }
      ],
      "solution": "function rbacAllows(role, permission) {\n  const ROLE_PERMISSIONS = {\n    'billing-viewer': ['view-invoice', 'view-billing-history'],\n    'billing-admin': ['view-invoice', 'view-billing-history', 'edit-invoice', 'refund-invoice'],\n  };\n  return (ROLE_PERMISSIONS[role] || []).includes(permission);\n}",
      "explanation": "Notice this function's signature never takes an invoice ID at all — it structurally can't express per-resource ownership, exactly demonstrating L2's point about RBAC's scope."
    },

    {
      "type": "code",
      "id": "rbacAllows-3",
      "poolId": "rbacAllows-pool",
      "level": 3,
      "prompt": "Implement `rbacAllows(role, permission)` (same permission map). Verify `edit-invoice` is granted to `billing-admin` but not to `billing-viewer`.",
      "starterCode": "function rbacAllows(role, permission) {\n  // TODO\n}\n",
      "tests": [
        {
          "description": "billing-admin can edit-invoice",
          "expr": "expect(rbacAllows('billing-admin', 'edit-invoice')).toBe(true)"
        },
        {
          "description": "billing-viewer cannot edit-invoice",
          "expr": "expect(rbacAllows('billing-viewer', 'edit-invoice')).toBe(false)"
        }
      ],
      "solution": "function rbacAllows(role, permission) {\n  const ROLE_PERMISSIONS = {\n    'billing-viewer': ['view-invoice', 'view-billing-history'],\n    'billing-admin': ['view-invoice', 'view-billing-history', 'edit-invoice', 'refund-invoice'],\n  };\n  return (ROLE_PERMISSIONS[role] || []).includes(permission);\n}",
      "explanation": "This is the same role-permission lookup, exercised against a different permission — reinforcing that RBAC's whole decision comes from the role's fixed permission list, nothing else."
    },

    {
      "type": "code",
      "id": "abacAllowsViewInvoice-1",
      "poolId": "abacAllowsViewInvoice-pool",
      "level": 3,
      "prompt": "Implement `abacAllowsViewInvoice(user, invoice)` — return `true` if `invoice.ownerId === user.id`, OR if `user.role === 'billing-admin'`. Otherwise `false`.",
      "starterCode": "function abacAllowsViewInvoice(user, invoice) {\n  // TODO\n}\n",
      "tests": [
        {
          "description": "non-owner, non-admin is denied",
          "expr": "expect(abacAllowsViewInvoice({id:42, role:'billing-viewer'}, {ownerId:7})).toBe(false)"
        },
        {
          "description": "owner is allowed",
          "expr": "expect(abacAllowsViewInvoice({id:7, role:'billing-viewer'}, {ownerId:7})).toBe(true)"
        },
        {
          "description": "admin is allowed regardless of ownership",
          "expr": "expect(abacAllowsViewInvoice({id:99, role:'billing-admin'}, {ownerId:7})).toBe(true)"
        }
      ],
      "solution": "function abacAllowsViewInvoice(user, invoice) {\n  return invoice.ownerId === user.id || user.role === 'billing-admin';\n}",
      "explanation": "This is L2/L3's exact ABAC rule — comparing an attribute of the user against an attribute of the resource, plus the admin carve-out, exactly matching the incident's fix."
    },
    {
      "type": "code",
      "id": "abacAllowsViewInvoice-2",
      "poolId": "abacAllowsViewInvoice-pool",
      "level": 3,
      "prompt": "Implement `abacAllowsViewInvoice(user, invoice)` (same rules). Verify it correctly reproduces the incident: it must deny user 42 viewing user 7's invoice, and allow user 7 viewing their own.",
      "starterCode": "function abacAllowsViewInvoice(user, invoice) {\n  // TODO\n}\n",
      "tests": [
        {
          "description": "denies the exact IDOR incident",
          "expr": "expect(abacAllowsViewInvoice({id:42, role:'billing-viewer'}, {ownerId:7})).toBe(false)"
        },
        {
          "description": "allows the legitimate owner",
          "expr": "expect(abacAllowsViewInvoice({id:7, role:'billing-viewer'}, {ownerId:7})).toBe(true)"
        }
      ],
      "solution": "function abacAllowsViewInvoice(user, invoice) {\n  return invoice.ownerId === user.id || user.role === 'billing-admin';\n}",
      "explanation": "Reproducing L1's exact incident with this function confirms it's the precise missing check that would have prevented the vulnerability from ever shipping."
    },
    {
      "type": "code",
      "id": "abacAllowsViewInvoice-3",
      "poolId": "abacAllowsViewInvoice-pool",
      "level": 3,
      "prompt": "Implement `abacAllowsViewInvoice(user, invoice)` (same rules). Confirm the function handles the case where `user.id` and `invoice.ownerId` are of different but comparable values correctly (e.g. both numbers).",
      "starterCode": "function abacAllowsViewInvoice(user, invoice) {\n  // TODO\n}\n",
      "tests": [
        {
          "description": "numeric ids compare correctly when equal",
          "expr": "expect(abacAllowsViewInvoice({id:100, role:'billing-viewer'}, {ownerId:100})).toBe(true)"
        },
        {
          "description": "numeric ids compare correctly when different",
          "expr": "expect(abacAllowsViewInvoice({id:100, role:'billing-viewer'}, {ownerId:101})).toBe(false)"
        }
      ],
      "solution": "function abacAllowsViewInvoice(user, invoice) {\n  return invoice.ownerId === user.id || user.role === 'billing-admin';\n}",
      "explanation": "Strict equality between the two numeric IDs is exactly the comparison this check needs — this is the simplest possible correct implementation of the ownership rule."
    },

    {
      "type": "code",
      "id": "combined-check-1",
      "poolId": "combined-check-pool",
      "level": 3,
      "prompt": "Implement `canAccessInvoice(user, invoice)` by composing `rbacAllows` and `abacAllowsViewInvoice` (both provided) — access requires BOTH the role permission for 'view-invoice' AND the ownership/admin check to pass.",
      "starterCode": "function rbacAllows(role, permission) {\n  const ROLE_PERMISSIONS = {\n    'billing-viewer': ['view-invoice', 'view-billing-history'],\n    'billing-admin': ['view-invoice', 'view-billing-history', 'edit-invoice', 'refund-invoice'],\n  };\n  return (ROLE_PERMISSIONS[role] || []).includes(permission);\n}\nfunction abacAllowsViewInvoice(user, invoice) {\n  return invoice.ownerId === user.id || user.role === 'billing-admin';\n}\nfunction canAccessInvoice(user, invoice) {\n  // TODO: use both functions above\n}\n",
      "tests": [
        {
          "description": "owner with correct role is allowed",
          "expr": "expect(canAccessInvoice({id:7, role:'billing-viewer'}, {ownerId:7})).toBe(true)"
        },
        {
          "description": "non-owner is denied even with the right role",
          "expr": "expect(canAccessInvoice({id:42, role:'billing-viewer'}, {ownerId:7})).toBe(false)"
        },
        {
          "description": "role with no view-invoice permission is denied even if somehow the owner",
          "expr": "expect(canAccessInvoice({id:7, role:'guest'}, {ownerId:7})).toBe(false)"
        }
      ],
      "solution": "function rbacAllows(role, permission) {\n  const ROLE_PERMISSIONS = {\n    'billing-viewer': ['view-invoice', 'view-billing-history'],\n    'billing-admin': ['view-invoice', 'view-billing-history', 'edit-invoice', 'refund-invoice'],\n  };\n  return (ROLE_PERMISSIONS[role] || []).includes(permission);\n}\nfunction abacAllowsViewInvoice(user, invoice) {\n  return invoice.ownerId === user.id || user.role === 'billing-admin';\n}\nfunction canAccessInvoice(user, invoice) {\n  return rbacAllows(user.role, 'view-invoice') && abacAllowsViewInvoice(user, invoice);\n}",
      "explanation": "This is L2's 'combine both' diagram made concrete — requiring both checks to pass together, catching both a wrong role AND a wrong resource, matching exactly the composed defense the unit describes."
    },
    {
      "type": "code",
      "id": "combined-check-2",
      "poolId": "combined-check-pool",
      "level": 3,
      "prompt": "Implement `canAccessInvoice(user, invoice)` (same composition). Verify it correctly denies access for a role with no invoice-viewing permission at all, even though they happen to be the owner (edge case demonstrating why RBAC alone isn't skippable).",
      "starterCode": "function rbacAllows(role, permission) {\n  const ROLE_PERMISSIONS = {\n    'billing-viewer': ['view-invoice', 'view-billing-history'],\n    'billing-admin': ['view-invoice', 'view-billing-history', 'edit-invoice', 'refund-invoice'],\n  };\n  return (ROLE_PERMISSIONS[role] || []).includes(permission);\n}\nfunction abacAllowsViewInvoice(user, invoice) {\n  return invoice.ownerId === user.id || user.role === 'billing-admin';\n}\nfunction canAccessInvoice(user, invoice) {\n  // TODO\n}\n",
      "tests": [
        {
          "description": "suspended/guest role denied despite ownership",
          "expr": "expect(canAccessInvoice({id:50, role:'suspended'}, {ownerId:50})).toBe(false)"
        },
        {
          "description": "billing-admin with no ownership still allowed",
          "expr": "expect(canAccessInvoice({id:1, role:'billing-admin'}, {ownerId:999})).toBe(true)"
        }
      ],
      "solution": "function rbacAllows(role, permission) {\n  const ROLE_PERMISSIONS = {\n    'billing-viewer': ['view-invoice', 'view-billing-history'],\n    'billing-admin': ['view-invoice', 'view-billing-history', 'edit-invoice', 'refund-invoice'],\n  };\n  return (ROLE_PERMISSIONS[role] || []).includes(permission);\n}\nfunction abacAllowsViewInvoice(user, invoice) {\n  return invoice.ownerId === user.id || user.role === 'billing-admin';\n}\nfunction canAccessInvoice(user, invoice) {\n  return rbacAllows(user.role, 'view-invoice') && abacAllowsViewInvoice(user, invoice);\n}",
      "explanation": "A suspended user owning their old invoice is still correctly denied because RBAC gates them out first — demonstrating why ABAC alone (ownership only) isn't sufficient either, exactly as L2 explains."
    },

    {
      "type": "code",
      "id": "team-based-abac-1",
      "poolId": "team-based-abac-pool",
      "level": 3,
      "prompt": "Implement `abacAllowsViewTeamInvoice(user, invoice, userTeamIds)` from L3's extension — return `true` if `userTeamIds` includes `invoice.teamId`, OR if `user.role === 'billing-admin'`.",
      "starterCode": "function abacAllowsViewTeamInvoice(user, invoice, userTeamIds) {\n  // TODO\n}\n",
      "tests": [
        {
          "description": "team member is allowed",
          "expr": "expect(abacAllowsViewTeamInvoice({id:42, role:'billing-viewer'}, {teamId:'team-9'}, ['team-9','team-12'])).toBe(true)"
        },
        {
          "description": "non-team-member is denied",
          "expr": "expect(abacAllowsViewTeamInvoice({id:42, role:'billing-viewer'}, {teamId:'team-99'}, ['team-9','team-12'])).toBe(false)"
        },
        {
          "description": "admin allowed regardless of team",
          "expr": "expect(abacAllowsViewTeamInvoice({id:1, role:'billing-admin'}, {teamId:'team-99'}, [])).toBe(true)"
        }
      ],
      "solution": "function abacAllowsViewTeamInvoice(user, invoice, userTeamIds) {\n  return userTeamIds.includes(invoice.teamId) || user.role === 'billing-admin';\n}",
      "explanation": "This generalizes the single-owner ABAC pattern to a membership check — same attribute-of-user-vs-attribute-of-resource shape, just comparing team membership instead of a single ID match, exactly as L3's extension describes."
    },
    {
      "type": "code",
      "id": "team-based-abac-2",
      "poolId": "team-based-abac-pool",
      "level": 3,
      "prompt": "Implement `abacAllowsViewTeamInvoice(user, invoice, userTeamIds)` (same rules). Verify a user on MULTIPLE teams is granted access if ANY of their teams matches the invoice's team.",
      "starterCode": "function abacAllowsViewTeamInvoice(user, invoice, userTeamIds) {\n  // TODO\n}\n",
      "tests": [
        {
          "description": "user on 3 teams, invoice matches the 2nd",
          "expr": "expect(abacAllowsViewTeamInvoice({id:5, role:'billing-viewer'}, {teamId:'team-b'}, ['team-a','team-b','team-c'])).toBe(true)"
        },
        {
          "description": "empty team list denies non-admin",
          "expr": "expect(abacAllowsViewTeamInvoice({id:5, role:'billing-viewer'}, {teamId:'team-a'}, [])).toBe(false)"
        }
      ],
      "solution": "function abacAllowsViewTeamInvoice(user, invoice, userTeamIds) {\n  return userTeamIds.includes(invoice.teamId) || user.role === 'billing-admin';\n}",
      "explanation": "Array membership (`.includes`) naturally handles the multi-team case — the user is granted access if any single team membership matches, without needing special-case logic for how many teams they belong to."
    },
    {
      "type": "code",
      "id": "team-based-abac-3",
      "poolId": "team-based-abac-pool",
      "level": 3,
      "prompt": "Implement `abacAllowsViewTeamInvoice(user, invoice, userTeamIds)` (same rules). Verify a billing-admin is allowed even with an empty `userTeamIds` list, while a non-admin with an empty list is denied.",
      "starterCode": "function abacAllowsViewTeamInvoice(user, invoice, userTeamIds) {\n  // TODO\n}\n",
      "tests": [
        {
          "description": "admin allowed with no team memberships at all",
          "expr": "expect(abacAllowsViewTeamInvoice({id:1, role:'billing-admin'}, {teamId:'team-z'}, [])).toBe(true)"
        },
        {
          "description": "non-admin with no teams is denied",
          "expr": "expect(abacAllowsViewTeamInvoice({id:2, role:'billing-viewer'}, {teamId:'team-z'}, [])).toBe(false)"
        }
      ],
      "solution": "function abacAllowsViewTeamInvoice(user, invoice, userTeamIds) {\n  return userTeamIds.includes(invoice.teamId) || user.role === 'billing-admin';\n}",
      "explanation": "The admin carve-out (OR clause) is independent of team membership entirely — this confirms the two conditions are evaluated separately, not as a combined single check."
    }
  ]
}
