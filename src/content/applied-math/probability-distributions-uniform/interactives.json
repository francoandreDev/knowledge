{
  "items": [
    {
      "id": "alert-threshold-rarity",
      "level": 3,
      "title": "How rare does a failure spike have to be before it's worth paging someone?",
      "description": "Using the exact binomial math from L3 (500 requests, 2% independent failure rate per request), drag the alert threshold and watch how quickly the probability of hitting it by pure chance collapses. At a threshold of 20, this reproduces L3's verified result of about 0.31% (roughly 1 in 320).",
      "params": [
        {
          "name": "threshold",
          "label": "Alert threshold (failure count)",
          "min": 5,
          "max": 35,
          "step": 1,
          "default": 20,
          "unit": ""
        }
      ],
      "compute": "function logFactorial(x) { let s = 0; for (let i = 2; i <= x; i++) s += Math.log(i); return s; } function binomPMF(n, p, k) { const logC = logFactorial(n) - logFactorial(k) - logFactorial(n - k); return Math.exp(logC + k * Math.log(p) + (n - k) * Math.log(1 - p)); } const n = 500; const p = 0.02; let sum = 0; for (let i = params.threshold; i <= n; i++) sum += binomPMF(n, p, i); return { chancePercent: sum * 100 };",
      "outputs": [
        {
          "key": "chancePercent",
          "label": "Chance of hitting the threshold by pure chance",
          "unit": "%",
          "color": "#f472b6"
        }
      ],
      "chartParam": "threshold"
    }
  ]
}
