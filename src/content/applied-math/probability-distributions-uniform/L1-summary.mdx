---
title: "L1 — How do I model something that involves chance? (probability distributions — uniform, binomial, normal — and where each shows up in systems)"
---

import Scenario from "../../../components/Scenario.astro";

<Scenario label="Server 3 got 32% of the traffic. Is the load balancer broken?">
  <Fragment slot="facts">
    <div class="not-prose flex flex-col gap-2 text-sm text-slate-700 dark:text-slate-300">
      <div class="flex items-center gap-1.5"><span>⚖️</span> <strong>The setup</strong> — 4 backend servers, picked uniformly at random per request</div>
      <div class="flex items-center gap-1.5"><span>📊</span> <strong>Expected</strong> — 25% of traffic per server, on average</div>
      <div class="flex items-center gap-1.5"><span>🚨</span> <strong>Observed</strong> — server 3 got 64 of the last 200 requests (32%)</div>
    </div>
  </Fragment>

A load balancer is configured to pick 1 of 4 backend servers uniformly
at random for every incoming request — each server should get about a
quarter of the traffic. An on-call engineer pulls up the dashboard
after 200 requests and sees server 3 handled 64 of them: 32%, not the
expected 25%. Is the load balancer's random selection broken, or is
this exactly what "random" looks like at this sample size?
**Without a way to say how much spread is normal for a fair coin flip
repeated 200 times, there's no way to answer that question — just a
gut feeling.**

</Scenario>

## The shape of the problem

- "Random" doesn't mean "perfectly even" — even a truly fair process
  produces uneven-looking results over a finite number of trials. The
  question isn't "is it uneven?" but "is it uneven by more than chance
  alone would produce?"
- Different kinds of chance-driven questions need different tools: how
  a single random pick lands (uniform), how many times something
  happens across many independent trials (binomial), and what a sum of
  many small independent effects tends to look like (normal, via the
  Central Limit Theorem).
- Each distribution comes with a **mean** (the expected center) and a
  **standard deviation** (how much spread is normal around that
  center) — and it's the standard deviation, not a gut feeling, that
  answers "is this reading suspicious?"

## Key terms

- **Probability distribution** — a description of how likely each
  possible outcome of a random process is, letting "how much variation
  is normal" be answered with a number instead of a guess.
- **Uniform distribution** — every outcome in a fixed set is equally
  likely (e.g. picking 1 of 4 servers with no bias toward any one of
  them).
- **Binomial distribution** — the count of "successes" across a fixed
  number of independent yes/no trials, each with the same probability
  (e.g. how many of the next 500 requests fail, if each has a 2%
  independent chance of failing).
- **Normal distribution** — the bell-curve shape that a sum of many
  independent, small random effects tends toward, regardless of what
  each individual effect looks like — this tendency is called the
  **Central Limit Theorem**.
- **Standard deviation** — a number describing how much a random
  quantity typically varies around its mean; a result several standard
  deviations away from the mean is a genuine outlier, not normal
  spread.

## What this unit covers

L2 works through what each distribution actually looks like, how its
mean and spread are calculated, and which kind of systems question
each one answers. L3 implements all three in real code — simulating
the load balancer scenario above, computing a binomial alerting
threshold for a flaky dependency, and demonstrating the Central Limit
Theorem by summing simulated network-hop delays into a realistic
latency distribution.
