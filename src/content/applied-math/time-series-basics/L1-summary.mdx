---
title: "L1 — How do I forecast a trend from historical data? (time series basics, extrapolation, regression fundamentals)"
---

import Scenario from "../../../components/Scenario.astro";

<Scenario label="Signups jumped from 80 to 120 in a week — a product manager projected that straight line out to 1,720 signups a week by year end.">
  <Fragment slot="facts">
    <div class="not-prose flex flex-col gap-2 text-sm text-slate-700 dark:text-slate-300">
      <div class="flex items-center gap-1.5"><span>📅</span> <strong>Week 11</strong> — 80 signups</div>
      <div class="flex items-center gap-1.5"><span>🚀</span> <strong>Week 12</strong> — 120 signups (a post about the product went viral)</div>
      <div class="flex items-center gap-1.5"><span>📈</span> <strong>The pitch</strong> — "we're gaining 40 signups a week; by week 52 that's 1,720 a week"</div>
    </div>
  </Fragment>

A product manager pulls up last week's signup count (120) and the
week before (80), computes the difference (40 more signups per
week), and multiplies that out to the end of the year: "at this rate
we'll be adding 1,720 signups a week by December." The board loves
it. **The forecast used exactly two numbers out of twelve weeks of
data the company actually has. Is a trend computed from two points
the same thing as a trend, or is something being ignored?**

</Scenario>

## The shape of the problem

- A **time series** is a sequence of measurements taken at regular
  intervals (daily, weekly, monthly) — signups per week, server load
  per hour, revenue per month.
- Any time series is a mix of **trend** (the underlying direction
  things are actually moving) and **noise** (random fluctuation that
  doesn't reflect a real change in direction — a single viral post, a
  slow news day, a holiday). Reading a two-point jump as pure trend
  ignores the possibility that some or all of it is noise.
- **Extrapolation** — projecting a fitted trend forward past the data
  you actually have — gets riskier the further out you project, and
  is especially unreliable when the trend itself was fit from very
  few points, or from points near the edge of the data that happen to
  be unusual.
- **Linear regression** (specifically, the method of **least
  squares**) fits a single straight line through _all_ the available
  points at once, by minimizing the total squared distance between
  the line and every point — not just the two most recent ones — which
  is far less swayed by any single noisy week.

## Key terms

- **Time series** — a sequence of values recorded at regular time
  intervals, where the order matters.
- **Trend** — the underlying direction a time series is moving,
  once noise is averaged out.
- **Noise (residual)** — the part of any single observation that
  doesn't fit the trend — a one-off spike or dip.
- **Extrapolation** — forecasting values outside the range of data
  actually observed, by continuing a fitted trend forward.
- **Linear regression (least squares)** — fitting a straight line
  through a full set of data points by minimizing the sum of squared
  differences between the line and each actual point.

## What this unit covers

L2 works through why fitting a line to all twelve weeks gives a
different (and more defensible) answer than eyeballing the last two
points, and why even a full regression can still be distorted by a
single unusual point sitting at the edge of the data. L3 implements
least-squares linear regression from scratch, applies it to the
Scenario's actual twelve weeks of signups, and compares the
naive two-point forecast against the full-regression forecast for
week 52.
