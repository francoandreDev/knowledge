---
title: "L1 — How do I measure something I can't directly observe?"
---

import Scenario from "../../../components/Scenario.astro";
import LevelIntro from "../../../components/LevelIntro.astro";

<Scenario>
  <Fragment slot="facts">
    <p class="not-prose mb-1 text-xs font-semibold tracking-wide text-slate-400 uppercase">Test coverage (the proxy)</p>
    <div class="not-prose mb-3 flex flex-col gap-1.5 text-sm text-slate-700 dark:text-slate-300">
      <div class="flex items-center gap-2">
        <span class="w-16 shrink-0">Team A</span>
        <div class="h-2 flex-1 overflow-hidden rounded-full bg-slate-200 dark:bg-slate-700">
          <div class="h-full rounded-full bg-sky-500 animate-fill-bar" style="--target-width: 100%"></div>
        </div>
        <span class="w-10 text-right text-xs">100%</span>
      </div>
      <div class="flex items-center gap-2">
        <span class="w-16 shrink-0">Team B</span>
        <div class="h-2 flex-1 overflow-hidden rounded-full bg-slate-200 dark:bg-slate-700">
          <div class="h-full rounded-full bg-sky-500 animate-fill-bar" style="--target-width: 98%"></div>
        </div>
        <span class="w-10 text-right text-xs">98%</span>
      </div>
    </div>

    <p class="not-prose mb-1 text-xs font-semibold tracking-wide text-slate-400 uppercase">Real defect-freedom (the target)</p>
    <div class="not-prose flex flex-col gap-1.5 text-sm text-slate-700 dark:text-slate-300">
      <div class="flex items-center gap-2">
        <span class="w-16 shrink-0">Team A</span>
        <div class="h-2 flex-1 overflow-hidden rounded-full bg-slate-200 dark:bg-slate-700">
          <div class="h-full rounded-full bg-emerald-500 animate-fill-bar" style="--target-width: 90%"></div>
        </div>
        <span class="w-10 text-right text-xs">90</span>
      </div>
      <div class="flex items-center gap-2">
        <span class="w-16 shrink-0">Team B</span>
        <div class="h-2 flex-1 overflow-hidden rounded-full bg-slate-200 dark:bg-slate-700">
          <div class="h-full rounded-full bg-rose-500 animate-fill-bar" style="--target-width: 30%"></div>
        </div>
        <span class="w-10 text-right text-xs">30</span>
      </div>
    </div>

  </Fragment>

Two teams both spend a quarter chasing the same number: test coverage. Both
start the quarter at 40% and both end it near 100% — Team A at 100%, Team B
at 98%. By the coverage number alone, they look identical: two teams that
took the same metric seriously and moved it the same distance.

| Team | Coverage (proxy), start → end | Real defect-freedom (target), start → end |
| ---- | ----------------------------- | ----------------------------------------- |
| A    | 40% → 100%                    | 40 → 90 (kept climbing)                   |
| B    | 40% → 98%                     | 40 → peaked near 53, then fell to 30      |

**Both teams pushed the same proxy to nearly the same place. Why did Team A's actual code quality keep improving while Team B's actively got worse — even though Team B's coverage number never stopped climbing?**

</Scenario>

<LevelIntro
  items={[
    "Proxies vs. real targets",
    "Validity vs. reliability",
    "Instrumentation and sampling costs",
  ]}
/>

The coverage number was never the thing anyone actually cared about — it was
a stand-in for something nobody can read off a dashboard directly: whether
the code is actually reliable.

The same pattern starts in ordinary school measurement. A ruler measures
length in centimeters; a scale measures mass in kilograms; a thermometer
measures temperature in degrees. Those are direct measurements. But a grade is
only a proxy for learning, a fever reading is only one proxy for health, and
test coverage is only a proxy for code quality. The harder the target is to
see directly, the more carefully the proxy has to be questioned.

| Everyday piece          | Simple example                        | Software example                    |
| ----------------------- | ------------------------------------- | ----------------------------------- |
| Instrument              | ruler, scale, thermometer, stopwatch  | dashboard, logs, tracing, test tool |
| Unit                    | cm, kg, °C, seconds                   | %, ms, requests/second, defects     |
| Random error            | slightly different readings each time | noisy latency samples               |
| Systematic error        | a scale always reads 2 kg too heavy   | a metric consistently misses bugs   |
| Reliability / precision | repeated readings are close together  | repeated runs give similar numbers  |
| Validity / accuracy     | the reading measures the real target  | the proxy tracks real quality       |

- Many things worth measuring aren't directly observable: "code quality," "system reliability," "developer productivity" — none of these are a number you can read off an instrument. What's actually measurable are **proxies**: specific, observable stand-ins believed to correlate with the real thing you care about. Test coverage is a proxy for code quality, not code quality itself.
- **A proxy is only useful to the extent it's actually correlated with the real target.** Team A's coverage number tracked real quality closely (a strong proxy — the new tests actually exercised meaningful behavior). Team B's coverage number was gamed — tests added to hit the number without asserting anything meaningful — so the proxy kept climbing while the real target it was supposed to represent stalled and then declined. This failure mode has a name: Goodhart's/Campbell's Law.
- **Validity** (is this proxy actually measuring the thing I care about) and **reliability** (does this measurement give consistent results on repeated measurement of the same thing) are separate properties — a bathroom scale that's off by five pounds but gives the same reading every time is reliable but not valid; a scale that gives a random reading every time is neither.
- **Instrumentation** is how a measurement actually gets taken from a running system — and instrumentation itself has a real, measurable cost (overhead, storage, the Heisenberg-like problem of a measurement tool changing the behavior it's measuring) that has to be weighed against the value of the data it produces.
- **Sampling** is measuring a subset and inferring about the whole — necessary when measuring every single instance is too expensive (every request's exact latency, every user's exact behavior) — and it introduces its own real error term, distinct from instrument error, that has to be accounted for separately.
- **Units and dimensional consistency** matter even for informal engineering measurement: a "faster" claim with no unit (seconds? requests-per-second? percentile?) isn't actually a measurement yet, it's a direction — the specific unit is what makes a claim checkable at all, tying directly back to `testing-quality`/`software-design`'s emphasis on falsifiable, specific claims.
- A sample is only useful if it represents the population. Asking five friends whether a school rule is fair is easy, but it may not represent the whole school; sampling only quiet production hours has the same problem for latency. `applied-math/statistics` develops that average/spread language, while `applied-math/orders-magnitude` helps keep units and scale honest.
- The practical skill this unit builds: given something you want to know but can't observe directly, identify a proxy, check whether it's actually valid (not just convenient to measure), and understand what instrumentation/sampling choices are shaping the number before treating it as ground truth.
