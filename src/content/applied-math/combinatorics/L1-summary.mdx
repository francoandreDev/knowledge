---
title: "L1 — Why did 'test the 8 important combos' miss the bug that broke checkout?"
---

import Scenario from "../../../components/Scenario.astro";

<Scenario label="8 test cases, 16 possible combinations, 1 bug in the gap">
  <Fragment slot="facts">
    <div class="not-prose flex flex-col gap-2 text-sm text-slate-700 dark:text-slate-300">
      <div class="flex items-center gap-1.5"><span>🚩</span> <strong>4 feature flags</strong> — new checkout UI, Apple Pay, promo code, express shipping</div>
      <div class="flex items-center gap-1.5"><span>✅</span> <strong>8 "important" test cases</strong> — picked by hand, felt thorough</div>
      <div class="flex items-center gap-1.5"><span>💥</span> <strong>Production bug</strong> — Apple Pay + promo code together broke the total calculation</div>
    </div>
  </Fragment>

A payments feature ships behind four independent flags. Before launch,
QA picks what feels like a solid set — 8 test cases covering "the
important combinations." It ships. A week later, a customer paying
with Apple Pay _and_ a promo code at the same time gets charged the
wrong total — a combination nobody had tested. **8 test cases felt
thorough. Why wasn't it?**

</Scenario>

## The shape of the problem

- Four independent on/off flags don't create 4 things to test, or even
  8 — they create every possible combination of on/off across all
  four, and that count grows far faster than most people's intuition
  expects.
- "Pick the important combinations by hand" is a judgment call, not a
  systematic method — it can feel thorough while silently leaving
  specific pairs of flags never tested together at all, which is
  exactly where interaction bugs like this one hide.

## Key terms

- **Factorial** (`n!`) — the number of ways to arrange `n` distinct
  items in order: `n × (n-1) × ... × 1`. The building block for both
  permutations and combinations.
- **Permutation** — an arrangement where _order matters_: how many
  distinct orderings of `r` items chosen from `n` are possible.
- **Combination** — a selection where _order doesn't matter_: how many
  distinct groups of `r` items chosen from `n` are possible, without
  caring about the order they were picked in.
- **Combinatorial explosion** — how quickly the count of possible
  combinations grows as the number of independent choices increases —
  often far faster than linear, which is what makes "just test the
  important ones" break down without anyone noticing.

## What this unit covers

L2 works through the difference between permutations and combinations,
and why a set of independent flags grows as a power of two rather than
a simple sum. L3 implements real functions to compute exact
combination counts and to check whether a hand-picked set of test
cases actually covers every pair of flags together — showing precisely
which pair the 8 "important" test cases left untested.
