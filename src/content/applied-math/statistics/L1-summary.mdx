---
title: "L1 — How do I know if a difference I measured is real or noise? (statistics: mean/variance/std. dev., confidence intervals, significance)"
---

import Scenario from "../../../components/Scenario.astro";

<Scenario label="The new button converted at 18% instead of 12% — a 50% relative lift. The team wants to ship it today.">
  <Fragment slot="facts">
    <div class="not-prose flex flex-col gap-2 text-sm text-slate-700 dark:text-slate-300">
      <div class="flex items-center gap-1.5"><span>🅰️</span> <strong>Control (old button)</strong> — 100 visitors, 12 conversions (12%)</div>
      <div class="flex items-center gap-1.5"><span>🅱️</span> <strong>Variant (new button)</strong> — 100 visitors, 18 conversions (18%)</div>
      <div class="flex items-center gap-1.5"><span>🎉</span> <strong>Team's reaction</strong> — "that's a 50% lift, ship it now"</div>
    </div>
  </Fragment>

An A/B test runs for a day: 100 visitors see the old button and 12
convert; 100 visitors see a new button color and 18 convert. That
looks like a big win — 18% versus 12%, a 50% relative improvement —
and the team wants to ship the new button immediately. **With only
100 visitors per side, could a swing of six conversions this size
happen just from which 100 people happened to show up each day, even
if the button color made no real difference at all?**

</Scenario>

## The shape of the problem

- Any sample of real-world data has **noise** — random variation that
  exists even when there's no true underlying difference. Flip a fair
  coin 100 times twice, and the two runs won't both land exactly 50
  heads; that gap alone doesn't mean the coin changed.
- A measured difference being **bigger** doesn't automatically make it
  **real** — a 50% relative lift computed from only 100 visitors per
  group can easily be within the range random noise alone would
  produce, while a smaller-looking 5% lift computed from 50,000
  visitors per group can be extremely unlikely to be noise.
- **Statistics**, in this context, is the machinery for answering one
  specific question: given the amount of data actually collected, how
  likely is it that a difference this large (or larger) would show up
  even if there were no real effect at all?

## Key terms

- **Mean** — the average of a set of values; the single number that
  best summarizes where the data is centered.
- **Variance / standard deviation** — measures of how spread out the
  values are around the mean. Standard deviation is the square root
  of variance, expressed in the same units as the original data,
  which makes it easier to interpret directly.
- **Confidence interval** — a range around a measured value that's
  likely to contain the true value, given the sample size and
  variability observed — wider with less data, narrower with more.
- **Statistical significance** — a measured difference is called
  significant when it's unlikely enough to have arisen from random
  noise alone (by a chosen threshold) that it's reasonable to treat
  it as a real effect, rather than "large" in an everyday sense.

## What this unit covers

L2 works through why small samples produce noisy measurements, what
a confidence interval actually represents, and what "significant"
means as a specific, checkable claim rather than a vague sense of
"looks like a big difference." L3 runs the actual numbers from this
unit's A/B test scenario through a real significance test, and shows
exactly how much more data would have been needed to trust the result.
