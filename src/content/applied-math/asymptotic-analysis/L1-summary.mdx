---
title: "L1 — How do I reason precisely about growth as input size increases? (asymptotic analysis, Big-O put on rigorous footing)"
---

import Scenario from "../../../components/Scenario.astro";

<Scenario label="A benchmark that lied">
  <Fragment slot="facts">
    <div class="not-prose flex flex-col gap-2 text-sm text-slate-700 dark:text-slate-300">
      <div class="flex items-center gap-1.5"><span>🧪</span> <strong>Benchmark</strong> — tested at n = 5 and n = 10 items</div>
      <div class="flex items-center gap-1.5"><span>📈</span> <strong>Doubling n roughly doubled runtime</strong> — "looks linear"</div>
      <div class="flex items-center gap-1.5"><span>🚨</span> <strong>Production</strong> — n reached 100,000+, the service fell over</div>
    </div>
  </Fragment>

A candidate benchmarks their solution at small input sizes — 5 items, then
10 — and doubling the input roughly doubles the runtime. They ship it,
confident it's linear. In production, the real input grows past 100,000
items, and the service falls over. **Was the benchmark lying, or was
something about _how_ they read it wrong?**

</Scenario>

## The shape of the problem

- "It doubled when I doubled the input" is a real observation — but it's
  only strong evidence about growth rate _near the sizes you actually
  tested_, not proof of the growth rate everywhere.
- Asymptotic analysis is the tool for reasoning about growth **as input
  size approaches infinity** — precisely the region small benchmarks can't
  see.

## Key terms

- **Growth rate** — how an algorithm's operation count scales as input
  size `n` increases, independent of any specific hardware or language.
- **Dominant term** — in a sum like `3n² + 100n`, the term that grows
  fastest eventually overwhelms every other term as `n` grows — here,
  `3n²` eventually dwarfs `100n`, no matter how much `100n` seemed to
  matter at small `n`.
- **Big-O, O(f(n))** — an upper bound: the algorithm's operation count
  grows _no faster than_ `f(n)`, once `n` is large enough.
- **Big-Omega, Ω(f(n))** — a lower bound: the algorithm's operation count
  grows _at least as fast as_ `f(n)`, once `n` is large enough.
- **Big-Theta, Θ(f(n))** — a tight bound: both an upper _and_ lower bound
  at once — the algorithm grows at _exactly_ the rate of `f(n)`, not just
  no faster and not just no slower.
- **Doubling ratio** — measuring `T(2n) / T(n)` at a given `n` is a cheap
  empirical probe: a ratio near 2 suggests roughly linear growth _at that
  n_; a ratio near 4 suggests roughly quadratic growth _at that n_ — but,
  per the scenario above, that ratio can itself shift as `n` grows, which
  is exactly what small-n benchmarks miss.

## What this unit covers

L2 works through the formal O/Ω/Θ definitions precisely (not just
intuition), and why the doubling ratio for `3n² + 100n` looks linear-ish
at small `n` but converges to quadratic at large `n`. L3 builds a real,
verified operation-counting toolkit — measuring actual growth exponents
from real functions, not estimated from wall-clock timing — and
reproduces the exact misleading-benchmark scenario above with real
numbers.
