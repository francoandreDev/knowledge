---
title: "L1 — How do I reason precisely about state and transformation? (linear algebra basics)"
---

import Scenario from "../../../components/Scenario.astro";
import LevelIntro from "../../../components/LevelIntro.astro";

<Scenario label="A sprite that drifted off-center">
  <Fragment slot="facts">
    <div class="not-prose flex flex-col gap-2 text-sm text-slate-700 dark:text-slate-300">
      <div class="flex items-center gap-1.5"><span>📍</span> <strong>Icon offset</strong> — 100px right, 0px down from its center</div>
      <div class="flex items-center gap-1.5"><span>🌀</span> <strong>Hover effect</strong> — rotate 45°, then scale 1.5×</div>
      <div class="flex items-center gap-1.5"><span>💥</span> <strong>Bug</strong> — swapping the order of "rotate" and "scale" in the code gave a different, wrong position</div>
    </div>
  </Fragment>

An engineer builds a hover animation for an icon: rotate it 45°, then scale
it up 1.5×. It works. Weeks later, a teammate reorders the two lines of
code — scale first, then rotate — for what looks like a harmless
refactor. The icon now lands in a visibly different spot on hover. **Same
two operations, same icon, same starting position — so why did just
reordering two lines of math change the answer?**

</Scenario>

<LevelIntro
  items={[
    "What a vector is (a bundle of related numbers)",
    "What a matrix is (a reusable transformation)",
    "Where vectors and matrices actually show up",
  ]}
/>

## The shape of the problem

- A position on screen, a color, a set of movie ratings, a word's meaning
  in an ML model — all of these are naturally **more than one number
  that has to move together**. Treating them as separate loose variables
  (`x`, `y` as two unrelated floats) throws away the fact that they
  describe one thing.
- A **transformation** — rotate, scale, move, "compare to another
  thing" — is an operation you want to apply consistently, and often
  **combine** with other transformations. The engineer's bug above is
  exactly what happens when "combine two transformations" is done by
  hand with ad hoc formulas instead of a tool built for combining them.

Linear algebra is the precise vocabulary for both halves of that
problem: a **vector** for the bundle of numbers (the state), and a
**matrix** for the reusable transformation (the operation) — including
how transformations combine, and in what order.

## Key terms

- **Vector** — an ordered list of numbers treated as a single bundle
  because they describe one thing: a 2D position `(x, y)`, an RGB color
  `(r, g, b)`, a set of feature values for one row in a dataset. Order
  matters — `(100, 0)` and `(0, 100)` are different vectors even though
  they contain the same numbers.
- **Matrix** — a rectangular grid of numbers that represents a
  transformation you can apply to a vector: rotate it, scale it, move
  it, or any combination. The same matrix can be applied to many
  different vectors, which is exactly why it's reusable in a way that
  "just write the rotation formula inline" isn't.
- **Vector addition** — adding two vectors component-by-component;
  geometrically, "start at the first vector's tip, then walk the second
  vector's distance and direction."
- **Scalar multiplication** — multiplying every component of a vector
  by the same plain number, stretching or shrinking it without changing
  its direction (or flipping it, for a negative scalar).
- **Dot product** — multiply matching components of two vectors and add
  the results into a single number. It measures how much two vectors
  point in the same direction — the basis for both "distance/length" and
  "similarity" (more in L2/L3).
- **Matrix-vector multiplication** — applying a matrix's transformation
  to a specific vector, producing a new, transformed vector. This is
  the operation the engineer's rotate/scale bug was really about.

## Where this actually shows up

| Field                    | The "vector"                                                                                 | The "matrix" / operation                                                                |
| ------------------------ | -------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------- |
| 2D/3D graphics & UI      | A shape's `(x, y)` position, or `(x, y, z)` in 3D                                            | A rotation, scale, or move, as in `CSS transform: matrix()` or a canvas/WebGL transform |
| Machine learning         | A row of features, or a word/image "embedding"                                               | A layer's weights, transforming one representation into another                         |
| Recommendation & search  | A user's ratings, or a document's word counts                                                | Dot product / cosine similarity between two such vectors                                |
| Any multi-quantity state | Anything tracked as several related numbers at once (velocity, color, a game entity's stats) | Any operation applied consistently to that whole bundle                                 |

The common thread: whenever a system's state is naturally **more than
one number that belongs together**, and an operation needs to be
**applied consistently — possibly combined with other operations, and
possibly in a specific order** — that's linear algebra's territory,
whether or not the code ever uses the words "vector" or "matrix."

## What this unit covers

L2 works through matrix-vector multiplication precisely enough to
explain the engineer's reordering bug — and shows why matrix
multiplication is not commutative, which is the exact mechanism behind
"rotate then scale" and "scale then rotate" landing in different
places. L3 builds a small, real, runnable 2D vector/matrix toolkit —
including the same rotate/scale pipeline as real transform code — plus
a dot-product similarity calculation of the kind used in recommendation
and search systems.
