{
  "items": [
    {
      "id": "delay-vs-routine-perception",
      "level": 2,
      "title": "How much does waiting to send a confirmation change how it reads?",
      "description": "Drag how many days pass between the verbal decision and the written confirmation being sent. Per L2, the closer a confirmation is sent to the original conversation, the more it reads as routine practice rather than a reaction to trouble — a message sent the same day (Version B in L3) reads as professional; one sent only after a problem has already surfaced (Version A, effectively never sent until the incident review, three weeks later) reads as defensive.",
      "params": [
        {
          "name": "daysDelayed",
          "label": "Days between the decision and the confirmation",
          "min": 0,
          "max": 21,
          "step": 1,
          "default": 0,
          "unit": "days"
        }
      ],
      "compute": "const routinePerception = Math.max(10, 100 - params.daysDelayed * 5); return { routinePerception };",
      "outputs": [
        {
          "key": "routinePerception",
          "label": "Reads as routine practice, not reactive",
          "unit": "%",
          "color": "#34d399"
        }
      ],
      "chartParam": "daysDelayed"
    }
  ]
}
