---
title: "L1 — Why does working more hours eventually produce less, not more?"
---

import Scenario from "../../../components/Scenario.astro";
import LevelIntro from "../../../components/LevelIntro.astro";

<Scenario>
  <Fragment slot="facts">
    <div class="not-prose flex flex-col gap-1.5 text-sm text-slate-700 dark:text-slate-300">
      <div class="flex items-center gap-1.5"><span>🗓️</span> Normal pace: 40h/week</div>
      <div class="flex items-center gap-1.5"><span>📈</span> Plan: 65h/week (+62%)</div>
      <div class="flex items-center gap-1.5"><span>🤔</span> Expectation: "+62% more done"</div>
      <div class="flex items-center gap-1.5"><span>❓</span> Reality: not what the math predicts</div>
    </div>

    <p class="not-prose mt-3 mb-1 text-xs font-semibold tracking-wide text-slate-400 uppercase">Why it breaks</p>
    <div class="not-prose flex flex-col gap-1 text-sm text-slate-700 dark:text-slate-300">
      <div class="flex items-center gap-1.5"><span>😴</span> Fatigue lowers focus and judgment</div>
      <div class="flex items-center gap-1.5"><span>⚠️</span> More errors, made later in the day</div>
      <div class="flex items-center gap-1.5"><span>🔁</span> Errors cost rework — paid in future hours</div>
    </div>

  </Fragment>

Dana is three weeks from a big deadline and does the math: "if I put in
62% more hours this month, I should get roughly 62% more done." It's the
same reasoning almost everyone reaches for under pressure — hours in,
output out, in a straight line.

**Before reading on: if Dana actually tracks this for a month, will the
extra effort produce roughly 62% more finished, usable work — more, less,
or could it produce _less_ than working the normal pace the whole time?**

</Scenario>

<LevelIntro
  items={[
    "The linear-output assumption",
    "Diminishing vs. negative returns",
    "Why the effect is a throughput argument, not just a wellness one",
  ]}
/>

- The intuitive model is **linear**: output = hours × some constant rate, so working 20% more hours should produce roughly 20% more output. Real output curves are not linear — they show **diminishing returns**, and past a certain point, **negative returns**, where more hours produce _less_ total output than fewer hours would have.
- The mechanism isn't mysterious: focus, judgment, and error rate all degrade with fatigue and time-on-task — a tired hour doesn't just produce less than a fresh hour, it can actively produce _negative_ value (a mistake made that costs more hours to fix later than the extra hour saved now).
- **Diminishing returns** means each additional hour produces less marginal output than the one before it — output is still increasing, just slower. **Negative returns** is the point past that where an additional hour actually _reduces_ total output, because the mistakes and rework it causes outweigh what it directly produces.
- This isn't primarily a wellness argument — the case here is one of throughput, made in the same terms as any other trade-off: if the goal is maximum sustained output over a real time horizon (a quarter, a year), pushing past the diminishing-returns point is a measurably worse strategy than a sustainable pace, not a nicer-but-slower one.
- The curve's exact shape differs by task type (deep, error-prone work degrades faster than routine, low-stakes work) and by time horizon (a single hard push before a real deadline is a different trade than a chronic long week sustained for months) — this unit is about the shape of the relationship, not a universal "N hours is the correct number."
- The practical implication: "we need this done faster" and "add more hours" are not the same lever past a certain point — sustained overtime is a real cost with a real, measurable ceiling on what it can buy, and treating it as free capacity is a modeling error, not a matter of discipline or effort.

| What you'd expect (linear)                | What actually happens                                             |
| ----------------------------------------- | ----------------------------------------------------------------- |
| More hours → proportionally more output   | More hours → more output, _then_ less-per-hour, _then_ less total |
| Effort and result move together forever   | The relationship bends, and can reverse                           |
| "Behind schedule" is fixed by "work more" | Past a point, "work more" makes "behind schedule" worse           |
