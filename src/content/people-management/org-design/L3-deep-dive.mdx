---
title: "L3 — Splitting Priya's team, twice: 5 to 20 over 18 months"
---

import Scenario from "../../../components/Scenario.astro";
import LevelIntro from "../../../components/LevelIntro.astro";
import Checkpoint from "../../../components/Checkpoint.astro";

<Scenario label="The same team, 18 months later, hitting the same ceiling twice">
  <Fragment slot="facts">
    <div class="not-prose flex flex-col gap-2 text-sm text-slate-700 dark:text-slate-300">
      <div class="flex items-center gap-1.5"><span>📅</span> <strong>Month 0</strong> — 5 engineers, 1 manager, functional generalist team</div>
      <div class="flex items-center gap-1.5"><span>📅</span> <strong>Month 9</strong> — 11 engineers, span breached, split into 2 pods</div>
      <div class="flex items-center gap-1.5"><span>📅</span> <strong>Month 18</strong> — 20 engineers total; the payments pod alone is now at 11</div>
    </div>
  </Fragment>

**Before reading further, predict: at month 18, the payments pod's
lead now has 11 direct reports — the exact number that forced Priya's
own split nine months earlier. Does the same fix apply a second time,
or does something have to be different now that there's a layer of
management already in between?**

</Scenario>

<LevelIntro
  items={[
    "Choosing a split boundary by domain, not by even headcount",
    "The transition mechanics that make a split actually stick",
    "Why one split is rarely the last one a growing team needs",
  ]}
/>

## Month 0–6: one team, functional, and still fine

At 5 engineers, Priya runs a single functional team — everyone reports
to her, work is assigned by whoever has capacity, and code review
rotates informally. This works well: 1:1s run the full 36 minutes,
decisions close same-day, and the team is small enough that shared
context substitutes for formal ownership. By month 6, headcount reaches 8. Span of control is at the edge of the typical ceiling but not
clearly over it, and while payments and search work are both present,
neither is big enough yet to obviously deserve its own team. **Is there
already a signal to split at month 6, or is 8 still a defensible "not
yet"?** By L2's signal table, span is borderline and ownership is still
mostly clear — the divergent-workstreams signal is present but weak.
This is a legitimate "not yet," provided it's actively watched rather
than ignored.

## Month 9: the ceiling breaks, and the decision gets made

By month 9, the team is 11. 1:1 time per report has dropped to 16
minutes (the same arithmetic from L2: 180 minutes ÷ 11). Payments and
search have become two genuinely different workstreams — different
stakeholders, different roadmaps, different failure modes — but both
still route every non-trivial decision through Priya, because no one
else has the standing to make the call. This satisfies three of L2's
five signals at once: span-of-control ceiling, divergent workstreams,
and decision bottleneck. Priya decides to split.

<Checkpoint question="At month 9, could Priya have instead solved this by hiring an assistant manager who handles some 1:1s but reports the same combined status to her, with the team otherwise unchanged?">

That would relieve the 1:1-time symptom without addressing the deeper
issue: decisions would still route through Priya because ownership of
payments versus search was never actually separated. An assistant
manager who reports combined status back up recreates the same
bottleneck one layer removed — Priya is still the single point every
non-trivial call passes through. The fix that actually works has to
give someone real decision authority over a bounded piece of the work,
not just redistribute meeting time.

</Checkpoint>

## Choosing the split boundary: domain, not headcount

**Priya could split her 11 reports into two groups of 5 and 6, picked
arbitrarily to balance the numbers. Would that work as well as
splitting along the payments/search line?** No — an even-headcount
split ignores what's actually driving the coordination cost.

| Split approach                       | What happens afterward                                                                                                                    |
| ------------------------------------ | ----------------------------------------------------------------------------------------------------------------------------------------- |
| Even headcount (arbitrary 5/6 split) | Both new pods still contain a mix of payments and search work — decisions about either area still cross a pod boundary as often as before |
| Domain boundary (payments / search)  | Each pod owns a coherent area end to end; cross-pod coordination drops because each domain's decisions stay inside its own pod            |

Priya splits along the domain line: a payments pod (6 engineers) and a
search pod (5 engineers). This matters because a split is meant to
reduce coordination cost, and coordination cost tracks who has to talk
to whom to get a decision made — not how evenly the roster divides.

## Picking the leads

Each pod needs a lead with real decision authority, not just a senior
title. Priya evaluates two internal candidates per pod against the
same two criteria: technical depth in that specific domain, and
evidence of already being the person others informally route decisions
to. Both leads she picks were already the de facto point of contact
for their area before the split made it official — the split is
formalizing an existing pattern, not inventing authority from nothing.
An external hire was considered for the search lead role but rejected
for this transition specifically: the pod needed someone who could make
confident calls from day one, and an external hire would need months of
context-building before reaching that bar.

## Transition mechanics: what actually has to happen

A split announced in a meeting and a split that actually works are not
the same thing. Priya runs the transition over four weeks:

1. **Written ownership doc** — for each pod, a one-page doc naming
   which services, which on-call rotation, and which stakeholder
   relationships now belong to that pod specifically, reviewed and
   signed off by both new leads before the split takes effect.
2. **Overlapping 1:1s for the first month** — each engineer keeps a
   short monthly check-in with Priya alongside their new lead's weekly
   1:1, so the handoff isn't a hard cutoff that leaves questions
   unanswered.
3. **On-call handoff with a real overlap window** — the new pod-level
   on-call rotations start two weeks before the old combined rotation
   ends, so an incident during the transition has two people who know
   what's happening rather than one person still learning the rotation
   under pressure.
4. **Code ownership file updated same day as the announcement** — so
   review routing and incident paging reflect the new boundary
   immediately, rather than lagging behind the org chart for weeks.

<Checkpoint question="Priya skips the code-ownership-file update, planning to get to it 'once things settle down' a few weeks after the announcement. What's the concrete risk in the meantime?">

During that gap, review requests and incident pages keep routing by
the old, now-inaccurate ownership — likely straight back to Priya, or
to whichever engineer happened to touch that code last, regardless of
which pod they're now in. That reproduces exactly the bottleneck the
split was meant to remove, just informally instead of on the org chart:
the structure says "split," but the tooling and routing still say "one
team," and the tooling is what actually determines who gets paged at
2am.

</Checkpoint>

## Month 18: the payments pod hits the same ceiling

Payments work grows faster than search over the next nine months —
new fraud-detection and regional-payment-method requirements pull in
five more engineers, while search grows by four. By month 18: payments
pod is 11 engineers under one lead; search pod is 9. The payments
lead is now exactly where Priya was at month 9 — same span-of-control
number, same 16-minutes-per-1:1 arithmetic — but with one difference:
there's now a management layer between that lead and Priya, and Priya
is no longer doing hands-on engineering management of individual
contributors at all. She's managing two (soon three) leads instead.

This is the answer to the opening prediction: the _arithmetic_ repeats
exactly, but the _fix_ isn't identical. Priya's original fix was
"split the team and promote leads." The payments lead's fix is the same
diagnosis — but the organization now has a choice the first split
didn't: either split the payments pod again (a third pod, likely along
a further domain line such as "core payments" vs. "fraud and risk"),
or leave it as one pod and hire a second layer of leadership under the
payments lead specifically. Either resolves the span-of-control number;
which one is right depends on whether payments itself has a real,
separable sub-domain boundary the way payments-vs-search did at month
9, or whether it's genuinely one coherent domain that's simply grown
large.

**Extend it: suppose payments and search shared a single library both
pods depend on and modify weekly — say, a shared risk-scoring
module neither domain owns outright. Does the domain-boundary split
from month 9 still hold up as cleanly, or does the shared dependency
recreate some of the coordination cost the split was meant to remove?
What would you do differently about that one module specifically, and
does the answer change if the module is small and rarely touched
versus large and touched by both pods every week?**

## Trade-offs and failure modes

| Failure mode                                                             | What it gets wrong                                                                                             |
| ------------------------------------------------------------------------ | -------------------------------------------------------------------------------------------------------------- |
| Splitting purely by headcount, ignoring domain boundaries                | Both resulting teams still cross-coordinate on the same decisions as before — the split reduces nothing        |
| Splitting before real signals appear, on anticipated future growth alone | Fragments a team that still benefits from shared context; each new pod is too thin to be self-sufficient       |
| Announcing a split without naming a real lead with real authority        | Decisions still bottleneck on the original manager — the org chart changed, the decision routing didn't        |
| Leaving the codebase's structure unchanged after an org-level split      | Conway's Law works against you: the old coordination cost reappears through the shared code, not the org chart |
| Treating one split as a permanent fix rather than a recurring decision   | A pod that keeps growing will hit the same ceiling again — org design isn't a one-time event                   |
