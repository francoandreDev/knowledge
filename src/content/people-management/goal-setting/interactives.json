{
  "items": [
    {
      "id": "total-downtime-from-two-key-results",
      "level": 3,
      "title": "Why two key results, not one: total customer-facing downtime",
      "description": "The rebuilt goal's first two key results — fewer P1 incidents, and faster recovery from each one — aren't redundant: they're two independent levers on the same real outcome, total customer-facing downtime (incident count × recovery time). Drag either one and see why hitting only one of the two targets still leaves real impact on the table.",
      "params": [
        {
          "name": "incidentCount",
          "label": "P1 incidents per quarter",
          "min": 0,
          "max": 15,
          "step": 1,
          "default": 8,
          "unit": ""
        },
        {
          "name": "mttrMinutes",
          "label": "Mean time to recovery",
          "min": 5,
          "max": 60,
          "step": 5,
          "default": 45,
          "unit": "min"
        }
      ],
      "compute": "return { totalDowntimeMinutes: params.incidentCount * params.mttrMinutes };",
      "outputs": [
        {
          "key": "totalDowntimeMinutes",
          "label": "Total customer-facing downtime per quarter",
          "unit": "min",
          "color": "#f472b6"
        }
      ],
      "chartParam": "incidentCount"
    }
  ]
}
