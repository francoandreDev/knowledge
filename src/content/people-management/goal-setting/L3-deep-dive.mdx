---
title: "L3 — Rebuilding the quarter's goal from the actual company priority"
---

import Scenario from "../../../components/Scenario.astro";
import LevelIntro from "../../../components/LevelIntro.astro";
import Checkpoint from "../../../components/Checkpoint.astro";

<Scenario label="Same team, same quarter, rebuilt goal">
  <Fragment slot="facts">
    <div class="not-prose flex flex-col gap-2 text-sm text-slate-700 dark:text-slate-300">
      <div class="flex items-center gap-1.5"><span>🏢</span> <strong>Company priority</strong> — hit 99.9% uptime to save a major enterprise renewal</div>
      <div class="flex items-center gap-1.5"><span>👥</span> <strong>Team's actual control</strong> — the service behind the outages is entirely this team's</div>
    </div>
  </Fragment>

**Before reading further, predict: given the same team and the same
quarter, what would a properly cascaded objective and set of key
results look like — and specifically, what would make them different
from "improve code quality" in a way a reader could point to, not
just a vague sense of "better"?**

</Scenario>

<LevelIntro
  items={[
    "Deriving key results from a real company priority",
    "Checking each key result against three questions",
    "Naming a skill gap before it's a delivery risk",
  ]}
/>

## Step 1: Start from what the organization actually needs

The company's stated priority for the quarter, from the leadership
planning doc: hit a 99.9% uptime target this quarter, because a major
enterprise customer's renewal is explicitly contingent on reliability
improving. This is the thing every team's goal should be able to trace
back to — not restated, but genuinely connected.

## Step 2: Identify what this specific team controls

The service causing the outages is entirely owned by this team — no
other team's work is a blocker or a dependency here. That's what makes
this a legitimate team-level objective rather than something that
belongs to a different team or to the org as a whole: the team has
real, direct control over the outcome.

## Step 3: Draft the objective and key results

- **Objective:** Make the service reliable enough to stop being the
  reason this renewal is at risk.
- **Key result 1:** Reduce P1 incidents from 8 per quarter to 2 or
  fewer.
- **Key result 2:** Reduce mean time to recovery from 45 minutes to
  under 15 minutes.
- **Key result 3:** Write incident runbooks covering the top 10
  historical failure modes (currently 0 written).

## Step 4: Check each key result against three questions

| Key result                     | Measurable?                               | Within the team's control?                     | Traces to the objective?                                           |
| ------------------------------ | ----------------------------------------- | ---------------------------------------------- | ------------------------------------------------------------------ |
| P1 incidents 8 → ≤2            | Yes — a count, checkable at quarter end   | Yes — root causes are all in this team's code  | Yes — fewer P1s is directly what "reliable" means here             |
| MTTR 45min → under 15min       | Yes — a duration, logged automatically    | Yes — response process is the team's to change | Yes — faster recovery reduces customer-visible impact              |
| Runbook coverage, top 10 modes | Yes — a checklist, either complete or not | Yes — writing them requires no other team      | Partially — enables faster MTTR, doesn't reduce incidents directly |

The third key result is weaker than the first two on the "traces to
the objective" question — it's an enabler for key result 2, not a
direct measure of reliability itself. That's a genuine judgment call
worth naming explicitly, not a reason to drop it: sometimes a key
result is legitimately supporting infrastructure for another one.

<Checkpoint question="The runbook-coverage key result scored only 'partially' on tracing to the objective, while the incident-count and MTTR key results scored fully. Does that weaker score mean the runbook key result should be dropped from the goal?">

No — a weaker score on one of the three questions isn't automatically
disqualifying, it's information to weigh. The runbook key result still
passes the other two tests cleanly: it's measurable and entirely
within the team's control. Its role is different from the other two —
it's an enabler that makes faster recovery achievable rather than a
direct measure of reliability itself. Naming that distinction
explicitly is the point: it lets the team (and leadership) understand
why the key result is there without mistaking it for a fourth,
independent measure of the same thing the other two already capture.

</Checkpoint>

## Step 5: Compare directly against the original goal

|                                      | "Improve code quality"                                  | The rebuilt objective                                       |
| ------------------------------------ | ------------------------------------------------------- | ----------------------------------------------------------- |
| Measurable?                          | No — no number was ever defined                         | Yes — three explicit key results                            |
| Traces to a real org need?           | No — chosen without reference to the company priority   | Yes — directly derived from the renewal-risk priority       |
| Would leadership recognize progress? | No — nothing in it maps to what they were worried about | Yes — every key result maps to what they were worried about |

This is the same team, the same amount of available effort, and
plausibly even overlapping actual work (writing tests and refactoring
could genuinely reduce P1 incidents) — the difference is entirely in
whether the goal was derived from the real priority and stated in a
form that's checkable at the end of the quarter.

## Step 6: Name the skill gap explicitly

Nobody on the team has written an incident runbook before. Left
unstated, "write 10 runbooks" risks becoming "write 10 mediocre
documents nobody trusts during an actual incident." A real plan names
this gap up front: the first runbook should be paired with someone who
has written good ones before (even from outside the team), reviewed
against a checklist for what makes a runbook actually usable at 2am,
and only then used as the template for the remaining nine.

<Checkpoint question="If the team had simply set 'write 10 runbooks' as a key result without the skill-gap step, and delivered 10 documents by quarter end, would the goal have been achieved?">

Only on the surface. The key result as literally written would show as
complete, but the underlying reason it existed — having runbooks
someone can actually rely on during a real incident — wouldn't
necessarily be true. Ten documents written by people who've never
written a good one before are likely to be incomplete, wrong in
places, or structured in a way that's hard to follow under pressure.
Naming the skill gap up front and pairing the first attempt with
someone experienced is what closes the distance between "the key
result technically happened" and "the thing the key result was
supposed to prove actually happened."

</Checkpoint>

## Extend it: what happens if the company priority changes mid-quarter?

**Suppose six weeks into the quarter, the renewal closes early and
leadership's top priority shifts to a different initiative entirely —
should the team's key results stay fixed for consistency, or should
they be revisited?** There's a real tension here: constantly
re-deriving goals erodes the team's ability to make sustained progress
on anything, but rigidly keeping a goal that no longer traces to a
real need reproduces exactly the "improve code quality" problem from a
different angle — busy work that leadership won't recognize as
progress. **What would you actually want to know before deciding
whether to revisit the goal — does it matter whether the shift is
temporary or permanent?**

## Failure modes

| Failure mode                                                                         | What it gets wrong                                                                                           |
| ------------------------------------------------------------------------------------ | ------------------------------------------------------------------------------------------------------------ |
| Picking a goal that's specific and effortful, but never checked against org priority | Specificity and difficulty aren't the same as being the right thing to work on                               |
| Setting a key result with no number attached                                         | "Improve" isn't checkable at quarter end — someone will have to argue about whether it happened              |
| Copying the company-level OKR word for word as the team's goal                       | A team goal needs to name the team's specific, controllable contribution, not restate the top-level ambition |
| Assuming effort spent proves the goal was achieved                                   | Leadership judges whether the org's actual problem moved, not how busy the team looked                       |
| Setting an ambitious key result without naming the skill gap it requires             | A goal that silently assumes a skill the team doesn't have yet will be attempted, not delivered              |
