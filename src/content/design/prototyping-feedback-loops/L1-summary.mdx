---
title: "L1 — How do I test an idea before I build it for real?"
---

import Scenario from "../../../components/Scenario.astro";
import LevelIntro from "../../../components/LevelIntro.astro";

<Scenario>
  <Fragment slot="facts">
    <div class="not-prose flex flex-col gap-1 text-sm text-slate-700 dark:text-slate-300">
      <div class="flex items-center gap-1.5"><span>📝</span> Paper sketch — ~1 hour to make</div>
      <div class="flex items-center gap-1.5"><span>🖼️</span> Static wireframe — ~1 day to make</div>
      <div class="flex items-center gap-1.5"><span>🖱️</span> Clickable prototype — ~3 days to make</div>
      <div class="flex items-center gap-1.5"><span>💻</span> Coded, real version — ~3 weeks to make</div>
    </div>
    <p class="not-prose mt-3 mb-1 text-xs font-semibold tracking-wide text-slate-400 uppercase">The actual question</p>
    <div class="not-prose text-sm text-slate-700 dark:text-slate-300">
      Does a reordered checkout reduce cart abandonment?
    </div>
  </Fragment>

A product team thinks that moving the shipping-address step _after_
payment, instead of before it, will stop people from abandoning their
cart. Nobody knows if that's actually true — it's a guess dressed up as a
plan. Engineering estimates that building the new flow for real, wired
into the live checkout, would take about three weeks.

**Before reading on: does the team need to spend those three weeks to find out if the guess is right?**

No. And spending them anyway — before anyone has evidence the idea even
works — is the exact mistake this unit is about.

</Scenario>

It wasn't obvious to this team that "test the idea" and "build the real
thing" could be two separate steps, at two very different costs. They
aren't the same step, and confusing them is where a lot of wasted
engineering time comes from.

<LevelIntro
  items={[
    "The fidelity spectrum: paper, wireframe, clickable, coded",
    "What each fidelity level can and can't tell you",
    "Matching how real a prototype feels to the actual question being asked",
  ]}
/>

- A **prototype** is anything that lets someone react to an idea _before_
  it's built for real — a sketch on paper, a set of static screens, a
  set of screens you can actually click through, or a working (but
  throwaway) version of the real feature.
- **Fidelity** is how closely a prototype resembles the finished
  product — in what it looks like, how it behaves, and how fast it
  responds. Fidelity is not a single dial; a prototype can be highly
  realistic visually while being completely fake behaviorally (a
  gorgeous static mockup that doesn't respond to a single tap), or the
  reverse (an ugly gray-box screen that behaves exactly like the real
  thing will).
- Every step up in fidelity buys you a more trustworthy answer — and
  costs meaningfully more time and money to build. The spectrum, roughly:

| Fidelity            | What it is                                             | Build cost | What it's good at testing                                       | What it can't tell you                                       |
| ------------------- | ------------------------------------------------------ | ---------- | --------------------------------------------------------------- | ------------------------------------------------------------ |
| Paper sketch        | Hand-drawn screens, flipped by hand                    | Minutes    | Is the overall flow/order right? Is anything obviously missing? | Whether real text, real data, or real motion feels good      |
| Wireframe           | Static digital screens, no links                       | Hours      | Layout, information hierarchy, wording                          | Whether the flow between screens makes sense                 |
| Clickable prototype | Linked screens you can tap through (e.g. Figma)        | Days       | Whether the flow itself makes sense end to end                  | How an interaction actually _feels_ (drag, swipe, animation) |
| Coded prototype     | Real, running code — but scoped to answer one question | Weeks      | Real interaction feel, real performance, real data edge cases   | Nothing, at real cost — this is close to "just build it"     |

- **A prototype's entire job is to answer one question as cheaply as
  possible.** The team's real question — "does reordering the steps
  reduce abandonment?" — is a question about the _order and structure_
  of a flow, not about how a button's animation feels. A paper sketch or
  a clickable prototype can answer that question in a day. Nothing about
  it requires three weeks of engineering.
- **A feedback loop** is the cycle of showing a prototype to a real
  person, watching what they do or asking what they think, and using
  that to decide what to build (or fix) next. A cheap prototype makes
  that loop fast — you can run it several times in the time one coded
  build would take. An expensive prototype makes the loop slow, which
  means fewer chances to catch a wrong idea before it's expensive to
  fix.

In plain words:

| Term             | Simple meaning                                                           |
| ---------------- | ------------------------------------------------------------------------ |
| Prototype        | A stand-in for the real thing, built to get a reaction before committing |
| Fidelity         | How closely the stand-in resembles the finished product                  |
| Feedback loop    | Show it → watch/ask → decide what to change → repeat                     |
| Cheap validation | Getting a trustworthy-enough answer for the least possible cost          |

A team that skips straight to "build it for real" isn't being more
rigorous — it's usually just skipping the step where a wrong idea is
still cheap to catch.
