{
  "items": [
    {
      "type": "quiz",
      "id": "l1-def-1",
      "poolId": "l1-def",
      "level": 1,
      "prompt": "What's the core difference between responsive design and adaptive design?",
      "choices": [
        "Responsive uses colors, adaptive uses grayscale",
        "Responsive is one fluid layout that reflows continuously; adaptive is a small set of fixed layouts, each matched to a device class",
        "They're two names for the exact same technique",
        "Adaptive only works on mobile, responsive only works on desktop"
      ],
      "correctIndex": 1,
      "explanation": "Responsive design uses one layout with relative units and breakpoints that continuously reflows as width changes. Adaptive design picks among a small number of discrete, fixed layouts built for known device classes — there's no continuous reflow between them.",
      "reference": "<div class='grid grid-cols-2 gap-1.5 text-xs'><div class='rounded border border-slate-300 p-1.5 dark:border-slate-600'>🌊 Responsive: one fluid layout</div><div class='rounded border border-slate-300 p-1.5 dark:border-slate-600'>🧩 Adaptive: fixed layout set</div></div><p class='mt-2 text-xs text-slate-500 dark:text-slate-400'><strong>Why:</strong> responsive reflows continuously from actual width; adaptive matches the closest of a few pre-built templates.</p>"
    },
    {
      "type": "quiz",
      "id": "l1-def-2",
      "poolId": "l1-def",
      "level": 1,
      "prompt": "A site ships three completely separate templates — one for phone, one for tablet, one for desktop — and the server picks the closest match for each visitor. Which strategy is this?",
      "choices": [
        "Responsive design",
        "Adaptive design",
        "Fluid layout",
        "This isn't a real layout strategy"
      ],
      "correctIndex": 1,
      "explanation": "Picking among a small, fixed set of pre-built templates based on device class — with no continuous reflow between them — is the definition of adaptive design.",
      "reference": "<div class='grid grid-cols-2 gap-1.5 text-xs'><div class='rounded border border-slate-300 p-1.5 dark:border-slate-600'>🌊 Responsive: one fluid layout</div><div class='rounded border border-slate-300 p-1.5 dark:border-slate-600'>🧩 Adaptive: fixed layout set</div></div><p class='mt-2 text-xs text-slate-500 dark:text-slate-400'><strong>Why:</strong> responsive reflows continuously from actual width; adaptive matches the closest of a few pre-built templates.</p>"
    },
    {
      "type": "quiz",
      "id": "l1-def-3",
      "poolId": "l1-def",
      "level": 1,
      "prompt": "In responsive design, what decides how the layout looks at any given moment?",
      "choices": [
        "A designer manually picks the layout for each visitor",
        "The browser, continuously, based on the actual current viewport width",
        "The device's operating system",
        "A fixed layout chosen once at page load and never re-evaluated"
      ],
      "correctIndex": 1,
      "explanation": "Responsive layouts use relative units and breakpoints so the browser recomputes the layout continuously as width changes — there's no single moment where 'the layout' gets decided and locked in.",
      "reference": "<div class='grid grid-cols-2 gap-1.5 text-xs'><div class='rounded border border-slate-300 p-1.5 dark:border-slate-600'>🌊 Responsive: one fluid layout</div><div class='rounded border border-slate-300 p-1.5 dark:border-slate-600'>🧩 Adaptive: fixed layout set</div></div><p class='mt-2 text-xs text-slate-500 dark:text-slate-400'><strong>Why:</strong> responsive reflows continuously from actual width; adaptive matches the closest of a few pre-built templates.</p>"
    },
    {
      "type": "quiz",
      "id": "l1-breakpoint-1",
      "poolId": "l1-breakpoint",
      "level": 1,
      "prompt": "What is a 'breakpoint' in responsive design?",
      "choices": [
        "A bug that breaks the layout",
        "A viewport width where the layout's structure changes, e.g. three columns become two",
        "The maximum width a page is allowed to be",
        "A tool for debugging JavaScript"
      ],
      "correctIndex": 1,
      "explanation": "A breakpoint is a specific viewport width, written as an @media condition, where the arrangement of elements changes — not where every value changes continuously (that's fluid layout).",
      "reference": "<div class='not-prose text-xs text-slate-600 dark:text-slate-400'><code>@media (min-width: 768px) { ... }</code> — rules inside only apply once the viewport reaches that width.</div><p class='mt-2 text-xs text-slate-500 dark:text-slate-400'><strong>Why:</strong> a breakpoint marks where structure changes, not a general 'things get different' point.</p>"
    },
    {
      "type": "quiz",
      "id": "l1-breakpoint-2",
      "poolId": "l1-breakpoint",
      "level": 1,
      "prompt": "A three-column grid stays three columns from 320px all the way up to 3840px, with columns getting painfully narrow on phones. What's missing?",
      "choices": [
        "More images",
        "A breakpoint that collapses the grid to fewer columns at narrow widths",
        "A larger font size",
        "Nothing — three columns is always correct"
      ],
      "correctIndex": 1,
      "explanation": "Without a breakpoint restructuring the grid at narrow widths, the same column count gets forced into every viewport, crushing columns on phones. A breakpoint is exactly the tool for a discrete structural change like column count.",
      "reference": "<div class='not-prose text-xs text-slate-600 dark:text-slate-400'><code>@media (min-width: 768px) { ... }</code> — rules inside only apply once the viewport reaches that width.</div><p class='mt-2 text-xs text-slate-500 dark:text-slate-400'><strong>Why:</strong> a breakpoint marks where structure changes, not a general 'things get different' point.</p>"
    },
    {
      "type": "quiz",
      "id": "l1-fluid-1",
      "poolId": "l1-fluid",
      "level": 1,
      "prompt": "What does 'fluid layout' add on top of breakpoints?",
      "choices": [
        "A way to animate page transitions",
        "Sizes expressed as a formula relative to the viewport, so values change continuously instead of only jumping at fixed breakpoints",
        "A requirement to use only percentages, never pixels",
        "Automatic dark mode support"
      ],
      "correctIndex": 1,
      "explanation": "Fluid layout uses relative, formula-driven values (like clamp() with vw units) so a size can change smoothly across the whole range of widths, rather than staying constant until it jumps at a breakpoint.",
      "reference": "<div class='not-prose text-xs text-slate-600 dark:text-slate-400'>Breakpoint: value jumps at fixed widths. Fluid: value changes continuously with viewport width.</div><p class='mt-2 text-xs text-slate-500 dark:text-slate-400'><strong>Why:</strong> fluid layout is for continuous scale (type size, spacing), breakpoints are for discrete structure (column count).</p>"
    },
    {
      "type": "quiz",
      "id": "l1-fluid-2",
      "poolId": "l1-fluid",
      "level": 1,
      "prompt": "A heading is 24px at every width below 900px, then instantly jumps to 40px at 900px and stays there. Is this fluid sizing?",
      "choices": [
        "Yes, because the size does change eventually",
        "No — a fluid value changes continuously across widths; this is a single breakpoint jump, the opposite pattern",
        "Yes, fluid just means 'not always the same number'",
        "It depends on the font used"
      ],
      "correctIndex": 1,
      "explanation": "A single jump at one width is exactly what a breakpoint produces, not fluid sizing. Fluid sizing (e.g. via clamp() with vw units) changes the value smoothly across the range instead of holding it constant and then jumping.",
      "reference": "<div class='not-prose text-xs text-slate-600 dark:text-slate-400'>Breakpoint: value jumps at fixed widths. Fluid: value changes continuously with viewport width.</div><p class='mt-2 text-xs text-slate-500 dark:text-slate-400'><strong>Why:</strong> fluid layout is for continuous scale (type size, spacing), breakpoints are for discrete structure (column count).</p>"
    },
    {
      "type": "quiz",
      "id": "l1-fluid-3",
      "poolId": "l1-fluid",
      "level": 1,
      "prompt": "Which of these is a better candidate for fluid sizing rather than a breakpoint jump?",
      "choices": [
        "The number of grid columns in a product listing",
        "Whether a sidebar is visible or collapsed into a menu",
        "A heading's font size scaling smoothly between a phone and a desktop",
        "Whether a navigation bar is horizontal or a hamburger menu"
      ],
      "correctIndex": 2,
      "explanation": "Column count, sidebar visibility, and nav style are all genuinely discrete states (a sidebar is either visible or it isn't) — breakpoint territory. Font size can sensibly take any value in a continuous range, which is what fluid sizing is for.",
      "reference": "<div class='not-prose text-xs text-slate-600 dark:text-slate-400'>Breakpoint: value jumps at fixed widths. Fluid: value changes continuously with viewport width.</div><p class='mt-2 text-xs text-slate-500 dark:text-slate-400'><strong>Why:</strong> fluid layout is for continuous scale (type size, spacing), breakpoints are for discrete structure (column count).</p>"
    },
    {
      "type": "quiz",
      "id": "l1-compare-1",
      "poolId": "l1-compare",
      "level": 1,
      "prompt": "Per L1's comparison table, what's a key cost difference between responsive and adaptive design?",
      "choices": [
        "Responsive is always cheaper, no exceptions",
        "Adaptive means designing and maintaining several fixed layouts separately, while responsive designs once with more CSS complexity",
        "There's no cost difference between them",
        "Adaptive requires no CSS at all"
      ],
      "correctIndex": 1,
      "explanation": "Responsive design is authored once but with more complex, flexible CSS to handle the full range of widths. Adaptive design means each fixed layout is its own design/maintenance surface — often 3-6 of them.",
      "reference": "<table class='w-full border-collapse text-xs'><thead><tr><th class='border-b border-slate-300 pb-1 text-left dark:border-slate-600'></th><th class='border-b border-slate-300 pb-1 text-left dark:border-slate-600'>Responsive</th><th class='border-b border-slate-300 pb-1 text-left dark:border-slate-600'>Adaptive</th></tr></thead><tbody><tr><td class='py-1 pr-2'>Layouts</td><td class='py-1 pr-2'>One, flexible</td><td class='py-1 pr-2'>Several, fixed</td></tr><tr><td class='py-1 pr-2'>Decided by</td><td class='py-1 pr-2'>Browser, continuously</td><td class='py-1 pr-2'>Closest device match</td></tr></tbody></table>"
    },
    {
      "type": "quiz",
      "id": "l1-compare-2",
      "poolId": "l1-compare",
      "level": 1,
      "prompt": "A brand-new foldable device ships with a viewport width nobody had designed for. What happens under each strategy?",
      "choices": [
        "Both strategies fail identically",
        "Responsive reflows automatically since it's continuous; adaptive needs a new fixed layout or falls back awkwardly to the nearest match",
        "Adaptive reflows automatically; responsive needs a new fixed layout",
        "Neither strategy can ever handle a new device width"
      ],
      "correctIndex": 1,
      "explanation": "Because responsive layouts compute continuously from actual width, a new width is just another point on the same continuum. Adaptive design has no in-between state — a new width either falls back to the nearest existing fixed layout or needs a new one built.",
      "reference": "<table class='w-full border-collapse text-xs'><thead><tr><th class='border-b border-slate-300 pb-1 text-left dark:border-slate-600'></th><th class='border-b border-slate-300 pb-1 text-left dark:border-slate-600'>Responsive</th><th class='border-b border-slate-300 pb-1 text-left dark:border-slate-600'>Adaptive</th></tr></thead><tbody><tr><td class='py-1 pr-2'>Layouts</td><td class='py-1 pr-2'>One, flexible</td><td class='py-1 pr-2'>Several, fixed</td></tr><tr><td class='py-1 pr-2'>Decided by</td><td class='py-1 pr-2'>Browser, continuously</td><td class='py-1 pr-2'>Closest device match</td></tr></tbody></table>"
    },
    {
      "type": "quiz",
      "id": "l1-usecase-1",
      "poolId": "l1-usecase",
      "level": 1,
      "prompt": "Per L1, which is the more common default for modern websites?",
      "choices": [
        "Adaptive design, always",
        "Responsive design, reaching for adaptive only for a handful of very different experiences",
        "Neither is used anymore",
        "A coin flip decides per project"
      ],
      "correctIndex": 1,
      "explanation": "Most production sites today are responsive-first. Adaptive is reserved for cases where the experience itself should genuinely differ per context, not just the layout size.",
      "reference": "<div class='not-prose text-xs text-slate-600 dark:text-slate-400'>Modern default: responsive. Adaptive: reserved for print layouts, kiosk modes, email clients with poor CSS support.</div>"
    },
    {
      "type": "quiz",
      "id": "l1-usecase-2",
      "poolId": "l1-usecase",
      "level": 1,
      "prompt": "Which of these is the classic example of when adaptive design is still commonly chosen over responsive?",
      "choices": [
        "A typical marketing landing page",
        "An email template, because many email clients have poor or no support for modern CSS features",
        "A blog with a single article layout",
        "A personal portfolio site"
      ],
      "correctIndex": 1,
      "explanation": "Email clients frequently lack support for @media, @container, or clamp(), so email templates commonly ship as a small set of fixed, table-based layouts — a textbook adaptive case driven by platform constraints.",
      "reference": "<div class='not-prose text-xs text-slate-600 dark:text-slate-400'>Modern default: responsive. Adaptive: reserved for print layouts, kiosk modes, email clients with poor CSS support.</div>"
    },
    {
      "type": "quiz",
      "id": "l1-usecase-3",
      "poolId": "l1-usecase",
      "level": 1,
      "prompt": "A team building a public kiosk/point-of-sale screen wants a deliberately different interaction model (larger touch targets, different information density) than their phone app, not just a resized version of it. What does L1/L3 suggest this points toward?",
      "choices": [
        "This is exactly what responsive design is for",
        "This is a case where the experience itself should differ, not just the layout — closer to adaptive design's use case",
        "This should be solved with more breakpoints",
        "This is only solvable with fluid typography"
      ],
      "correctIndex": 1,
      "explanation": "When the interaction model and information density should genuinely differ per context — not just get resized — that's the adaptive case: a deliberately different, fixed experience for that context rather than one design reflowing.",
      "reference": "<div class='not-prose text-xs text-slate-600 dark:text-slate-400'>Modern default: responsive. Adaptive: reserved for print layouts, kiosk modes, email clients with poor CSS support.</div>"
    },
    {
      "type": "quiz",
      "id": "l2-content-decides-1",
      "poolId": "l2-content-decides",
      "level": 2,
      "prompt": "Per L2, where should a breakpoint actually go?",
      "choices": [
        "At standard device widths like 375px and 768px, always",
        "Wherever this specific design's own content starts to visibly strain, regardless of any device's width",
        "Exactly halfway between the narrowest and widest supported viewport",
        "Wherever the designer's monitor happens to be"
      ],
      "correctIndex": 1,
      "explanation": "A content-based breakpoint is chosen by resizing the actual layout until it visibly strains and putting the breakpoint there — which rarely lines up with a specific device's reported width and stays correct as new device sizes appear.",
      "reference": "<div class='not-prose text-xs text-slate-600 dark:text-slate-400'>Device-based breakpoints: match a device chart, risk breaking at the wrong point. Content-based: resize until it strains, put the breakpoint there.</div><p class='mt-2 text-xs text-slate-500 dark:text-slate-400'><strong>Why:</strong> a breakpoint should serve this design's content, not a device category.</p>"
    },
    {
      "type": "quiz",
      "id": "l2-content-decides-2",
      "poolId": "l2-content-decides",
      "level": 2,
      "prompt": "A team sets a breakpoint at 768px because it's 'the tablet width,' but their three-column layout actually crushes its columns at 820px on a real tablet in landscape. What went wrong?",
      "choices": [
        "Nothing — 768px is always correct for tablets",
        "The breakpoint was chosen from a device chart instead of from where this specific content actually strains",
        "The layout needs more columns, not a different breakpoint",
        "Tablets don't support breakpoints"
      ],
      "correctIndex": 1,
      "explanation": "768px matched a device category, not this layout's actual content. The right breakpoint is found by resizing until the specific design strains, not by trusting a device-width chart, which is why it landed 52px off.",
      "reference": "<div class='not-prose text-xs text-slate-600 dark:text-slate-400'>Device-based breakpoints: match a device chart, risk breaking at the wrong point. Content-based: resize until it strains, put the breakpoint there.</div><p class='mt-2 text-xs text-slate-500 dark:text-slate-400'><strong>Why:</strong> a breakpoint should serve this design's content, not a device category.</p>"
    },
    {
      "type": "quiz",
      "id": "l2-content-decides-3",
      "poolId": "l2-content-decides",
      "level": 2,
      "prompt": "Why is picking breakpoints from a device-width chart considered risky, per L2?",
      "choices": [
        "Device charts are always inaccurate",
        "New devices with different widths keep appearing, and a device's reported width isn't guaranteed to match what CSS measures once browser chrome or zoom is involved",
        "Devices don't actually have consistent widths",
        "It's not risky at all — it's the recommended approach"
      ],
      "correctIndex": 1,
      "explanation": "A device list is a moving target, and even a known device's CSS-measured width can differ from its marketing spec once browser chrome, zoom, or split-screen multitasking are factored in. Content-based breakpoints avoid depending on either.",
      "reference": "<div class='not-prose text-xs text-slate-600 dark:text-slate-400'>Device-based breakpoints: match a device chart, risk breaking at the wrong point. Content-based: resize until it strains, put the breakpoint there.</div><p class='mt-2 text-xs text-slate-500 dark:text-slate-400'><strong>Why:</strong> a breakpoint should serve this design's content, not a device category.</p>"
    },
    {
      "type": "quiz",
      "id": "l2-mobile-first-1",
      "poolId": "l2-mobile-first",
      "level": 2,
      "prompt": "In mobile-first CSS, what do base (no media query) styles target, and what do breakpoints do?",
      "choices": [
        "Base styles target the widest screen; breakpoints remove features as space shrinks",
        "Base styles target the narrowest case; each breakpoint adds capability as more space becomes available",
        "Base styles target a specific device; breakpoints are unrelated to width",
        "There's no meaningful difference from desktop-first"
      ],
      "correctIndex": 1,
      "explanation": "Mobile-first means the unqualified base rules are written for the narrowest case, and min-width breakpoints layer on additional capability as space increases — an additive cascade.",
      "reference": "<div class='not-prose text-xs text-slate-600 dark:text-slate-400'>Mobile-first (min-width): base = narrow, breakpoints add. Desktop-first (max-width): base = wide, breakpoints subtract.</div><p class='mt-2 text-xs text-slate-500 dark:text-slate-400'><strong>Why:</strong> additive overrides are easier to reason about as more breakpoints get added over time.</p>"
    },
    {
      "type": "quiz",
      "id": "l2-mobile-first-2",
      "poolId": "l2-mobile-first",
      "level": 2,
      "prompt": "Which media query syntax is characteristic of a mobile-first approach?",
      "choices": [
        "@media (max-width: ...) exclusively",
        "@media (min-width: ...), layering rules on as the viewport grows",
        "@media (device-type: mobile)",
        "No media queries at all"
      ],
      "correctIndex": 1,
      "explanation": "min-width queries only apply once the viewport reaches or exceeds that width, matching mobile-first's additive, 'add capability as space appears' model.",
      "reference": "<div class='not-prose text-xs text-slate-600 dark:text-slate-400'>Mobile-first (min-width): base = narrow, breakpoints add. Desktop-first (max-width): base = wide, breakpoints subtract.</div><p class='mt-2 text-xs text-slate-500 dark:text-slate-400'><strong>Why:</strong> additive overrides are easier to reason about as more breakpoints get added over time.</p>"
    },
    {
      "type": "quiz",
      "id": "l2-mobile-first-3",
      "poolId": "l2-mobile-first",
      "level": 2,
      "prompt": "Per L2, why is mobile-first the dominant convention today, beyond mobile traffic being the majority case?",
      "choices": [
        "Because max-width queries don't work in most browsers",
        "Because it keeps overrides additive (turning features on) rather than subtractive (turning them off), which stays easier to reason about as more breakpoints accumulate",
        "Because it requires less CSS overall in every case",
        "Because desktop-first is a deprecated, unsupported technique"
      ],
      "correctIndex": 1,
      "explanation": "Additive cascades (mobile-first) stay easier to reason about as a page grows more breakpoints, since each new rule only adds capability. Subtractive cascades (desktop-first) require every narrower rule to correctly undo wide-screen assumptions.",
      "reference": "<div class='not-prose text-xs text-slate-600 dark:text-slate-400'>Mobile-first (min-width): base = narrow, breakpoints add. Desktop-first (max-width): base = wide, breakpoints subtract.</div><p class='mt-2 text-xs text-slate-500 dark:text-slate-400'><strong>Why:</strong> additive overrides are easier to reason about as more breakpoints get added over time.</p>"
    },
    {
      "type": "quiz",
      "id": "l2-clamp-syntax-1",
      "poolId": "l2-clamp-syntax",
      "level": 2,
      "prompt": "What do the three arguments of CSS clamp(min, preferred, max) mean?",
      "choices": [
        "Three separate colors to choose between",
        "A floor value, a formula (often using vw) that scales with viewport, and a ceiling value",
        "Three fixed breakpoints in pixels",
        "The minimum, average, and maximum number of columns"
      ],
      "correctIndex": 1,
      "explanation": "clamp() picks the 'preferred' middle value unless it would fall below min or above max, in which case it's clamped to that bound. The preferred term is typically a formula using vw units so it scales continuously with viewport width.",
      "reference": "<div class='not-prose text-xs text-slate-600 dark:text-slate-400'><code>font-size: clamp(1.5rem, 1rem + 3vw, 3rem);</code> — never below 1.5rem, never above 3rem, scales in between.</div><p class='mt-2 text-xs text-slate-500 dark:text-slate-400'><strong>Why:</strong> clamp() replaces a breakpoint jump with a continuous formula between a floor and a ceiling.</p>"
    },
    {
      "type": "quiz",
      "id": "l2-clamp-syntax-2",
      "poolId": "l2-clamp-syntax",
      "level": 2,
      "prompt": "font-size: clamp(1.5rem, 1rem + 3vw, 3rem) is applied. At a very narrow viewport where '1rem + 3vw' evaluates to 1.2rem, what font size is actually used?",
      "choices": [
        "1.2rem, exactly as computed",
        "1.5rem — the formula's result is below the floor, so it clamps to the minimum",
        "3rem, always the maximum",
        "0rem — the text disappears"
      ],
      "correctIndex": 1,
      "explanation": "clamp() never lets the preferred value go below the minimum bound. Since 1.2rem is under the 1.5rem floor, the actual rendered size clamps up to 1.5rem.",
      "reference": "<div class='not-prose text-xs text-slate-600 dark:text-slate-400'><code>font-size: clamp(1.5rem, 1rem + 3vw, 3rem);</code> — never below 1.5rem, never above 3rem, scales in between.</div><p class='mt-2 text-xs text-slate-500 dark:text-slate-400'><strong>Why:</strong> clamp() replaces a breakpoint jump with a continuous formula between a floor and a ceiling.</p>"
    },
    {
      "type": "quiz",
      "id": "l2-clamp-syntax-3",
      "poolId": "l2-clamp-syntax",
      "level": 2,
      "prompt": "Why does the 'preferred' term of a clamp() formula typically include a vw unit rather than being a fixed rem value?",
      "choices": [
        "vw units are required by the CSS spec inside clamp()",
        "Because vw is a percentage of viewport width, so the formula's output changes continuously as the viewport resizes, instead of staying constant",
        "vw units render faster than rem units",
        "There's no real reason, it's just convention"
      ],
      "correctIndex": 1,
      "explanation": "1vw equals 1% of viewport width, so a formula like '1rem + 3vw' produces a different value at every viewport width. A fixed rem value wouldn't change at all, defeating the point of fluid sizing.",
      "reference": "<div class='not-prose text-xs text-slate-600 dark:text-slate-400'><code>font-size: clamp(1.5rem, 1rem + 3vw, 3rem);</code> — never below 1.5rem, never above 3rem, scales in between.</div><p class='mt-2 text-xs text-slate-500 dark:text-slate-400'><strong>Why:</strong> clamp() replaces a breakpoint jump with a continuous formula between a floor and a ceiling.</p>"
    },
    {
      "type": "quiz",
      "id": "l2-tradeoff-1",
      "poolId": "l2-tradeoff",
      "level": 2,
      "prompt": "Per L2, when should a value use a breakpoint instead of fluid clamp() sizing?",
      "choices": [
        "Never — clamp() should replace all breakpoints",
        "When the value is genuinely discrete, like a grid's column count, which can't sensibly be '2.4 columns'",
        "Only for colors",
        "Only on mobile viewports"
      ],
      "correctIndex": 1,
      "explanation": "Breakpoints suit genuinely discrete states — a grid has 1, 2, or 4 columns, never a fractional in-between value. Fluid sizing suits values that can meaningfully take any number in a range, like font size.",
      "reference": "<table class='w-full border-collapse text-xs'><thead><tr><th class='border-b border-slate-300 pb-1 text-left dark:border-slate-600'></th><th class='border-b border-slate-300 pb-1 text-left dark:border-slate-600'>Breakpoint-only</th><th class='border-b border-slate-300 pb-1 text-left dark:border-slate-600'>Fluid (clamp)</th></tr></thead><tbody><tr><td class='py-1 pr-2'>Between breakpoints</td><td class='py-1 pr-2'>Constant, jumps</td><td class='py-1 pr-2'>Changes continuously</td></tr><tr><td class='py-1 pr-2'>Best for</td><td class='py-1 pr-2'>Structure</td><td class='py-1 pr-2'>Continuous scale</td></tr></tbody></table>"
    },
    {
      "type": "quiz",
      "id": "l2-tradeoff-2",
      "poolId": "l2-tradeoff",
      "level": 2,
      "prompt": "A real production page typically uses both breakpoints and fluid clamp() values together. Why isn't one enough on its own?",
      "choices": [
        "Using both is redundant and should be avoided",
        "Because a page usually has both discrete structural decisions (column count, nav style) and continuous scale decisions (font size, spacing) that call for different tools",
        "Because clamp() doesn't work without at least one breakpoint present",
        "Because breakpoints are being deprecated in favor of clamp() entirely"
      ],
      "correctIndex": 1,
      "explanation": "Structural changes (like column count) are discrete and suit breakpoints; scale changes (like font size) are continuous and suit clamp(). Most real pages have both kinds of decisions, so both tools are used together, not as substitutes.",
      "reference": "<table class='w-full border-collapse text-xs'><thead><tr><th class='border-b border-slate-300 pb-1 text-left dark:border-slate-600'></th><th class='border-b border-slate-300 pb-1 text-left dark:border-slate-600'>Breakpoint-only</th><th class='border-b border-slate-300 pb-1 text-left dark:border-slate-600'>Fluid (clamp)</th></tr></thead><tbody><tr><td class='py-1 pr-2'>Between breakpoints</td><td class='py-1 pr-2'>Constant, jumps</td><td class='py-1 pr-2'>Changes continuously</td></tr><tr><td class='py-1 pr-2'>Best for</td><td class='py-1 pr-2'>Structure</td><td class='py-1 pr-2'>Continuous scale</td></tr></tbody></table>"
    },
    {
      "type": "quiz",
      "id": "l2-minmax-1",
      "poolId": "l2-minmax",
      "level": 2,
      "prompt": "What's the practical difference between writing a rule inside @media (min-width: 768px) versus @media (max-width: 768px)?",
      "choices": [
        "They're identical — min-width and max-width are just alternate spellings",
        "min-width applies the rule once the viewport reaches or exceeds 768px; max-width applies it while the viewport is at or below 768px — opposite directions",
        "min-width only works on mobile, max-width only works on desktop",
        "max-width is deprecated and no longer supported"
      ],
      "correctIndex": 1,
      "explanation": "min-width is a floor — the rule activates at that width and above, which is why it pairs with mobile-first's additive model. max-width is a ceiling — the rule activates at that width and below, pairing with desktop-first's subtractive model.",
      "reference": "<div class='not-prose text-xs text-slate-600 dark:text-slate-400'><code>min-width: 768px</code> → applies at 768px and above. <code>max-width: 768px</code> → applies at 768px and below.</div><p class='mt-2 text-xs text-slate-500 dark:text-slate-400'><strong>Why:</strong> the two point in opposite directions, which is what makes mobile-first vs. desktop-first genuinely different cascades, not just different syntax.</p>"
    },
    {
      "type": "quiz",
      "id": "l2-minmax-2",
      "poolId": "l2-minmax",
      "level": 2,
      "prompt": "A stylesheet mixes @media (min-width: 640px) rules and @media (max-width: 900px) rules for the same element, in the same project. What's the risk?",
      "choices": [
        "No risk — mixing the two directions is always safe",
        "Between 640px and 900px, both rule sets can be active at once, and source order (not clear intent) decides which wins — harder to reason about than committing to one direction",
        "The browser will refuse to apply either rule",
        "This automatically triggers a fallback to adaptive design"
      ],
      "correctIndex": 1,
      "explanation": "Mixing min-width and max-width breakpoints for the same element means their ranges can overlap, and which rule ultimately applies depends on CSS source order rather than a clear 'narrow vs. wide' intent — this is exactly why picking one direction (mobile-first, in most modern projects) and staying consistent matters.",
      "reference": "<div class='not-prose text-xs text-slate-600 dark:text-slate-400'><code>min-width: 768px</code> → applies at 768px and above. <code>max-width: 768px</code> → applies at 768px and below.</div><p class='mt-2 text-xs text-slate-500 dark:text-slate-400'><strong>Why:</strong> the two point in opposite directions, which is what makes mobile-first vs. desktop-first genuinely different cascades, not just different syntax.</p>"
    },
    {
      "type": "quiz",
      "id": "l3-container-1",
      "poolId": "l3-container",
      "level": 3,
      "prompt": "Why can't a @media breakpoint alone handle a product card that needs to switch to a vertical layout only when a sidebar opens and eats its horizontal space?",
      "choices": [
        "@media queries can only be used once per page",
        "@media only measures the viewport, never the width of the specific element being styled — so it can't tell a wide-viewport/narrow-card case from a wide-viewport/wide-card case",
        "Cards can never be styled with @media",
        "@media queries don't support flexbox layouts"
      ],
      "correctIndex": 1,
      "explanation": "A card that's 220px wide because a sidebar opened, and a card that's 220px wide on a narrow phone viewport, look identical to @media — both only see the viewport width, not the card's actual box width.",
      "reference": "<div class='not-prose text-xs text-slate-600 dark:text-slate-400'><code>@container card (min-width: 360px) { ... }</code> — reacts to the card's own box, not the viewport.</div><p class='mt-2 text-xs text-slate-500 dark:text-slate-400'><strong>Why:</strong> container queries fix the case where a component's context (not the viewport) changes its available width.</p>"
    },
    {
      "type": "quiz",
      "id": "l3-container-2",
      "poolId": "l3-container",
      "level": 3,
      "prompt": "What does a CSS container query (@container) measure, that a media query (@media) does not?",
      "choices": [
        "The user's operating system",
        "The width of a specific named containing element's box, rather than the browser viewport",
        "The number of colors on the page",
        "The device's pixel density only"
      ],
      "correctIndex": 1,
      "explanation": "container-type: inline-size marks an element as a query container; @container then measures that specific box's width, which can shrink or grow independently of the viewport (e.g. when a sidebar opens).",
      "reference": "<div class='not-prose text-xs text-slate-600 dark:text-slate-400'><code>@container card (min-width: 360px) { ... }</code> — reacts to the card's own box, not the viewport.</div><p class='mt-2 text-xs text-slate-500 dark:text-slate-400'><strong>Why:</strong> container queries fix the case where a component's context (not the viewport) changes its available width.</p>"
    },
    {
      "type": "quiz",
      "id": "l3-container-3",
      "poolId": "l3-container",
      "level": 3,
      "prompt": "A card component is reused in a sidebar-adjacent grid, a full-width 'related products' row, and a narrow modal — all on the exact same page viewport. Which query type correctly lets the card adapt to each placement?",
      "choices": [
        "@media, since the viewport is what matters",
        "@container, since it's the card's own box width that differs across placements, not the viewport",
        "Neither — this requires JavaScript",
        "@page"
      ],
      "correctIndex": 1,
      "explanation": "Since the viewport doesn't change across these three placements but the card's own available width does, only a container query (measuring the card's box) can distinguish between them — a media query would see identical conditions in all three.",
      "reference": "<div class='not-prose text-xs text-slate-600 dark:text-slate-400'><code>@container card (min-width: 360px) { ... }</code> — reacts to the card's own box, not the viewport.</div><p class='mt-2 text-xs text-slate-500 dark:text-slate-400'><strong>Why:</strong> container queries fix the case where a component's context (not the viewport) changes its available width.</p>"
    },
    {
      "type": "quiz",
      "id": "l3-clamp-math-1",
      "poolId": "l3-clamp-math",
      "level": 3,
      "prompt": "A heading should be 32px at a 500px viewport and 56px at a 1300px viewport. Following L3's worked recipe, what's the slope (change in px per 1px of viewport width)?",
      "choices": [
        "(56 - 32) / (1300 - 500) = 24 / 800 = 0.03",
        "(56 + 32) / (1300 + 500) = 0.048",
        "56 / 1300 = 0.043",
        "There's no way to compute this without a design tool"
      ],
      "correctIndex": 0,
      "explanation": "Slope is (target max - target min) / (viewport max - viewport min) = (56 - 32) / (1300 - 500) = 24 / 800 = 0.03px of font size per 1px of viewport width, which becomes 3vw in the formula (0.03 * 100).",
      "reference": "<div class='not-prose text-xs text-slate-600 dark:text-slate-400'>Recipe: slope = (max - min) / (vwMax - vwMin); intercept = min - slope * vwMin; result: <code>clamp(min, intercept + slope*100 + 'vw', max)</code>.</div><p class='mt-2 text-xs text-slate-500 dark:text-slate-400'><strong>Why:</strong> this turns 'A at width W1, B at width W2' into an exact clamp() line.</p>"
    },
    {
      "type": "quiz",
      "id": "l3-clamp-math-2",
      "poolId": "l3-clamp-math",
      "level": 3,
      "prompt": "Using the same target (32px at 500px viewport, 56px at 1300px viewport, slope 0.03px/px = 3vw), what is the intercept term (the value the formula predicts at 0vw)?",
      "choices": [
        "32 - 0.03 * 500 = 32 - 15 = 17px",
        "56 - 0.03 * 1300 = 17px (same answer via the other point, confirming the line)",
        "Both A and B are correct — either point on the line gives the same intercept",
        "0px, since clamp() formulas always start at zero"
      ],
      "correctIndex": 2,
      "explanation": "The intercept is where the line would cross 0vw, and since it's the same line, computing it from either known point gives the same answer: 32 - 0.03*500 = 17, and 56 - 0.03*1300 = 17. Both calculations confirming each other is exactly how you'd sanity-check the formula.",
      "reference": "<div class='not-prose text-xs text-slate-600 dark:text-slate-400'>Recipe: slope = (max - min) / (vwMax - vwMin); intercept = min - slope * vwMin; result: <code>clamp(min, intercept + slope*100 + 'vw', max)</code>.</div><p class='mt-2 text-xs text-slate-500 dark:text-slate-400'><strong>Why:</strong> this turns 'A at width W1, B at width W2' into an exact clamp() line.</p>"
    },
    {
      "type": "quiz",
      "id": "l3-clamp-math-3",
      "poolId": "l3-clamp-math",
      "level": 3,
      "prompt": "A clamp() recipe is applied where the minimum and maximum are set to the exact same value (e.g. clamp(15px, 15px, 15px)). What happens?",
      "choices": [
        "The value oscillates unpredictably",
        "The value is fixed at that number at every viewport width — the slope becomes irrelevant since it can never legally exceed a floor and ceiling that are identical",
        "This causes a CSS parse error",
        "It behaves exactly like a normal fluid clamp() with visible scaling"
      ],
      "correctIndex": 1,
      "explanation": "When min equals max, the result is clamped to that single value everywhere. A fixed value would produce the identical result with less to read — reaching for clamp() here adds unneeded complexity rather than solving a real scaling need.",
      "reference": "<div class='not-prose text-xs text-slate-600 dark:text-slate-400'>Recipe: slope = (max - min) / (vwMax - vwMin); intercept = min - slope * vwMin; result: <code>clamp(min, intercept + slope*100 + 'vw', max)</code>.</div><p class='mt-2 text-xs text-slate-500 dark:text-slate-400'><strong>Why:</strong> this turns 'A at width W1, B at width W2' into an exact clamp() line.</p>"
    },
    {
      "type": "quiz",
      "id": "l3-4k-cap-1",
      "poolId": "l3-4k-cap",
      "level": 3,
      "prompt": "A responsive grid keeps adding/stretching columns as the viewport grows, with no upper limit. What happens on a 3840px 4K monitor, and what fixes it?",
      "choices": [
        "Nothing happens, responsive design has no upper limit problem",
        "Content stretches uncomfortably wide (e.g. product photos scaled comically large); a max-width on the wrapping container plus centering fixes it",
        "The browser automatically caps width at 1920px",
        "This can only be fixed by switching to adaptive design entirely"
      ],
      "correctIndex": 1,
      "explanation": "Without a cap, a responsive layout keeps filling available width indefinitely, which reads as stretched-thin on very wide screens. A max-width on the wrapper plus margin-inline: auto centers the content and stops it from over-stretching, while still remaining a single responsive layout.",
      "reference": "<div class='not-prose text-xs text-slate-600 dark:text-slate-400'><code>.listing-wrap { max-width: 1600px; margin-inline: auto; }</code></div><p class='mt-2 text-xs text-slate-500 dark:text-slate-400'><strong>Why:</strong> 'responsive' should mean 'adapts within a sensible range,' not 'always fills 100% of any width.'</p>"
    },
    {
      "type": "quiz",
      "id": "l3-4k-cap-2",
      "poolId": "l3-4k-cap",
      "level": 3,
      "prompt": "Why is an unbounded-stretch bug (missing max-width) easy to miss during typical testing?",
      "choices": [
        "It's actually easy to catch on any normal laptop screen",
        "It only shows up at very wide viewports (ultra-wide/4K), which a normal laptop-width test pass doesn't cover",
        "It only affects mobile devices",
        "It's a server-side bug, not a CSS one"
      ],
      "correctIndex": 1,
      "explanation": "Testing only up to a typical laptop width (say 1440px) never triggers this bug — it only appears once the viewport is wide enough that the grid runs out of columns to add and starts stretching existing content instead.",
      "reference": "<div class='not-prose text-xs text-slate-600 dark:text-slate-400'><code>.listing-wrap { max-width: 1600px; margin-inline: auto; }</code></div><p class='mt-2 text-xs text-slate-500 dark:text-slate-400'><strong>Why:</strong> 'responsive' should mean 'adapts within a sensible range,' not 'always fills 100% of any width.'</p>"
    },
    {
      "type": "quiz",
      "id": "l3-a11y-reflow-1",
      "poolId": "l3-a11y-reflow",
      "level": 3,
      "prompt": "What does WCAG 1.4.10 (Reflow) actually require?",
      "choices": [
        "That every page support at least 3 breakpoints",
        "That content remain usable without horizontal scrolling when zoomed to 400%, on a viewport equivalent to 320px CSS pixels wide",
        "That all text use a minimum 16px font size",
        "That pages load in under 3 seconds"
      ],
      "correctIndex": 1,
      "explanation": "1.4.10 is a specific, testable requirement: at 400% zoom on an effective 320px-wide viewport, content must reflow to remain usable without needing to scroll horizontally (with limited exceptions like data tables).",
      "reference": "<div class='not-prose text-xs text-slate-600 dark:text-slate-400'>WCAG 1.4.10: no horizontal scroll needed at 400% zoom on a 320px-equivalent viewport.</div><p class='mt-2 text-xs text-slate-500 dark:text-slate-400'><strong>Why:</strong> a layout that looks fine at 100% zoom can still fail this real, testable accessibility floor.</p>"
    },
    {
      "type": "quiz",
      "id": "l3-a11y-reflow-2",
      "poolId": "l3-a11y-reflow",
      "level": 3,
      "prompt": "A page looks perfect at 100% zoom on every device tested. Why might it still fail an accessibility check?",
      "choices": [
        "It can't — 100% zoom testing across devices is sufficient",
        "Because reflow requirements (WCAG 1.4.10) test zoomed-in width (400% zoom on a 320px-equivalent viewport), which is a different, narrower effective width than any physical device test covers",
        "Because accessibility checks only care about color contrast",
        "Because zoom level has no relationship to layout width"
      ],
      "correctIndex": 1,
      "explanation": "Zooming to 400% effectively shrinks the usable viewport width dramatically — testing only physical device widths at 100% zoom never exercises this narrower effective width, which is exactly where a clamp() floor set too high can still fail.",
      "reference": "<div class='not-prose text-xs text-slate-600 dark:text-slate-400'>WCAG 1.4.10: no horizontal scroll needed at 400% zoom on a 320px-equivalent viewport.</div><p class='mt-2 text-xs text-slate-500 dark:text-slate-400'><strong>Why:</strong> a layout that looks fine at 100% zoom can still fail this real, testable accessibility floor.</p>"
    },
    {
      "type": "quiz",
      "id": "l3-when-adaptive-1",
      "poolId": "l3-when-adaptive",
      "level": 3,
      "prompt": "Per L3, what's the deeper signal that a case genuinely calls for adaptive design rather than 'just add more breakpoints' to a responsive page?",
      "choices": [
        "The page has more than 3 breakpoints already",
        "The content and interaction model should genuinely differ per context (not just resize), e.g. a warehouse tablet needing larger tap targets and fewer decorative images than a consumer phone app",
        "The page uses clamp() anywhere",
        "The team prefers writing separate templates"
      ],
      "correctIndex": 1,
      "explanation": "More breakpoints only helps when it's still the same design reflowing. When the actual content, density, or interaction model needs to differ by context — not just get resized — that's a sign the honest answer is a separate, adaptive template, not another breakpoint.",
      "reference": "<div class='not-prose text-xs text-slate-600 dark:text-slate-400'>Adaptive still wins for: email clients (CSS support), kiosk/POS (different interaction model), print (content stripped, not reflowed).</div><p class='mt-2 text-xs text-slate-500 dark:text-slate-400'><strong>Why:</strong> the judgment call is recognizing when it's no longer the same design at all.</p>"
    },
    {
      "type": "quiz",
      "id": "l3-when-adaptive-2",
      "poolId": "l3-when-adaptive",
      "level": 3,
      "prompt": "Why is a dedicated print stylesheet (@media print) described as 'adaptive in spirit, even though implemented with the responsive toolset'?",
      "choices": [
        "Because print stylesheets don't use CSS at all",
        "Because the print layout typically strips navigation and interactive elements entirely rather than reflowing them, which is closer to a discrete experience swap than a continuous reflow",
        "Because @media print requires a completely separate HTML file",
        "Because print stylesheets can't use breakpoints"
      ],
      "correctIndex": 1,
      "explanation": "Even though @media is the responsive mechanism, a print stylesheet's actual behavior — removing interactive chrome outright rather than reflowing it — matches adaptive design's spirit: a deliberately different, fixed experience for that context.",
      "reference": "<div class='not-prose text-xs text-slate-600 dark:text-slate-400'>Adaptive still wins for: email clients (CSS support), kiosk/POS (different interaction model), print (content stripped, not reflowed).</div><p class='mt-2 text-xs text-slate-500 dark:text-slate-400'><strong>Why:</strong> the judgment call is recognizing when it's no longer the same design at all.</p>"
    },
    {
      "type": "quiz",
      "id": "l3-testing-1",
      "poolId": "l3-testing",
      "level": 3,
      "prompt": "Testing a layout at exactly 375px, 768px, and 1440px (three fixed presets) missed a bug that only appears at 820px. What testing practice would have caught it?",
      "choices": [
        "Testing at even more fixed presets, like 400 and 900",
        "Dragging the browser width continuously rather than only checking fixed preset widths",
        "Only testing on real physical devices",
        "Switching to adaptive design so presets aren't needed"
      ],
      "correctIndex": 1,
      "explanation": "Any finite set of fixed presets can straddle a real problem width. Continuously dragging the viewport width exposes the exact pixel range where a breakpoint is set wrong, which spot-checking fixed presets can miss entirely.",
      "reference": "<table class='w-full border-collapse text-xs'><thead><tr><th class='border-b border-slate-300 pb-1 text-left dark:border-slate-600'>Check</th><th class='border-b border-slate-300 pb-1 text-left dark:border-slate-600'>Catches</th></tr></thead><tbody><tr><td class='py-1 pr-2 align-top'>Drag width continuously</td><td class='py-1 pr-2 align-top'>Wrong breakpoint pixel range</td></tr><tr><td class='py-1 pr-2 align-top'>Zoom to 400%</td><td class='py-1 pr-2 align-top'>WCAG 1.4.10 reflow failures</td></tr><tr><td class='py-1 pr-2 align-top'>Widest realistic viewport</td><td class='py-1 pr-2 align-top'>Unbounded-stretch bugs</td></tr></tbody></table>"
    },
    {
      "type": "quiz",
      "id": "l3-testing-2",
      "poolId": "l3-testing",
      "level": 3,
      "prompt": "Why does testing a reusable card component only at the page's narrowest and widest viewport miss real bugs, per L3?",
      "choices": [
        "It doesn't miss anything — viewport testing is sufficient for every component",
        "Because a container-query-driven component's layout depends on its own container's width, which can differ from the page viewport (e.g. sidebar open vs. closed) — testing only viewport extremes skips those in-between container widths",
        "Because components can't be tested independently of the full page",
        "Because card components never use container queries"
      ],
      "correctIndex": 1,
      "explanation": "A component's actual rendered width can vary with its container context, independent of the page viewport. Testing only page-viewport extremes can entirely skip the specific container widths where a @container breakpoint bug would show up.",
      "reference": "<table class='w-full border-collapse text-xs'><thead><tr><th class='border-b border-slate-300 pb-1 text-left dark:border-slate-600'>Check</th><th class='border-b border-slate-300 pb-1 text-left dark:border-slate-600'>Catches</th></tr></thead><tbody><tr><td class='py-1 pr-2 align-top'>Drag width continuously</td><td class='py-1 pr-2 align-top'>Wrong breakpoint pixel range</td></tr><tr><td class='py-1 pr-2 align-top'>Zoom to 400%</td><td class='py-1 pr-2 align-top'>WCAG 1.4.10 reflow failures</td></tr><tr><td class='py-1 pr-2 align-top'>Widest realistic viewport</td><td class='py-1 pr-2 align-top'>Unbounded-stretch bugs</td></tr></tbody></table>"
    }
  ]
}
