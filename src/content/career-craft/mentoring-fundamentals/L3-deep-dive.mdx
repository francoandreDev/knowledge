---
title: "L3 — The full conversation, both ways, and what changes when it can't go slow"
---

import Scenario from "../../../components/Scenario.astro";

If you do not know testing yet, read the bug like this: a fake tool used
for practice was not cleaned after the first practice run, so the second
run saw leftover information. The exact `beforeEach` syntax matters less
than the mentoring question: did Sam learn how to notice "leftover state"
next time?

<Scenario label="Same bug, two conversations">
  <Fragment slot="facts">
    <div class="not-prose flex flex-col gap-2 text-sm text-slate-700 dark:text-slate-300">
      <div class="flex items-center gap-1.5"><span>🐛</span> <strong>The bug</strong> — a stale mock isn't reset between test cases</div>
      <div class="flex items-center gap-1.5"><span>🧑‍💻</span> <strong>Sam</strong> — has tried three things, all variations on "add another assertion"</div>
      <div class="flex items-center gap-1.5"><span>🕐</span> <strong>Time cost</strong> — Path A: ~5 min. Path B: ~25 min, this time.</div>
    </div>
  </Fragment>

**Before reading either conversation: what would you expect Sam to be able
to do, unassisted, the next time a stale-mock bug shows up — after Path A,
versus after Path B?**

</Scenario>

## Path A — Priya fixes it

> **Priya:** "Oh, I see it — this mock isn't getting reset between test
> cases, so test two is picking up test one's leftover state. Here, add a
> `beforeEach` that resets it."
>
> **Sam:** "Oh — got it, thanks!" _(pastes it in, test passes, moves on)_

Five minutes. The test passes. Sam did not form a hypothesis, test it, or
see it fail — the entire diagnostic process happened inside Priya's head
and only the conclusion was transferred. **Nothing Sam does differently on
the next bug** — there is no reason the same stale-mock pattern will be any
more visible to Sam next time than it was this time.

## Path B — Priya asks, and waits

> **Priya:** "Okay, walk me through it — what does the test expect to
> happen, versus what's actually happening?"
>
> **Sam:** "It expects the mock to return `null` on the second call, but
> it's still returning the value from the first call..." _(pauses)_ "...
> wait, is the mock not getting reset somewhere?"
>
> **Priya:** "That's exactly the right question. Where would you check?"
>
> **Sam:** _(finds the test setup, notices there's no `beforeEach` at
> all)_ "There's no reset between tests — so test two is just seeing
> whatever test one left behind."
>
> **Priya:** "Right. What would fix that?"
>
> **Sam:** _(writes a `beforeEach` resetting the mock)_ "This should do
> it... yeah, tests pass."

Twenty-five minutes. Sam didn't just receive the fix — Sam re-derived the
specific chain of reasoning ("what's expected vs. actual" → "state is
leaking somewhere" → "check the setup" → "there's no reset") that finds
this entire _class_ of bug, not just this one instance of it. Priya's three
questions did the minimum necessary redirecting — she never said "it's the
mock reset," she asked the question whose honest answer led there.

## What Priya actually did — a diagnostic question, not a hint

Notice what Priya's first question was _not_: it wasn't "have you checked
if the mock resets?" — that would have handed Sam the conclusion disguised
as a question. "What does the test expect, versus what's actually
happening?" is a genuinely open diagnostic question — it works for _any_
bug, not just this one, which is exactly why it builds a reusable skill
instead of a memorized fact about mocks specifically.

| Priya's question                                            | What it does                          | What it avoids                          |
| ----------------------------------------------------------- | ------------------------------------- | --------------------------------------- |
| "What does the test expect vs. what's happening?"           | Forces Sam to state the gap precisely | Doesn't point at the mock at all        |
| "That's exactly the right question. Where would you check?" | Confirms progress, redirects effort   | Doesn't say "check the setup file"      |
| "What would fix that?"                                      | Hands the actual fix-writing to Sam   | Doesn't write the `beforeEach` for them |

## Extend the scenario: what if it can't go slow?

**Everything above assumes Priya has 25 minutes to spend. What happens to
this framework when the answer is genuinely needed in the next five
minutes — a production incident, a demo starting shortly?**

Real urgency is a legitimate reason to move down the gradient toward
"I do, you watch," even for a mentee who's normally at "you do, I watch"
for this kind of problem. A **production incident** means real users are
affected right now; a **demo** means a presentation is about to depend on
the thing working. The fix is: **narrate the reasoning out loud while
doing it**, rather than silently fixing it the way Path A did. "I'm
checking the setup file because the symptom looks like state leaking
between tests" transfers the diagnostic reasoning even when there's no
time for Sam to drive. The distinction that matters isn't "did the mentor
do the work" — it's "did any of the reasoning get transferred, or only the
conclusion."

**A second extension, harder than urgency: what should Priya do if this
exact class of bug — a missing test reset — is the third time Sam has hit
it in a month?** A recurring pattern across multiple incidents is a
different signal than a single stuck moment — it suggests the gap isn't
"hasn't derived this specific fix yet" but "doesn't yet have a general
mental model for test isolation at all." The right response shifts from a
single diagnostic question in the moment to a short, dedicated
conversation about the underlying concept — closer to stage 1 or 2 of the
gradient, deliberately, rather than repeating stage 3 a fourth time on a
mentee who isn't actually building toward independence on this topic yet.

Non-technical transfer: imagine Sam is preparing a short presentation and
keeps making the same unclear opening. Path A is Priya rewriting the first
slide. Path B is Priya asking, "What should the audience understand in the
first thirty seconds, and which sentence says that?" If Sam answers and
rewrites it, the slide improves and Sam learns a reusable diagnosis for the
next presentation.

## Failure modes

| Failure mode                                                                                 | What it gets wrong                                                                                                                  |
| -------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------- |
| Always fixing it directly, even when there's time to teach                                   | Optimizes today's task at the cost of the same question recurring indefinitely                                                      |
| Asking questions that are really the answer in disguise ("have you checked the mock reset?") | Feels like teaching but transfers the conclusion just as directly as Path A did                                                     |
| Treating 40 minutes of struggle as automatically "too long"                                  | The actual signal is whether new hypotheses are still being formed, not elapsed time                                                |
| Using the same gradient stage for every mentee on every kind of problem                      | A mentee's readiness varies by specific skill, not as one fixed trait about the person                                              |
| Continuing "ask and wait" on the fourth recurrence of the same root gap                      | A repeating pattern signals a missing mental model, not a one-off stuck moment — it calls for a different, more direct intervention |
