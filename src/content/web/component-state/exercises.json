{
  "items": [
    {
      "type": "quiz",
      "id": "def-state-1",
      "poolId": "def-state-pool",
      "level": 1,
      "prompt": "What is 'state,' as this unit defines it?",
      "choices": [
        "The data that determines what a page currently shows, which can change over time in response to user actions",
        "The CSS styles applied to an element",
        "The initial HTML markup a server sends before any JavaScript runs",
        "A permanent record of every action a user has ever taken"
      ],
      "correctIndex": 0,
      "explanation": "State is specifically the data driving what's currently displayed — a cart count, a form value, whether a menu is open — and it changes as the user interacts with the page.",
      "reference": "<div class=\"not-prose rounded-md border border-slate-300 bg-slate-50 p-3 text-xs dark:border-slate-700 dark:bg-slate-900\"><div class=\"mb-1.5 font-semibold text-slate-700 dark:text-slate-300\">State</div><div class=\"flex flex-col gap-1\"><div>🔢 Drives what the page shows</div><div>🔁 Changes over time from actions</div></div><div class=\"mt-1.5\"><strong>Why:</strong> state is the thing that can drift out of sync when it's copied in multiple places.</div></div>"
    },
    {
      "type": "quiz",
      "id": "def-state-2",
      "poolId": "def-state-pool",
      "level": 1,
      "prompt": "In the L1 scenario, what is the piece of 'state' that both badges are supposed to reflect?",
      "choices": [
        "The cart's item count",
        "The color of the header",
        "The URL of the current page",
        "The browser's window width"
      ],
      "correctIndex": 0,
      "explanation": "The cart's item count is the state driving both badges — the bug is that it isn't tracked as a single, shared value both displays read from.",
      "reference": "<div class=\"not-prose rounded-md border border-slate-300 bg-slate-50 p-3 text-xs dark:border-slate-700 dark:bg-slate-900\"><div class=\"mb-1.5 font-semibold text-slate-700 dark:text-slate-300\">State</div><div class=\"flex flex-col gap-1\"><div>🔢 Drives what the page shows</div><div>🔁 Changes over time from actions</div></div><div class=\"mt-1.5\"><strong>Why:</strong> state is the thing that can drift out of sync when it's copied in multiple places.</div></div>"
    },
    {
      "type": "quiz",
      "id": "def-single-source-of-truth-1",
      "poolId": "def-single-source-of-truth-pool",
      "level": 1,
      "prompt": "What does 'single source of truth' mean?",
      "choices": [
        "Keeping exactly one authoritative copy of a piece of state, so every display reads from the same place",
        "Storing all of a page's data in a single file on the server",
        "Only allowing one developer to edit the state-related code",
        "Using a single CSS class to style every stateful element"
      ],
      "correctIndex": 0,
      "explanation": "A single source of truth means one authoritative value per piece of state — not one giant shared variable for everything, just no separate driftable copies of the same value.",
      "reference": "<div class=\"not-prose rounded-md border border-slate-300 bg-slate-50 p-3 text-xs dark:border-slate-700 dark:bg-slate-900\"><div class=\"mb-1.5 font-semibold text-slate-700 dark:text-slate-300\">Single source of truth</div><div class=\"flex flex-col gap-1\"><div>1️⃣ One authoritative value</div><div>👀 Every display reads from it</div></div><div class=\"mt-1.5\"><strong>Why:</strong> separate copies of the same value are what let displays disagree.</div></div>"
    },
    {
      "type": "quiz",
      "id": "def-single-source-of-truth-2",
      "poolId": "def-single-source-of-truth-pool",
      "level": 1,
      "prompt": "In the naive cart example, why don't the header and drawer badges share a single source of truth?",
      "choices": [
        "Each badge is updated directly by whichever handler happens to touch it, rather than both reading from one shared value",
        "They do share a single source of truth, which is why the bug is surprising",
        "The badges are stored in separate files, which prevents sharing state",
        "JavaScript doesn't allow two DOM elements to reflect the same variable"
      ],
      "correctIndex": 0,
      "explanation": "In the naive version, `count` exists, but the badges aren't derived from it consistently — each handler decides on its own which badges to touch, so there's no enforced single source of truth in practice.",
      "reference": "<div class=\"not-prose rounded-md border border-slate-300 bg-slate-50 p-3 text-xs dark:border-slate-700 dark:bg-slate-900\"><div class=\"mb-1.5 font-semibold text-slate-700 dark:text-slate-300\">Single source of truth</div><div class=\"flex flex-col gap-1\"><div>1️⃣ One authoritative value</div><div>👀 Every display reads from it</div></div><div class=\"mt-1.5\"><strong>Why:</strong> separate copies of the same value are what let displays disagree.</div></div>"
    },
    {
      "type": "quiz",
      "id": "def-unidirectional-data-flow-1",
      "poolId": "def-unidirectional-data-flow-pool",
      "level": 1,
      "prompt": "What is 'unidirectional data flow'?",
      "choices": [
        "A discipline where state only changes through one defined path: an action updates state, then the update triggers re-rendering everything that depends on it",
        "A rule that data can only be read once per page load",
        "A network protocol that sends data in only one direction between client and server",
        "A CSS layout technique for arranging elements in a single column"
      ],
      "correctIndex": 0,
      "explanation": "Unidirectional data flow is specifically about the path a change takes: action → state update → render — not about network direction or layout.",
      "reference": "<div class=\"not-prose rounded-md border border-slate-300 bg-slate-50 p-3 text-xs dark:border-slate-700 dark:bg-slate-900\"><div class=\"mb-1.5 font-semibold text-slate-700 dark:text-slate-300\">Unidirectional data flow</div><div class=\"flex flex-col gap-1\"><div>🖱️ Action</div><div>➡️ State update</div><div>➡️ Render (every display)</div></div><div class=\"mt-1.5\"><strong>Why:</strong> no shortcut exists from an action straight to one specific display.</div></div>"
    },
    {
      "type": "quiz",
      "id": "def-unidirectional-data-flow-2",
      "poolId": "def-unidirectional-data-flow-pool",
      "level": 1,
      "prompt": "In the naive cart example, which part of unidirectional data flow is missing?",
      "choices": [
        "There's no consistent 'state update triggers render for every display' step — displays are updated individually inside each action",
        "There's no action at all — the badges update themselves automatically",
        "There's no state at all — the badges are hardcoded",
        "There's no render step, since the DOM never changes"
      ],
      "correctIndex": 0,
      "explanation": "The naive version has actions (button clicks) and eventually reaches state (the count), but skips the consistent render step — each handler decides individually which displays to touch instead of every display reacting to the same state change.",
      "reference": "<div class=\"not-prose rounded-md border border-slate-300 bg-slate-50 p-3 text-xs dark:border-slate-700 dark:bg-slate-900\"><div class=\"mb-1.5 font-semibold text-slate-700 dark:text-slate-300\">Unidirectional data flow</div><div class=\"flex flex-col gap-1\"><div>🖱️ Action</div><div>➡️ State update</div><div>➡️ Render (every display)</div></div><div class=\"mt-1.5\"><strong>Why:</strong> no shortcut exists from an action straight to one specific display.</div></div>"
    },
    {
      "type": "quiz",
      "id": "def-render-function-1",
      "poolId": "def-render-function-pool",
      "level": 1,
      "prompt": "What is a 'render function,' as this unit defines it?",
      "choices": [
        "Code that takes the current state and produces what the page should show, run every time the state changes",
        "A function that generates random values for testing",
        "A function that only runs once, when the page first loads",
        "A function that sends data to a server"
      ],
      "correctIndex": 0,
      "explanation": "A render function's job is to reflect state onto the page consistently — it's called whenever state changes, rather than being invoked inconsistently by individual handlers.",
      "reference": "<div class=\"not-prose rounded-md border border-slate-300 bg-slate-50 p-3 text-xs dark:border-slate-700 dark:bg-slate-900\"><div class=\"mb-1.5 font-semibold text-slate-700 dark:text-slate-300\">Render function</div><div class=\"flex flex-col gap-1\"><div>📥 Takes current state</div><div>📤 Produces what the page shows</div></div><div class=\"mt-1.5\"><strong>Why:</strong> a render function run consistently is what replaces scattered, handler-specific updates.</div></div>"
    },
    {
      "type": "quiz",
      "id": "def-render-function-2",
      "poolId": "def-render-function-pool",
      "level": 1,
      "prompt": "In L3's fixed version, what plays the role of the render function for the header badge?",
      "choices": [
        "The subscriber callback that sets `headerBadge.textContent` whenever the store notifies it",
        "The `addItem` function",
        "The `createCartStore` function itself",
        "The `getState` function"
      ],
      "correctIndex": 0,
      "explanation": "The subscriber callback is what takes the current state and updates the header badge — it runs every time the store's state changes, which is exactly the render function's job.",
      "reference": "<div class=\"not-prose rounded-md border border-slate-300 bg-slate-50 p-3 text-xs dark:border-slate-700 dark:bg-slate-900\"><div class=\"mb-1.5 font-semibold text-slate-700 dark:text-slate-300\">Render function</div><div class=\"flex flex-col gap-1\"><div>📥 Takes current state</div><div>📤 Produces what the page shows</div></div><div class=\"mt-1.5\"><strong>Why:</strong> a render function run consistently is what replaces scattered, handler-specific updates.</div></div>"
    },
    {
      "type": "quiz",
      "id": "why-badges-diverge-1",
      "poolId": "why-badges-diverge-pool",
      "level": 1,
      "prompt": "In the L1 scenario, why does the header badge end up wrong after the quick-buy action, even though the underlying count is correct?",
      "choices": [
        "The quick-buy handler updates the drawer badge but was never written to also update the header badge",
        "The count variable itself becomes incorrect after the quick-buy action",
        "The header badge is a read-only element that can never be updated",
        "The product-page handler runs after the quick-buy handler and overwrites it incorrectly"
      ],
      "correctIndex": 0,
      "explanation": "The count is accurate; the header badge is simply never told to update inside the quick-buy handler — a missing line in one specific handler, not a data problem.",
      "reference": "<div class=\"not-prose rounded-md border border-slate-300 bg-slate-50 p-3 text-xs dark:border-slate-700 dark:bg-slate-900\"><div class=\"mb-1.5 font-semibold text-slate-700 dark:text-slate-300\">Why they diverge</div><div class=\"flex flex-col gap-1\"><div>✅ count is correct</div><div>🚫 header badge update is missing</div></div><div class=\"mt-1.5\"><strong>Why:</strong> the number is right; the display update was simply never wired up for this action.</div></div>"
    },
    {
      "type": "quiz",
      "id": "why-badges-diverge-2",
      "poolId": "why-badges-diverge-pool",
      "level": 1,
      "prompt": "Is the two-badge bug in L1 caused by a bug in the counting logic itself?",
      "choices": [
        "No — the counting logic is correct; the bug is that one handler forgets to update one display",
        "Yes — the count increments by the wrong amount in one of the handlers",
        "Yes — the count resets unexpectedly between actions",
        "No — there is no bug, the two badges are supposed to show different numbers"
      ],
      "correctIndex": 0,
      "explanation": "The count increments correctly every time — the bug is entirely about which displays get updated after each action, not about the arithmetic.",
      "reference": "<div class=\"not-prose rounded-md border border-slate-300 bg-slate-50 p-3 text-xs dark:border-slate-700 dark:bg-slate-900\"><div class=\"mb-1.5 font-semibold text-slate-700 dark:text-slate-300\">Why they diverge</div><div class=\"flex flex-col gap-1\"><div>✅ count is correct</div><div>🚫 header badge update is missing</div></div><div class=\"mt-1.5\"><strong>Why:</strong> the number is right; the display update was simply never wired up for this action.</div></div>"
    },
    {
      "type": "quiz",
      "id": "scenario-facts-1",
      "poolId": "scenario-facts-pool",
      "level": 1,
      "prompt": "In the L1 scenario, after the shopper adds one item from the product page, what do both badges show?",
      "choices": [
        "1",
        "0",
        "2",
        "They show different numbers even after the first action"
      ],
      "correctIndex": 0,
      "explanation": "The first action (adding from the product page) correctly updates both badges to 1 — the bug only appears after the second action, from quick buy.",
      "reference": "<div class=\"not-prose rounded-md border border-slate-300 bg-slate-50 p-3 text-xs dark:border-slate-700 dark:bg-slate-900\"><div class=\"mb-1.5 font-semibold text-slate-700 dark:text-slate-300\">The scenario</div><div class=\"flex flex-col gap-1\"><div>1️⃣ Add from product page → both show 1</div><div>2️⃣ Add from quick buy → header stuck at 1, drawer shows 2</div></div><div class=\"mt-1.5\"><strong>Why:</strong> the first action happens to touch both badges; the second doesn't.</div></div>"
    },
    {
      "type": "quiz",
      "id": "scenario-facts-2",
      "poolId": "scenario-facts-pool",
      "level": 1,
      "prompt": "After the second action (quick buy) in the L1 scenario, what does the header badge show, and what does the drawer badge show?",
      "choices": [
        "Header: 1, Drawer: 2",
        "Header: 2, Drawer: 2",
        "Header: 2, Drawer: 1",
        "Header: 0, Drawer: 2"
      ],
      "correctIndex": 0,
      "explanation": "This is the exact mismatch the scenario describes: the header badge is stuck at 1 because quick buy never updates it, while the drawer badge correctly reaches 2.",
      "reference": "<div class=\"not-prose rounded-md border border-slate-300 bg-slate-50 p-3 text-xs dark:border-slate-700 dark:bg-slate-900\"><div class=\"mb-1.5 font-semibold text-slate-700 dark:text-slate-300\">The scenario</div><div class=\"flex flex-col gap-1\"><div>1️⃣ Add from product page → both show 1</div><div>2️⃣ Add from quick buy → header stuck at 1, drawer shows 2</div></div><div class=\"mt-1.5\"><strong>Why:</strong> the first action happens to touch both badges; the second doesn't.</div></div>"
    },
    {
      "type": "quiz",
      "id": "scenario-facts-3",
      "poolId": "scenario-facts-pool",
      "level": 1,
      "prompt": "Which two places display the cart count in L1's scenario?",
      "choices": [
        "A header badge and a cart drawer badge",
        "A header badge and the browser tab title",
        "A product page and a checkout page",
        "A header badge and an email receipt"
      ],
      "correctIndex": 0,
      "explanation": "The scenario is specifically about a header badge and a cart drawer badge, both meant to show the same cart item count.",
      "reference": "<div class=\"not-prose rounded-md border border-slate-300 bg-slate-50 p-3 text-xs dark:border-slate-700 dark:bg-slate-900\"><div class=\"mb-1.5 font-semibold text-slate-700 dark:text-slate-300\">The scenario</div><div class=\"flex flex-col gap-1\"><div>1️⃣ Add from product page → both show 1</div><div>2️⃣ Add from quick buy → header stuck at 1, drawer shows 2</div></div><div class=\"mt-1.5\"><strong>Why:</strong> the first action happens to touch both badges; the second doesn't.</div></div>"
    },
    {
      "type": "quiz",
      "id": "unidirectional-loop-diagram-1",
      "poolId": "unidirectional-loop-diagram-pool",
      "level": 2,
      "prompt": "In L2's unidirectional loop diagram, what happens immediately after an action fires?",
      "choices": [
        "The state update step runs",
        "Every display updates itself directly, independent of state",
        "The action fires again, in a loop",
        "Nothing — actions and state are unrelated in the diagram"
      ],
      "correctIndex": 0,
      "explanation": "The loop's first step after an action is always the state update — actions never touch a display directly in this model.",
      "reference": "<div class=\"not-prose rounded-md border border-slate-300 bg-slate-50 p-3 text-xs dark:border-slate-700 dark:bg-slate-900\"><div class=\"mb-1.5 font-semibold text-slate-700 dark:text-slate-300\">The loop</div><div class=\"flex flex-col gap-1\"><div>🖱️ Action → 🔄 State update → 🖼️ Render → displays</div></div><div class=\"mt-1.5\"><strong>Why:</strong> no arrow skips straight from action to a specific display.</div></div>"
    },
    {
      "type": "quiz",
      "id": "unidirectional-loop-diagram-2",
      "poolId": "unidirectional-loop-diagram-pool",
      "level": 2,
      "prompt": "In L2's unidirectional loop diagram, is there any arrow that goes directly from 'action' to a specific display (like the header badge)?",
      "choices": [
        "No — every path from an action to a display passes through state update and render first",
        "Yes — each display has its own direct arrow from the actions that affect it",
        "Yes, but only for the header badge specifically",
        "The diagram doesn't include any displays at all"
      ],
      "correctIndex": 0,
      "explanation": "The absence of a direct action-to-display arrow is the whole point of the diagram — it's exactly the shortcut that caused the header badge to be skipped in the naive version.",
      "reference": "<div class=\"not-prose rounded-md border border-slate-300 bg-slate-50 p-3 text-xs dark:border-slate-700 dark:bg-slate-900\"><div class=\"mb-1.5 font-semibold text-slate-700 dark:text-slate-300\">The loop</div><div class=\"flex flex-col gap-1\"><div>🖱️ Action → 🔄 State update → 🖼️ Render → displays</div></div><div class=\"mt-1.5\"><strong>Why:</strong> no arrow skips straight from action to a specific display.</div></div>"
    },
    {
      "type": "quiz",
      "id": "scattered-vs-store-table-1",
      "poolId": "scattered-vs-store-table-pool",
      "level": 2,
      "prompt": "According to L2's comparison table, what does adding a new display require in the scattered-updates approach?",
      "choices": [
        "Updating every existing handler to also touch the new display",
        "Nothing — new displays update themselves automatically",
        "Rewriting the entire state object from scratch",
        "Only updating the render function, nothing else"
      ],
      "correctIndex": 0,
      "explanation": "In the scattered approach, every existing action handler has to be found and edited to include the new display — exactly the structural risk that causes bugs like L1's.",
      "reference": "<div class=\"not-prose rounded-md border border-slate-300 bg-slate-50 p-3 text-xs dark:border-slate-700 dark:bg-slate-900\"><div class=\"mb-1.5 font-semibold text-slate-700 dark:text-slate-300\">Comparison</div><div class=\"flex flex-col gap-1\"><div>🔀 Scattered: edit every handler</div><div>🏪 Store: just subscribe</div></div><div class=\"mt-1.5\"><strong>Why:</strong> the store approach removes the need to touch existing code at all.</div></div>"
    },
    {
      "type": "quiz",
      "id": "scattered-vs-store-table-2",
      "poolId": "scattered-vs-store-table-pool",
      "level": 2,
      "prompt": "According to L2's comparison table, what does adding a new display require in the single-store approach?",
      "choices": [
        "Just subscribing the new display — no existing code changes",
        "Editing every handler that changes the state, the same as the scattered approach",
        "Rewriting the store's internal implementation",
        "Nothing is required, new displays appear automatically without any code"
      ],
      "correctIndex": 0,
      "explanation": "In the store approach, a new display only needs to call `subscribe` once — every existing action and handler stays untouched.",
      "reference": "<div class=\"not-prose rounded-md border border-slate-300 bg-slate-50 p-3 text-xs dark:border-slate-700 dark:bg-slate-900\"><div class=\"mb-1.5 font-semibold text-slate-700 dark:text-slate-300\">Comparison</div><div class=\"flex flex-col gap-1\"><div>🔀 Scattered: edit every handler</div><div>🏪 Store: just subscribe</div></div><div class=\"mt-1.5\"><strong>Why:</strong> the store approach removes the need to touch existing code at all.</div></div>"
    },
    {
      "type": "quiz",
      "id": "why-careful-doesnt-scale-1",
      "poolId": "why-careful-doesnt-scale-pool",
      "level": 2,
      "prompt": "According to L2, would writing a test for the quick-buy handler have fully solved the underlying problem?",
      "choices": [
        "It would have caught this specific case, but the underlying risk — every new handler must remember every display — would remain",
        "Yes, completely — tests eliminate the need for any structural change",
        "No, tests are irrelevant to this kind of bug entirely",
        "Yes, because tests automatically fix the code they find bugs in"
      ],
      "correctIndex": 0,
      "explanation": "A test would catch the specific missed case, but every future handler still faces the same risk of forgetting a display — the structural fix removes the category of mistake, not just this one instance.",
      "reference": "<div class=\"not-prose rounded-md border border-slate-300 bg-slate-50 p-3 text-xs dark:border-slate-700 dark:bg-slate-900\"><div class=\"mb-1.5 font-semibold text-slate-700 dark:text-slate-300\">Why 'be careful' doesn't scale</div><div class=\"flex flex-col gap-1\"><div>🧪 Tests catch specific misses</div><div>🏗️ Structure removes the risk category</div></div><div class=\"mt-1.5\"><strong>Why:</strong> the fix targets the pattern, not just this one bug.</div></div>"
    },
    {
      "type": "quiz",
      "id": "why-careful-doesnt-scale-2",
      "poolId": "why-careful-doesnt-scale-pool",
      "level": 2,
      "prompt": "Why doesn't 'developers should just remember to update every display' scale as a solution?",
      "choices": [
        "Every new feature that reads the same state is another place a future handler can independently forget",
        "Because JavaScript makes it impossible to update more than two elements at once",
        "Because remembering is only hard for junior developers, not senior ones",
        "It does scale fine, as long as the team is small enough"
      ],
      "correctIndex": 0,
      "explanation": "The risk compounds with every new display added — each one is an independent opportunity for a future handler to forget, regardless of how careful any individual developer is.",
      "reference": "<div class=\"not-prose rounded-md border border-slate-300 bg-slate-50 p-3 text-xs dark:border-slate-700 dark:bg-slate-900\"><div class=\"mb-1.5 font-semibold text-slate-700 dark:text-slate-300\">Why 'be careful' doesn't scale</div><div class=\"flex flex-col gap-1\"><div>🧪 Tests catch specific misses</div><div>🏗️ Structure removes the risk category</div></div><div class=\"mt-1.5\"><strong>Why:</strong> the fix targets the pattern, not just this one bug.</div></div>"
    },
    {
      "type": "quiz",
      "id": "adding-new-display-1",
      "poolId": "adding-new-display-pool",
      "level": 2,
      "prompt": "A new 'recently viewed' widget needs to show the cart count too. In the single-store approach, what's the minimum required to wire it up correctly?",
      "choices": [
        "Call `subscribe` once with a function that updates the widget from the current state",
        "Edit every existing action handler to also update the widget",
        "Create an entirely new, separate state variable just for this widget",
        "Rewrite the `createCartStore` function from scratch"
      ],
      "correctIndex": 0,
      "explanation": "Subscribing once is sufficient — the widget will receive every future state update automatically, with zero changes to any existing handler.",
      "reference": "<div class=\"not-prose rounded-md border border-slate-300 bg-slate-50 p-3 text-xs dark:border-slate-700 dark:bg-slate-900\"><div class=\"mb-1.5 font-semibold text-slate-700 dark:text-slate-300\">Adding a display</div><div class=\"flex flex-col gap-1\"><div>➕ subscribe() once</div><div>🚫 No existing code touched</div></div><div class=\"mt-1.5\"><strong>Why:</strong> the new display just needs to know about the store, not about every action.</div></div>"
    },
    {
      "type": "quiz",
      "id": "adding-new-display-2",
      "poolId": "adding-new-display-pool",
      "level": 2,
      "prompt": "In the single-store approach, does adding a new display risk breaking the header or drawer badges?",
      "choices": [
        "No — subscribing a new function doesn't modify how existing subscribers receive updates",
        "Yes — every new subscriber replaces the previous ones",
        "Yes — the store can only support one subscriber at a time",
        "It depends on the order the subscribers were added in"
      ],
      "correctIndex": 0,
      "explanation": "Subscribers are independent — adding one more doesn't change how the existing ones are notified, so existing displays keep working exactly as before.",
      "reference": "<div class=\"not-prose rounded-md border border-slate-300 bg-slate-50 p-3 text-xs dark:border-slate-700 dark:bg-slate-900\"><div class=\"mb-1.5 font-semibold text-slate-700 dark:text-slate-300\">Adding a display</div><div class=\"flex flex-col gap-1\"><div>➕ subscribe() once</div><div>🚫 No existing code touched</div></div><div class=\"mt-1.5\"><strong>Why:</strong> the new display just needs to know about the store, not about every action.</div></div>"
    },
    {
      "type": "quiz",
      "id": "generalizable-lesson-1",
      "poolId": "generalizable-lesson-pool",
      "level": 2,
      "prompt": "According to L2's generalizable lesson, does this pattern only apply to cart badges?",
      "choices": [
        "No — it applies anywhere the same state is displayed in more than one place, like a logged-in name or a notification count",
        "Yes — it's specific to e-commerce shopping carts",
        "Yes — it only applies to badge-shaped UI elements",
        "No, but it's limited to counters specifically, not other kinds of state"
      ],
      "correctIndex": 0,
      "explanation": "The unit generalizes past cart badges to any case of the same state shown in multiple places: a logged-in name, a notification count, a form's validity shown two ways.",
      "reference": "<div class=\"not-prose rounded-md border border-slate-300 bg-slate-50 p-3 text-xs dark:border-slate-700 dark:bg-slate-900\"><div class=\"mb-1.5 font-semibold text-slate-700 dark:text-slate-300\">Generalizes to</div><div class=\"flex flex-col gap-1\"><div>👤 Logged-in name, header + settings</div><div>🔔 Notification count, tab title + bell icon</div></div><div class=\"mt-1.5\"><strong>Why:</strong> the same shape recurs anywhere one value is displayed more than once.</div></div>"
    },
    {
      "type": "quiz",
      "id": "generalizable-lesson-2",
      "poolId": "generalizable-lesson-pool",
      "level": 2,
      "prompt": "According to L2, what question should you ask whenever two displays that are supposed to agree don't?",
      "choices": [
        "Is there one source of truth, and does every display derive from it through the same path?",
        "Which developer is responsible for the mistake?",
        "Is the bug caused by a browser compatibility issue?",
        "Should one of the two displays simply be removed from the page?"
      ],
      "correctIndex": 0,
      "explanation": "This is the unit's generalized diagnostic question — it applies to any state-consistency bug, not just the specific cart-badge example.",
      "reference": "<div class=\"not-prose rounded-md border border-slate-300 bg-slate-50 p-3 text-xs dark:border-slate-700 dark:bg-slate-900\"><div class=\"mb-1.5 font-semibold text-slate-700 dark:text-slate-300\">Generalizes to</div><div class=\"flex flex-col gap-1\"><div>👤 Logged-in name, header + settings</div><div>🔔 Notification count, tab title + bell icon</div></div><div class=\"mt-1.5\"><strong>Why:</strong> the same shape recurs anywhere one value is displayed more than once.</div></div>"
    },
    {
      "type": "quiz",
      "id": "no-direct-action-to-display-arrow-1",
      "poolId": "no-direct-action-to-display-arrow-pool",
      "level": 2,
      "prompt": "Why is the absence of a direct 'action to display' arrow the key feature of L2's unidirectional loop diagram?",
      "choices": [
        "Because that missing shortcut is exactly what forces every action's effects to flow through state and render, reaching every display consistently",
        "Because arrows are only decorative in the diagram and carry no real meaning",
        "Because direct arrows would make the diagram too complicated to read",
        "Because state updates are optional in the unidirectional model"
      ],
      "correctIndex": 0,
      "explanation": "Removing the possibility of an action-to-display shortcut is what structurally prevents the exact bug from L1 — there's no path left for an action to reach one display while skipping another.",
      "reference": "<div class=\"not-prose rounded-md border border-slate-300 bg-slate-50 p-3 text-xs dark:border-slate-700 dark:bg-slate-900\"><div class=\"mb-1.5 font-semibold text-slate-700 dark:text-slate-300\">The loop</div><div class=\"flex flex-col gap-1\"><div>🖱️ Action → 🔄 State update → 🖼️ Render → displays</div></div><div class=\"mt-1.5\"><strong>Why:</strong> no arrow skips straight from action to a specific display.</div></div>"
    },
    {
      "type": "quiz",
      "id": "no-direct-action-to-display-arrow-2",
      "poolId": "no-direct-action-to-display-arrow-pool",
      "level": 2,
      "prompt": "In the naive cart version, which arrow effectively exists that the unidirectional diagram intentionally removes?",
      "choices": [
        "A direct arrow from the quick-buy action to the drawer badge, skipping the header badge entirely",
        "An arrow from the header badge back to the count variable",
        "An arrow from render straight to the action",
        "There is no such arrow in the naive version either"
      ],
      "correctIndex": 0,
      "explanation": "The naive `addFromQuickBuy` function acts as a direct, selective path from an action to one specific display (the drawer badge) — exactly the shortcut the unidirectional model is designed to eliminate.",
      "reference": "<div class=\"not-prose rounded-md border border-slate-300 bg-slate-50 p-3 text-xs dark:border-slate-700 dark:bg-slate-900\"><div class=\"mb-1.5 font-semibold text-slate-700 dark:text-slate-300\">The loop</div><div class=\"flex flex-col gap-1\"><div>🖱️ Action → 🔄 State update → 🖼️ Render → displays</div></div><div class=\"mt-1.5\"><strong>Why:</strong> no arrow skips straight from action to a specific display.</div></div>"
    },
    {
      "type": "quiz",
      "id": "no-direct-action-to-display-arrow-3",
      "poolId": "no-direct-action-to-display-arrow-pool",
      "level": 2,
      "prompt": "If a new action handler is added that only updates state (never touches any DOM element directly), what guarantees every display stays correct?",
      "choices": [
        "Every display already subscribed to the store will receive the updated state and re-render itself, regardless of which action triggered it",
        "Nothing — the developer must still remember to manually update each display",
        "The new handler must explicitly list every display it wants to update",
        "Displays only update if the handler calls a special 'update all' function by name"
      ],
      "correctIndex": 0,
      "explanation": "Because every display is already wired to the store via `subscribe`, any action that goes through `setState` automatically reaches every display — no per-action bookkeeping required.",
      "reference": "<div class=\"not-prose rounded-md border border-slate-300 bg-slate-50 p-3 text-xs dark:border-slate-700 dark:bg-slate-900\"><div class=\"mb-1.5 font-semibold text-slate-700 dark:text-slate-300\">The loop</div><div class=\"flex flex-col gap-1\"><div>🖱️ Action → 🔄 State update → 🖼️ Render → displays</div></div><div class=\"mt-1.5\"><strong>Why:</strong> no arrow skips straight from action to a specific display.</div></div>"
    },
    {
      "type": "quiz",
      "id": "why-header-badge-wrong-1",
      "poolId": "why-header-badge-wrong-pool",
      "level": 3,
      "prompt": "L3 predicts the header badge will be wrong after quick buy, even though `count` increments correctly. What's the actual mechanism?",
      "choices": [
        "`addFromQuickBuy` increments `count` and updates `drawerBadge`, but its code simply never assigns to `headerBadge.textContent`",
        "`count` is declared separately inside each function, so they're tracking different values",
        "`headerBadge` is a read-only object that throws an error when written to",
        "The two functions run in the wrong order, causing `headerBadge` to be overwritten"
      ],
      "correctIndex": 0,
      "explanation": "`count` is a single shared variable, correctly incremented by both functions — the bug is purely that `addFromQuickBuy`'s body never includes a line updating `headerBadge`.",
      "reference": "<div class=\"not-prose rounded-md border border-slate-300 bg-slate-50 p-3 text-xs dark:border-slate-700 dark:bg-slate-900\"><div class=\"mb-1.5 font-semibold text-slate-700 dark:text-slate-300\">The naive bug</div><div class=\"flex flex-col gap-1\"><div>✅ count is correct</div><div>🚫 headerBadge update missing in one function</div></div><div class=\"mt-1.5\"><strong>Why:</strong> verified via running code — header stays \"1\", drawer reaches \"2\".</div></div>"
    },
    {
      "type": "quiz",
      "id": "why-header-badge-wrong-2",
      "poolId": "why-header-badge-wrong-pool",
      "level": 3,
      "prompt": "After running L3's naive example (`addFromProductCard()` then `addFromQuickBuy()`), what are the verified values of `headerBadge.textContent` and `drawerBadge.textContent`?",
      "choices": [
        "headerBadge: \"1\", drawerBadge: \"2\"",
        "headerBadge: \"2\", drawerBadge: \"2\"",
        "headerBadge: \"0\", drawerBadge: \"2\"",
        "headerBadge: \"2\", drawerBadge: \"1\""
      ],
      "correctIndex": 0,
      "explanation": "This is the exact verified output: the header badge is stuck at \"1\" from the first call, while the drawer badge correctly reaches \"2\" after both calls.",
      "reference": "<div class=\"not-prose rounded-md border border-slate-300 bg-slate-50 p-3 text-xs dark:border-slate-700 dark:bg-slate-900\"><div class=\"mb-1.5 font-semibold text-slate-700 dark:text-slate-300\">The naive bug</div><div class=\"flex flex-col gap-1\"><div>✅ count is correct</div><div>🚫 headerBadge update missing in one function</div></div><div class=\"mt-1.5\"><strong>Why:</strong> verified via running code — header stays \"1\", drawer reaches \"2\".</div></div>"
    },
    {
      "type": "quiz",
      "id": "fix-mechanism-1",
      "poolId": "fix-mechanism-pool",
      "level": 3,
      "prompt": "In L3's fixed version, why do both badges correctly reach \"2\" after the same two actions that caused a mismatch in the naive version?",
      "choices": [
        "Both badges are subscriber callbacks that run whenever `setState` is called, regardless of which action triggered it",
        "The fixed version calls each badge's update function twice as often as the naive version",
        "The fixed version uses `document.querySelector` to find and update both badges automatically",
        "The fixed version doesn't actually track a count — it just always displays \"2\""
      ],
      "correctIndex": 0,
      "explanation": "Both `headerBadge` and `drawerBadge` are updated by callbacks registered via `subscribe`, and `setState` calls every subscriber after every change — so neither action needs to know which displays exist.",
      "reference": "<div class=\"not-prose rounded-md border border-slate-300 bg-slate-50 p-3 text-xs dark:border-slate-700 dark:bg-slate-900\"><div class=\"mb-1.5 font-semibold text-slate-700 dark:text-slate-300\">The fix</div><div class=\"flex flex-col gap-1\"><div>🏪 One store, one state.count</div><div>📡 Every badge subscribes once</div></div><div class=\"mt-1.5\"><strong>Why:</strong> setState notifies every subscriber, so no action needs to know which displays exist.</div></div>"
    },
    {
      "type": "quiz",
      "id": "fix-mechanism-2",
      "poolId": "fix-mechanism-pool",
      "level": 3,
      "prompt": "In L3's `createCartStore`, what does `addItem` actually do?",
      "choices": [
        "Calls `setState` with an updater function that returns a new state object with `count` incremented by 1",
        "Directly sets `headerBadge.textContent` and `drawerBadge.textContent`",
        "Increments a global `count` variable that lives outside the store",
        "Calls `subscribe` to register a new display"
      ],
      "correctIndex": 0,
      "explanation": "`addItem` never touches any badge directly — it only calls `setState`, which is what triggers every subscriber (including both badges) to update.",
      "reference": "<div class=\"not-prose rounded-md border border-slate-300 bg-slate-50 p-3 text-xs dark:border-slate-700 dark:bg-slate-900\"><div class=\"mb-1.5 font-semibold text-slate-700 dark:text-slate-300\">The fix</div><div class=\"flex flex-col gap-1\"><div>🏪 One store, one state.count</div><div>📡 Every badge subscribes once</div></div><div class=\"mt-1.5\"><strong>Why:</strong> setState notifies every subscriber, so no action needs to know which displays exist.</div></div>"
    },
    {
      "type": "quiz",
      "id": "extend-it-third-display-1",
      "poolId": "extend-it-third-display-pool",
      "level": 3,
      "prompt": "L3's extend-it example adds a 'recently viewed' widget. In the store version, what does wiring it up correctly require?",
      "choices": [
        "Only calling `store.subscribe` once with a function that updates the new widget",
        "Editing `addFromProductCard` and `addFromQuickBuy` to also update the new widget",
        "Creating an entirely new store just for the new widget",
        "Rewriting `setState` to accept a third argument for the new widget"
      ],
      "correctIndex": 0,
      "explanation": "Verified in L3: subscribing the new widget once was sufficient, with zero changes to any existing action or handler — that's the whole point of the store approach.",
      "reference": "<div class=\"not-prose rounded-md border border-slate-300 bg-slate-50 p-3 text-xs dark:border-slate-700 dark:bg-slate-900\"><div class=\"mb-1.5 font-semibold text-slate-700 dark:text-slate-300\">Extend it</div><div class=\"flex flex-col gap-1\"><div>➕ New widget just subscribes</div><div>✅ All three badges verified at \"3\"</div></div><div class=\"mt-1.5\"><strong>Why:</strong> no existing code needed to change to add the third display.</div></div>"
    },
    {
      "type": "quiz",
      "id": "extend-it-third-display-2",
      "poolId": "extend-it-third-display-pool",
      "level": 3,
      "prompt": "After L3's extend-it example subscribes the new widget and calls `store.addItem()` once more, what do all three displays verify to?",
      "choices": [
        "\"3\" for all three (header, drawer, and recently-viewed)",
        "\"3\" for header and drawer, but \"1\" for the new widget",
        "\"2\" for header and drawer, and \"1\" for the new widget",
        "Each display shows a different number"
      ],
      "correctIndex": 0,
      "explanation": "All three displays verify to \"3\" — the new widget receives the full current state immediately upon subscribing and stays in sync with the other two from then on.",
      "reference": "<div class=\"not-prose rounded-md border border-slate-300 bg-slate-50 p-3 text-xs dark:border-slate-700 dark:bg-slate-900\"><div class=\"mb-1.5 font-semibold text-slate-700 dark:text-slate-300\">Extend it</div><div class=\"flex flex-col gap-1\"><div>➕ New widget just subscribes</div><div>✅ All three badges verified at \"3\"</div></div><div class=\"mt-1.5\"><strong>Why:</strong> no existing code needed to change to add the third display.</div></div>"
    },
    {
      "type": "quiz",
      "id": "failure-mode-identify-1",
      "poolId": "failure-mode-identify-pool",
      "level": 3,
      "prompt": "According to L3's failure modes, what goes wrong if code mutates `state` directly instead of going through `setState`?",
      "choices": [
        "Subscribers never get notified, so the state changes but nothing re-renders — a silent desync",
        "The program throws an error immediately and refuses to run",
        "Nothing goes wrong — direct mutation and `setState` are equivalent",
        "The state resets to its initial value automatically"
      ],
      "correctIndex": 0,
      "explanation": "Direct mutation bypasses the notification step entirely — the underlying value changes, but every display stays showing the old value since nothing tells them to re-render.",
      "reference": "<div class=\"not-prose rounded-md border border-slate-300 bg-slate-50 p-3 text-xs dark:border-slate-700 dark:bg-slate-900\"><div class=\"mb-1.5 font-semibold text-slate-700 dark:text-slate-300\">Failure modes</div><div class=\"flex flex-col gap-1\"><div>🚫 Direct mutation skips notification</div><div>🌐 'Single source' ≠ one giant global</div></div><div class=\"mt-1.5\"><strong>Why:</strong> both failure modes come from missing the actual mechanism the store provides.</div></div>"
    },
    {
      "type": "quiz",
      "id": "failure-mode-identify-2",
      "poolId": "failure-mode-identify-pool",
      "level": 3,
      "prompt": "According to L3's failure modes, what's the mistake in thinking 'single source of truth' means putting all of a page's unrelated state into one giant object?",
      "choices": [
        "The point is one authoritative value per piece of state, not collapsing every unrelated value into a single shared object",
        "There is no mistake — that's exactly what single source of truth means",
        "It's only a mistake if the object has more than 10 properties",
        "It's a mistake only in TypeScript, not in plain JavaScript"
      ],
      "correctIndex": 0,
      "explanation": "Single source of truth is about avoiding duplicate copies of the *same* value, not about merging every unrelated piece of state on the page into one undifferentiated object.",
      "reference": "<div class=\"not-prose rounded-md border border-slate-300 bg-slate-50 p-3 text-xs dark:border-slate-700 dark:bg-slate-900\"><div class=\"mb-1.5 font-semibold text-slate-700 dark:text-slate-300\">Failure modes</div><div class=\"flex flex-col gap-1\"><div>🚫 Direct mutation skips notification</div><div>🌐 'Single source' ≠ one giant global</div></div><div class=\"mt-1.5\"><strong>Why:</strong> both failure modes come from missing the actual mechanism the store provides.</div></div>"
    },
    {
      "type": "quiz",
      "id": "naive-vs-store-tradeoff-1",
      "poolId": "naive-vs-store-tradeoff-pool",
      "level": 3,
      "prompt": "Is the naive version of the cart example 'wrong' code, in the sense of having invalid syntax or crashing?",
      "choices": [
        "No — it runs without errors; the problem is a structural risk of forgetting a display, not invalid code",
        "Yes — it throws an exception every time addFromQuickBuy is called",
        "Yes — it fails to compile because count is never declared",
        "No, and it has no downsides compared to the store version at all"
      ],
      "correctIndex": 0,
      "explanation": "The naive version runs perfectly fine and produces a real (if wrong) result — the issue isn't a crash or syntax error, it's that the code's structure makes a specific class of bug easy to introduce and hard to notice.",
      "reference": "<div class=\"not-prose rounded-md border border-slate-300 bg-slate-50 p-3 text-xs dark:border-slate-700 dark:bg-slate-900\"><div class=\"mb-1.5 font-semibold text-slate-700 dark:text-slate-300\">Naive vs. store</div><div class=\"flex flex-col gap-1\"><div>▶️ Naive: runs fine, risks silent drift</div><div>🏪 Store: same result, drift structurally prevented</div></div><div class=\"mt-1.5\"><strong>Why:</strong> the naive version isn't broken syntax — it's a working pattern with a hidden risk.</div></div>"
    },
    {
      "type": "quiz",
      "id": "naive-vs-store-tradeoff-2",
      "poolId": "naive-vs-store-tradeoff-pool",
      "level": 3,
      "prompt": "What's the actual functional difference in what the naive and store versions of `addFromProductCard`/`addItem` accomplish, when only the original two badges exist?",
      "choices": [
        "Nothing in that specific case — both correctly update both badges; the difference only shows up once a new handler or display is added",
        "The store version is fundamentally incapable of updating two badges at once",
        "The naive version is faster because it skips the subscription mechanism",
        "The store version requires a real browser DOM to function, unlike the naive version"
      ],
      "correctIndex": 0,
      "explanation": "With just the original product-card handler, both approaches work identically — the store's advantage only becomes visible once a second handler (quick buy) or a third display (recently viewed) enters the picture.",
      "reference": "<div class=\"not-prose rounded-md border border-slate-300 bg-slate-50 p-3 text-xs dark:border-slate-700 dark:bg-slate-900\"><div class=\"mb-1.5 font-semibold text-slate-700 dark:text-slate-300\">Naive vs. store</div><div class=\"flex flex-col gap-1\"><div>▶️ Naive: runs fine, risks silent drift</div><div>🏪 Store: same result, drift structurally prevented</div></div><div class=\"mt-1.5\"><strong>Why:</strong> the naive version isn't broken syntax — it's a working pattern with a hidden risk.</div></div>"
    },
    {
      "type": "code",
      "id": "cart-store-add-item-1",
      "poolId": "create-cart-store-pool",
      "level": 3,
      "prompt": "Complete `createCartStore` by implementing `addItem`, matching L3: it should call `setState` with an updater function that returns a new state object with `count` incremented by 1. Don't mutate `state` directly — go through `setState` so subscribers get notified.",
      "starterCode": "function createCartStore(initialCount) {\n  let state = { count: initialCount };\n  const subscribers = [];\n\n  function getState() {\n    return state;\n  }\n\n  function setState(updater) {\n    state = updater(state);\n    subscribers.forEach((fn) => fn(state));\n  }\n\n  function subscribe(fn) {\n    subscribers.push(fn);\n  }\n\n  function addItem() {\n    // TODO: use setState to increment state.count by 1\n  }\n\n  return { getState, setState, subscribe, addItem };\n}\n",
      "tests": [
        {
          "description": "increments count by 1 after one call",
          "expr": "(() => { const store = createCartStore(0); store.addItem(); return expect(store.getState().count).toBe(1); })()"
        },
        {
          "description": "increments count correctly after two calls",
          "expr": "(() => { const store = createCartStore(0); store.addItem(); store.addItem(); return expect(store.getState().count).toBe(2); })()"
        },
        {
          "description": "notifies subscribers with the updated count",
          "expr": "(() => { const store = createCartStore(0); let received = null; store.subscribe((s) => { received = s.count; }); store.addItem(); return expect(received).toBe(1); })()"
        }
      ],
      "solution": "function addItem() {\n  setState((s) => ({ count: s.count + 1 }));\n}",
      "explanation": "Routing through `setState` (rather than mutating `state.count` directly) is what guarantees every subscriber — every badge — gets notified and stays in sync, which is the entire point of the store pattern from L3.",
      "reference": "<div class=\"not-prose rounded-md border border-slate-300 bg-slate-50 p-3 text-xs dark:border-slate-700 dark:bg-slate-900\"><div class=\"mb-1.5 font-semibold text-slate-700 dark:text-slate-300\">createCartStore</div><div class=\"flex flex-col gap-1\"><div>🏪 One store, one state.count</div><div>📡 Every badge subscribes once</div></div><div class=\"mt-1.5\"><strong>Why:</strong> setState notifies every subscriber, so no action needs to know which displays exist.</div></div>"
    },
    {
      "type": "code",
      "id": "cart-store-remove-item-1",
      "poolId": "create-cart-store-pool",
      "level": 3,
      "prompt": "Complete `createCartStore` by implementing `removeItem`, the same pattern as `addItem` but decrementing: use `setState` to decrement `state.count` by 1, but never let it go below 0.",
      "starterCode": "function createCartStore(initialCount) {\n  let state = { count: initialCount };\n  const subscribers = [];\n\n  function getState() {\n    return state;\n  }\n\n  function setState(updater) {\n    state = updater(state);\n    subscribers.forEach((fn) => fn(state));\n  }\n\n  function subscribe(fn) {\n    subscribers.push(fn);\n  }\n\n  function removeItem() {\n    // TODO: use setState to decrement state.count by 1, but never go below 0\n  }\n\n  return { getState, setState, subscribe, removeItem };\n}\n",
      "tests": [
        {
          "description": "decrements count by 1 when above 0",
          "expr": "(() => { const store = createCartStore(2); store.removeItem(); return expect(store.getState().count).toBe(1); })()"
        },
        {
          "description": "clamps at 0 instead of going negative",
          "expr": "(() => { const store = createCartStore(0); store.removeItem(); return expect(store.getState().count).toBe(0); })()"
        }
      ],
      "solution": "function removeItem() {\n  setState((s) => ({ count: Math.max(0, s.count - 1) }));\n}",
      "explanation": "Same principle as `addItem`: the change goes through `setState` so every subscriber is notified, with `Math.max(0, ...)` guarding against a cart count that would otherwise go negative.",
      "reference": "<div class=\"not-prose rounded-md border border-slate-300 bg-slate-50 p-3 text-xs dark:border-slate-700 dark:bg-slate-900\"><div class=\"mb-1.5 font-semibold text-slate-700 dark:text-slate-300\">createCartStore</div><div class=\"flex flex-col gap-1\"><div>🏪 One store, one state.count</div><div>📡 Every badge subscribes once</div></div><div class=\"mt-1.5\"><strong>Why:</strong> setState notifies every subscriber, so no action needs to know which displays exist.</div></div>"
    },
    {
      "type": "code",
      "id": "generic-store-set-state-1",
      "poolId": "subscribe-notify-pool",
      "level": 3,
      "prompt": "Complete this generic `createStore` by implementing `setState`: it should update `state` by calling `updater(state)` and assigning the result back to `state`, then call every function in `subscribers` with the new state.",
      "starterCode": "function createStore(initialState) {\n  let state = initialState;\n  const subscribers = [];\n\n  function getState() {\n    return state;\n  }\n\n  function setState(updater) {\n    // TODO: update `state` using updater(state), then call every function\n    // in `subscribers` with the new state\n  }\n\n  function subscribe(fn) {\n    subscribers.push(fn);\n  }\n\n  return { getState, setState, subscribe };\n}\n",
      "tests": [
        {
          "description": "updates the state returned by getState",
          "expr": "(() => { const store = createStore({ count: 0 }); store.setState((s) => ({ count: s.count + 5 })); return expect(store.getState().count).toBe(5); })()"
        },
        {
          "description": "notifies a subscriber with the new state",
          "expr": "(() => { const store = createStore({ count: 0 }); let received = null; store.subscribe((s) => { received = s.count; }); store.setState((s) => ({ count: s.count + 5 })); return expect(received).toBe(5); })()"
        }
      ],
      "solution": "function setState(updater) {\n  state = updater(state);\n  subscribers.forEach((fn) => fn(state));\n}",
      "explanation": "This is the core of the unidirectional loop from L2: every state change flows through this one function, which is what guarantees every subscriber (every display) hears about every change, with no exceptions possible.",
      "reference": "<div class=\"not-prose rounded-md border border-slate-300 bg-slate-50 p-3 text-xs dark:border-slate-700 dark:bg-slate-900\"><div class=\"mb-1.5 font-semibold text-slate-700 dark:text-slate-300\">The store's core loop</div><div class=\"flex flex-col gap-1\"><div>🔄 setState updates state</div><div>📢 ... then notifies every subscriber</div></div><div class=\"mt-1.5\"><strong>Why:</strong> this single function is what the entire unidirectional guarantee rests on.</div></div>"
    },
    {
      "type": "code",
      "id": "generic-store-subscribe-1",
      "poolId": "subscribe-notify-pool",
      "level": 3,
      "prompt": "Complete this generic `createStore` by implementing `subscribe`: it should add `fn` to `subscribers`, and return an `unsubscribe` function that, when called, removes `fn` from `subscribers`.",
      "starterCode": "function createStore(initialState) {\n  let state = initialState;\n  const subscribers = [];\n\n  function getState() {\n    return state;\n  }\n\n  function setState(updater) {\n    state = updater(state);\n    subscribers.forEach((fn) => fn(state));\n  }\n\n  function subscribe(fn) {\n    // TODO: add fn to subscribers, and return a function that removes it\n    // from subscribers when called\n  }\n\n  return { getState, setState, subscribe };\n}\n",
      "tests": [
        {
          "description": "notifies a newly subscribed function on the next setState",
          "expr": "(() => { const store = createStore({ count: 0 }); let callCount = 0; store.subscribe(() => { callCount += 1; }); store.setState((s) => ({ count: s.count + 1 })); return expect(callCount).toBe(1); })()"
        },
        {
          "description": "stops notifying after unsubscribe is called",
          "expr": "(() => { const store = createStore({ count: 0 }); let callCount = 0; const unsubscribe = store.subscribe(() => { callCount += 1; }); store.setState((s) => ({ count: s.count + 1 })); unsubscribe(); store.setState((s) => ({ count: s.count + 1 })); return expect(callCount).toBe(1); })()"
        }
      ],
      "solution": "function subscribe(fn) {\n  subscribers.push(fn);\n  return function unsubscribe() {\n    const index = subscribers.indexOf(fn);\n    if (index !== -1) subscribers.splice(index, 1);\n  };\n}",
      "explanation": "Returning an unsubscribe closure lets a display stop listening (for example, when it's removed from the page) without needing the store to expose its internal `subscribers` array — the same single-store idea from L3, plus proper cleanup.",
      "reference": "<div class=\"not-prose rounded-md border border-slate-300 bg-slate-50 p-3 text-xs dark:border-slate-700 dark:bg-slate-900\"><div class=\"mb-1.5 font-semibold text-slate-700 dark:text-slate-300\">The store's core loop</div><div class=\"flex flex-col gap-1\"><div>🔄 setState updates state</div><div>📢 ... then notifies every subscriber</div></div><div class=\"mt-1.5\"><strong>Why:</strong> this single function is what the entire unidirectional guarantee rests on.</div></div>"
    }
  ]
}
