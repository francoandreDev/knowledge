{
  "items": [
    {
      "id": "dedup-savings-explorer",
      "level": 2,
      "title": "How much duplication does module deduplication actually avoid?",
      "description": "Uses this unit's own resolveOrder mechanism directly: a shared module gets included exactly once no matter how many other modules import it, unlike hand-maintained script tags with no such guarantee. Drag the number of importers up and watch how much duplicated code a naive, non-deduplicated approach would have shipped, for the exact same shared module size.",
      "params": [
        {
          "name": "numImporters",
          "label": "Modules importing the shared file",
          "min": 1,
          "max": 20,
          "step": 1,
          "default": 2
        },
        {
          "name": "sharedModuleSizeKb",
          "label": "Shared module size",
          "min": 1,
          "max": 100,
          "step": 1,
          "default": 20,
          "unit": "KB"
        }
      ],
      "compute": "const bytesSaved = (params.numImporters - 1) * params.sharedModuleSizeKb; return { bytesSaved };",
      "outputs": [
        {
          "key": "bytesSaved",
          "label": "Duplicated bytes avoided by dedup",
          "unit": "KB",
          "color": "#34d399"
        }
      ],
      "chartParam": "numImporters"
    }
  ]
}
