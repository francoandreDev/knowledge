{
  "items": [
    {
      "type": "quiz",
      "id": "status-code-range",
      "poolId": "status-code-range-pool",
      "level": 1,
      "prompt": "A client sends a request with a typo in the path and gets back a 404. Which range does that status code belong to?",
      "choices": [
        "1xx — informational",
        "2xx — success",
        "4xx — client error",
        "5xx — server error"
      ],
      "correctIndex": 2,
      "explanation": "4xx means the client's request itself was the problem (in this case, asking for a path that doesn't exist) — the server understood the request fine, it just has nothing to return for it."
    },
    {
      "type": "quiz",
      "id": "status-code-range-v2",
      "poolId": "status-code-range-pool",
      "level": 1,
      "prompt": "A server is completely fine, but the database it depends on just crashed, so it can't fulfill a valid request. Which status range should it return?",
      "choices": [
        "2xx — success",
        "4xx — client error",
        "5xx — server error",
        "3xx — redirect"
      ],
      "correctIndex": 2,
      "explanation": "5xx means the server side failed to do its job even though the request itself was valid — a dead dependency (like the database here) is exactly the server's problem, not the client's, so it's not 4xx."
    },
    {
      "type": "quiz",
      "id": "statelessness",
      "poolId": "statelessness-pool",
      "level": 1,
      "prompt": "A load balancer sends request #1 to server A and request #2 (from the same user, milliseconds later) to server B, with no coordination between A and B. Why does this work?",
      "choices": [
        "Servers A and B share a database of active users",
        "Each request carries everything needed to answer it — HTTP is stateless by default",
        "The load balancer secretly forwards state between servers",
        "It only works because both requests use the same TCP connection"
      ],
      "correctIndex": 1,
      "explanation": "Neither server needs to remember the other request exists — statelessness is exactly what makes this kind of load balancing possible without coordination."
    },
    {
      "type": "quiz",
      "id": "statelessness-v2",
      "poolId": "statelessness-pool",
      "level": 1,
      "prompt": "Why can you `curl` a public GET endpoint and get a meaningful answer without \"logging in\" first?",
      "choices": [
        "curl automatically authenticates as a guest user",
        "The endpoint remembers curl from previous requests",
        "The request itself contains everything the server needs to answer it — nothing prior is required",
        "GET requests are exempt from needing a connection"
      ],
      "correctIndex": 2,
      "explanation": "Statelessness means a request is self-contained — the server doesn't need any memory of what came before to produce a response, so there's nothing to \"log in\" to unless the application itself layers auth on top."
    },
    {
      "type": "quiz",
      "id": "keep-alive-parsing",
      "level": 2,
      "prompt": "Why does HTTP/1.1 keep-alive make writing a request parser harder than assuming one request per connection?",
      "choices": [
        "Keep-alive requests use a different text format",
        "The parser must re-synchronize on request boundaries itself, since the connection no longer closes to mark where one request ends",
        "Keep-alive requests can't have a body",
        "Keep-alive is only used for responses, never requests"
      ],
      "correctIndex": 1,
      "explanation": "Without a close to rely on, the parser has to track exactly how many bytes belonged to the current request (via Content-Length or chunked encoding) and treat anything after that as the start of the next one."
    },
    {
      "type": "code",
      "id": "byte-length",
      "level": 3,
      "prompt": "Content-Length has to count bytes, not characters — a body with multi-byte UTF-8 characters has more bytes than JS string .length would suggest. Write byteLength(str) so it returns the correct UTF-8 byte count for any string.",
      "starterCode": "function byteLength(str) {\n  // TODO: return the number of UTF-8 bytes in str\n}\n",
      "tests": [
        {
          "description": "ASCII string: 1 byte per character",
          "expr": "expect(byteLength(\"hello\")).toBe(5)"
        },
        {
          "description": "empty string is 0 bytes",
          "expr": "expect(byteLength(\"\")).toBe(0)"
        },
        {
          "description": "a multi-byte UTF-8 character counts as more than 1 byte",
          "expr": "expect(byteLength(\"héllo\")).toBe(6)"
        },
        {
          "description": "an emoji (4 UTF-8 bytes) is not the same as its JS .length",
          "expr": "expect(byteLength(\"👍\")).toBe(4)"
        }
      ],
      "solution": "function byteLength(str) {\n  return new TextEncoder().encode(str).length;\n}",
      "explanation": "TextEncoder().encode(str) gives you the actual UTF-8 byte sequence the string would be serialized to on the wire — .length on that Uint8Array is the real byte count. Plain str.length counts UTF-16 code units instead, which happens to match byte count for ASCII but silently diverges for anything outside it (accented letters, most emoji), which is exactly the kind of body that would get truncated by a Content-Length computed from the wrong measurement."
    }
  ]
}
