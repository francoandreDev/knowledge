{
  "items": [
    {
      "id": "keep-alive-savings",
      "level": 2,
      "title": "How much does keep-alive actually save?",
      "description": "L2 explained that keep-alive reuses one TCP connection instead of opening a new one per request. Move the sliders to see when that reuse starts to matter — and when it barely does.",
      "params": [
        {
          "name": "numRequests",
          "label": "Requests",
          "min": 1,
          "max": 20,
          "step": 1,
          "default": 5
        },
        {
          "name": "tcpHandshakeMs",
          "label": "Connection setup cost",
          "min": 10,
          "max": 200,
          "step": 10,
          "default": 50,
          "unit": "ms"
        },
        {
          "name": "requestMs",
          "label": "Cost per request",
          "min": 10,
          "max": 500,
          "step": 10,
          "default": 100,
          "unit": "ms"
        }
      ],
      "compute": "const withoutKeepAlive = params.numRequests * (params.tcpHandshakeMs + params.requestMs);\nconst withKeepAlive = params.tcpHandshakeMs + params.numRequests * params.requestMs;\nreturn { withoutKeepAlive, withKeepAlive };",
      "outputs": [
        {
          "key": "withoutKeepAlive",
          "label": "New connection every time",
          "unit": "ms",
          "color": "#f87171"
        },
        {
          "key": "withKeepAlive",
          "label": "One reused connection",
          "unit": "ms",
          "color": "#34d399"
        }
      ],
      "chartParam": "numRequests"
    }
  ]
}
