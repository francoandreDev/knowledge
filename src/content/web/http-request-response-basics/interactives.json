{
  "items": [
    {
      "id": "keep-alive-savings",
      "level": 2,
      "title": "How much does keep-alive actually save?",
      "description": "L3's own numbers: a cold connection pays roughly 90ms for DNS+TCP+TLS setup before a single request/response round-trip (about 40ms) even happens. Keep-alive pays that 90ms once and reuses the connection. Move the sliders to see when reuse starts to matter — and when it barely does.",
      "params": [
        {
          "name": "numRequests",
          "label": "Requests",
          "min": 1,
          "max": 20,
          "step": 1,
          "default": 5
        },
        {
          "name": "tcpHandshakeMs",
          "label": "Connection setup cost (DNS+TCP+TLS)",
          "min": 10,
          "max": 200,
          "step": 10,
          "default": 90,
          "unit": "ms"
        },
        {
          "name": "requestMs",
          "label": "Cost per request (request+response)",
          "min": 10,
          "max": 500,
          "step": 10,
          "default": 40,
          "unit": "ms"
        }
      ],
      "compute": "const withoutKeepAlive = params.numRequests * (params.tcpHandshakeMs + params.requestMs);\nconst withKeepAlive = params.tcpHandshakeMs + params.numRequests * params.requestMs;\nreturn { withoutKeepAlive, withKeepAlive };",
      "outputs": [
        {
          "key": "withoutKeepAlive",
          "label": "New connection every time",
          "unit": "ms",
          "color": "#f87171"
        },
        {
          "key": "withKeepAlive",
          "label": "One reused connection",
          "unit": "ms",
          "color": "#34d399"
        }
      ],
      "chartParam": "numRequests"
    }
  ]
}
