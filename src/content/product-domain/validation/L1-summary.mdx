---
title: "L1 — How do I know if I'm building the right thing before I build it? (validation, prototyping, MVP thinking)"
---

import Scenario from "../../../components/Scenario.astro";
import LevelIntro from "../../../components/LevelIntro.astro";

<Scenario label="Six weeks of engineering, 1% adoption">
  <Fragment slot="facts">
    <div class="not-prose flex flex-col gap-2 text-sm text-slate-700 dark:text-slate-300">
      <div class="flex items-center gap-1.5"><span>🛠️</span> <strong>What was built</strong> — a full bulk CSV/PDF export feature, because sales asked for "export"</div>
      <div class="flex items-center gap-1.5"><span>⏱️</span> <strong>Time spent</strong> — 6 weeks of engineering, before a single customer saw it</div>
      <div class="flex items-center gap-1.5"><span>📉</span> <strong>Result</strong> — 2 of 200 eligible customers used it in the first month</div>
    </div>
  </Fragment>

Sales tells the product team customers want to "export their data."
The team spends six weeks building a full bulk export feature — CSV
and PDF, every field, configurable columns. It ships. A month later,
only 2 of the 200 eligible customers have used it. Nobody built
anything wrong, technically — the export feature works exactly as
designed. **The team built the thing they were asked for. Why did
almost nobody want it?**

Read the setup in plain language: "sales" is the group hearing
customer requests, "product" decides what should be built,
"engineering" builds it, and CSV/PDF are just downloadable file
formats. "Adoption" means the blunt part: how many people actually
used the thing after it existed.

</Scenario>

<LevelIntro
  items={[
    "Why a stated request isn't the actual problem",
    "Validation, prototype, and MVP as key terms",
    "Assumption vs. evidence, and what adoption means",
  ]}
/>

## The shape of the problem

- "Customers want to export their data" is a solution already, not the
  actual problem — it describes a mechanism (export) without
  confirming what customers are trying to accomplish once they have
  the exported file.
  It is like hearing "I need a hammer" and building a beautiful
  hammer before asking whether the real job was "hang this picture,"
  where a hook, tape, or a different tool might solve the need better.
- Building the full version of a feature commits weeks of effort to a
  guess about what people want, before any real evidence exists that
  the guess is correct — if the guess is wrong, all of that effort is
  spent finding out the hard way.
- The fix isn't building faster or building less — it's checking the
  underlying assumption cheaply, before committing the effort a full
  build requires.

## Key terms

- **Validation** — cheaply testing whether an assumption about what
  people want is actually true, before spending the effort to build
  the full solution based on that assumption.
- **Prototype** — a fast, low-effort stand-in for a feature, built
  specifically to test a question rather than to be shipped as the
  real thing.
- **MVP (minimum viable product)** — the smallest version of an idea
  that can actually test the riskiest assumption behind it — not
  simply "a smaller, cheaper version of the final feature."
- **Assumption vs. evidence** — the difference between believing
  something about what customers want and having a real signal
  (behavior, not just stated preference) that confirms it.
- **Adoption** — actual use after something is available. In this
  story, 2 out of 200 means almost nobody adopted the export.
- **Export** — taking data out of the product into a file, such as a
  CSV spreadsheet or a PDF document.

## What this unit covers

L2 works through the validate-before-build loop and why "the smallest
version of the final feature" is a different thing from "the smallest
test of the riskiest assumption." L3 walks through what actually
happened when a second team tested the same "export" assumption before
building anything, what they found out, and what they built instead.
