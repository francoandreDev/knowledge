---
title: "L1 — Why do two teams that agree on a word still build things that don't fit together?"
---

import Scenario from "../../../components/Scenario.astro";

<Scenario label="Two teams, one word, one integration bug">
  <Fragment slot="facts">
    <div class="not-prose flex flex-col gap-2 text-sm text-slate-700 dark:text-slate-300">
      <div class="flex items-center gap-1.5"><span>🛒</span> <strong>Sales' "Order"</strong> — created the moment a cart is submitted, before payment clears or stock is confirmed</div>
      <div class="flex items-center gap-1.5"><span>📦</span> <strong>Warehouse's "Order"</strong> — something ready to physically pick, pack, and ship</div>
      <div class="flex items-center gap-1.5"><span>💥</span> <strong>The bug</strong> — Warehouse started shipping orders the moment Sales created them, including several whose payment later failed</div>
    </div>
  </Fragment>

Sales and Warehouse both agree, in plain conversation, on what an "Order"
is — a customer bought something. Sales wires their `Order` object
straight into Warehouse's system the moment a cart is submitted.
Warehouse starts picking and packing immediately. Weeks later, someone
notices dozens of shipped packages whose payment never actually cleared.
**Both teams used the same word and never disagreed about its
definition out loud — so where did the mismatch actually come from?**

In this example, **Sales** is the part of an online store system that
handles carts and purchases. **Warehouse** is the part that prepares
physical packages to ship. An `Order` object is simply the data record
Sales creates for a submitted cart.

</Scenario>

## The shape of the problem

- Two teams can use the exact same word, agree on its dictionary meaning,
  and still build systems that don't fit together — because the word
  carries a different set of _guarantees_ in each team's world, even when
  neither team would describe it differently if asked directly.
- A **guarantee** is something another part of the system can safely
  assume is already true. For Warehouse, "safe to ship" means payment is
  confirmed and stock is available; Sales' early `Order` does not
  guarantee those things yet.
- Wiring one team's internal model directly into another team's system —
  with no translation in between — means every future change to the first
  team's model silently becomes a risk to the second team's system too.

## Key terms

- **Bounded context** — the boundary around a part of the system where a
  concept's specific meaning and rules hold; the same word can mean
  something different just outside that boundary.
- **Context map** — a picture of which bounded contexts exist and how
  they relate to and depend on each other.
- **Upstream / downstream** — in a relationship between two contexts, the
  upstream context's model changes independently and the downstream
  context has to adapt to it, not the other way around.
- **Anticorruption layer** — a deliberate translation step at the
  boundary between two contexts, so that one context's internal model
  never leaks directly into another's — the fix Sales and Warehouse were
  missing.
- **Model** — the names, data, and rules a part of the system uses to
  represent its work. Sales' model can be correct for Sales and still be
  unsafe for Warehouse to use directly.

## What this unit covers

L2 works through why "same word, same intent, still incompatible" happens
even between well-meaning teams, and what an anticorruption layer actually
does at the boundary. L3 works the Sales/Warehouse example through in
full — the bug, the fix, and what changes when one side has no power to
negotiate the shape of the relationship at all.
