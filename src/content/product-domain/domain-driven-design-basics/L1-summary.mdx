---
title: "L1 — How do I model a business domain so the code doesn't fight the business?"
---

import Scenario from "../../../components/Scenario.astro";

<Scenario label="Two teams, one word, two different meanings">
  <Fragment slot="facts">
    <div class="not-prose flex flex-col gap-2 text-sm text-slate-700 dark:text-slate-300">
      <div class="flex items-center gap-1.5"><span>💳</span> <strong>Billing team's "Customer"</strong> — whoever pays the invoice (a company's finance department)</div>
      <div class="flex items-center gap-1.5"><span>🎫</span> <strong>Support team's "Customer"</strong> — whoever opens a support ticket (an employee at that company)</div>
      <div class="flex items-center gap-1.5"><span>🐛</span> <strong>The bug</strong> — a churn-risk report counted support tickets per "Customer," billing counted invoices per "Customer" — same word, two different numbers</div>
    </div>
    <div class="not-prose mt-3 grid grid-cols-2 gap-2 text-xs">
      <div class="rounded border border-rose-300 bg-rose-50 p-1.5 dark:border-rose-800 dark:bg-rose-950">
        <p class="font-semibold text-rose-600 dark:text-rose-400">Billing's count</p>
        1,200 "Customers" (paying accounts)
      </div>
      <div class="rounded border border-emerald-300 bg-emerald-50 p-1.5 dark:border-emerald-800 dark:bg-emerald-950">
        <p class="font-semibold text-emerald-600 dark:text-emerald-400">Support's count</p>
        4,800 "Customers" (individual ticket submitters)
      </div>
    </div>
  </Fragment>

An executive asks: "How many customers do we have?" Billing answers 1,200.
Support answers 4,800. Both are right — and both are talking about
something genuinely different. Billing's "Customer" is the paying account
(one per company). Support's "Customer" is whoever opens a ticket (often
several people per company). **The system has one database table called
`customers`. Which definition does it actually implement — and what
happens to every report and every line of code built on top of it?**

The table was built early on, by whichever team touched it first, using
their own meaning of "customer" — and every other team's code silently
inherited that meaning, whether it matched their own mental model or not.

</Scenario>

- **A word used casually in business conversation and a word used as a
  code concept aren't automatically the same thing — and when they quietly
  drift apart, every report, every bug fix, and every new feature built on
  top of the mismatch inherits the confusion.** "Customer" wasn't wrong in
  either team's mouth; it was two different concepts sharing one name.
- **Ubiquitous language** is the practice of deliberately keeping the
  words used in business conversation and the words used in the code
  identical and precise — when a domain expert says "Customer," the code's
  `Customer` should mean exactly that, not an approximation drifted from
  whichever team happened to build the database table first.
- **When the same word genuinely means different things in different parts
  of the business, the fix isn't picking one "correct" definition** — it's
  recognizing these are two separate concepts that happen to share a
  label, and giving each one its own explicit name and model: a **bounded
  context** is the boundary around where a term's specific meaning holds.
  Inside the billing context, "Customer" means the paying account. Inside
  the support context, it means the ticket submitter. Neither is wrong;
  they're just scoped differently.
- **Modeling the domain means capturing the actual business concepts and
  rules the domain experts think in — not just storing whatever data
  fields happen to be convenient.** A domain model that says "a lapsed
  subscription enters a 14-day grace period before cancellation" captures
  a real business rule; a database column called `status` with no
  enforced rules around what states can follow which just stores data
  without capturing what the business actually means by it.
- The practical skill this unit builds: **noticing when a shared word is
  hiding two different concepts**, and giving each one an explicit,
  correctly-scoped name instead of forcing a single definition to serve
  every team.

## What the mismatch actually costs

|                                     | One shared, undefined "Customer"                             | Two explicit, scoped concepts                                                |
| ----------------------------------- | ------------------------------------------------------------ | ---------------------------------------------------------------------------- |
| Executive asks "how many customers" | Ambiguous — depends who answers                              | Clear: "1,200 paying accounts" or "4,800 support contacts," named explicitly |
| New engineer joins Billing          | Has to reverse-engineer which meaning the code actually uses | Reads the model, sees `BillingAccount`                                       |
| A report merges both teams' data    | Silently double-counts or undercounts                        | Each concept has its own field, explicitly related                           |
