---
title: "L3 — The meeting that resolves it, and what changes at real organizational scale"
---

import Scenario from "../../../components/Scenario.astro";

<Scenario label="The reconciliation meeting">
  <Fragment slot="facts">
    <div class="not-prose flex flex-col gap-2 text-sm text-slate-700 dark:text-slate-300">
      <div class="flex items-center gap-1.5"><span>👩‍💼</span> <strong>Dana</strong> — Billing lead</div>
      <div class="flex items-center gap-1.5"><span>🧑‍💼</span> <strong>Marcus</strong> — Support lead</div>
      <div class="flex items-center gap-1.5"><span>🧑‍💻</span> <strong>Priya</strong> — engineer, called in after the conflicting reports reached an executive</div>
    </div>
  </Fragment>

**Before reading the conversation: is the goal of this meeting to decide
whose definition of "Customer" is correct?**

</Scenario>

## The conversation

> **Priya:** "Before we talk about the report numbers — Dana, when you say
> 'Customer,' what are you actually counting?"
>
> **Dana:** "The account that pays the invoice. One per company, usually."
>
> **Priya:** "And Marcus, when Support says 'Customer'?"
>
> **Marcus:** "Whoever opened the ticket. Could be five different people
> at the same company in a given month."
>
> **Priya:** "So these are both real, both correct for what each of you
> actually needs — they're just not the same thing. What if we stop
> calling both of them 'Customer' and name them separately: a
> **Billing Account** for what Dana's team tracks, and a **Support
> Contact** for what Marcus's team tracks?"
>
> **Dana:** "That works for us — but how does the executive dashboard
> combine them without going back to being confusing?"
>
> **Priya:** "Every Support Contact links to exactly one Billing Account —
> that relationship already exists, people just weren't naming it. The
> dashboard can say '1,200 Billing Accounts, 4,800 Support Contacts' and
> both numbers are correct and clearly labeled."

The disagreement wasn't a disagreement at all once it was named correctly
— Dana and Marcus were never wrong about their own numbers, they were
using one shared word for two different, equally real concepts.

## What changed, concretely

| Before                                                                             | After                                                                     |
| ---------------------------------------------------------------------------------- | ------------------------------------------------------------------------- |
| One `customers` table, meaning silently depended on who queried it                 | `BillingAccount` and `SupportContact`, each with an explicit definition   |
| "How many customers" had no single correct answer                                  | "How many billing accounts" and "how many support contacts" both have one |
| The relationship between the two existed in practice but wasn't named              | Explicit: each Support Contact belongs to exactly one Billing Account     |
| New team members had to ask around to learn which "Customer" meaning applied where | The name itself tells you — no tribal knowledge required                  |

## Extend the scenario: what if the business hasn't agreed on its own terms yet?

**The meeting above assumes Dana and Marcus each already had a clear,
internally consistent idea of what their own team means by "Customer" —
they just hadn't compared notes. What if a team's own definition is
genuinely fuzzy, even internally?**

This is common, and it's a different problem than the one above: it's not
that two clear definitions collided, it's that no clear definition exists
yet at all. The fix isn't a naming exercise — it's the harder work of
sitting with the domain experts until a genuinely precise definition
emerges, often by asking about edge cases directly ("if someone opens a
ticket but never actually pays anything, are they a Support Contact? A
prospective Billing Account? Neither, until they convert?"). Ubiquitous
language can only be kept precise once it exists — it can't paper over a
concept nobody has actually pinned down yet.

An **edge case** is a rare or boundary case that forces the rule to become
clear. It is useful precisely because a vague definition often breaks
when you test it against unusual examples.

## Extend the scenario: what happens at the scale of ten teams, not two?

**With just Billing and Support, one conversation and one relationship
were enough. What changes once Sales, Marketing, Finance, and six other
teams are all using overlapping terms slightly differently?**

At two teams, informally comparing definitions in a meeting works fine.
As the number of teams with their own bounded context grows, the number
of _pairs_ of contexts that might define an overlapping term differently
grows much faster than the number of teams itself — with ten teams, there
are 45 possible pairs to reconcile, not 10. At that scale, ad-hoc
meetings stop scaling, and organizations typically maintain an explicit
**context map**: a standing, documented record of which contexts exist,
what each owns, and how terms translate at every boundary that actually
matters — replacing "ask around until someone remembers" with something
written down and kept current.

Why 45? A pair means two teams compared together. With four teams you
would have A-B, A-C, A-D, B-C, B-D, and C-D: six pairs already. Ten teams
create many more pairings, so the organization needs a written map. That
map can be as simple as a table: context, what this term means here, what
other context it connects to, and how the term translates at that
boundary.

## Failure modes

| Failure mode                                                                                            | What it gets wrong                                                                                                          |
| ------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------- |
| Picking one team's definition as "correct" and forcing everyone onto it                                 | Deletes real information the other team's model actually needs                                                              |
| Splitting every ambiguous-sounding word into separate concepts by default                               | Adds unnecessary boundaries and relationships to maintain where none were actually needed                                   |
| Naming two concepts separately but never defining the relationship between them                         | Recreates the original confusion one level up — now with two names instead of one                                           |
| Assuming a fuzzy internal definition just needs a better name                                           | A name can't fix a concept nobody has actually pinned down yet — that requires sitting with domain experts on the substance |
| Relying on informal conversations to keep terms aligned once the organization has many bounded contexts | Doesn't scale past a handful of contexts — needs an explicit, maintained context map                                        |
