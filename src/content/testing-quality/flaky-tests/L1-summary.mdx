---
title: "L1 — Why does my test suite pass locally but fail in CI? (flaky tests, non-determinism, environment leakage)"
---

import Scenario from "../../../components/Scenario.astro";

<Scenario label="Passes every time on your laptop. Fails about once every twenty runs in CI — always a different test.">
  <Fragment slot="facts">
    <div class="not-prose flex flex-col gap-2 text-sm text-slate-700 dark:text-slate-300">
      <div class="flex items-center gap-1.5"><span>💻</span> <strong>Locally</strong> — 100/100 runs pass, always the same order</div>
      <div class="flex items-center gap-1.5"><span>🤖</span> <strong>In CI</strong> — fails roughly 1 in 20 runs, no code change in between</div>
      <div class="flex items-center gap-1.5"><span>❓</span> <strong>The actual cause</strong> — two tests share one mutable in-memory store, and CI runs tests in a different order than a developer's machine ever does</div>
    </div>
  </Fragment>

A test suite has 40 tests. Every one passes, every time, on every
developer's laptop. In CI, about once every twenty runs, one test
fails — never the same one twice, and re-running the exact same commit
usually makes it pass. Nobody touched the failing test recently.
**If the code and the test are both unchanged between a passing run
and a failing run, what's actually different?**

</Scenario>

## The shape of the problem

- A **flaky test** is one that produces different results (pass or
  fail) across runs of the _same_ code with no relevant input change
  — its outcome depends on something the test itself doesn't control
  or account for.
- Flakiness is almost never about the assertion being wrong — the
  logic under test is usually fine. It's about the test's outcome
  secretly depending on something outside the test: execution order,
  real timing, real randomness, or state left behind by another test.
- CI often exposes flakiness that local runs never do, not because CI
  is "less reliable," but because CI frequently runs tests in a
  different order, in parallel, or under different timing than a
  single developer running the same suite the same way every time.

## Key terms

- **Non-determinism** — a test's outcome can vary between runs of
  identical code, because it depends on something that isn't fixed
  (the system clock, random number generation, network latency,
  thread/task scheduling).
- **Test isolation** — the property that one test's outcome doesn't
  depend on whether, or in what order, any other test ran before it.
- **Environment leakage** — state (in-memory, on disk, in a shared
  service) created or modified by one test that isn't cleaned up,
  and ends up affecting a different test that runs afterward.
- **Race condition (in a test)** — a test asserts on the result of an
  asynchronous operation before that operation has actually finished,
  so the outcome depends on unpredictable timing rather than logic.

## What this unit covers

L2 maps the main categories of flakiness (shared state, real time,
real randomness, unresolved async work) and why CI's execution model
surfaces them more often than a single developer's local runs do. L3
walks the module-level shared-store bug behind this unit's Scenario
through a full fix, plus a second, equally common flakiness cause: an
async assertion that runs before the operation it's checking has
actually completed.
