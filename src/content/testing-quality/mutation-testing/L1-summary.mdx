---
title: "L1 — How do I know if my tests are actually testing anything? (mutation testing, coverage as a signal, not a target)"
---

import Scenario from "../../../components/Scenario.astro";
import LevelIntro from "../../../components/LevelIntro.astro";

<Scenario label="95% coverage, and the bug shipped anyway">
  <Fragment slot="facts">
    <div class="not-prose flex flex-col gap-2 text-sm text-slate-700 dark:text-slate-300">
      <div>
        <div class="mb-1 flex items-center justify-between">
          <span>Line coverage</span><span class="font-semibold">95%</span>
        </div>
        <div class="h-2 w-full overflow-hidden rounded-full bg-slate-200 dark:bg-slate-700">
          <div class="h-full rounded-full bg-emerald-500 animate-fill-bar" style="--target-width: 95%"></div>
        </div>
      </div>
      <div>
        <div class="mb-1 flex items-center justify-between">
          <span>Mutation score</span><span class="font-semibold">42%</span>
        </div>
        <div class="h-2 w-full overflow-hidden rounded-full bg-slate-200 dark:bg-slate-700">
          <div class="h-full rounded-full bg-rose-500 animate-fill-bar" style="--target-width: 42%"></div>
        </div>
      </div>
      <div class="flex items-center gap-1.5"><span>🐛</span> <strong>The bug</strong> — a heavy-package surcharge boundary flipped from <code>&gt;</code> to <code>&gt;=</code>, and shipped anyway</div>
      <div class="flex items-center gap-1.5"><span>✅</span> <strong>The existing test</strong> — ran that exact line, but never checked the value it produced at the boundary</div>
    </div>
  </Fragment>

A shipping-fee calculator has a test suite. The team's dashboard shows
**95% line coverage** — almost every line in the file runs during the
test run. Then a one-character bug ships: the surcharge boundary for
heavy packages got flipped from `weight > 20` to `weight >= 20`, and a
package weighing exactly 20kg gets charged a surcharge it shouldn't.

| Signal                                   | Value | Would a reviewer trust it?                |
| ---------------------------------------- | ----- | ----------------------------------------- |
| Line coverage (did the code run?)        | 95%   | Looks great on a dashboard                |
| Mutation score (would a bug get caught?) | 42%   | Reveals most of that 95% never got tested |

**The buggy line ran during the test suite. Coverage counted it as
"tested." So why didn't any test fail?**

Because coverage only asks _did this line execute_ — never _would a test
fail if this line's logic were wrong_. A test can run a line and never
inspect the value that line produced. That gap is exactly what mutation
testing is built to expose.

</Scenario>

<LevelIntro
  items={[
    "Why coverage can be near-100% and still miss real bugs",
    "What a 'mutant', a 'kill', and a 'survivor' are",
    "What a mutation score actually measures",
  ]}
/>

## The shape of the problem

- **Coverage measures execution, not verification.** A line counted as
  "covered" only means some test made it run — not that any test checked
  whether it ran correctly.
- **Mutation testing measures verification directly**, by asking a
  sharper question: if this line's logic were subtly wrong, would any
  test in the suite actually fail?
- It answers that question mechanically: it makes a tiny, deliberate
  change to the code (a **mutant**) — like flipping `>` to `>=`, or `+`
  to `-` — then reruns the test suite against that mutated code.
- If a test fails, the mutant is **killed** — the suite caught it. If
  every test still passes, the mutant **survived** — nothing in the
  suite would have noticed that exact bug either.
- The **mutation score** is the percentage of mutants killed. A low
  score on high-coverage code is the tell: the tests run the code but
  don't check the right things — the same gap `assertions-matter`
  describes for a single test, generalized into a repeatable, automated
  check across an entire file.

## Key terms

- **Mutant** — a copy of the code with one small, deliberate change
  applied (one operator, one comparison, one line).
- **Mutation operator** — the rule used to generate a mutant (e.g.
  "replace `>` with `>=`", "delete this statement").
- **Kill** — at least one test fails against the mutant; the suite
  caught the injected bug.
- **Survive** — every test still passes against the mutant; the suite
  has a real gap at that exact spot.
- **Mutation score** — killed mutants ÷ total mutants, as a percentage.
  The mutation-testing analogue of a coverage percentage, but measuring
  whether tests actually assert on the right thing.
- **Equivalent mutant** — a mutant that can't actually be killed because
  the change never affects observable behavior (a false survivor, not a
  real gap — covered in L3).
