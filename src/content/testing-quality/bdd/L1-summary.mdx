---
title: "L1 — How do I test behavior instead of implementation? (BDD, testing through the public interface)"
---

import Scenario from "../../../components/Scenario.astro";
import LevelIntro from "../../../components/LevelIntro.astro";

<Scenario label="No behavior changed, but 15 tests turned red">
  <Fragment slot="facts">
    <div class="not-prose flex flex-col gap-2 text-sm text-slate-700 dark:text-slate-300">
      <div class="flex items-center gap-1.5"><span>🔧</span> <strong>The refactor</strong> — a shopping cart's internal storage changes from an array to a Map</div>
      <div class="flex items-center gap-1.5"><span>✅</span> <strong>What still works</strong> — adding items, getting the total, checking the item count — all unchanged</div>
      <div class="flex items-center gap-1.5"><span>🔴</span> <strong>What broke</strong> — 15 tests, all asserting directly on <code>cart.items.length</code> and <code>cart.items[0]</code></div>
    </div>
  </Fragment>

An engineer refactors a shopping cart's internal storage from an array
to a `Map`, purely for performance — looking up an item by name used to
require scanning the whole array. Every externally visible behavior
still works exactly the same: adding items, computing the total,
counting items. Running the test suite, 15 tests turn red. **No
behavior actually changed. Why did over a dozen tests suddenly start
failing?**

</Scenario>

<LevelIntro
  items={[
    "Behavior vs. implementation detail",
    "Why the public interface is the real contract",
    "Given-When-Then as a test structure",
  ]}
/>

## The shape of the problem

- Those 15 tests weren't asserting on what the cart _does_ — they were
  asserting on how it happens to store data internally right now
  (`cart.items.length`, `cart.items[0].name`), details that were never
  part of the cart's actual contract with the rest of the codebase.
- A test suite that fails on every internal refactor, even when
  external behavior is unchanged, actively punishes improving the
  code — every refactor now has to also rewrite a pile of tests that
  had nothing real to say about whether the change was safe.
- The fix is testing through the same interface any other code
  actually uses to interact with the cart — its public methods — never
  the internals only the class itself should know about.

## Key terms

- **BDD (behavior-driven development)** — writing tests that describe
  what a system does, from the outside, in terms of its actual
  observable behavior — not how it's implemented internally.
- **Public interface** — the methods and behavior a class exposes for
  everything else to use; the actual contract other code depends on.
- **Implementation detail** — internal structure (which data structure
  is used, how a value is computed) that's free to change as long as
  the public interface's behavior stays the same.
- **Given-When-Then** — a common structure for writing a behavior test:
  the starting state (given), the action taken (when), and the
  expected observable outcome (then) — phrased entirely in terms of
  behavior, never internals.

## What this unit covers

L2 works through exactly where the line sits between behavior and
implementation, and what Given-When-Then looks like in practice. L3
rebuilds the shopping cart both ways — implementation-coupled tests and
behavior-based tests — and runs both against the actual array-to-Map
refactor to show precisely which ones break and which ones don't.
