---
title: "L1 — How do I write a test that actually catches bugs, not just exercises code?"
---

import Scenario from "../../../components/Scenario.astro";
import LevelIntro from "../../../components/LevelIntro.astro";

<Scenario label="100% green, and a real bug shipped anyway">
  <Fragment slot="facts">
    <div class="not-prose flex flex-col gap-2 text-sm text-slate-700 dark:text-slate-300">
      <div class="flex items-center gap-1.5"><span>💰</span> <strong>The spec</strong> — orders of $100 or more get a 10% discount</div>
      <div class="flex items-center gap-1.5"><span>🐛</span> <strong>The bug</strong> — code checks <code>subtotal &gt; 100</code> instead of <code>&gt;= 100</code>, so a $100 order gets no discount</div>
      <div class="flex items-center gap-1.5"><span>✅</span> <strong>The existing test</strong> — <code>expect(typeof total).toBe('number')</code> — passes on both the buggy and the correct code</div>
    </div>
  </Fragment>

The test suite is green. Every test passes. A customer places a $100
order and doesn't get the discount they were promised — the code checks
`subtotal > 100` instead of `subtotal >= 100`, a one-character bug. **The
test file has a test for this exact function. Why didn't it catch a bug
this simple?**

The test checks `typeof calculateOrderTotal(100) === 'number'`. That
assertion is true whether the discount was applied correctly, applied
incorrectly, or not applied at all — it only confirms the function
returns _a number_, never _which_ number. The test exercised the code.
It never actually checked the thing that mattered.

</Scenario>

- **A test passing tells you the code ran without crashing — it doesn't
  tell you the code did the right thing, unless the assertion itself
  checks for the right thing specifically.** These are different claims,
  and a weak assertion only proves the first one.
- **A tautological test is one that's mathematically guaranteed to pass
  regardless of whether the code is correct** — checking that a result
  "is a number," "is defined," or "doesn't throw" when the actual bug
  risk is about _which_ number, _which_ value, _which_ behavior.
- **The sharpest test for whether an assertion is doing real work: would
  it fail if the implementation had a plausible bug?** If a one-character
  change to the real code (like `>` instead of `>=`) wouldn't make any
  existing test fail, none of those tests were actually testing that
  behavior — they were exercising the code path without checking its
  output.
- **This isn't about writing more tests — the original codebase already
  had a test for this function.** It's about whether each test's
  assertion is precise enough to distinguish correct behavior from the
  specific incorrect behaviors that are actually likely to occur, especially
  right at boundary values like exactly $100.
- The practical skill this unit builds: **before trusting a passing test,
  asking what specific bug it would have caught** — and if the honest
  answer is "none, it would pass either way," treating that as a test
  that needs a sharper assertion, not a real safety net.

<LevelIntro
  items={[
    "What a tautological assertion is",
    "The 'would this catch a plausible bug' test",
    "Weak vs. strong assertions on the same function",
  ]}
/>

## Two tests, same function, different guarantees

|                                | Weak assertion                         | Strong assertion                                            |
| ------------------------------ | -------------------------------------- | ----------------------------------------------------------- |
| What it checks                 | `typeof result === 'number'`           | `calculateOrderTotal(100) === 90`                           |
| Passes on the buggy version?   | Yes                                    | No                                                          |
| Passes on the correct version? | Yes                                    | Yes                                                         |
| What passing actually proves   | The function ran and returned a number | The discount boundary is handled correctly, at exactly $100 |
