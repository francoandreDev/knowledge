---
title: "L1 — Why does writing the test first change what the code ends up looking like?"
---

import Scenario from "../../../components/Scenario.astro";

<Scenario label="Same feature, two starting points">
  <Fragment slot="facts">
    <div class="not-prose flex flex-col gap-2 text-sm text-slate-700 dark:text-slate-300">
      <div class="flex items-center gap-1.5"><span>🖨️</span> <strong>Code-first attempt</strong> — a password checker that `console.warn`s issues directly</div>
      <div class="flex items-center gap-1.5"><span>🧪</span> <strong>Test-first attempt</strong> — the same feature, but the interface came from the assertion written before any implementation</div>
      <div class="flex items-center gap-1.5"><span>🔀</span> <strong>The difference</strong> — one design can be reused in a UI or an API; the other can only print to a console</div>
    </div>
  </Fragment>

Two engineers are each asked to build a password-strength checker. One
starts by writing the checking logic, then adds `console.warn` calls to
report problems as they're found. The other starts by writing
`expect(check("short")).toEqual({ score: 0, feedback: [...] })` — before
any implementation exists at all — then writes just enough code to make
it pass. **Both engineers end up with code that correctly flags weak
passwords. Why does only one of the two designs turn out to be reusable
somewhere other than a terminal?**

</Scenario>

## The shape of the problem

- Writing an assertion forces a decision that's easy to skip when
  writing implementation first: exactly what shape the result has to
  take, before a single line of logic exists to produce it.
- Test-first isn't primarily about catching bugs sooner — it's a design
  pressure. An assertion that's awkward to write is usually a sign the
  interface being tested is awkward too, and that signal shows up
  _before_ the interface gets built, not after.

## Key terms

- **TDD (test-driven development)** — writing a failing test for a
  small piece of behavior before writing the code that implements it.
- **Red-green-refactor** — the three-step loop TDD runs in: red (write
  a test that fails, because the behavior doesn't exist yet), green
  (write the minimum code to make it pass), refactor (clean up the
  implementation without changing behavior, while the passing test
  guards against breaking it).
- **Minimum code to pass** — in the green step, resist writing more
  than the current test actually requires — extra, untested behavior
  has no test proving it's needed or correct.
- **Design pressure** — the effect of writing the test first: an
  interface that's hard to assert against gets noticed and reshaped
  immediately, rather than being discovered as awkward only after
  other code has already started depending on it.

## What this unit covers

L2 works through the red-green-refactor loop precisely, and why writing
the assertion first specifically pressures the design toward a
testable shape. L3 works a full example — the password-strength
checker — through several real red-green-refactor cycles, showing
exactly where the test-first interface diverges from what a
code-first version would likely have produced.
