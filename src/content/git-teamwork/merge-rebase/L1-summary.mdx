---
title: "L1 — Why does git pull sometimes leave a weird extra commit nobody wrote?"
---

import Scenario from "../../../components/Scenario.astro";

<Scenario label="One command, two very different outcomes">
  <Fragment slot="facts">
    <div class="not-prose flex flex-col gap-2 text-sm text-slate-700 dark:text-slate-300">
      <div class="flex items-center gap-1.5"><span>✅</span> <strong>Monday's pull</strong> — clean, no extra commit, history stays a straight line</div>
      <div class="flex items-center gap-1.5"><span>😕</span> <strong>Tuesday's pull</strong> — same command, but git adds a commit titled "Merge branch 'main' of ..."</div>
      <div class="flex items-center gap-1.5"><span>❓</span> <strong>Nobody wrote it</strong> — no one typed that commit message, and no code in it belongs to either person</div>
    </div>
  </Fragment>

An engineer runs `git pull` on Monday — it finishes cleanly, no extra
commits, history stays a straight line. On Tuesday, the exact same
command on the exact same branch adds a new commit nobody wrote:
"Merge branch 'main' of ...". **Same command, same person, same repo.
What changed between Monday and Tuesday that made git decide a new
commit was needed this time?**

</Scenario>

## The minimum map

- **Local** is your copy of the repository on your machine.
- **Remote** is the shared copy, usually the one hosted on GitHub,
  GitLab, or another server.
- A **commit** is one saved snapshot in the history.
- A **branch** is a movable label pointing at one commit.
- **HEAD** means "the commit my current branch is pointing at right
  now."

So when this unit says "local and remote diverged," it means: your
branch label and the remote branch label no longer point along the
same simple line of commits. Each side has at least one commit the
other side does not have yet.

## The shape of the problem

- `git pull` is really two steps combined: fetch the remote's latest
  commits, then reconcile them with local history — and _how_ it
  reconciles depends entirely on whether local history has moved since
  the two last agreed on a common point.
- When local and remote have both added commits since they diverged,
  git can't just "add the new stuff" — it has to decide how to combine
  two different, real sequences of change, and a merge commit is one
  specific way of recording that decision, not the only one.

| What you see after `git pull`                   | What it usually means                                                                | What to do next                                                               |
| ----------------------------------------------- | ------------------------------------------------------------------------------------ | ----------------------------------------------------------------------------- |
| It finishes quietly, with no merge commit       | Your local commit was already behind remote, so git fast-forwarded your branch       | Keep working; history stayed linear because only remote had moved             |
| An editor opens for a `Merge branch ...` commit | Both local and remote had new commits, so git is recording that the histories joined | Confirm the merge if the combined code is correct; resolve conflicts if asked |
| Git reports conflicts                           | Both histories touched overlapping content that git could not combine automatically  | Edit the files, choose the correct combined result, then complete the merge   |
| You pulled with rebase and see replay messages  | Git is copying your local commits on top of remote's latest commit                   | Check each replayed commit; conflicts can still happen one commit at a time   |

## Key terms

- **Fast-forward** — when the local branch's current commit is a
  direct ancestor of the remote's commit, so incorporating the remote's
  changes just means moving the local pointer forward — no new commit
  needed, no reconciliation required.
- **Merge commit** — when local history has diverged from remote (both
  sides added commits since the common point), git creates a new
  commit with two parents to record that both histories are now
  combined.
- **Rebase** — instead of creating a merge commit, replaying local
  commits one by one on top of the remote's latest commit, producing a
  straight-line history as if the local work had started from there in
  the first place.
- **Divergence** — the state where local and remote both have commits
  the other doesn't, counted from their last common ancestor — this is
  exactly the condition that makes a fast-forward impossible.

## What this unit covers

L2 works through exactly when a fast-forward is possible versus when
divergence forces a real reconciliation, and what merge and rebase each
actually do differently with the same diverged history. L3 implements
both a real ancestor-check function and a real rebase operation on an
actual commit graph, to show precisely why Monday's pull stayed clean
and Tuesday's didn't.
