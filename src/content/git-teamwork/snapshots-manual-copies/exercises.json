{
  "items": [
    {
      "type": "quiz",
      "id": "git-snapshot-vs-diff",
      "level": 1,
      "prompt": "What does Git actually store when you make a commit?",
      "choices": [
        "A diff (line-by-line changes) from the previous commit",
        "A full snapshot of the entire project, with unchanged files deduplicated by content",
        "A copy of only the files you explicitly listed in the commit message",
        "A compressed archive of the working directory, re-copied in full every time"
      ],
      "correctIndex": 1,
      "explanation": "Git conceptually stores a full snapshot per commit, not a diff — diffs are computed on demand for display. Snapshots stay cheap because unchanged file content is deduplicated by its content hash, not re-copied, so committing cost is proportional to what changed, not total project size."
    },
    {
      "type": "quiz",
      "id": "git-three-states",
      "level": 1,
      "prompt": "You edit a file, then run `git add file.txt`, but haven't run `git commit` yet. Which statement is true?",
      "choices": [
        "The change is already permanently in the project's history",
        "The change is staged (marked for the next snapshot) but not yet committed to history",
        "Nothing has happened yet — `git add` only previews the change",
        "The change is committed, but not yet pushed to a remote"
      ],
      "correctIndex": 1,
      "explanation": "The three-state model (working directory → staging area → repository) means staging is a real, distinct middle state: `git add` moves a change into 'will be part of the next snapshot,' but only `git commit` actually writes that snapshot into permanent history."
    },
    {
      "type": "quiz",
      "id": "git-branch-cost",
      "poolId": "branch-cost-pool",
      "level": 2,
      "prompt": "Why is creating a new Git branch nearly instant, regardless of how large the project is?",
      "choices": [
        "Git only creates branches for small projects",
        "A branch is just a movable pointer to a commit — no files are copied or snapshotted to create one",
        "Branch creation happens lazily and doesn't actually finish until the first commit on it",
        "Git compresses the whole project before branching, which is fast on modern hardware"
      ],
      "correctIndex": 1,
      "explanation": "A branch is a small text file holding a commit hash (a pointer), nothing more. Creating one writes that pointer and nothing else — it's O(1) with respect to project size because it never touches the working files or the history that already exists."
    },
    {
      "type": "quiz",
      "id": "git-branch-cost-v2",
      "poolId": "branch-cost-pool",
      "level": 2,
      "prompt": "In the manual-copies world (no version control), what's the closest equivalent to \"try a risky change without touching the working version,\" and why is it worse than a Git branch?",
      "choices": [
        "Editing the file directly and hoping you remember the original — worse because there's no safety net at all",
        "Copying the entire project directory — worse because it's O(project size) in both time and disk, unlike a branch's O(1) pointer",
        "Emailing a copy to a teammate — worse because it introduces a dependency on someone else",
        "There is no equivalent; manual-copy workflows never attempt this"
      ],
      "correctIndex": 1,
      "explanation": "Manual full-directory copies scale with total project size every single time, and multiply the more experimental branches you want to keep around simultaneously — the exact cost a Git branch's pointer-only design avoids entirely."
    },
    {
      "type": "quiz",
      "id": "git-content-addressing",
      "level": 2,
      "prompt": "Two different commits both contain a file with byte-for-byte identical content. How many times does Git actually store that content?",
      "choices": [
        "Twice — once per commit, since commits are independent snapshots",
        "Once — Git addresses objects by the hash of their content, so identical content is stored a single time and referenced from both commits",
        "Zero times — Git only stores diffs, and there's no diff between identical files",
        "It depends on whether the two commits are on the same branch"
      ],
      "correctIndex": 1,
      "explanation": "Git's objects are content-addressed: the storage key is a hash of the bytes themselves, not the filename, path, or commit. Identical content anywhere in history hashes to the same key and is physically stored exactly once, referenced by as many commits/trees as need it."
    },
    {
      "type": "code",
      "id": "git-content-dedup",
      "level": 3,
      "prompt": "Content-addressed storage means identical content is only ever stored once, no matter how many places reference it. Implement `dedupeByContent(files)`, which takes an array of file content strings and returns an array of the *unique* content strings, in the order each first appeared — modeling the core idea behind Git's object store.",
      "starterCode": "function dedupeByContent(files) {\n  // TODO: return unique content strings, first-occurrence order preserved\n}\n",
      "tests": [
        {
          "description": "removes exact duplicates",
          "expr": "expect(dedupeByContent(['a', 'b', 'a', 'c', 'b'])).toEqual(['a', 'b', 'c'])"
        },
        {
          "description": "preserves first-occurrence order",
          "expr": "expect(dedupeByContent(['x', 'y', 'x'])).toEqual(['x', 'y'])"
        },
        {
          "description": "returns everything when all unique",
          "expr": "expect(dedupeByContent(['a', 'b', 'c'])).toEqual(['a', 'b', 'c'])"
        },
        {
          "description": "handles an empty list",
          "expr": "expect(dedupeByContent([])).toEqual([])"
        }
      ],
      "solution": "function dedupeByContent(files) {\n  const seen = new Set();\n  const result = [];\n  for (const content of files) {\n    if (!seen.has(content)) {\n      seen.add(content);\n      result.push(content);\n    }\n  }\n  return result;\n}",
      "explanation": "A `Set` keyed on the content itself (a stand-in for Git's content hash) is exactly the deduplication mechanism content-addressed storage relies on: checking 'have I already stored this exact content?' before storing it again, rather than blindly appending every occurrence."
    }
  ]
}
