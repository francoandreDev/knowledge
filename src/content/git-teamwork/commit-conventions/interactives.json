{
  "items": [
    {
      "id": "bisect-steps-by-history-size",
      "level": 3,
      "title": "How many bisect steps does a larger history actually cost?",
      "description": "Using the exact binary-search logic from L3, drag the number of commits between the last known-good state and now. At 16 commits, this reproduces L3's verified result of 4 steps; at 64, it reproduces the extend-it result of 6 steps.",
      "params": [
        {
          "name": "historySize",
          "label": "Commits between good and bad",
          "min": 2,
          "max": 256,
          "step": 1,
          "default": 16,
          "unit": ""
        }
      ],
      "compute": "return { bisectSteps: Math.ceil(Math.log2(Math.max(2, params.historySize))) };",
      "outputs": [
        {
          "key": "bisectSteps",
          "label": "Bisect steps needed",
          "unit": "",
          "color": "#34d399"
        }
      ],
      "chartParam": "historySize"
    }
  ]
}
