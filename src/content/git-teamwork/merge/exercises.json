{
  "items": [
    {
      "type": "quiz",
      "id": "merge-base-1",
      "poolId": "merge-base-def-pool",
      "level": 1,
      "prompt": "What is the merge base?",
      "choices": [
        "The most recently modified file in the repository",
        "The last commit both branches share before they diverged",
        "The branch with more total commits",
        "Whichever branch was created first"
      ],
      "correctIndex": 1,
      "explanation": "The merge base is the shared ancestor commit — the point where the two branches' histories split. Git needs it to tell what each branch actually changed.",
      "reference": "<div class=\"not-prose rounded-md border border-slate-300 bg-slate-50 p-3 text-xs dark:border-slate-700 dark:bg-slate-900\"><div class=\"mb-1.5 font-semibold text-slate-700 dark:text-slate-300\">Merge base</div><div class=\"flex flex-col gap-1 text-slate-600 dark:text-slate-400\"><div>📍 the last commit both branches share</div><div>🌿 Alex's branch → diverges here</div><div>🌿 Sam's branch → diverges here</div></div></div><p class=\"mt-2 text-xs text-slate-600 dark:text-slate-400\"><strong>Why:</strong> git diffs each branch against this shared commit separately — that's what lets it tell \"Alex changed this\" apart from \"Sam changed this.\"</p>"
    },
    {
      "type": "quiz",
      "id": "merge-base-2",
      "poolId": "merge-base-def-pool",
      "level": 1,
      "prompt": "Why does git need a merge base at all instead of just comparing the two branches to each other?",
      "choices": [
        "It doesn't — a direct comparison would work just as well",
        "The merge base lets git attribute each change to whoever made it, not just see that the files disagree",
        "The merge base is only used for fast-forward merges",
        "The merge base determines who has commit permissions"
      ],
      "correctIndex": 1,
      "explanation": "A direct two-way diff only shows that two files disagree, with no way to know who caused the disagreement. Comparing each branch against the shared base is what makes attribution possible.",
      "reference": "<div class=\"not-prose rounded-md border border-slate-300 bg-slate-50 p-3 text-xs dark:border-slate-700 dark:bg-slate-900\"><div class=\"mb-1.5 font-semibold text-slate-700 dark:text-slate-300\">Merge base</div><div class=\"flex flex-col gap-1 text-slate-600 dark:text-slate-400\"><div>📍 the last commit both branches share</div><div>🌿 Alex's branch → diverges here</div><div>🌿 Sam's branch → diverges here</div></div></div><p class=\"mt-2 text-xs text-slate-600 dark:text-slate-400\"><strong>Why:</strong> git diffs each branch against this shared commit separately — that's what lets it tell \"Alex changed this\" apart from \"Sam changed this.\"</p>"
    },
    {
      "type": "quiz",
      "id": "auto-merge-condition-1",
      "poolId": "auto-merge-condition-pool",
      "level": 1,
      "prompt": "Alex adds a line near the top of a file; Sam edits a line near the bottom of the same file, on a different branch. What happens on merge?",
      "choices": [
        "Automatic conflict, since both branches touched the same file",
        "Auto-merge — both changes are combined with no conflict",
        "Whichever branch merges second silently wins, no message shown",
        "Git refuses to merge files that were both edited"
      ],
      "correctIndex": 1,
      "explanation": "Conflicts are decided at the line level, not the file level. Different lines touched on each side means both changes combine automatically.",
      "reference": "<div class=\"not-prose rounded-md border border-slate-300 bg-slate-50 p-3 text-xs dark:border-slate-700 dark:bg-slate-900\"><div class=\"mb-1.5 font-semibold text-slate-700 dark:text-slate-300\">Auto-merge condition</div><div class=\"flex flex-col gap-1 text-slate-600 dark:text-slate-400\"><div>🟢 different lines touched → auto-merge</div><div>🔴 same line, different content → conflict</div></div></div><p class=\"mt-2 text-xs text-slate-600 dark:text-slate-400\"><strong>Why:</strong> Git compares which lines each side's diff actually touched — not which file, and not how many lines.</p>"
    },
    {
      "type": "quiz",
      "id": "auto-merge-condition-2",
      "poolId": "auto-merge-condition-pool",
      "level": 1,
      "prompt": "What actually determines whether a merge needs human help?",
      "choices": [
        "How many files were changed across both branches",
        "Whether the two branches changed the exact same line to different content",
        "Which branch has a more descriptive commit message",
        "The total number of commits on each branch"
      ],
      "correctIndex": 1,
      "explanation": "The deciding factor is line-level overlap with differing content — not file count, commit count, or anything about commit messages.",
      "reference": "<div class=\"not-prose rounded-md border border-slate-300 bg-slate-50 p-3 text-xs dark:border-slate-700 dark:bg-slate-900\"><div class=\"mb-1.5 font-semibold text-slate-700 dark:text-slate-300\">Auto-merge condition</div><div class=\"flex flex-col gap-1 text-slate-600 dark:text-slate-400\"><div>🟢 different lines touched → auto-merge</div><div>🔴 same line, different content → conflict</div></div></div><p class=\"mt-2 text-xs text-slate-600 dark:text-slate-400\"><strong>Why:</strong> Git compares which lines each side's diff actually touched — not which file, and not how many lines.</p>"
    },
    {
      "type": "quiz",
      "id": "conflict-condition-1",
      "poolId": "conflict-condition-pool",
      "level": 1,
      "prompt": "Alex and Sam both independently change the same constant to the exact same new value on their branches. What happens on merge?",
      "choices": [
        "Conflict — same line touched by both sides always conflicts",
        "Auto-merge — since both sides agree on the new content, there's nothing to ask",
        "Git picks whichever branch is 'ours' and discards the other silently",
        "The file is left unchanged, both edits are dropped"
      ],
      "correctIndex": 1,
      "explanation": "A conflict requires the same line to be changed to *different* content on each side. Identical edits from both sides merge cleanly.",
      "reference": "<div class=\"not-prose rounded-md border border-slate-300 bg-slate-50 p-3 text-xs dark:border-slate-700 dark:bg-slate-900\"><div class=\"mb-1.5 font-semibold text-slate-700 dark:text-slate-300\">Real conflict, precisely</div><div class=\"flex flex-col gap-1 text-slate-600 dark:text-slate-400\"><div>same base line</div><div>changed by both sides</div><div>to different content</div></div></div><p class=\"mt-2 text-xs text-slate-600 dark:text-slate-400\"><strong>Why:</strong> If both sides change a line to the <em>same</em> new content, git doesn't ask — they agreed, nothing to resolve.</p>"
    },
    {
      "type": "quiz",
      "id": "conflict-condition-2",
      "poolId": "conflict-condition-pool",
      "level": 1,
      "prompt": "Which of these is a genuine merge conflict?",
      "choices": [
        "Both branches add different new functions to different parts of the file",
        "Both branches change the same line's value to two different numbers",
        "One branch adds a comment; the other renames a variable elsewhere",
        "Both branches leave a particular line completely untouched"
      ],
      "correctIndex": 1,
      "explanation": "Only the case where the same base line is changed to two different pieces of content on each side forces a real conflict.",
      "reference": "<div class=\"not-prose rounded-md border border-slate-300 bg-slate-50 p-3 text-xs dark:border-slate-700 dark:bg-slate-900\"><div class=\"mb-1.5 font-semibold text-slate-700 dark:text-slate-300\">Real conflict, precisely</div><div class=\"flex flex-col gap-1 text-slate-600 dark:text-slate-400\"><div>same base line</div><div>changed by both sides</div><div>to different content</div></div></div><p class=\"mt-2 text-xs text-slate-600 dark:text-slate-400\"><strong>Why:</strong> If both sides change a line to the <em>same</em> new content, git doesn't ask — they agreed, nothing to resolve.</p>"
    },
    {
      "type": "quiz",
      "id": "conflict-markers-1",
      "poolId": "conflict-markers-pool",
      "level": 1,
      "prompt": "What does the `=======` line inside a conflict marker block represent?",
      "choices": [
        "The end of the entire file",
        "The dividing line between 'our' version and 'their' version of the conflicting content",
        "A syntax error that needs to be deleted",
        "The original, pre-conflict version of the line"
      ],
      "correctIndex": 1,
      "explanation": "`=======` separates the two conflicting versions shown between `<<<<<<<` and `>>>>>>>` — everything above it is 'ours,' everything below is 'theirs.'",
      "reference": "<div class=\"not-prose rounded-md border border-slate-300 bg-slate-50 p-3 text-xs dark:border-slate-700 dark:bg-slate-900\"><div class=\"mb-1.5 font-semibold text-slate-700 dark:text-slate-300\">Conflict markers</div><div class=\"flex flex-col gap-1 text-slate-600 dark:text-slate-400\"><div>&lt;&lt;&lt;&lt;&lt;&lt;&lt; ours — your branch's version</div><div>======= — the dividing line</div><div>&gt;&gt;&gt;&gt;&gt;&gt;&gt; theirs — the other branch's version</div></div></div><p class=\"mt-2 text-xs text-slate-600 dark:text-slate-400\"><strong>Why:</strong> Both full versions are shown inline in the file itself, so you can edit, pick one, or combine them by hand.</p>"
    },
    {
      "type": "quiz",
      "id": "conflict-markers-2",
      "poolId": "conflict-markers-pool",
      "level": 1,
      "prompt": "After git inserts conflict markers into a file, what is the developer expected to do?",
      "choices": [
        "Nothing — git resolves it automatically after a short delay",
        "Edit the file to remove the markers and leave the correct final content, then mark it resolved",
        "Delete the entire file and start over",
        "Revert both branches to the merge base"
      ],
      "correctIndex": 1,
      "explanation": "Conflict markers are a prompt for a human decision — edit the file down to the intended final content (from either or both sides) and remove the markers before completing the merge.",
      "reference": "<div class=\"not-prose rounded-md border border-slate-300 bg-slate-50 p-3 text-xs dark:border-slate-700 dark:bg-slate-900\"><div class=\"mb-1.5 font-semibold text-slate-700 dark:text-slate-300\">Conflict markers</div><div class=\"flex flex-col gap-1 text-slate-600 dark:text-slate-400\"><div>&lt;&lt;&lt;&lt;&lt;&lt;&lt; ours — your branch's version</div><div>======= — the dividing line</div><div>&gt;&gt;&gt;&gt;&gt;&gt;&gt; theirs — the other branch's version</div></div></div><p class=\"mt-2 text-xs text-slate-600 dark:text-slate-400\"><strong>Why:</strong> Both full versions are shown inline in the file itself, so you can edit, pick one, or combine them by hand.</p>"
    },
    {
      "type": "quiz",
      "id": "fast-forward-def-1",
      "poolId": "fast-forward-def-pool",
      "level": 1,
      "prompt": "What makes a merge a 'fast-forward' merge?",
      "choices": [
        "It happens faster than a normal merge, timing-wise",
        "The branch being merged into hasn't diverged — it's still exactly at the merge base",
        "Both branches changed exactly the same lines",
        "The merge was done using a keyboard shortcut"
      ],
      "correctIndex": 1,
      "explanation": "Fast-forward specifically means the receiving branch has no commits of its own since the split — git just moves its pointer forward, no reconciliation needed.",
      "reference": "<div class=\"not-prose rounded-md border border-slate-300 bg-slate-50 p-3 text-xs dark:border-slate-700 dark:bg-slate-900\"><div class=\"mb-1.5 font-semibold text-slate-700 dark:text-slate-300\">Fast-forward merge</div><div class=\"flex flex-col gap-1 text-slate-600 dark:text-slate-400\"><div>receiving branch hasn't moved</div><div>→ pointer just moves forward</div><div>no merge commit created</div></div></div><p class=\"mt-2 text-xs text-slate-600 dark:text-slate-400\"><strong>Why:</strong> No independent history exists on the receiving side, so there's nothing to reconcile — just relabel where the branch points.</p>"
    },
    {
      "type": "quiz",
      "id": "fast-forward-def-2",
      "poolId": "fast-forward-def-pool",
      "level": 1,
      "prompt": "Does a fast-forward merge ever produce a real merge conflict?",
      "choices": [
        "Yes, whenever the two branches touch the same file",
        "No — there's no independent history to compare, so nothing can conflict",
        "Only if the file is larger than a certain size",
        "Yes, but only for binary files"
      ],
      "correctIndex": 1,
      "explanation": "A fast-forward has no divergent history on the receiving side to compare against — the three-way comparison that produces conflicts never runs.",
      "reference": "<div class=\"not-prose rounded-md border border-slate-300 bg-slate-50 p-3 text-xs dark:border-slate-700 dark:bg-slate-900\"><div class=\"mb-1.5 font-semibold text-slate-700 dark:text-slate-300\">Fast-forward merge</div><div class=\"flex flex-col gap-1 text-slate-600 dark:text-slate-400\"><div>receiving branch hasn't moved</div><div>→ pointer just moves forward</div><div>no merge commit created</div></div></div><p class=\"mt-2 text-xs text-slate-600 dark:text-slate-400\"><strong>Why:</strong> No independent history exists on the receiving side, so there's nothing to reconcile — just relabel where the branch points.</p>"
    },
    {
      "type": "quiz",
      "id": "three-way-purpose-1",
      "poolId": "three-way-purpose-pool",
      "level": 1,
      "prompt": "What problem does comparing against a merge base solve that a direct branch-to-branch diff can't?",
      "choices": [
        "It makes the diff run faster",
        "It lets git tell which branch actually made each change, instead of just seeing two files disagree",
        "It removes the need for conflict markers entirely",
        "It only matters for binary files, not text"
      ],
      "correctIndex": 1,
      "explanation": "A direct diff between two branches shows a disagreement but not its cause. Diffing each branch against the shared base attributes every change to whoever made it.",
      "reference": "<div class=\"not-prose rounded-md border border-slate-300 bg-slate-50 p-3 text-xs dark:border-slate-700 dark:bg-slate-900\"><div class=\"mb-1.5 font-semibold text-slate-700 dark:text-slate-300\">Why three-way, not two-way</div><div class=\"flex flex-col gap-1 text-slate-600 dark:text-slate-400\"><div>base → ours = Alex's real changes</div><div>base → theirs = Sam's real changes</div><div>compare those two change lists</div></div></div><p class=\"mt-2 text-xs text-slate-600 dark:text-slate-400\"><strong>Why:</strong> A raw ours-vs-theirs diff can only show disagreement, not who caused it — that's the whole reason the base is needed.</p>"
    },
    {
      "type": "quiz",
      "id": "three-way-purpose-2",
      "poolId": "three-way-purpose-pool",
      "level": 1,
      "prompt": "In a three-way merge, how many diffs does git actually compute?",
      "choices": [
        "One — directly between the two branches",
        "Two — base against each branch — then those two change lists are compared to each other",
        "Three — one per branch plus one for the merge result",
        "Zero — git relies on manual inspection only"
      ],
      "correctIndex": 1,
      "explanation": "Two diffs (base→ours, base→theirs) produce two independent change lists, which git then reconciles line by line.",
      "reference": "<div class=\"not-prose rounded-md border border-slate-300 bg-slate-50 p-3 text-xs dark:border-slate-700 dark:bg-slate-900\"><div class=\"mb-1.5 font-semibold text-slate-700 dark:text-slate-300\">Why three-way, not two-way</div><div class=\"flex flex-col gap-1 text-slate-600 dark:text-slate-400\"><div>base → ours = Alex's real changes</div><div>base → theirs = Sam's real changes</div><div>compare those two change lists</div></div></div><p class=\"mt-2 text-xs text-slate-600 dark:text-slate-400\"><strong>Why:</strong> A raw ours-vs-theirs diff can only show disagreement, not who caused it — that's the whole reason the base is needed.</p>"
    },
    {
      "type": "quiz",
      "id": "three-way-purpose-3",
      "poolId": "three-way-purpose-pool",
      "level": 1,
      "prompt": "Sam's branch deletes a line that Alex's branch never touched. What does the three-way merge conclude?",
      "choices": [
        "Conflict — any deletion always requires human review",
        "The line is removed in the merged result, since only theirs changed it and ours kept it",
        "The line is kept, since deletions are ignored by three-way merges",
        "Git refuses to merge branches that contain any deletions"
      ],
      "correctIndex": 1,
      "explanation": "Only one side changed (deleted) this line while the other kept it — that's a one-sided change, which auto-merges by taking the side that changed it.",
      "reference": "<div class=\"not-prose rounded-md border border-slate-300 bg-slate-50 p-3 text-xs dark:border-slate-700 dark:bg-slate-900\"><div class=\"mb-1.5 font-semibold text-slate-700 dark:text-slate-300\">Why three-way, not two-way</div><div class=\"flex flex-col gap-1 text-slate-600 dark:text-slate-400\"><div>base → ours = Alex's real changes</div><div>base → theirs = Sam's real changes</div><div>compare those two change lists</div></div></div><p class=\"mt-2 text-xs text-slate-600 dark:text-slate-400\"><strong>Why:</strong> A raw ours-vs-theirs diff can only show disagreement, not who caused it — that's the whole reason the base is needed.</p>"
    },
    {
      "type": "quiz",
      "id": "why-not-two-way-1",
      "poolId": "why-not-two-way-pool",
      "level": 2,
      "prompt": "A direct two-way diff between Alex's and Sam's files shows line 4 differs. What can it NOT tell you?",
      "choices": [
        "That line 4 is different between the two files",
        "Which side actually changed line 4, versus which side is the original",
        "The exact byte content of line 4 in each file",
        "That the two files have a different total line count"
      ],
      "correctIndex": 1,
      "explanation": "A two-way diff only reports disagreement, not causation — it has no third reference point (the base) to know which side actually moved away from the original.",
      "reference": "<div class=\"not-prose rounded-md border border-slate-300 bg-slate-50 p-3 text-xs dark:border-slate-700 dark:bg-slate-900\"><div class=\"mb-1.5 font-semibold text-slate-700 dark:text-slate-300\">Two-way vs. three-way</div><div class=\"flex flex-col gap-1 text-slate-600 dark:text-slate-400\"><div>two-way: 'these files disagree'</div><div>three-way: 'here is exactly what each side changed'</div></div></div><p class=\"mt-2 text-xs text-slate-600 dark:text-slate-400\"><strong>Why:</strong> Attribution, not just detection, is the thing a raw two-way diff can never give you.</p>"
    },
    {
      "type": "quiz",
      "id": "why-not-two-way-2",
      "poolId": "why-not-two-way-pool",
      "level": 2,
      "prompt": "Why can't a two-way diff safely auto-merge non-overlapping changes either?",
      "choices": [
        "It actually can, three-way isn't needed for that case",
        "Without knowing what changed relative to a shared base, it can't tell 'this is new' from 'this was always different'",
        "Two-way diffs are always slower than three-way diffs",
        "Two-way diffs only work on binary files"
      ],
      "correctIndex": 1,
      "explanation": "Even the 'easy' auto-merge case depends on knowing what each side changed relative to a common starting point — which only the base comparison provides.",
      "reference": "<div class=\"not-prose rounded-md border border-slate-300 bg-slate-50 p-3 text-xs dark:border-slate-700 dark:bg-slate-900\"><div class=\"mb-1.5 font-semibold text-slate-700 dark:text-slate-300\">Two-way vs. three-way</div><div class=\"flex flex-col gap-1 text-slate-600 dark:text-slate-400\"><div>two-way: 'these files disagree'</div><div>three-way: 'here is exactly what each side changed'</div></div></div><p class=\"mt-2 text-xs text-slate-600 dark:text-slate-400\"><strong>Why:</strong> Attribution, not just detection, is the thing a raw two-way diff can never give you.</p>"
    },
    {
      "type": "quiz",
      "id": "per-line-decision-1",
      "poolId": "per-line-decision-pool",
      "level": 2,
      "prompt": "Per the merge procedure in L2, what happens when only Alex's branch changed a base line and Sam's branch left it alone?",
      "choices": [
        "Conflict, since any change to a shared line requires review",
        "Alex's version of that line is taken, with no conflict",
        "The original base line is kept, ignoring Alex's change",
        "The line is deleted entirely"
      ],
      "correctIndex": 1,
      "explanation": "A one-sided change (only one branch touched the line) auto-merges by taking whichever side made the change.",
      "reference": "<div class=\"not-prose rounded-md border border-slate-300 bg-slate-50 p-3 text-xs dark:border-slate-700 dark:bg-slate-900\"><div class=\"mb-1.5 font-semibold text-slate-700 dark:text-slate-300\">Per base line, per side</div><div class=\"flex flex-col gap-1 text-slate-600 dark:text-slate-400\"><div>neither changed → keep base</div><div>one changed → take that side</div><div>both changed, differ → conflict</div><div>both changed, same → take it, no conflict</div></div></div><p class=\"mt-2 text-xs text-slate-600 dark:text-slate-400\"><strong>Why:</strong> The exact same four-way branch runs for every line in the base file.</p>"
    },
    {
      "type": "quiz",
      "id": "per-line-decision-2",
      "poolId": "per-line-decision-pool",
      "level": 2,
      "prompt": "Per the merge procedure, when do conflict markers actually get inserted?",
      "choices": [
        "Whenever both branches touch the same file, regardless of which lines",
        "Only when both branches changed the same base line to different content",
        "Whenever a branch has more than 10 commits",
        "Whenever the file exceeds a certain size"
      ],
      "correctIndex": 1,
      "explanation": "The procedure only branches into a conflict when both sides changed the same line AND their replacements actually differ.",
      "reference": "<div class=\"not-prose rounded-md border border-slate-300 bg-slate-50 p-3 text-xs dark:border-slate-700 dark:bg-slate-900\"><div class=\"mb-1.5 font-semibold text-slate-700 dark:text-slate-300\">Per base line, per side</div><div class=\"flex flex-col gap-1 text-slate-600 dark:text-slate-400\"><div>neither changed → keep base</div><div>one changed → take that side</div><div>both changed, differ → conflict</div><div>both changed, same → take it, no conflict</div></div></div><p class=\"mt-2 text-xs text-slate-600 dark:text-slate-400\"><strong>Why:</strong> The exact same four-way branch runs for every line in the base file.</p>"
    },
    {
      "type": "quiz",
      "id": "identical-edits-1",
      "poolId": "identical-edits-no-conflict-pool",
      "level": 2,
      "prompt": "Alex and Sam both independently fix the same bug on the same line, landing on identical corrected code. Does this conflict?",
      "choices": [
        "Yes — two branches touching the same line is always a conflict",
        "No — since the replacement content is identical on both sides, there's nothing to resolve",
        "Only if they used different variable names for local variables elsewhere",
        "Yes, but only if the fix spans more than one line"
      ],
      "correctIndex": 1,
      "explanation": "The merge only flags a conflict when the two sides' replacements for the same line actually differ. Identical replacements merge silently.",
      "reference": "<div class=\"not-prose rounded-md border border-slate-300 bg-slate-50 p-3 text-xs dark:border-slate-700 dark:bg-slate-900\"><div class=\"mb-1.5 font-semibold text-slate-700 dark:text-slate-300\">Identical edits</div><div class=\"flex flex-col gap-1 text-slate-600 dark:text-slate-400\"><div>Alex changes TAX_RATE → 0.0825</div><div>Sam changes TAX_RATE → 0.0825</div><div>same target value → no conflict</div></div></div><p class=\"mt-2 text-xs text-slate-600 dark:text-slate-400\"><strong>Why:</strong> The merge logic explicitly compares the two replacement contents before deciding there's anything to ask about.</p>"
    },
    {
      "type": "quiz",
      "id": "identical-edits-2",
      "poolId": "identical-edits-no-conflict-pool",
      "level": 2,
      "prompt": "Why does this behavior matter in practice for teams working on the same codebase?",
      "choices": [
        "It doesn't come up often enough to matter",
        "It means independently converging on the same fix (e.g. both applying a linter's suggested change) won't create needless manual work",
        "It means git will silently pick a random one of the two edits",
        "It only applies to whitespace changes, not real code"
      ],
      "correctIndex": 1,
      "explanation": "This is exactly what saves a team from an unnecessary conflict resolution step when two people independently arrive at the same correct fix.",
      "reference": "<div class=\"not-prose rounded-md border border-slate-300 bg-slate-50 p-3 text-xs dark:border-slate-700 dark:bg-slate-900\"><div class=\"mb-1.5 font-semibold text-slate-700 dark:text-slate-300\">Identical edits</div><div class=\"flex flex-col gap-1 text-slate-600 dark:text-slate-400\"><div>Alex changes TAX_RATE → 0.0825</div><div>Sam changes TAX_RATE → 0.0825</div><div>same target value → no conflict</div></div></div><p class=\"mt-2 text-xs text-slate-600 dark:text-slate-400\"><strong>Why:</strong> The merge logic explicitly compares the two replacement contents before deciding there's anything to ask about.</p>"
    },
    {
      "type": "quiz",
      "id": "identical-edits-3",
      "poolId": "identical-edits-no-conflict-pool",
      "level": 2,
      "prompt": "What specifically does the merge algorithm compare to decide 'identical edit, no conflict' versus 'real conflict'?",
      "choices": [
        "The commit messages on each branch",
        "The exact replacement content each side produced for that line (or insertion)",
        "The timestamp of each change",
        "The author name on each commit"
      ],
      "correctIndex": 1,
      "explanation": "The comparison is purely over the actual line content each side produced — nothing about commit metadata factors into the decision.",
      "reference": "<div class=\"not-prose rounded-md border border-slate-300 bg-slate-50 p-3 text-xs dark:border-slate-700 dark:bg-slate-900\"><div class=\"mb-1.5 font-semibold text-slate-700 dark:text-slate-300\">Identical edits</div><div class=\"flex flex-col gap-1 text-slate-600 dark:text-slate-400\"><div>Alex changes TAX_RATE → 0.0825</div><div>Sam changes TAX_RATE → 0.0825</div><div>same target value → no conflict</div></div></div><p class=\"mt-2 text-xs text-slate-600 dark:text-slate-400\"><strong>Why:</strong> The merge logic explicitly compares the two replacement contents before deciding there's anything to ask about.</p>"
    },
    {
      "type": "quiz",
      "id": "ff-vs-commit-1",
      "poolId": "fast-forward-vs-commit-pool",
      "level": 2,
      "prompt": "Sam's branch has zero commits of its own since branching from Alex's. Sam merges Alex's branch in. What happens?",
      "choices": [
        "A merge commit is always created, regardless of divergence",
        "Fast-forward — Sam's branch pointer just moves to match Alex's latest commit, no merge commit",
        "Git refuses the merge since Sam's branch is 'empty'",
        "A conflict is generated automatically"
      ],
      "correctIndex": 1,
      "explanation": "With no divergent history on Sam's side, there's nothing to reconcile — git simply advances the pointer, producing no merge commit.",
      "reference": "<div class=\"not-prose rounded-md border border-slate-300 bg-slate-50 p-3 text-xs dark:border-slate-700 dark:bg-slate-900\"><div class=\"mb-1.5 font-semibold text-slate-700 dark:text-slate-300\">Fast-forward vs. true merge</div><div class=\"flex flex-col gap-1 text-slate-600 dark:text-slate-400\"><div>fast-forward: pointer moves, no new commit</div><div>true merge: independent history on both sides → merge commit created</div></div></div><p class=\"mt-2 text-xs text-slate-600 dark:text-slate-400\"><strong>Why:</strong> Whether a merge commit appears at all depends entirely on whether real divergence exists to reconcile.</p>"
    },
    {
      "type": "quiz",
      "id": "ff-vs-commit-2",
      "poolId": "fast-forward-vs-commit-pool",
      "level": 2,
      "prompt": "Why does a true (non-fast-forward) merge produce a merge commit, but a fast-forward doesn't?",
      "choices": [
        "Merge commits are just a cosmetic git setting, unrelated to divergence",
        "A merge commit records how two independently-diverged histories were reconciled — with no divergence, there's nothing to record",
        "Fast-forward merges are actually slower, so git skips the commit to save time",
        "Merge commits only happen when there's a conflict"
      ],
      "correctIndex": 1,
      "explanation": "A merge commit exists specifically to capture the reconciliation of two divergent histories. When one side never diverged, there's no reconciliation event to record.",
      "reference": "<div class=\"not-prose rounded-md border border-slate-300 bg-slate-50 p-3 text-xs dark:border-slate-700 dark:bg-slate-900\"><div class=\"mb-1.5 font-semibold text-slate-700 dark:text-slate-300\">Fast-forward vs. true merge</div><div class=\"flex flex-col gap-1 text-slate-600 dark:text-slate-400\"><div>fast-forward: pointer moves, no new commit</div><div>true merge: independent history on both sides → merge commit created</div></div></div><p class=\"mt-2 text-xs text-slate-600 dark:text-slate-400\"><strong>Why:</strong> Whether a merge commit appears at all depends entirely on whether real divergence exists to reconcile.</p>"
    },
    {
      "type": "quiz",
      "id": "conflict-risk-1",
      "poolId": "conflict-risk-lifetime-pool",
      "level": 2,
      "prompt": "Why does keeping branches short-lived actually reduce merge conflicts, mechanically?",
      "choices": [
        "Short branches are exempt from the three-way merge algorithm",
        "Less time and fewer touched lines means lower odds that another branch's change list overlaps with yours on the same lines",
        "Git has a hidden time limit after which merges are forced to succeed",
        "It doesn't actually reduce conflicts — it's a myth"
      ],
      "correctIndex": 1,
      "explanation": "Conflict risk is driven by line-level overlap between two independent change lists — shorter branches touch fewer lines over less time, directly lowering that overlap.",
      "reference": "<div class=\"not-prose rounded-md border border-slate-300 bg-slate-50 p-3 text-xs dark:border-slate-700 dark:bg-slate-900\"><div class=\"mb-1.5 font-semibold text-slate-700 dark:text-slate-300\">Branch lifetime and risk</div><div class=\"flex flex-col gap-1 text-slate-600 dark:text-slate-400\"><div>longer-lived branch</div><div>→ more lines touched</div><div>→ more time for others to touch the same lines</div><div>→ higher line-overlap odds</div></div></div><p class=\"mt-2 text-xs text-slate-600 dark:text-slate-400\"><strong>Why:</strong> This is a mechanical consequence of the merge procedure, not a rule of thumb without a cause.</p>"
    },
    {
      "type": "quiz",
      "id": "conflict-risk-2",
      "poolId": "conflict-risk-lifetime-pool",
      "level": 2,
      "prompt": "Two branches each touch 5% of a file's lines, chosen independently. A third pair of branches each touch 40% of the same file. Which pair is more likely to hit a genuine conflict?",
      "choices": [
        "Both pairs are equally likely — percentage touched doesn't matter",
        "The 40%-each pair, since more touched lines raises the odds their change lists overlap",
        "The 5%-each pair, since smaller changes are harder to merge",
        "Neither can conflict unless they touch 100% of the file"
      ],
      "correctIndex": 1,
      "explanation": "More lines touched by each side raises the probability that some line falls in both change lists — directly increasing conflict odds.",
      "reference": "<div class=\"not-prose rounded-md border border-slate-300 bg-slate-50 p-3 text-xs dark:border-slate-700 dark:bg-slate-900\"><div class=\"mb-1.5 font-semibold text-slate-700 dark:text-slate-300\">Branch lifetime and risk</div><div class=\"flex flex-col gap-1 text-slate-600 dark:text-slate-400\"><div>longer-lived branch</div><div>→ more lines touched</div><div>→ more time for others to touch the same lines</div><div>→ higher line-overlap odds</div></div></div><p class=\"mt-2 text-xs text-slate-600 dark:text-slate-400\"><strong>Why:</strong> This is a mechanical consequence of the merge procedure, not a rule of thumb without a cause.</p>"
    },
    {
      "type": "quiz",
      "id": "insertion-anchor-1",
      "poolId": "insertion-anchoring-pool",
      "level": 2,
      "prompt": "Alex inserts a new line right after line 3; Sam inserts a different new line at that exact same spot. What determines whether this conflicts?",
      "choices": [
        "Insertions never conflict, only modifications to existing lines do",
        "Both insertions are anchored to the same base line, so it follows the same same-anchor-different-content rule as a modified line",
        "Whichever branch merges first always wins silently",
        "Insertions always conflict regardless of content"
      ],
      "correctIndex": 1,
      "explanation": "Insertions are tracked anchored to the nearest surviving base line — two different insertions at the same anchor point follow the exact same conflict rule as two different edits to the same line.",
      "reference": "<div class=\"not-prose rounded-md border border-slate-300 bg-slate-50 p-3 text-xs dark:border-slate-700 dark:bg-slate-900\"><div class=\"mb-1.5 font-semibold text-slate-700 dark:text-slate-300\">Anchored insertions</div><div class=\"flex flex-col gap-1 text-slate-600 dark:text-slate-400\"><div>a new line isn't 'in' the base file</div><div>so it's anchored to the nearest surviving base line</div><div>same logic applies: same anchor + different content → conflict</div></div></div><p class=\"mt-2 text-xs text-slate-600 dark:text-slate-400\"><strong>Why:</strong> Insertions are handled by the same per-line procedure as modifications — they just need an anchor point since they don't correspond to an existing base line.</p>"
    },
    {
      "type": "quiz",
      "id": "insertion-anchor-2",
      "poolId": "insertion-anchoring-pool",
      "level": 2,
      "prompt": "Why can't an inserted line be tracked by its own line number in the base file the way a modification can?",
      "choices": [
        "It could be, git just chooses not to for performance reasons",
        "An inserted line doesn't correspond to any existing base line — it has to be anchored relative to a line that does still exist",
        "Insertions are actually stored in a completely separate file",
        "Line numbers are irrelevant to how git tracks any change"
      ],
      "correctIndex": 1,
      "explanation": "A brand-new line has no position in the original base file, so it has to be described relative to a base line it sits next to, rather than by an index into the base itself.",
      "reference": "<div class=\"not-prose rounded-md border border-slate-300 bg-slate-50 p-3 text-xs dark:border-slate-700 dark:bg-slate-900\"><div class=\"mb-1.5 font-semibold text-slate-700 dark:text-slate-300\">Anchored insertions</div><div class=\"flex flex-col gap-1 text-slate-600 dark:text-slate-400\"><div>a new line isn't 'in' the base file</div><div>so it's anchored to the nearest surviving base line</div><div>same logic applies: same anchor + different content → conflict</div></div></div><p class=\"mt-2 text-xs text-slate-600 dark:text-slate-400\"><strong>Why:</strong> Insertions are handled by the same per-line procedure as modifications — they just need an anchor point since they don't correspond to an existing base line.</p>"
    },
    {
      "type": "code",
      "id": "diffLines-1",
      "poolId": "diffLines-basic-pool",
      "level": 3,
      "prompt": "Implement `diffLines(a, b)` so it returns the correct sequence of {type, line} ops for two arrays that share a common prefix and suffix, differing only in the middle.",
      "starterCode": "function diffLines(a, b) {\n  // TODO: implement\n}",
      "tests": [
        {
          "description": "identical arrays are all 'same'",
          "expr": "expect(diffLines([\"x\",\"y\"], [\"x\",\"y\"]).every(function(o){return o.type===\"same\";})).toBeTruthy()"
        },
        {
          "description": "a pure insertion produces one 'add'",
          "expr": "expect(diffLines([\"a\"], [\"a\",\"b\"])).toEqual([{type:\"same\",line:\"a\"},{type:\"add\",line:\"b\"}])"
        },
        {
          "description": "a pure deletion produces one 'del'",
          "expr": "expect(diffLines([\"a\",\"b\"], [\"a\"])).toEqual([{type:\"same\",line:\"a\"},{type:\"del\",line:\"b\"}])"
        }
      ],
      "solution": "function diffLines(a, b) {\n  const n = a.length, m = b.length;\n  const dp = Array.from({ length: n + 1 }, () => new Array(m + 1).fill(0));\n  for (let i = n - 1; i >= 0; i--) {\n    for (let j = m - 1; j >= 0; j--) {\n      dp[i][j] = a[i] === b[j] ? dp[i + 1][j + 1] + 1 : Math.max(dp[i + 1][j], dp[i][j + 1]);\n    }\n  }\n  const ops = [];\n  let i = 0, j = 0;\n  while (i < n && j < m) {\n    if (a[i] === b[j]) { ops.push({ type: \"same\", line: a[i] }); i++; j++; }\n    else if (dp[i + 1][j] >= dp[i][j + 1]) { ops.push({ type: \"del\", line: a[i] }); i++; }\n    else { ops.push({ type: \"add\", line: b[j] }); j++; }\n  }\n  while (i < n) { ops.push({ type: \"del\", line: a[i] }); i++; }\n  while (j < m) { ops.push({ type: \"add\", line: b[j] }); j++; }\n  return ops;\n}",
      "explanation": "diffLines produces a same/del/add op per line based on the longest-common-subsequence between the two arrays — unchanged lines become 'same', removed lines 'del', new lines 'add'."
    },
    {
      "type": "code",
      "id": "diffLines-2",
      "poolId": "diffLines-basic-pool",
      "level": 3,
      "prompt": "Using your `diffLines`, confirm a middle-line replacement shows up as a del immediately followed by an add, with the surrounding lines marked 'same'.",
      "starterCode": "function diffLines(a, b) {\n  // TODO: implement\n}",
      "tests": [
        {
          "description": "middle-line replacement is del+add, bracketed by same",
          "expr": "expect(diffLines([\"a\",\"old\",\"c\"], [\"a\",\"new\",\"c\"])).toEqual([{type:\"same\",line:\"a\"},{type:\"del\",line:\"old\"},{type:\"add\",line:\"new\"},{type:\"same\",line:\"c\"}])"
        }
      ],
      "solution": "function diffLines(a, b) {\n  const n = a.length, m = b.length;\n  const dp = Array.from({ length: n + 1 }, () => new Array(m + 1).fill(0));\n  for (let i = n - 1; i >= 0; i--) {\n    for (let j = m - 1; j >= 0; j--) {\n      dp[i][j] = a[i] === b[j] ? dp[i + 1][j + 1] + 1 : Math.max(dp[i + 1][j], dp[i][j + 1]);\n    }\n  }\n  const ops = [];\n  let i = 0, j = 0;\n  while (i < n && j < m) {\n    if (a[i] === b[j]) { ops.push({ type: \"same\", line: a[i] }); i++; j++; }\n    else if (dp[i + 1][j] >= dp[i][j + 1]) { ops.push({ type: \"del\", line: a[i] }); i++; }\n    else { ops.push({ type: \"add\", line: b[j] }); j++; }\n  }\n  while (i < n) { ops.push({ type: \"del\", line: a[i] }); i++; }\n  while (j < m) { ops.push({ type: \"add\", line: b[j] }); j++; }\n  return ops;\n}",
      "explanation": "diffLines produces a same/del/add op per line based on the longest-common-subsequence between the two arrays — unchanged lines become 'same', removed lines 'del', new lines 'add'."
    },
    {
      "type": "code",
      "id": "diffLines-3",
      "poolId": "diffLines-basic-pool",
      "level": 3,
      "prompt": "Confirm your `diffLines` handles two completely empty arrays (no ops at all) and two completely disjoint arrays (all del then all add).",
      "starterCode": "function diffLines(a, b) {\n  // TODO: implement\n}",
      "tests": [
        {
          "description": "two empty arrays produce no ops",
          "expr": "expect(diffLines([], [])).toEqual([])"
        },
        {
          "description": "fully disjoint arrays produce all dels then all adds",
          "expr": "expect(diffLines([\"a\"], [\"z\"])).toEqual([{type:\"del\",line:\"a\"},{type:\"add\",line:\"z\"}])"
        }
      ],
      "solution": "function diffLines(a, b) {\n  const n = a.length, m = b.length;\n  const dp = Array.from({ length: n + 1 }, () => new Array(m + 1).fill(0));\n  for (let i = n - 1; i >= 0; i--) {\n    for (let j = m - 1; j >= 0; j--) {\n      dp[i][j] = a[i] === b[j] ? dp[i + 1][j + 1] + 1 : Math.max(dp[i + 1][j], dp[i][j + 1]);\n    }\n  }\n  const ops = [];\n  let i = 0, j = 0;\n  while (i < n && j < m) {\n    if (a[i] === b[j]) { ops.push({ type: \"same\", line: a[i] }); i++; j++; }\n    else if (dp[i + 1][j] >= dp[i][j + 1]) { ops.push({ type: \"del\", line: a[i] }); i++; }\n    else { ops.push({ type: \"add\", line: b[j] }); j++; }\n  }\n  while (i < n) { ops.push({ type: \"del\", line: a[i] }); i++; }\n  while (j < m) { ops.push({ type: \"add\", line: b[j] }); j++; }\n  return ops;\n}",
      "explanation": "diffLines produces a same/del/add op per line based on the longest-common-subsequence between the two arrays — unchanged lines become 'same', removed lines 'del', new lines 'add'."
    },
    {
      "type": "code",
      "id": "threeWayMerge-nonoverlap-1",
      "poolId": "threeWayMerge-nonoverlap-pool",
      "level": 3,
      "prompt": "Implement `threeWayMerge(base, ours, theirs)` (building on diffLines/buildChangeMap) so that non-overlapping single-line changes on each side both survive with conflict === false.",
      "starterCode": "function diffLines(a, b) { const n = a.length, m = b.length; const dp = Array.from({ length: n + 1 }, () => new Array(m + 1).fill(0)); for (let i = n - 1; i >= 0; i--) { for (let j = m - 1; j >= 0; j--) { dp[i][j] = a[i] === b[j] ? dp[i + 1][j + 1] + 1 : Math.max(dp[i + 1][j], dp[i][j + 1]); } } const ops = []; let i = 0, j = 0; while (i < n && j < m) { if (a[i] === b[j]) { ops.push({ type: \"same\", line: a[i] }); i++; j++; } else if (dp[i + 1][j] >= dp[i][j + 1]) { ops.push({ type: \"del\", line: a[i] }); i++; } else { ops.push({ type: \"add\", line: b[j] }); j++; } } while (i < n) { ops.push({ type: \"del\", line: a[i] }); i++; } while (j < m) { ops.push({ type: \"add\", line: b[j] }); j++; } return ops; }\nfunction buildChangeMap(ops) { const perBaseLine = []; let baseIdx = -1; let pendingAdds = []; for (const op of ops) { if (op.type === \"same\") { perBaseLine.push({ baseIdx: ++baseIdx, kept: true, adds: pendingAdds }); pendingAdds = []; } else if (op.type === \"del\") { perBaseLine.push({ baseIdx: ++baseIdx, kept: false, adds: pendingAdds }); pendingAdds = []; } else { pendingAdds.push(op.line); } } return { perBaseLine, trailingAdds: pendingAdds }; }\nfunction threeWayMerge(base, ours, theirs) {\n  // TODO: implement\n}",
      "tests": [
        {
          "description": "two different lines changed on each side auto-merge",
          "expr": "expect(threeWayMerge([\"a\",\"b\",\"c\"], [\"a\",\"B\",\"c\"], [\"a\",\"b\",\"C\"])).toEqual({merged:[\"a\",\"B\",\"C\"],conflict:false})"
        }
      ],
      "solution": "function diffLines(a, b) { const n = a.length, m = b.length; const dp = Array.from({ length: n + 1 }, () => new Array(m + 1).fill(0)); for (let i = n - 1; i >= 0; i--) { for (let j = m - 1; j >= 0; j--) { dp[i][j] = a[i] === b[j] ? dp[i + 1][j + 1] + 1 : Math.max(dp[i + 1][j], dp[i][j + 1]); } } const ops = []; let i = 0, j = 0; while (i < n && j < m) { if (a[i] === b[j]) { ops.push({ type: \"same\", line: a[i] }); i++; j++; } else if (dp[i + 1][j] >= dp[i][j + 1]) { ops.push({ type: \"del\", line: a[i] }); i++; } else { ops.push({ type: \"add\", line: b[j] }); j++; } } while (i < n) { ops.push({ type: \"del\", line: a[i] }); i++; } while (j < m) { ops.push({ type: \"add\", line: b[j] }); j++; } return ops; }\nfunction buildChangeMap(ops) { const perBaseLine = []; let baseIdx = -1; let pendingAdds = []; for (const op of ops) { if (op.type === \"same\") { perBaseLine.push({ baseIdx: ++baseIdx, kept: true, adds: pendingAdds }); pendingAdds = []; } else if (op.type === \"del\") { perBaseLine.push({ baseIdx: ++baseIdx, kept: false, adds: pendingAdds }); pendingAdds = []; } else { pendingAdds.push(op.line); } } return { perBaseLine, trailingAdds: pendingAdds }; }\nfunction threeWayMerge(base, ours, theirs) {\n  const oursMap = buildChangeMap(diffLines(base, ours));\n  const theirsMap = buildChangeMap(diffLines(base, theirs));\n  const result = [];\n  let conflict = false;\n  function mergeAdds(oAdds, tAdds) {\n    if (oAdds.length && tAdds.length) {\n      if (JSON.stringify(oAdds) === JSON.stringify(tAdds)) { result.push(...oAdds); }\n      else { conflict = true; result.push(\"<<<<<<< ours\", ...oAdds, \"=======\", ...tAdds, \">>>>>>> theirs\"); }\n    } else { result.push(...(oAdds.length ? oAdds : tAdds)); }\n  }\n  for (let k = 0; k < base.length; k++) {\n    const o = oursMap.perBaseLine[k];\n    const t = theirsMap.perBaseLine[k];\n    if (o.adds.length || t.adds.length) mergeAdds(o.adds, t.adds);\n    if (o.kept && t.kept) { result.push(base[k]); }\n  }\n  mergeAdds(oursMap.trailingAdds, theirsMap.trailingAdds);\n  return { merged: result, conflict };\n}",
      "explanation": "When each side changes different base lines, both change sets survive independently in the merged output, and conflict stays false — this is the mechanical definition of auto-merge."
    },
    {
      "type": "code",
      "id": "threeWayMerge-nonoverlap-2",
      "poolId": "threeWayMerge-nonoverlap-pool",
      "level": 3,
      "prompt": "Confirm your `threeWayMerge` correctly combines an insertion from one side with an unrelated edit from the other side, both surviving in the result.",
      "starterCode": "function diffLines(a, b) { const n = a.length, m = b.length; const dp = Array.from({ length: n + 1 }, () => new Array(m + 1).fill(0)); for (let i = n - 1; i >= 0; i--) { for (let j = m - 1; j >= 0; j--) { dp[i][j] = a[i] === b[j] ? dp[i + 1][j + 1] + 1 : Math.max(dp[i + 1][j], dp[i][j + 1]); } } const ops = []; let i = 0, j = 0; while (i < n && j < m) { if (a[i] === b[j]) { ops.push({ type: \"same\", line: a[i] }); i++; j++; } else if (dp[i + 1][j] >= dp[i][j + 1]) { ops.push({ type: \"del\", line: a[i] }); i++; } else { ops.push({ type: \"add\", line: b[j] }); j++; } } while (i < n) { ops.push({ type: \"del\", line: a[i] }); i++; } while (j < m) { ops.push({ type: \"add\", line: b[j] }); j++; } return ops; }\nfunction buildChangeMap(ops) { const perBaseLine = []; let baseIdx = -1; let pendingAdds = []; for (const op of ops) { if (op.type === \"same\") { perBaseLine.push({ baseIdx: ++baseIdx, kept: true, adds: pendingAdds }); pendingAdds = []; } else if (op.type === \"del\") { perBaseLine.push({ baseIdx: ++baseIdx, kept: false, adds: pendingAdds }); pendingAdds = []; } else { pendingAdds.push(op.line); } } return { perBaseLine, trailingAdds: pendingAdds }; }\nfunction threeWayMerge(base, ours, theirs) {\n  // TODO: implement\n}",
      "tests": [
        {
          "description": "an insertion on ours and an edit on theirs both survive",
          "expr": "expect(threeWayMerge([\"a\",\"b\"], [\"a\",\"new\",\"b\"], [\"a\",\"B\"])).toEqual({merged:[\"a\",\"new\",\"B\"],conflict:false})"
        }
      ],
      "solution": "function diffLines(a, b) { const n = a.length, m = b.length; const dp = Array.from({ length: n + 1 }, () => new Array(m + 1).fill(0)); for (let i = n - 1; i >= 0; i--) { for (let j = m - 1; j >= 0; j--) { dp[i][j] = a[i] === b[j] ? dp[i + 1][j + 1] + 1 : Math.max(dp[i + 1][j], dp[i][j + 1]); } } const ops = []; let i = 0, j = 0; while (i < n && j < m) { if (a[i] === b[j]) { ops.push({ type: \"same\", line: a[i] }); i++; j++; } else if (dp[i + 1][j] >= dp[i][j + 1]) { ops.push({ type: \"del\", line: a[i] }); i++; } else { ops.push({ type: \"add\", line: b[j] }); j++; } } while (i < n) { ops.push({ type: \"del\", line: a[i] }); i++; } while (j < m) { ops.push({ type: \"add\", line: b[j] }); j++; } return ops; }\nfunction buildChangeMap(ops) { const perBaseLine = []; let baseIdx = -1; let pendingAdds = []; for (const op of ops) { if (op.type === \"same\") { perBaseLine.push({ baseIdx: ++baseIdx, kept: true, adds: pendingAdds }); pendingAdds = []; } else if (op.type === \"del\") { perBaseLine.push({ baseIdx: ++baseIdx, kept: false, adds: pendingAdds }); pendingAdds = []; } else { pendingAdds.push(op.line); } } return { perBaseLine, trailingAdds: pendingAdds }; }\nfunction threeWayMerge(base, ours, theirs) {\n  const oursMap = buildChangeMap(diffLines(base, ours));\n  const theirsMap = buildChangeMap(diffLines(base, theirs));\n  const result = [];\n  let conflict = false;\n  function mergeAdds(oAdds, tAdds) {\n    if (oAdds.length && tAdds.length) {\n      if (JSON.stringify(oAdds) === JSON.stringify(tAdds)) { result.push(...oAdds); }\n      else { conflict = true; result.push(\"<<<<<<< ours\", ...oAdds, \"=======\", ...tAdds, \">>>>>>> theirs\"); }\n    } else { result.push(...(oAdds.length ? oAdds : tAdds)); }\n  }\n  for (let k = 0; k < base.length; k++) {\n    const o = oursMap.perBaseLine[k];\n    const t = theirsMap.perBaseLine[k];\n    if (o.adds.length || t.adds.length) mergeAdds(o.adds, t.adds);\n    if (o.kept && t.kept) { result.push(base[k]); }\n  }\n  mergeAdds(oursMap.trailingAdds, theirsMap.trailingAdds);\n  return { merged: result, conflict };\n}",
      "explanation": "When each side changes different base lines, both change sets survive independently in the merged output, and conflict stays false — this is the mechanical definition of auto-merge."
    },
    {
      "type": "code",
      "id": "threeWayMerge-nonoverlap-3",
      "poolId": "threeWayMerge-nonoverlap-pool",
      "level": 3,
      "prompt": "Confirm that when neither side changes anything, `threeWayMerge` returns the base unchanged with conflict === false.",
      "starterCode": "function diffLines(a, b) { const n = a.length, m = b.length; const dp = Array.from({ length: n + 1 }, () => new Array(m + 1).fill(0)); for (let i = n - 1; i >= 0; i--) { for (let j = m - 1; j >= 0; j--) { dp[i][j] = a[i] === b[j] ? dp[i + 1][j + 1] + 1 : Math.max(dp[i + 1][j], dp[i][j + 1]); } } const ops = []; let i = 0, j = 0; while (i < n && j < m) { if (a[i] === b[j]) { ops.push({ type: \"same\", line: a[i] }); i++; j++; } else if (dp[i + 1][j] >= dp[i][j + 1]) { ops.push({ type: \"del\", line: a[i] }); i++; } else { ops.push({ type: \"add\", line: b[j] }); j++; } } while (i < n) { ops.push({ type: \"del\", line: a[i] }); i++; } while (j < m) { ops.push({ type: \"add\", line: b[j] }); j++; } return ops; }\nfunction buildChangeMap(ops) { const perBaseLine = []; let baseIdx = -1; let pendingAdds = []; for (const op of ops) { if (op.type === \"same\") { perBaseLine.push({ baseIdx: ++baseIdx, kept: true, adds: pendingAdds }); pendingAdds = []; } else if (op.type === \"del\") { perBaseLine.push({ baseIdx: ++baseIdx, kept: false, adds: pendingAdds }); pendingAdds = []; } else { pendingAdds.push(op.line); } } return { perBaseLine, trailingAdds: pendingAdds }; }\nfunction threeWayMerge(base, ours, theirs) {\n  // TODO: implement\n}",
      "tests": [
        {
          "description": "no changes on either side returns the base as-is",
          "expr": "expect(threeWayMerge([\"a\",\"b\",\"c\"], [\"a\",\"b\",\"c\"], [\"a\",\"b\",\"c\"])).toEqual({merged:[\"a\",\"b\",\"c\"],conflict:false})"
        }
      ],
      "solution": "function diffLines(a, b) { const n = a.length, m = b.length; const dp = Array.from({ length: n + 1 }, () => new Array(m + 1).fill(0)); for (let i = n - 1; i >= 0; i--) { for (let j = m - 1; j >= 0; j--) { dp[i][j] = a[i] === b[j] ? dp[i + 1][j + 1] + 1 : Math.max(dp[i + 1][j], dp[i][j + 1]); } } const ops = []; let i = 0, j = 0; while (i < n && j < m) { if (a[i] === b[j]) { ops.push({ type: \"same\", line: a[i] }); i++; j++; } else if (dp[i + 1][j] >= dp[i][j + 1]) { ops.push({ type: \"del\", line: a[i] }); i++; } else { ops.push({ type: \"add\", line: b[j] }); j++; } } while (i < n) { ops.push({ type: \"del\", line: a[i] }); i++; } while (j < m) { ops.push({ type: \"add\", line: b[j] }); j++; } return ops; }\nfunction buildChangeMap(ops) { const perBaseLine = []; let baseIdx = -1; let pendingAdds = []; for (const op of ops) { if (op.type === \"same\") { perBaseLine.push({ baseIdx: ++baseIdx, kept: true, adds: pendingAdds }); pendingAdds = []; } else if (op.type === \"del\") { perBaseLine.push({ baseIdx: ++baseIdx, kept: false, adds: pendingAdds }); pendingAdds = []; } else { pendingAdds.push(op.line); } } return { perBaseLine, trailingAdds: pendingAdds }; }\nfunction threeWayMerge(base, ours, theirs) {\n  const oursMap = buildChangeMap(diffLines(base, ours));\n  const theirsMap = buildChangeMap(diffLines(base, theirs));\n  const result = [];\n  let conflict = false;\n  function mergeAdds(oAdds, tAdds) {\n    if (oAdds.length && tAdds.length) {\n      if (JSON.stringify(oAdds) === JSON.stringify(tAdds)) { result.push(...oAdds); }\n      else { conflict = true; result.push(\"<<<<<<< ours\", ...oAdds, \"=======\", ...tAdds, \">>>>>>> theirs\"); }\n    } else { result.push(...(oAdds.length ? oAdds : tAdds)); }\n  }\n  for (let k = 0; k < base.length; k++) {\n    const o = oursMap.perBaseLine[k];\n    const t = theirsMap.perBaseLine[k];\n    if (o.adds.length || t.adds.length) mergeAdds(o.adds, t.adds);\n    if (o.kept && t.kept) { result.push(base[k]); }\n  }\n  mergeAdds(oursMap.trailingAdds, theirsMap.trailingAdds);\n  return { merged: result, conflict };\n}",
      "explanation": "When each side changes different base lines, both change sets survive independently in the merged output, and conflict stays false — this is the mechanical definition of auto-merge."
    },
    {
      "type": "code",
      "id": "threeWayMerge-conflict-1",
      "poolId": "threeWayMerge-conflict-pool",
      "level": 3,
      "prompt": "Confirm `threeWayMerge` sets conflict to true and produces `<<<<<<< ours` / `=======` / `>>>>>>> theirs` markers when both sides change the same single line to different content.",
      "starterCode": "function diffLines(a, b) { const n = a.length, m = b.length; const dp = Array.from({ length: n + 1 }, () => new Array(m + 1).fill(0)); for (let i = n - 1; i >= 0; i--) { for (let j = m - 1; j >= 0; j--) { dp[i][j] = a[i] === b[j] ? dp[i + 1][j + 1] + 1 : Math.max(dp[i + 1][j], dp[i][j + 1]); } } const ops = []; let i = 0, j = 0; while (i < n && j < m) { if (a[i] === b[j]) { ops.push({ type: \"same\", line: a[i] }); i++; j++; } else if (dp[i + 1][j] >= dp[i][j + 1]) { ops.push({ type: \"del\", line: a[i] }); i++; } else { ops.push({ type: \"add\", line: b[j] }); j++; } } while (i < n) { ops.push({ type: \"del\", line: a[i] }); i++; } while (j < m) { ops.push({ type: \"add\", line: b[j] }); j++; } return ops; }\nfunction buildChangeMap(ops) { const perBaseLine = []; let baseIdx = -1; let pendingAdds = []; for (const op of ops) { if (op.type === \"same\") { perBaseLine.push({ baseIdx: ++baseIdx, kept: true, adds: pendingAdds }); pendingAdds = []; } else if (op.type === \"del\") { perBaseLine.push({ baseIdx: ++baseIdx, kept: false, adds: pendingAdds }); pendingAdds = []; } else { pendingAdds.push(op.line); } } return { perBaseLine, trailingAdds: pendingAdds }; }\nfunction threeWayMerge(base, ours, theirs) {\n  // TODO: implement\n}",
      "tests": [
        {
          "description": "same line changed differently produces conflict markers",
          "expr": "expect(threeWayMerge([\"x\"], [\"A\"], [\"B\"])).toEqual({merged:[\"<<<<<<< ours\",\"A\",\"=======\",\"B\",\">>>>>>> theirs\"],conflict:true})"
        }
      ],
      "solution": "function diffLines(a, b) { const n = a.length, m = b.length; const dp = Array.from({ length: n + 1 }, () => new Array(m + 1).fill(0)); for (let i = n - 1; i >= 0; i--) { for (let j = m - 1; j >= 0; j--) { dp[i][j] = a[i] === b[j] ? dp[i + 1][j + 1] + 1 : Math.max(dp[i + 1][j], dp[i][j + 1]); } } const ops = []; let i = 0, j = 0; while (i < n && j < m) { if (a[i] === b[j]) { ops.push({ type: \"same\", line: a[i] }); i++; j++; } else if (dp[i + 1][j] >= dp[i][j + 1]) { ops.push({ type: \"del\", line: a[i] }); i++; } else { ops.push({ type: \"add\", line: b[j] }); j++; } } while (i < n) { ops.push({ type: \"del\", line: a[i] }); i++; } while (j < m) { ops.push({ type: \"add\", line: b[j] }); j++; } return ops; }\nfunction buildChangeMap(ops) { const perBaseLine = []; let baseIdx = -1; let pendingAdds = []; for (const op of ops) { if (op.type === \"same\") { perBaseLine.push({ baseIdx: ++baseIdx, kept: true, adds: pendingAdds }); pendingAdds = []; } else if (op.type === \"del\") { perBaseLine.push({ baseIdx: ++baseIdx, kept: false, adds: pendingAdds }); pendingAdds = []; } else { pendingAdds.push(op.line); } } return { perBaseLine, trailingAdds: pendingAdds }; }\nfunction threeWayMerge(base, ours, theirs) {\n  const oursMap = buildChangeMap(diffLines(base, ours));\n  const theirsMap = buildChangeMap(diffLines(base, theirs));\n  const result = [];\n  let conflict = false;\n  function mergeAdds(oAdds, tAdds) {\n    if (oAdds.length && tAdds.length) {\n      if (JSON.stringify(oAdds) === JSON.stringify(tAdds)) { result.push(...oAdds); }\n      else { conflict = true; result.push(\"<<<<<<< ours\", ...oAdds, \"=======\", ...tAdds, \">>>>>>> theirs\"); }\n    } else { result.push(...(oAdds.length ? oAdds : tAdds)); }\n  }\n  for (let k = 0; k < base.length; k++) {\n    const o = oursMap.perBaseLine[k];\n    const t = theirsMap.perBaseLine[k];\n    if (o.adds.length || t.adds.length) mergeAdds(o.adds, t.adds);\n    if (o.kept && t.kept) { result.push(base[k]); }\n  }\n  mergeAdds(oursMap.trailingAdds, theirsMap.trailingAdds);\n  return { merged: result, conflict };\n}",
      "explanation": "When both sides change the same base line to genuinely different content, the merge inserts real conflict markers around both versions and sets conflict to true — this is the one case the algorithm refuses to guess on."
    },
    {
      "type": "code",
      "id": "threeWayMerge-conflict-2",
      "poolId": "threeWayMerge-conflict-pool",
      "level": 3,
      "prompt": "Confirm the conflict flag is true even when the conflicting line is surrounded by other unchanged lines that merge cleanly.",
      "starterCode": "function diffLines(a, b) { const n = a.length, m = b.length; const dp = Array.from({ length: n + 1 }, () => new Array(m + 1).fill(0)); for (let i = n - 1; i >= 0; i--) { for (let j = m - 1; j >= 0; j--) { dp[i][j] = a[i] === b[j] ? dp[i + 1][j + 1] + 1 : Math.max(dp[i + 1][j], dp[i][j + 1]); } } const ops = []; let i = 0, j = 0; while (i < n && j < m) { if (a[i] === b[j]) { ops.push({ type: \"same\", line: a[i] }); i++; j++; } else if (dp[i + 1][j] >= dp[i][j + 1]) { ops.push({ type: \"del\", line: a[i] }); i++; } else { ops.push({ type: \"add\", line: b[j] }); j++; } } while (i < n) { ops.push({ type: \"del\", line: a[i] }); i++; } while (j < m) { ops.push({ type: \"add\", line: b[j] }); j++; } return ops; }\nfunction buildChangeMap(ops) { const perBaseLine = []; let baseIdx = -1; let pendingAdds = []; for (const op of ops) { if (op.type === \"same\") { perBaseLine.push({ baseIdx: ++baseIdx, kept: true, adds: pendingAdds }); pendingAdds = []; } else if (op.type === \"del\") { perBaseLine.push({ baseIdx: ++baseIdx, kept: false, adds: pendingAdds }); pendingAdds = []; } else { pendingAdds.push(op.line); } } return { perBaseLine, trailingAdds: pendingAdds }; }\nfunction threeWayMerge(base, ours, theirs) {\n  // TODO: implement\n}",
      "tests": [
        {
          "description": "conflict flag true when one of several lines conflicts",
          "expr": "expect(threeWayMerge([\"keep\",\"x\",\"keep2\"], [\"keep\",\"A\",\"keep2\"], [\"keep\",\"B\",\"keep2\"]).conflict).toBe(true)"
        }
      ],
      "solution": "function diffLines(a, b) { const n = a.length, m = b.length; const dp = Array.from({ length: n + 1 }, () => new Array(m + 1).fill(0)); for (let i = n - 1; i >= 0; i--) { for (let j = m - 1; j >= 0; j--) { dp[i][j] = a[i] === b[j] ? dp[i + 1][j + 1] + 1 : Math.max(dp[i + 1][j], dp[i][j + 1]); } } const ops = []; let i = 0, j = 0; while (i < n && j < m) { if (a[i] === b[j]) { ops.push({ type: \"same\", line: a[i] }); i++; j++; } else if (dp[i + 1][j] >= dp[i][j + 1]) { ops.push({ type: \"del\", line: a[i] }); i++; } else { ops.push({ type: \"add\", line: b[j] }); j++; } } while (i < n) { ops.push({ type: \"del\", line: a[i] }); i++; } while (j < m) { ops.push({ type: \"add\", line: b[j] }); j++; } return ops; }\nfunction buildChangeMap(ops) { const perBaseLine = []; let baseIdx = -1; let pendingAdds = []; for (const op of ops) { if (op.type === \"same\") { perBaseLine.push({ baseIdx: ++baseIdx, kept: true, adds: pendingAdds }); pendingAdds = []; } else if (op.type === \"del\") { perBaseLine.push({ baseIdx: ++baseIdx, kept: false, adds: pendingAdds }); pendingAdds = []; } else { pendingAdds.push(op.line); } } return { perBaseLine, trailingAdds: pendingAdds }; }\nfunction threeWayMerge(base, ours, theirs) {\n  const oursMap = buildChangeMap(diffLines(base, ours));\n  const theirsMap = buildChangeMap(diffLines(base, theirs));\n  const result = [];\n  let conflict = false;\n  function mergeAdds(oAdds, tAdds) {\n    if (oAdds.length && tAdds.length) {\n      if (JSON.stringify(oAdds) === JSON.stringify(tAdds)) { result.push(...oAdds); }\n      else { conflict = true; result.push(\"<<<<<<< ours\", ...oAdds, \"=======\", ...tAdds, \">>>>>>> theirs\"); }\n    } else { result.push(...(oAdds.length ? oAdds : tAdds)); }\n  }\n  for (let k = 0; k < base.length; k++) {\n    const o = oursMap.perBaseLine[k];\n    const t = theirsMap.perBaseLine[k];\n    if (o.adds.length || t.adds.length) mergeAdds(o.adds, t.adds);\n    if (o.kept && t.kept) { result.push(base[k]); }\n  }\n  mergeAdds(oursMap.trailingAdds, theirsMap.trailingAdds);\n  return { merged: result, conflict };\n}",
      "explanation": "When both sides change the same base line to genuinely different content, the merge inserts real conflict markers around both versions and sets conflict to true — this is the one case the algorithm refuses to guess on."
    },
    {
      "type": "code",
      "id": "threeWayMerge-conflict-3",
      "poolId": "threeWayMerge-conflict-pool",
      "level": 3,
      "prompt": "Confirm that two DIFFERENT insertions at the same anchor point (both sides insert something new right after the same base line) also produce conflict markers.",
      "starterCode": "function diffLines(a, b) { const n = a.length, m = b.length; const dp = Array.from({ length: n + 1 }, () => new Array(m + 1).fill(0)); for (let i = n - 1; i >= 0; i--) { for (let j = m - 1; j >= 0; j--) { dp[i][j] = a[i] === b[j] ? dp[i + 1][j + 1] + 1 : Math.max(dp[i + 1][j], dp[i][j + 1]); } } const ops = []; let i = 0, j = 0; while (i < n && j < m) { if (a[i] === b[j]) { ops.push({ type: \"same\", line: a[i] }); i++; j++; } else if (dp[i + 1][j] >= dp[i][j + 1]) { ops.push({ type: \"del\", line: a[i] }); i++; } else { ops.push({ type: \"add\", line: b[j] }); j++; } } while (i < n) { ops.push({ type: \"del\", line: a[i] }); i++; } while (j < m) { ops.push({ type: \"add\", line: b[j] }); j++; } return ops; }\nfunction buildChangeMap(ops) { const perBaseLine = []; let baseIdx = -1; let pendingAdds = []; for (const op of ops) { if (op.type === \"same\") { perBaseLine.push({ baseIdx: ++baseIdx, kept: true, adds: pendingAdds }); pendingAdds = []; } else if (op.type === \"del\") { perBaseLine.push({ baseIdx: ++baseIdx, kept: false, adds: pendingAdds }); pendingAdds = []; } else { pendingAdds.push(op.line); } } return { perBaseLine, trailingAdds: pendingAdds }; }\nfunction threeWayMerge(base, ours, theirs) {\n  // TODO: implement\n}",
      "tests": [
        {
          "description": "conflicting insertions at the same anchor produce markers",
          "expr": "expect(threeWayMerge([\"a\"], [\"a\",\"fromOurs\"], [\"a\",\"fromTheirs\"])).toEqual({merged:[\"a\",\"<<<<<<< ours\",\"fromOurs\",\"=======\",\"fromTheirs\",\">>>>>>> theirs\"],conflict:true})"
        }
      ],
      "solution": "function diffLines(a, b) { const n = a.length, m = b.length; const dp = Array.from({ length: n + 1 }, () => new Array(m + 1).fill(0)); for (let i = n - 1; i >= 0; i--) { for (let j = m - 1; j >= 0; j--) { dp[i][j] = a[i] === b[j] ? dp[i + 1][j + 1] + 1 : Math.max(dp[i + 1][j], dp[i][j + 1]); } } const ops = []; let i = 0, j = 0; while (i < n && j < m) { if (a[i] === b[j]) { ops.push({ type: \"same\", line: a[i] }); i++; j++; } else if (dp[i + 1][j] >= dp[i][j + 1]) { ops.push({ type: \"del\", line: a[i] }); i++; } else { ops.push({ type: \"add\", line: b[j] }); j++; } } while (i < n) { ops.push({ type: \"del\", line: a[i] }); i++; } while (j < m) { ops.push({ type: \"add\", line: b[j] }); j++; } return ops; }\nfunction buildChangeMap(ops) { const perBaseLine = []; let baseIdx = -1; let pendingAdds = []; for (const op of ops) { if (op.type === \"same\") { perBaseLine.push({ baseIdx: ++baseIdx, kept: true, adds: pendingAdds }); pendingAdds = []; } else if (op.type === \"del\") { perBaseLine.push({ baseIdx: ++baseIdx, kept: false, adds: pendingAdds }); pendingAdds = []; } else { pendingAdds.push(op.line); } } return { perBaseLine, trailingAdds: pendingAdds }; }\nfunction threeWayMerge(base, ours, theirs) {\n  const oursMap = buildChangeMap(diffLines(base, ours));\n  const theirsMap = buildChangeMap(diffLines(base, theirs));\n  const result = [];\n  let conflict = false;\n  function mergeAdds(oAdds, tAdds) {\n    if (oAdds.length && tAdds.length) {\n      if (JSON.stringify(oAdds) === JSON.stringify(tAdds)) { result.push(...oAdds); }\n      else { conflict = true; result.push(\"<<<<<<< ours\", ...oAdds, \"=======\", ...tAdds, \">>>>>>> theirs\"); }\n    } else { result.push(...(oAdds.length ? oAdds : tAdds)); }\n  }\n  for (let k = 0; k < base.length; k++) {\n    const o = oursMap.perBaseLine[k];\n    const t = theirsMap.perBaseLine[k];\n    if (o.adds.length || t.adds.length) mergeAdds(o.adds, t.adds);\n    if (o.kept && t.kept) { result.push(base[k]); }\n  }\n  mergeAdds(oursMap.trailingAdds, theirsMap.trailingAdds);\n  return { merged: result, conflict };\n}",
      "explanation": "When both sides change the same base line to genuinely different content, the merge inserts real conflict markers around both versions and sets conflict to true — this is the one case the algorithm refuses to guess on."
    },
    {
      "type": "code",
      "id": "threeWayMerge-identical-1",
      "poolId": "threeWayMerge-identical-pool",
      "level": 3,
      "prompt": "Confirm `threeWayMerge` does NOT flag a conflict when both sides change the same line to the exact same new content.",
      "starterCode": "function diffLines(a, b) { const n = a.length, m = b.length; const dp = Array.from({ length: n + 1 }, () => new Array(m + 1).fill(0)); for (let i = n - 1; i >= 0; i--) { for (let j = m - 1; j >= 0; j--) { dp[i][j] = a[i] === b[j] ? dp[i + 1][j + 1] + 1 : Math.max(dp[i + 1][j], dp[i][j + 1]); } } const ops = []; let i = 0, j = 0; while (i < n && j < m) { if (a[i] === b[j]) { ops.push({ type: \"same\", line: a[i] }); i++; j++; } else if (dp[i + 1][j] >= dp[i][j + 1]) { ops.push({ type: \"del\", line: a[i] }); i++; } else { ops.push({ type: \"add\", line: b[j] }); j++; } } while (i < n) { ops.push({ type: \"del\", line: a[i] }); i++; } while (j < m) { ops.push({ type: \"add\", line: b[j] }); j++; } return ops; }\nfunction buildChangeMap(ops) { const perBaseLine = []; let baseIdx = -1; let pendingAdds = []; for (const op of ops) { if (op.type === \"same\") { perBaseLine.push({ baseIdx: ++baseIdx, kept: true, adds: pendingAdds }); pendingAdds = []; } else if (op.type === \"del\") { perBaseLine.push({ baseIdx: ++baseIdx, kept: false, adds: pendingAdds }); pendingAdds = []; } else { pendingAdds.push(op.line); } } return { perBaseLine, trailingAdds: pendingAdds }; }\nfunction threeWayMerge(base, ours, theirs) {\n  // TODO: implement\n}",
      "tests": [
        {
          "description": "identical replacement content merges cleanly",
          "expr": "expect(threeWayMerge([\"old\"], [\"new\"], [\"new\"])).toEqual({merged:[\"new\"],conflict:false})"
        }
      ],
      "solution": "function diffLines(a, b) { const n = a.length, m = b.length; const dp = Array.from({ length: n + 1 }, () => new Array(m + 1).fill(0)); for (let i = n - 1; i >= 0; i--) { for (let j = m - 1; j >= 0; j--) { dp[i][j] = a[i] === b[j] ? dp[i + 1][j + 1] + 1 : Math.max(dp[i + 1][j], dp[i][j + 1]); } } const ops = []; let i = 0, j = 0; while (i < n && j < m) { if (a[i] === b[j]) { ops.push({ type: \"same\", line: a[i] }); i++; j++; } else if (dp[i + 1][j] >= dp[i][j + 1]) { ops.push({ type: \"del\", line: a[i] }); i++; } else { ops.push({ type: \"add\", line: b[j] }); j++; } } while (i < n) { ops.push({ type: \"del\", line: a[i] }); i++; } while (j < m) { ops.push({ type: \"add\", line: b[j] }); j++; } return ops; }\nfunction buildChangeMap(ops) { const perBaseLine = []; let baseIdx = -1; let pendingAdds = []; for (const op of ops) { if (op.type === \"same\") { perBaseLine.push({ baseIdx: ++baseIdx, kept: true, adds: pendingAdds }); pendingAdds = []; } else if (op.type === \"del\") { perBaseLine.push({ baseIdx: ++baseIdx, kept: false, adds: pendingAdds }); pendingAdds = []; } else { pendingAdds.push(op.line); } } return { perBaseLine, trailingAdds: pendingAdds }; }\nfunction threeWayMerge(base, ours, theirs) {\n  const oursMap = buildChangeMap(diffLines(base, ours));\n  const theirsMap = buildChangeMap(diffLines(base, theirs));\n  const result = [];\n  let conflict = false;\n  function mergeAdds(oAdds, tAdds) {\n    if (oAdds.length && tAdds.length) {\n      if (JSON.stringify(oAdds) === JSON.stringify(tAdds)) { result.push(...oAdds); }\n      else { conflict = true; result.push(\"<<<<<<< ours\", ...oAdds, \"=======\", ...tAdds, \">>>>>>> theirs\"); }\n    } else { result.push(...(oAdds.length ? oAdds : tAdds)); }\n  }\n  for (let k = 0; k < base.length; k++) {\n    const o = oursMap.perBaseLine[k];\n    const t = theirsMap.perBaseLine[k];\n    if (o.adds.length || t.adds.length) mergeAdds(o.adds, t.adds);\n    if (o.kept && t.kept) { result.push(base[k]); }\n  }\n  mergeAdds(oursMap.trailingAdds, theirsMap.trailingAdds);\n  return { merged: result, conflict };\n}",
      "explanation": "The merge explicitly compares the two sides' replacement content before deciding there's a conflict — when both sides land on the identical new content, it's taken with conflict left false."
    },
    {
      "type": "code",
      "id": "threeWayMerge-identical-2",
      "poolId": "threeWayMerge-identical-pool",
      "level": 3,
      "prompt": "Confirm two identical insertions at the same anchor point (both sides insert the same new line after the same base line) merge without conflict, producing just one copy of the line.",
      "starterCode": "function diffLines(a, b) { const n = a.length, m = b.length; const dp = Array.from({ length: n + 1 }, () => new Array(m + 1).fill(0)); for (let i = n - 1; i >= 0; i--) { for (let j = m - 1; j >= 0; j--) { dp[i][j] = a[i] === b[j] ? dp[i + 1][j + 1] + 1 : Math.max(dp[i + 1][j], dp[i][j + 1]); } } const ops = []; let i = 0, j = 0; while (i < n && j < m) { if (a[i] === b[j]) { ops.push({ type: \"same\", line: a[i] }); i++; j++; } else if (dp[i + 1][j] >= dp[i][j + 1]) { ops.push({ type: \"del\", line: a[i] }); i++; } else { ops.push({ type: \"add\", line: b[j] }); j++; } } while (i < n) { ops.push({ type: \"del\", line: a[i] }); i++; } while (j < m) { ops.push({ type: \"add\", line: b[j] }); j++; } return ops; }\nfunction buildChangeMap(ops) { const perBaseLine = []; let baseIdx = -1; let pendingAdds = []; for (const op of ops) { if (op.type === \"same\") { perBaseLine.push({ baseIdx: ++baseIdx, kept: true, adds: pendingAdds }); pendingAdds = []; } else if (op.type === \"del\") { perBaseLine.push({ baseIdx: ++baseIdx, kept: false, adds: pendingAdds }); pendingAdds = []; } else { pendingAdds.push(op.line); } } return { perBaseLine, trailingAdds: pendingAdds }; }\nfunction threeWayMerge(base, ours, theirs) {\n  // TODO: implement\n}",
      "tests": [
        {
          "description": "identical insertions produce one copy, no conflict",
          "expr": "expect(threeWayMerge([\"a\"], [\"a\",\"same\"], [\"a\",\"same\"])).toEqual({merged:[\"a\",\"same\"],conflict:false})"
        }
      ],
      "solution": "function diffLines(a, b) { const n = a.length, m = b.length; const dp = Array.from({ length: n + 1 }, () => new Array(m + 1).fill(0)); for (let i = n - 1; i >= 0; i--) { for (let j = m - 1; j >= 0; j--) { dp[i][j] = a[i] === b[j] ? dp[i + 1][j + 1] + 1 : Math.max(dp[i + 1][j], dp[i][j + 1]); } } const ops = []; let i = 0, j = 0; while (i < n && j < m) { if (a[i] === b[j]) { ops.push({ type: \"same\", line: a[i] }); i++; j++; } else if (dp[i + 1][j] >= dp[i][j + 1]) { ops.push({ type: \"del\", line: a[i] }); i++; } else { ops.push({ type: \"add\", line: b[j] }); j++; } } while (i < n) { ops.push({ type: \"del\", line: a[i] }); i++; } while (j < m) { ops.push({ type: \"add\", line: b[j] }); j++; } return ops; }\nfunction buildChangeMap(ops) { const perBaseLine = []; let baseIdx = -1; let pendingAdds = []; for (const op of ops) { if (op.type === \"same\") { perBaseLine.push({ baseIdx: ++baseIdx, kept: true, adds: pendingAdds }); pendingAdds = []; } else if (op.type === \"del\") { perBaseLine.push({ baseIdx: ++baseIdx, kept: false, adds: pendingAdds }); pendingAdds = []; } else { pendingAdds.push(op.line); } } return { perBaseLine, trailingAdds: pendingAdds }; }\nfunction threeWayMerge(base, ours, theirs) {\n  const oursMap = buildChangeMap(diffLines(base, ours));\n  const theirsMap = buildChangeMap(diffLines(base, theirs));\n  const result = [];\n  let conflict = false;\n  function mergeAdds(oAdds, tAdds) {\n    if (oAdds.length && tAdds.length) {\n      if (JSON.stringify(oAdds) === JSON.stringify(tAdds)) { result.push(...oAdds); }\n      else { conflict = true; result.push(\"<<<<<<< ours\", ...oAdds, \"=======\", ...tAdds, \">>>>>>> theirs\"); }\n    } else { result.push(...(oAdds.length ? oAdds : tAdds)); }\n  }\n  for (let k = 0; k < base.length; k++) {\n    const o = oursMap.perBaseLine[k];\n    const t = theirsMap.perBaseLine[k];\n    if (o.adds.length || t.adds.length) mergeAdds(o.adds, t.adds);\n    if (o.kept && t.kept) { result.push(base[k]); }\n  }\n  mergeAdds(oursMap.trailingAdds, theirsMap.trailingAdds);\n  return { merged: result, conflict };\n}",
      "explanation": "The merge explicitly compares the two sides' replacement content before deciding there's a conflict — when both sides land on the identical new content, it's taken with conflict left false."
    },
    {
      "type": "code",
      "id": "threeWayMerge-identical-3",
      "poolId": "threeWayMerge-identical-pool",
      "level": 3,
      "prompt": "Confirm that a near-identical (but not exact) match — where the two sides' new content differs even slightly, e.g. by whitespace — is still treated as a real conflict, not silently merged.",
      "starterCode": "function diffLines(a, b) { const n = a.length, m = b.length; const dp = Array.from({ length: n + 1 }, () => new Array(m + 1).fill(0)); for (let i = n - 1; i >= 0; i--) { for (let j = m - 1; j >= 0; j--) { dp[i][j] = a[i] === b[j] ? dp[i + 1][j + 1] + 1 : Math.max(dp[i + 1][j], dp[i][j + 1]); } } const ops = []; let i = 0, j = 0; while (i < n && j < m) { if (a[i] === b[j]) { ops.push({ type: \"same\", line: a[i] }); i++; j++; } else if (dp[i + 1][j] >= dp[i][j + 1]) { ops.push({ type: \"del\", line: a[i] }); i++; } else { ops.push({ type: \"add\", line: b[j] }); j++; } } while (i < n) { ops.push({ type: \"del\", line: a[i] }); i++; } while (j < m) { ops.push({ type: \"add\", line: b[j] }); j++; } return ops; }\nfunction buildChangeMap(ops) { const perBaseLine = []; let baseIdx = -1; let pendingAdds = []; for (const op of ops) { if (op.type === \"same\") { perBaseLine.push({ baseIdx: ++baseIdx, kept: true, adds: pendingAdds }); pendingAdds = []; } else if (op.type === \"del\") { perBaseLine.push({ baseIdx: ++baseIdx, kept: false, adds: pendingAdds }); pendingAdds = []; } else { pendingAdds.push(op.line); } } return { perBaseLine, trailingAdds: pendingAdds }; }\nfunction threeWayMerge(base, ours, theirs) {\n  // TODO: implement\n}",
      "tests": [
        {
          "description": "near-identical but not exact content still conflicts",
          "expr": "expect(threeWayMerge([\"old\"], [\"new\"], [\"new \"]).conflict).toBe(true)"
        }
      ],
      "solution": "function diffLines(a, b) { const n = a.length, m = b.length; const dp = Array.from({ length: n + 1 }, () => new Array(m + 1).fill(0)); for (let i = n - 1; i >= 0; i--) { for (let j = m - 1; j >= 0; j--) { dp[i][j] = a[i] === b[j] ? dp[i + 1][j + 1] + 1 : Math.max(dp[i + 1][j], dp[i][j + 1]); } } const ops = []; let i = 0, j = 0; while (i < n && j < m) { if (a[i] === b[j]) { ops.push({ type: \"same\", line: a[i] }); i++; j++; } else if (dp[i + 1][j] >= dp[i][j + 1]) { ops.push({ type: \"del\", line: a[i] }); i++; } else { ops.push({ type: \"add\", line: b[j] }); j++; } } while (i < n) { ops.push({ type: \"del\", line: a[i] }); i++; } while (j < m) { ops.push({ type: \"add\", line: b[j] }); j++; } return ops; }\nfunction buildChangeMap(ops) { const perBaseLine = []; let baseIdx = -1; let pendingAdds = []; for (const op of ops) { if (op.type === \"same\") { perBaseLine.push({ baseIdx: ++baseIdx, kept: true, adds: pendingAdds }); pendingAdds = []; } else if (op.type === \"del\") { perBaseLine.push({ baseIdx: ++baseIdx, kept: false, adds: pendingAdds }); pendingAdds = []; } else { pendingAdds.push(op.line); } } return { perBaseLine, trailingAdds: pendingAdds }; }\nfunction threeWayMerge(base, ours, theirs) {\n  const oursMap = buildChangeMap(diffLines(base, ours));\n  const theirsMap = buildChangeMap(diffLines(base, theirs));\n  const result = [];\n  let conflict = false;\n  function mergeAdds(oAdds, tAdds) {\n    if (oAdds.length && tAdds.length) {\n      if (JSON.stringify(oAdds) === JSON.stringify(tAdds)) { result.push(...oAdds); }\n      else { conflict = true; result.push(\"<<<<<<< ours\", ...oAdds, \"=======\", ...tAdds, \">>>>>>> theirs\"); }\n    } else { result.push(...(oAdds.length ? oAdds : tAdds)); }\n  }\n  for (let k = 0; k < base.length; k++) {\n    const o = oursMap.perBaseLine[k];\n    const t = theirsMap.perBaseLine[k];\n    if (o.adds.length || t.adds.length) mergeAdds(o.adds, t.adds);\n    if (o.kept && t.kept) { result.push(base[k]); }\n  }\n  mergeAdds(oursMap.trailingAdds, theirsMap.trailingAdds);\n  return { merged: result, conflict };\n}",
      "explanation": "The merge explicitly compares the two sides' replacement content before deciding there's a conflict — when both sides land on the identical new content, it's taken with conflict left false."
    },
    {
      "type": "code",
      "id": "buildChangeMap-1",
      "poolId": "buildChangeMap-pool",
      "level": 3,
      "prompt": "Implement `buildChangeMap(ops)` so that a 'same' op is recorded as `kept: true` and a 'del' op as `kept: false`, each anchored to the correct increasing base index.",
      "starterCode": "function buildChangeMap(ops) {\n  // TODO: implement\n}",
      "tests": [
        {
          "description": "same/del ops are tagged kept true/false with increasing baseIdx",
          "expr": "expect(buildChangeMap([{type:\"same\",line:\"a\"},{type:\"del\",line:\"b\"}]).perBaseLine).toEqual([{baseIdx:0,kept:true,adds:[]},{baseIdx:1,kept:false,adds:[]}])"
        }
      ],
      "solution": "function buildChangeMap(ops) {\n  const perBaseLine = [];\n  let baseIdx = -1;\n  let pendingAdds = [];\n  for (const op of ops) {\n    if (op.type === \"same\") { perBaseLine.push({ baseIdx: ++baseIdx, kept: true, adds: pendingAdds }); pendingAdds = []; }\n    else if (op.type === \"del\") { perBaseLine.push({ baseIdx: ++baseIdx, kept: false, adds: pendingAdds }); pendingAdds = []; }\n    else { pendingAdds.push(op.line); }\n  }\n  return { perBaseLine, trailingAdds: pendingAdds };\n}",
      "explanation": "buildChangeMap walks a diff's ops in order, incrementing a base-line index on every 'same'/'del' (since those correspond to a real base line) while bucketing 'add' ops as pending insertions anchored to whichever base line comes next."
    },
    {
      "type": "code",
      "id": "buildChangeMap-2",
      "poolId": "buildChangeMap-pool",
      "level": 3,
      "prompt": "Confirm `buildChangeMap` correctly captures trailing insertions (adds that occur after the very last base line) in a separate `trailingAdds` array, not attached to any `perBaseLine` entry.",
      "starterCode": "function buildChangeMap(ops) {\n  // TODO: implement\n}",
      "tests": [
        {
          "description": "an add after the last same/del lands in trailingAdds",
          "expr": "expect(buildChangeMap([{type:\"same\",line:\"a\"},{type:\"add\",line:\"tail\"}]).trailingAdds).toEqual([\"tail\"])"
        },
        {
          "description": "adds before a same line are attached to that entry's adds array",
          "expr": "expect(buildChangeMap([{type:\"add\",line:\"head\"},{type:\"same\",line:\"a\"}]).perBaseLine[0].adds).toEqual([\"head\"])"
        }
      ],
      "solution": "function buildChangeMap(ops) {\n  const perBaseLine = [];\n  let baseIdx = -1;\n  let pendingAdds = [];\n  for (const op of ops) {\n    if (op.type === \"same\") { perBaseLine.push({ baseIdx: ++baseIdx, kept: true, adds: pendingAdds }); pendingAdds = []; }\n    else if (op.type === \"del\") { perBaseLine.push({ baseIdx: ++baseIdx, kept: false, adds: pendingAdds }); pendingAdds = []; }\n    else { pendingAdds.push(op.line); }\n  }\n  return { perBaseLine, trailingAdds: pendingAdds };\n}",
      "explanation": "buildChangeMap walks a diff's ops in order, incrementing a base-line index on every 'same'/'del' (since those correspond to a real base line) while bucketing 'add' ops as pending insertions anchored to whichever base line comes next."
    }
  ]
}
