{
  "items": [
    {
      "type": "quiz",
      "id": "full-scan-def-1",
      "poolId": "full-scan-def-pool",
      "level": 1,
      "prompt": "What is a \"full table scan\"?",
      "choices": [
        "Checking every row in a table one by one until a match is found",
        "A special kind of index built for text search",
        "A backup of the entire database",
        "A query that only reads the first row of a table"
      ],
      "correctIndex": 0,
      "explanation": "A full table scan checks every single row in order, looking for a match — its cost grows in direct proportion to the number of rows in the table.",
      "reference": "<div class=\"not-prose flex flex-col gap-1 text-sm\"><div>🔎 <strong>Full scan</strong> — check row 1, row 2, row 3 ... until a match or the end</div><div>📈 <strong>Cost</strong> — grows linearly: 2x rows ≈ 2x checks</div></div><p><strong>Why:</strong> without an index, this is the only way a database can find a row — nothing tells it where to look.</p>"
    },
    {
      "type": "quiz",
      "id": "full-scan-def-2",
      "poolId": "full-scan-def-pool",
      "level": 1,
      "prompt": "How does a full table scan's cost change as row count grows?",
      "choices": [
        "It stays constant regardless of row count",
        "It grows in direct (linear) proportion to row count",
        "It shrinks as row count grows",
        "It grows only if the table has an index"
      ],
      "correctIndex": 1,
      "explanation": "A full scan checks every row, so doubling the row count roughly doubles the number of checks needed — cost grows linearly with data size.",
      "reference": "<div class=\"not-prose flex flex-col gap-1 text-sm\"><div>🔎 <strong>Full scan</strong> — check row 1, row 2, row 3 ... until a match or the end</div><div>📈 <strong>Cost</strong> — grows linearly: 2x rows ≈ 2x checks</div></div><p><strong>Why:</strong> without an index, this is the only way a database can find a row — nothing tells it where to look.</p>"
    },
    {
      "type": "quiz",
      "id": "index-def-1",
      "poolId": "index-def-pool",
      "level": 1,
      "prompt": "What is a database index?",
      "choices": [
        "A separate structure built ahead of time to make lookups on a column fast without scanning every row",
        "A list of every query the database has ever run",
        "A backup copy of the table stored on a different disk",
        "A setting that makes writes to a table faster"
      ],
      "correctIndex": 0,
      "explanation": "An index is a separate, pre-built structure (sorted or hashed) mapping column values to row locations, so a lookup can jump close to the answer instead of scanning every row.",
      "reference": "<div class=\"not-prose flex flex-col gap-1 text-sm\"><div>📖 <strong>Index</strong> — a dictionary's alphabetical order, not the book's chapter order</div><div>⚖️ <strong>Trade-off</strong> — costs disk space + slower writes, buys faster reads</div></div><p><strong>Why:</strong> a dictionary lets you find a word without reading the whole book because its entries are pre-sorted — an index makes the same trade for a database column.</p>"
    },
    {
      "type": "quiz",
      "id": "index-def-2",
      "poolId": "index-def-pool",
      "level": 1,
      "prompt": "Which trade-off does adding a database index actually make?",
      "choices": [
        "It's pure upside — faster reads with no downside",
        "It uses extra disk space and slows down writes to the indexed column, in exchange for faster reads",
        "It makes reads slower but writes faster",
        "It only affects how the table is displayed, not query speed"
      ],
      "correctIndex": 1,
      "explanation": "An index needs its own storage, and every insert/update/delete on the indexed column has to update the index too — indexing trades write cost and disk space for read speed, not a free upgrade.",
      "reference": "<div class=\"not-prose flex flex-col gap-1 text-sm\"><div>📖 <strong>Index</strong> — a dictionary's alphabetical order, not the book's chapter order</div><div>⚖️ <strong>Trade-off</strong> — costs disk space + slower writes, buys faster reads</div></div><p><strong>Why:</strong> a dictionary lets you find a word without reading the whole book because its entries are pre-sorted — an index makes the same trade for a database column.</p>"
    },
    {
      "type": "quiz",
      "id": "btree-def-1",
      "poolId": "btree-def-pool",
      "level": 1,
      "prompt": "What makes a B-tree index different from a full table scan?",
      "choices": [
        "It stores the data in a random order to speed up writes",
        "It's a balanced, sorted tree structure where lookup cost grows logarithmically, not linearly, with row count",
        "It only works on numeric columns",
        "It removes the need to store the original table at all"
      ],
      "correctIndex": 1,
      "explanation": "A B-tree keeps values sorted in a balanced tree, so a lookup only needs to make a handful of comparisons (roughly log2 of the row count) instead of checking every row.",
      "reference": "<div class=\"not-prose flex flex-col gap-1 text-sm\"><div>🌳 <strong>B-tree</strong> — balanced, sorted tree</div><div>📉 <strong>Cost</strong> — O(log n): doubling rows adds ~1 comparison, not double the comparisons</div></div><p><strong>Why:</strong> because the tree stays sorted, it also answers range queries (\"age &gt; 30\") for free — a full scan or hash index can't say the same.</p>"
    },
    {
      "type": "quiz",
      "id": "btree-def-2",
      "poolId": "btree-def-pool",
      "level": 1,
      "prompt": "Roughly how does a B-tree's lookup cost grow as the row count grows?",
      "choices": [
        "Logarithmically — doubling the rows adds only about one more comparison",
        "Linearly — doubling the rows doubles the comparisons",
        "Exponentially — doubling the rows squares the comparisons",
        "It doesn't grow at all, regardless of row count"
      ],
      "correctIndex": 0,
      "explanation": "A B-tree lookup cost is O(log n) — at 2,000,000 rows it takes roughly 21 comparisons, barely more than the ~9 comparisons needed at 500 rows.",
      "reference": "<div class=\"not-prose flex flex-col gap-1 text-sm\"><div>🌳 <strong>B-tree</strong> — balanced, sorted tree</div><div>📉 <strong>Cost</strong> — O(log n): doubling rows adds ~1 comparison, not double the comparisons</div></div><p><strong>Why:</strong> because the tree stays sorted, it also answers range queries (\"age &gt; 30\") for free — a full scan or hash index can't say the same.</p>"
    },
    {
      "type": "quiz",
      "id": "hash-index-def-1",
      "poolId": "hash-index-def-pool",
      "level": 1,
      "prompt": "What is a hash index best suited for?",
      "choices": [
        "Range queries like \"find every row between two values\"",
        "Exact-match lookups, where average-case cost is constant regardless of row count",
        "Sorting a table by the indexed column",
        "Finding the row closest to a given value"
      ],
      "correctIndex": 1,
      "explanation": "A hash index gives average O(1) exact-match lookups — but hashing destroys value order, so it can't answer range or sorted-output queries at all.",
      "reference": "<div class=\"not-prose flex flex-col gap-1 text-sm\"><div>#️⃣ <strong>Hash index</strong> — O(1) average for exact match</div><div>🚫 <strong>Can't do</strong> — ranges, sorted output (order is scrambled)</div></div><p><strong>Why:</strong> the same scrambling that makes lookups fast is what destroys the ordering a range query needs.</p>"
    },
    {
      "type": "quiz",
      "id": "hash-index-def-2",
      "poolId": "hash-index-def-pool",
      "level": 1,
      "prompt": "Why can't a hash index answer a range query like \"find every row with age between 30 and 40\"?",
      "choices": [
        "Because hash indexes are always slower than B-trees",
        "Because hashing scatters values into unrelated positions, so there's no way to know which values fall between two others without checking every one",
        "Because range queries are not a valid kind of database query",
        "Because hash indexes only work on string columns"
      ],
      "correctIndex": 1,
      "explanation": "A hash function deliberately scrambles similar inputs into unrelated bucket positions — that's what makes lookups fast, but it also destroys the ordering a range query depends on.",
      "reference": "<div class=\"not-prose flex flex-col gap-1 text-sm\"><div>#️⃣ <strong>Hash index</strong> — O(1) average for exact match</div><div>🚫 <strong>Can't do</strong> — ranges, sorted output (order is scrambled)</div></div><p><strong>Why:</strong> the same scrambling that makes lookups fast is what destroys the ordering a range query needs.</p>"
    },
    {
      "type": "quiz",
      "id": "selectivity-def-1",
      "poolId": "selectivity-def-pool",
      "level": 1,
      "prompt": "What does \"selectivity\" mean for an indexed column?",
      "choices": [
        "How much disk space the index uses",
        "How much the index actually narrows a search — a mostly-unique column is highly selective, a mostly-repeated column is not",
        "Whether the column allows NULL values",
        "How often the column is updated"
      ],
      "correctIndex": 1,
      "explanation": "Selectivity measures how much an index narrows the search. A near-unique column like email narrows a lot; a column like a boolean flag with only two values narrows almost nothing.",
      "reference": "<div class=\"not-prose flex flex-col gap-1 text-sm\"><div>🎯 <strong>High selectivity</strong> — email (mostly unique) narrows a lot</div><div>🪨 <strong>Low selectivity</strong> — is_active (2 values) narrows almost nothing</div></div><p><strong>Why:</strong> an index only helps if following it actually eliminates most of the table — a low-selectivity index may not be worth using at all.</p>"
    },
    {
      "type": "quiz",
      "id": "selectivity-def-2",
      "poolId": "selectivity-def-pool",
      "level": 1,
      "prompt": "Why does an index on a boolean column like is_active (true/false) usually help a query very little?",
      "choices": [
        "Because booleans can't be indexed at all",
        "Because with only two possible values, the index barely narrows the search — roughly half the table still matches either value",
        "Because boolean indexes are always slower to build than other indexes",
        "Because booleans are stored as text, which breaks indexing"
      ],
      "correctIndex": 1,
      "explanation": "A column with only a couple of distinct values has low selectivity — an index on it still leaves a huge fraction of the table matching, so the database may reasonably prefer a full scan.",
      "reference": "<div class=\"not-prose flex flex-col gap-1 text-sm\"><div>🎯 <strong>High selectivity</strong> — email (mostly unique) narrows a lot</div><div>🪨 <strong>Low selectivity</strong> — is_active (2 values) narrows almost nothing</div></div><p><strong>Why:</strong> an index only helps if following it actually eliminates most of the table — a low-selectivity index may not be worth using at all.</p>"
    },
    {
      "type": "quiz",
      "id": "scenario-1",
      "poolId": "scenario-pool",
      "level": 1,
      "prompt": "In the opening scenario, the login query took 2ms at 500 users and 400ms at 2,000,000 users. What changed about the query itself between those two points?",
      "choices": [
        "The query was rewritten to be less efficient",
        "Nothing about the query changed — only the row count grew",
        "The database engine was downgraded",
        "The email column was renamed"
      ],
      "correctIndex": 1,
      "explanation": "The query and code were unchanged — the slowdown came entirely from the table outgrowing the point where a full scan was cheap enough to go unnoticed.",
      "reference": "<div class=\"not-prose flex flex-col gap-1 text-sm\"><div>🐣 <strong>500 users</strong> — 2ms</div><div>📈 <strong>2,000,000 users</strong> — 400ms, same query</div></div><p><strong>Why:</strong> an unindexed lookup's cost is tied to table size, not query complexity — growth alone is enough to make a fine query slow.</p>"
    },
    {
      "type": "quiz",
      "id": "scenario-2",
      "poolId": "scenario-pool",
      "level": 1,
      "prompt": "What is the most direct fix for the login query slowdown described in the opening scenario, without changing the query text?",
      "choices": [
        "Add an index on the email column",
        "Rewrite the query in a different programming language",
        "Delete old user records to shrink the table",
        "Increase the server's screen resolution"
      ],
      "correctIndex": 0,
      "explanation": "Adding an index on email lets the database jump close to the matching row instead of scanning all 2,000,000 rows — the query text doesn't need to change at all.",
      "reference": "<div class=\"not-prose flex flex-col gap-1 text-sm\"><div>🐣 <strong>500 users</strong> — 2ms</div><div>📈 <strong>2,000,000 users</strong> — 400ms, same query</div></div><p><strong>Why:</strong> an unindexed lookup's cost is tied to table size, not query complexity — growth alone is enough to make a fine query slow.</p>"
    },
    {
      "type": "quiz",
      "id": "dictionary-analogy-1",
      "poolId": "dictionary-analogy-pool",
      "level": 1,
      "prompt": "How is a database index similar to a dictionary?",
      "choices": [
        "Both store data in the order it was originally entered",
        "Both are organized (sorted) specifically to let you find an entry without reading everything before it",
        "Both require memorizing every entry in advance",
        "Neither has anything in common with the other"
      ],
      "correctIndex": 1,
      "explanation": "A dictionary's alphabetical order exists precisely so you can find a word without reading the whole book — an index makes the same trade for a database column.",
      "reference": "<div class=\"not-prose flex flex-col gap-1 text-sm\"><div>📖 <strong>Index</strong> — a dictionary's alphabetical order, not the book's chapter order</div><div>⚖️ <strong>Trade-off</strong> — costs disk space + slower writes, buys faster reads</div></div><p><strong>Why:</strong> a dictionary lets you find a word without reading the whole book because its entries are pre-sorted — an index makes the same trade for a database column.</p>"
    },
    {
      "type": "quiz",
      "id": "linear-vs-log-1",
      "poolId": "linear-vs-log-pool",
      "level": 2,
      "prompt": "Going from 500 to 2,000,000 rows is a 4,000x increase in data. Roughly how many more comparisons does a B-tree lookup need at 2,000,000 rows compared to 500?",
      "choices": [
        "About 4,000x more comparisons",
        "About 12 more comparisons total (21 vs. 9) — barely more, not proportionally more",
        "Exactly the same number of comparisons",
        "About 100x more comparisons"
      ],
      "correctIndex": 1,
      "explanation": "A B-tree's cost is O(log n) — log2(2,000,000) ≈ 21 vs. log2(500) ≈ 9, so a 4,000x growth in data adds only about 12 more comparisons, not a proportional increase.",
      "reference": "<div class=\"not-prose flex flex-col gap-1 text-sm\"><div>📊 <strong>Full scan</strong> — 500 → 2,000,000 checks (linear)</div><div>🌳 <strong>B-tree</strong> — 9 → 21 comparisons (logarithmic)</div></div><p><strong>Why:</strong> linear vs. logarithmic is the whole story — the same 4,000x data growth means 4,000x more full-scan work but barely any more B-tree work.</p>"
    },
    {
      "type": "quiz",
      "id": "linear-vs-log-2",
      "poolId": "linear-vs-log-pool",
      "level": 2,
      "prompt": "What is the key difference between how full-scan cost and B-tree lookup cost grow as row count increases?",
      "choices": [
        "Full scan grows linearly with row count; B-tree grows logarithmically",
        "Both grow at exactly the same rate",
        "Full scan grows logarithmically; B-tree grows linearly",
        "Neither grows as row count increases"
      ],
      "correctIndex": 0,
      "explanation": "Full scan cost is O(n) — directly proportional to row count. B-tree lookup cost is O(log n) — it barely grows even as row count grows enormously.",
      "reference": "<div class=\"not-prose flex flex-col gap-1 text-sm\"><div>📊 <strong>Full scan</strong> — 500 → 2,000,000 checks (linear)</div><div>🌳 <strong>B-tree</strong> — 9 → 21 comparisons (logarithmic)</div></div><p><strong>Why:</strong> linear vs. logarithmic is the whole story — the same 4,000x data growth means 4,000x more full-scan work but barely any more B-tree work.</p>"
    },
    {
      "type": "quiz",
      "id": "btree-vs-hash-table-1",
      "poolId": "btree-vs-hash-table-pool",
      "level": 2,
      "prompt": "Which of these can a B-tree index do that a hash index cannot?",
      "choices": [
        "Exact-match lookup on the indexed column",
        "Answer a range query like WHERE age > 30, or return results in sorted order",
        "Store more than one column",
        "Be used at all in a database"
      ],
      "correctIndex": 1,
      "explanation": "Because a B-tree keeps values in sorted order, it can answer range queries and sorted output for free. A hash index's scrambled bucket layout can't support either.",
      "reference": "<div class=\"not-prose overflow-x-auto text-sm\"><table class=\"w-full border-collapse\"><tr><th class=\"text-left pr-4\"></th><th class=\"text-left pr-4\">B-tree</th><th class=\"text-left\">Hash</th></tr><tr><td class=\"pr-4\">Exact match</td><td class=\"pr-4\">O(log n)</td><td>O(1) avg</td></tr><tr><td class=\"pr-4\">Range query</td><td class=\"pr-4\">✅ yes</td><td>❌ no</td></tr><tr><td class=\"pr-4\">Sorted output</td><td class=\"pr-4\">✅ free</td><td>❌ no</td></tr></table></div><p><strong>Why:</strong> the hash function's scrambling is what makes lookups O(1) — it's the same mechanism that destroys the order a range query needs.</p>"
    },
    {
      "type": "quiz",
      "id": "btree-vs-hash-table-2",
      "poolId": "btree-vs-hash-table-pool",
      "level": 2,
      "prompt": "For a typical exact-match-only lookup (like a session-token cache keyed by an opaque token that's never sorted or range-queried), which index type is usually the better fit?",
      "choices": [
        "A hash index — no ranges or sorting will ever be needed, so its O(1) average lookup is pure upside",
        "A B-tree — it's always strictly better than a hash index",
        "Neither — indexes should never be used for cache-like lookups",
        "It doesn't matter; both perform identically in every case"
      ],
      "correctIndex": 0,
      "explanation": "When range queries and sorted output will genuinely never be needed, a hash index's O(1) average exact-match lookup is the better fit — the B-tree's extra range capability isn't being used, so its slightly higher per-lookup cost buys nothing here.",
      "reference": "<div class=\"not-prose overflow-x-auto text-sm\"><table class=\"w-full border-collapse\"><tr><th class=\"text-left pr-4\"></th><th class=\"text-left pr-4\">B-tree</th><th class=\"text-left\">Hash</th></tr><tr><td class=\"pr-4\">Exact match</td><td class=\"pr-4\">O(log n)</td><td>O(1) avg</td></tr><tr><td class=\"pr-4\">Range query</td><td class=\"pr-4\">✅ yes</td><td>❌ no</td></tr><tr><td class=\"pr-4\">Sorted output</td><td class=\"pr-4\">✅ free</td><td>❌ no</td></tr></table></div><p><strong>Why:</strong> the hash function's scrambling is what makes lookups O(1) — it's the same mechanism that destroys the order a range query needs.</p>"
    },
    {
      "type": "quiz",
      "id": "leading-wildcard-1",
      "poolId": "leading-wildcard-pool",
      "level": 2,
      "prompt": "Why does WHERE email LIKE '%@example.com' typically fail to use an index on email, while WHERE email LIKE 'alice%' can use it?",
      "choices": [
        "A leading wildcard means a match could start anywhere in the string, so sorted order can't narrow the search — a trailing wildcard still lets sorted order narrow from the front",
        "LIKE queries can never use an index, regardless of wildcard position",
        "The '%' character is invalid in SQL and causes an error either way",
        "Trailing wildcards are always slower than leading wildcards"
      ],
      "correctIndex": 0,
      "explanation": "A leading wildcard ('%...') means the match could start anywhere, so the sorted order an index relies on doesn't help. A trailing wildcard ('alice%') still lets the index narrow the search to entries starting with 'alice'.",
      "reference": "<div class=\"not-prose flex flex-col gap-1 text-sm\"><div>❌ <strong>LIKE '%@x.com'</strong> — match could start anywhere, index can't help</div><div>✅ <strong>LIKE 'alice%'</strong> — sorted order narrows from the front</div><div>❌ <strong>WHERE LOWER(email)=?</strong> — indexes the raw value, not the function's output</div></div><p><strong>Why:</strong> an index only helps when its sort order actually narrows the search — these patterns each break that in a different way.</p>"
    },
    {
      "type": "quiz",
      "id": "leading-wildcard-2",
      "poolId": "leading-wildcard-pool",
      "level": 2,
      "prompt": "A query uses WHERE LOWER(email) = 'alice@example.com' on a table with a plain index on email. Why might this query not use that index?",
      "choices": [
        "Because the index was built on the raw email values, not on the output of LOWER(email) — the database would need a separate index on the expression itself",
        "Because LOWER() is not a valid SQL function",
        "Because indexes only work on numeric columns",
        "Because equality comparisons never use indexes"
      ],
      "correctIndex": 0,
      "explanation": "An index on email matches raw stored values. Applying a function like LOWER() changes what's being looked up, so the plain index doesn't match unless an index on the expression itself was created.",
      "reference": "<div class=\"not-prose flex flex-col gap-1 text-sm\"><div>❌ <strong>LIKE '%@x.com'</strong> — match could start anywhere, index can't help</div><div>✅ <strong>LIKE 'alice%'</strong> — sorted order narrows from the front</div><div>❌ <strong>WHERE LOWER(email)=?</strong> — indexes the raw value, not the function's output</div></div><p><strong>Why:</strong> an index only helps when its sort order actually narrows the search — these patterns each break that in a different way.</p>"
    },
    {
      "type": "quiz",
      "id": "index-everything-1",
      "poolId": "index-everything-pool",
      "level": 2,
      "prompt": "Why isn't \"just add an index to every column\" a good default strategy?",
      "choices": [
        "Because databases only support a maximum of two indexes per table",
        "Because every index costs disk space and slows down writes to that column, whether or not it ever gets used by a query",
        "Because indexes make read queries slower, not faster",
        "Because indexes are automatically deleted after 30 days"
      ],
      "correctIndex": 1,
      "explanation": "Each index is extra storage and has to be updated on every write to that column — indexing a column nobody queries that way trades write performance for a read benefit that's never used.",
      "reference": "<div class=\"not-prose flex flex-col gap-1 text-sm\"><div>💾 <strong>Every index</strong> — costs disk space</div><div>🐢 <strong>Every write</strong> — must update every index on that column</div><div>🎯 <strong>Right call</strong> — match indexes to actual query patterns, not \"just in case\"</div></div><p><strong>Why:</strong> an index only pays for itself if the read savings outweigh the ongoing write cost — that's not automatic.</p>"
    },
    {
      "type": "quiz",
      "id": "index-everything-2",
      "poolId": "index-everything-pool",
      "level": 2,
      "prompt": "A table has an index on last_name, but every query filters by last_name AND birth_year together. What's the issue?",
      "choices": [
        "There is no issue — the last_name index will fully optimize this query on its own",
        "The index on last_name alone doesn't match the actual query pattern — a combined index on (last_name, birth_year) would serve this specific query better",
        "Indexes can only be built on one column ever, so this query can never be optimized",
        "birth_year should never be used in a WHERE clause"
      ],
      "correctIndex": 1,
      "explanation": "An index should generally match how columns are actually queried together. An index on last_name alone still leaves birth_year filtering unindexed for this query — a composite index on both matches the real access pattern.",
      "reference": "<div class=\"not-prose flex flex-col gap-1 text-sm\"><div>💾 <strong>Every index</strong> — costs disk space</div><div>🐢 <strong>Every write</strong> — must update every index on that column</div><div>🎯 <strong>Right call</strong> — match indexes to actual query patterns, not \"just in case\"</div></div><p><strong>Why:</strong> an index only pays for itself if the read savings outweigh the ongoing write cost — that's not automatic.</p>"
    },
    {
      "type": "quiz",
      "id": "chart-reading-1",
      "poolId": "chart-reading-pool",
      "level": 2,
      "prompt": "L2 shows two separate charts (full scan vs. B-tree) instead of one combined chart. Why?",
      "choices": [
        "Because Mermaid cannot render two data series in one chart at all",
        "Because putting both on one linear axis would flatten the B-tree's tiny bars to invisible slivers next to the full scan's much larger numbers",
        "Because the two charts represent unrelated data with no shared meaning",
        "Because full-scan data and B-tree data use different row counts"
      ],
      "correctIndex": 1,
      "explanation": "The full scan's values (up to 2,000,000) and the B-tree's values (up to 21) are so different in scale that one linear axis would make the B-tree bars invisible — splitting them into two charts with matched x-axes keeps both readable.",
      "reference": "<div class=\"not-prose flex flex-col gap-1 text-sm\"><div>📊 <strong>Chart 1</strong> — full scan, y-axis 0–2,000,000</div><div>📈 <strong>Chart 2</strong> — B-tree, y-axis 0–25</div><div>🔗 <strong>Shared</strong> — same 5 row counts on both x-axes</div></div><p><strong>Why:</strong> this is the same finding as the linear-vs-logarithmic comparison, just shown visually rather than only in words.</p>"
    },
    {
      "type": "quiz",
      "id": "chart-reading-2",
      "poolId": "chart-reading-pool",
      "level": 2,
      "prompt": "Looking at L2's two charts, what does the B-tree chart's near-flat shape across 500 to 2,000,000 rows actually demonstrate?",
      "choices": [
        "That the B-tree index stops working correctly at high row counts",
        "That B-tree lookup cost grows so slowly (logarithmically) that a 4,000x increase in rows is barely visible in comparison cost",
        "That the chart contains a rendering error",
        "That B-tree indexes only work up to 2,000,000 rows"
      ],
      "correctIndex": 1,
      "explanation": "The near-flat B-tree bars are the whole point — logarithmic growth means even a massive increase in data barely changes the number of comparisons needed.",
      "reference": "<div class=\"not-prose flex flex-col gap-1 text-sm\"><div>📊 <strong>Chart 1</strong> — full scan, y-axis 0–2,000,000</div><div>📈 <strong>Chart 2</strong> — B-tree, y-axis 0–25</div><div>🔗 <strong>Shared</strong> — same 5 row counts on both x-axes</div></div><p><strong>Why:</strong> this is the same finding as the linear-vs-logarithmic comparison, just shown visually rather than only in words.</p>"
    },
    {
      "type": "code",
      "id": "binary-search-by-email",
      "poolId": "binary-search-pool",
      "level": 3,
      "prompt": "Implement `binarySearchByEmail(sortedUsers, email)`. `sortedUsers` is an array of `{ id, email }` objects already sorted ascending by `email`. Return the matching user object, or `null` if no row has that exact email. Use binary search — don't scan the array linearly.",
      "starterCode": "function binarySearchByEmail(sortedUsers, email) {\n  // TODO: binary search over sortedUsers by email\n}\n",
      "tests": [
        {
          "description": "finds a row present at a known position",
          "expr": "(() => { const users = Array.from({length: 500}, (_, i) => ({ id: i, email: `user${String(i).padStart(7,'0')}@example.com` })); const r = binarySearchByEmail(users, 'user0000250@example.com'); return expect(r && r.id).toBe(250); })()"
        },
        {
          "description": "returns null for an email that isn't in the data",
          "expr": "(() => { const users = Array.from({length: 500}, (_, i) => ({ id: i, email: `user${String(i).padStart(7,'0')}@example.com` })); return expect(binarySearchByEmail(users, 'nope@example.com')).toBe(null); })()"
        },
        {
          "description": "finds the first row in the sorted array",
          "expr": "(() => { const users = Array.from({length: 500}, (_, i) => ({ id: i, email: `user${String(i).padStart(7,'0')}@example.com` })); const r = binarySearchByEmail(users, 'user0000000@example.com'); return expect(r && r.id).toBe(0); })()"
        },
        {
          "description": "finds the last row in the sorted array",
          "expr": "(() => { const users = Array.from({length: 500}, (_, i) => ({ id: i, email: `user${String(i).padStart(7,'0')}@example.com` })); const r = binarySearchByEmail(users, 'user0000499@example.com'); return expect(r && r.id).toBe(499); })()"
        }
      ],
      "solution": "function binarySearchByEmail(sortedUsers, email) {\n  let lo = 0, hi = sortedUsers.length - 1;\n  while (lo <= hi) {\n    const mid = (lo + hi) >> 1;\n    const midEmail = sortedUsers[mid].email;\n    if (midEmail === email) return sortedUsers[mid];\n    if (midEmail < email) lo = mid + 1;\n    else hi = mid - 1;\n  }\n  return null;\n}",
      "explanation": "Binary search halves the remaining range on each comparison by checking the midpoint and discarding the half that can't contain the target. Because sortedUsers is ordered by email, this reaches any row in O(log n) comparisons instead of scanning every row — this is the same lookup behavior a real B-tree provides, without the on-disk tree machinery.",
      "reference": "<div class=\"not-prose flex flex-col gap-1 text-sm\"><div>🌳 <strong>B-tree behavior</strong> — sorted array + binary search</div><div>📉 <strong>Cost</strong> — O(log n), ~9 comparisons at 500 rows, ~21 at 2,000,000</div></div><p><strong>Why:</strong> this is L3's exact `binarySearchByEmail` function — the same one benchmarked against a full scan.</p>"
    },
    {
      "type": "code",
      "id": "binary-search-by-id",
      "poolId": "binary-search-pool",
      "level": 3,
      "prompt": "Implement `binarySearchById(sortedUsers, id)`. `sortedUsers` is an array of `{ id, email }` objects already sorted ascending by numeric `id`. Return the matching user object, or `null` if no row has that exact id. Use binary search — don't scan the array linearly.",
      "starterCode": "function binarySearchById(sortedUsers, id) {\n  // TODO: binary search over sortedUsers by id\n}\n",
      "tests": [
        {
          "description": "finds a row present at a known position",
          "expr": "(() => { const users = Array.from({length: 500}, (_, i) => ({ id: i, email: `user${String(i).padStart(7,'0')}@example.com` })); const r = binarySearchById(users, 250); return expect(r && r.email).toBe('user0000250@example.com'); })()"
        },
        {
          "description": "returns null for an id that isn't in the data",
          "expr": "(() => { const users = Array.from({length: 500}, (_, i) => ({ id: i, email: `user${String(i).padStart(7,'0')}@example.com` })); return expect(binarySearchById(users, 99999)).toBe(null); })()"
        },
        {
          "description": "finds the first row in the sorted array",
          "expr": "(() => { const users = Array.from({length: 500}, (_, i) => ({ id: i, email: `user${String(i).padStart(7,'0')}@example.com` })); const r = binarySearchById(users, 0); return expect(r && r.id).toBe(0); })()"
        },
        {
          "description": "finds the last row in the sorted array",
          "expr": "(() => { const users = Array.from({length: 500}, (_, i) => ({ id: i, email: `user${String(i).padStart(7,'0')}@example.com` })); const r = binarySearchById(users, 499); return expect(r && r.id).toBe(499); })()"
        }
      ],
      "solution": "function binarySearchById(sortedUsers, id) {\n  let lo = 0, hi = sortedUsers.length - 1;\n  while (lo <= hi) {\n    const mid = (lo + hi) >> 1;\n    const midId = sortedUsers[mid].id;\n    if (midId === id) return sortedUsers[mid];\n    if (midId < id) lo = mid + 1;\n    else hi = mid - 1;\n  }\n  return null;\n}",
      "explanation": "Binary search halves the remaining range on each comparison by checking the midpoint and discarding the half that can't contain the target. Because sortedUsers is ordered by id, this reaches any row in O(log n) comparisons instead of scanning every row — the same lookup behavior a real B-tree provides on a numeric key.",
      "reference": "<div class=\"not-prose flex flex-col gap-1 text-sm\"><div>🌳 <strong>B-tree behavior</strong> — sorted array + binary search</div><div>📉 <strong>Cost</strong> — O(log n), ~9 comparisons at 500 rows, ~21 at 2,000,000</div></div><p><strong>Why:</strong> this is L3's exact `binarySearchByEmail` function — the same one benchmarked against a full scan.</p>"
    },
    {
      "type": "code",
      "id": "range-query-by-email",
      "poolId": "range-query-pool",
      "level": 3,
      "prompt": "Implement `rangeQuerySorted(sortedUsers, fromEmail, toEmail)`. `sortedUsers` is sorted ascending by `email`. Return an array of every user whose `email` is between `fromEmail` and `toEmail`, inclusive on both ends, in ascending order. Use binary search to find the starting point rather than scanning from the beginning.",
      "starterCode": "function rangeQuerySorted(sortedUsers, fromEmail, toEmail) {\n  // TODO: find the range [fromEmail, toEmail] inclusive\n}\n",
      "tests": [
        {
          "description": "returns exactly the 5 rows in a known range",
          "expr": "(() => { const users = Array.from({length: 500}, (_, i) => ({ id: i, email: `user${String(i).padStart(7,'0')}@example.com` })); const r = rangeQuerySorted(users, 'user0000100@example.com', 'user0000104@example.com'); return expect(r.map(u => u.id)).toEqual([100,101,102,103,104]); })()"
        },
        {
          "description": "returns an empty array when nothing matches",
          "expr": "(() => { const users = Array.from({length: 500}, (_, i) => ({ id: i, email: `user${String(i).padStart(7,'0')}@example.com` })); const r = rangeQuerySorted(users, 'zzz1@example.com', 'zzz2@example.com'); return expect(r.length).toBe(0); })()"
        },
        {
          "description": "includes both boundary rows",
          "expr": "(() => { const users = Array.from({length: 500}, (_, i) => ({ id: i, email: `user${String(i).padStart(7,'0')}@example.com` })); const r = rangeQuerySorted(users, 'user0000010@example.com', 'user0000010@example.com'); return expect(r.map(u => u.id)).toEqual([10]); })()"
        }
      ],
      "solution": "function rangeQuerySorted(sortedUsers, fromEmail, toEmail) {\n  let lo = 0, hi = sortedUsers.length;\n  while (lo < hi) {\n    const mid = (lo + hi) >> 1;\n    if (sortedUsers[mid].email < fromEmail) lo = mid + 1;\n    else hi = mid;\n  }\n  const results = [];\n  for (let i = lo; i < sortedUsers.length && sortedUsers[i].email <= toEmail; i++) {\n    results.push(sortedUsers[i]);\n  }\n  return results;\n}",
      "explanation": "Binary search locates the first row whose email is >= fromEmail, then a simple forward walk collects rows until email exceeds toEmail. Because the data is sorted, this only touches rows actually in range — a hash index has no equivalent operation, since hashing destroys the ordering this relies on.",
      "reference": "<div class=\"not-prose flex flex-col gap-1 text-sm\"><div>🔍 <strong>Step 1</strong> — binary search for the range's start</div><div>➡️ <strong>Step 2</strong> — walk forward until past the range's end</div><div>🚫 <strong>A hash index can't do this</strong> — order is scrambled at build time</div></div><p><strong>Why:</strong> this is exactly why B-tree indexes remain the default choice even though hash indexes can be faster for pure exact-match lookups.</p>"
    },
    {
      "type": "code",
      "id": "range-query-by-id",
      "poolId": "range-query-pool",
      "level": 3,
      "prompt": "Implement `rangeQueryById(sortedUsers, fromId, toId)`. `sortedUsers` is sorted ascending by numeric `id`. Return an array of every user whose `id` is between `fromId` and `toId`, inclusive on both ends, in ascending order. Use binary search to find the starting point rather than scanning from the beginning.",
      "starterCode": "function rangeQueryById(sortedUsers, fromId, toId) {\n  // TODO: find the range [fromId, toId] inclusive\n}\n",
      "tests": [
        {
          "description": "returns exactly the 5 rows in a known range",
          "expr": "(() => { const users = Array.from({length: 500}, (_, i) => ({ id: i, email: `user${String(i).padStart(7,'0')}@example.com` })); const r = rangeQueryById(users, 100, 104); return expect(r.map(u => u.id)).toEqual([100,101,102,103,104]); })()"
        },
        {
          "description": "returns an empty array when the range is outside the data",
          "expr": "(() => { const users = Array.from({length: 500}, (_, i) => ({ id: i, email: `user${String(i).padStart(7,'0')}@example.com` })); const r = rangeQueryById(users, 10000, 20000); return expect(r.length).toBe(0); })()"
        },
        {
          "description": "includes both boundary rows",
          "expr": "(() => { const users = Array.from({length: 500}, (_, i) => ({ id: i, email: `user${String(i).padStart(7,'0')}@example.com` })); const r = rangeQueryById(users, 10, 10); return expect(r.map(u => u.id)).toEqual([10]); })()"
        }
      ],
      "solution": "function rangeQueryById(sortedUsers, fromId, toId) {\n  let lo = 0, hi = sortedUsers.length;\n  while (lo < hi) {\n    const mid = (lo + hi) >> 1;\n    if (sortedUsers[mid].id < fromId) lo = mid + 1;\n    else hi = mid;\n  }\n  const results = [];\n  for (let i = lo; i < sortedUsers.length && sortedUsers[i].id <= toId; i++) {\n    results.push(sortedUsers[i]);\n  }\n  return results;\n}",
      "explanation": "Binary search locates the first row whose id is >= fromId, then a forward walk collects rows until id exceeds toId. Because the data is sorted by id, this only touches rows actually in range — the same operation rangeQuerySorted performs on email, applied to a numeric key.",
      "reference": "<div class=\"not-prose flex flex-col gap-1 text-sm\"><div>🔍 <strong>Step 1</strong> — binary search for the range's start</div><div>➡️ <strong>Step 2</strong> — walk forward until past the range's end</div><div>🚫 <strong>A hash index can't do this</strong> — order is scrambled at build time</div></div><p><strong>Why:</strong> this is exactly why B-tree indexes remain the default choice even though hash indexes can be faster for pure exact-match lookups.</p>"
    },
    {
      "type": "quiz",
      "id": "benchmark-reading-1",
      "poolId": "benchmark-reading-pool",
      "level": 3,
      "prompt": "L3's benchmark shows the full scan slowed down about 6,200x while the binary search only slowed down about 7x, when going from 500 to 2,000,000 rows (a 4,000x increase in data). What does this demonstrate?",
      "choices": [
        "That binary search is broken at large row counts",
        "That linear-cost operations (full scan) degrade roughly proportional to data growth, while logarithmic-cost operations (binary search / B-tree) barely degrade at all",
        "That the benchmark measurements are unreliable and shouldn't be trusted",
        "That 2,000,000 rows is too many for any database to handle"
      ],
      "correctIndex": 1,
      "explanation": "This is the practical, measured version of the O(n) vs. O(log n) difference: a 4,000x data increase means roughly 4,000x more full-scan work (plus real hardware effects like cache misses push it even higher), but the binary search barely notices.",
      "reference": "<div class=\"not-prose overflow-x-auto text-sm\"><table class=\"w-full border-collapse\"><tr><th class=\"text-left pr-4\">Rows</th><th class=\"text-left pr-4\">Full scan</th><th class=\"text-left\">Binary search</th></tr><tr><td class=\"pr-4\">500</td><td class=\"pr-4\">0.0022ms</td><td>0.00017ms</td></tr><tr><td class=\"pr-4\">2,000,000</td><td class=\"pr-4\">13.47ms</td><td>0.0012ms</td></tr></table></div><p><strong>Why:</strong> these are L3's actual measured numbers, not estimates — the gap is the same linear-vs-logarithmic story, now with real timings attached.</p>"
    },
    {
      "type": "quiz",
      "id": "benchmark-reading-2",
      "poolId": "benchmark-reading-pool",
      "level": 3,
      "prompt": "L3 notes that the full scan's real slowdown (~6,200x) was higher than the row-count increase (4,000x). What's the most likely explanation given the unit's own framing?",
      "choices": [
        "The benchmark code has a bug that inflates full-scan timing",
        "Real hardware effects like cache misses on a much larger array push a linear-cost operation's actual slowdown past a perfectly proportional increase",
        "JavaScript arrays become slower than their theoretical complexity as they grow, invalidating Big-O reasoning entirely",
        "The 2,000,000-row test used a different CPU than the 500-row test"
      ],
      "correctIndex": 1,
      "explanation": "Big-O describes the shape of growth, not the exact constant factor — real-world costs like cache misses on a much larger array can make an O(n) operation's actual slowdown exceed the row-count ratio itself, while the underlying linear relationship still holds.",
      "reference": "<div class=\"not-prose overflow-x-auto text-sm\"><table class=\"w-full border-collapse\"><tr><th class=\"text-left pr-4\">Rows</th><th class=\"text-left pr-4\">Full scan</th><th class=\"text-left\">Binary search</th></tr><tr><td class=\"pr-4\">500</td><td class=\"pr-4\">0.0022ms</td><td>0.00017ms</td></tr><tr><td class=\"pr-4\">2,000,000</td><td class=\"pr-4\">13.47ms</td><td>0.0012ms</td></tr></table></div><p><strong>Why:</strong> these are L3's actual measured numbers, not estimates — the gap is the same linear-vs-logarithmic story, now with real timings attached.</p>"
    },
    {
      "type": "quiz",
      "id": "generalization-1",
      "poolId": "generalization-pool",
      "level": 3,
      "prompt": "L3 says the exact millisecond numbers from its benchmark are specific to this machine and data, but the O(n)-vs-O(log n)/O(1) *shape* generalizes. What does that distinction mean in practice?",
      "choices": [
        "A production database would show the identical millisecond timings this benchmark measured",
        "A production database will have different absolute numbers (disk I/O, caching, query planning add real overhead), but the same qualitative relationship — indexed lookups scale far better than full scans as data grows",
        "The benchmark's findings don't apply to real databases at all",
        "Production databases never perform full table scans"
      ],
      "correctIndex": 1,
      "explanation": "The relative relationship (full scan degrades proportionally to data, indexed lookups barely degrade) holds in any real system, even though a production database's absolute numbers differ due to disk I/O, caching layers, and query-planner decisions this in-memory simulation doesn't model.",
      "reference": "<div class=\"not-prose flex flex-col gap-1 text-sm\"><div>✅ <strong>Generalizes</strong> — the O(n) vs. O(log n)/O(1) shape, for any column/data</div><div>❌ <strong>Doesn't generalize</strong> — the exact millisecond numbers (this machine, this data)</div></div><p><strong>Why:</strong> confusing a worked example's specific numbers with its underlying mechanism is a common way to over- or under-trust a benchmark.</p>"
    },
    {
      "type": "quiz",
      "id": "generalization-2",
      "poolId": "generalization-pool",
      "level": 3,
      "prompt": "Which part of L3's simulation is explicitly called out as NOT present in a real production database?",
      "choices": [
        "The concept of comparisons growing logarithmically for a sorted structure",
        "Disk I/O, page caching, and query-planner decisions — all absent from this in-memory JavaScript simulation",
        "The existence of B-tree and hash indexes as two different structures",
        "The fact that full scans check every row"
      ],
      "correctIndex": 1,
      "explanation": "L3 explicitly notes that a real database adds disk I/O, caching layers, and query-planner decisions this simulation doesn't model — the underlying complexity relationships still hold, but the exact numbers won't match a real system.",
      "reference": "<div class=\"not-prose flex flex-col gap-1 text-sm\"><div>✅ <strong>Generalizes</strong> — the O(n) vs. O(log n)/O(1) shape, for any column/data</div><div>❌ <strong>Doesn't generalize</strong> — the exact millisecond numbers (this machine, this data)</div></div><p><strong>Why:</strong> confusing a worked example's specific numbers with its underlying mechanism is a common way to over- or under-trust a benchmark.</p>"
    },
    {
      "type": "quiz",
      "id": "planner-decision-1",
      "poolId": "planner-decision-pool",
      "level": 2,
      "prompt": "A table has an index on a low-selectivity column. Will the database's query planner always use that index when the column appears in a WHERE clause?",
      "choices": [
        "Yes — if an index exists on a column, the planner is required to use it",
        "Not necessarily — the planner may estimate a full scan is cheaper than following an index into a very large fraction of matching rows",
        "No — indexes are only ever used for ORDER BY, never WHERE",
        "It depends only on the table's total size, never on the column's selectivity"
      ],
      "correctIndex": 1,
      "explanation": "Having an index doesn't force its use. A query planner estimates cost, and following a low-selectivity index into millions of matching rows can genuinely be more expensive than just scanning — so the planner may reasonably skip the index.",
      "reference": "<div class=\"not-prose flex flex-col gap-1 text-sm\"><div>🧮 <strong>Planner</strong> — estimates cost, doesn't blindly use every index</div><div>🪨 <strong>Low selectivity</strong> — following the index may cost more than scanning</div></div><p><strong>Why:</strong> an index existing and an index being used are two different things — the planner decides based on estimated cost.</p>"
    },
    {
      "type": "quiz",
      "id": "planner-decision-2",
      "poolId": "planner-decision-pool",
      "level": 2,
      "prompt": "Why might a database's query planner choose a full scan over an existing index for a particular query?",
      "choices": [
        "Because using an index is always slower than a full scan",
        "Because the planner estimated that, for this specific query, scanning is actually cheaper than following the index — e.g. the indexed column has low selectivity",
        "Because indexes expire after a certain number of queries",
        "Because full scans are required by SQL syntax rules"
      ],
      "correctIndex": 1,
      "explanation": "The planner picks whichever strategy it estimates is cheaper for that specific query — an index isn't automatically the winner, especially against a low-selectivity column where it barely narrows the search.",
      "reference": "<div class=\"not-prose flex flex-col gap-1 text-sm\"><div>🧮 <strong>Planner</strong> — estimates cost, doesn't blindly use every index</div><div>🪨 <strong>Low selectivity</strong> — following the index may cost more than scanning</div></div><p><strong>Why:</strong> an index existing and an index being used are two different things — the planner decides based on estimated cost.</p>"
    },
    {
      "type": "quiz",
      "id": "composite-index-1",
      "poolId": "composite-index-pool",
      "level": 2,
      "prompt": "What is a composite (multi-column) index best suited for?",
      "choices": [
        "Queries that filter or sort on more than one of the indexed columns together, matching the actual combined access pattern",
        "Making every single-column query on the table faster automatically",
        "Replacing the need for a primary key",
        "Storing multiple copies of the same table for redundancy"
      ],
      "correctIndex": 0,
      "explanation": "A composite index is built to match a query pattern that filters on multiple columns together — e.g. (last_name, birth_year) — something a single-column index on either alone doesn't fully serve.",
      "reference": "<div class=\"not-prose flex flex-col gap-1 text-sm\"><div>🧩 <strong>Composite index</strong> — one index over multiple columns, e.g. (last_name, birth_year)</div><div>🎯 <strong>Fits</strong> — queries that filter on that exact column combination</div></div><p><strong>Why:</strong> the right index matches the actual query pattern, not just \"this column looks important on its own.\"</p>"
    },
    {
      "type": "quiz",
      "id": "composite-index-2",
      "poolId": "composite-index-pool",
      "level": 2,
      "prompt": "A table has separate single-column indexes on last_name and on birth_year. Every query filters WHERE last_name = ? AND birth_year = ? together. What would likely serve this query pattern better?",
      "choices": [
        "A single composite index on (last_name, birth_year) built specifically for that combined lookup",
        "Deleting both existing indexes, since neither is useful",
        "Adding a third, unrelated index on email",
        "Nothing — two single-column indexes are always equivalent to one composite index"
      ],
      "correctIndex": 0,
      "explanation": "Two separate single-column indexes don't combine into the same thing as one composite index built for the exact (last_name, birth_year) pattern the queries actually use — the composite index is purpose-built for this access pattern.",
      "reference": "<div class=\"not-prose flex flex-col gap-1 text-sm\"><div>🧩 <strong>Composite index</strong> — one index over multiple columns, e.g. (last_name, birth_year)</div><div>🎯 <strong>Fits</strong> — queries that filter on that exact column combination</div></div><p><strong>Why:</strong> the right index matches the actual query pattern, not just \"this column looks important on its own.\"</p>"
    },
    {
      "type": "quiz",
      "id": "hash-map-limitation-1",
      "poolId": "hash-map-limitation-pool",
      "level": 3,
      "prompt": "L3 asks you to try writing a range query against `buildHashIndex`'s `Map`. What's the fundamental problem?",
      "choices": [
        "JavaScript's Map object doesn't support the .get() method",
        "There's no way to ask a Map for \"every key between X and Y\" without iterating every entry — the same full-scan cost the index was supposed to eliminate",
        "Map objects can only hold string keys, not the email values used here",
        "Map objects are slower than plain objects for every operation"
      ],
      "correctIndex": 1,
      "explanation": "A Map's keys are scattered across hash buckets with no relationship to their sorted order — answering a range query requires checking every entry, which reintroduces the exact O(n) cost an index exists to avoid.",
      "reference": "<div class=\"not-prose flex flex-col gap-1 text-sm\"><div>#️⃣ <strong>Map.get(key)</strong> — O(1), exact match only</div><div>🚫 <strong>Map + range</strong> — no operation for \"keys between X and Y\"; only full iteration</div></div><p><strong>Why:</strong> this is the concrete, code-level version of \"hashing destroys order\" — there's no shortcut, only iterate-and-check.</p>"
    },
    {
      "type": "quiz",
      "id": "hash-map-limitation-2",
      "poolId": "hash-map-limitation-pool",
      "level": 3,
      "prompt": "Why does iterating every entry of a hash-based Map to answer a range query defeat the entire purpose of having an index?",
      "choices": [
        "It doesn't — iterating a Map is just as fast as binary search",
        "Because checking every entry one by one is exactly the O(n) full-scan cost the index existed to eliminate in the first place",
        "Because Map objects don't allow iteration at all",
        "Because range queries are rare enough that this cost never matters"
      ],
      "correctIndex": 1,
      "explanation": "The entire point of an index is avoiding an O(n) check-every-row cost. Falling back to iterating every Map entry for a range query brings that exact cost right back — the hash index provides no advantage for this kind of query.",
      "reference": "<div class=\"not-prose flex flex-col gap-1 text-sm\"><div>#️⃣ <strong>Map.get(key)</strong> — O(1), exact match only</div><div>🚫 <strong>Map + range</strong> — no operation for \"keys between X and Y\"; only full iteration</div></div><p><strong>Why:</strong> this is the concrete, code-level version of \"hashing destroys order\" — there's no shortcut, only iterate-and-check.</p>"
    },
    {
      "type": "quiz",
      "id": "batch-lookup-1",
      "poolId": "batch-lookup-pool",
      "level": 3,
      "prompt": "L3's extend-it question asks about `WHERE email IN (id1, ..., id500)` — 500 exact-match lookups in one query — versus a single lookup. Does the hash index's per-lookup O(1) advantage over the B-tree's O(log n) matter more, less, or about the same at that batch size?",
      "choices": [
        "It matters more overall, since the per-lookup gap is now paid 500 times instead of once, even though each individual gap is still small",
        "It disappears completely — batch lookups make both index types exactly equal",
        "It matters less, since batching somehow removes the need to look anything up",
        "The comparison becomes meaningless because IN queries never use indexes"
      ],
      "correctIndex": 0,
      "explanation": "Each lookup still costs roughly O(1) (hash) vs. O(log n) (B-tree) individually, so doing 500 of them multiplies whatever small per-lookup gap exists by 500 — the total absolute time difference grows, even though the *relationship* between the two costs per lookup hasn't changed.",
      "reference": "<div class=\"not-prose flex flex-col gap-1 text-sm\"><div>1️⃣ <strong>Single lookup</strong> — gap is a fraction of a millisecond, easy to ignore</div><div>🔁 <strong>500 lookups</strong> — same per-lookup gap, now paid 500 times</div></div><p><strong>Why:</strong> this is the same underlying cost difference as a single lookup — batching doesn't change the relationship, it just makes the total more visible.</p>"
    },
    {
      "type": "quiz",
      "id": "batch-lookup-2",
      "poolId": "batch-lookup-pool",
      "level": 3,
      "prompt": "If a hash index's per-lookup advantage over a B-tree is a fraction of a millisecond for one query, why would that advantage ever be worth choosing a hash index over a B-tree in practice?",
      "choices": [
        "It's never worth it — the advantage is too small to matter under any circumstance",
        "At high enough lookup volume (e.g. thousands of exact-match lookups per second in a hot path like a session cache), even a small per-lookup gap compounds into a real, measurable difference",
        "Because B-trees stop working correctly under heavy load",
        "Because hash indexes get faster the more lookups are performed"
      ],
      "correctIndex": 1,
      "explanation": "A small per-lookup difference can become significant when multiplied across high call volume — this is exactly why hash indexes are the right default for something like a session-token cache doing many exact-match lookups per second, even though the same small gap wouldn't justify the choice for an occasional query.",
      "reference": "<div class=\"not-prose flex flex-col gap-1 text-sm\"><div>1️⃣ <strong>Single lookup</strong> — gap is a fraction of a millisecond, easy to ignore</div><div>🔁 <strong>500 lookups</strong> — same per-lookup gap, now paid 500 times</div></div><p><strong>Why:</strong> this is the same underlying cost difference as a single lookup — batching doesn't change the relationship, it just makes the total more visible.</p>"
    }
  ]
}
