{
  "items": [
    {
      "id": "recursion-depth-budget",
      "level": 3,
      "title": "How deep can you recurse before the stack overflows?",
      "description": "Every recursive call costs some fixed number of bytes on the stack. Move the sliders to see how frame size and stack size trade off against how deep a recursion can safely go.",
      "params": [
        {
          "name": "frameSize",
          "label": "Bytes per stack frame",
          "min": 20,
          "max": 500,
          "step": 10,
          "default": 80,
          "unit": "B"
        },
        {
          "name": "stackLimitKB",
          "label": "Stack size limit",
          "min": 128,
          "max": 8192,
          "step": 128,
          "default": 1024,
          "unit": "KB"
        }
      ],
      "compute": "const stackLimitBytes = params.stackLimitKB * 1024;\nconst maxDepth = Math.floor(stackLimitBytes / params.frameSize);\nreturn { maxDepth };",
      "outputs": [
        {
          "key": "maxDepth",
          "label": "Max recursion depth before overflow",
          "unit": "",
          "color": "#38bdf8"
        }
      ],
      "chartParam": "frameSize"
    }
  ]
}
