{
  "items": [
    {
      "type": "quiz",
      "id": "isolation-1",
      "poolId": "isolation",
      "level": 1,
      "prompt": "Two processes both have a pointer with the same value, 0x7fff1000. Why doesn't reading through it in process A ever accidentally return data from process B?",
      "choices": [
        "The OS randomizes pointer values so they never collide",
        "Each process has its own page table \u2014 the same virtual address maps to a different (or no) physical location per process",
        "Processes share physical memory directly, but the CPU blocks cross-process reads in hardware",
        "It's mostly coincidence \u2014 occasionally it does leak data between processes"
      ],
      "correctIndex": 1,
      "explanation": "Addresses are never physical from a process's point of view. The MMU translates every virtual address through that specific process's page table, so identical virtual addresses in two processes almost always resolve to entirely different physical memory \u2014 often none at all.",
      "reference": "<div class='grid grid-cols-2 gap-2 text-xs'><div class=\"rounded border border-slate-300 p-1.5 dark:border-slate-600\"><p class='font-semibold text-slate-500'>\ud83c\udd70\ufe0f Process A</p>page table A</div><div class=\"rounded border border-sky-300 bg-sky-50 p-1.5 dark:border-sky-700 dark:bg-sky-950\"><p class='font-semibold text-sky-600'>\ud83c\udd71\ufe0f Process B</p>page table B</div></div><p class='mt-2 text-xs text-slate-500 dark:text-slate-400'><strong>Why:</strong> the same virtual address in two processes is translated through two independent page tables.</p>",
      "learnMore": "<h5 class='mt-0 mb-2 text-sm font-semibold text-slate-700 dark:text-slate-200'>Virtual addresses aren't physical</h5><div class='text-sm text-slate-600 dark:text-slate-300 space-y-2'><p>Every pointer a process holds is a <em>virtual</em> address \u2014 a number that only means something once it passes through that process's own page table. The MMU (memory management unit) does this translation on every single memory access, not just occasionally.</p><p>When two processes both hold the value <code>0x7fff1000</code>, that's a coincidence of numbering, not a shared location. Process A's page table maps it to one physical page; process B's page table maps it to a completely different physical page \u2014 or to nothing at all. Reading through a pointer never bypasses this translation, so there is no path by which A's data could show up when B reads 'the same' address.</p><p>This is also why concepts like shared memory (e.g. POSIX shared memory segments) have to be requested <em>explicitly</em> by both processes \u2014 the kernel has to deliberately map the same physical pages into both page tables. Isolation is the default; sharing is the opt-in.</p></div>"
    },
    {
      "type": "quiz",
      "id": "isolation-2",
      "poolId": "isolation",
      "level": 1,
      "prompt": "Process X writes the string \"secret\" to virtual address 0x5000. Process Y, running at the same time, reads virtual address 0x5000. What does Y most likely get?",
      "choices": [
        "\"secret\" \u2014 both processes are reading the same address",
        "Whatever Y's own page table maps 0x5000 to, unrelated to X's write \u2014 very likely not \"secret\"",
        "A permission error every single time, with no exceptions",
        "Half of X's data, mixed with Y's own memory at that address"
      ],
      "correctIndex": 1,
      "explanation": "0x5000 is only a number until it passes through a page table. X's page table and Y's page table almost certainly map 0x5000 to different physical pages (or Y's mapping may not even exist), so Y's read has no relationship to what X wrote.",
      "reference": "<div class='grid grid-cols-2 gap-2 text-xs'><div class=\"rounded border border-slate-300 p-1.5 dark:border-slate-600\"><p class='font-semibold text-slate-500'>\ud83c\udd70\ufe0f Process A</p>page table A</div><div class=\"rounded border border-sky-300 bg-sky-50 p-1.5 dark:border-sky-700 dark:bg-sky-950\"><p class='font-semibold text-sky-600'>\ud83c\udd71\ufe0f Process B</p>page table B</div></div><p class='mt-2 text-xs text-slate-500 dark:text-slate-400'><strong>Why:</strong> the same virtual address in two processes is translated through two independent page tables.</p>",
      "learnMore": "<h5 class='mt-0 mb-2 text-sm font-semibold text-slate-700 dark:text-slate-200'>Virtual addresses aren't physical</h5><div class='text-sm text-slate-600 dark:text-slate-300 space-y-2'><p>Every pointer a process holds is a <em>virtual</em> address \u2014 a number that only means something once it passes through that process's own page table. The MMU (memory management unit) does this translation on every single memory access, not just occasionally.</p><p>When two processes both hold the value <code>0x7fff1000</code>, that's a coincidence of numbering, not a shared location. Process A's page table maps it to one physical page; process B's page table maps it to a completely different physical page \u2014 or to nothing at all. Reading through a pointer never bypasses this translation, so there is no path by which A's data could show up when B reads 'the same' address.</p><p>This is also why concepts like shared memory (e.g. POSIX shared memory segments) have to be requested <em>explicitly</em> by both processes \u2014 the kernel has to deliberately map the same physical pages into both page tables. Isolation is the default; sharing is the opt-in.</p></div>"
    },
    {
      "type": "quiz",
      "id": "isolation-3",
      "poolId": "isolation",
      "level": 1,
      "prompt": "Two browser tabs run the identical script. Tab A sets `counter = 999`. Tab B reads `counter` and gets `0`. What's the most precise explanation?",
      "choices": [
        "Tab B's script has a typo",
        "Tabs run as separate processes (or process-like isolates), each with its own private memory \u2014 there was never one shared `counter` to begin with",
        "The browser deliberately hides changes from other tabs for security theater, not a real technical reason",
        "JavaScript variables are always read-only across script re-runs"
      ],
      "correctIndex": 1,
      "explanation": "Each tab's `counter` lives in that tab's own process memory. \"Setting it in tab A\" and \"reading it in tab B\" were never operating on the same storage \u2014 this is process isolation, the same mechanism that keeps any two unrelated processes from stepping on each other's memory.",
      "reference": "<div class='grid grid-cols-2 gap-2 text-xs'><div class=\"rounded border border-slate-300 p-1.5 dark:border-slate-600\"><p class='font-semibold text-slate-500'>\ud83c\udd70\ufe0f Process A</p>page table A</div><div class=\"rounded border border-sky-300 bg-sky-50 p-1.5 dark:border-sky-700 dark:bg-sky-950\"><p class='font-semibold text-sky-600'>\ud83c\udd71\ufe0f Process B</p>page table B</div></div><p class='mt-2 text-xs text-slate-500 dark:text-slate-400'><strong>Why:</strong> the same virtual address in two processes is translated through two independent page tables.</p>",
      "learnMore": "<h5 class='mt-0 mb-2 text-sm font-semibold text-slate-700 dark:text-slate-200'>Virtual addresses aren't physical</h5><div class='text-sm text-slate-600 dark:text-slate-300 space-y-2'><p>Every pointer a process holds is a <em>virtual</em> address \u2014 a number that only means something once it passes through that process's own page table. The MMU (memory management unit) does this translation on every single memory access, not just occasionally.</p><p>When two processes both hold the value <code>0x7fff1000</code>, that's a coincidence of numbering, not a shared location. Process A's page table maps it to one physical page; process B's page table maps it to a completely different physical page \u2014 or to nothing at all. Reading through a pointer never bypasses this translation, so there is no path by which A's data could show up when B reads 'the same' address.</p><p>This is also why concepts like shared memory (e.g. POSIX shared memory segments) have to be requested <em>explicitly</em> by both processes \u2014 the kernel has to deliberately map the same physical pages into both page tables. Isolation is the default; sharing is the opt-in.</p></div>"
    },
    {
      "type": "quiz",
      "id": "null-deref-1",
      "poolId": "null-deref",
      "level": 1,
      "prompt": "A program dereferences a NULL pointer and crashes immediately instead of silently reading garbage data. Why?",
      "choices": [
        "NULL is a special CPU instruction that always crashes",
        "Address 0x0 is deliberately left unmapped in the process's page table, so any access to it faults",
        "The compiler inserts a runtime check before every pointer dereference",
        "RAM physically has no data at address 0, so the CPU errors automatically"
      ],
      "correctIndex": 1,
      "explanation": "Leaving 0x0 unmapped is a deliberate OS/loader decision, not a hardware property of RAM \u2014 it turns a whole class of bugs (using a pointer before it's set to something real) into an immediate, loud fault instead of a silent read of unrelated memory.",
      "reference": "<div class='rounded border border-rose-300 bg-rose-50 p-2 text-xs dark:border-rose-800 dark:bg-rose-950'><code>0x0</code> \u2192 <strong>unmapped</strong> \u2192 immediate fault</div><p class='mt-2 text-xs text-slate-500 dark:text-slate-400'><strong>Why:</strong> leaving address 0 unmapped turns a whole class of bugs into a loud, immediate crash instead of a silent bad read.</p>",
      "learnMore": "<h5 class='mt-0 mb-2 text-sm font-semibold text-slate-700 dark:text-slate-200'>Why NULL crashes instead of returning garbage</h5><div class='text-sm text-slate-600 dark:text-slate-300 space-y-2'><p>A pointer that was never assigned a real value is conventionally set to <code>NULL</code> (address <code>0</code>). The OS loader deliberately leaves address <code>0</code> unmapped in every process's page table \u2014 no valid physical page backs it, on purpose.</p><p>So when code dereferences a NULL pointer, the MMU tries to translate address <code>0</code>, fails to find a mapping, and raises a page fault. The kernel sees there's no legitimate reason for this access and delivers <code>SIGSEGV</code>, killing the process (or, in a managed runtime, raising a catchable null-reference error instead).</p><p>This is a deliberate design choice, not a law of physics \u2014 a different OS could in principle map something at address 0 and let you silently read whatever's there. Unix chose to leave it unmapped specifically because a loud crash at the exact moment of the bad access is far easier to debug than data silently corrupted somewhere downstream.</p></div>"
    },
    {
      "type": "quiz",
      "id": "null-deref-2",
      "poolId": "null-deref",
      "level": 1,
      "prompt": "If the OS instead mapped a real, readable page at address 0x0 (rather than leaving it unmapped), what would change about NULL-pointer bugs?",
      "choices": [
        "Nothing \u2014 NULL pointer bugs would still crash exactly the same way",
        "They'd become silent \u2014 the program would read whatever happens to be at that page instead of crashing, making the bug much harder to notice",
        "The program would run faster, since fewer faults occur",
        "It's not possible for an OS to ever map address 0x0"
      ],
      "correctIndex": 1,
      "explanation": "The crash isn't inherent to the number 0 \u2014 it's inherent to that address being unmapped. If it were mapped, a NULL dereference would just read whatever's on that page and keep running with corrupted-looking data, which is strictly worse for debugging than an immediate fault.",
      "reference": "<div class='rounded border border-rose-300 bg-rose-50 p-2 text-xs dark:border-rose-800 dark:bg-rose-950'><code>0x0</code> \u2192 <strong>unmapped</strong> \u2192 immediate fault</div><p class='mt-2 text-xs text-slate-500 dark:text-slate-400'><strong>Why:</strong> leaving address 0 unmapped turns a whole class of bugs into a loud, immediate crash instead of a silent bad read.</p>",
      "learnMore": "<h5 class='mt-0 mb-2 text-sm font-semibold text-slate-700 dark:text-slate-200'>Why NULL crashes instead of returning garbage</h5><div class='text-sm text-slate-600 dark:text-slate-300 space-y-2'><p>A pointer that was never assigned a real value is conventionally set to <code>NULL</code> (address <code>0</code>). The OS loader deliberately leaves address <code>0</code> unmapped in every process's page table \u2014 no valid physical page backs it, on purpose.</p><p>So when code dereferences a NULL pointer, the MMU tries to translate address <code>0</code>, fails to find a mapping, and raises a page fault. The kernel sees there's no legitimate reason for this access and delivers <code>SIGSEGV</code>, killing the process (or, in a managed runtime, raising a catchable null-reference error instead).</p><p>This is a deliberate design choice, not a law of physics \u2014 a different OS could in principle map something at address 0 and let you silently read whatever's there. Unix chose to leave it unmapped specifically because a loud crash at the exact moment of the bad access is far easier to debug than data silently corrupted somewhere downstream.</p></div>"
    },
    {
      "type": "quiz",
      "id": "regions-1",
      "poolId": "address-space-regions",
      "level": 1,
      "prompt": "Which region of a process's address space holds the compiled machine code, and why is it typically marked read-only?",
      "choices": [
        "The heap \u2014 code is just data allocated at startup",
        "The text segment \u2014 read-only so a bug can't accidentally overwrite the program's own instructions",
        "The stack \u2014 code lives alongside the current call frame",
        "The data/bss segment, because it's loaded first"
      ],
      "correctIndex": 1,
      "explanation": "The text segment holds the compiled instructions. Marking it read-only means a stray write (a buffer overrun, a bad pointer) can corrupt data or crash the process, but it can't rewrite the program's own logic on the fly.",
      "reference": "<div class='not-prose flex flex-col gap-1 text-xs'><div>\u2b06\ufe0f <strong>stack</strong> \u2014 frames, grows down</div><div>\u2195\ufe0f <strong>heap</strong> \u2014 dynamic alloc, grows up</div><div>\ud83d\udce6 <strong>data/bss</strong> \u2014 globals</div><div>\ud83d\udd12 <strong>text</strong> \u2014 code, read-only</div></div><p class='mt-2 text-xs text-slate-500 dark:text-slate-400'><strong>Why:</strong> each region has a distinct purpose and distinct permissions \u2014 text is read-only precisely because code shouldn't be writable at runtime.</p>",
      "learnMore": "<h5 class='mt-0 mb-2 text-sm font-semibold text-slate-700 dark:text-slate-200'>The four regions and why they're separate</h5><div class='text-sm text-slate-600 dark:text-slate-300 space-y-2'><p>A process's address space isn't one undifferentiated block \u2014 it's split into regions because each kind of data has different lifetime and permission needs.</p><p><strong>text</strong> holds compiled instructions and is mapped read-only (and often execute-only) so a bug can't accidentally overwrite the program's own code. <strong>data/bss</strong> holds globals and statics \u2014 fixed in number, known at compile/link time. <strong>heap</strong> holds anything allocated at runtime (<code>malloc</code>, <code>new</code>, objects) and can grow or shrink as the program requests more or frees it. <strong>stack</strong> holds function call frames \u2014 return addresses, parameters, locals \u2014 and grows and shrinks automatically as functions call and return.</p><p>Heap and stack are placed at opposite ends of the same free region specifically so neither one has to be sized up front \u2014 they only collide if one of them grows unexpectedly large, which is exactly the stack-overflow / heap-exhaustion failure mode.</p></div>"
    },
    {
      "type": "quiz",
      "id": "regions-2",
      "poolId": "address-space-regions",
      "level": 1,
      "prompt": "A function allocates an object with `new` and another calls itself recursively 1000 times. Which two regions grow to accommodate these, respectively?",
      "choices": [
        "Both grow the text segment",
        "The heap grows for the `new` allocation; the stack grows for the recursive calls",
        "The stack grows for the `new` allocation; the heap grows for the recursive calls",
        "Both grow the data/bss segment"
      ],
      "correctIndex": 1,
      "explanation": "Dynamic allocation (`new`, `malloc`) always comes from the heap. Function calls \u2014 including recursive ones \u2014 push frames onto the stack, one per call, regardless of what the function does internally.",
      "reference": "<div class='not-prose flex flex-col gap-1 text-xs'><div>\u2b06\ufe0f <strong>stack</strong> \u2014 frames, grows down</div><div>\u2195\ufe0f <strong>heap</strong> \u2014 dynamic alloc, grows up</div><div>\ud83d\udce6 <strong>data/bss</strong> \u2014 globals</div><div>\ud83d\udd12 <strong>text</strong> \u2014 code, read-only</div></div><p class='mt-2 text-xs text-slate-500 dark:text-slate-400'><strong>Why:</strong> each region has a distinct purpose and distinct permissions \u2014 text is read-only precisely because code shouldn't be writable at runtime.</p>",
      "learnMore": "<h5 class='mt-0 mb-2 text-sm font-semibold text-slate-700 dark:text-slate-200'>The four regions and why they're separate</h5><div class='text-sm text-slate-600 dark:text-slate-300 space-y-2'><p>A process's address space isn't one undifferentiated block \u2014 it's split into regions because each kind of data has different lifetime and permission needs.</p><p><strong>text</strong> holds compiled instructions and is mapped read-only (and often execute-only) so a bug can't accidentally overwrite the program's own code. <strong>data/bss</strong> holds globals and statics \u2014 fixed in number, known at compile/link time. <strong>heap</strong> holds anything allocated at runtime (<code>malloc</code>, <code>new</code>, objects) and can grow or shrink as the program requests more or frees it. <strong>stack</strong> holds function call frames \u2014 return addresses, parameters, locals \u2014 and grows and shrinks automatically as functions call and return.</p><p>Heap and stack are placed at opposite ends of the same free region specifically so neither one has to be sized up front \u2014 they only collide if one of them grows unexpectedly large, which is exactly the stack-overflow / heap-exhaustion failure mode.</p></div>"
    },
    {
      "type": "quiz",
      "id": "regions-3",
      "poolId": "address-space-regions",
      "level": 1,
      "prompt": "Why are the stack and heap conventionally placed at opposite ends of the same free memory region, growing toward each other?",
      "choices": [
        "It's an arbitrary convention with no technical reason",
        "So neither one needs a fixed size chosen up front \u2014 each can grow as needed until they'd eventually collide",
        "To make debugging harder by keeping their addresses close",
        "Because the CPU requires stack and heap to be adjacent"
      ],
      "correctIndex": 1,
      "explanation": "Placing them at opposite ends of shared free space means neither has to be pre-sized \u2014 both can grow independently for as long as there's room. It also means their eventual collision (or the stack simply outrunning its reserved space) is exactly what a stack overflow is.",
      "reference": "<div class='not-prose flex flex-col gap-1 text-xs'><div>\u2b06\ufe0f <strong>stack</strong> \u2014 frames, grows down</div><div>\u2195\ufe0f <strong>heap</strong> \u2014 dynamic alloc, grows up</div><div>\ud83d\udce6 <strong>data/bss</strong> \u2014 globals</div><div>\ud83d\udd12 <strong>text</strong> \u2014 code, read-only</div></div><p class='mt-2 text-xs text-slate-500 dark:text-slate-400'><strong>Why:</strong> each region has a distinct purpose and distinct permissions \u2014 text is read-only precisely because code shouldn't be writable at runtime.</p>",
      "learnMore": "<h5 class='mt-0 mb-2 text-sm font-semibold text-slate-700 dark:text-slate-200'>The four regions and why they're separate</h5><div class='text-sm text-slate-600 dark:text-slate-300 space-y-2'><p>A process's address space isn't one undifferentiated block \u2014 it's split into regions because each kind of data has different lifetime and permission needs.</p><p><strong>text</strong> holds compiled instructions and is mapped read-only (and often execute-only) so a bug can't accidentally overwrite the program's own code. <strong>data/bss</strong> holds globals and statics \u2014 fixed in number, known at compile/link time. <strong>heap</strong> holds anything allocated at runtime (<code>malloc</code>, <code>new</code>, objects) and can grow or shrink as the program requests more or frees it. <strong>stack</strong> holds function call frames \u2014 return addresses, parameters, locals \u2014 and grows and shrinks automatically as functions call and return.</p><p>Heap and stack are placed at opposite ends of the same free region specifically so neither one has to be sized up front \u2014 they only collide if one of them grows unexpectedly large, which is exactly the stack-overflow / heap-exhaustion failure mode.</p></div>"
    },
    {
      "type": "quiz",
      "id": "pid-1",
      "poolId": "pid-uniqueness",
      "level": 1,
      "prompt": "You run the same script twice in two separate terminal windows. What's true of the two resulting processes?",
      "choices": [
        "They share one PID since it's the same program",
        "They get distinct PIDs and separate address spaces, even though they're running identical code",
        "The second launch fails because the program is already running",
        "They share memory so any variable change in one is visible in the other"
      ],
      "correctIndex": 1,
      "explanation": "A PID identifies a running instance, not a program. Two launches of the same script are two independent processes with two PIDs and two private address spaces \u2014 running the same code doesn't mean sharing anything at runtime.",
      "reference": "<div class='not-prose text-xs'>Every <code>fork()</code> \u2192 a brand-new, never-reused (until wraparound) PID</div><p class='mt-2 text-xs text-slate-500 dark:text-slate-400'><strong>Why:</strong> the PID is the OS's unique handle for addressing a specific running process, distinct from any other process ever.</p>",
      "learnMore": "<h5 class='mt-0 mb-2 text-sm font-semibold text-slate-700 dark:text-slate-200'>PIDs identify processes, not programs</h5><div class='text-sm text-slate-600 dark:text-slate-300 space-y-2'><p>A PID (process ID) names one specific running instance, not the program it's running. Launch the same executable five times and you get five different processes, five different PIDs, five separate address spaces \u2014 all sharing the same code on disk but none of the same memory.</p><p>The OS uses the PID as the key for everything process-related: sending a signal (<code>kill -9 &lt;pid&gt;</code>), reading its status (<code>/proc/&lt;pid&gt;/...</code> on Linux), or waiting for it to exit (<code>waitpid(pid, ...)</code>). Without a stable per-instance identifier, none of these operations could target a specific running process instead of \"some process running this program.\"</p></div>"
    },
    {
      "type": "quiz",
      "id": "pid-2",
      "poolId": "pid-uniqueness",
      "level": 1,
      "prompt": "Why does the OS need a PID at all, instead of just letting tools refer to processes by their program name (e.g. \"node\")?",
      "choices": [
        "It doesn't need one \u2014 program name is always sufficient",
        "Multiple processes can run the same program simultaneously; a PID is needed to target one specific running instance for operations like sending a signal",
        "PIDs exist only for logging, not for any functional purpose",
        "PIDs are required by the filesystem to name files"
      ],
      "correctIndex": 1,
      "explanation": "Program name is ambiguous the moment more than one instance is running. A PID disambiguates: `kill -9 61278` targets exactly one process, where `kill -9 node` wouldn't even be a valid way to express \"the second one I started\".",
      "reference": "<div class='not-prose text-xs'>Every <code>fork()</code> \u2192 a brand-new, never-reused (until wraparound) PID</div><p class='mt-2 text-xs text-slate-500 dark:text-slate-400'><strong>Why:</strong> the PID is the OS's unique handle for addressing a specific running process, distinct from any other process ever.</p>",
      "learnMore": "<h5 class='mt-0 mb-2 text-sm font-semibold text-slate-700 dark:text-slate-200'>PIDs identify processes, not programs</h5><div class='text-sm text-slate-600 dark:text-slate-300 space-y-2'><p>A PID (process ID) names one specific running instance, not the program it's running. Launch the same executable five times and you get five different processes, five different PIDs, five separate address spaces \u2014 all sharing the same code on disk but none of the same memory.</p><p>The OS uses the PID as the key for everything process-related: sending a signal (<code>kill -9 &lt;pid&gt;</code>), reading its status (<code>/proc/&lt;pid&gt;/...</code> on Linux), or waiting for it to exit (<code>waitpid(pid, ...)</code>). Without a stable per-instance identifier, none of these operations could target a specific running process instead of \"some process running this program.\"</p></div>"
    },
    {
      "type": "quiz",
      "id": "vm-1",
      "poolId": "virtual-memory-belief",
      "level": 1,
      "prompt": "What does it mean, precisely, that a process \"believes it owns the entire address space\"?",
      "choices": [
        "The process actually does own all of physical RAM while it runs",
        "The process's view of memory is a private illusion maintained by the OS/MMU \u2014 its virtual addresses have no necessary relationship to what any other process sees",
        "It means the process can read any other process's memory if it tries hard enough",
        "It's a marketing term with no real technical meaning"
      ],
      "correctIndex": 1,
      "explanation": "Virtual memory gives every process the same full private address range to program against, regardless of how many other processes exist or how much physical RAM is actually installed \u2014 the OS/MMU handle translating that illusion down to real, shared physical memory behind the scenes.",
      "reference": "<div class='not-prose text-xs'>Every process 'believes' it owns the full address space \u2014 the OS/MMU maintains that illusion per process.</div><p class='mt-2 text-xs text-slate-500 dark:text-slate-400'><strong>Why:</strong> the illusion of a full private address space is what lets every process be written as if it's alone on the machine.</p>",
      "learnMore": "<h5 class='mt-0 mb-2 text-sm font-semibold text-slate-700 dark:text-slate-200'>The virtual memory illusion</h5><div class='text-sm text-slate-600 dark:text-slate-300 space-y-2'><p>Without virtual memory, every program would need to know, at compile time, exactly which physical addresses were free \u2014 and no two programs running together could ever pick the same address for anything. That's unworkable at scale.</p><p>Virtual memory solves this by giving each process its own full, private view of an address space (e.g. the full 48-bit range on a 64-bit system), regardless of how much physical RAM actually exists or what other processes are doing. The MMU and page tables silently translate each process's virtual addresses to wherever physical RAM the OS actually has available \u2014 different for every process, invisible to the program itself.</p><p>This is also what makes physical RAM oversubscription and swapping possible: a page marked in a process's page table doesn't even have to be backed by RAM at every moment \u2014 it could be on disk, brought in only when actually touched.</p></div>"
    },
    {
      "type": "quiz",
      "id": "vm-2",
      "poolId": "virtual-memory-belief",
      "level": 1,
      "prompt": "A machine has 8GB of physical RAM but is running 20 processes, each with its own large virtual address space. How is this possible?",
      "choices": [
        "It isn't \u2014 the OS would refuse to start more processes than RAM can fit",
        "Virtual address space is nearly free to reserve; only the pages a process actually touches get backed by real physical RAM",
        "Each process is secretly given only 400MB of virtual address space",
        "The processes silently share one physical address space with no isolation"
      ],
      "correctIndex": 1,
      "explanation": "Reserving virtual address space costs almost nothing \u2014 it's bookkeeping in a page table. Physical RAM is only consumed for pages a process actually reads or writes, which is why many processes can each declare a large address space without the machine running out of memory immediately.",
      "reference": "<div class='not-prose text-xs'>Every process 'believes' it owns the full address space \u2014 the OS/MMU maintains that illusion per process.</div><p class='mt-2 text-xs text-slate-500 dark:text-slate-400'><strong>Why:</strong> the illusion of a full private address space is what lets every process be written as if it's alone on the machine.</p>",
      "learnMore": "<h5 class='mt-0 mb-2 text-sm font-semibold text-slate-700 dark:text-slate-200'>The virtual memory illusion</h5><div class='text-sm text-slate-600 dark:text-slate-300 space-y-2'><p>Without virtual memory, every program would need to know, at compile time, exactly which physical addresses were free \u2014 and no two programs running together could ever pick the same address for anything. That's unworkable at scale.</p><p>Virtual memory solves this by giving each process its own full, private view of an address space (e.g. the full 48-bit range on a 64-bit system), regardless of how much physical RAM actually exists or what other processes are doing. The MMU and page tables silently translate each process's virtual addresses to wherever physical RAM the OS actually has available \u2014 different for every process, invisible to the program itself.</p><p>This is also what makes physical RAM oversubscription and swapping possible: a page marked in a process's page table doesn't even have to be backed by RAM at every moment \u2014 it could be on disk, brought in only when actually touched.</p></div>"
    },
    {
      "type": "quiz",
      "id": "kmode-1",
      "poolId": "kernel-vs-user-mode",
      "level": 1,
      "prompt": "Why can't an ordinary process just directly read or write another process's memory, if it wanted to?",
      "choices": [
        "It's only prevented by a polite convention that well-behaved programs follow",
        "Hardware access and cross-process memory access require kernel mode; ordinary process code runs in a restricted user mode and must go through system calls the kernel controls",
        "The CPU has no concept of privilege levels \u2014 this is purely an OS-level software check",
        "It actually can, but antivirus software usually blocks it"
      ],
      "correctIndex": 1,
      "explanation": "The restriction is enforced by the CPU's privilege levels, not just convention: user-mode code physically cannot execute the instructions needed to touch arbitrary hardware or another process's memory. Every such operation must go through a system call into the trusted kernel, which can and does refuse illegitimate requests.",
      "reference": "<div class='not-prose grid grid-cols-2 gap-2 text-xs'><div class=\"rounded border border-slate-300 p-1.5 dark:border-slate-600\">\ud83d\udc64 <strong>User mode</strong><br/>your code, restricted</div><div class=\"rounded border border-amber-300 bg-amber-50 p-1.5 dark:border-amber-700 dark:bg-amber-950\">\ud83d\udee1\ufe0f <strong>Kernel mode</strong><br/>full hardware access</div></div><p class='mt-2 text-xs text-slate-500 dark:text-slate-400'><strong>Why:</strong> a system call is the only sanctioned door between the two \u2014 it's what lets untrusted code request privileged work without granting it direct access.</p>",
      "learnMore": "<h5 class='mt-0 mb-2 text-sm font-semibold text-slate-700 dark:text-slate-200'>Why user code can't touch hardware directly</h5><div class='text-sm text-slate-600 dark:text-slate-300 space-y-2'><p>If every process could directly manipulate hardware (write raw disk sectors, reprogram the network card, touch another process's memory), no OS-level guarantee \u2014 isolation, permissions, scheduling fairness \u2014 could be enforced. Any process could simply route around them.</p><p>Instead, the CPU itself supports privilege levels. Ordinary application code runs in <strong>user mode</strong>, where certain instructions and memory regions are off-limits. To do anything that touches shared hardware or global state \u2014 open a file, allocate memory, send a network packet \u2014 user code issues a <strong>system call</strong>, a well-defined request that traps into <strong>kernel mode</strong>, where the OS itself (fully trusted, unrestricted) performs the operation on the process's behalf and returns control.</p><p>This is the actual enforcement mechanism behind \"a process can't read another process's memory\" \u2014 it's not merely convention, it's that the only doors to privileged operations are ones the kernel controls and can refuse.</p></div>"
    },
    {
      "type": "quiz",
      "id": "kmode-2",
      "poolId": "kernel-vs-user-mode",
      "level": 1,
      "prompt": "A program calls a function to read a file. What has to happen for that read to actually reach the disk?",
      "choices": [
        "The user-mode code reads the disk controller registers directly",
        "The user-mode code issues a system call, trapping into kernel mode, where the OS performs the privileged disk operation and returns the result",
        "Nothing special \u2014 file reads never involve the kernel",
        "The program must first request root/administrator access from the user"
      ],
      "correctIndex": 1,
      "explanation": "Any interaction with real hardware \u2014 including disk reads \u2014 is a privileged operation. The user-mode program can't perform it directly; it has to request it via a system call, which is the sanctioned crossing point into kernel mode where the OS does the actual work.",
      "reference": "<div class='not-prose grid grid-cols-2 gap-2 text-xs'><div class=\"rounded border border-slate-300 p-1.5 dark:border-slate-600\">\ud83d\udc64 <strong>User mode</strong><br/>your code, restricted</div><div class=\"rounded border border-amber-300 bg-amber-50 p-1.5 dark:border-amber-700 dark:bg-amber-950\">\ud83d\udee1\ufe0f <strong>Kernel mode</strong><br/>full hardware access</div></div><p class='mt-2 text-xs text-slate-500 dark:text-slate-400'><strong>Why:</strong> a system call is the only sanctioned door between the two \u2014 it's what lets untrusted code request privileged work without granting it direct access.</p>",
      "learnMore": "<h5 class='mt-0 mb-2 text-sm font-semibold text-slate-700 dark:text-slate-200'>Why user code can't touch hardware directly</h5><div class='text-sm text-slate-600 dark:text-slate-300 space-y-2'><p>If every process could directly manipulate hardware (write raw disk sectors, reprogram the network card, touch another process's memory), no OS-level guarantee \u2014 isolation, permissions, scheduling fairness \u2014 could be enforced. Any process could simply route around them.</p><p>Instead, the CPU itself supports privilege levels. Ordinary application code runs in <strong>user mode</strong>, where certain instructions and memory regions are off-limits. To do anything that touches shared hardware or global state \u2014 open a file, allocate memory, send a network packet \u2014 user code issues a <strong>system call</strong>, a well-defined request that traps into <strong>kernel mode</strong>, where the OS itself (fully trusted, unrestricted) performs the operation on the process's behalf and returns control.</p><p>This is the actual enforcement mechanism behind \"a process can't read another process's memory\" \u2014 it's not merely convention, it's that the only doors to privileged operations are ones the kernel controls and can refuse.</p></div>"
    },
    {
      "type": "quiz",
      "id": "fork-exec-split-1",
      "poolId": "fork-exec-split",
      "level": 2,
      "prompt": "Why does Unix process creation split into two separate steps \u2014 fork() then exec() \u2014 instead of one combined \"start_process()\" call?",
      "choices": [
        "Historical accident with no real benefit today",
        "Splitting them lets code run in the child (e.g. redirecting file descriptors) after duplication but before the new program's code takes over",
        "fork() is faster than exec(), so splitting them improves performance",
        "exec() requires superuser permissions and fork() doesn't"
      ],
      "correctIndex": 1,
      "explanation": "The gap between fork() and exec() is exactly where a shell sets up things like output redirection (> file, | pipe) \u2014 code the shell controls runs there, in the child, before the child's identity is replaced by the new program.",
      "reference": "<div class='not-prose text-xs space-y-1'><div>1\ufe0f\u20e3 <code>fork()</code> \u2014 duplicate</div><div>2\ufe0f\u20e3 (redirect fds, etc \u2014 only in child)</div><div>3\ufe0f\u20e3 <code>exec()</code> \u2014 replace</div></div><p class='mt-2 text-xs text-slate-500 dark:text-slate-400'><strong>Why:</strong> splitting the two steps leaves a window where the child can be modified before its identity is replaced.</p>",
      "learnMore": "<h5 class='mt-0 mb-2 text-sm font-semibold text-slate-700 dark:text-slate-200'>Why fork and exec are two calls, not one</h5><div class='text-sm text-slate-600 dark:text-slate-300 space-y-2'><p>A combined \"start_process(path)\" call would have no way to let the caller customize the child's environment before the new program takes over \u2014 file descriptor redirection, changing the working directory, dropping privileges, all of it would have to be built into that one call's API, or be impossible.</p><p>By splitting fork (duplicate) from exec (replace), the code that runs between them \u2014 still the old program, still able to call ordinary functions \u2014 has a window to modify only the <em>child's</em> state: closing/reopening file descriptors, changing environment variables, chrooting, before calling exec to become the new program. This is exactly how a shell implements <code>&gt;</code>, <code>&lt;</code>, and <code>|</code> \u2014 none of it needs special-casing inside exec itself.</p><p>`fork()` without `exec()` is also independently useful \u2014 some servers fork to handle a request in a child running the same code, no new program involved at all.</p></div>"
    },
    {
      "type": "quiz",
      "id": "fork-exec-split-2",
      "poolId": "fork-exec-split",
      "level": 2,
      "prompt": "A shell runs `node app.js > out.log`. At what point does the redirection to `out.log` actually get set up?",
      "choices": [
        "Before fork() is called, in the shell's own process",
        "In the child, after fork() returns 0 but before exec() replaces the child's program",
        "Inside exec() itself, as a built-in feature of every program",
        "After exec() completes, by the newly-running node process"
      ],
      "correctIndex": 1,
      "explanation": "The redirection has to happen to the child only, and it has to happen before the child becomes a different program \u2014 which is precisely the window between fork() returning in the child and exec() being called. Doing it before fork() would also redirect the shell's own output.",
      "reference": "<div class='not-prose text-xs space-y-1'><div>1\ufe0f\u20e3 <code>fork()</code> \u2014 duplicate</div><div>2\ufe0f\u20e3 (redirect fds, etc \u2014 only in child)</div><div>3\ufe0f\u20e3 <code>exec()</code> \u2014 replace</div></div><p class='mt-2 text-xs text-slate-500 dark:text-slate-400'><strong>Why:</strong> splitting the two steps leaves a window where the child can be modified before its identity is replaced.</p>",
      "learnMore": "<h5 class='mt-0 mb-2 text-sm font-semibold text-slate-700 dark:text-slate-200'>Why fork and exec are two calls, not one</h5><div class='text-sm text-slate-600 dark:text-slate-300 space-y-2'><p>A combined \"start_process(path)\" call would have no way to let the caller customize the child's environment before the new program takes over \u2014 file descriptor redirection, changing the working directory, dropping privileges, all of it would have to be built into that one call's API, or be impossible.</p><p>By splitting fork (duplicate) from exec (replace), the code that runs between them \u2014 still the old program, still able to call ordinary functions \u2014 has a window to modify only the <em>child's</em> state: closing/reopening file descriptors, changing environment variables, chrooting, before calling exec to become the new program. This is exactly how a shell implements <code>&gt;</code>, <code>&lt;</code>, and <code>|</code> \u2014 none of it needs special-casing inside exec itself.</p><p>`fork()` without `exec()` is also independently useful \u2014 some servers fork to handle a request in a child running the same code, no new program involved at all.</p></div>"
    },
    {
      "type": "quiz",
      "id": "fork-exec-split-3",
      "poolId": "fork-exec-split",
      "level": 2,
      "prompt": "What is `fork()` useful for on its own, without ever calling `exec()` afterward?",
      "choices": [
        "Nothing \u2014 fork() only ever makes sense immediately before exec()",
        "A process can fork to run a copy of its own code in a child (e.g. a server handling one request per forked child), with no new program involved",
        "fork() without exec() always crashes the child",
        "fork() automatically calls exec() internally if you don't do it yourself"
      ],
      "correctIndex": 1,
      "explanation": "fork() alone duplicates the calling process, including its code \u2014 useful any time you want a fresh, isolated copy running the same program, such as a classic \"fork a worker per connection\" server design, with no new executable being loaded at all.",
      "reference": "<div class='not-prose text-xs space-y-1'><div>1\ufe0f\u20e3 <code>fork()</code> \u2014 duplicate</div><div>2\ufe0f\u20e3 (redirect fds, etc \u2014 only in child)</div><div>3\ufe0f\u20e3 <code>exec()</code> \u2014 replace</div></div><p class='mt-2 text-xs text-slate-500 dark:text-slate-400'><strong>Why:</strong> splitting the two steps leaves a window where the child can be modified before its identity is replaced.</p>",
      "learnMore": "<h5 class='mt-0 mb-2 text-sm font-semibold text-slate-700 dark:text-slate-200'>Why fork and exec are two calls, not one</h5><div class='text-sm text-slate-600 dark:text-slate-300 space-y-2'><p>A combined \"start_process(path)\" call would have no way to let the caller customize the child's environment before the new program takes over \u2014 file descriptor redirection, changing the working directory, dropping privileges, all of it would have to be built into that one call's API, or be impossible.</p><p>By splitting fork (duplicate) from exec (replace), the code that runs between them \u2014 still the old program, still able to call ordinary functions \u2014 has a window to modify only the <em>child's</em> state: closing/reopening file descriptors, changing environment variables, chrooting, before calling exec to become the new program. This is exactly how a shell implements <code>&gt;</code>, <code>&lt;</code>, and <code>|</code> \u2014 none of it needs special-casing inside exec itself.</p><p>`fork()` without `exec()` is also independently useful \u2014 some servers fork to handle a request in a child running the same code, no new program involved at all.</p></div>"
    },
    {
      "type": "quiz",
      "id": "fork-cow-1",
      "poolId": "fork-cow",
      "level": 2,
      "prompt": "A parent process has a 2GB heap. It calls fork(). Does the kernel immediately copy all 2GB into the new child?",
      "choices": [
        "Yes, always \u2014 that's the whole point of fork()",
        "No \u2014 pages start out shared (copy-on-write); a physical copy of a page only happens the instant either process writes to it",
        "No \u2014 the child simply shares the parent's memory forever with no copying ever",
        "It depends on whether the child calls exec() afterward"
      ],
      "correctIndex": 1,
      "explanation": "Copy-on-write makes fork() cheap regardless of heap size: pages are shared read-only at first, and only the specific page that gets written is actually duplicated, at the moment of the write \u2014 not the whole address space up front.",
      "reference": "<div class='not-prose text-xs'>fork() \u2192 pages start <strong>shared</strong> \u2192 first write \u2192 real copy made (that page only)</div><p class='mt-2 text-xs text-slate-500 dark:text-slate-400'><strong>Why:</strong> copy-on-write defers the cost of duplicating memory until a write actually happens, and only for the page that changed.</p>",
      "learnMore": "<h5 class='mt-0 mb-2 text-sm font-semibold text-slate-700 dark:text-slate-200'>Copy-on-write: cheap fork(), true isolation</h5><div class='text-sm text-slate-600 dark:text-slate-300 space-y-2'><p>If `fork()` had to physically copy every byte of the parent's memory immediately, forking a process with a large heap would be slow every single time \u2014 even though most fork+exec sequences immediately discard that copied memory the moment exec() replaces it.</p><p>Instead, the kernel makes fork() cheap by not copying anything up front: it duplicates the page <em>table</em> (pointers to memory), marking every page in both processes' tables as read-only and copy-on-write. Both processes' pages point at the same physical memory initially. The instant either process <em>writes</em> to one of those pages, the CPU traps the write, the kernel makes a real physical copy of just that one page, updates the writer's page table to point at the new copy, and lets the write proceed.</p><p>The result: fork() is fast (near-O(1) regardless of heap size), and isolation still holds perfectly \u2014 a write by one process is invisible to the other, because by the time any write happens, they're no longer sharing that page.</p></div>"
    },
    {
      "type": "quiz",
      "id": "fork-cow-2",
      "poolId": "fork-cow",
      "level": 2,
      "prompt": "Right after fork(), before either process has written anything, what's true about the parent's and child's memory?",
      "choices": [
        "They already point at completely separate physical pages",
        "They point at the same physical pages, both marked read-only/copy-on-write, until one of them writes",
        "The child has no memory at all until exec() is called",
        "The parent's memory is temporarily frozen until the child exits"
      ],
      "correctIndex": 1,
      "explanation": "Immediately after fork(), the two processes share physical pages for efficiency \u2014 no copying has happened yet. The pages are marked copy-on-write specifically so a later write by either side triggers a real, private copy at that moment, not before.",
      "reference": "<div class='not-prose text-xs'>fork() \u2192 pages start <strong>shared</strong> \u2192 first write \u2192 real copy made (that page only)</div><p class='mt-2 text-xs text-slate-500 dark:text-slate-400'><strong>Why:</strong> copy-on-write defers the cost of duplicating memory until a write actually happens, and only for the page that changed.</p>",
      "learnMore": "<h5 class='mt-0 mb-2 text-sm font-semibold text-slate-700 dark:text-slate-200'>Copy-on-write: cheap fork(), true isolation</h5><div class='text-sm text-slate-600 dark:text-slate-300 space-y-2'><p>If `fork()` had to physically copy every byte of the parent's memory immediately, forking a process with a large heap would be slow every single time \u2014 even though most fork+exec sequences immediately discard that copied memory the moment exec() replaces it.</p><p>Instead, the kernel makes fork() cheap by not copying anything up front: it duplicates the page <em>table</em> (pointers to memory), marking every page in both processes' tables as read-only and copy-on-write. Both processes' pages point at the same physical memory initially. The instant either process <em>writes</em> to one of those pages, the CPU traps the write, the kernel makes a real physical copy of just that one page, updates the writer's page table to point at the new copy, and lets the write proceed.</p><p>The result: fork() is fast (near-O(1) regardless of heap size), and isolation still holds perfectly \u2014 a write by one process is invisible to the other, because by the time any write happens, they're no longer sharing that page.</p></div>"
    },
    {
      "type": "quiz",
      "id": "fork-cow-3",
      "poolId": "fork-cow",
      "level": 2,
      "prompt": "Why does copy-on-write make fork() practical to use even for processes with very large heaps?",
      "choices": [
        "Because it skips duplicating the page table entirely",
        "Because the expensive part (physically copying memory) is deferred until, and limited to, the specific pages that actually get written \u2014 most forked processes barely touch most of their memory before exec() replaces it anyway",
        "Because large heaps are automatically compressed during fork()",
        "It doesn't \u2014 fork() is always slow for large heaps regardless of copy-on-write"
      ],
      "correctIndex": 1,
      "explanation": "fork()'s cost without copy-on-write would scale with heap size, since it would have to copy everything upfront. With copy-on-write, the cost is proportional to how much memory actually gets modified afterward \u2014 often very little, especially right before an exec() throws the copy away anyway.",
      "reference": "<div class='not-prose text-xs'>fork() \u2192 pages start <strong>shared</strong> \u2192 first write \u2192 real copy made (that page only)</div><p class='mt-2 text-xs text-slate-500 dark:text-slate-400'><strong>Why:</strong> copy-on-write defers the cost of duplicating memory until a write actually happens, and only for the page that changed.</p>",
      "learnMore": "<h5 class='mt-0 mb-2 text-sm font-semibold text-slate-700 dark:text-slate-200'>Copy-on-write: cheap fork(), true isolation</h5><div class='text-sm text-slate-600 dark:text-slate-300 space-y-2'><p>If `fork()` had to physically copy every byte of the parent's memory immediately, forking a process with a large heap would be slow every single time \u2014 even though most fork+exec sequences immediately discard that copied memory the moment exec() replaces it.</p><p>Instead, the kernel makes fork() cheap by not copying anything up front: it duplicates the page <em>table</em> (pointers to memory), marking every page in both processes' tables as read-only and copy-on-write. Both processes' pages point at the same physical memory initially. The instant either process <em>writes</em> to one of those pages, the CPU traps the write, the kernel makes a real physical copy of just that one page, updates the writer's page table to point at the new copy, and lets the write proceed.</p><p>The result: fork() is fast (near-O(1) regardless of heap size), and isolation still holds perfectly \u2014 a write by one process is invisible to the other, because by the time any write happens, they're no longer sharing that page.</p></div>"
    },
    {
      "type": "quiz",
      "id": "ptrans-1",
      "poolId": "page-table-translation",
      "level": 2,
      "prompt": "What hardware component performs the virtual-to-physical address translation on every memory access, and what does it consult to do it?",
      "choices": [
        "The disk controller, consulting the filesystem",
        "The MMU, consulting the current process's page table",
        "The CPU's L1 cache, consulting a hash of the process name",
        "The OS scheduler, consulting the process's priority"
      ],
      "correctIndex": 1,
      "explanation": "The MMU (memory management unit) is dedicated CPU hardware for this exact purpose \u2014 it translates every virtual address a running process touches into a physical one, by looking it up in that process's page table.",
      "reference": "<div class='not-prose text-xs'>virtual addr \u2192 <strong>MMU</strong> (via page table) \u2192 physical addr</div><p class='mt-2 text-xs text-slate-500 dark:text-slate-400'><strong>Why:</strong> the MMU consults the current process's page table on every memory access \u2014 there is no path that skips this.</p>",
      "learnMore": "<h5 class='mt-0 mb-2 text-sm font-semibold text-slate-700 dark:text-slate-200'>Page tables and the MMU, mechanically</h5><div class='text-sm text-slate-600 dark:text-slate-300 space-y-2'><p>A page table is a per-process data structure the kernel maintains, mapping ranges of virtual addresses (\"pages\") to ranges of physical addresses (\"frames\"). The CPU has dedicated hardware, the MMU, whose job is to consult the currently-active process's page table on every single memory access and perform this translation, transparently to the running code.</p><p>When the scheduler context-switches from one process to another, part of that switch is telling the MMU which page table to use next \u2014 this is why a context switch is more expensive than it might look: it can invalidate cached translations (the TLB, a small cache of recent translations) and force the CPU to re-walk the new process's page table on its next several memory accesses.</p><p>A page table entry can also simply be absent or marked invalid \u2014 which is exactly the case for address 0x0 in every process (the deliberate NULL-guard) and for any address a process hasn't been given any mapping for at all.</p></div>"
    },
    {
      "type": "quiz",
      "id": "ptrans-2",
      "poolId": "page-table-translation",
      "level": 2,
      "prompt": "When the scheduler context-switches from process A to process B, what has to change about address translation?",
      "choices": [
        "Nothing \u2014 both processes share the same page table",
        "The MMU must be pointed at B's page table instead of A's, so subsequent virtual addresses resolve according to B's private mappings",
        "The physical RAM is physically swapped out for a different chip",
        "Address translation is disabled entirely during the switch"
      ],
      "correctIndex": 1,
      "explanation": "Each process has its own page table. A context switch has to include switching which page table the MMU consults, or B's code would end up executing against A's memory mappings \u2014 this switch is part of what makes context switches non-free.",
      "reference": "<div class='not-prose text-xs'>virtual addr \u2192 <strong>MMU</strong> (via page table) \u2192 physical addr</div><p class='mt-2 text-xs text-slate-500 dark:text-slate-400'><strong>Why:</strong> the MMU consults the current process's page table on every memory access \u2014 there is no path that skips this.</p>",
      "learnMore": "<h5 class='mt-0 mb-2 text-sm font-semibold text-slate-700 dark:text-slate-200'>Page tables and the MMU, mechanically</h5><div class='text-sm text-slate-600 dark:text-slate-300 space-y-2'><p>A page table is a per-process data structure the kernel maintains, mapping ranges of virtual addresses (\"pages\") to ranges of physical addresses (\"frames\"). The CPU has dedicated hardware, the MMU, whose job is to consult the currently-active process's page table on every single memory access and perform this translation, transparently to the running code.</p><p>When the scheduler context-switches from one process to another, part of that switch is telling the MMU which page table to use next \u2014 this is why a context switch is more expensive than it might look: it can invalidate cached translations (the TLB, a small cache of recent translations) and force the CPU to re-walk the new process's page table on its next several memory accesses.</p><p>A page table entry can also simply be absent or marked invalid \u2014 which is exactly the case for address 0x0 in every process (the deliberate NULL-guard) and for any address a process hasn't been given any mapping for at all.</p></div>"
    },
    {
      "type": "quiz",
      "id": "growth-1",
      "poolId": "memory-region-growth",
      "level": 2,
      "prompt": "What triggers the heap to grow, versus what triggers the stack to grow?",
      "choices": [
        "Both grow automatically on a fixed timer",
        "The heap grows when code explicitly requests more memory (malloc/new); the stack grows implicitly, one frame per function call",
        "The heap grows on function calls; the stack grows on memory allocation requests",
        "Neither actually grows after the process starts \u2014 both are fixed-size from launch"
      ],
      "correctIndex": 1,
      "explanation": "Heap growth is explicit and request-driven \u2014 an allocator asks the OS for more address space when it runs low. Stack growth is implicit and automatic \u2014 every function call adds a frame with no explicit \"grow the stack\" call from the programmer.",
      "reference": "<div class='not-prose grid grid-cols-2 gap-2 text-xs'><div class=\"rounded border border-sky-300 bg-sky-50 p-1.5 dark:border-sky-700 dark:bg-sky-950\">\ud83d\udcc8 <strong>heap</strong><br/>grows on request (malloc/new)</div><div class=\"rounded border border-amber-300 bg-amber-50 p-1.5 dark:border-amber-700 dark:bg-amber-950\">\ud83d\udcc9 <strong>stack</strong><br/>grows automatically per call frame</div></div><p class='mt-2 text-xs text-slate-500 dark:text-slate-400'><strong>Why:</strong> heap growth is explicit (you ask for it); stack growth is implicit (it happens as a side effect of calling a function).</p>",
      "learnMore": "<h5 class='mt-0 mb-2 text-sm font-semibold text-slate-700 dark:text-slate-200'>Two different growth mechanisms, one shared risk</h5><div class='text-sm text-slate-600 dark:text-slate-300 space-y-2'><p>The heap grows only when code explicitly asks for more memory (`malloc`, `new`, appending to a dynamic array that needs to reallocate). The allocator tracks free and used blocks and, when it runs out of already-reserved space, asks the OS (via a system call like `brk`/`mmap`) for more virtual address space to manage.</p><p>The stack grows implicitly: every function call pushes a new frame \u2014 return address, parameters, local variables \u2014 with zero explicit action from the programmer. Most OSes let the stack grow on demand up to a configured limit (checked via `ulimit -s` on Linux) rather than reserving its maximum size up front.</p><p>Both mechanisms share the same underlying risk: if either one grows without bound (an allocator that never frees, a recursive function with no reachable base case), it eventually exhausts either the available memory or the reserved stack region \u2014 heap exhaustion and stack overflow are the same category of failure, arrived at from opposite directions.</p></div>"
    },
    {
      "type": "quiz",
      "id": "growth-2",
      "poolId": "memory-region-growth",
      "level": 2,
      "prompt": "An allocator that never frees memory it no longer needs, and a recursive function with an unreachable base case, fail in what similar way?",
      "choices": [
        "They don't share any similarity \u2014 one is a heap problem, one is a stack problem, unrelated failure modes",
        "Both are the same category of failure \u2014 unbounded growth of a memory region until it exhausts its available space \u2014 just approached from the heap side versus the stack side",
        "Both are actually caused by the same underlying CPU bug",
        "Neither one is a real failure; both are just slow, not broken"
      ],
      "correctIndex": 1,
      "explanation": "Heap exhaustion (never freeing) and stack overflow (never returning) are structurally the same failure \u2014 a region growing without bound until it runs out of space \u2014 they just live in different regions and get triggered by different code patterns.",
      "reference": "<div class='not-prose grid grid-cols-2 gap-2 text-xs'><div class=\"rounded border border-sky-300 bg-sky-50 p-1.5 dark:border-sky-700 dark:bg-sky-950\">\ud83d\udcc8 <strong>heap</strong><br/>grows on request (malloc/new)</div><div class=\"rounded border border-amber-300 bg-amber-50 p-1.5 dark:border-amber-700 dark:bg-amber-950\">\ud83d\udcc9 <strong>stack</strong><br/>grows automatically per call frame</div></div><p class='mt-2 text-xs text-slate-500 dark:text-slate-400'><strong>Why:</strong> heap growth is explicit (you ask for it); stack growth is implicit (it happens as a side effect of calling a function).</p>",
      "learnMore": "<h5 class='mt-0 mb-2 text-sm font-semibold text-slate-700 dark:text-slate-200'>Two different growth mechanisms, one shared risk</h5><div class='text-sm text-slate-600 dark:text-slate-300 space-y-2'><p>The heap grows only when code explicitly asks for more memory (`malloc`, `new`, appending to a dynamic array that needs to reallocate). The allocator tracks free and used blocks and, when it runs out of already-reserved space, asks the OS (via a system call like `brk`/`mmap`) for more virtual address space to manage.</p><p>The stack grows implicitly: every function call pushes a new frame \u2014 return address, parameters, local variables \u2014 with zero explicit action from the programmer. Most OSes let the stack grow on demand up to a configured limit (checked via `ulimit -s` on Linux) rather than reserving its maximum size up front.</p><p>Both mechanisms share the same underlying risk: if either one grows without bound (an allocator that never frees, a recursive function with no reachable base case), it eventually exhausts either the available memory or the reserved stack region \u2014 heap exhaustion and stack overflow are the same category of failure, arrived at from opposite directions.</p></div>"
    },
    {
      "type": "quiz",
      "id": "seg-1",
      "poolId": "segfault-mechanism",
      "level": 2,
      "prompt": "Put these in the correct order for how a segfault actually happens: (a) kernel delivers SIGSEGV, (b) code accesses an invalid virtual address, (c) MMU raises a page fault, (d) kernel's page-fault handler finds no valid mapping",
      "choices": ["a, b, c, d", "b, c, d, a", "c, b, a, d", "d, c, b, a"],
      "correctIndex": 1,
      "explanation": "The access happens first (b), which the MMU can't translate so it faults (c), the kernel's handler inspects the fault and finds no legitimate mapping (d), and only then does it deliver the fatal signal (a) \u2014 a segfault is the end of a pipeline, not a single instantaneous event.",
      "reference": "<div class='not-prose text-xs'>bad access \u2192 MMU page fault \u2192 kernel checks mapping \u2192 none found \u2192 <code>SIGSEGV</code></div><p class='mt-2 text-xs text-slate-500 dark:text-slate-400'><strong>Why:</strong> a segfault is the kernel's considered response to an access it has no valid mapping for, not a hardware malfunction.</p>",
      "learnMore": "<h5 class='mt-0 mb-2 text-sm font-semibold text-slate-700 dark:text-slate-200'>The segfault pipeline, step by step</h5><div class='text-sm text-slate-600 dark:text-slate-300 space-y-2'><p>When code accesses a virtual address, the MMU tries to translate it using the current page table. If there's no valid entry for that address (unmapped, or mapped with permissions that forbid the attempted operation \u2014 e.g. writing to the read-only text segment), the MMU raises a page fault, trapping into the kernel.</p><p>The kernel's page-fault handler then has to decide what this fault means. Some page faults are entirely normal and handled silently \u2014 e.g. a page that's valid but not yet backed by physical RAM (lazily allocated, or swapped out to disk), which the kernel just backs and lets the process continue. But if the faulting address has no valid mapping <em>at all</em> for this process \u2014 the classic \"this pointer points nowhere\" case \u2014 the kernel concludes the access is illegitimate and delivers `SIGSEGV` to the process, whose default handler terminates it.</p><p>This is why \"segfault\" specifically means an access to genuinely unmapped/forbidden memory, not any memory-related slowness or error \u2014 a page fault that resolves successfully (lazy allocation, swap-in) never becomes a segfault at all.</p></div>"
    },
    {
      "type": "quiz",
      "id": "seg-2",
      "poolId": "segfault-mechanism",
      "level": 2,
      "prompt": "Not every page fault becomes a segfault. What's an example of a page fault the kernel resolves silently, without killing the process?",
      "choices": [
        "There is no such case \u2014 every page fault is fatal",
        "A page that's validly mapped for this process but not yet backed by physical RAM (lazily allocated, or swapped to disk) \u2014 the kernel backs it and lets execution continue",
        "Any access during the first second after a process starts",
        "A page fault caused by reading (as opposed to writing) memory"
      ],
      "correctIndex": 1,
      "explanation": "The kernel's page-fault handler distinguishes between \"valid mapping, just not resident yet\" (fixed silently by loading/backing the page) and \"no valid mapping at all\" (fatal \u2014 SIGSEGV). Lazy allocation and swap-in rely entirely on the first, benign kind of page fault.",
      "reference": "<div class='not-prose text-xs'>bad access \u2192 MMU page fault \u2192 kernel checks mapping \u2192 none found \u2192 <code>SIGSEGV</code></div><p class='mt-2 text-xs text-slate-500 dark:text-slate-400'><strong>Why:</strong> a segfault is the kernel's considered response to an access it has no valid mapping for, not a hardware malfunction.</p>",
      "learnMore": "<h5 class='mt-0 mb-2 text-sm font-semibold text-slate-700 dark:text-slate-200'>The segfault pipeline, step by step</h5><div class='text-sm text-slate-600 dark:text-slate-300 space-y-2'><p>When code accesses a virtual address, the MMU tries to translate it using the current page table. If there's no valid entry for that address (unmapped, or mapped with permissions that forbid the attempted operation \u2014 e.g. writing to the read-only text segment), the MMU raises a page fault, trapping into the kernel.</p><p>The kernel's page-fault handler then has to decide what this fault means. Some page faults are entirely normal and handled silently \u2014 e.g. a page that's valid but not yet backed by physical RAM (lazily allocated, or swapped out to disk), which the kernel just backs and lets the process continue. But if the faulting address has no valid mapping <em>at all</em> for this process \u2014 the classic \"this pointer points nowhere\" case \u2014 the kernel concludes the access is illegitimate and delivers `SIGSEGV` to the process, whose default handler terminates it.</p><p>This is why \"segfault\" specifically means an access to genuinely unmapped/forbidden memory, not any memory-related slowness or error \u2014 a page fault that resolves successfully (lazy allocation, swap-in) never becomes a segfault at all.</p></div>"
    },
    {
      "type": "quiz",
      "id": "seg-3",
      "poolId": "segfault-mechanism",
      "level": 2,
      "prompt": "Writing to the process's own text segment (its compiled code, normally read-only) triggers a page fault even though the address is validly mapped. Why does this still segfault?",
      "choices": [
        "It doesn't \u2014 writable text is the default on most systems",
        "The mapping exists but forbids writes; the fault handler sees a permission violation, not a missing mapping, and still concludes the access is illegitimate",
        "Text segment writes are silently ignored, no fault at all",
        "Only reads can ever trigger page faults, never writes"
      ],
      "correctIndex": 1,
      "explanation": "A page fault isn't only about missing mappings \u2014 it also fires when an access violates the permissions on an existing mapping, like attempting to write a read-only page. The kernel treats that as just as illegitimate as no mapping at all, and still delivers SIGSEGV.",
      "reference": "<div class='not-prose text-xs'>bad access \u2192 MMU page fault \u2192 kernel checks mapping \u2192 none found \u2192 <code>SIGSEGV</code></div><p class='mt-2 text-xs text-slate-500 dark:text-slate-400'><strong>Why:</strong> a segfault is the kernel's considered response to an access it has no valid mapping for, not a hardware malfunction.</p>",
      "learnMore": "<h5 class='mt-0 mb-2 text-sm font-semibold text-slate-700 dark:text-slate-200'>The segfault pipeline, step by step</h5><div class='text-sm text-slate-600 dark:text-slate-300 space-y-2'><p>When code accesses a virtual address, the MMU tries to translate it using the current page table. If there's no valid entry for that address (unmapped, or mapped with permissions that forbid the attempted operation \u2014 e.g. writing to the read-only text segment), the MMU raises a page fault, trapping into the kernel.</p><p>The kernel's page-fault handler then has to decide what this fault means. Some page faults are entirely normal and handled silently \u2014 e.g. a page that's valid but not yet backed by physical RAM (lazily allocated, or swapped out to disk), which the kernel just backs and lets the process continue. But if the faulting address has no valid mapping <em>at all</em> for this process \u2014 the classic \"this pointer points nowhere\" case \u2014 the kernel concludes the access is illegitimate and delivers `SIGSEGV` to the process, whose default handler terminates it.</p><p>This is why \"segfault\" specifically means an access to genuinely unmapped/forbidden memory, not any memory-related slowness or error \u2014 a page fault that resolves successfully (lazy allocation, swap-in) never becomes a segfault at all.</p></div>"
    },
    {
      "type": "code",
      "id": "stack-overflow-math-1",
      "poolId": "stack-overflow-math",
      "level": 3,
      "prompt": "L3 showed a stack limit turning unbounded recursion into a catchable error rather than a silent runaway. Write willStackOverflow(currentDepth, frameSizeBytes, stackLimitBytes) that returns true once the recursion would exceed the stack's size.",
      "starterCode": "function willStackOverflow(currentDepth, frameSizeBytes, stackLimitBytes) {\n  // TODO: true once currentDepth * frameSizeBytes exceeds stackLimitBytes\n}\n",
      "tests": [
        {
          "description": "well within the limit",
          "expr": "expect(willStackOverflow(5, 100, 1000)).toBe(false)"
        },
        {
          "description": "exactly at the limit is not yet an overflow",
          "expr": "expect(willStackOverflow(10, 100, 1000)).toBe(false)"
        },
        {
          "description": "one frame past the limit overflows",
          "expr": "expect(willStackOverflow(11, 100, 1000)).toBe(true)"
        },
        {
          "description": "zero depth never overflows",
          "expr": "expect(willStackOverflow(0, 100, 1000)).toBe(false)"
        }
      ],
      "solution": "function willStackOverflow(currentDepth, frameSizeBytes, stackLimitBytes) {\n  return currentDepth * frameSizeBytes > stackLimitBytes;\n}",
      "explanation": "This is the same arithmetic the runtime is implicitly doing when it decides to throw \"Maximum call stack size exceeded\": total space used so far (depth \u00d7 cost per frame) compared against the reserved stack size. It's `>` and not `>=` because using exactly the full budget hasn't overflowed it yet \u2014 the next frame would.",
      "reference": "<div class='not-prose text-xs'>depth \u00d7 frame size <strong>vs</strong> stack limit</div><p class='mt-2 text-xs text-slate-500 dark:text-slate-400'><strong>Why:</strong> this is literally the arithmetic a runtime does internally before deciding to throw a stack-overflow error.</p>",
      "learnMore": "<h5 class='mt-0 mb-2 text-sm font-semibold text-slate-700 dark:text-slate-200'>The arithmetic behind a stack-overflow guard</h5><div class='text-sm text-slate-600 dark:text-slate-300 space-y-2'><p>A managed runtime (like JS's V8) doesn't wait for the OS to segfault it \u2014 it tracks roughly how much stack space has been consumed and compares that against a configured limit, throwing a catchable error once the budget would be exceeded.</p><p>The comparison is simple: how many frames deep is the current call (depth), multiplied by roughly how much space each frame costs (which varies by how many locals/parameters a function has), compared against the total reserved stack size. Once that product would exceed the limit, the runtime throws rather than letting the next call actually run \u2014 this is what `L3-deep-dive.md`'s `stack.mjs` example is demonstrating happening automatically.</p><p>Using exactly the full budget (`depth * frameSize === limit`) hasn't overflowed yet \u2014 the next single frame would be what pushes it over, so the boundary condition matters: strictly greater-than, not greater-or-equal.</p></div>"
    },
    {
      "type": "code",
      "id": "stack-overflow-math-2",
      "poolId": "stack-overflow-math",
      "level": 3,
      "prompt": "Write maxSafeDepth(frameSizeBytes, stackLimitBytes) that returns the deepest recursion depth that does NOT overflow the stack (an integer \u2014 the largest depth where depth * frameSizeBytes <= stackLimitBytes).",
      "starterCode": "function maxSafeDepth(frameSizeBytes, stackLimitBytes) {\n  // TODO\n}\n",
      "tests": [
        {
          "description": "exact division",
          "expr": "expect(maxSafeDepth(100, 1000)).toBe(10)"
        },
        {
          "description": "rounds down when it doesn't divide evenly",
          "expr": "expect(maxSafeDepth(300, 1000)).toBe(3)"
        },
        {
          "description": "large stack, tiny frames",
          "expr": "expect(maxSafeDepth(1, 1024)).toBe(1024)"
        },
        {
          "description": "frame bigger than the whole stack means zero safe depth",
          "expr": "expect(maxSafeDepth(2000, 1000)).toBe(0)"
        }
      ],
      "solution": "function maxSafeDepth(frameSizeBytes, stackLimitBytes) {\n  return Math.floor(stackLimitBytes / frameSizeBytes);\n}",
      "explanation": "This is the inverse of the overflow check \u2014 instead of asking \"does this depth overflow,\" it directly computes the largest depth that fits, by integer-dividing the total budget by the per-frame cost. It's the same formula this unit's interactives.json demo uses to compute max recursion depth from frame size and stack limit.",
      "reference": "<div class='not-prose text-xs'>depth \u00d7 frame size <strong>vs</strong> stack limit</div><p class='mt-2 text-xs text-slate-500 dark:text-slate-400'><strong>Why:</strong> this is literally the arithmetic a runtime does internally before deciding to throw a stack-overflow error.</p>",
      "learnMore": "<h5 class='mt-0 mb-2 text-sm font-semibold text-slate-700 dark:text-slate-200'>The arithmetic behind a stack-overflow guard</h5><div class='text-sm text-slate-600 dark:text-slate-300 space-y-2'><p>A managed runtime (like JS's V8) doesn't wait for the OS to segfault it \u2014 it tracks roughly how much stack space has been consumed and compares that against a configured limit, throwing a catchable error once the budget would be exceeded.</p><p>The comparison is simple: how many frames deep is the current call (depth), multiplied by roughly how much space each frame costs (which varies by how many locals/parameters a function has), compared against the total reserved stack size. Once that product would exceed the limit, the runtime throws rather than letting the next call actually run \u2014 this is what `L3-deep-dive.md`'s `stack.mjs` example is demonstrating happening automatically.</p><p>Using exactly the full budget (`depth * frameSize === limit`) hasn't overflowed yet \u2014 the next single frame would be what pushes it over, so the boundary condition matters: strictly greater-than, not greater-or-equal.</p></div>"
    },
    {
      "type": "code",
      "id": "cow-simulation-1",
      "poolId": "cow-simulation",
      "level": 3,
      "prompt": "Model copy-on-write forking. Write forkAndWrite(parentMemory, writes) where parentMemory is an object of key/value pairs and writes is an object of keys to overwrite in the CHILD only. Return { parent, child } where parent is unchanged and child has the writes applied on top of a copy of parentMemory.",
      "starterCode": "function forkAndWrite(parentMemory, writes) {\n  // TODO: return { parent: parentMemory (unchanged), child: copy of parentMemory with writes applied }\n}\n",
      "tests": [
        {
          "description": "parent is returned unchanged",
          "expr": "expect(forkAndWrite({ counter: 0 }, { counter: 999 }).parent).toEqual({ counter: 0 })"
        },
        {
          "description": "child has the write applied",
          "expr": "expect(forkAndWrite({ counter: 0 }, { counter: 999 }).child).toEqual({ counter: 999 })"
        },
        {
          "description": "keys not in writes are inherited from the parent in the child",
          "expr": "expect(forkAndWrite({ a: 1, b: 2 }, { a: 99 }).child).toEqual({ a: 99, b: 2 })"
        },
        {
          "description": "mutating the returned child does not affect the returned parent object",
          "expr": "const r = forkAndWrite({ x: 1 }, { x: 2 }); r.child.x = 500; expect(r.parent.x).toBe(1)"
        }
      ],
      "solution": "function forkAndWrite(parentMemory, writes) {\n  const child = { ...parentMemory, ...writes };\n  return { parent: parentMemory, child };\n}",
      "explanation": "This mirrors fork_isolation.py's actual behavior: the parent's memory is never touched, and the child gets a full logical copy with only its own writes layered on top \u2014 exactly what copy-on-write guarantees at the OS level, just modeled here with a plain object spread instead of real page tables.",
      "reference": "<div class='not-prose text-xs'>write \u2192 copy that page only \u2192 diverge</div><p class='mt-2 text-xs text-slate-500 dark:text-slate-400'><strong>Why:</strong> modeling copy-on-write as 'copy on first write, share until then' captures the real kernel behavior from fork_isolation.py.</p>",
      "learnMore": "<h5 class='mt-0 mb-2 text-sm font-semibold text-slate-700 dark:text-slate-200'>Modeling copy-on-write in code</h5><div class='text-sm text-slate-600 dark:text-slate-300 space-y-2'><p>`fork_isolation.py` in L3 showed real copy-on-write behavior: parent and child start with identical values, and a write in one is invisible to the other. The underlying rule the kernel implements is: pages start shared, and a physical copy of a page is made lazily, only for the process that writes to it, only at the moment it writes.</p><p>Modeling this in ordinary code (without real OS pages) means treating 'the child's view' as a copy that only diverges from the parent's once the child writes \u2014 before any write, reading from either side should see the same values, exactly mirroring what the real `fork()` guarantees.</p></div>"
    },
    {
      "type": "code",
      "id": "cow-simulation-2",
      "poolId": "cow-simulation",
      "level": 3,
      "prompt": "Write pagesNeedingCopy(parentPages, childWrites) that models which pages actually get physically copied under copy-on-write. parentPages is an array of page ids the process has; childWrites is an array of page ids the CHILD writes to. Return the array of page ids that require a real copy (only pages that are both in parentPages AND get written).",
      "starterCode": "function pagesNeedingCopy(parentPages, childWrites) {\n  // TODO\n}\n",
      "tests": [
        {
          "description": "only written pages that exist get copied",
          "expr": "expect(pagesNeedingCopy(['p1','p2','p3'], ['p2'])).toEqual(['p2'])"
        },
        {
          "description": "no writes means no copies",
          "expr": "expect(pagesNeedingCopy(['p1','p2'], [])).toEqual([])"
        },
        {
          "description": "writes to pages the process doesn't have are ignored",
          "expr": "expect(pagesNeedingCopy(['p1'], ['p9'])).toEqual([])"
        },
        {
          "description": "multiple written pages all get copied",
          "expr": "expect(pagesNeedingCopy(['p1','p2','p3'], ['p1','p3'])).toEqual(['p1','p3'])"
        }
      ],
      "solution": "function pagesNeedingCopy(parentPages, childWrites) {\n  const owned = new Set(parentPages);\n  return childWrites.filter((p) => owned.has(p));\n}",
      "explanation": "This is the essence of copy-on-write's laziness: out of every page the process has, only the subset that's actually written ever gets physically duplicated \u2014 untouched pages stay shared for the rest of the process's life, which is why fork() stays cheap even for processes with huge heaps.",
      "reference": "<div class='not-prose text-xs'>write \u2192 copy that page only \u2192 diverge</div><p class='mt-2 text-xs text-slate-500 dark:text-slate-400'><strong>Why:</strong> modeling copy-on-write as 'copy on first write, share until then' captures the real kernel behavior from fork_isolation.py.</p>",
      "learnMore": "<h5 class='mt-0 mb-2 text-sm font-semibold text-slate-700 dark:text-slate-200'>Modeling copy-on-write in code</h5><div class='text-sm text-slate-600 dark:text-slate-300 space-y-2'><p>`fork_isolation.py` in L3 showed real copy-on-write behavior: parent and child start with identical values, and a write in one is invisible to the other. The underlying rule the kernel implements is: pages start shared, and a physical copy of a page is made lazily, only for the process that writes to it, only at the moment it writes.</p><p>Modeling this in ordinary code (without real OS pages) means treating 'the child's view' as a copy that only diverges from the parent's once the child writes \u2014 before any write, reading from either side should see the same values, exactly mirroring what the real `fork()` guarantees.</p></div>"
    },
    {
      "type": "code",
      "id": "maps-parsing-1",
      "poolId": "maps-parsing",
      "level": 3,
      "prompt": "L3's maps.mjs read real lines like '324d8000-324f9000 rw-p 00000000 00:00 0 [heap]' from /proc/self/maps. Write parseMapsLine(line) returning { start, end, perms, name } where start/end are the hex strings before the dash, perms is the permission string, and name is whatever trailing bracketed/path text exists (or '' if none).",
      "starterCode": "function parseMapsLine(line) {\n  // TODO: split on whitespace; first field is 'start-end'\n}\n",
      "tests": [
        {
          "description": "parses a heap line",
          "expr": "expect(parseMapsLine('324d8000-324f9000 rw-p 00000000 00:00 0 [heap]')).toEqual({ start: '324d8000', end: '324f9000', perms: 'rw-p', name: '[heap]' })"
        },
        {
          "description": "parses a stack line",
          "expr": "expect(parseMapsLine('7fffec4aa000-7fffec4fd000 rw-p 00000000 00:00 0 [stack]')).toEqual({ start: '7fffec4aa000', end: '7fffec4fd000', perms: 'rw-p', name: '[stack]' })"
        },
        {
          "description": "handles a line with no trailing name",
          "expr": "expect(parseMapsLine('400000-401000 r-xp 00000000 08:01 12345').name).toBe('')"
        }
      ],
      "solution": "function parseMapsLine(line) {\n  const fields = line.trim().split(/\\s+/);\n  const [start, end] = fields[0].split('-');\n  const perms = fields[1];\n  const name = fields.slice(5).join(' ') || '';\n  return { start, end, perms, name };\n}",
      "explanation": "The maps format is whitespace-separated fields with the address range packed into the first one as 'start-end'. Splitting on the dash for that field, and treating anything past the fifth field as an optional name, correctly handles both named regions like [heap]/[stack] and anonymous mappings that have no trailing name at all.",
      "reference": "<div class='not-prose text-xs'><code>start-end perms offset dev inode [name]</code></div><p class='mt-2 text-xs text-slate-500 dark:text-slate-400'><strong>Why:</strong> /proc/self/maps is the kernel's own record \u2014 parsing it directly is parsing real page-table-derived output, not a simulation.</p>",
      "learnMore": "<h5 class='mt-0 mb-2 text-sm font-semibold text-slate-700 dark:text-slate-200'>Reading /proc/self/maps lines</h5><div class='text-sm text-slate-600 dark:text-slate-300 space-y-2'><p>Each line of `/proc/<pid>/maps` on Linux describes one contiguous virtual memory region for that process: the address range (`start-end` in hex), permissions (`rwxp` or similar), file offset, device, inode, and optionally a name (a file path, or a bracketed pseudo-name like `[heap]` or `[stack]`).</p><p>Parsing these lines programmatically is a real, practical skill \u2014 tools that inspect a process's memory layout (debuggers, profilers, security scanners checking for unexpected writable+executable regions) all start from exactly this format.</p></div>"
    },
    {
      "type": "code",
      "id": "maps-parsing-2",
      "poolId": "maps-parsing",
      "level": 3,
      "prompt": "Write regionSizeBytes(startHex, endHex) that computes the size in bytes of a memory region given its start and end addresses as hex strings (no '0x' prefix), as seen in /proc/self/maps lines.",
      "starterCode": "function regionSizeBytes(startHex, endHex) {\n  // TODO: parse both as hex and return the difference\n}\n",
      "tests": [
        {
          "description": "computes a small region's size",
          "expr": "expect(regionSizeBytes('1000', '2000')).toBe(4096)"
        },
        {
          "description": "matches the real heap line from L3 (0x324f9000 - 0x324d8000)",
          "expr": "expect(regionSizeBytes('324d8000', '324f9000')).toBe(0x324f9000 - 0x324d8000)"
        },
        {
          "description": "zero-size region",
          "expr": "expect(regionSizeBytes('abc', 'abc')).toBe(0)"
        }
      ],
      "solution": "function regionSizeBytes(startHex, endHex) {\n  return parseInt(endHex, 16) - parseInt(startHex, 16);\n}",
      "explanation": "Both addresses are hex strings, so parseInt with base 16 converts them to actual numbers before subtracting \u2014 this is exactly how you'd compute how large the [heap] or [stack] region actually is from a real /proc/self/maps line, useful for spotting an unexpectedly large or fast-growing region.",
      "reference": "<div class='not-prose text-xs'><code>start-end perms offset dev inode [name]</code></div><p class='mt-2 text-xs text-slate-500 dark:text-slate-400'><strong>Why:</strong> /proc/self/maps is the kernel's own record \u2014 parsing it directly is parsing real page-table-derived output, not a simulation.</p>",
      "learnMore": "<h5 class='mt-0 mb-2 text-sm font-semibold text-slate-700 dark:text-slate-200'>Reading /proc/self/maps lines</h5><div class='text-sm text-slate-600 dark:text-slate-300 space-y-2'><p>Each line of `/proc/<pid>/maps` on Linux describes one contiguous virtual memory region for that process: the address range (`start-end` in hex), permissions (`rwxp` or similar), file offset, device, inode, and optionally a name (a file path, or a bracketed pseudo-name like `[heap]` or `[stack]`).</p><p>Parsing these lines programmatically is a real, practical skill \u2014 tools that inspect a process's memory layout (debuggers, profilers, security scanners checking for unexpected writable+executable regions) all start from exactly this format.</p></div>"
    },
    {
      "type": "code",
      "id": "heap-tracker-1",
      "poolId": "heap-growth-tracker",
      "level": 3,
      "prompt": "Write heapGrowth(beforeBytes, afterBytes) that returns the growth in megabytes (as a number, not a string), rounded to 1 decimal place, the same measurement heap.mjs's 'before'/'after' lines are showing in MB.",
      "starterCode": "function heapGrowth(beforeBytes, afterBytes) {\n  // TODO: (afterBytes - beforeBytes) in MB, rounded to 1 decimal\n}\n",
      "tests": [
        {
          "description": "matches the L3 example's growth",
          "expr": "expect(heapGrowth(0.2 * 1024 * 1024, 120.9 * 1024 * 1024)).toBeCloseTo(120.7, 1)"
        },
        {
          "description": "zero growth",
          "expr": "expect(heapGrowth(1000, 1000)).toBe(0)"
        },
        {
          "description": "negative growth (after a GC) is allowed",
          "expr": "expect(heapGrowth(2 * 1024 * 1024, 1 * 1024 * 1024)).toBe(-1)"
        }
      ],
      "solution": "function heapGrowth(beforeBytes, afterBytes) {\n  return Math.round(((afterBytes - beforeBytes) / 1024 / 1024) * 10) / 10;\n}",
      "explanation": "This is exactly heap.mjs's mb() helper applied to a delta instead of a single reading \u2014 converting a byte difference into MB and rounding, which is the standard way memory growth is reported (and how you'd notice a leak: growth that never comes back down across repeated measurements).",
      "reference": "<div class='not-prose text-xs'>heapUsed(before) \u2192 allocate \u2192 heapUsed(after) \u2192 delta = growth</div><p class='mt-2 text-xs text-slate-500 dark:text-slate-400'><strong>Why:</strong> measuring memory growth as a before/after delta is the same technique L3's heap.mjs example uses to prove the heap actually grew.</p>",
      "learnMore": "<h5 class='mt-0 mb-2 text-sm font-semibold text-slate-700 dark:text-slate-200'>Tracking heap growth over time</h5><div class='text-sm text-slate-600 dark:text-slate-300 space-y-2'><p>L3's `heap.mjs` proved the heap grows by snapshotting `process.memoryUsage().heapUsed` before and after a large allocation and comparing the two numbers. That same before/after-delta technique generalizes to tracking memory growth across any sequence of operations \u2014 which is exactly what a memory profiler or a leak-detection tool does under the hood, just automated and sampled continuously instead of at two manually-chosen points.</p><p>A useful derived signal from a series of such snapshots is whether usage is trending upward without ever coming back down \u2014 that pattern (monotonically increasing, never released) is the practical signature of a leak, as opposed to normal allocate/free churn that goes up and down but stays roughly flat over time.</p></div>"
    },
    {
      "type": "code",
      "id": "heap-tracker-2",
      "poolId": "heap-growth-tracker",
      "level": 3,
      "prompt": "Write isLikelyLeaking(samples) where samples is an array of heapUsed readings taken over time (e.g. once per minute). Return true only if every consecutive sample is greater than or equal to the previous one AND the last sample is strictly greater than the first (monotonically non-decreasing, with real net growth \u2014 not just flat).",
      "starterCode": "function isLikelyLeaking(samples) {\n  // TODO\n}\n",
      "tests": [
        {
          "description": "steadily increasing samples look like a leak",
          "expr": "expect(isLikelyLeaking([10, 20, 35, 50])).toBe(true)"
        },
        {
          "description": "samples that go up then down are not a leak",
          "expr": "expect(isLikelyLeaking([10, 50, 20, 30])).toBe(false)"
        },
        {
          "description": "flat samples are not a leak",
          "expr": "expect(isLikelyLeaking([10, 10, 10])).toBe(false)"
        },
        {
          "description": "fewer than 2 samples can't establish a trend",
          "expr": "expect(isLikelyLeaking([10])).toBe(false)"
        }
      ],
      "solution": "function isLikelyLeaking(samples) {\n  if (samples.length < 2) return false;\n  for (let i = 1; i < samples.length; i++) {\n    if (samples[i] < samples[i - 1]) return false;\n  }\n  return samples[samples.length - 1] > samples[0];\n}",
      "explanation": "A real leak signature is memory that never comes back down (monotonically non-decreasing) and shows real net growth over the window (not flat). A single dip anywhere in the series (garbage collection freeing something) is normal and rules out calling it a leak from this data alone \u2014 which is why both checks are needed, not just 'did it end higher than it started.'",
      "reference": "<div class='not-prose text-xs'>heapUsed(before) \u2192 allocate \u2192 heapUsed(after) \u2192 delta = growth</div><p class='mt-2 text-xs text-slate-500 dark:text-slate-400'><strong>Why:</strong> measuring memory growth as a before/after delta is the same technique L3's heap.mjs example uses to prove the heap actually grew.</p>",
      "learnMore": "<h5 class='mt-0 mb-2 text-sm font-semibold text-slate-700 dark:text-slate-200'>Tracking heap growth over time</h5><div class='text-sm text-slate-600 dark:text-slate-300 space-y-2'><p>L3's `heap.mjs` proved the heap grows by snapshotting `process.memoryUsage().heapUsed` before and after a large allocation and comparing the two numbers. That same before/after-delta technique generalizes to tracking memory growth across any sequence of operations \u2014 which is exactly what a memory profiler or a leak-detection tool does under the hood, just automated and sampled continuously instead of at two manually-chosen points.</p><p>A useful derived signal from a series of such snapshots is whether usage is trending upward without ever coming back down \u2014 that pattern (monotonically increasing, never released) is the practical signature of a leak, as opposed to normal allocate/free churn that goes up and down but stays roughly flat over time.</p></div>"
    },
    {
      "type": "code",
      "id": "recursion-depth-budget-1",
      "poolId": "recursion-depth-budget",
      "level": 3,
      "prompt": "This unit's interactive demo computes max recursion depth from frame size and stack limit. Write maxRecursionDepth(frameSizeBytes, stackLimitKB) matching that same formula (stack limit is given in KB here, convert to bytes first).",
      "starterCode": "function maxRecursionDepth(frameSizeBytes, stackLimitKB) {\n  // TODO\n}\n",
      "tests": [
        {
          "description": "matches the demo's default (80 bytes/frame, 1024KB stack)",
          "expr": "expect(maxRecursionDepth(80, 1024)).toBe(Math.floor((1024*1024)/80))"
        },
        {
          "description": "smaller frames allow deeper recursion",
          "expr": "expect(maxRecursionDepth(20, 1024) > maxRecursionDepth(80, 1024)).toBeTruthy()"
        },
        {
          "description": "larger stack allows deeper recursion",
          "expr": "expect(maxRecursionDepth(80, 8192) > maxRecursionDepth(80, 1024)).toBeTruthy()"
        }
      ],
      "solution": "function maxRecursionDepth(frameSizeBytes, stackLimitKB) {\n  const stackLimitBytes = stackLimitKB * 1024;\n  return Math.floor(stackLimitBytes / frameSizeBytes);\n}",
      "explanation": "Converting KB to bytes and dividing by per-frame cost gives the deepest safe recursion \u2014 the exact formula this unit's interactives.json 'compute' function uses, which is why dragging the frame-size slider up (bigger frames) or the stack-limit slider down both visibly shrink the max depth on the chart.",
      "reference": "<div class='not-prose text-xs'>stackLimitKB \u00d7 1024 \u00f7 frameSize = maxDepth</div><p class='mt-2 text-xs text-slate-500 dark:text-slate-400'><strong>Why:</strong> this is the exact formula backing this unit's interactives.json slider demo \u2014 bigger frames or a smaller stack both lower how deep you can safely recurse.</p>",
      "learnMore": "<h5 class='mt-0 mb-2 text-sm font-semibold text-slate-700 dark:text-slate-200'>Frame size, stack size, and safe recursion depth</h5><div class='text-sm text-slate-600 dark:text-slate-300 space-y-2'><p>The interactive demo on this unit lets you drag both a per-frame cost and a total stack size to see how deep a recursion can go before overflowing. The relationship is direct division: more bytes per frame (more local variables, more parameters) means fewer frames fit in the same stack budget; a larger configured stack size (raised via `ulimit -s` on Linux, for example) means more frames fit for the same per-frame cost.</p><p>This is a real, practical lever: a recursive algorithm that's \"fine\" in testing with small inputs can blow the stack in production with larger inputs, and the fix isn't always 'raise the stack limit' \u2014 sometimes it's restructuring the recursion to use less state per frame, or converting it to an iterative loop that uses the heap (or no extra memory at all) instead of the stack.</p></div>"
    },
    {
      "type": "code",
      "id": "recursion-depth-budget-2",
      "poolId": "recursion-depth-budget",
      "level": 3,
      "prompt": "Write requiredStackKB(frameSizeBytes, targetDepth) \u2014 the minimum stack size in whole KB needed to safely reach targetDepth recursive calls at the given frame size (round up, since a fractional KB still needs a full KB reserved).",
      "starterCode": "function requiredStackKB(frameSizeBytes, targetDepth) {\n  // TODO: round UP to the nearest whole KB\n}\n",
      "tests": [
        {
          "description": "exact fit still needs that many KB",
          "expr": "expect(requiredStackKB(1024, 4)).toBe(4)"
        },
        {
          "description": "rounds up a fractional KB requirement",
          "expr": "expect(requiredStackKB(100, 10)).toBe(1)"
        },
        {
          "description": "larger example rounds up correctly",
          "expr": "expect(requiredStackKB(300, 100)).toBe(30)"
        }
      ],
      "solution": "function requiredStackKB(frameSizeBytes, targetDepth) {\n  const totalBytes = frameSizeBytes * targetDepth;\n  return Math.ceil(totalBytes / 1024);\n}",
      "explanation": "This is the inverse of maxRecursionDepth: instead of asking how deep a given stack allows, it asks how large a stack a given depth requires \u2014 rounding up because the OS reserves stack space in whole pages/KB, so a requirement of 0.1KB still consumes a full KB of real reservation.",
      "reference": "<div class='not-prose text-xs'>stackLimitKB \u00d7 1024 \u00f7 frameSize = maxDepth</div><p class='mt-2 text-xs text-slate-500 dark:text-slate-400'><strong>Why:</strong> this is the exact formula backing this unit's interactives.json slider demo \u2014 bigger frames or a smaller stack both lower how deep you can safely recurse.</p>",
      "learnMore": "<h5 class='mt-0 mb-2 text-sm font-semibold text-slate-700 dark:text-slate-200'>Frame size, stack size, and safe recursion depth</h5><div class='text-sm text-slate-600 dark:text-slate-300 space-y-2'><p>The interactive demo on this unit lets you drag both a per-frame cost and a total stack size to see how deep a recursion can go before overflowing. The relationship is direct division: more bytes per frame (more local variables, more parameters) means fewer frames fit in the same stack budget; a larger configured stack size (raised via `ulimit -s` on Linux, for example) means more frames fit for the same per-frame cost.</p><p>This is a real, practical lever: a recursive algorithm that's \"fine\" in testing with small inputs can blow the stack in production with larger inputs, and the fix isn't always 'raise the stack limit' \u2014 sometimes it's restructuring the recursion to use less state per frame, or converting it to an iterative loop that uses the heap (or no extra memory at all) instead of the stack.</p></div>"
    },
    {
      "type": "code",
      "id": "process-tree-1",
      "poolId": "process-tree",
      "level": 3,
      "prompt": "pids.mjs showed a parent spawning one child with its own PID. Write buildProcessTree(processes) where processes is an array of { pid, ppid } and the function returns a map from each pid to an array of its direct children's pids.",
      "starterCode": "function buildProcessTree(processes) {\n  // TODO: return { [pid]: [childPid, ...] }\n}\n",
      "tests": [
        {
          "description": "single parent-child pair",
          "expr": "expect(buildProcessTree([{ pid: 100, ppid: null }, { pid: 101, ppid: 100 }])).toEqual({ 100: [101] })"
        },
        {
          "description": "multiple children of the same parent",
          "expr": "expect(buildProcessTree([{ pid: 1, ppid: null }, { pid: 2, ppid: 1 }, { pid: 3, ppid: 1 }])).toEqual({ 1: [2, 3] })"
        },
        {
          "description": "a three-generation chain",
          "expr": "expect(buildProcessTree([{ pid: 1, ppid: null }, { pid: 2, ppid: 1 }, { pid: 3, ppid: 2 }])).toEqual({ 1: [2], 2: [3] })"
        }
      ],
      "solution": "function buildProcessTree(processes) {\n  const tree = {};\n  for (const { pid, ppid } of processes) {\n    if (ppid === null || ppid === undefined) continue;\n    if (!tree[ppid]) tree[ppid] = [];\n    tree[ppid].push(pid);\n  }\n  return tree;\n}",
      "explanation": "Every process (besides the very first) has a parent, tracked by PPID \u2014 grouping processes by their ppid reconstructs the same tree structure `pstree` shows on a real machine, just from raw { pid, ppid } records instead of reading them live from the kernel.",
      "reference": "<div class='not-prose text-xs'>shell (PID 100) \u2192 fork \u2192 child (PID 101) \u2192 fork \u2192 grandchild (PID 102)</div><p class='mt-2 text-xs text-slate-500 dark:text-slate-400'><strong>Why:</strong> pids.mjs showed a real parent/child PID pair \u2014 a process tree is just that relationship extended across multiple generations.</p>",
      "learnMore": "<h5 class='mt-0 mb-2 text-sm font-semibold text-slate-700 dark:text-slate-200'>Process trees and PPIDs</h5><div class='text-sm text-slate-600 dark:text-slate-300 space-y-2'><p>Every process (except the kernel's very first one, PID 1 / init) has a parent \u2014 the process whose fork() created it. The OS tracks this as the PPID (parent PID), and the full set of these relationships across every running process forms a tree, visible on Linux via `ps --forest` or `pstree`.</p><p>This tree matters operationally: killing a parent doesn't automatically kill its children (they get \"re-parented\" to init/PID 1 and keep running as orphans, unless the parent explicitly waits for and manages them), and a parent that never calls `wait()` on an exited child leaves it as a zombie \u2014 an entry in the process table with no running code, just exit-status bookkeeping the parent never collected.</p></div>"
    },
    {
      "type": "code",
      "id": "process-tree-2",
      "poolId": "process-tree",
      "level": 3,
      "prompt": "Write findOrphans(processes, alivePids) where processes is an array of { pid, ppid } and alivePids is a Set of pids still running. Return the pids whose ppid is NOT in alivePids (i.e. their parent already exited) \u2014 these would be re-parented to init on a real Unix system.",
      "starterCode": "function findOrphans(processes, alivePids) {\n  // TODO\n}\n",
      "tests": [
        {
          "description": "child of a dead parent is an orphan",
          "expr": "expect(findOrphans([{ pid: 2, ppid: 1 }], new Set([2]))).toEqual([2])"
        },
        {
          "description": "child of a living parent is not an orphan",
          "expr": "expect(findOrphans([{ pid: 2, ppid: 1 }], new Set([1, 2]))).toEqual([])"
        },
        {
          "description": "root process with no parent is never an orphan",
          "expr": "expect(findOrphans([{ pid: 1, ppid: null }], new Set([1]))).toEqual([])"
        }
      ],
      "solution": "function findOrphans(processes, alivePids) {\n  return processes\n    .filter((p) => p.ppid !== null && p.ppid !== undefined && !alivePids.has(p.ppid))\n    .map((p) => p.pid);\n}",
      "explanation": "A process becomes an orphan the moment its parent exits while it's still running \u2014 real Unix systems handle this by re-parenting orphans to PID 1 (init), which adopts them so something is always responsible for eventually reaping their exit status.",
      "reference": "<div class='not-prose text-xs'>shell (PID 100) \u2192 fork \u2192 child (PID 101) \u2192 fork \u2192 grandchild (PID 102)</div><p class='mt-2 text-xs text-slate-500 dark:text-slate-400'><strong>Why:</strong> pids.mjs showed a real parent/child PID pair \u2014 a process tree is just that relationship extended across multiple generations.</p>",
      "learnMore": "<h5 class='mt-0 mb-2 text-sm font-semibold text-slate-700 dark:text-slate-200'>Process trees and PPIDs</h5><div class='text-sm text-slate-600 dark:text-slate-300 space-y-2'><p>Every process (except the kernel's very first one, PID 1 / init) has a parent \u2014 the process whose fork() created it. The OS tracks this as the PPID (parent PID), and the full set of these relationships across every running process forms a tree, visible on Linux via `ps --forest` or `pstree`.</p><p>This tree matters operationally: killing a parent doesn't automatically kill its children (they get \"re-parented\" to init/PID 1 and keep running as orphans, unless the parent explicitly waits for and manages them), and a parent that never calls `wait()` on an exited child leaves it as a zombie \u2014 an entry in the process table with no running code, just exit-status bookkeeping the parent never collected.</p></div>"
    },
    {
      "type": "code",
      "id": "memory-permission-check-1",
      "poolId": "memory-permission-check",
      "level": 3,
      "prompt": "Write isExecutable(permString) that returns true if a /proc/self/maps permission string like 'rw-p' or 'r-xp' indicates the region is executable (the third character is 'x', not '-').",
      "starterCode": "function isExecutable(permString) {\n  // TODO\n}\n",
      "tests": [
        {
          "description": "text segment permissions are executable",
          "expr": "expect(isExecutable('r-xp')).toBe(true)"
        },
        {
          "description": "heap permissions are not executable",
          "expr": "expect(isExecutable('rw-p')).toBe(false)"
        },
        {
          "description": "stack permissions are not executable",
          "expr": "expect(isExecutable('rw-p')).toBe(false)"
        }
      ],
      "solution": "function isExecutable(permString) {\n  return permString[2] === 'x';\n}",
      "explanation": "The permission string is always four characters in a fixed order (read, write, execute, shared/private) \u2014 checking index 2 for 'x' tells you whether code is allowed to run from that region at all, which is exactly the distinction that makes text executable but heap/stack (correctly) not.",
      "reference": "<div class='not-prose text-xs'><code>rwxp</code> \u2192 read / write / execute / private-or-shared</div><p class='mt-2 text-xs text-slate-500 dark:text-slate-400'><strong>Why:</strong> text is r-x (readable+executable, not writable); heap/stack are rw- (readable+writable, not executable) \u2014 the permission bits enforce exactly the boundaries L1/L2 described.</p>",
      "learnMore": "<h5 class='mt-0 mb-2 text-sm font-semibold text-slate-700 dark:text-slate-200'>What the permission letters in /proc/self/maps mean</h5><div class='text-sm text-slate-600 dark:text-slate-300 space-y-2'><p>Each maps line's permission field is four characters: read, write, execute, and shared-or-private. A region marked `r-xp` (like the text segment) can be read and executed but not written \u2014 this is the hardware-enforced version of \"code shouldn't be writable at runtime\" from L1. A region marked `rw-p` (like heap and stack) can be read and written but not executed \u2014 this is also a real security boundary: it's what stops a classic buffer-overflow exploit from simply writing shellcode onto the stack and jumping to it, since the stack's pages aren't executable.</p><p>The trailing `p` vs `s` indicates private (copy-on-write, this process's own eventual copy) vs shared (genuinely shared physical memory, e.g. a shared library's code mapped into multiple processes at once).</p></div>"
    },
    {
      "type": "code",
      "id": "memory-permission-check-2",
      "poolId": "memory-permission-check",
      "level": 3,
      "prompt": "Write isSuspiciousRegion(permString) that flags a region as suspicious if it's BOTH writable and executable at the same time (position 1 is 'w' AND position 2 is 'x') \u2014 a real red flag security scanners look for, since legitimate regions are almost never both.",
      "starterCode": "function isSuspiciousRegion(permString) {\n  // TODO\n}\n",
      "tests": [
        {
          "description": "normal heap (rw-p) is not suspicious",
          "expr": "expect(isSuspiciousRegion('rw-p')).toBe(false)"
        },
        {
          "description": "normal text (r-xp) is not suspicious",
          "expr": "expect(isSuspiciousRegion('r-xp')).toBe(false)"
        },
        {
          "description": "writable AND executable is flagged",
          "expr": "expect(isSuspiciousRegion('rwxp')).toBe(true)"
        },
        {
          "description": "read-only, non-executable is not suspicious",
          "expr": "expect(isSuspiciousRegion('r--p')).toBe(false)"
        }
      ],
      "solution": "function isSuspiciousRegion(permString) {\n  return permString[1] === 'w' && permString[2] === 'x';\n}",
      "explanation": "A writable+executable (W^X-violating) region is unusual precisely because it defeats the protection this pool's reference explains: if code can be written and then executed from the same page, an attacker who can write arbitrary bytes there can run arbitrary code \u2014 which is exactly why the text segment is r-x and the stack/heap are rw-, never both at once, on a well-configured system.",
      "reference": "<div class='not-prose text-xs'><code>rwxp</code> \u2192 read / write / execute / private-or-shared</div><p class='mt-2 text-xs text-slate-500 dark:text-slate-400'><strong>Why:</strong> text is r-x (readable+executable, not writable); heap/stack are rw- (readable+writable, not executable) \u2014 the permission bits enforce exactly the boundaries L1/L2 described.</p>",
      "learnMore": "<h5 class='mt-0 mb-2 text-sm font-semibold text-slate-700 dark:text-slate-200'>What the permission letters in /proc/self/maps mean</h5><div class='text-sm text-slate-600 dark:text-slate-300 space-y-2'><p>Each maps line's permission field is four characters: read, write, execute, and shared-or-private. A region marked `r-xp` (like the text segment) can be read and executed but not written \u2014 this is the hardware-enforced version of \"code shouldn't be writable at runtime\" from L1. A region marked `rw-p` (like heap and stack) can be read and written but not executed \u2014 this is also a real security boundary: it's what stops a classic buffer-overflow exploit from simply writing shellcode onto the stack and jumping to it, since the stack's pages aren't executable.</p><p>The trailing `p` vs `s` indicates private (copy-on-write, this process's own eventual copy) vs shared (genuinely shared physical memory, e.g. a shared library's code mapped into multiple processes at once).</p></div>"
    }
  ]
}
