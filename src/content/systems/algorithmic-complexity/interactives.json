{
  "items": [
    {
      "id": "linear-vs-quadratic-growth",
      "level": 1,
      "title": "Why does the same function feel instant in dev and unusable in production?",
      "description": "The unit's own incident: at n=200 (the CI fixture), O(n) work is 200 units and O(n²) work is 40,000 units — both finish instantly. Drag n toward production scale and watch how fast the quadratic curve pulls away from the linear one.",
      "params": [
        {
          "name": "n",
          "label": "Input size (n)",
          "min": 10,
          "max": 2000,
          "step": 10,
          "default": 200
        }
      ],
      "compute": "const linearWork = params.n;\nconst quadraticWork = params.n * params.n;\nreturn { linearWork, quadraticWork };",
      "outputs": [
        {
          "key": "linearWork",
          "label": "O(n) — linear",
          "color": "#34d399"
        },
        {
          "key": "quadraticWork",
          "label": "O(n²) — quadratic",
          "color": "#f87171"
        }
      ],
      "chartParam": "n"
    }
  ]
}
