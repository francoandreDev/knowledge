---
title: "L1 — Twelve green dashboards, one very unhappy set of users: how do you know infrastructure is actually healthy?"
---

import Scenario from "../../../components/Scenario.astro";
import LevelIntro from "../../../components/LevelIntro.astro";

<Scenario label="All green, still broken">
  <Fragment slot="facts">
    <div class="not-prose flex flex-col gap-1.5 text-sm text-slate-700 dark:text-slate-300">
      <div class="flex items-center gap-1.5"><span>📊</span> <strong>Dashboards checked</strong> — CPU, memory, disk, network, per node, per cluster: 12 panels</div>
      <div class="flex items-center gap-1.5"><span>✅</span> <strong>Dashboard verdict</strong> — every single one reading in its normal range</div>
      <div class="flex items-center gap-1.5"><span>🔥</span> <strong>Actual user impact</strong> — real, ongoing, and growing for 40 minutes before anyone notices</div>
    </div>
    <div class="not-prose mt-3 grid grid-cols-2 gap-2 text-xs">
      <div class="rounded border border-emerald-300 bg-emerald-50 p-1.5 dark:border-emerald-800 dark:bg-emerald-950">
        <p class="font-semibold text-emerald-600 dark:text-emerald-400">What the dashboards say</p>
        CPU 42%. Memory 61%. Disk 55%. Network normal. Nothing to see here.
      </div>
      <div class="rounded border border-rose-300 bg-rose-50 p-1.5 dark:border-rose-800 dark:bg-rose-950">
        <p class="font-semibold text-rose-600 dark:text-rose-400">What users experience</p>
        1 in 6 requests fail. Nobody paged, because nothing "abnormal" fired.
      </div>
    </div>

  </Fragment>

An on-call engineer opens the infrastructure dashboard during a routine
check: CPU, memory, disk, network — twelve panels across four nodes, every
one of them green. By every resource metric the team tracks, the cluster
looks perfectly healthy. Meanwhile, a support channel is quietly filling
up with "checkout keeps failing" messages. The engineer only finds the
problem by accident, tailing raw logs — one node's disk is fine in
aggregate, but a single overloaded connection pool on that node is
rejecting roughly one in six requests. No resource metric ever crossed a
threshold. No alert fired. The infrastructure was, by every number the
team was watching, "healthy" the entire time users were failing.

| Signal watched                     | Reading during the incident | Would it have caught this? |
| ---------------------------------- | --------------------------- | -------------------------- |
| CPU utilization (per node)         | 38-46%, normal              | No                         |
| Memory utilization (per node)      | 55-64%, normal              | No                         |
| Disk usage                         | 50-58%, normal              | No                         |
| Network throughput                 | within normal bounds        | No                         |
| Request success rate (user-facing) | dropped from 99.95% to 83%  | **Yes — immediately**      |

**Twelve raw resource dashboards, all green, for the entire 40 minutes
real users were failing.** The gap wasn't a missing dashboard — the team
had plenty. It was that none of those dashboards measured the thing that
actually matters: whether the infrastructure was successfully doing its
job for the people depending on it. A node can look perfectly healthy on
every resource chart and still be failing the one metric a user actually
experiences.

</Scenario>

<LevelIntro
  items={[
    "SLIs, SLOs, and SLAs — a measurable signal, a target, and a contract",
    "The error budget: an SLO's target is a spendable allowance, not just a pass/fail line",
    "Alerting on user-facing symptoms instead of every raw resource metric",
  ]}
/>

- **SLI (Service Level Indicator)** — a concrete, measurable signal that
  reflects whether infrastructure is doing its job: request success rate,
  a latency percentile, node availability. Not a resource metric like "CPU
  at 42%" — a signal tied to actual outcomes.
- **SLO (Service Level Objective)** — a target threshold on an SLI that a
  team commits to internally, e.g. "99.9% of requests succeed over a
  rolling 30 days." The SLO is what turns a raw number into a decision
  boundary: above it, things are fine; approaching or below it, that's a
  real signal to act on.
- **SLA (Service Level Agreement)** — an external, often contractual
  commitment, usually set looser than the internal SLO, with real
  consequences (refunds, penalties) for breaching it. Covered briefly here
  — it's a business commitment layered on top of SLOs, not this unit's
  main focus.
- **Error budget** — an SLO implies exactly how much failure is allowed.
  99.9% availability over 30 days permits roughly 43 minutes of downtime
  that month. That budget is a real, spendable resource for deciding how
  aggressively to ship risky changes — not just a compliance number to
  report on.
- **Symptom-based alerting** — alerting on the SLI itself (the user-facing
  signal burning through the error budget), not on every raw resource
  metric individually. This is what would have caught this scenario's
  incident: none of the 12 resource panels crossed a threshold, but
  request success rate dropping from 99.95% to 83% is an unmistakable,
  single, unambiguous signal.

This unit's lens is **infrastructure itself** — nodes, clusters, network,
resource saturation, deploy pipeline health — as distinct from
application-level logging and tracing, which is its own well-covered
topic elsewhere. The question here isn't "what did the code do," it's
"is the infrastructure underneath actually serving users the way it's
supposed to."

**Key terms:** SLI, SLO, SLA, error budget, burn rate, symptom-based
alerting, alert fatigue, resource metric vs. user-facing signal.
