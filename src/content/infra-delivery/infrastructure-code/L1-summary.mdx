---
title: "L1 — How do we change infrastructure without clicking through a console? (Infrastructure as Code)"
---

import Scenario from "../../../components/Scenario.astro";
import LevelIntro from "../../../components/LevelIntro.astro";

<Scenario label="Someone resized a server during an incident. Nobody wrote it down.">
  <Fragment slot="facts">
    <div class="not-prose flex flex-col gap-2 text-sm text-slate-700 dark:text-slate-300">
      <div class="flex items-center gap-1.5"><span>🔥</span> <strong>Incident</strong> — on-call resizes `web-1` from small to xlarge in the console</div>
      <div class="flex items-center gap-1.5"><span>✅</span> <strong>Resolved</strong> — nobody documents the change</div>
      <div class="flex items-center gap-1.5"><span>💥</span> <strong>Next deploy</strong> — a teammate's routine deploy resizes `web-1` back to small</div>
    </div>
  </Fragment>

An on-call engineer resizes a struggling server from `small` to
`xlarge` directly in the cloud console to survive a traffic spike.
The incident resolves. Nobody writes down what they did. A week
later, a teammate runs a routine deployment and — with no ill
intent, following the documented process exactly — the server gets
resized back down to `small`, and the site falls over again the next
time traffic spikes. **Nobody did anything wrong by their own
process. What's actually missing that let a correct, well-intentioned
action from one week undo a correct, well-intentioned action from
another?**

</Scenario>

<LevelIntro
  items={[
    "What Infrastructure as Code actually declares",
    "Declared config, state file, and actual cloud as three separate pictures",
    "Key terms: plan, apply, drift, idempotency",
  ]}
/>

## The shape of the problem

- Clicking through a console makes a change **real but invisible** —
  it takes effect immediately, but nothing records that it happened,
  why, or that it should stay that way.
- The teammate's deploy wasn't wrong: whatever process they followed
  didn't know about the console change, because the console change
  never became part of any process — it lived only in the actual
  running infrastructure, not in anything anyone else could read.
- **Infrastructure as Code (IaC)** means describing the desired
  infrastructure state in a file, checked into version control like
  any other code (a shared history like Git), and letting a tool compute
  and apply the difference between what's declared and what's currently
  running — so "what should this look like" has exactly one source of
  truth: the place everyone looks to know what should exist, not one
  source of truth per person who happened to touch the console.

Keep three pictures separate as you read the rest of the unit:

| Picture         | What it means in this unit                                 |
| --------------- | ---------------------------------------------------------- |
| Declared config | The file that says what should exist                       |
| State file      | The tool's memory of what it last created or updated       |
| Actual cloud    | The real servers/databases/network rules running right now |

## Key terms

- **Declarative configuration** — describing the desired end state
  ("this server should be size `large`"), not the steps to get there —
  the tool figures out what needs to change.
- **State file** — the IaC tool's own record of what it last applied,
  used to compute what's different the next time a change is planned.
- **Plan** — a computed list of the specific create/update/delete
  actions needed to make the actual infrastructure match the declared
  configuration, shown before anything is changed.
- **Apply** — actually executing a plan, making the real infrastructure
  match the declared state.
- **Drift** — infrastructure that no longer matches its state file,
  usually because someone changed it directly (through a console, a
  CLI command, or another tool) instead of through the declared
  configuration.
- **Idempotency** — applying the same declared configuration twice
  produces the same result both times; the second apply is a no-op
  because nothing has changed since the last one.

## What this unit covers

L2 works through the plan → apply cycle and what a state file
actually tracks, plus what makes an operation idempotent and why
that property matters for a tool that might get re-run after a
partial failure. L3 builds a real, working plan/apply engine in
code — computing create/update/delete diffs, applying them to a
simulated cloud, verifying idempotency on a repeated apply, and
detecting the exact kind of drift from the opening scenario when a
resource is changed outside the tool.
