{
  "items": [
    {
      "type": "quiz",
      "id": "progressive-delivery-def-1",
      "poolId": "progressive-delivery-def-pool",
      "level": 1,
      "prompt": "What is \"progressive delivery\"?",
      "choices": [
        "The general practice of rolling out a change gradually and monitoring at each stage, rather than shipping to all users at once",
        "A specific brand of continuous integration software",
        "The practice of writing detailed release notes for every deploy",
        "A deployment strategy that always requires manual approval from a director"
      ],
      "correctIndex": 0,
      "explanation": "Progressive delivery is specifically about gradual, monitored rollout — not a specific software product, documentation practice, or approval hierarchy."
    },
    {
      "type": "quiz",
      "id": "progressive-delivery-def-2",
      "poolId": "progressive-delivery-def-pool",
      "level": 1,
      "prompt": "Does progressive delivery prevent bugs that only surface under real production traffic?",
      "choices": [
        "No — it doesn't prevent this category of bug; it changes how many users are exposed to it before it's caught",
        "Yes — progressive delivery guarantees such bugs are caught before affecting any user",
        "No, and it provides no benefit at all for this kind of bug",
        "Yes, but only for bugs related to database performance specifically"
      ],
      "correctIndex": 0,
      "explanation": "L1 is explicit that progressive delivery doesn't prevent production-only bugs — its value is limiting exposure, not prevention."
    },
    {
      "type": "quiz",
      "id": "canary-def-1",
      "poolId": "canary-def-pool",
      "level": 1,
      "prompt": "What is a \"canary deployment\"?",
      "choices": [
        "Routing a small, defined percentage of production traffic to a new version, then expanding the percentage in stages if the canary shows no problems",
        "A deployment that only happens on weekends to minimize risk",
        "A deployment strategy that requires two separate teams to approve",
        "A backup deployment that only activates if the primary deployment fails"
      ],
      "correctIndex": 0,
      "explanation": "Canary deployment is specifically about gradual traffic percentage — not scheduling, approval processes, or failover backups."
    },
    {
      "type": "quiz",
      "id": "canary-def-2",
      "poolId": "canary-def-pool",
      "level": 1,
      "prompt": "In the Scenario, would a canary deployment starting at 5% have prevented the bug from existing in the code?",
      "choices": [
        "No — it wouldn't prevent the bug from existing, but it would limit how many users encountered it before it was caught",
        "Yes — canary deployments are described as guaranteeing bug-free code before full rollout",
        "No, and canary deployments provide no benefit in this scenario at all",
        "Yes, but only if the canary stage lasts longer than 24 hours"
      ],
      "correctIndex": 0,
      "explanation": "A canary deployment doesn't fix the underlying bug — it limits exposure to it while it's being detected, which is the core value described in L1."
    },
    {
      "type": "quiz",
      "id": "blue-green-def-1",
      "poolId": "blue-green-def-pool",
      "level": 1,
      "prompt": "What is \"blue-green deployment\"?",
      "choices": [
        "Running two complete, identical production environments, where traffic is switched entirely from one to the other, allowing an instant switch back",
        "A deployment strategy named for the two most common company brand colors",
        "A technique for gradually increasing traffic to a new version by percentage",
        "A monitoring dashboard color-coded by service health"
      ],
      "correctIndex": 0,
      "explanation": "Blue-green is specifically about two parallel environments and instant traffic switching — not branding, gradual percentage rollout, or dashboards."
    },
    {
      "type": "quiz",
      "id": "blue-green-def-2",
      "poolId": "blue-green-def-pool",
      "level": 1,
      "prompt": "Per L1, would blue-green deployment alone have limited how many users hit the Scenario's bug before it was noticed?",
      "choices": [
        "No — blue-green makes rollback fast once a bug is noticed, but doesn't limit how many users are exposed before that point",
        "Yes — blue-green deployment guarantees zero users are ever exposed to a new bug",
        "Yes, but only if the two environments run on identical hardware",
        "No, and blue-green deployment provides no benefit in the Scenario at all"
      ],
      "correctIndex": 0,
      "explanation": "L1/L2 distinguish blue-green's rollback-speed benefit from canary's exposure-limiting benefit — they solve different parts of the problem."
    },
    {
      "type": "quiz",
      "id": "feature-flag-def-1",
      "poolId": "feature-flag-def-pool",
      "level": 1,
      "prompt": "What is a \"feature flag\"?",
      "choices": [
        "A runtime toggle that controls whether a piece of code is active, allowing a feature to be turned off instantly without a new deploy",
        "A comment in source code marking a section as deprecated",
        "A flag passed to a build tool to enable verbose logging",
        "A visual marker in a UI indicating a feature is still in beta"
      ],
      "correctIndex": 0,
      "explanation": "A feature flag is specifically a runtime toggle independent of deploys — not a code comment, build flag, or UI badge."
    },
    {
      "type": "quiz",
      "id": "feature-flag-def-2",
      "poolId": "feature-flag-def-pool",
      "level": 1,
      "prompt": "How does a feature flag's mechanism differ from a canary deployment's mechanism?",
      "choices": [
        "A feature flag toggles whether code runs at all, independent of which deployed version is active, while canary controls what percentage of traffic reaches a different deployed version",
        "They are functionally identical mechanisms with different names",
        "A feature flag can only be used in staging environments, never in production",
        "A canary deployment requires feature flags to function at all"
      ],
      "correctIndex": 0,
      "explanation": "L1 draws a real mechanical distinction: feature flags gate code execution at runtime, while canary routes traffic between deployed versions — related but not the same lever."
    },
    {
      "type": "quiz",
      "id": "scenario-lesson-1",
      "poolId": "scenario-lesson-pool",
      "level": 1,
      "prompt": "In the Scenario, could the bug have reliably been caught by tests or staging before production?",
      "choices": [
        "No — the Scenario states it only showed up under real production traffic patterns that staging couldn't fully replicate",
        "Yes — the Scenario states the bug was actually caught in staging but ignored",
        "Yes, but only because the team skipped their staging environment entirely",
        "It's impossible to tell from the Scenario's facts whether staging could have caught it"
      ],
      "correctIndex": 0,
      "explanation": "The Scenario explicitly frames this as a bug category that only surfaces under real production conditions, not one that better testing alone would have caught."
    },
    {
      "type": "quiz",
      "id": "scenario-lesson-2",
      "poolId": "scenario-lesson-pool",
      "level": 1,
      "prompt": "Given that the bug wasn't reliably testable beforehand, what does L1 suggest was actually avoidable about the Scenario's incident?",
      "choices": [
        "Whether shipping to 100% of traffic at once, rather than a smaller percentage first, was necessary",
        "The existence of the bug itself, which L1 claims was fully preventable",
        "Nothing was avoidable — the Scenario describes a purely unavoidable incident",
        "The 20 minutes it took to diagnose the problem, which L1 claims should have been instantaneous"
      ],
      "correctIndex": 0,
      "explanation": "L1's closing question specifically targets the decision to ship to 100% of traffic at once, not the bug's existence or the exact diagnosis time."
    },
    {
      "type": "quiz",
      "id": "three-techniques-different-problems-1",
      "poolId": "three-techniques-different-problems-pool",
      "level": 2,
      "prompt": "Per L2's table, what does canary deployment control that blue-green deployment doesn't?",
      "choices": [
        "How many users are exposed to a new version before it's fully rolled out",
        "How fast a full rollback can happen once a bug is noticed",
        "Whether a specific piece of code runs at all",
        "The physical region where a service is hosted"
      ],
      "correctIndex": 0,
      "explanation": "L2's table attributes exposure control specifically to canary, not to blue-green (which controls rollback speed instead)."
    },
    {
      "type": "quiz",
      "id": "three-techniques-different-problems-2",
      "poolId": "three-techniques-different-problems-pool",
      "level": 2,
      "prompt": "Per L2, does blue-green deployment limit how many users hit a bug before someone notices and switches back?",
      "choices": [
        "No — L2 explicitly lists this as something blue-green doesn't solve",
        "Yes — limiting user exposure before detection is described as blue-green's core purpose",
        "Yes, but only for deployments smaller than a certain traffic volume",
        "No, and L2 states blue-green provides no safety benefit of any kind"
      ],
      "correctIndex": 0,
      "explanation": "L2's table explicitly lists 'doesn't limit how many users hit a bug before someone notices' under blue-green's column — that's canary's job, not blue-green's."
    },
    {
      "type": "quiz",
      "id": "staged-rollout-model-1",
      "poolId": "staged-rollout-model-pool",
      "level": 2,
      "prompt": "Per L2's diagram, what happens at each stage of a staged canary rollout?",
      "choices": [
        "A real health check gates whether the rollout advances to the next percentage or halts and rolls back",
        "The rollout automatically advances to the next stage after a fixed amount of time, with no health check",
        "Each stage requires a completely new deployment from scratch",
        "Traffic percentage decreases at each stage until reaching zero"
      ],
      "correctIndex": 0,
      "explanation": "L2's diagram shows each stage gated by a genuine health signal (healthy → advance, unhealthy → halt/roll back) — not a timer alone, a fresh redeploy, or decreasing percentages."
    },
    {
      "type": "quiz",
      "id": "staged-rollout-model-2",
      "poolId": "staged-rollout-model-pool",
      "level": 2,
      "prompt": "Per L2, why not start a canary rollout at 50% instead of 5%, to reach full rollout faster?",
      "choices": [
        "The point is limiting exposure while the signal is still uncertain — starting small means a bug caught at the first stage affects the fewest possible users",
        "Starting at 50% is technically impossible with canary deployment infrastructure",
        "Starting higher always produces a slower overall deployment regardless of monitoring",
        "There's no real reason — L2 states 50% would be equally safe as a starting point"
      ],
      "correctIndex": 0,
      "explanation": "L2 explicitly ties the small starting percentage to limiting exposure during the period of highest uncertainty — not to a technical limitation or an unexplained rule."
    },
    {
      "type": "quiz",
      "id": "failure-mode-l2-1",
      "poolId": "failure-mode-l2-pool",
      "level": 2,
      "prompt": "Per L2's failure modes, what's wrong with treating any one of canary, blue-green, or feature flags as covering all three purposes?",
      "choices": [
        "Each solves a different part of the problem — blue-green without canary still exposes 100% of traffic to a new bug immediately, just with a faster fix once found",
        "There's no real issue — any one of the three techniques is described as fully sufficient on its own",
        "This is only a problem for companies with fewer than 10 engineers",
        "The issue is purely about cost, not about actual risk coverage"
      ],
      "correctIndex": 0,
      "explanation": "L2 explicitly warns against treating the techniques as interchangeable — each addresses a distinct failure mode, and skipping one leaves that specific risk unaddressed."
    },
    {
      "type": "quiz",
      "id": "failure-mode-l2-2",
      "poolId": "failure-mode-l2-pool",
      "level": 2,
      "prompt": "Per L2's failure modes, what's the risk of a canary stage that only checks \"did the server crash\"?",
      "choices": [
        "It misses a subtle correctness bug that doesn't crash anything, just quietly returns wrong results — exactly the Scenario's failure mode",
        "There's no risk — crash-only monitoring is described as sufficient for any kind of bug",
        "The risk only applies to serverless deployments, not traditional servers",
        "Crash-only monitoring is actually described as overly cautious and unnecessary"
      ],
      "correctIndex": 0,
      "explanation": "L2 explicitly ties this failure mode back to the Scenario's own bug — a correctness issue that a crash-only check would never catch."
    },
    {
      "type": "code",
      "id": "assign-version-1",
      "poolId": "assign-version-pool",
      "level": 3,
      "prompt": "Fix assignVersion so it deterministically and consistently routes users whose hash bucket is below canaryPercent to \"new\". Across many users this approximates the rollout percentage, while keeping each user's assignment stable.",
      "starterCode": "function hashUserId(userId) {\n  let hash = 0;\n  for (let i = 0; i < userId.length; i++) {\n    hash = (hash * 31 + userId.charCodeAt(i)) >>> 0;\n  }\n  return hash;\n}\n\nfunction assignVersion(userId, canaryPercent) {\n  // TODO: compute bucket = hashUserId(userId) % 100\n  // TODO: return \"new\" if bucket < canaryPercent, otherwise \"old\"\n  return \"old\";\n}\n",
      "tests": [
        {
          "description": "at 0% canary, every user gets the old version",
          "expr": "expect(assignVersion(\"user-42\", 0)).toBe(\"old\")"
        },
        {
          "description": "at 100% canary, every user gets the new version",
          "expr": "expect(assignVersion(\"user-42\", 100)).toBe(\"new\")"
        },
        {
          "description": "the same user gets the same version every time at a fixed percentage",
          "expr": "expect(assignVersion(\"user-42\", 30)).toBe(assignVersion(\"user-42\", 30))"
        }
      ],
      "solution": "function hashUserId(userId) {\n  let hash = 0;\n  for (let i = 0; i < userId.length; i++) {\n    hash = (hash * 31 + userId.charCodeAt(i)) >>> 0;\n  }\n  return hash;\n}\n\nfunction assignVersion(userId, canaryPercent) {\n  const bucket = hashUserId(userId) % 100;\n  return bucket < canaryPercent ? \"new\" : \"old\";\n}\n",
      "explanation": "Hashing the user id into a fixed 0-99 bucket and comparing against the canary percentage gives both determinism (same input, same bucket, always) and percentage control by bucket threshold. Real user counts can be approximate, but the routing rule is stable and needs no per-user state.",
      "reference": "<div class=\"not-prose flex flex-col gap-2 text-sm text-slate-700 dark:text-slate-300\"><div class=\"flex items-center gap-1.5\"><span>🎲</span> <strong>Deterministic hash</strong> — same user id always maps to the same bucket</div><div class=\"flex items-center gap-1.5\"><span>📊</span> <strong>Threshold</strong> — moving it changes which buckets get routed to \"new\"</div></div><p><strong>Why:</strong> a canary rollout needs both properties at once — controllable exposure, and a stable per-user experience.</p>"
    },
    {
      "type": "code",
      "id": "assign-version-2",
      "poolId": "assign-version-pool",
      "level": 3,
      "prompt": "Fix assignVersion so it deterministically and consistently routes users whose hash bucket is below canaryPercent to \"new\". Across many users this approximates the rollout percentage, while keeping each user's assignment stable.",
      "starterCode": "function hashUserId(userId) {\n  let hash = 0;\n  for (let i = 0; i < userId.length; i++) {\n    hash = (hash * 31 + userId.charCodeAt(i)) >>> 0;\n  }\n  return hash;\n}\n\nfunction assignVersion(userId, canaryPercent) {\n  // TODO: compute bucket = hashUserId(userId) % 100\n  // TODO: return \"new\" if bucket < canaryPercent, otherwise \"old\"\n  return \"old\";\n}\n",
      "tests": [
        {
          "description": "at 0% canary, every user gets the old version",
          "expr": "expect(assignVersion(\"account-777\", 0)).toBe(\"old\")"
        },
        {
          "description": "at 100% canary, every user gets the new version",
          "expr": "expect(assignVersion(\"account-777\", 100)).toBe(\"new\")"
        },
        {
          "description": "the same user gets the same version every time at a fixed percentage",
          "expr": "expect(assignVersion(\"account-777\", 40)).toBe(assignVersion(\"account-777\", 40))"
        }
      ],
      "solution": "function hashUserId(userId) {\n  let hash = 0;\n  for (let i = 0; i < userId.length; i++) {\n    hash = (hash * 31 + userId.charCodeAt(i)) >>> 0;\n  }\n  return hash;\n}\n\nfunction assignVersion(userId, canaryPercent) {\n  const bucket = hashUserId(userId) % 100;\n  return bucket < canaryPercent ? \"new\" : \"old\";\n}\n",
      "explanation": "Same fix regardless of the id used: hashing into a fixed 0-99 bucket and comparing against the canary percentage gives deterministic, controllable per-user routing with no stored state. The exact user count can be approximate; the threshold rule is the important part.",
      "reference": "<div class=\"not-prose flex flex-col gap-2 text-sm text-slate-700 dark:text-slate-300\"><div class=\"flex items-center gap-1.5\"><span>🎲</span> <strong>Deterministic hash</strong> — same user id always maps to the same bucket</div><div class=\"flex items-center gap-1.5\"><span>📊</span> <strong>Threshold</strong> — moving it changes which buckets get routed to \"new\"</div></div><p><strong>Why:</strong> a canary rollout needs both properties at once — controllable exposure, and a stable per-user experience.</p>"
    },
    {
      "type": "code",
      "id": "should-halt-rollout-1",
      "poolId": "should-halt-rollout-pool",
      "level": 3,
      "prompt": "Fix shouldHaltRollout so it correctly computes the error rate as a percentage and compares it against the threshold, handling zero total requests safely.",
      "starterCode": "function shouldHaltRollout(errorCount, totalRequests, thresholdPercent) {\n  // TODO: if totalRequests is 0, return false (nothing to measure yet)\n  // TODO: otherwise, compute errorRate = (errorCount / totalRequests) * 100\n  // TODO: return true if errorRate exceeds thresholdPercent, false otherwise\n  return false;\n}\n",
      "tests": [
        {
          "description": "halts when the error rate exceeds the threshold (3% > 2%)",
          "expr": "expect(shouldHaltRollout(30, 1000, 2)).toBe(true)"
        },
        {
          "description": "does not halt when the error rate is within the threshold (1% <= 2%)",
          "expr": "expect(shouldHaltRollout(10, 1000, 2)).toBe(false)"
        },
        {
          "description": "does not halt when there are zero total requests yet",
          "expr": "expect(shouldHaltRollout(0, 0, 2)).toBe(false)"
        }
      ],
      "solution": "function shouldHaltRollout(errorCount, totalRequests, thresholdPercent) {\n  if (totalRequests === 0) return false;\n  const errorRate = (errorCount / totalRequests) * 100;\n  return errorRate > thresholdPercent;\n}\n",
      "explanation": "Converting to a rate (not an absolute count) is what makes this check meaningful at any canary size — 30 errors out of 1,000 canary requests is a real 3% signal, while 30 errors would mean something completely different out of a million requests.",
      "reference": "<div class=\"not-prose flex flex-col gap-2 text-sm text-slate-700 dark:text-slate-300\"><div class=\"flex items-center gap-1.5\"><span>🔢</span> <strong>Absolute count</strong> — meaningless without knowing the traffic volume it came from</div><div class=\"flex items-center gap-1.5\"><span>📈</span> <strong>Rate</strong> — comparable against a fixed threshold regardless of canary size</div></div><p><strong>Why:</strong> a small canary stage needs a rate-based check to catch a real problem without waiting for a large absolute error count.</p>"
    },
    {
      "type": "code",
      "id": "should-halt-rollout-2",
      "poolId": "should-halt-rollout-pool",
      "level": 3,
      "prompt": "Fix shouldHaltRollout so it correctly computes the error rate as a percentage and compares it against the threshold, handling zero total requests safely.",
      "starterCode": "function shouldHaltRollout(errorCount, totalRequests, thresholdPercent) {\n  // TODO: if totalRequests is 0, return false (nothing to measure yet)\n  // TODO: otherwise, compute errorRate = (errorCount / totalRequests) * 100\n  // TODO: return true if errorRate exceeds thresholdPercent, false otherwise\n  return false;\n}\n",
      "tests": [
        {
          "description": "halts when the error rate exceeds the threshold (3% > 1%)",
          "expr": "expect(shouldHaltRollout(150, 5000, 1)).toBe(true)"
        },
        {
          "description": "does not halt when the error rate is within the threshold (0.4% <= 1%)",
          "expr": "expect(shouldHaltRollout(20, 5000, 1)).toBe(false)"
        },
        {
          "description": "does not halt when there are zero total requests yet",
          "expr": "expect(shouldHaltRollout(0, 0, 1)).toBe(false)"
        }
      ],
      "solution": "function shouldHaltRollout(errorCount, totalRequests, thresholdPercent) {\n  if (totalRequests === 0) return false;\n  const errorRate = (errorCount / totalRequests) * 100;\n  return errorRate > thresholdPercent;\n}\n",
      "explanation": "Same rate-based check regardless of the specific numbers: converting counts to a percentage rate is what makes the threshold comparison meaningful at any canary size.",
      "reference": "<div class=\"not-prose flex flex-col gap-2 text-sm text-slate-700 dark:text-slate-300\"><div class=\"flex items-center gap-1.5\"><span>🔢</span> <strong>Absolute count</strong> — meaningless without knowing the traffic volume it came from</div><div class=\"flex items-center gap-1.5\"><span>📈</span> <strong>Rate</strong> — comparable against a fixed threshold regardless of canary size</div></div><p><strong>Why:</strong> a small canary stage needs a rate-based check to catch a real problem without waiting for a large absolute error count.</p>"
    }
  ]
}
