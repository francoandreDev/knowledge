---
title: "L1 — How do we ship a risky change without risking everyone? (progressive delivery: canary, blue-green, feature flags)"
---

import Scenario from "../../../components/Scenario.astro";

<Scenario label="A new recommendation algorithm shipped to 100% of traffic at once. A subtle bug hit every single user simultaneously.">
  <Fragment slot="facts">
    <div class="not-prose flex flex-col gap-2 text-sm text-slate-700 dark:text-slate-300">
      <div class="flex items-center gap-1.5"><span>🚀</span> <strong>The deploy</strong> — new recommendation algorithm shipped to 100% of production traffic at once</div>
      <div class="flex items-center gap-1.5"><span>🐛</span> <strong>The bug</strong> — a subtle edge case in the new algorithm returned broken recommendations for a common user segment</div>
      <div class="flex items-center gap-1.5"><span>💥</span> <strong>The blast radius</strong> — every single user hit the bug simultaneously, for the full 20 minutes it took to detect and roll back</div>
    </div>
  </Fragment>

A team ships a new recommendation algorithm. It passed all tests and
staging checks, so it goes to 100% of production traffic in one
deploy. A subtle bug — an edge case only common user behavior
triggers — starts returning broken recommendations. It takes 20
minutes to notice, diagnose, and roll back. **The bug wasn't the kind
of thing tests or staging would reliably catch — it only showed up
under real production traffic patterns. Given that, was shipping to
100% of traffic at once ever actually necessary?**

</Scenario>

## The shape of the problem

- Some bugs genuinely can't be caught before production — they only
  surface under real user traffic, real data patterns, or real scale
  that staging can't fully replicate. Progressive delivery doesn't
  try to prevent this category of bug; it changes **how many users
  are exposed to it before it's caught**. Here, **traffic** means the
  real visits or requests arriving at the production system.
- A **canary deployment** routes a small percentage of real traffic
  to a new version first, monitors it, and only expands to more
  traffic if things look healthy — turning "100% of users hit the bug
  simultaneously" into "5% of users hit the bug, it's caught, and the
  other 95% never see it."
- **Blue-green deployment** and **feature flags** solve a related but
  distinct problem: not gradual traffic exposure, but instant,
  reliable rollback — having a known-good version ready to switch
  back to immediately, without waiting for a new deploy to finish.
  Blue-green switches traffic between two complete environments;
  a feature flag turns a specific behavior on or off inside the app.

## Key terms

- **Progressive delivery** — the general practice of rolling out a
  change gradually and monitoring at each stage, rather than shipping
  to all users at once.
- **Canary deployment** — routing a small, defined percentage of
  production traffic to a new version, then expanding the percentage
  in stages if the canary shows no problems.
- **Blue-green deployment** — running two complete, identical
  production environments ("blue" and "green"), where traffic is
  switched entirely from one to the other, allowing an instant switch
  back if the new version has a problem.
- **Feature flag** — a runtime toggle that controls whether a piece
  of code is active, allowing a feature to be turned off instantly
  without a new deploy, independent of which version of the code is
  actually running. "Runtime" means while the app is already running,
  not while code is being built or shipped.

## What this unit covers

L2 works through what each of the three techniques actually optimizes
for and why they're not interchangeable, plus the staged-rollout model
that most canary systems use in practice. L3 implements a
percentage-based canary router with a consistent-hashing scheme (so
the same user always gets the same version during a rollout — think
"put each user in the same numbered bucket every time") and an
error-rate monitor that would have automatically halted the Scenario's
rollout before it ever reached 100%.
