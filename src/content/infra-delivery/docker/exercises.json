{
  "items": [
    {
      "type": "quiz",
      "id": "image-vs-container-1",
      "poolId": "image-vs-container",
      "level": 1,
      "prompt": "What's the relationship between a Docker image and a Docker container?",
      "choices": [
        "They're the same thing, just different names used in different contexts",
        "An image is a built, immutable artifact (a stack of layers); a container is a running instance of an image — the same relationship as a program file and a running process",
        "A container is built first, then an image is created from it",
        "An image can only ever produce one container; running it a second time requires rebuilding"
      ],
      "correctIndex": 1,
      "explanation": "This is the foundational distinction: `docker build` produces an image once; `docker run` can start many independent containers from that same unchanged image, each with its own running process and writable layer on top.",
      "reference": "<div class='grid grid-cols-2 gap-2 text-xs'><div class=\"rounded border border-slate-300 p-1.5 dark:border-slate-600\"><p class='font-semibold text-slate-500'>📦 Image</p>built once, immutable</div><div class=\"rounded border border-sky-300 bg-sky-50 p-1.5 dark:border-sky-700 dark:bg-sky-950\"><p class='font-semibold text-sky-600'>▶️ Container</p>a running instance</div></div><p class='mt-2 text-xs text-slate-500 dark:text-slate-400'><strong>Why:</strong> the same image can produce many independent containers.</p>",
      "learnMore": "<h5 class='mt-0 mb-2 text-sm font-semibold text-slate-700 dark:text-slate-200'>Image vs. container</h5><p class='text-sm text-slate-600 dark:text-slate-300'>An image is a fixed, content-addressed artifact — a stack of filesystem layers plus metadata. A container is what you get when you actually run it.</p><table class='mt-2 w-full border-collapse text-xs'><thead><tr><th class='border-b border-slate-300 pb-1 text-left font-semibold text-slate-500 dark:border-slate-600'></th><th class='border-b border-slate-300 pb-1 text-left font-semibold text-slate-500 dark:border-slate-600'>Image</th><th class='border-b border-slate-300 pb-1 text-left font-semibold text-slate-500 dark:border-slate-600'>Container</th></tr></thead><tbody><tr><td class='py-1 pr-2 align-top'>Created by</td><td class='py-1 pr-2 align-top'>docker build</td><td class='py-1 pr-2 align-top'>docker run</td></tr><tr><td class='py-1 pr-2 align-top'>Mutable?</td><td class='py-1 pr-2 align-top'>No</td><td class='py-1 pr-2 align-top'>Yes — its own writable layer on top</td></tr><tr><td class='py-1 pr-2 align-top'>How many from one image?</td><td class='py-1 pr-2 align-top'>N/A</td><td class='py-1 pr-2 align-top'>Unlimited, independent instances</td></tr></tbody></table><div class=\"mt-3 rounded-md border border-amber-300 bg-amber-50 p-2 dark:border-amber-800 dark:bg-amber-950\"><p class='text-xs font-semibold text-amber-700 dark:text-amber-400'>Common mistake</p><p class='mt-0.5 text-xs text-amber-800 dark:text-amber-300'>Treating \"rebuild the image\" and \"restart the container\" as the same operation. Restarting a container just starts a fresh instance from the same unchanged image; nothing about the image itself is re-derived.</p></div>"
    },
    {
      "type": "quiz",
      "id": "image-vs-container-2",
      "poolId": "image-vs-container",
      "level": 1,
      "prompt": "You run `docker run myapp` three times in a row without rebuilding. What do you get?",
      "choices": [
        "An error — an image can only be run once",
        "Three independent containers, each a separate running instance of the exact same unchanged image",
        "The same single container, just restarted three times",
        "Three new images, one per run"
      ],
      "correctIndex": 1,
      "explanation": "`docker run` starts a new container instance every time it's invoked — the image itself is never modified or consumed by running it, so the same image can back any number of simultaneous, independent containers.",
      "reference": "<div class='grid grid-cols-2 gap-2 text-xs'><div class=\"rounded border border-slate-300 p-1.5 dark:border-slate-600\"><p class='font-semibold text-slate-500'>📦 Image</p>built once, immutable</div><div class=\"rounded border border-sky-300 bg-sky-50 p-1.5 dark:border-sky-700 dark:bg-sky-950\"><p class='font-semibold text-sky-600'>▶️ Container</p>a running instance</div></div><p class='mt-2 text-xs text-slate-500 dark:text-slate-400'><strong>Why:</strong> the same image can produce many independent containers.</p>",
      "learnMore": "<h5 class='mt-0 mb-2 text-sm font-semibold text-slate-700 dark:text-slate-200'>Image vs. container</h5><p class='text-sm text-slate-600 dark:text-slate-300'>An image is a fixed, content-addressed artifact — a stack of filesystem layers plus metadata. A container is what you get when you actually run it.</p><table class='mt-2 w-full border-collapse text-xs'><thead><tr><th class='border-b border-slate-300 pb-1 text-left font-semibold text-slate-500 dark:border-slate-600'></th><th class='border-b border-slate-300 pb-1 text-left font-semibold text-slate-500 dark:border-slate-600'>Image</th><th class='border-b border-slate-300 pb-1 text-left font-semibold text-slate-500 dark:border-slate-600'>Container</th></tr></thead><tbody><tr><td class='py-1 pr-2 align-top'>Created by</td><td class='py-1 pr-2 align-top'>docker build</td><td class='py-1 pr-2 align-top'>docker run</td></tr><tr><td class='py-1 pr-2 align-top'>Mutable?</td><td class='py-1 pr-2 align-top'>No</td><td class='py-1 pr-2 align-top'>Yes — its own writable layer on top</td></tr><tr><td class='py-1 pr-2 align-top'>How many from one image?</td><td class='py-1 pr-2 align-top'>N/A</td><td class='py-1 pr-2 align-top'>Unlimited, independent instances</td></tr></tbody></table><div class=\"mt-3 rounded-md border border-amber-300 bg-amber-50 p-2 dark:border-amber-800 dark:bg-amber-950\"><p class='text-xs font-semibold text-amber-700 dark:text-amber-400'>Common mistake</p><p class='mt-0.5 text-xs text-amber-800 dark:text-amber-300'>Treating \"rebuild the image\" and \"restart the container\" as the same operation. Restarting a container just starts a fresh instance from the same unchanged image; nothing about the image itself is re-derived.</p></div>"
    },
    {
      "type": "quiz",
      "id": "image-vs-container-3",
      "poolId": "image-vs-container",
      "level": 1,
      "prompt": "A container writes a temporary file, then the container is removed. Is that file part of the image now?",
      "choices": [
        "Yes, images are updated automatically by anything a container writes",
        "No — a container's writable layer is separate from the immutable image it started from; removing the container discards that layer, leaving the original image completely unchanged",
        "Only if the file was written to /tmp",
        "Only if the container ran as root"
      ],
      "correctIndex": 1,
      "explanation": "The image is immutable by design. A running container gets its own writable layer on top of the image's read-only layers — anything written there lives and dies with that specific container, never touching the image itself.",
      "reference": "<div class='grid grid-cols-2 gap-2 text-xs'><div class=\"rounded border border-slate-300 p-1.5 dark:border-slate-600\"><p class='font-semibold text-slate-500'>📦 Image</p>built once, immutable</div><div class=\"rounded border border-sky-300 bg-sky-50 p-1.5 dark:border-sky-700 dark:bg-sky-950\"><p class='font-semibold text-sky-600'>▶️ Container</p>a running instance</div></div><p class='mt-2 text-xs text-slate-500 dark:text-slate-400'><strong>Why:</strong> the same image can produce many independent containers.</p>",
      "learnMore": "<h5 class='mt-0 mb-2 text-sm font-semibold text-slate-700 dark:text-slate-200'>Image vs. container</h5><p class='text-sm text-slate-600 dark:text-slate-300'>An image is a fixed, content-addressed artifact — a stack of filesystem layers plus metadata. A container is what you get when you actually run it.</p><table class='mt-2 w-full border-collapse text-xs'><thead><tr><th class='border-b border-slate-300 pb-1 text-left font-semibold text-slate-500 dark:border-slate-600'></th><th class='border-b border-slate-300 pb-1 text-left font-semibold text-slate-500 dark:border-slate-600'>Image</th><th class='border-b border-slate-300 pb-1 text-left font-semibold text-slate-500 dark:border-slate-600'>Container</th></tr></thead><tbody><tr><td class='py-1 pr-2 align-top'>Created by</td><td class='py-1 pr-2 align-top'>docker build</td><td class='py-1 pr-2 align-top'>docker run</td></tr><tr><td class='py-1 pr-2 align-top'>Mutable?</td><td class='py-1 pr-2 align-top'>No</td><td class='py-1 pr-2 align-top'>Yes — its own writable layer on top</td></tr><tr><td class='py-1 pr-2 align-top'>How many from one image?</td><td class='py-1 pr-2 align-top'>N/A</td><td class='py-1 pr-2 align-top'>Unlimited, independent instances</td></tr></tbody></table><div class=\"mt-3 rounded-md border border-amber-300 bg-amber-50 p-2 dark:border-amber-800 dark:bg-amber-950\"><p class='text-xs font-semibold text-amber-700 dark:text-amber-400'>Common mistake</p><p class='mt-0.5 text-xs text-amber-800 dark:text-amber-300'>Treating \"rebuild the image\" and \"restart the container\" as the same operation. Restarting a container just starts a fresh instance from the same unchanged image; nothing about the image itself is re-derived.</p></div>"
    },
    {
      "type": "quiz",
      "id": "container-vs-vm-1",
      "poolId": "container-vs-vm",
      "level": 1,
      "prompt": "Why do containers typically start in milliseconds-to-seconds, while a VM takes tens of seconds to minutes?",
      "choices": [
        "Containers are always smaller in disk size than VM images",
        "Containers share the host machine's kernel; a VM has to boot an entire separate guest operating system, kernel included, before anything can run",
        "Containers don't actually start a process, so there's nothing to boot",
        "VMs always run on slower hardware than containers"
      ],
      "correctIndex": 1,
      "explanation": "The speed gap traces to what each has to bring up: a container reuses the host's already-running kernel, so starting one is closer to starting a process than booting a machine — a VM genuinely has to boot a full guest OS first.",
      "reference": "<div class='grid grid-cols-2 gap-2 text-xs'><div class=\"rounded border border-slate-300 p-1.5 dark:border-slate-600\"><p class='font-semibold text-slate-500'>🖥️ VM</p>own guest OS + kernel, boots fully</div><div class=\"rounded border border-sky-300 bg-sky-50 p-1.5 dark:border-sky-700 dark:bg-sky-950\"><p class='font-semibold text-sky-600'>📦 Container</p>shares host kernel, starts like a process</div></div><p class='mt-2 text-xs text-slate-500 dark:text-slate-400'><strong>Trade-off:</strong> faster, lighter — at a narrower isolation boundary.</p>",
      "learnMore": "<h5 class='mt-0 mb-2 text-sm font-semibold text-slate-700 dark:text-slate-200'>Containers vs. virtual machines</h5><p class='text-sm text-slate-600 dark:text-slate-300'>Both isolate an app from the host, but at different layers of the stack — and that's a trade-off, not a strict improvement in either direction.</p><table class='mt-2 w-full border-collapse text-xs'><thead><tr><th class='border-b border-slate-300 pb-1 text-left font-semibold text-slate-500 dark:border-slate-600'></th><th class='border-b border-slate-300 pb-1 text-left font-semibold text-slate-500 dark:border-slate-600'>VM</th><th class='border-b border-slate-300 pb-1 text-left font-semibold text-slate-500 dark:border-slate-600'>Container</th></tr></thead><tbody><tr><td class='py-1 pr-2 align-top'>Kernel</td><td class='py-1 pr-2 align-top'>Own guest kernel</td><td class='py-1 pr-2 align-top'>Shares host kernel</td></tr><tr><td class='py-1 pr-2 align-top'>Startup time</td><td class='py-1 pr-2 align-top'>Tens of seconds to minutes</td><td class='py-1 pr-2 align-top'>Milliseconds to seconds</td></tr><tr><td class='py-1 pr-2 align-top'>Isolation boundary</td><td class='py-1 pr-2 align-top'>Deeper (full OS)</td><td class='py-1 pr-2 align-top'>Narrower (shared kernel)</td></tr></tbody></table><div class=\"mt-3 rounded-md border border-amber-300 bg-amber-50 p-2 dark:border-amber-800 dark:bg-amber-950\"><p class='text-xs font-semibold text-amber-700 dark:text-amber-400'>Common mistake</p><p class='mt-0.5 text-xs text-amber-800 dark:text-amber-300'>Treating containers as \"just lightweight VMs.\" The mechanism is fundamentally different — sharing a kernel is what buys the speed, and it's also exactly what makes the isolation boundary narrower.</p></div>"
    },
    {
      "type": "quiz",
      "id": "container-vs-vm-2",
      "poolId": "container-vs-vm",
      "level": 1,
      "prompt": "What's the actual trade-off a team accepts when choosing containers over VMs for isolation?",
      "choices": [
        "There's no trade-off — containers are strictly better in every dimension",
        "Faster startup and lower overhead, in exchange for a narrower isolation boundary since the host kernel is shared rather than virtualized",
        "Containers are more isolated than VMs, but slower to start",
        "VMs can't run Linux workloads at all"
      ],
      "correctIndex": 1,
      "explanation": "Sharing the host kernel is exactly what makes containers fast and light — and exactly what makes their isolation boundary narrower than a VM's, which virtualizes a whole separate OS. It's a genuine trade-off, not a flaw in containers.",
      "reference": "<div class='grid grid-cols-2 gap-2 text-xs'><div class=\"rounded border border-slate-300 p-1.5 dark:border-slate-600\"><p class='font-semibold text-slate-500'>🖥️ VM</p>own guest OS + kernel, boots fully</div><div class=\"rounded border border-sky-300 bg-sky-50 p-1.5 dark:border-sky-700 dark:bg-sky-950\"><p class='font-semibold text-sky-600'>📦 Container</p>shares host kernel, starts like a process</div></div><p class='mt-2 text-xs text-slate-500 dark:text-slate-400'><strong>Trade-off:</strong> faster, lighter — at a narrower isolation boundary.</p>",
      "learnMore": "<h5 class='mt-0 mb-2 text-sm font-semibold text-slate-700 dark:text-slate-200'>Containers vs. virtual machines</h5><p class='text-sm text-slate-600 dark:text-slate-300'>Both isolate an app from the host, but at different layers of the stack — and that's a trade-off, not a strict improvement in either direction.</p><table class='mt-2 w-full border-collapse text-xs'><thead><tr><th class='border-b border-slate-300 pb-1 text-left font-semibold text-slate-500 dark:border-slate-600'></th><th class='border-b border-slate-300 pb-1 text-left font-semibold text-slate-500 dark:border-slate-600'>VM</th><th class='border-b border-slate-300 pb-1 text-left font-semibold text-slate-500 dark:border-slate-600'>Container</th></tr></thead><tbody><tr><td class='py-1 pr-2 align-top'>Kernel</td><td class='py-1 pr-2 align-top'>Own guest kernel</td><td class='py-1 pr-2 align-top'>Shares host kernel</td></tr><tr><td class='py-1 pr-2 align-top'>Startup time</td><td class='py-1 pr-2 align-top'>Tens of seconds to minutes</td><td class='py-1 pr-2 align-top'>Milliseconds to seconds</td></tr><tr><td class='py-1 pr-2 align-top'>Isolation boundary</td><td class='py-1 pr-2 align-top'>Deeper (full OS)</td><td class='py-1 pr-2 align-top'>Narrower (shared kernel)</td></tr></tbody></table><div class=\"mt-3 rounded-md border border-amber-300 bg-amber-50 p-2 dark:border-amber-800 dark:bg-amber-950\"><p class='text-xs font-semibold text-amber-700 dark:text-amber-400'>Common mistake</p><p class='mt-0.5 text-xs text-amber-800 dark:text-amber-300'>Treating containers as \"just lightweight VMs.\" The mechanism is fundamentally different — sharing a kernel is what buys the speed, and it's also exactly what makes the isolation boundary narrower.</p></div>"
    },
    {
      "type": "quiz",
      "id": "container-vs-vm-3",
      "poolId": "container-vs-vm",
      "level": 1,
      "prompt": "Does a container include its own kernel, the way a VM includes a guest OS with its own kernel?",
      "choices": [
        "Yes, every container ships a full kernel of its own",
        "No — a container has its own filesystem layers and process space, but runs on top of the host machine's existing kernel rather than bringing its own",
        "Only Windows containers include a kernel",
        "Only if the Dockerfile explicitly installs one"
      ],
      "correctIndex": 1,
      "explanation": "A container packages an application and its filesystem, not an operating system kernel — it relies on the host's kernel for process scheduling, memory management, and so on, which is the core mechanical difference from a VM.",
      "reference": "<div class='grid grid-cols-2 gap-2 text-xs'><div class=\"rounded border border-slate-300 p-1.5 dark:border-slate-600\"><p class='font-semibold text-slate-500'>🖥️ VM</p>own guest OS + kernel, boots fully</div><div class=\"rounded border border-sky-300 bg-sky-50 p-1.5 dark:border-sky-700 dark:bg-sky-950\"><p class='font-semibold text-sky-600'>📦 Container</p>shares host kernel, starts like a process</div></div><p class='mt-2 text-xs text-slate-500 dark:text-slate-400'><strong>Trade-off:</strong> faster, lighter — at a narrower isolation boundary.</p>",
      "learnMore": "<h5 class='mt-0 mb-2 text-sm font-semibold text-slate-700 dark:text-slate-200'>Containers vs. virtual machines</h5><p class='text-sm text-slate-600 dark:text-slate-300'>Both isolate an app from the host, but at different layers of the stack — and that's a trade-off, not a strict improvement in either direction.</p><table class='mt-2 w-full border-collapse text-xs'><thead><tr><th class='border-b border-slate-300 pb-1 text-left font-semibold text-slate-500 dark:border-slate-600'></th><th class='border-b border-slate-300 pb-1 text-left font-semibold text-slate-500 dark:border-slate-600'>VM</th><th class='border-b border-slate-300 pb-1 text-left font-semibold text-slate-500 dark:border-slate-600'>Container</th></tr></thead><tbody><tr><td class='py-1 pr-2 align-top'>Kernel</td><td class='py-1 pr-2 align-top'>Own guest kernel</td><td class='py-1 pr-2 align-top'>Shares host kernel</td></tr><tr><td class='py-1 pr-2 align-top'>Startup time</td><td class='py-1 pr-2 align-top'>Tens of seconds to minutes</td><td class='py-1 pr-2 align-top'>Milliseconds to seconds</td></tr><tr><td class='py-1 pr-2 align-top'>Isolation boundary</td><td class='py-1 pr-2 align-top'>Deeper (full OS)</td><td class='py-1 pr-2 align-top'>Narrower (shared kernel)</td></tr></tbody></table><div class=\"mt-3 rounded-md border border-amber-300 bg-amber-50 p-2 dark:border-amber-800 dark:bg-amber-950\"><p class='text-xs font-semibold text-amber-700 dark:text-amber-400'>Common mistake</p><p class='mt-0.5 text-xs text-amber-800 dark:text-amber-300'>Treating containers as \"just lightweight VMs.\" The mechanism is fundamentally different — sharing a kernel is what buys the speed, and it's also exactly what makes the isolation boundary narrower.</p></div>"
    },
    {
      "type": "quiz",
      "id": "registry-role-1",
      "poolId": "registry-role",
      "level": 1,
      "prompt": "What does a registry (like Docker Hub or a private registry) actually let a team do?",
      "choices": [
        "Run containers directly, without needing docker run",
        "Store and retrieve built images by name and tag — pushing publishes an image there, pulling retrieves it, which is what lets one CI build be reused across every environment",
        "Automatically rebuild an image whenever its source code changes",
        "Replace the need for a Dockerfile entirely"
      ],
      "correctIndex": 1,
      "explanation": "A registry is purely storage-and-retrieval for images. The actual build-once, run-everywhere guarantee comes from CI pushing exactly one built image and every environment pulling that same one — the registry is the handoff point that makes that possible.",
      "reference": "<div class=\"rounded border border-slate-300 bg-slate-50 p-2 text-xs dark:border-slate-600 dark:bg-slate-900\">CI builds once → <strong>pushes</strong> to registry → every environment <strong>pulls</strong> the identical image — not a rebuilt approximation.</div>",
      "learnMore": "<h5 class='mt-0 mb-2 text-sm font-semibold text-slate-700 dark:text-slate-200'>What a registry is for</h5><p class='text-sm text-slate-600 dark:text-slate-300'>A content-addressable store for images, keyed by name and tag and, underneath, by content digest.</p><table class='mt-2 w-full border-collapse text-xs'><thead><tr><th class='border-b border-slate-300 pb-1 text-left font-semibold text-slate-500 dark:border-slate-600'>Action</th><th class='border-b border-slate-300 pb-1 text-left font-semibold text-slate-500 dark:border-slate-600'>What happens</th></tr></thead><tbody><tr><td class='py-1 pr-2 align-top'>docker push</td><td class='py-1 pr-2 align-top'>Uploads layers the registry doesn't already have</td></tr><tr><td class='py-1 pr-2 align-top'>docker pull</td><td class='py-1 pr-2 align-top'>Downloads layers the local machine doesn't already have</td></tr></tbody></table><div class=\"mt-3 rounded-md border border-amber-300 bg-amber-50 p-2 dark:border-amber-800 dark:bg-amber-950\"><p class='text-xs font-semibold text-amber-700 dark:text-amber-400'>Common mistake</p><p class='mt-0.5 text-xs text-amber-800 dark:text-amber-300'>Assuming every environment rebuilds the image from the same Dockerfile. That reintroduces exactly the parity gap Docker exists to close — a rebuild can drift (different base image patch, different dependency resolution) even from identical source.</p></div>"
    },
    {
      "type": "quiz",
      "id": "registry-role-2",
      "poolId": "registry-role",
      "level": 1,
      "prompt": "Why does having CI push one built image to a registry (instead of each environment rebuilding it) matter for environment parity?",
      "choices": [
        "It doesn't matter — rebuilding from the same Dockerfile always produces identical results",
        "Because every environment that pulls the same tag/digest gets the byte-identical artifact CI tested, rather than a separately-built approximation that could drift",
        "It only matters for saving disk space",
        "Registries are required by Docker's license terms"
      ],
      "correctIndex": 1,
      "explanation": "A rebuild, even from unchanged source, can drift — a different base image patch version, a different dependency resolution at install time. Pulling the exact image CI already built and tested removes that whole class of drift.",
      "reference": "<div class=\"rounded border border-slate-300 bg-slate-50 p-2 text-xs dark:border-slate-600 dark:bg-slate-900\">CI builds once → <strong>pushes</strong> to registry → every environment <strong>pulls</strong> the identical image — not a rebuilt approximation.</div>",
      "learnMore": "<h5 class='mt-0 mb-2 text-sm font-semibold text-slate-700 dark:text-slate-200'>What a registry is for</h5><p class='text-sm text-slate-600 dark:text-slate-300'>A content-addressable store for images, keyed by name and tag and, underneath, by content digest.</p><table class='mt-2 w-full border-collapse text-xs'><thead><tr><th class='border-b border-slate-300 pb-1 text-left font-semibold text-slate-500 dark:border-slate-600'>Action</th><th class='border-b border-slate-300 pb-1 text-left font-semibold text-slate-500 dark:border-slate-600'>What happens</th></tr></thead><tbody><tr><td class='py-1 pr-2 align-top'>docker push</td><td class='py-1 pr-2 align-top'>Uploads layers the registry doesn't already have</td></tr><tr><td class='py-1 pr-2 align-top'>docker pull</td><td class='py-1 pr-2 align-top'>Downloads layers the local machine doesn't already have</td></tr></tbody></table><div class=\"mt-3 rounded-md border border-amber-300 bg-amber-50 p-2 dark:border-amber-800 dark:bg-amber-950\"><p class='text-xs font-semibold text-amber-700 dark:text-amber-400'>Common mistake</p><p class='mt-0.5 text-xs text-amber-800 dark:text-amber-300'>Assuming every environment rebuilds the image from the same Dockerfile. That reintroduces exactly the parity gap Docker exists to close — a rebuild can drift (different base image patch, different dependency resolution) even from identical source.</p></div>"
    },
    {
      "type": "quiz",
      "id": "layer-basics-1",
      "poolId": "layer-basics",
      "level": 1,
      "prompt": "What produces a new layer in a Docker image?",
      "choices": [
        "Each character typed in the Dockerfile",
        "Each instruction in the Dockerfile (FROM, COPY, RUN, etc.) produces its own layer in the resulting image",
        "Layers are created only when the image is pushed to a registry",
        "A single layer holds the entire image; Dockerfiles don't actually produce multiple layers"
      ],
      "correctIndex": 1,
      "explanation": "An image is a stack of layers, one per Dockerfile instruction. That's what makes per-instruction caching possible at all — each layer is independently cacheable because it corresponds to exactly one instruction's effect.",
      "reference": "<div class=\"rounded border border-slate-300 bg-slate-50 p-2 font-mono text-xs dark:border-slate-600 dark:bg-slate-900\">FROM ... <span class='text-slate-400'>← layer 1</span><br/>COPY ... <span class='text-slate-400'>← layer 2</span><br/>RUN ... <span class='text-slate-400'>← layer 3</span></div><p class='mt-2 text-xs text-slate-500 dark:text-slate-400'><strong>Why:</strong> one instruction, one layer, one cache key.</p>",
      "learnMore": "<h5 class='mt-0 mb-2 text-sm font-semibold text-slate-700 dark:text-slate-200'>Layers, one per instruction</h5><p class='text-sm text-slate-600 dark:text-slate-300'>Each layer is cached by a hash of its inputs: the previous layer plus the instruction itself, plus any files it copies in.</p><table class='mt-2 w-full border-collapse text-xs'><thead><tr><th class='border-b border-slate-300 pb-1 text-left font-semibold text-slate-500 dark:border-slate-600'>Instruction</th><th class='border-b border-slate-300 pb-1 text-left font-semibold text-slate-500 dark:border-slate-600'>Layer depends on</th></tr></thead><tbody><tr><td class='py-1 pr-2 align-top'>FROM</td><td class='py-1 pr-2 align-top'>The base image tag</td></tr><tr><td class='py-1 pr-2 align-top'>COPY x y</td><td class='py-1 pr-2 align-top'>x's content + previous layer</td></tr><tr><td class='py-1 pr-2 align-top'>RUN cmd</td><td class='py-1 pr-2 align-top'>Previous layer's output + the command text</td></tr></tbody></table><div class=\"mt-3 rounded-md border border-amber-300 bg-amber-50 p-2 dark:border-amber-800 dark:bg-amber-950\"><p class='text-xs font-semibold text-amber-700 dark:text-amber-400'>Common mistake</p><p class='mt-0.5 text-xs text-amber-800 dark:text-amber-300'>Thinking layer order is purely stylistic. It directly determines cache reuse — see L2's cache-invalidation cascade.</p></div>"
    },
    {
      "type": "quiz",
      "id": "layer-basics-2",
      "poolId": "layer-basics",
      "level": 1,
      "prompt": "A Dockerfile has 6 instructions. Roughly how many layers does the resulting image have?",
      "choices": [
        "Always exactly 1, regardless of instruction count",
        "Around 6 — one per instruction, each independently cacheable",
        "It depends only on the image's total file size, not the instruction count",
        "Layers only exist for RUN instructions, not FROM or COPY"
      ],
      "correctIndex": 1,
      "explanation": "Each Dockerfile instruction produces its own layer (FROM, WORKDIR, COPY, RUN — all of them), which is exactly what makes per-layer caching possible: Docker can reuse any layer whose own inputs are unchanged, without touching the rest.",
      "reference": "<div class=\"rounded border border-slate-300 bg-slate-50 p-2 font-mono text-xs dark:border-slate-600 dark:bg-slate-900\">FROM ... <span class='text-slate-400'>← layer 1</span><br/>COPY ... <span class='text-slate-400'>← layer 2</span><br/>RUN ... <span class='text-slate-400'>← layer 3</span></div><p class='mt-2 text-xs text-slate-500 dark:text-slate-400'><strong>Why:</strong> one instruction, one layer, one cache key.</p>",
      "learnMore": "<h5 class='mt-0 mb-2 text-sm font-semibold text-slate-700 dark:text-slate-200'>Layers, one per instruction</h5><p class='text-sm text-slate-600 dark:text-slate-300'>Each layer is cached by a hash of its inputs: the previous layer plus the instruction itself, plus any files it copies in.</p><table class='mt-2 w-full border-collapse text-xs'><thead><tr><th class='border-b border-slate-300 pb-1 text-left font-semibold text-slate-500 dark:border-slate-600'>Instruction</th><th class='border-b border-slate-300 pb-1 text-left font-semibold text-slate-500 dark:border-slate-600'>Layer depends on</th></tr></thead><tbody><tr><td class='py-1 pr-2 align-top'>FROM</td><td class='py-1 pr-2 align-top'>The base image tag</td></tr><tr><td class='py-1 pr-2 align-top'>COPY x y</td><td class='py-1 pr-2 align-top'>x's content + previous layer</td></tr><tr><td class='py-1 pr-2 align-top'>RUN cmd</td><td class='py-1 pr-2 align-top'>Previous layer's output + the command text</td></tr></tbody></table><div class=\"mt-3 rounded-md border border-amber-300 bg-amber-50 p-2 dark:border-amber-800 dark:bg-amber-950\"><p class='text-xs font-semibold text-amber-700 dark:text-amber-400'>Common mistake</p><p class='mt-0.5 text-xs text-amber-800 dark:text-amber-300'>Thinking layer order is purely stylistic. It directly determines cache reuse — see L2's cache-invalidation cascade.</p></div>"
    },
    {
      "type": "quiz",
      "id": "layer-basics-3",
      "poolId": "layer-basics",
      "level": 1,
      "prompt": "What is a layer's cache key based on?",
      "choices": [
        "A random value generated fresh on every build",
        "A hash of that layer's actual inputs — the previous layer's output, the instruction itself, and the content of any files it copies in",
        "The current date and time",
        "The name of the developer who wrote the Dockerfile"
      ],
      "correctIndex": 1,
      "explanation": "Content-based hashing is what makes the cache correct, not just fast: a layer only reuses its cached result when its actual inputs are provably unchanged, never based on something incidental like a timestamp.",
      "reference": "<div class=\"rounded border border-slate-300 bg-slate-50 p-2 font-mono text-xs dark:border-slate-600 dark:bg-slate-900\">FROM ... <span class='text-slate-400'>← layer 1</span><br/>COPY ... <span class='text-slate-400'>← layer 2</span><br/>RUN ... <span class='text-slate-400'>← layer 3</span></div><p class='mt-2 text-xs text-slate-500 dark:text-slate-400'><strong>Why:</strong> one instruction, one layer, one cache key.</p>",
      "learnMore": "<h5 class='mt-0 mb-2 text-sm font-semibold text-slate-700 dark:text-slate-200'>Layers, one per instruction</h5><p class='text-sm text-slate-600 dark:text-slate-300'>Each layer is cached by a hash of its inputs: the previous layer plus the instruction itself, plus any files it copies in.</p><table class='mt-2 w-full border-collapse text-xs'><thead><tr><th class='border-b border-slate-300 pb-1 text-left font-semibold text-slate-500 dark:border-slate-600'>Instruction</th><th class='border-b border-slate-300 pb-1 text-left font-semibold text-slate-500 dark:border-slate-600'>Layer depends on</th></tr></thead><tbody><tr><td class='py-1 pr-2 align-top'>FROM</td><td class='py-1 pr-2 align-top'>The base image tag</td></tr><tr><td class='py-1 pr-2 align-top'>COPY x y</td><td class='py-1 pr-2 align-top'>x's content + previous layer</td></tr><tr><td class='py-1 pr-2 align-top'>RUN cmd</td><td class='py-1 pr-2 align-top'>Previous layer's output + the command text</td></tr></tbody></table><div class=\"mt-3 rounded-md border border-amber-300 bg-amber-50 p-2 dark:border-amber-800 dark:bg-amber-950\"><p class='text-xs font-semibold text-amber-700 dark:text-amber-400'>Common mistake</p><p class='mt-0.5 text-xs text-amber-800 dark:text-amber-300'>Thinking layer order is purely stylistic. It directly determines cache reuse — see L2's cache-invalidation cascade.</p></div>"
    },
    {
      "type": "quiz",
      "id": "multistage-goal-1",
      "poolId": "multistage-goal",
      "level": 1,
      "prompt": "What's the purpose of a multi-stage Dockerfile build?",
      "choices": [
        "It's purely a stylistic convention with no functional effect",
        "One stage can use a full build toolchain (compilers, dev dependencies) while a second, separate stage produces the final image with only what's needed to run — build tools never end up in the shipped image, reducing size and attack surface",
        "It allows a single Dockerfile to build images for multiple different applications at once",
        "It's required whenever a Dockerfile has more than 5 instructions"
      ],
      "correctIndex": 1,
      "explanation": "Multi-stage builds separate 'what's needed to build' from 'what's needed to run' — only files explicitly pulled across with COPY --from=<stage> make it into the final image, so compilers and dev tooling from the build stage never ship in production.",
      "reference": "<div class='grid grid-cols-2 gap-2 text-xs'><div class=\"rounded border border-amber-300 bg-amber-50 p-1.5 dark:border-amber-800 dark:bg-amber-950\"><p class='font-semibold text-amber-700 dark:text-amber-400'>Build stage</p>compiler, dev deps, raw source</div><div class=\"rounded border border-emerald-300 bg-emerald-50 p-1.5 dark:border-emerald-800 dark:bg-emerald-950\"><p class='font-semibold text-emerald-600 dark:text-emerald-400'>Runtime stage</p>only what COPY --from pulled across</div></div>",
      "learnMore": "<h5 class='mt-0 mb-2 text-sm font-semibold text-slate-700 dark:text-slate-200'>Why split build from runtime</h5><p class='text-sm text-slate-600 dark:text-slate-300'>A compiler or dev tool sitting in a production image is one more thing that could be exploited if the container is ever compromised, for zero runtime benefit — multi-stage builds remove that for free.</p><table class='mt-2 w-full border-collapse text-xs'><thead><tr><th class='border-b border-slate-300 pb-1 text-left font-semibold text-slate-500 dark:border-slate-600'>Benefit</th><th class='border-b border-slate-300 pb-1 text-left font-semibold text-slate-500 dark:border-slate-600'>Why</th></tr></thead><tbody><tr><td class='py-1 pr-2 align-top'>Smaller image</td><td class='py-1 pr-2 align-top'>Less to pull, less disk</td></tr><tr><td class='py-1 pr-2 align-top'>Smaller attack surface</td><td class='py-1 pr-2 align-top'>No compiler/dev tooling shipped to production</td></tr></tbody></table><div class=\"mt-3 rounded-md border border-amber-300 bg-amber-50 p-2 dark:border-amber-800 dark:bg-amber-950\"><p class='text-xs font-semibold text-amber-700 dark:text-amber-400'>Common mistake</p><p class='mt-0.5 text-xs text-amber-800 dark:text-amber-300'>Assuming multi-stage is only about image size. The attack-surface reduction matters just as much, and is easy to overlook.</p></div>"
    },
    {
      "type": "quiz",
      "id": "multistage-goal-2",
      "poolId": "multistage-goal",
      "level": 1,
      "prompt": "A Dockerfile compiles TypeScript in one stage and copies only the compiled `dist/` output into a second, final stage. Why not just ship the single-stage version that has everything?",
      "choices": [
        "There's no real reason — both approaches produce equally sized, equally secure images",
        "The single-stage image would carry the TypeScript compiler and dev dependencies into production for no runtime benefit — extra size and extra attack surface with nothing gained",
        "Single-stage Dockerfiles cannot use COPY at all",
        "TypeScript specifically cannot be built without multi-stage Dockerfiles"
      ],
      "correctIndex": 1,
      "explanation": "The compiled output is all the running app needs — the compiler and dev dependencies that produced it serve no purpose once compilation is done, but a single-stage build would ship them anyway.",
      "reference": "<div class='grid grid-cols-2 gap-2 text-xs'><div class=\"rounded border border-amber-300 bg-amber-50 p-1.5 dark:border-amber-800 dark:bg-amber-950\"><p class='font-semibold text-amber-700 dark:text-amber-400'>Build stage</p>compiler, dev deps, raw source</div><div class=\"rounded border border-emerald-300 bg-emerald-50 p-1.5 dark:border-emerald-800 dark:bg-emerald-950\"><p class='font-semibold text-emerald-600 dark:text-emerald-400'>Runtime stage</p>only what COPY --from pulled across</div></div>",
      "learnMore": "<h5 class='mt-0 mb-2 text-sm font-semibold text-slate-700 dark:text-slate-200'>Why split build from runtime</h5><p class='text-sm text-slate-600 dark:text-slate-300'>A compiler or dev tool sitting in a production image is one more thing that could be exploited if the container is ever compromised, for zero runtime benefit — multi-stage builds remove that for free.</p><table class='mt-2 w-full border-collapse text-xs'><thead><tr><th class='border-b border-slate-300 pb-1 text-left font-semibold text-slate-500 dark:border-slate-600'>Benefit</th><th class='border-b border-slate-300 pb-1 text-left font-semibold text-slate-500 dark:border-slate-600'>Why</th></tr></thead><tbody><tr><td class='py-1 pr-2 align-top'>Smaller image</td><td class='py-1 pr-2 align-top'>Less to pull, less disk</td></tr><tr><td class='py-1 pr-2 align-top'>Smaller attack surface</td><td class='py-1 pr-2 align-top'>No compiler/dev tooling shipped to production</td></tr></tbody></table><div class=\"mt-3 rounded-md border border-amber-300 bg-amber-50 p-2 dark:border-amber-800 dark:bg-amber-950\"><p class='text-xs font-semibold text-amber-700 dark:text-amber-400'>Common mistake</p><p class='mt-0.5 text-xs text-amber-800 dark:text-amber-300'>Assuming multi-stage is only about image size. The attack-surface reduction matters just as much, and is easy to overlook.</p></div>"
    },
    {
      "type": "quiz",
      "id": "cache-cascade-1",
      "poolId": "cache-cascade",
      "level": 2,
      "prompt": "A Dockerfile has layers in order: FROM, WORKDIR, COPY manifest, RUN npm ci, COPY source. If `package.json` changes, which layers must rebuild?",
      "choices": [
        "Only the COPY manifest layer — the rest reuse their cache",
        "COPY manifest, RUN npm ci, and COPY source — the change cascades forward to every layer after the one whose inputs changed, because each depends on the one before it",
        "Every layer, including FROM and WORKDIR",
        "None — package.json changes don't affect the Docker build cache"
      ],
      "correctIndex": 1,
      "explanation": "Invalidation cascades forward, never backward: the manifest layer's cache key changes directly, and every layer after it depends on that layer's output, so npm ci and the final source copy both rebuild too — layers 1 and 2 (FROM, WORKDIR) are untouched since nothing about their own inputs changed.",
      "reference": "<div class=\"rounded border border-slate-300 bg-slate-50 p-2 font-mono text-xs dark:border-slate-600 dark:bg-slate-900\">FROM <span class='text-emerald-600'>cached</span><br/>WORKDIR <span class='text-emerald-600'>cached</span><br/>COPY manifest <span class='text-amber-600'>rebuild</span><br/>RUN npm ci <span class='text-amber-600'>rebuild (cascade)</span><br/>COPY source <span class='text-amber-600'>rebuild (cascade)</span></div>",
      "learnMore": "<h5 class='mt-0 mb-2 text-sm font-semibold text-slate-700 dark:text-slate-200'>Cascading invalidation</h5><p class='text-sm text-slate-600 dark:text-slate-300'>A layer rebuilds if its own inputs changed, OR if any earlier layer it depends on already rebuilt — the cascade only moves forward through the Dockerfile, never backward.</p><table class='mt-2 w-full border-collapse text-xs'><thead><tr><th class='border-b border-slate-300 pb-1 text-left font-semibold text-slate-500 dark:border-slate-600'>Changed input</th><th class='border-b border-slate-300 pb-1 text-left font-semibold text-slate-500 dark:border-slate-600'>Layers that rebuild</th></tr></thead><tbody><tr><td class='py-1 pr-2 align-top'>source-tree only</td><td class='py-1 pr-2 align-top'>Just the final COPY layer</td></tr><tr><td class='py-1 pr-2 align-top'>package.json</td><td class='py-1 pr-2 align-top'>Manifest copy → install → source copy</td></tr></tbody></table><div class=\"mt-3 rounded-md border border-amber-300 bg-amber-50 p-2 dark:border-amber-800 dark:bg-amber-950\"><p class='text-xs font-semibold text-amber-700 dark:text-amber-400'>Common mistake</p><p class='mt-0.5 text-xs text-amber-800 dark:text-amber-300'>Assuming only the exact layer that copies a changed file rebuilds. Everything after it in the Dockerfile rebuilds too, since each layer's cache key includes the output of the one before it.</p></div>"
    },
    {
      "type": "quiz",
      "id": "cache-cascade-2",
      "poolId": "cache-cascade",
      "level": 2,
      "prompt": "In the same 5-layer Dockerfile, only `server.js` (part of the final `COPY . .`) changes — `package.json` doesn't. Which layers rebuild?",
      "choices": [
        "All five layers, since Docker rebuilds top to bottom regardless of what changed",
        "Only the final COPY layer — everything before it (base image, workdir, manifest copy, npm ci) has unchanged inputs and reuses its cache",
        "Only the npm ci layer",
        "None — source file changes never trigger any rebuild"
      ],
      "correctIndex": 1,
      "explanation": "The cascade only starts at the layer whose own inputs actually changed. Since server.js is part of the source tree copied by the final COPY, only that layer's cache key changes — everything before it in the Dockerfile is untouched.",
      "reference": "<div class=\"rounded border border-slate-300 bg-slate-50 p-2 font-mono text-xs dark:border-slate-600 dark:bg-slate-900\">FROM <span class='text-emerald-600'>cached</span><br/>WORKDIR <span class='text-emerald-600'>cached</span><br/>COPY manifest <span class='text-amber-600'>rebuild</span><br/>RUN npm ci <span class='text-amber-600'>rebuild (cascade)</span><br/>COPY source <span class='text-amber-600'>rebuild (cascade)</span></div>",
      "learnMore": "<h5 class='mt-0 mb-2 text-sm font-semibold text-slate-700 dark:text-slate-200'>Cascading invalidation</h5><p class='text-sm text-slate-600 dark:text-slate-300'>A layer rebuilds if its own inputs changed, OR if any earlier layer it depends on already rebuilt — the cascade only moves forward through the Dockerfile, never backward.</p><table class='mt-2 w-full border-collapse text-xs'><thead><tr><th class='border-b border-slate-300 pb-1 text-left font-semibold text-slate-500 dark:border-slate-600'>Changed input</th><th class='border-b border-slate-300 pb-1 text-left font-semibold text-slate-500 dark:border-slate-600'>Layers that rebuild</th></tr></thead><tbody><tr><td class='py-1 pr-2 align-top'>source-tree only</td><td class='py-1 pr-2 align-top'>Just the final COPY layer</td></tr><tr><td class='py-1 pr-2 align-top'>package.json</td><td class='py-1 pr-2 align-top'>Manifest copy → install → source copy</td></tr></tbody></table><div class=\"mt-3 rounded-md border border-amber-300 bg-amber-50 p-2 dark:border-amber-800 dark:bg-amber-950\"><p class='text-xs font-semibold text-amber-700 dark:text-amber-400'>Common mistake</p><p class='mt-0.5 text-xs text-amber-800 dark:text-amber-300'>Assuming only the exact layer that copies a changed file rebuilds. Everything after it in the Dockerfile rebuilds too, since each layer's cache key includes the output of the one before it.</p></div>"
    },
    {
      "type": "quiz",
      "id": "cache-cascade-3",
      "poolId": "cache-cascade",
      "level": 2,
      "prompt": "Can invalidation ever cascade backward — a later layer changing causing an earlier layer to rebuild?",
      "choices": [
        "Yes, whenever the later layer is large enough",
        "No — a layer's cache key only depends on its own inputs and the output of layers before it, never layers after it, so invalidation only ever moves forward through the Dockerfile",
        "Yes, but only for RUN instructions",
        "It depends on the Docker version in use"
      ],
      "correctIndex": 1,
      "explanation": "Each layer's cache key is a function of the layer before it plus its own inputs — there's no dependency in the other direction, so a later instruction changing can never invalidate something earlier in the Dockerfile.",
      "reference": "<div class=\"rounded border border-slate-300 bg-slate-50 p-2 font-mono text-xs dark:border-slate-600 dark:bg-slate-900\">FROM <span class='text-emerald-600'>cached</span><br/>WORKDIR <span class='text-emerald-600'>cached</span><br/>COPY manifest <span class='text-amber-600'>rebuild</span><br/>RUN npm ci <span class='text-amber-600'>rebuild (cascade)</span><br/>COPY source <span class='text-amber-600'>rebuild (cascade)</span></div>",
      "learnMore": "<h5 class='mt-0 mb-2 text-sm font-semibold text-slate-700 dark:text-slate-200'>Cascading invalidation</h5><p class='text-sm text-slate-600 dark:text-slate-300'>A layer rebuilds if its own inputs changed, OR if any earlier layer it depends on already rebuilt — the cascade only moves forward through the Dockerfile, never backward.</p><table class='mt-2 w-full border-collapse text-xs'><thead><tr><th class='border-b border-slate-300 pb-1 text-left font-semibold text-slate-500 dark:border-slate-600'>Changed input</th><th class='border-b border-slate-300 pb-1 text-left font-semibold text-slate-500 dark:border-slate-600'>Layers that rebuild</th></tr></thead><tbody><tr><td class='py-1 pr-2 align-top'>source-tree only</td><td class='py-1 pr-2 align-top'>Just the final COPY layer</td></tr><tr><td class='py-1 pr-2 align-top'>package.json</td><td class='py-1 pr-2 align-top'>Manifest copy → install → source copy</td></tr></tbody></table><div class=\"mt-3 rounded-md border border-amber-300 bg-amber-50 p-2 dark:border-amber-800 dark:bg-amber-950\"><p class='text-xs font-semibold text-amber-700 dark:text-amber-400'>Common mistake</p><p class='mt-0.5 text-xs text-amber-800 dark:text-amber-300'>Assuming only the exact layer that copies a changed file rebuilds. Everything after it in the Dockerfile rebuilds too, since each layer's cache key includes the output of the one before it.</p></div>"
    },
    {
      "type": "quiz",
      "id": "layer-order-1",
      "poolId": "layer-order",
      "level": 2,
      "prompt": "Why is it a well-known best practice to COPY dependency manifests and run the install BEFORE copying the rest of the source code?",
      "choices": [
        "It has no real effect — the order of COPY instructions is purely cosmetic",
        "It maximizes how often the expensive install layer gets reused: a source-only change then only invalidates the final COPY, not the install step, since the install layer's cache key never depends on files it doesn't touch",
        "Docker requires manifests to be copied first or the build fails",
        "It makes the final image smaller, regardless of caching"
      ],
      "correctIndex": 1,
      "explanation": "Putting COPY . . before the install would make the install layer's cache key depend on the entire source tree, invalidating it on every source change — installing first (from just the manifest files) keeps that expensive step cached across ordinary source edits.",
      "reference": "<div class='grid grid-cols-2 gap-2 text-xs'><div class=\"rounded border border-rose-300 bg-rose-50 p-1.5 dark:border-rose-800 dark:bg-rose-950\"><p class='font-semibold text-rose-600 dark:text-rose-400'>COPY . . first</p>every source edit reruns install</div><div class=\"rounded border border-emerald-300 bg-emerald-50 p-1.5 dark:border-emerald-800 dark:bg-emerald-950\"><p class='font-semibold text-emerald-600 dark:text-emerald-400'>Manifest + install first</p>source edits skip install entirely</div></div>",
      "learnMore": "<h5 class='mt-0 mb-2 text-sm font-semibold text-slate-700 dark:text-slate-200'>Why instruction order has real performance consequences</h5><p class='text-sm text-slate-600 dark:text-slate-300'>Ordering doesn't change what the image contains — it changes what depends on what, which is exactly what determines cache reuse.</p><table class='mt-2 w-full border-collapse text-xs'><thead><tr><th class='border-b border-slate-300 pb-1 text-left font-semibold text-slate-500 dark:border-slate-600'>Order</th><th class='border-b border-slate-300 pb-1 text-left font-semibold text-slate-500 dark:border-slate-600'>Source-only edit cost</th></tr></thead><tbody><tr><td class='py-1 pr-2 align-top'>Manifest → install → source</td><td class='py-1 pr-2 align-top'>Fast — install stays cached</td></tr><tr><td class='py-1 pr-2 align-top'>Source (all) → install</td><td class='py-1 pr-2 align-top'>Slow — install reruns every time</td></tr></tbody></table><div class=\"mt-3 rounded-md border border-amber-300 bg-amber-50 p-2 dark:border-amber-800 dark:bg-amber-950\"><p class='text-xs font-semibold text-amber-700 dark:text-amber-400'>Common mistake</p><p class='mt-0.5 text-xs text-amber-800 dark:text-amber-300'>Treating this as a micro-optimization. On active development, this ordering is the difference between a multi-second and a near-instant rebuild on every single edit.</p></div>"
    },
    {
      "type": "quiz",
      "id": "layer-order-2",
      "poolId": "layer-order",
      "level": 2,
      "prompt": "A teammate writes `COPY . .` immediately after `FROM`, then installs dependencies afterward. What's the practical consequence during active development?",
      "choices": [
        "None — Docker is smart enough to skip the install regardless of instruction order",
        "Every single source edit, even one unrelated to dependencies, invalidates the install layer's cache key, forcing a full reinstall on every rebuild",
        "The image simply fails to build",
        "It only matters the first time the image is built, never afterward"
      ],
      "correctIndex": 1,
      "explanation": "Once COPY . . runs before the install, the install layer's cache key includes the entire source tree's content — any source edit changes that key, and the expensive install step reruns even though nothing about the actual dependencies changed.",
      "reference": "<div class='grid grid-cols-2 gap-2 text-xs'><div class=\"rounded border border-rose-300 bg-rose-50 p-1.5 dark:border-rose-800 dark:bg-rose-950\"><p class='font-semibold text-rose-600 dark:text-rose-400'>COPY . . first</p>every source edit reruns install</div><div class=\"rounded border border-emerald-300 bg-emerald-50 p-1.5 dark:border-emerald-800 dark:bg-emerald-950\"><p class='font-semibold text-emerald-600 dark:text-emerald-400'>Manifest + install first</p>source edits skip install entirely</div></div>",
      "learnMore": "<h5 class='mt-0 mb-2 text-sm font-semibold text-slate-700 dark:text-slate-200'>Why instruction order has real performance consequences</h5><p class='text-sm text-slate-600 dark:text-slate-300'>Ordering doesn't change what the image contains — it changes what depends on what, which is exactly what determines cache reuse.</p><table class='mt-2 w-full border-collapse text-xs'><thead><tr><th class='border-b border-slate-300 pb-1 text-left font-semibold text-slate-500 dark:border-slate-600'>Order</th><th class='border-b border-slate-300 pb-1 text-left font-semibold text-slate-500 dark:border-slate-600'>Source-only edit cost</th></tr></thead><tbody><tr><td class='py-1 pr-2 align-top'>Manifest → install → source</td><td class='py-1 pr-2 align-top'>Fast — install stays cached</td></tr><tr><td class='py-1 pr-2 align-top'>Source (all) → install</td><td class='py-1 pr-2 align-top'>Slow — install reruns every time</td></tr></tbody></table><div class=\"mt-3 rounded-md border border-amber-300 bg-amber-50 p-2 dark:border-amber-800 dark:bg-amber-950\"><p class='text-xs font-semibold text-amber-700 dark:text-amber-400'>Common mistake</p><p class='mt-0.5 text-xs text-amber-800 dark:text-amber-300'>Treating this as a micro-optimization. On active development, this ordering is the difference between a multi-second and a near-instant rebuild on every single edit.</p></div>"
    },
    {
      "type": "quiz",
      "id": "layer-order-3",
      "poolId": "layer-order",
      "level": 2,
      "prompt": "You're adding a `RUN apt-get install -y libvips` step (a native OS dependency, unrelated to npm packages) to a Dockerfile. Where should it go to maximize cache reuse?",
      "choices": [
        "Immediately after FROM, before anything that depends on application-level files — its cache key then only depends on the base image, staying cached across both source and dependency-manifest changes",
        "After COPY . ., so it always has the latest source available",
        "It doesn't matter where OS-level installs go; they're never cached",
        "Right before CMD, at the very end of the Dockerfile"
      ],
      "correctIndex": 0,
      "explanation": "A step whose real inputs are just the base image (not application source or manifests) should be placed as early as possible — its cache key then depends on nothing that changes often, so it survives both source edits and dependency-manifest edits untouched.",
      "reference": "<div class='grid grid-cols-2 gap-2 text-xs'><div class=\"rounded border border-rose-300 bg-rose-50 p-1.5 dark:border-rose-800 dark:bg-rose-950\"><p class='font-semibold text-rose-600 dark:text-rose-400'>COPY . . first</p>every source edit reruns install</div><div class=\"rounded border border-emerald-300 bg-emerald-50 p-1.5 dark:border-emerald-800 dark:bg-emerald-950\"><p class='font-semibold text-emerald-600 dark:text-emerald-400'>Manifest + install first</p>source edits skip install entirely</div></div>",
      "learnMore": "<h5 class='mt-0 mb-2 text-sm font-semibold text-slate-700 dark:text-slate-200'>Why instruction order has real performance consequences</h5><p class='text-sm text-slate-600 dark:text-slate-300'>Ordering doesn't change what the image contains — it changes what depends on what, which is exactly what determines cache reuse.</p><table class='mt-2 w-full border-collapse text-xs'><thead><tr><th class='border-b border-slate-300 pb-1 text-left font-semibold text-slate-500 dark:border-slate-600'>Order</th><th class='border-b border-slate-300 pb-1 text-left font-semibold text-slate-500 dark:border-slate-600'>Source-only edit cost</th></tr></thead><tbody><tr><td class='py-1 pr-2 align-top'>Manifest → install → source</td><td class='py-1 pr-2 align-top'>Fast — install stays cached</td></tr><tr><td class='py-1 pr-2 align-top'>Source (all) → install</td><td class='py-1 pr-2 align-top'>Slow — install reruns every time</td></tr></tbody></table><div class=\"mt-3 rounded-md border border-amber-300 bg-amber-50 p-2 dark:border-amber-800 dark:bg-amber-950\"><p class='text-xs font-semibold text-amber-700 dark:text-amber-400'>Common mistake</p><p class='mt-0.5 text-xs text-amber-800 dark:text-amber-300'>Treating this as a micro-optimization. On active development, this ordering is the difference between a multi-second and a near-instant rebuild on every single edit.</p></div>"
    },
    {
      "type": "quiz",
      "id": "registry-digest-1",
      "poolId": "registry-digest",
      "level": 2,
      "prompt": "A registry stores and retrieves images \"by name and tag, and more precisely, by content digest.\" What does the content digest actually buy over the tag alone?",
      "choices": [
        "Nothing extra — a tag and a digest are two names for the same guarantee",
        "A digest is derived from the image's actual content, so it identifies one exact set of bytes; a tag is just a movable pointer that can be repointed to different content over time",
        "Digests are only used for compressing images, not identifying them",
        "A digest is assigned by the pushing user manually, like a tag"
      ],
      "correctIndex": 1,
      "explanation": "A digest is content-addressed — it can only refer to the exact bytes it was computed from. A tag is a separate, mutable label that a registry maps to whatever image was most recently pushed under that name, which is exactly why tags alone (`latest` especially) don't guarantee reproducibility.",
      "reference": "<div class=\"rounded border border-slate-300 bg-slate-50 p-2 text-xs dark:border-slate-600 dark:bg-slate-900\">Tag: <code>myapp:1.4.2</code> — a movable label<br/>Digest: <code>sha256:9f2c...</code> — the exact bytes, always</div>",
      "learnMore": "<h5 class='mt-0 mb-2 text-sm font-semibold text-slate-700 dark:text-slate-200'>Tags vs. digests</h5><p class='text-sm text-slate-600 dark:text-slate-300'>A registry is content-addressable underneath its tag-based interface — pushing/pulling by tag is convenient, but the digest is the actual guarantee.</p><table class='mt-2 w-full border-collapse text-xs'><thead><tr><th class='border-b border-slate-300 pb-1 text-left font-semibold text-slate-500 dark:border-slate-600'></th><th class='border-b border-slate-300 pb-1 text-left font-semibold text-slate-500 dark:border-slate-600'>Tag</th><th class='border-b border-slate-300 pb-1 text-left font-semibold text-slate-500 dark:border-slate-600'>Digest</th></tr></thead><tbody><tr><td class='py-1 pr-2 align-top'>Mutable?</td><td class='py-1 pr-2 align-top'>Yes — can be repointed</td><td class='py-1 pr-2 align-top'>No — derived from content</td></tr><tr><td class='py-1 pr-2 align-top'>Guarantees</td><td class='py-1 pr-2 align-top'>\"Whatever was last pushed here\"</td><td class='py-1 pr-2 align-top'>These exact bytes, always</td></tr></tbody></table><div class=\"mt-3 rounded-md border border-amber-300 bg-amber-50 p-2 dark:border-amber-800 dark:bg-amber-950\"><p class='text-xs font-semibold text-amber-700 dark:text-amber-400'>Common mistake</p><p class='mt-0.5 text-xs text-amber-800 dark:text-amber-300'>Treating a version tag as an equally strong guarantee as a digest. A specific tag like 1.4.2 is far more stable in practice than latest, but only a digest is truly immutable.</p></div>"
    },
    {
      "type": "quiz",
      "id": "registry-digest-2",
      "poolId": "registry-digest",
      "level": 2,
      "prompt": "When `docker pull` retrieves an image, why does it typically only download some layers rather than the entire image?",
      "choices": [
        "Registries always send the full image; the client just displays a partial progress bar",
        "Layers are individually cached and shared — pulling downloads only the layers the local machine doesn't already have, since each layer is content-addressed and reusable across images",
        "Docker deletes half the layers randomly to save bandwidth",
        "Only the base image layer is ever transferred; the rest are generated locally"
      ],
      "correctIndex": 1,
      "explanation": "Because layers are content-addressed independently of any one image, a machine that already has a given base-image layer cached (from a previous pull of any image sharing that layer) doesn't need to download it again — pulling only fetches what's actually missing locally.",
      "reference": "<div class=\"rounded border border-slate-300 bg-slate-50 p-2 text-xs dark:border-slate-600 dark:bg-slate-900\">Tag: <code>myapp:1.4.2</code> — a movable label<br/>Digest: <code>sha256:9f2c...</code> — the exact bytes, always</div>",
      "learnMore": "<h5 class='mt-0 mb-2 text-sm font-semibold text-slate-700 dark:text-slate-200'>Tags vs. digests</h5><p class='text-sm text-slate-600 dark:text-slate-300'>A registry is content-addressable underneath its tag-based interface — pushing/pulling by tag is convenient, but the digest is the actual guarantee.</p><table class='mt-2 w-full border-collapse text-xs'><thead><tr><th class='border-b border-slate-300 pb-1 text-left font-semibold text-slate-500 dark:border-slate-600'></th><th class='border-b border-slate-300 pb-1 text-left font-semibold text-slate-500 dark:border-slate-600'>Tag</th><th class='border-b border-slate-300 pb-1 text-left font-semibold text-slate-500 dark:border-slate-600'>Digest</th></tr></thead><tbody><tr><td class='py-1 pr-2 align-top'>Mutable?</td><td class='py-1 pr-2 align-top'>Yes — can be repointed</td><td class='py-1 pr-2 align-top'>No — derived from content</td></tr><tr><td class='py-1 pr-2 align-top'>Guarantees</td><td class='py-1 pr-2 align-top'>\"Whatever was last pushed here\"</td><td class='py-1 pr-2 align-top'>These exact bytes, always</td></tr></tbody></table><div class=\"mt-3 rounded-md border border-amber-300 bg-amber-50 p-2 dark:border-amber-800 dark:bg-amber-950\"><p class='text-xs font-semibold text-amber-700 dark:text-amber-400'>Common mistake</p><p class='mt-0.5 text-xs text-amber-800 dark:text-amber-300'>Treating a version tag as an equally strong guarantee as a digest. A specific tag like 1.4.2 is far more stable in practice than latest, but only a digest is truly immutable.</p></div>"
    },
    {
      "type": "quiz",
      "id": "multistage-isolation-1",
      "poolId": "multistage-isolation",
      "level": 2,
      "prompt": "In a multi-stage Dockerfile, `npm run build` in the build stage produces `/app/dist`. Does the runtime stage automatically have access to `/app/dist`?",
      "choices": [
        "Yes — all stages share the same filesystem by default",
        "No — each stage starts completely fresh with its own filesystem; `/app/dist` only exists in the runtime stage if a `COPY --from=build /app/dist ./dist` instruction explicitly pulls it across",
        "Yes, but only if both stages use the same base image",
        "No, and there's no way to move files between stages at all"
      ],
      "correctIndex": 1,
      "explanation": "Stage isolation is total by default — nothing from stage 1's filesystem carries over automatically. `COPY --from=<stage>` is the one explicit mechanism for crossing that boundary, and it's opt-in per file/directory, not automatic.",
      "reference": "<div class=\"rounded border border-slate-300 bg-slate-50 p-2 text-xs dark:border-slate-600 dark:bg-slate-900\">Stage 1 (build): compiler, dev deps, /app/dist<br/>Stage 2 (runtime): <em>starts empty</em> — gets only what <code>COPY --from=build</code> names</div>",
      "learnMore": "<h5 class='mt-0 mb-2 text-sm font-semibold text-slate-700 dark:text-slate-200'>Stage isolation is the whole mechanism</h5><p class='text-sm text-slate-600 dark:text-slate-300'>Isolation between stages is what makes multi-stage builds useful at all — without it, there'd be no way to exclude build tooling from the final image.</p><table class='mt-2 w-full border-collapse text-xs'><thead><tr><th class='border-b border-slate-300 pb-1 text-left font-semibold text-slate-500 dark:border-slate-600'>What crosses stages</th><th class='border-b border-slate-300 pb-1 text-left font-semibold text-slate-500 dark:border-slate-600'>How</th></tr></thead><tbody><tr><td class='py-1 pr-2 align-top'>Files/directories</td><td class='py-1 pr-2 align-top'>COPY --from=&lt;stage&gt; only</td></tr><tr><td class='py-1 pr-2 align-top'>Environment variables, installed packages</td><td class='py-1 pr-2 align-top'>Never automatically</td></tr></tbody></table><div class=\"mt-3 rounded-md border border-amber-300 bg-amber-50 p-2 dark:border-amber-800 dark:bg-amber-950\"><p class='text-xs font-semibold text-amber-700 dark:text-amber-400'>Common mistake</p><p class='mt-0.5 text-xs text-amber-800 dark:text-amber-300'>Assuming a package installed or a variable set in an earlier stage carries forward. It produces confusing \"why isn't this here\" failures in the final image.</p></div>"
    },
    {
      "type": "quiz",
      "id": "multistage-isolation-2",
      "poolId": "multistage-isolation",
      "level": 2,
      "prompt": "A build stage sets an environment variable with `ENV NODE_ENV=development` to install dev dependencies. Is that variable set in the runtime stage too?",
      "choices": [
        "Yes, environment variables always carry across every stage in a Dockerfile",
        "No — environment variables, like everything else, are stage-local; the runtime stage needs its own ENV instruction if it needs one at all",
        "Only if both stages share the same base image tag",
        "Only for variables whose name starts with NODE_"
      ],
      "correctIndex": 1,
      "explanation": "Nothing about a stage's environment is shared automatically — variables, installed packages, and files all stay local to the stage that set them unless explicitly copied across with COPY --from, which doesn't even apply to environment variables (only files).",
      "reference": "<div class=\"rounded border border-slate-300 bg-slate-50 p-2 text-xs dark:border-slate-600 dark:bg-slate-900\">Stage 1 (build): compiler, dev deps, /app/dist<br/>Stage 2 (runtime): <em>starts empty</em> — gets only what <code>COPY --from=build</code> names</div>",
      "learnMore": "<h5 class='mt-0 mb-2 text-sm font-semibold text-slate-700 dark:text-slate-200'>Stage isolation is the whole mechanism</h5><p class='text-sm text-slate-600 dark:text-slate-300'>Isolation between stages is what makes multi-stage builds useful at all — without it, there'd be no way to exclude build tooling from the final image.</p><table class='mt-2 w-full border-collapse text-xs'><thead><tr><th class='border-b border-slate-300 pb-1 text-left font-semibold text-slate-500 dark:border-slate-600'>What crosses stages</th><th class='border-b border-slate-300 pb-1 text-left font-semibold text-slate-500 dark:border-slate-600'>How</th></tr></thead><tbody><tr><td class='py-1 pr-2 align-top'>Files/directories</td><td class='py-1 pr-2 align-top'>COPY --from=&lt;stage&gt; only</td></tr><tr><td class='py-1 pr-2 align-top'>Environment variables, installed packages</td><td class='py-1 pr-2 align-top'>Never automatically</td></tr></tbody></table><div class=\"mt-3 rounded-md border border-amber-300 bg-amber-50 p-2 dark:border-amber-800 dark:bg-amber-950\"><p class='text-xs font-semibold text-amber-700 dark:text-amber-400'>Common mistake</p><p class='mt-0.5 text-xs text-amber-800 dark:text-amber-300'>Assuming a package installed or a variable set in an earlier stage carries forward. It produces confusing \"why isn't this here\" failures in the final image.</p></div>"
    },
    {
      "type": "quiz",
      "id": "multistage-isolation-3",
      "poolId": "multistage-isolation",
      "level": 2,
      "prompt": "Why is isolation between stages actually a feature, not a limitation, of multi-stage builds?",
      "choices": [
        "It isn't a feature — it's a limitation teams have to work around",
        "It's exactly what makes it possible to exclude build-only tooling from the final image; if stages shared state automatically, there'd be no clean boundary between \"what was needed to build\" and \"what's needed to run\"",
        "Isolation only matters for security compliance, not for image size",
        "Stages are isolated only in Docker's free tier"
      ],
      "correctIndex": 1,
      "explanation": "The entire value proposition of multi-stage builds depends on stages NOT sharing state by default — that's what guarantees the final image only contains what was explicitly pulled across, rather than everything the build process touched.",
      "reference": "<div class=\"rounded border border-slate-300 bg-slate-50 p-2 text-xs dark:border-slate-600 dark:bg-slate-900\">Stage 1 (build): compiler, dev deps, /app/dist<br/>Stage 2 (runtime): <em>starts empty</em> — gets only what <code>COPY --from=build</code> names</div>",
      "learnMore": "<h5 class='mt-0 mb-2 text-sm font-semibold text-slate-700 dark:text-slate-200'>Stage isolation is the whole mechanism</h5><p class='text-sm text-slate-600 dark:text-slate-300'>Isolation between stages is what makes multi-stage builds useful at all — without it, there'd be no way to exclude build tooling from the final image.</p><table class='mt-2 w-full border-collapse text-xs'><thead><tr><th class='border-b border-slate-300 pb-1 text-left font-semibold text-slate-500 dark:border-slate-600'>What crosses stages</th><th class='border-b border-slate-300 pb-1 text-left font-semibold text-slate-500 dark:border-slate-600'>How</th></tr></thead><tbody><tr><td class='py-1 pr-2 align-top'>Files/directories</td><td class='py-1 pr-2 align-top'>COPY --from=&lt;stage&gt; only</td></tr><tr><td class='py-1 pr-2 align-top'>Environment variables, installed packages</td><td class='py-1 pr-2 align-top'>Never automatically</td></tr></tbody></table><div class=\"mt-3 rounded-md border border-amber-300 bg-amber-50 p-2 dark:border-amber-800 dark:bg-amber-950\"><p class='text-xs font-semibold text-amber-700 dark:text-amber-400'>Common mistake</p><p class='mt-0.5 text-xs text-amber-800 dark:text-amber-300'>Assuming a package installed or a variable set in an earlier stage carries forward. It produces confusing \"why isn't this here\" failures in the final image.</p></div>"
    },
    {
      "type": "quiz",
      "id": "build-once-run-everywhere-1",
      "poolId": "build-once-run-everywhere",
      "level": 2,
      "prompt": "CI builds an image once, pushes it to a registry, and staging, QA, and production all pull that same tag. What guarantee does this actually provide that separately rebuilding in each environment wouldn't?",
      "choices": [
        "No real guarantee — pulling and rebuilding are functionally identical",
        "Every environment runs the byte-identical artifact CI already built and tested, closing the gap where a separate rebuild could drift (a different base image patch, different dependency resolution) even from unchanged source",
        "It only saves CI minutes, with no correctness benefit",
        "It guarantees the app has zero bugs"
      ],
      "correctIndex": 1,
      "explanation": "The registry is what turns 'built once' into 'run everywhere, identically' — a separate rebuild per environment reintroduces exactly the kind of environment-to-environment drift Docker exists to eliminate, even when the Dockerfile and source are unchanged.",
      "reference": "<div class=\"rounded border border-slate-300 bg-slate-50 p-2 text-xs dark:border-slate-600 dark:bg-slate-900\">CI builds once → push → staging/QA/prod all <strong>pull</strong> the same digest — never rebuild separately.</div>",
      "learnMore": "<h5 class='mt-0 mb-2 text-sm font-semibold text-slate-700 dark:text-slate-200'>Build once, run everywhere</h5><p class='text-sm text-slate-600 dark:text-slate-300'>This is the practical payoff the rest of the unit's mechanics (layers, caching, registries) exist to serve — not an abstract goal, a specific pipeline shape.</p><table class='mt-2 w-full border-collapse text-xs'><thead><tr><th class='border-b border-slate-300 pb-1 text-left font-semibold text-slate-500 dark:border-slate-600'>Approach</th><th class='border-b border-slate-300 pb-1 text-left font-semibold text-slate-500 dark:border-slate-600'>Risk</th></tr></thead><tbody><tr><td class='py-1 pr-2 align-top'>Build once, pull everywhere</td><td class='py-1 pr-2 align-top'>None — identical bytes everywhere</td></tr><tr><td class='py-1 pr-2 align-top'>Rebuild per environment</td><td class='py-1 pr-2 align-top'>Drift: base image patch, dependency resolution can differ silently</td></tr></tbody></table><div class=\"mt-3 rounded-md border border-amber-300 bg-amber-50 p-2 dark:border-amber-800 dark:bg-amber-950\"><p class='text-xs font-semibold text-amber-700 dark:text-amber-400'>Common mistake</p><p class='mt-0.5 text-xs text-amber-800 dark:text-amber-300'>Assuming \"same Dockerfile, same source\" is enough for parity. A rebuild can still resolve a different dependency version or pull a different base image patch than the original build did.</p></div>"
    },
    {
      "type": "quiz",
      "id": "build-once-run-everywhere-2",
      "poolId": "build-once-run-everywhere",
      "level": 2,
      "prompt": "Which single mechanic from this unit is most directly responsible for closing L1's opening gap — a laptop passing tests while prod, on different Node/library versions, crashes?",
      "choices": [
        "Layer caching, since it speeds up local rebuilds",
        "The image being a fixed, portable artifact pulled identically everywhere via a registry — the exact Node version and system libraries the laptop had ship inside it, instead of living only in one machine's installed state",
        "Multi-stage builds, since they only affect build tooling",
        "The choice of base image tag has no bearing on this"
      ],
      "correctIndex": 1,
      "explanation": "Layer caching and multi-stage builds are about build speed and image size — the actual environment-parity fix is that the image itself carries every dependency the laptop had, and a registry lets every environment pull that identical artifact instead of reconstructing its own approximation.",
      "reference": "<div class=\"rounded border border-slate-300 bg-slate-50 p-2 text-xs dark:border-slate-600 dark:bg-slate-900\">CI builds once → push → staging/QA/prod all <strong>pull</strong> the same digest — never rebuild separately.</div>",
      "learnMore": "<h5 class='mt-0 mb-2 text-sm font-semibold text-slate-700 dark:text-slate-200'>Build once, run everywhere</h5><p class='text-sm text-slate-600 dark:text-slate-300'>This is the practical payoff the rest of the unit's mechanics (layers, caching, registries) exist to serve — not an abstract goal, a specific pipeline shape.</p><table class='mt-2 w-full border-collapse text-xs'><thead><tr><th class='border-b border-slate-300 pb-1 text-left font-semibold text-slate-500 dark:border-slate-600'>Approach</th><th class='border-b border-slate-300 pb-1 text-left font-semibold text-slate-500 dark:border-slate-600'>Risk</th></tr></thead><tbody><tr><td class='py-1 pr-2 align-top'>Build once, pull everywhere</td><td class='py-1 pr-2 align-top'>None — identical bytes everywhere</td></tr><tr><td class='py-1 pr-2 align-top'>Rebuild per environment</td><td class='py-1 pr-2 align-top'>Drift: base image patch, dependency resolution can differ silently</td></tr></tbody></table><div class=\"mt-3 rounded-md border border-amber-300 bg-amber-50 p-2 dark:border-amber-800 dark:bg-amber-950\"><p class='text-xs font-semibold text-amber-700 dark:text-amber-400'>Common mistake</p><p class='mt-0.5 text-xs text-amber-800 dark:text-amber-300'>Assuming \"same Dockerfile, same source\" is enough for parity. A rebuild can still resolve a different dependency version or pull a different base image patch than the original build did.</p></div>"
    },
    {
      "type": "code",
      "id": "rebuild-plan-1",
      "poolId": "rebuild-plan",
      "level": 3,
      "prompt": "Implement `computeRebuildPlan(layers, changedInputs)` from L3: `layers` is an array of `{ name, inputs }` in Dockerfile order; `changedInputs` is a Set of input names that changed. A layer rebuilds if its own inputs changed, OR if any earlier layer already needed a rebuild (invalidation cascades forward). Return an array of the names of every layer that must rebuild, in order.",
      "starterCode": "function computeRebuildPlan(layers, changedInputs) {\n  // TODO\n}\n",
      "tests": [
        {
          "description": "only the last layer rebuilds when only its own input changed",
          "expr": "expect(computeRebuildPlan([{ name: 'A', inputs: ['x'] }, { name: 'B', inputs: ['y'] }], new Set(['y']))).toEqual(['B'])"
        },
        {
          "description": "an early layer change cascades to every layer after it",
          "expr": "expect(computeRebuildPlan([{ name: 'A', inputs: ['x'] }, { name: 'B', inputs: ['y'] }, { name: 'C', inputs: [] }], new Set(['x']))).toEqual(['A', 'B', 'C'])"
        },
        {
          "description": "no changed inputs means no rebuilds at all",
          "expr": "expect(computeRebuildPlan([{ name: 'A', inputs: ['x'] }, { name: 'B', inputs: ['y'] }], new Set())).toEqual([])"
        },
        {
          "description": "a layer with no inputs of its own still rebuilds if cascading from an earlier layer",
          "expr": "expect(computeRebuildPlan([{ name: 'A', inputs: ['x'] }, { name: 'B', inputs: [] }], new Set(['x']))).toEqual(['A', 'B'])"
        }
      ],
      "solution": "function computeRebuildPlan(layers, changedInputs) {\n  const rebuilds = [];\n  let cascading = false;\n  for (const layer of layers) {\n    const inputsChanged = layer.inputs.some((i) => changedInputs.has(i));\n    if (inputsChanged || cascading) {\n      rebuilds.push(layer.name);\n      cascading = true;\n    }\n  }\n  return rebuilds;\n}",
      "explanation": "This is the exact rule behind the three real docker build outputs in L3: invalidation only cascades forward — a layer that hasn't changed and follows only unchanged layers stays cached, but once any layer rebuilds, every layer after it must too, since each layer's cache key depends on the output of the one before it.",
      "reference": "<div class=\"rounded border border-slate-300 bg-slate-50 p-2 font-mono text-xs dark:border-slate-600 dark:bg-slate-900\">cascading = false<br/>for each layer: if own-inputs-changed OR cascading → rebuild, cascading = true</div><p class='mt-2 text-xs text-slate-500 dark:text-slate-400'><strong>Why:</strong> a boolean flag is enough because the cascade only ever moves forward.</p>",
      "learnMore": "<h5 class='mt-0 mb-2 text-sm font-semibold text-slate-700 dark:text-slate-200'>Modeling cache invalidation as a pure function</h5><p class='text-sm text-slate-600 dark:text-slate-300'>The real Docker build engine does something equivalent to this, layer by layer, using content hashes instead of a named-input Set — but the logic is the same shape: once a rebuild is triggered, it never un-triggers for the rest of the chain.</p><p class='text-sm text-slate-600 dark:text-slate-300'>This is why a single boolean <code>cascading</code> flag is sufficient — there's no need to track which specific earlier layer triggered the rebuild, only whether the chain has already been broken.</p><div class=\"mt-3 rounded-md border border-amber-300 bg-amber-50 p-2 dark:border-amber-800 dark:bg-amber-950\"><p class='text-xs font-semibold text-amber-700 dark:text-amber-400'>Common mistake</p><p class='mt-0.5 text-xs text-amber-800 dark:text-amber-300'>Checking each layer's inputs against the ORIGINAL changedInputs set only, forgetting the cascade — that would incorrectly report a downstream layer as cached even though an earlier layer it depends on already rebuilt.</p></div>"
    },
    {
      "type": "code",
      "id": "rebuild-plan-2",
      "poolId": "rebuild-plan",
      "level": 3,
      "prompt": "Implement `computeRebuildPlan(layers, changedInputs)`, same contract as before: `layers` is `[{ name, inputs }]` in order, `changedInputs` is a Set. Return the names of every layer that rebuilds, given that invalidation cascades forward once triggered. Verify it against a 4-layer Dockerfile-shaped chain, including a middle layer with no inputs of its own.",
      "starterCode": "function computeRebuildPlan(layers, changedInputs) {\n  // TODO\n}\n",
      "tests": [
        {
          "description": "a middle layer with no inputs stays cached if nothing before it changed",
          "expr": "expect(computeRebuildPlan([{ name: 'FROM', inputs: ['base'] }, { name: 'WORKDIR', inputs: [] }, { name: 'COPY manifest', inputs: ['pkg'] }, { name: 'RUN install', inputs: [] }], new Set(['pkg']))).toEqual(['COPY manifest', 'RUN install'])"
        },
        {
          "description": "changing the base image cascades through the entire chain",
          "expr": "expect(computeRebuildPlan([{ name: 'FROM', inputs: ['base'] }, { name: 'WORKDIR', inputs: [] }, { name: 'COPY manifest', inputs: ['pkg'] }, { name: 'RUN install', inputs: [] }], new Set(['base']))).toEqual(['FROM', 'WORKDIR', 'COPY manifest', 'RUN install'])"
        },
        {
          "description": "an unrelated changed input that no layer declares triggers nothing",
          "expr": "expect(computeRebuildPlan([{ name: 'FROM', inputs: ['base'] }, { name: 'WORKDIR', inputs: [] }], new Set(['unrelated-file']))).toEqual([])"
        }
      ],
      "solution": "function computeRebuildPlan(layers, changedInputs) {\n  const rebuilds = [];\n  let cascading = false;\n  for (const layer of layers) {\n    const inputsChanged = layer.inputs.some((i) => changedInputs.has(i));\n    if (inputsChanged || cascading) {\n      rebuilds.push(layer.name);\n      cascading = true;\n    }\n  }\n  return rebuilds;\n}",
      "explanation": "Same rule as before, checked against a shape closer to a real Dockerfile: WORKDIR has no inputs of its own and stays cached when nothing upstream of it changed, but a base-image change (the earliest possible layer) cascades through literally everything after it, since every later layer transitively depends on it.",
      "reference": "<div class=\"rounded border border-slate-300 bg-slate-50 p-2 font-mono text-xs dark:border-slate-600 dark:bg-slate-900\">cascading = false<br/>for each layer: if own-inputs-changed OR cascading → rebuild, cascading = true</div><p class='mt-2 text-xs text-slate-500 dark:text-slate-400'><strong>Why:</strong> a boolean flag is enough because the cascade only ever moves forward.</p>",
      "learnMore": "<h5 class='mt-0 mb-2 text-sm font-semibold text-slate-700 dark:text-slate-200'>Modeling cache invalidation as a pure function</h5><p class='text-sm text-slate-600 dark:text-slate-300'>The real Docker build engine does something equivalent to this, layer by layer, using content hashes instead of a named-input Set — but the logic is the same shape: once a rebuild is triggered, it never un-triggers for the rest of the chain.</p><p class='text-sm text-slate-600 dark:text-slate-300'>This is why a single boolean <code>cascading</code> flag is sufficient — there's no need to track which specific earlier layer triggered the rebuild, only whether the chain has already been broken.</p><div class=\"mt-3 rounded-md border border-amber-300 bg-amber-50 p-2 dark:border-amber-800 dark:bg-amber-950\"><p class='text-xs font-semibold text-amber-700 dark:text-amber-400'>Common mistake</p><p class='mt-0.5 text-xs text-amber-800 dark:text-amber-300'>Checking each layer's inputs against the ORIGINAL changedInputs set only, forgetting the cascade — that would incorrectly report a downstream layer as cached even though an earlier layer it depends on already rebuilt.</p></div>"
    },
    {
      "type": "code",
      "id": "dockerignore-match-1",
      "poolId": "dockerignore-match",
      "level": 3,
      "prompt": "Implement `shouldIgnore(filePath, patterns)`, a simplified `.dockerignore` matcher. `patterns` is an array of strings like `'node_modules/'` (matches the path or anything inside it) or `'*.log'` (matches any file ending in that suffix, anywhere). Return true if any pattern matches `filePath`.",
      "starterCode": "function shouldIgnore(filePath, patterns) {\n  // TODO\n}\n",
      "tests": [
        {
          "description": "a directory pattern matches files inside that directory",
          "expr": "expect(shouldIgnore('node_modules/lodash/index.js', ['node_modules/'])).toBe(true)"
        },
        {
          "description": "a directory pattern matches the directory path itself",
          "expr": "expect(shouldIgnore('node_modules/', ['node_modules/'])).toBe(true)"
        },
        {
          "description": "a wildcard suffix pattern matches a file with that extension anywhere",
          "expr": "expect(shouldIgnore('logs/build.log', ['*.log'])).toBe(true)"
        },
        {
          "description": "a file not matching any pattern is not ignored",
          "expr": "expect(shouldIgnore('src/server.js', ['node_modules/', '*.log'])).toBe(false)"
        },
        {
          "description": "a file that merely contains the pattern text as a substring, not a path/suffix match, is not ignored",
          "expr": "expect(shouldIgnore('src/node_modules_backup.js', ['node_modules/'])).toBe(false)"
        }
      ],
      "solution": "function shouldIgnore(filePath, patterns) {\n  return patterns.some((pattern) => {\n    if (pattern.endsWith('/')) {\n      return filePath === pattern || filePath.startsWith(pattern);\n    }\n    if (pattern.startsWith('*.')) {\n      const suffix = pattern.slice(1); // e.g. '.log'\n      return filePath.endsWith(suffix);\n    }\n    return filePath === pattern;\n  });\n}",
      "explanation": "A directory pattern ('node_modules/') has to match either the directory itself or anything with that prefix — a plain substring check would wrongly flag unrelated files like 'node_modules_backup.js' that merely contain the same text without actually being inside that directory, which is exactly why the prefix check (not includes()) matters here.",
      "reference": "<div class=\"rounded border border-slate-300 bg-slate-50 p-2 font-mono text-xs dark:border-slate-600 dark:bg-slate-900\">.dockerignore:<br/>node_modules/<br/>.git/<br/>*.log</div><p class='mt-2 text-xs text-slate-500 dark:text-slate-400'><strong>Why:</strong> without it, every build sends and hashes files the app never uses.</p>",
      "learnMore": "<h5 class='mt-0 mb-2 text-sm font-semibold text-slate-700 dark:text-slate-200'>Why .dockerignore matters beyond just size</h5><p class='text-sm text-slate-600 dark:text-slate-300'>Every file in the build context gets sent to the Docker daemon and, for anything a COPY instruction touches, folded into that layer's cache key. Irrelevant files (node_modules, .git, log files) don't just waste bandwidth — they can bloat or destabilize the cache key of a COPY . . layer with content that has nothing to do with the actual application.</p><div class=\"mt-3 rounded-md border border-amber-300 bg-amber-50 p-2 dark:border-amber-800 dark:bg-amber-950\"><p class='text-xs font-semibold text-amber-700 dark:text-amber-400'>Common mistake</p><p class='mt-0.5 text-xs text-amber-800 dark:text-amber-300'>Using a naive substring/includes() match for directory patterns. It over-matches unrelated files that merely share a name fragment, which is a correctness bug, not just an inefficiency.</p></div>"
    },
    {
      "type": "code",
      "id": "dockerignore-match-2",
      "poolId": "dockerignore-match",
      "level": 3,
      "prompt": "Implement `shouldIgnore(filePath, patterns)` again, this time verified against a mixed set of directory and wildcard patterns applied to a realistic file list, to confirm files that should ship (like `src/server.js`) are never accidentally excluded.",
      "starterCode": "function shouldIgnore(filePath, patterns) {\n  // TODO\n}\n",
      "tests": [
        {
          "description": ".git/ directory contents are ignored",
          "expr": "expect(shouldIgnore('.git/HEAD', ['.git/', '*.log', 'dist/'])).toBe(true)"
        },
        {
          "description": "dist/ directory contents are ignored",
          "expr": "expect(shouldIgnore('dist/bundle.js', ['.git/', '*.log', 'dist/'])).toBe(true)"
        },
        {
          "description": "a real source file is never ignored by an unrelated pattern set",
          "expr": "expect(shouldIgnore('src/server.js', ['.git/', '*.log', 'dist/'])).toBe(false)"
        },
        {
          "description": "an exact filename pattern (no wildcard, no trailing slash) matches only that exact path",
          "expr": "expect(shouldIgnore('.env', ['.env'])).toBe(true)"
        },
        {
          "description": "an exact filename pattern does not match a differently-named file",
          "expr": "expect(shouldIgnore('.env.example', ['.env'])).toBe(false)"
        }
      ],
      "solution": "function shouldIgnore(filePath, patterns) {\n  return patterns.some((pattern) => {\n    if (pattern.endsWith('/')) {\n      return filePath === pattern || filePath.startsWith(pattern);\n    }\n    if (pattern.startsWith('*.')) {\n      const suffix = pattern.slice(1);\n      return filePath.endsWith(suffix);\n    }\n    return filePath === pattern;\n  });\n}",
      "explanation": "The exact-match branch (no trailing slash, no wildcard) has to compare full equality, not a prefix or substring — '.env' and '.env.example' are different files, and a pattern meant to exclude only the real secrets file shouldn't accidentally also exclude a checked-in example file with a similar name.",
      "reference": "<div class=\"rounded border border-slate-300 bg-slate-50 p-2 font-mono text-xs dark:border-slate-600 dark:bg-slate-900\">.dockerignore:<br/>node_modules/<br/>.git/<br/>*.log</div><p class='mt-2 text-xs text-slate-500 dark:text-slate-400'><strong>Why:</strong> without it, every build sends and hashes files the app never uses.</p>",
      "learnMore": "<h5 class='mt-0 mb-2 text-sm font-semibold text-slate-700 dark:text-slate-200'>Why .dockerignore matters beyond just size</h5><p class='text-sm text-slate-600 dark:text-slate-300'>Every file in the build context gets sent to the Docker daemon and, for anything a COPY instruction touches, folded into that layer's cache key. Irrelevant files (node_modules, .git, log files) don't just waste bandwidth — they can bloat or destabilize the cache key of a COPY . . layer with content that has nothing to do with the actual application.</p><div class=\"mt-3 rounded-md border border-amber-300 bg-amber-50 p-2 dark:border-amber-800 dark:bg-amber-950\"><p class='text-xs font-semibold text-amber-700 dark:text-amber-400'>Common mistake</p><p class='mt-0.5 text-xs text-amber-800 dark:text-amber-300'>Using a naive substring/includes() match for directory patterns. It over-matches unrelated files that merely share a name fragment, which is a correctness bug, not just an inefficiency.</p></div>"
    },
    {
      "type": "quiz",
      "id": "latest-tag-risk-1",
      "poolId": "latest-tag-risk",
      "level": 3,
      "prompt": "Why is pulling an image tagged `latest` risky for reproducible builds, per L3's failure modes?",
      "choices": [
        "`latest` images are always corrupted",
        "`latest` is just a mutable tag like any other — it can be repointed to a different image at any time, so pulling it doesn't guarantee the same bytes twice, undermining the build-once-run-everywhere guarantee",
        "Docker doesn't actually support the `latest` tag; it's a common misconception",
        "`latest` only works for base images, never for application images"
      ],
      "correctIndex": 1,
      "explanation": "A tag is just a movable pointer to an image, and `latest` is not special in that regard — a specific version tag or content digest is what actually pins a reproducible reference, since tags in general (latest included) can be repointed.",
      "reference": "<div class=\"rounded border border-rose-300 bg-rose-50 p-2 text-xs dark:border-rose-800 dark:bg-rose-950\"><code>myapp:latest</code> today ≠ <code>myapp:latest</code> tomorrow — same name, possibly different bytes.</div>",
      "learnMore": "<h5 class='mt-0 mb-2 text-sm font-semibold text-slate-700 dark:text-slate-200'>latest is not a version</h5><p class='text-sm text-slate-600 dark:text-slate-300'>latest is Docker's default tag when none is specified — it carries no semantic meaning about recency or stability, it's simply whatever was pushed most recently under that name.</p><table class='mt-2 w-full border-collapse text-xs'><thead><tr><th class='border-b border-slate-300 pb-1 text-left font-semibold text-slate-500 dark:border-slate-600'>Reference</th><th class='border-b border-slate-300 pb-1 text-left font-semibold text-slate-500 dark:border-slate-600'>Reproducible?</th></tr></thead><tbody><tr><td class='py-1 pr-2 align-top'>myapp:latest</td><td class='py-1 pr-2 align-top'>No — can change under you</td></tr><tr><td class='py-1 pr-2 align-top'>myapp:1.4.2</td><td class='py-1 pr-2 align-top'>Mostly — depends on team discipline not to overwrite it</td></tr><tr><td class='py-1 pr-2 align-top'>myapp@sha256:...</td><td class='py-1 pr-2 align-top'>Yes — content-addressed, cannot change</td></tr></tbody></table><div class=\"mt-3 rounded-md border border-amber-300 bg-amber-50 p-2 dark:border-amber-800 dark:bg-amber-950\"><p class='text-xs font-semibold text-amber-700 dark:text-amber-400'>Common mistake</p><p class='mt-0.5 text-xs text-amber-800 dark:text-amber-300'>Assuming latest means \"the newest, most tested version.\" It only means \"whatever was pushed last\" — that could even be an accidental or broken push.</p></div>"
    },
    {
      "type": "quiz",
      "id": "latest-tag-risk-2",
      "poolId": "latest-tag-risk",
      "level": 3,
      "prompt": "Two deploys, a week apart, both run `docker pull myapp:latest && docker run myapp:latest`. Are they guaranteed to run the same image?",
      "choices": [
        "Yes, tags never change once pushed",
        "No — if anyone pushed a new image under the `latest` tag in that week, the second deploy silently runs different bytes than the first, despite using an identical command",
        "Yes, but only if the Dockerfile didn't change",
        "No, because `docker run` always rebuilds from source"
      ],
      "correctIndex": 1,
      "explanation": "A tag is a pointer, and `latest` gets repointed on every push that doesn't specify a different tag — the exact same pull-and-run command a week apart can retrieve genuinely different images with no warning.",
      "reference": "<div class=\"rounded border border-rose-300 bg-rose-50 p-2 text-xs dark:border-rose-800 dark:bg-rose-950\"><code>myapp:latest</code> today ≠ <code>myapp:latest</code> tomorrow — same name, possibly different bytes.</div>",
      "learnMore": "<h5 class='mt-0 mb-2 text-sm font-semibold text-slate-700 dark:text-slate-200'>latest is not a version</h5><p class='text-sm text-slate-600 dark:text-slate-300'>latest is Docker's default tag when none is specified — it carries no semantic meaning about recency or stability, it's simply whatever was pushed most recently under that name.</p><table class='mt-2 w-full border-collapse text-xs'><thead><tr><th class='border-b border-slate-300 pb-1 text-left font-semibold text-slate-500 dark:border-slate-600'>Reference</th><th class='border-b border-slate-300 pb-1 text-left font-semibold text-slate-500 dark:border-slate-600'>Reproducible?</th></tr></thead><tbody><tr><td class='py-1 pr-2 align-top'>myapp:latest</td><td class='py-1 pr-2 align-top'>No — can change under you</td></tr><tr><td class='py-1 pr-2 align-top'>myapp:1.4.2</td><td class='py-1 pr-2 align-top'>Mostly — depends on team discipline not to overwrite it</td></tr><tr><td class='py-1 pr-2 align-top'>myapp@sha256:...</td><td class='py-1 pr-2 align-top'>Yes — content-addressed, cannot change</td></tr></tbody></table><div class=\"mt-3 rounded-md border border-amber-300 bg-amber-50 p-2 dark:border-amber-800 dark:bg-amber-950\"><p class='text-xs font-semibold text-amber-700 dark:text-amber-400'>Common mistake</p><p class='mt-0.5 text-xs text-amber-800 dark:text-amber-300'>Assuming latest means \"the newest, most tested version.\" It only means \"whatever was pushed last\" — that could even be an accidental or broken push.</p></div>"
    },
    {
      "type": "quiz",
      "id": "latest-tag-risk-3",
      "poolId": "latest-tag-risk",
      "level": 3,
      "prompt": "What's the most robust way to reference an image if you need a guarantee that you'll always get the exact same bytes?",
      "choices": [
        "Tag it `stable` instead of `latest`",
        "Reference it by content digest (e.g. `myapp@sha256:...`), since a digest is derived from the content itself and can't be repointed the way any tag — including `latest` and specific version tags — technically can",
        "Always rebuild from source instead of pulling",
        "Use the shortest possible tag name"
      ],
      "correctIndex": 1,
      "explanation": "Any tag, no matter what it's named, is ultimately a mutable pointer someone could repoint. A content digest is the one reference that's inherently tied to specific bytes and cannot be reassigned to different content.",
      "reference": "<div class=\"rounded border border-rose-300 bg-rose-50 p-2 text-xs dark:border-rose-800 dark:bg-rose-950\"><code>myapp:latest</code> today ≠ <code>myapp:latest</code> tomorrow — same name, possibly different bytes.</div>",
      "learnMore": "<h5 class='mt-0 mb-2 text-sm font-semibold text-slate-700 dark:text-slate-200'>latest is not a version</h5><p class='text-sm text-slate-600 dark:text-slate-300'>latest is Docker's default tag when none is specified — it carries no semantic meaning about recency or stability, it's simply whatever was pushed most recently under that name.</p><table class='mt-2 w-full border-collapse text-xs'><thead><tr><th class='border-b border-slate-300 pb-1 text-left font-semibold text-slate-500 dark:border-slate-600'>Reference</th><th class='border-b border-slate-300 pb-1 text-left font-semibold text-slate-500 dark:border-slate-600'>Reproducible?</th></tr></thead><tbody><tr><td class='py-1 pr-2 align-top'>myapp:latest</td><td class='py-1 pr-2 align-top'>No — can change under you</td></tr><tr><td class='py-1 pr-2 align-top'>myapp:1.4.2</td><td class='py-1 pr-2 align-top'>Mostly — depends on team discipline not to overwrite it</td></tr><tr><td class='py-1 pr-2 align-top'>myapp@sha256:...</td><td class='py-1 pr-2 align-top'>Yes — content-addressed, cannot change</td></tr></tbody></table><div class=\"mt-3 rounded-md border border-amber-300 bg-amber-50 p-2 dark:border-amber-800 dark:bg-amber-950\"><p class='text-xs font-semibold text-amber-700 dark:text-amber-400'>Common mistake</p><p class='mt-0.5 text-xs text-amber-800 dark:text-amber-300'>Assuming latest means \"the newest, most tested version.\" It only means \"whatever was pushed last\" — that could even be an accidental or broken push.</p></div>"
    },
    {
      "type": "quiz",
      "id": "stage-isolation-1",
      "poolId": "stage-isolation",
      "level": 3,
      "prompt": "In a multi-stage Dockerfile, a package is installed in the build stage. Does it automatically exist in the runtime stage's final image?",
      "choices": [
        "Yes — all stages in a Dockerfile share the same filesystem state automatically",
        "No — each stage starts completely fresh; nothing from an earlier stage exists in a later one unless explicitly pulled across with COPY --from=<stage>",
        "Yes, but only for npm packages specifically",
        "No, multi-stage builds don't allow copying anything between stages at all"
      ],
      "correctIndex": 1,
      "explanation": "Stages are isolated by design — this is precisely what makes multi-stage builds useful for excluding build-only tooling from the final image, but it also means anything needed in the final stage (like compiled output) must be explicitly named in a COPY --from instruction.",
      "reference": "<div class=\"rounded border border-slate-300 bg-slate-50 p-2 text-xs dark:border-slate-600 dark:bg-slate-900\">Stage isolation + explicit COPY --from is the entire mechanism multi-stage builds rely on.</div>",
      "learnMore": "<h5 class='mt-0 mb-2 text-sm font-semibold text-slate-700 dark:text-slate-200'>Stage isolation, revisited with real failure examples</h5><p class='text-sm text-slate-600 dark:text-slate-300'>The failure mode isn't hypothetical: a runtime image that mysteriously can't find a binary the build stage installed is almost always this exact mistake — the install happened in a stage whose filesystem never crosses into the final image.</p><div class=\"mt-3 rounded-md border border-amber-300 bg-amber-50 p-2 dark:border-amber-800 dark:bg-amber-950\"><p class='text-xs font-semibold text-amber-700 dark:text-amber-400'>Common mistake</p><p class='mt-0.5 text-xs text-amber-800 dark:text-amber-300'>Debugging a \"missing binary in production\" issue by re-checking the build stage's Dockerfile lines, when the real fix is adding a COPY --from for that specific file.</p></div>"
    },
    {
      "type": "quiz",
      "id": "stage-isolation-2",
      "poolId": "stage-isolation",
      "level": 3,
      "prompt": "A runtime image mysteriously can't find a compiled binary that the build stage's Dockerfile clearly installs. What's the most likely cause?",
      "choices": [
        "The binary was installed with the wrong permissions",
        "The build stage never copied it across with COPY --from=<stage> — it exists only inside the build stage's isolated filesystem, which the runtime stage never sees",
        "Docker silently deletes binaries larger than 10MB",
        "The base images in the two stages are incompatible"
      ],
      "correctIndex": 1,
      "explanation": "This is the single most common multi-stage debugging trap: the binary genuinely was installed, just in a stage whose filesystem is completely separate from the final image unless a COPY --from names it explicitly.",
      "reference": "<div class=\"rounded border border-slate-300 bg-slate-50 p-2 text-xs dark:border-slate-600 dark:bg-slate-900\">Stage isolation + explicit COPY --from is the entire mechanism multi-stage builds rely on.</div>",
      "learnMore": "<h5 class='mt-0 mb-2 text-sm font-semibold text-slate-700 dark:text-slate-200'>Stage isolation, revisited with real failure examples</h5><p class='text-sm text-slate-600 dark:text-slate-300'>The failure mode isn't hypothetical: a runtime image that mysteriously can't find a binary the build stage installed is almost always this exact mistake — the install happened in a stage whose filesystem never crosses into the final image.</p><div class=\"mt-3 rounded-md border border-amber-300 bg-amber-50 p-2 dark:border-amber-800 dark:bg-amber-950\"><p class='text-xs font-semibold text-amber-700 dark:text-amber-400'>Common mistake</p><p class='mt-0.5 text-xs text-amber-800 dark:text-amber-300'>Debugging a \"missing binary in production\" issue by re-checking the build stage's Dockerfile lines, when the real fix is adding a COPY --from for that specific file.</p></div>"
    },
    {
      "type": "quiz",
      "id": "stage-isolation-3",
      "poolId": "stage-isolation",
      "level": 3,
      "prompt": "Fixing a \"binary not found in production\" bug caused by multi-stage isolation requires which change?",
      "choices": [
        "Switching back to a single-stage Dockerfile permanently",
        "Adding an explicit COPY --from=<build-stage-name> <path> instruction in the runtime stage naming that specific file or directory",
        "Increasing the runtime stage's memory limit",
        "Rebuilding the build stage a second time inside the runtime stage"
      ],
      "correctIndex": 1,
      "explanation": "The fix is narrow and explicit: name exactly what needs to cross the stage boundary with COPY --from. Going back to single-stage would fix the symptom but throw away every benefit (image size, attack surface) multi-stage builds provide.",
      "reference": "<div class=\"rounded border border-slate-300 bg-slate-50 p-2 text-xs dark:border-slate-600 dark:bg-slate-900\">Stage isolation + explicit COPY --from is the entire mechanism multi-stage builds rely on.</div>",
      "learnMore": "<h5 class='mt-0 mb-2 text-sm font-semibold text-slate-700 dark:text-slate-200'>Stage isolation, revisited with real failure examples</h5><p class='text-sm text-slate-600 dark:text-slate-300'>The failure mode isn't hypothetical: a runtime image that mysteriously can't find a binary the build stage installed is almost always this exact mistake — the install happened in a stage whose filesystem never crosses into the final image.</p><div class=\"mt-3 rounded-md border border-amber-300 bg-amber-50 p-2 dark:border-amber-800 dark:bg-amber-950\"><p class='text-xs font-semibold text-amber-700 dark:text-amber-400'>Common mistake</p><p class='mt-0.5 text-xs text-amber-800 dark:text-amber-300'>Debugging a \"missing binary in production\" issue by re-checking the build stage's Dockerfile lines, when the real fix is adding a COPY --from for that specific file.</p></div>"
    },
    {
      "type": "quiz",
      "id": "early-stable-step-1",
      "poolId": "early-stable-step",
      "level": 3,
      "prompt": "A `RUN apt-get install -y libvips` step (a native OS dependency) only ever depends on the base image, never on application source or package.json. Where does it survive the most cache invalidation if placed there?",
      "choices": [
        "Right before CMD, at the very end",
        "As early as possible, right after FROM — its cache key then depends on nothing that changes on ordinary source or dependency-manifest edits, so it survives both",
        "It has to go after COPY package.json, or Docker rejects the Dockerfile",
        "Placement makes no difference for OS-level installs specifically"
      ],
      "correctIndex": 1,
      "explanation": "The general rule extends past npm installs: any step whose real inputs are limited to the base image should sit as early as possible, since that minimizes what could invalidate it — it then survives both source edits and manifest edits, only rebuilding if the base image itself changes.",
      "reference": "<div class=\"rounded border border-slate-300 bg-slate-50 p-2 text-xs dark:border-slate-600 dark:bg-slate-900\">General rule: place a layer as early as its <em>real</em> inputs allow — not just dependency installs, anything genuinely base-image-only.</div>",
      "learnMore": "<h5 class='mt-0 mb-2 text-sm font-semibold text-slate-700 dark:text-slate-200'>Layer ordering generalizes past npm ci</h5><p class='text-sm text-slate-600 dark:text-slate-300'>The dependency-manifest-before-source pattern is one instance of a broader rule: order layers by how often their actual inputs change, cheapest/most-stable first. An OS-level package install that never depends on application files is exactly this case.</p><div class=\"mt-3 rounded-md border border-amber-300 bg-amber-50 p-2 dark:border-amber-800 dark:bg-amber-950\"><p class='text-xs font-semibold text-amber-700 dark:text-amber-400'>Common mistake</p><p class='mt-0.5 text-xs text-amber-800 dark:text-amber-300'>Only applying the \"install before source\" rule to npm/pip-style installs, and missing that the same reasoning applies to any layer with genuinely stable inputs.</p></div>"
    },
    {
      "type": "quiz",
      "id": "early-stable-step-2",
      "poolId": "early-stable-step",
      "level": 3,
      "prompt": "What's the general principle behind \"copy manifests and install dependencies before copying source,\" once you generalize it past that one specific case?",
      "choices": [
        "Every RUN instruction should come before every COPY instruction, no exceptions",
        "Order layers by how often their real inputs change — the most stable, least-often-changing steps as early as possible, so they survive the widest range of later edits without invalidating",
        "Instruction order is purely a style preference with no measurable effect",
        "Only steps involving package managers benefit from being placed early"
      ],
      "correctIndex": 1,
      "explanation": "The npm-ci-before-source pattern is a specific case of a general rule: put whatever changes least often earliest, so the most expensive, most stable steps get invalidated the least frequently.",
      "reference": "<div class=\"rounded border border-slate-300 bg-slate-50 p-2 text-xs dark:border-slate-600 dark:bg-slate-900\">General rule: place a layer as early as its <em>real</em> inputs allow — not just dependency installs, anything genuinely base-image-only.</div>",
      "learnMore": "<h5 class='mt-0 mb-2 text-sm font-semibold text-slate-700 dark:text-slate-200'>Layer ordering generalizes past npm ci</h5><p class='text-sm text-slate-600 dark:text-slate-300'>The dependency-manifest-before-source pattern is one instance of a broader rule: order layers by how often their actual inputs change, cheapest/most-stable first. An OS-level package install that never depends on application files is exactly this case.</p><div class=\"mt-3 rounded-md border border-amber-300 bg-amber-50 p-2 dark:border-amber-800 dark:bg-amber-950\"><p class='text-xs font-semibold text-amber-700 dark:text-amber-400'>Common mistake</p><p class='mt-0.5 text-xs text-amber-800 dark:text-amber-300'>Only applying the \"install before source\" rule to npm/pip-style installs, and missing that the same reasoning applies to any layer with genuinely stable inputs.</p></div>"
    }
  ]
}
