---
title: "L1 — How do we run many containers reliably across many machines? (orchestration basics: Kubernetes concepts)"
---

import Scenario from "../../../components/Scenario.astro";
import LevelIntro from "../../../components/LevelIntro.astro";

<Scenario label="3am: one server dies, and nothing brings the app back">
  <Fragment slot="facts">
    <div class="not-prose flex flex-col gap-2 text-sm text-slate-700 dark:text-slate-300">
      <div class="flex items-center gap-1.5"><span>🖥️</span> <strong>Setup</strong> — 2 servers, containers started manually on each</div>
      <div class="flex items-center gap-1.5"><span>💥</span> <strong>3am</strong> — one server crashes, taking its containers down with it</div>
      <div class="flex items-center gap-1.5"><span>😴</span> <strong>Recovery</strong> — none, until an on-call engineer wakes up and logs in</div>
    </div>
  </Fragment>

A small team runs their app as a handful of containers, started by hand
on two servers — `ssh` in, `docker run`, done. It works, until 3am,
when one server crashes. Its containers don't come back. Nothing was
watching them. Half the app's capacity is gone, and it stays gone
until a paged engineer logs in, notices what died, and manually starts
replacement containers on the surviving server. **The team didn't do
anything wrong when they set this up — so what's actually missing?**

</Scenario>

<LevelIntro
  items={[
    "What container orchestration actually solves",
    "The reconciliation loop, in plain terms",
    "Nodes, pods, and the scheduler's job",
  ]}
/>

## The shape of the problem

- Read the scenario in plain language: a **container** is one running
  copy of an app packaged with what it needs, a **server/node** is a
  machine where that copy can run, and `docker run` is the manual
  command that starts one. If the Docker unit is fresh in your mind,
  this page is asking what happens after you have many containers, not
  just one.
- Running one container by hand is easy. Running enough containers to
  matter, across enough machines to survive one of them dying, without
  a human watching 24/7, is a different problem entirely.
- What's missing isn't more servers or more containers — it's
  something that constantly checks "is what's actually running what
  I said should be running?" and takes action the moment those two
  things disagree, without waiting for a human to notice.
- Deciding _where_ each container should run is also a real problem
  on its own: which machine has enough spare CPU and memory left, and
  what happens to that decision when a machine disappears mid-flight.

## Key terms

- **Container orchestration** — a system that keeps a fleet of
  containers running as intended across multiple machines, handling
  placement and failure recovery without manual intervention.
- **Desired state / reconciliation loop** — the core mechanism: a
  declared target ("5 replicas of this app should be running") is
  continuously compared against the actual observed state, and any
  gap triggers a corrective action.
- **Scheduler** — the component that decides which machine a given
  container should run on, based on each machine's available capacity
  (CPU, memory) and the container's declared resource requirements.
- **Node** — a single machine (physical or virtual) that's part of
  the cluster and can have containers scheduled onto it.
- **Pod** — the smallest schedulable unit in Kubernetes: one or more
  containers that are always placed on the same node together. For this
  unit, you can read "pod" as "the package the scheduler places"; real
  Kubernetes usually schedules pods rather than loose containers.
- **Self-healing** — the reconciliation loop's practical effect: when
  a node dies and takes its containers with it, the loop notices the
  gap between desired and actual state and reschedules the lost
  containers elsewhere, without anyone paging on-call.

## What this unit covers

L2 works through the reconciliation loop's actual mechanics and how a
scheduler decides where each container lands. L3 implements both in
real code — a scheduler that spreads containers across nodes based on
available capacity, and a reconciliation function that detects a
node's failure and reschedules its lost containers onto surviving
nodes, including the case where there isn't enough spare capacity left
to fully recover.
