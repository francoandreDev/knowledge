{
  "items": [
    {
      "type": "quiz",
      "id": "rollback-def-1",
      "poolId": "rollback-def",
      "level": 1,
      "prompt": "What is a \"rollback\" in the sense this unit uses it?",
      "choices": [
        "Reverting a system to its previous known-good deployed state after a bad deploy",
        "A code review process that happens before any deploy",
        "A type of automated test that runs after a deploy",
        "Renaming a git branch back to its original name"
      ],
      "correctIndex": 0,
      "explanation": "A rollback specifically means reverting to the previous deployed state, as distinct from testing, review, or unrelated git operations.",
      "reference": "<div class=\"not-prose flex flex-col gap-1.5 text-sm\"><div class=\"flex items-center gap-1.5\"><span>↩️</span> rollback = revert to the previous known-good deployed state</div><div class=\"flex items-center gap-1.5\"><span>➕</span> fix-forward = ship a new correction on top of the bad version</div></div><p class=\"not-prose text-xs mt-2\"><strong>Why:</strong> These are two different responses to a bad deploy — confusing them means reaching for the wrong tool during an incident.</p>"
    },
    {
      "type": "quiz",
      "id": "rollback-def-2",
      "poolId": "rollback-def",
      "level": 1,
      "prompt": "How does a rollback differ from a \"fix forward\" deploy?",
      "choices": [
        "A rollback reverts to a previous state; a fix-forward deploy ships a new correction on top of the bad version instead of reverting",
        "They are two names for the exact same action",
        "A rollback always requires a database restore, while fix-forward never does",
        "Fix-forward only applies to frontend deploys, rollback only to backend deploys"
      ],
      "correctIndex": 0,
      "explanation": "L1 defines rollback specifically as reverting, in contrast to fixing forward with a new deploy — they're two distinct incident responses, not the same thing under different names.",
      "reference": "<div class=\"not-prose flex flex-col gap-1.5 text-sm\"><div class=\"flex items-center gap-1.5\"><span>↩️</span> rollback = revert to the previous known-good deployed state</div><div class=\"flex items-center gap-1.5\"><span>➕</span> fix-forward = ship a new correction on top of the bad version</div></div><p class=\"not-prose text-xs mt-2\"><strong>Why:</strong> These are two different responses to a bad deploy — confusing them means reaching for the wrong tool during an incident.</p>"
    },
    {
      "type": "quiz",
      "id": "scenario-bottleneck-1",
      "poolId": "scenario-bottleneck",
      "level": 1,
      "prompt": "In L1's Scenario, was the 40-minute rollback slow because the on-call engineer took a long time to decide to roll back?",
      "choices": [
        "No — the decision was fast; the mechanics of actually performing the rollback (rebuilding from source) were the bottleneck",
        "Yes — the Scenario states the engineer spent most of the 40 minutes debating whether to roll back",
        "Yes, because the engineer first tried three other fixes before rolling back",
        "It's not stated in the Scenario whether the decision or the mechanics took longer"
      ],
      "correctIndex": 0,
      "explanation": "L1 is explicit: the decision to roll back was fast, and the 40 minutes was lost in rebuilding the artifact from source — a preparation problem, not a decision problem.",
      "reference": "<div class=\"not-prose flex flex-col gap-1.5 text-sm\"><div class=\"flex items-center gap-1.5\"><span>🔥</span> deciding to roll back: fast</div><div class=\"flex items-center gap-1.5\"><span>🐢</span> rebuilding from source: 40 minutes</div></div><p class=\"not-prose text-xs mt-2\"><strong>Why:</strong> Rollback speed is mostly a preparation problem — whether a ready artifact already exists — not a decision-making problem.</p>"
    },
    {
      "type": "quiz",
      "id": "scenario-bottleneck-2",
      "poolId": "scenario-bottleneck",
      "level": 1,
      "prompt": "What made the difference between the 40-minute team and the 2-minute team in L1's facts?",
      "choices": [
        "Whether a previous deployable artifact was already ready to redeploy, decided before the incident started",
        "The 2-minute team had a larger on-call team available that night",
        "The 40-minute team's bug was objectively more severe",
        "The 2-minute team skipped verifying the rollback actually fixed the problem"
      ],
      "correctIndex": 0,
      "explanation": "The gap is entirely about preparation made in advance — having a ready artifact — not team size, bug severity, or skipped verification.",
      "reference": "<div class=\"not-prose flex flex-col gap-1.5 text-sm\"><div class=\"flex items-center gap-1.5\"><span>🔥</span> deciding to roll back: fast</div><div class=\"flex items-center gap-1.5\"><span>🐢</span> rebuilding from source: 40 minutes</div></div><p class=\"not-prose text-xs mt-2\"><strong>Why:</strong> Rollback speed is mostly a preparation problem — whether a ready artifact already exists — not a decision-making problem.</p>"
    },
    {
      "type": "quiz",
      "id": "artifact-def-1",
      "poolId": "artifact-def",
      "level": 1,
      "prompt": "What is a \"deployable artifact\"?",
      "choices": [
        "The built, ready-to-run output of a deploy (like a container image or compiled binary), as opposed to the source code it was built from",
        "The written documentation describing how a deploy works",
        "A backup of the production database taken before a deploy",
        "The ticket or approval record for a deploy"
      ],
      "correctIndex": 0,
      "explanation": "A deployable artifact is the built, runnable output — not documentation, a database backup, or an approval record.",
      "reference": "<div class=\"not-prose flex flex-col gap-1.5 text-sm\"><div class=\"flex items-center gap-1.5\"><span>💾</span> deployable artifact = the built, ready-to-run output (image/binary)</div><div class=\"flex items-center gap-1.5\"><span>📄</span> source code = what it was built from, not the same thing</div></div><p class=\"not-prose text-xs mt-2\"><strong>Why:</strong> Rollback speed depends on whether the artifact still exists, not whether the source still exists — source has to be rebuilt.</p>"
    },
    {
      "type": "quiz",
      "id": "artifact-def-2",
      "poolId": "artifact-def",
      "level": 1,
      "prompt": "Why does having source code in version control not guarantee a fast rollback?",
      "choices": [
        "Because turning source code into a running artifact requires a build step, which takes time if it has to happen again during an incident",
        "Because version control systems don't reliably store old commits",
        "Because source code cannot be checked out once a newer commit exists",
        "Because rollbacks are illegal to perform from source code directly, by convention"
      ],
      "correctIndex": 0,
      "explanation": "The gap between source and a running artifact is the build step — that's exactly the tax the Scenario's team paid when they had to rebuild during the incident.",
      "reference": "<div class=\"not-prose flex flex-col gap-1.5 text-sm\"><div class=\"flex items-center gap-1.5\"><span>💾</span> deployable artifact = the built, ready-to-run output (image/binary)</div><div class=\"flex items-center gap-1.5\"><span>📄</span> source code = what it was built from, not the same thing</div></div><p class=\"not-prose text-xs mt-2\"><strong>Why:</strong> Rollback speed depends on whether the artifact still exists, not whether the source still exists — source has to be rebuilt.</p>"
    },
    {
      "type": "quiz",
      "id": "migration-safety-def-1",
      "poolId": "migration-safety-def",
      "level": 1,
      "prompt": "What is a \"backward-compatible migration\"?",
      "choices": [
        "A schema change written so that both the old and new application code can run correctly against it",
        "A migration that only adds indexes, never columns",
        "A migration that has been reviewed by at least two engineers",
        "A migration that can be undone with a single SQL command"
      ],
      "correctIndex": 0,
      "explanation": "Backward-compatibility is about both code versions working against the same schema at once — not about indexes specifically, review process, or undo mechanics.",
      "reference": "<div class=\"not-prose flex flex-col gap-1.5 text-sm\"><div class=\"flex items-center gap-1.5\"><span>✅</span> backward-compatible = old code AND new code both work against the schema</div><div class=\"flex items-center gap-1.5\"><span>⚠️</span> without it, a code rollback can crash against the new schema</div></div><p class=\"not-prose text-xs mt-2\"><strong>Why:</strong> A rollback that reverts code but not the schema can leave the system worse off than the original bug.</p>"
    },
    {
      "type": "quiz",
      "id": "migration-safety-def-2",
      "poolId": "migration-safety-def",
      "level": 1,
      "prompt": "Why can a rollback that reverts application code still leave a system broken?",
      "choices": [
        "If the bad deploy also changed the database schema in a way the old code can't handle, the old code may crash against the new schema",
        "Because git does not actually support reverting to an older commit",
        "Because rollbacks always require restarting every server in the fleet",
        "Because reverted code loses access to production secrets automatically"
      ],
      "correctIndex": 0,
      "explanation": "The database migration trap is exactly this: code and schema must agree, and a rollback that only reverts code can leave them mismatched.",
      "reference": "<div class=\"not-prose flex flex-col gap-1.5 text-sm\"><div class=\"flex items-center gap-1.5\"><span>✅</span> backward-compatible = old code AND new code both work against the schema</div><div class=\"flex items-center gap-1.5\"><span>⚠️</span> without it, a code rollback can crash against the new schema</div></div><p class=\"not-prose text-xs mt-2\"><strong>Why:</strong> A rollback that reverts code but not the schema can leave the system worse off than the original bug.</p>"
    },
    {
      "type": "quiz",
      "id": "health-check-def-1",
      "poolId": "health-check-def",
      "level": 1,
      "prompt": "What is a \"health check\" in the context of rollback strategy?",
      "choices": [
        "An automated signal, like error rate or latency, watched continuously after a deploy to judge whether it's behaving correctly",
        "A manual checklist an engineer fills out once, before a deploy starts",
        "A test suite that only runs in a staging environment",
        "A survey sent to users asking if the app feels slow"
      ],
      "correctIndex": 0,
      "explanation": "A health check is specifically an automated, continuous signal — not a pre-deploy checklist, a staging-only test suite, or a user survey."
    },
    {
      "type": "quiz",
      "id": "health-check-def-2",
      "poolId": "health-check-def",
      "level": 1,
      "prompt": "Why does a health check need to be continuous rather than a one-time check right after a deploy finishes?",
      "choices": [
        "Because some regressions only show up after real traffic accumulates, not in the instant a deploy completes",
        "Because continuous checks are required by law in most jurisdictions",
        "Because a one-time check would use too much CPU to be practical",
        "Continuous checking is not actually necessary — a one-time check is equally effective"
      ],
      "correctIndex": 0,
      "explanation": "L1/L2 frame health checks as watching over time specifically because problems can take real traffic to surface — a snapshot at deploy time would miss that."
    },
    {
      "type": "quiz",
      "id": "trigger-def-1",
      "poolId": "trigger-def",
      "level": 1,
      "prompt": "What does \"rollback trigger\" refer to?",
      "choices": [
        "The condition that starts a rollback — either automatic (a health check crosses a threshold) or manual (a human reviews and decides)",
        "The specific git command used to revert a commit",
        "A physical button in a deployment dashboard's UI",
        "The name of the engineer who is on-call during an incident"
      ],
      "correctIndex": 0,
      "explanation": "A rollback trigger is the condition that starts the rollback — automatic or manual — not a specific command, UI element, or person's name."
    },
    {
      "type": "quiz",
      "id": "trigger-def-2",
      "poolId": "trigger-def",
      "level": 1,
      "prompt": "Per L1, can a rollback trigger be either automatic or manual?",
      "choices": [
        "Yes — a health check crossing a threshold can trigger it automatically, or a person can review an ambiguous signal and decide manually",
        "No — rollback triggers must always be fully manual, per L1",
        "No — rollback triggers must always be fully automatic, per L1",
        "L1 states rollback triggers are decided by a vote among the whole engineering team"
      ],
      "correctIndex": 0,
      "explanation": "L1 explicitly defines the trigger as either automatic or manual, depending on the situation — not exclusively one or the other."
    },
    {
      "type": "quiz",
      "id": "incident-response-link-1",
      "poolId": "incident-response-link",
      "level": 1,
      "prompt": "Per L1, when should a rollback strategy be designed, relative to when an incident happens?",
      "choices": [
        "Before the incident — designed in advance, not improvised live during an incident",
        "During the incident, once the exact bug is fully understood",
        "After the incident, as part of the post-mortem",
        "Rollback strategy is described as not needing any advance design at all"
      ],
      "correctIndex": 0,
      "explanation": "L1 draws the same connection incident-response makes: a plan decided in advance is more reliable than one improvised under pressure, live."
    },
    {
      "type": "quiz",
      "id": "l2-migration-necessity-1",
      "level": 2,
      "prompt": "Given the migration trap L2 describes, does that mean risky deploys should just avoid database migrations entirely?",
      "choices": [
        "No — it means migrations need to be written so both code versions can run against the same schema at once, for as long as a rollback might still happen (e.g. expand-contract), not that migrations should be avoided",
        "Yes — L2 concludes migrations should never be run alongside any deploy that changes application code",
        "Yes, but only for deploys made outside of business hours",
        "No, but L2 concludes rollback should be abandoned as a strategy whenever migrations are involved"
      ],
      "correctIndex": 0,
      "explanation": "L2 is explicit that the fix is writing rollback-compatible migrations (like expand-contract), not avoiding migrations altogether — avoidance isn't the lesson, compatibility is."
    },
    {
      "type": "quiz",
      "id": "l2-artifact-speed-1",
      "poolId": "l2-artifact-speed",
      "level": 2,
      "prompt": "Per L2, what closes the gap between \"source code still exists\" and \"a rollback happens instantly\"?",
      "choices": [
        "Keeping the previous deploy's build artifact around after it's superseded, so a rollback redeploys it instead of rebuilding it",
        "Writing more detailed commit messages so engineers can find the right commit faster",
        "Requiring two approvals before any deploy can go out",
        "Increasing the number of engineers on the on-call rotation"
      ],
      "correctIndex": 0,
      "explanation": "L2's core fast-rollback mechanic is retention of the previous build artifact — not commit messages, approvals, or staffing.",
      "reference": "<div class=\"not-prose flex flex-col gap-1.5 text-sm\"><div class=\"flex items-center gap-1.5\"><span>📦</span> keep the previous artifact ready — don't discard it after a successful deploy</div><div class=\"flex items-center gap-1.5\"><span>♻️</span> rollback = redeploy that existing artifact, same mechanism as a forward deploy</div></div><p class=\"not-prose text-xs mt-2\"><strong>Why:</strong> Deleting old artifacts to save storage turns every future rollback into a rebuild-from-source under incident pressure.</p>"
    },
    {
      "type": "quiz",
      "id": "l2-artifact-speed-2",
      "poolId": "l2-artifact-speed",
      "level": 2,
      "prompt": "A team deletes container images for any version older than the currently-running one, right after each successful deploy. What will their next rollback look like?",
      "choices": [
        "Slow — there's no ready artifact for the previous version, so a rollback has to rebuild it from source first",
        "Fast — deleting old images has no effect on rollback speed",
        "Fast, but only if the rollback happens within the first hour of the new deploy",
        "It depends only on how many replicas the service currently has running"
      ],
      "correctIndex": 0,
      "explanation": "This is exactly the failure mode L2's Checkpoint covers — discarding the previous artifact removes the thing that makes rollback fast, forcing a rebuild under pressure.",
      "reference": "<div class=\"not-prose flex flex-col gap-1.5 text-sm\"><div class=\"flex items-center gap-1.5\"><span>📦</span> keep the previous artifact ready — don't discard it after a successful deploy</div><div class=\"flex items-center gap-1.5\"><span>♻️</span> rollback = redeploy that existing artifact, same mechanism as a forward deploy</div></div><p class=\"not-prose text-xs mt-2\"><strong>Why:</strong> Deleting old artifacts to save storage turns every future rollback into a rebuild-from-source under incident pressure.</p>"
    },
    {
      "type": "quiz",
      "id": "l2-migration-table-1",
      "poolId": "l2-migration-table",
      "level": 2,
      "prompt": "Per L2's migration pattern table, is dropping a column the old code still reads rollback-compatible?",
      "choices": [
        "No — the old code will crash or error trying to read a column that no longer exists",
        "Yes — dropping columns is always safe as long as the drop happens quickly",
        "Yes, as long as the column was empty (all null) before it was dropped",
        "It depends only on which database engine is being used"
      ],
      "correctIndex": 0,
      "explanation": "L2's table is explicit: dropping a column the old code reads breaks that old code the moment it's redeployed — not safe regardless of speed, emptiness, or engine.",
      "reference": "<div class=\"not-prose overflow-x-auto text-sm\"><table class=\"not-prose w-full text-left\"><tbody><tr><td class=\"pr-3 py-1\">❌ Drop a column old code reads</td><td class=\"py-1\">old code crashes</td></tr><tr><td class=\"pr-3 py-1\">❌ Rename in place</td><td class=\"py-1\">one version always mismatches</td></tr><tr><td class=\"pr-3 py-1\">✅ Add nullable column</td><td class=\"py-1\">old code ignores it</td></tr><tr><td class=\"pr-3 py-1\">✅ Expand-contract</td><td class=\"py-1\">both versions work until the later drop</td></tr></tbody></table></div><p class=\"not-prose text-xs mt-2\"><strong>Why:</strong> Rollback safety depends on whether the OLD code, not just the new code, can still run against the current schema.</p>"
    },
    {
      "type": "quiz",
      "id": "l2-migration-table-2",
      "poolId": "l2-migration-table",
      "level": 2,
      "prompt": "Per L2's migration pattern table, is renaming a column in place, in the same deploy as a code change, rollback-compatible?",
      "choices": [
        "No — whichever code version doesn't match the current name (old or new) breaks",
        "Yes — renames are always safe because the data itself is unchanged",
        "Yes, as long as the rename happens in a separate database transaction",
        "It's only unsafe for renamed primary key columns, safe for all other columns"
      ],
      "correctIndex": 0,
      "explanation": "A straight rename means only one code version's expected column name is correct at any moment — exactly like a drop, just with a different failure mode.",
      "reference": "<div class=\"not-prose overflow-x-auto text-sm\"><table class=\"not-prose w-full text-left\"><tbody><tr><td class=\"pr-3 py-1\">❌ Drop a column old code reads</td><td class=\"py-1\">old code crashes</td></tr><tr><td class=\"pr-3 py-1\">❌ Rename in place</td><td class=\"py-1\">one version always mismatches</td></tr><tr><td class=\"pr-3 py-1\">✅ Add nullable column</td><td class=\"py-1\">old code ignores it</td></tr><tr><td class=\"pr-3 py-1\">✅ Expand-contract</td><td class=\"py-1\">both versions work until the later drop</td></tr></tbody></table></div><p class=\"not-prose text-xs mt-2\"><strong>Why:</strong> Rollback safety depends on whether the OLD code, not just the new code, can still run against the current schema.</p>"
    },
    {
      "type": "quiz",
      "id": "l2-expand-contract-1",
      "poolId": "l2-expand-contract",
      "level": 2,
      "prompt": "What is the \"expand-contract\" pattern?",
      "choices": [
        "Add the new column first and write both old and new, then drop the old column only in a later, separate deploy after the rollback window has passed",
        "Expand the database's storage capacity before contracting it back down after a migration",
        "Run a migration twice — once expanded across all shards, then contracted to just one",
        "A technique for compressing database backups before archiving them"
      ],
      "correctIndex": 0,
      "explanation": "Expand-contract specifically means splitting a schema change into an additive step and a much-later removal step — not about storage capacity, sharding, or backups.",
      "reference": "<div class=\"not-prose flex flex-col gap-1.5 text-sm\"><div class=\"flex items-center gap-1.5\"><span>1️⃣</span> expand: add the new column, write both, remove nothing</div><div class=\"flex items-center gap-1.5\"><span>2️⃣</span> contract: drop the old column, only in a LATER separate deploy</div></div><p class=\"not-prose text-xs mt-2\"><strong>Why:</strong> Splitting the risky code deploy from the schema cleanup means the deploy that might get rolled back never depends on something being removed.</p>"
    },
    {
      "type": "quiz",
      "id": "l2-expand-contract-2",
      "poolId": "l2-expand-contract",
      "level": 2,
      "prompt": "Why does expand-contract put the column drop in a separate, later deploy instead of the same deploy as the code change?",
      "choices": [
        "So that the risky code deploy, which might need a rollback, never depends on something already being removed — the drop only happens once a rollback is no longer a realistic concern",
        "Because most databases technically forbid more than one schema change per deploy",
        "Because the code change and the schema drop must always be reviewed by different engineers",
        "To reduce the total number of git commits associated with the feature"
      ],
      "correctIndex": 0,
      "explanation": "The whole point of separating the steps is timing: the drop is safe only once the code deploy's rollback window has passed, so it's deliberately pushed to its own later deploy.",
      "reference": "<div class=\"not-prose flex flex-col gap-1.5 text-sm\"><div class=\"flex items-center gap-1.5\"><span>1️⃣</span> expand: add the new column, write both, remove nothing</div><div class=\"flex items-center gap-1.5\"><span>2️⃣</span> contract: drop the old column, only in a LATER separate deploy</div></div><p class=\"not-prose text-xs mt-2\"><strong>Why:</strong> Splitting the risky code deploy from the schema cleanup means the deploy that might get rolled back never depends on something being removed.</p>"
    },
    {
      "type": "quiz",
      "id": "l2-trigger-boundary-1",
      "poolId": "l2-trigger-boundary",
      "level": 2,
      "prompt": "Per L2, when is it appropriate to let a rollback happen fully automatically, without a human decision?",
      "choices": [
        "When the failure signature is sharp, sustained, and well-understood — unambiguous enough that a machine acting alone is low-risk",
        "Only during business hours, never overnight",
        "Only for deploys smaller than a certain number of changed files",
        "Never — L2 states every rollback must always involve a human decision"
      ],
      "correctIndex": 0,
      "explanation": "L2's flowchart routes unambiguous, clearly-over-threshold signals to automatic rollback, and ambiguous ones to a human — the deciding factor is signal certainty, not time of day or deploy size."
    },
    {
      "type": "quiz",
      "id": "l2-trigger-boundary-2",
      "poolId": "l2-trigger-boundary",
      "level": 2,
      "prompt": "Per L2, why isn't a slow, mild uptick in latency automatically rolled back the same way a sharp error-rate spike is?",
      "choices": [
        "Because a mild, ambiguous signal might just be an unrelated traffic pattern, and automating a rollback on it risks reverting a perfectly good deploy over noise",
        "Because latency metrics are technically impossible to monitor automatically",
        "Because only error rate, never latency, is considered a valid health signal",
        "Because mild signals always turn out to be false alarms, so they're ignored entirely"
      ],
      "correctIndex": 0,
      "explanation": "L2 draws the line at ambiguity, not at which metric is involved — an ambiguous latency uptick is exactly the kind of signal better left to a human's broader context."
    },
    {
      "type": "quiz",
      "id": "l2-checkpoint-echo-1",
      "level": 2,
      "prompt": "A deploy ships a code change and, in the same deploy, a migration that drops a column the old code reads. If the new code turns out to be buggy, what's the actual risk of redeploying the old code as a rollback?",
      "choices": [
        "The old code will error or crash reading a column that the migration already removed, potentially making things worse than the original bug",
        "There is no risk — redeploying old code is always safe regardless of schema changes",
        "The only risk is a brief increase in deploy time, not a functional break",
        "The risk only applies if the column being dropped was the primary key"
      ],
      "correctIndex": 0,
      "explanation": "This is L2's central nuance: a code-only rollback assumes the schema underneath hasn't moved in an incompatible way — when it has, the 'safe' rollback isn't actually safe."
    },
    {
      "type": "quiz",
      "id": "l2-blue-green-link-1",
      "level": 2,
      "prompt": "How does L2 connect blue-green deployment and feature flags (from progressive-delivery) to this unit's rollback model?",
      "choices": [
        "Both are concrete instances of \"keep the known-good state ready\" — a warm second environment, or a flag that can flip a behavior off instantly without a redeploy",
        "They are described as unrelated to rollback strategy and only relevant to gradual traffic exposure",
        "Blue-green and feature flags are described as slower than a standard artifact-based rollback in every case",
        "L2 states that only feature flags, not blue-green, count as a real rollback mechanism"
      ],
      "correctIndex": 0,
      "explanation": "L2 explicitly frames both as the same underlying principle as a ready build artifact — having the known-good state prepared in advance — just implemented at different layers."
    },
    {
      "type": "quiz",
      "id": "l2-preplanning-1",
      "level": 2,
      "prompt": "Per L2 (and L1's incident-response connection), why is deciding \"what does rollback mean for this system\" a bad thing to figure out live, during an incident?",
      "choices": [
        "Because that's exactly the moment the decision is most likely to be made badly — the same reasoning incident-response applies to disclosure and post-mortems being planned in advance",
        "Because most incident-response tooling technically locks configuration changes during an active incident",
        "Because engineers are contractually required to have a rollback plan documented before deploying",
        "It isn't actually a problem — L2 states live decisions during an incident are usually higher quality"
      ],
      "correctIndex": 0,
      "explanation": "L1/L2 draw a direct parallel to incident-response's pre-planning principle: decisions made under live incident pressure are worse than the same decisions made calmly in advance."
    },
    {
      "type": "quiz",
      "id": "l2-model-summary-1",
      "level": 2,
      "prompt": "Per L2's overall model, does a rollback strategy consist of a single decision, or several separate concerns?",
      "choices": [
        "Several separate concerns: how fast reverting can happen, whether reverting is actually safe given the schema, and what triggers the revert (automatic or manual)",
        "A single decision: whichever engineer is on-call chooses to revert or not",
        "A single technical setting: how many replicas the deployment runs",
        "L2 states rollback strategy reduces entirely to picking a confirmation-window size"
      ],
      "correctIndex": 0,
      "explanation": "L2 explicitly separates speed (artifact readiness), safety (migration compatibility), and triggering (automatic vs. manual) as three distinct concerns that together make up a rollback strategy, not one single decision."
    },
    {
      "type": "code",
      "id": "l3-health-decision-complete",
      "poolId": "l3-health-decision-code",
      "level": 3,
      "prompt": "Complete decideRollbackAction(state, metrics, config) so it returns { action: 'rollback' } once errorRate (errors/sampleSize) reaches or exceeds config.severeErrorRateThreshold, without needing any consecutive bad windows first. Assume sampleSize always meets config.minSampleSize in these tests.",
      "starterCode": "function decideRollbackAction(state, metrics, config) {\n  const errorRate = metrics.errors / metrics.sampleSize;\n  // TODO: if errorRate is severe (>= config.severeErrorRateThreshold),\n  // return { action: 'rollback', state: { consecutiveBadWindows: 0, consecutiveGoodWindows: 0 } }\n  // otherwise, for this exercise, just return { action: 'watching', state };\n  return { action: 'watching', state };\n}\n",
      "tests": [
        {
          "description": "a severe error rate triggers immediate rollback",
          "expr": "expect(decideRollbackAction({consecutiveBadWindows:0,consecutiveGoodWindows:0}, {errors:40,sampleSize:100}, {severeErrorRateThreshold:0.25}).action).toBe('rollback')"
        },
        {
          "description": "an error rate below the severe threshold does not roll back",
          "expr": "expect(decideRollbackAction({consecutiveBadWindows:0,consecutiveGoodWindows:0}, {errors:2,sampleSize:100}, {severeErrorRateThreshold:0.25}).action).toBe('watching')"
        }
      ],
      "solution": "function decideRollbackAction(state, metrics, config) {\n  const errorRate = metrics.errors / metrics.sampleSize;\n  if (errorRate >= config.severeErrorRateThreshold) {\n    return { action: 'rollback', state: { consecutiveBadWindows: 0, consecutiveGoodWindows: 0 } };\n  }\n  return { action: 'watching', state };\n}\n",
      "explanation": "A severe error rate is unambiguous enough that the function shouldn't wait for consecutive confirmation windows — it should bypass the counters entirely and act immediately, exactly like L3's classifyWindow/decideRollbackAction pair does for its 'severe' classification.",
      "reference": "<div class=\"not-prose flex flex-col gap-1.5 text-sm\"><div class=\"flex items-center gap-1.5\"><span>🚨</span> severe = errorRate above the severe threshold</div><div class=\"flex items-center gap-1.5\"><span>⚡</span> severe skips the confirmation counters entirely</div></div><p class=\"not-prose text-xs mt-2\"><strong>Why:</strong> Waiting for confirmation on an unambiguous spike only costs time without buying real confidence.</p>"
    },
    {
      "type": "code",
      "id": "l3-health-decision-fix",
      "poolId": "l3-health-decision-code",
      "level": 3,
      "prompt": "This buggy decideRollbackAction checks errorRate against config.severeErrorRateThreshold using the wrong comparison (> instead of >=), so a window exactly at the threshold is missed. Fix the comparison.",
      "starterCode": "function decideRollbackAction(state, metrics, config) {\n  const errorRate = metrics.errors / metrics.sampleSize;\n  if (errorRate > config.severeErrorRateThreshold) {\n    return { action: 'rollback', state: { consecutiveBadWindows: 0, consecutiveGoodWindows: 0 } };\n  }\n  return { action: 'watching', state };\n}\n",
      "tests": [
        {
          "description": "an error rate exactly at the severe threshold triggers rollback",
          "expr": "expect(decideRollbackAction({consecutiveBadWindows:0,consecutiveGoodWindows:0}, {errors:25,sampleSize:100}, {severeErrorRateThreshold:0.25}).action).toBe('rollback')"
        },
        {
          "description": "an error rate above the severe threshold still triggers rollback",
          "expr": "expect(decideRollbackAction({consecutiveBadWindows:0,consecutiveGoodWindows:0}, {errors:40,sampleSize:100}, {severeErrorRateThreshold:0.25}).action).toBe('rollback')"
        }
      ],
      "solution": "function decideRollbackAction(state, metrics, config) {\n  const errorRate = metrics.errors / metrics.sampleSize;\n  if (errorRate >= config.severeErrorRateThreshold) {\n    return { action: 'rollback', state: { consecutiveBadWindows: 0, consecutiveGoodWindows: 0 } };\n  }\n  return { action: 'watching', state };\n}\n",
      "explanation": "config.severeErrorRateThreshold is meant to be the boundary that counts as severe, so a window exactly at that rate should still trigger a rollback — that requires >=, not a strict >, which would silently let a threshold-exact spike through as merely 'watching'.",
      "reference": "<div class=\"not-prose flex flex-col gap-1.5 text-sm\"><div class=\"flex items-center gap-1.5\"><span>🚨</span> severe = errorRate above the severe threshold</div><div class=\"flex items-center gap-1.5\"><span>⚡</span> severe skips the confirmation counters entirely</div></div><p class=\"not-prose text-xs mt-2\"><strong>Why:</strong> Waiting for confirmation on an unambiguous spike only costs time without buying real confidence.</p>"
    },
    {
      "type": "code",
      "id": "l3-consecutive-bad-complete",
      "poolId": "l3-consecutive-code",
      "level": 3,
      "prompt": "Complete countConsecutiveBad(windows, isBad) so it returns the length of the longest run of consecutive 'bad' windows at the END of the array (a trailing streak), given isBad(window) returns true/false per window. If the array ends on a good window, the answer is 0.",
      "starterCode": "function countConsecutiveBad(windows, isBad) {\n  // TODO: count how many windows, counting backwards from the end,\n  // are 'bad' before the first good one is hit\n  return 0;\n}\n",
      "tests": [
        {
          "description": "three bad windows in a row at the end returns 3",
          "expr": "expect(countConsecutiveBad([{e:1},{e:9},{e:9},{e:9}], w => w.e > 5)).toBe(3)"
        },
        {
          "description": "ending on a good window returns 0",
          "expr": "expect(countConsecutiveBad([{e:9},{e:9},{e:1}], w => w.e > 5)).toBe(0)"
        },
        {
          "description": "all windows bad returns the full length",
          "expr": "expect(countConsecutiveBad([{e:9},{e:9}], w => w.e > 5)).toBe(2)"
        }
      ],
      "solution": "function countConsecutiveBad(windows, isBad) {\n  let count = 0;\n  for (let i = windows.length - 1; i >= 0; i--) {\n    if (!isBad(windows[i])) break;\n    count++;\n  }\n  return count;\n}\n",
      "explanation": "The decision function only cares about the current trailing streak, not the whole history, so this walks backward from the end and stops at the first good window — mirroring how decideRollbackAction's consecutiveBadWindows counter resets to 0 the instant a good window appears."
    },
    {
      "type": "code",
      "id": "l3-consecutive-bad-fix",
      "poolId": "l3-consecutive-code",
      "level": 3,
      "prompt": "This buggy countConsecutiveBad counts bad windows across the WHOLE array instead of just the trailing streak at the end, so an old bad window earlier in the array wrongly still gets counted even after a good window has since occurred. Fix it so only the trailing run counts.",
      "starterCode": "function countConsecutiveBad(windows, isBad) {\n  let count = 0;\n  for (let i = 0; i < windows.length; i++) {\n    if (isBad(windows[i])) count++;\n  }\n  return count;\n}\n",
      "tests": [
        {
          "description": "a bad window followed by a good window at the end returns 0, not 1",
          "expr": "expect(countConsecutiveBad([{e:9},{e:1}], w => w.e > 5)).toBe(0)"
        },
        {
          "description": "three trailing bad windows returns 3",
          "expr": "expect(countConsecutiveBad([{e:1},{e:9},{e:9},{e:9}], w => w.e > 5)).toBe(3)"
        }
      ],
      "solution": "function countConsecutiveBad(windows, isBad) {\n  let count = 0;\n  for (let i = windows.length - 1; i >= 0; i--) {\n    if (!isBad(windows[i])) break;\n    count++;\n  }\n  return count;\n}\n",
      "explanation": "Summing bad windows across the entire history ignores that a good window should reset the streak — the fix walks backward from the end and stops at the first good window, so only the current unbroken trailing run counts, matching the state machine's own reset-on-good behavior."
    },
    {
      "type": "quiz",
      "id": "l3-flapping-1",
      "poolId": "l3-flapping",
      "level": 3,
      "prompt": "What does \"flapping\" mean in the context of an automatic rollback trigger?",
      "choices": [
        "A trigger so sensitive that ordinary metric noise repeatedly rolls back perfectly healthy deploys",
        "A deploy that alternates between two different versions on purpose, for A/B testing",
        "A health check that never returns a result and hangs indefinitely",
        "A rollback that succeeds but takes longer than the original deploy did"
      ],
      "correctIndex": 0,
      "explanation": "Flapping is specifically about over-sensitivity causing repeated unnecessary rollbacks on noise — not intentional A/B alternation, a hang, or a slow-but-successful rollback.",
      "reference": "<div class=\"not-prose flex flex-col gap-1.5 text-sm\"><div class=\"flex items-center gap-1.5\"><span>🪢</span> too sensitive → flapping: healthy deploys get rolled back on noise</div><div class=\"flex items-center gap-1.5\"><span>🐌</span> too conservative → slow: a real regression runs for a long time before anything acts</div></div><p class=\"not-prose text-xs mt-2\"><strong>Why:</strong> The confirmation-window size is a real trade-off with no single correct value — it depends on the system's own baseline noise.</p>"
    },
    {
      "type": "quiz",
      "id": "l3-flapping-2",
      "poolId": "l3-flapping",
      "level": 3,
      "prompt": "L3's Scenario B (a single noisy blip, then three good windows) resulted in 'healthy', not 'rollback'. What would have changed that outcome if badWindowsToTriggerRollback had been set to 1 instead of 3?",
      "choices": [
        "The single blip alone would have triggered an unnecessary rollback of a perfectly healthy deploy — this is exactly what flapping looks like",
        "Nothing would change — the outcome does not depend on that configuration value at all",
        "It would have taken longer to reach 'healthy', but never triggered a rollback",
        "The severe threshold would have activated instead, rolling back immediately regardless"
      ],
      "correctIndex": 0,
      "explanation": "L3 states this directly: with the confirmation requirement lowered to 1, the same noisy blip that Scenario B correctly absorbed would have caused a false-positive rollback instead.",
      "reference": "<div class=\"not-prose flex flex-col gap-1.5 text-sm\"><div class=\"flex items-center gap-1.5\"><span>🪢</span> too sensitive → flapping: healthy deploys get rolled back on noise</div><div class=\"flex items-center gap-1.5\"><span>🐌</span> too conservative → slow: a real regression runs for a long time before anything acts</div></div><p class=\"not-prose text-xs mt-2\"><strong>Why:</strong> The confirmation-window size is a real trade-off with no single correct value — it depends on the system's own baseline noise.</p>"
    },
    {
      "type": "quiz",
      "id": "l3-conservative-tradeoff-1",
      "poolId": "l3-conservative-tradeoff",
      "level": 3,
      "prompt": "Per L3, what's the cost of setting badWindowsToTriggerRollback very high (e.g. 10) to avoid flapping entirely?",
      "choices": [
        "A real regression now runs in production for many more windows before anything acts, undercutting the speed benefit automation was meant to provide",
        "There is no downside — a higher number is strictly better in every situation",
        "It only affects how the rollback is logged, not when it actually happens",
        "It causes the severe-threshold bypass to stop working entirely"
      ],
      "correctIndex": 0,
      "explanation": "L3 is explicit: over-conservative tuning reintroduces the exact slowness problem this whole unit opened with, just with extra steps."
    },
    {
      "type": "quiz",
      "id": "l3-conservative-tradeoff-2",
      "poolId": "l3-conservative-tradeoff",
      "level": 3,
      "prompt": "Per L3, is there a single universally correct value for badWindowsToTriggerRollback that every system should use?",
      "choices": [
        "No — the right value depends on how noisy a given system's baseline metrics are and how costly a false-positive rollback is relative to extra minutes of a real regression",
        "Yes — L3 states 3 consecutive windows is the universal industry standard for all systems",
        "Yes — the correct value is always equal to the number of replicas running",
        "No such value can ever be chosen; L3 argues rollback triggers should never use consecutive windows"
      ],
      "correctIndex": 0,
      "explanation": "L3 explicitly rejects a one-size-fits-all number — it's a tuning trade-off specific to each system's own traffic and cost profile."
    },
    {
      "type": "quiz",
      "id": "l3-unsafe-migration-1",
      "poolId": "l3-unsafe-migration",
      "level": 3,
      "prompt": "In L3's unsafe migration example, what specifically breaks if v42 is bad and the team redeploys v41 after legacy_status has already been dropped?",
      "choices": [
        "Every call to getOrderSummary in v41 throws, because it reads a column (legacy_status) that no longer exists in the schema",
        "Nothing breaks — the rollback succeeds and the incident is fully resolved",
        "Only new orders created after the rollback are affected, not existing ones",
        "The rollback silently succeeds but order statuses display as blank for all users"
      ],
      "correctIndex": 0,
      "explanation": "L3 traces the exact failure: v41's getOrderSummary function reads legacy_status directly, and that column is gone, so every call throws — a new, guaranteed incident on top of the original bug.",
      "reference": "<div class=\"not-prose flex flex-col gap-1.5 text-sm\"><div class=\"flex items-center gap-1.5\"><span>❌</span> unsafe: drop legacy_status in the same deploy → v41 rollback crashes reading it</div><div class=\"flex items-center gap-1.5\"><span>✅</span> safe: add order_status, write both, drop legacy_status only in a LATER deploy</div></div><p class=\"not-prose text-xs mt-2\"><strong>Why:</strong> A rollback is only as safe as the least backward-compatible thing shipped alongside it.</p>"
    },
    {
      "type": "quiz",
      "id": "l3-unsafe-migration-2",
      "poolId": "l3-unsafe-migration",
      "level": 3,
      "prompt": "In L3's safe (expand-contract) rewrite, why is v41 unaffected even while v42 is live and writing to both columns?",
      "choices": [
        "Because v41 was never changed to know about order_status — it only ever reads legacy_status, which the expand step never touches",
        "Because v41 automatically detects and adapts to schema changes at runtime",
        "Because the database rejects any write to order_status if v41 is also running",
        "Because v41 and v42 are described as running on entirely separate databases"
      ],
      "correctIndex": 0,
      "explanation": "The safety comes from what's NOT touched during the expand step — legacy_status stays exactly as v41 expects it, so a rollback to v41 during this window changes nothing about what v41 depends on.",
      "reference": "<div class=\"not-prose flex flex-col gap-1.5 text-sm\"><div class=\"flex items-center gap-1.5\"><span>❌</span> unsafe: drop legacy_status in the same deploy → v41 rollback crashes reading it</div><div class=\"flex items-center gap-1.5\"><span>✅</span> safe: add order_status, write both, drop legacy_status only in a LATER deploy</div></div><p class=\"not-prose text-xs mt-2\"><strong>Why:</strong> A rollback is only as safe as the least backward-compatible thing shipped alongside it.</p>"
    },
    {
      "type": "quiz",
      "id": "l3-generalization-1",
      "level": 3,
      "prompt": "Per L3's closing section, does the migration-safety lesson only apply to databases, or does it generalize further?",
      "choices": [
        "It generalizes to any coupled, independently-versioned pair, like a mobile app and its API, or a message queue producer and consumer",
        "It applies strictly and only to relational database schemas, per L3",
        "It generalizes to literally any two files in the same codebase",
        "L3 states the lesson does not generalize beyond this exact worked example"
      ],
      "correctIndex": 0,
      "explanation": "L3 explicitly extends the core lesson (a rollback is only as safe as the least backward-compatible thing shipped with it) to any independently-versioned coupled pair, not just databases."
    },
    {
      "type": "quiz",
      "id": "l3-extension-question-1",
      "level": 3,
      "prompt": "L3's extension question asks what happens if a bad deploy already accepted writes in a new, incompatible format before rollback triggered. What does this add to the migration-safety discussion?",
      "choices": [
        "That redeploying old code alone may not be enough — data already written in the new format might also need a repair step, beyond just reverting code and schema",
        "Nothing new — L3 states this scenario is functionally identical to the ordinary expand-contract case",
        "That this situation makes automatic rollback triggers entirely unnecessary going forward",
        "That this situation can only occur if the severe-threshold bypass was never configured"
      ],
      "correctIndex": 0,
      "explanation": "This is exactly the extension L3 poses: a rollback undoes code and (with the right migration pattern) schema, but data already written under the bad format is a separate problem a code/schema rollback alone doesn't fix."
    },
    {
      "type": "quiz",
      "id": "l3-insufficient-data-1",
      "poolId": "l3-insufficient-data",
      "level": 3,
      "prompt": "In classifyWindow, why does a window with too few requests (below minSampleSize) return 'insufficient-data' instead of being judged as good or bad?",
      "choices": [
        "A rate computed from very few requests is unreliable — a small absolute number of errors can produce a misleadingly extreme rate at low volume",
        "Because the function is designed to always favor 'good' when traffic is low, to avoid unnecessary rollbacks",
        "Because computing a percentage requires at least 1,000 requests, by definition",
        "It's an arbitrary safeguard with no real justification given in L3"
      ],
      "correctIndex": 0,
      "explanation": "L3's own Checkpoint covers this directly: 1 error out of 4 requests is a 25% rate that would look severe, but it's meaningless noise at that volume — insufficient-data avoids trusting a rate that small a sample can't support.",
      "reference": "<div class=\"not-prose flex flex-col gap-1.5 text-sm\"><div class=\"flex items-center gap-1.5\"><span>🔢</span> a rate from a tiny sample can swing wildly on a couple of errors</div><div class=\"flex items-center gap-1.5\"><span>⏸️</span> insufficient-data = wait for more traffic before trusting the rate</div></div><p class=\"not-prose text-xs mt-2\"><strong>Why:</strong> A computed rate only means something once there's enough volume behind it.</p>"
    },
    {
      "type": "quiz",
      "id": "l3-insufficient-data-2",
      "poolId": "l3-insufficient-data",
      "level": 3,
      "prompt": "A deploy's health check window sees only 8 requests total, 2 of which errored (a 25% error rate, at or above a 25% severe threshold). Per classifyWindow's logic, should this trigger an immediate rollback?",
      "choices": [
        "No — with minSampleSize unmet, classifyWindow should return 'insufficient-data' rather than trusting a rate computed from only 8 requests",
        "Yes — any error rate at or above the severe threshold triggers immediate rollback, regardless of sample size",
        "Yes, but only because 2 raw errors is itself an unacceptably high number",
        "No, because classifyWindow only ever evaluates latency, never error rate, at low traffic volumes"
      ],
      "correctIndex": 0,
      "explanation": "The sample-size check runs before the rate is even computed against thresholds — with too few requests, classifyWindow deliberately withholds judgment rather than acting on a rate that small a sample can't support.",
      "reference": "<div class=\"not-prose flex flex-col gap-1.5 text-sm\"><div class=\"flex items-center gap-1.5\"><span>🔢</span> a rate from a tiny sample can swing wildly on a couple of errors</div><div class=\"flex items-center gap-1.5\"><span>⏸️</span> insufficient-data = wait for more traffic before trusting the rate</div></div><p class=\"not-prose text-xs mt-2\"><strong>Why:</strong> A computed rate only means something once there's enough volume behind it.</p>"
    }
  ]
}
