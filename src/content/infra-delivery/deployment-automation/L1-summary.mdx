---
title: "L1 — Why do manual deploys eventually cause an outage?"
---

import Scenario from "../../../components/Scenario.astro";

<Scenario label="The deploy that worked fine for eight months">
  <Fragment slot="facts">
    <div class="not-prose flex flex-col gap-2 text-sm text-slate-700 dark:text-slate-300">
      <div class="flex items-center gap-1.5"><span>📅</span> <strong>Deploys 1–8</strong> — SSH in, `git pull`, `npm install`, restart — no incidents</div>
      <div class="flex items-center gap-1.5"><span>💥</span> <strong>Deploy 9</strong> — same steps, but `npm install` gets skipped, service crashes on restart</div>
      <div class="flex items-center gap-1.5"><span>⏱️</span> <strong>Result</strong> — 20 minutes of downtime spent diagnosing before anyone notices the missed step</div>
    </div>
  </Fragment>

Every release, an engineer SSHes into the production server, runs
`git pull`, then `npm install`, then restarts the service. This has
worked without incident for eight releases in a row. On the ninth, a
new dependency was added to the codebase, but the engineer running
this deploy — someone else, in a hurry — skips `npm install` because
it "usually doesn't matter." The service crashes on restart. **The
steps didn't change. What did?**

</Scenario>

## The shape of the problem

- Read the scenario in plain language: **SSH** means "log into a
  remote machine," the **production server** is the machine real users
  are depending on, `git pull` brings down the newest code, and
  `npm install` installs any outside packages the code now needs. A
  **dependency** is one of those outside packages; **downtime** is the
  period where users cannot use the service.
- A deploy is really a short sequence of steps that all have to happen,
  in order, every single time — but when a human executes that
  sequence from memory or a wiki page, nothing enforces that the steps
  actually happen, or happen in the right order.
- The failure isn't really about `npm install` specifically — it's
  that a manual process's correctness depends on the person running it
  remembering and following every step exactly, and that assumption
  quietly breaks the moment someone is rushed, new, or simply tired.

## Key terms

- **Deployment** — the process of taking a new version of code from a
  built artifact to a running, live traffic-serving service. A built
  artifact is the packaged output the server can run; if that idea is
  still fuzzy, the Docker unit's image/container distinction is the
  closest nearby mental model.
- **Idempotency** — running the same deploy steps twice (or replaying
  them after a partial failure) produces the same correct end state,
  rather than compounding errors or requiring a human to figure out what
  already happened. Everyday version: pressing "save" twice should not
  create two copies of the same profile.
- **Repeatability** — the exact same sequence of steps runs the same
  way every time, regardless of who triggers the deploy or how tired
  they are — this is what a manual checklist can't actually guarantee.
- **Atomic switch** — flipping traffic from the old version to the new
  one in a single, instantaneous step (like swapping a symlink) so
  there's never a moment where some requests hit a half-updated state.
- **Rollback** — automatically reverting to the last known-good version
  when a new deploy fails a health check, instead of leaving the
  broken version live while someone investigates.

## What this unit covers

L2 works through what a scripted deploy pipeline actually looks like —
the fixed step order, the health check, and the automatic rollback —
and why each of those is specifically what a manual process can't
reliably guarantee. L3 implements a real deploy-pipeline function and a
real rollback-triggering function, to show precisely why the ninth
deploy's `npm install` mistake couldn't happen to a scripted pipeline.
