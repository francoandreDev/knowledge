---
title: "L3 — Both searches in full, and what to do when there's no single source"
---

import Scenario from "../../../components/Scenario.astro";
import LevelIntro from "../../../components/LevelIntro.astro";
import Checkpoint from "../../../components/Checkpoint.astro";

<Scenario label="The full search session, both ways">
  <Fragment slot="facts">
    <div class="not-prose flex flex-col gap-2 text-sm text-slate-700 dark:text-slate-300">
      <div class="flex items-center gap-1.5"><span>📶</span> <strong>Symptom</strong> — Wi-Fi drops for a few seconds, several times a day</div>
      <div class="flex items-center gap-1.5"><span>🔎</span> <strong>Anchors available but unused at first</strong> — the router's model number, printed on the device itself</div>
    </div>
  </Fragment>

**Before reading either search: is there any anchor term Jordan already
has access to before typing a single query — even before search 1?**

</Scenario>

<LevelIntro
  items={[
    "Both full searches, side by side",
    "What to do with no single authoritative source",
    "Searching before you know the domain's own vocabulary",
  ]}
/>

## Search 1 — the vague version

> **Query:** "wifi keeps disconnecting"
>
> **Results, skimmed for 20 minutes:**
>
> - A 2014 forum thread blaming "interference," no specifics
> - A completely different router brand's troubleshooting page
> - Several posts recommending "restart your router" with no diagnosis
> - One reply, buried in a thread, mentioning: _"Same thing happened on my
>   [router model] — turned out to be a firmware bug"_

The one useful thing in twenty minutes of scrolling was a single reply
mentioning the exact router model and the word "firmware" — neither of
which Jordan had searched for, both of which were sitting on the bottom of
the router itself the entire time.

## Search 2 — using what search 1 surfaced

> **Query:** "[router model] intermittent disconnect firmware known issue"
>
> **Results:**
>
> - **Result 2: the manufacturer's own support page**, titled exactly
>   "Intermittent Wi-Fi Disconnects — Firmware 2.3.1 Known Issue," with a
>   direct download link to the fix

Two minutes. The manufacturer had already documented this exact bug — it
was never a mystery that required forum archaeology, it required the right
two words in the query.

## What actually changed between the searches

|                           | Search 1                                          | Search 2                                  |
| ------------------------- | ------------------------------------------------- | ----------------------------------------- |
| Anchor terms used         | None — generic words only                         | Model number + "firmware" + exact symptom |
| Where the answer was      | Already published, on the manufacturer's own site | Same place — just not found yet           |
| What search 1 contributed | Surfaced the two anchor terms search 2 needed     | —                                         |

The manufacturer's support page existed before Jordan ever typed the first
query — it wasn't that the second search "worked" and the first one
"failed" at finding something that didn't exist yet. The first search's
generic terms simply couldn't distinguish that one specific, already-
published page from 40,000 unrelated ones.

<Checkpoint question="Given that the manufacturer's support page existed before Jordan ever searched, was search 1 pointless, or did it still do something useful?">

It still did something useful, just not the thing it was aimed at. Search 1
never had a chance of surfacing the manufacturer's page directly, because
its terms were too generic to distinguish that one page from the tens of
thousands of unrelated Wi-Fi complaints competing with it. But it wasn't
pointless: it surfaced the model number and the word "firmware," which is
exactly what search 2 needed to narrow down to that page. The value of
search 1 was reconnaissance for search 2, not an attempt at the answer
itself.

</Checkpoint>

## Extend the scenario: what if there's no single authoritative source?

**Everything above assumes a manufacturer's support page exists somewhere,
waiting to be found with the right terms. What changes if the product is
old, unsupported, or from a small company with no real documentation at
all?**

This is where the search problem turns into a trust problem. A **primary
source** is the original place that can actually settle the claim; a
**secondary source** is someone describing, summarizing, or guessing from
outside. If there is no primary source you can reach, you are no longer
asking "where is the answer?" You are asking "which secondary sources are
independent and careful enough to rely on for now?" That deeper check is
the subject of [source evaluation](/learning-craft/source-evaluation/).

When no canonical source exists, the anchor-term strategy still applies,
but the target shifts from "the one official page" to "the smallest number
of independent people who report the exact same symptom, ideally with a
model/version match" — a forum thread where three unrelated people, using
the same exact model, describe the identical symptom is meaningfully more
trustworthy than one person's guess, even without an official source to
confirm it. This is where search literacy and source evaluation (a
separate but related skill) meet: once there's no primary source to go to
directly, judging the _secondary_ sources against each other becomes the
next-best tool.

<Checkpoint question="A discontinued kitchen appliance has no manufacturer support page left online. You find one forum post, from a single unverified user, claiming a specific fix works. Using the primary/secondary distinction above, how should you treat that claim differently than you would treat the manufacturer's own support page from Jordan's story?">

Treat it as one unverified secondary source, not as settled fact. The
manufacturer's page was a primary source — the original place that could
actually confirm the bug and its fix, which is why finding it ended the
search. A single forum post from one unverified person is a secondary
source with no primary source behind it to confirm it, so instead of
stopping there, the more trustworthy move is to look for whether other
independent people report the exact same fix working on the exact same
model — one person's claim carries much less weight than several
independent, matching reports, precisely because there's no manufacturer
page left to settle the question outright.

</Checkpoint>

**A second extension: what if you don't yet know the right terminology at
all** — not just the model number, but the actual technical word for the
symptom you're experiencing? This is arguably the harder, more common
case: searching "internet keeps stopping for a second" (accurate,
everyday language) returns very different, far less precise results than
"intermittent packet loss," which is the term people who actually diagnose
this use. In plain words, "packet loss" means tiny pieces of internet
traffic fail to arrive; "intermittent" means the problem appears and
disappears instead of staying constant. The fix is the same refinement loop from L2, aimed one level
earlier: the first search's job isn't to answer the question yet, it's to
surface the _domain's own vocabulary_ for the symptom — which then becomes
the anchor term for the search that actually answers it.

## Failure modes

| Failure mode                                                                                                  | What it gets wrong                                                                                            |
| ------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------- |
| Rephrasing the same vague query five different ways                                                           | Doesn't add anchor terms, just shuffles equally generic words                                                 |
| Scrolling deeper into low-quality results instead of re-searching with better terms                           | Wastes time inside a bad result set instead of using what it revealed                                         |
| Searching broadly for something that has one authoritative source                                             | Adds a layer of possible error (summaries, guesses) the primary source doesn't have                           |
| Assuming a bad first search means the answer doesn't exist                                                    | Often it exists — the query just wasn't specific enough to surface it yet                                     |
| Treating "I don't know the exact terminology" as a dead end rather than something the first search can supply | Everyday language and domain vocabulary often return very different result quality for the identical question |
