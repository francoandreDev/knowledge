---
title: "L3 — A worked verification, and what changes when the claim is harder to check"
---

import LevelIntro from "../../../components/LevelIntro.astro";
import Checkpoint from "../../../components/Checkpoint.astro";

L2 explained why a fluent AI answer isn't evidence of correctness, and
sketched the accelerant loop: ask, get an answer, check the load-bearing
claim, then let only the checked version become belief. L3 walks that
loop in full, step by step, on the exact honey claim from L1 — then asks
what changes when the claim being checked isn't nearly as easy to verify
as a pediatric guideline page.

<LevelIntro
  items={[
    "Ines's verification, walked step by step",
    "What changes when a claim is highly technical, not easily checkable",
    "What changes when speed matters more than certainty",
  ]}
/>

## Walking through Ines's thirty seconds

Here is exactly what happened between Ines reading the AI's answer and
deciding not to serve the honey — not a summary of it, the actual steps:

1. **The AI's answer, in full:** "Yes, in small amounts, honey is fine
   for babies once they're past 6 months and already eating solid foods.
   It's a natural sweetener and many parents introduce it around this
   age without issue." Fluent, specific, reassuring — and confidently
   wrong.
2. **Identify what's actually load-bearing.** The sentence contains
   several claims (an age threshold, a safety judgment, a general note
   about common practice). Ines doesn't verify all of it — she isolates
   the one claim a real decision rests on tonight: _is 8 months old
   enough to safely give honey._ The rest is color, not the thing that
   matters.
3. **Pick a source that's actually independent and actually
   authoritative for this claim.** Not another AI query worded
   differently — the same model, asked again, is liable to produce a
   similarly plausible-sounding answer for the same underlying reason.
   Ines opens her pediatrician's own patient-education page, a source
   she already has a reason to trust on infant care specifically.
4. **Compare the claim against the source, directly.** The page states,
   plainly, that honey should never be given to a baby under 12 months
   because of the risk of infant botulism — spores that an infant's
   digestive system can't yet handle safely, but an older child's can.
   This isn't a stylistic disagreement with the AI's answer. It
   contradicts the specific age threshold the AI gave.
5. **Act on the checked version, not the original.** Honey is skipped.
   Ines calls the pediatrician's office the next morning to ask what to
   introduce instead — a two-minute call that closes the loop instead of
   leaving a gap.

The entire check took under a minute and required no expertise Ines
didn't already have access to — just the habit of treating "the AI said
so" as a draft, not a fact, for the one claim that mattered that night.

<Checkpoint question="Ines didn't fact-check the AI's line about honey being 'a natural sweetener many parents introduce around this age.' Was that a gap in her verification habit?">

No — that line isn't load-bearing. Whether honey is commonly described as
a "natural sweetener," or how many parents introduce it around some age,
changes nothing about what Ines does that night; the only claim her
decision actually rests on is the safety threshold. Verifying every
sentence an AI produces isn't the habit, and treating it as the habit
would make the whole approach too slow to actually use. The skill
demonstrated here is picking out the one claim that's load-bearing and
checking that one specifically — not applying equal scrutiny everywhere.

</Checkpoint>

## What changes when the claim is highly technical, not easily checkable

The honey claim was fortunate in one specific way: a single, clearly
authoritative, easy-to-find source (a pediatrician's own page) settled it
outright. Not every claim worth verifying is that convenient. Consider
an unrelated example from a very different domain — a programmer using
an AI assistant to learn how a language's built-in sort function behaves,
while writing code where performance actually matters:

The AI is asked: "What's the time complexity of Python's built-in
`list.sort()` on a large list?" It answers, fluently and specifically:
"O(n) — Python's sort is a linear-time operation, which is one of the
reasons it's so fast even on very large lists." This is wrong: Python's
built-in sort (Timsort) has a worst-case and average time complexity of
O(n log n), not O(n) — a meaningful difference for someone about to make
a decision based on how the running time will scale as their input
grows. The claim reads with exactly the same fluent confidence as the
correct answer would have.

Here, unlike the honey claim, there usually isn't one obvious page a
non-specialist can open and immediately settle the question by reading a
single sentence — the check requires either consulting the language's
own official documentation (which does state the actual complexity
directly, if you know to look for it) or, more convincingly, running a
small timed experiment on lists of increasing size and observing that
the running time grows faster than a straight line. Both of those are
still checks anyone can do without being a computer-science expert — they
just take more than thirty seconds and require picking the right kind of
check for the kind of claim.

| Property                            | Easily checkable claim (honey)        | Highly technical / specialized claim (sort complexity)                            |
| ----------------------------------- | ------------------------------------- | --------------------------------------------------------------------------------- |
| Where to check                      | One authoritative page, read directly | Official documentation, or a small experiment — takes longer to find and confirm  |
| Who can do the check                | Anyone, no special background needed  | Requires knowing where the authoritative source lives, or how to test it directly |
| Confidence after one check          | Usually settles it outright           | May only narrow it — a second independent check adds real value here              |
| What to do if you truly can't check | Rare for this kind of claim           | Explicitly flag the claim as unverified before it becomes load-bearing elsewhere  |

The habit doesn't change — check the load-bearing claim before it
becomes belief. What changes is how much a single check can be trusted
to settle it, and how much effort finding the right kind of check takes.

## What changes when speed matters more than certainty

Neither Ravi's evening nor the sort-complexity example involved any real
time pressure — there was time to check before anything happened. That
isn't always true. Sometimes a decision has to be made before a claim can
be fully verified: a live presentation where a fact just came up, an
urgent operational decision, a conversation that can't be paused for ten
minutes of research.

The habit doesn't disappear under time pressure — it compresses and
becomes explicit instead of silent. Two things still happen even in a
two-minute window: a fast, partial check where one is possible (a single
quick search, a glance at one authoritative source, rather than none at
all), and clearly flagging what wasn't verified rather than presenting an
unverified AI claim with the same confidence as a checked one. "I
believe X, but I haven't confirmed it — treat it as provisional" is a
completely different, honest statement from repeating the AI's fluent
claim as settled fact just because there wasn't time to check it
properly. Speed pressure is a reason to shrink the check, not a license
to skip flagging that it didn't happen.

<Checkpoint question="A colleague, under real time pressure, repeats an AI's claim in a meeting as fact, without saying it hasn't been checked — and it turns out to be correct anyway that time. Does the outcome being right mean skipping the flag was the correct call?">

No — the outcome being right doesn't retroactively make the process sound.
An unverified claim presented as settled fact will occasionally be true
by luck; the problem is that the people hearing it had no way to tell the
checked claims from the unchecked one, and next time the unverified claim
might be the honey answer instead of a correct one. The correct move
under time pressure isn't necessarily to delay the meeting for a full
verification — it's to say plainly that the claim hasn't been checked
yet, so anyone relying on it downstream knows exactly how much weight it
can bear. Being right this time was luck, not evidence the shortcut was
safe to take again.

</Checkpoint>

## Extend it yourself

The honey example and the sort-complexity example are two cases, not the
whole territory. **What would change about Ines's verification step if
the claim had instead been about an interaction between two prescription
medications — a domain she has no independent knowledge of at all, where
even finding the right authoritative source takes real effort? And what
would change if, instead of a calm evening with time to spare, this had
been a decision she had to make in the next two minutes, with no way to
reach anyone who'd know?** Work through what a reasonable version of the
habit looks like in each case before assuming the honey scenario's
thirty-second check generalizes cleanly to every situation.

## Failure modes at this level

- **Assuming one easy check generalizes to every kind of claim.** A
  highly technical or specialized claim often needs a different kind of
  check, and more of it, than a claim with one obvious authoritative
  source.
- **Letting time pressure justify silence, not just a smaller check.**
  Compressing the verification is reasonable under real time pressure;
  presenting an unverified claim as if it were checked is not — those
  are two different responses to the same pressure, and only one of them
  is honest.
- **Treating a lucky correct outcome as proof the shortcut was safe.**
  An unverified claim that happened to be true doesn't validate skipping
  the check — it just means the error, if there was one, didn't surface
  this particular time.
- **Never verifying anything, ever, out of habit-fatigue.** The point of
  scaling scrutiny to stakes (L2) is to make the habit sustainable, not
  to become an excuse for checking nothing because most things turn out
  fine most of the time.
