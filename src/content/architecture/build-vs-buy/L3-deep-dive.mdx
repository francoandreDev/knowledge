---
title: "L3 — Pricing the invisible half: a real 3-year TCO model, and what a fee number leaves out"
---

import Scenario from "../../../components/Scenario.astro";
import LevelIntro from "../../../components/LevelIntro.astro";
import Checkpoint from "../../../components/Checkpoint.astro";

<Scenario label="The finance review asks for real numbers">
  <Fragment slot="facts">
    <div class="not-prose flex flex-col gap-2 text-sm text-slate-700 dark:text-slate-300">
      <div class="flex items-center gap-1.5"><span>📈</span> <strong>Revenue path</strong> — $12M this year, projected $20M next, $30M the year after</div>
      <div class="flex items-center gap-1.5"><span>🧑‍💻</span> <strong>Build proposal</strong> — 2 senior engineers, PCI-DSS Level 1 compliance, ongoing on-call</div>
      <div class="flex items-center gap-1.5"><span>💳</span> <strong>Buy proposal</strong> — stay on Stripe, ~3.3% blended effective rate</div>
    </div>
  </Fragment>

The finance team isn't satisfied with "$380,000 feels like a lot." They ask
for an actual 3-year total-cost model, with the real growth numbers already
on the table from this year's plan: revenue projected at $12M this year,
$20M next year, $30M the year after. **Given those specific numbers — not
the abstract crossover chart from L2 — which option actually wins for this
company, over these 3 years?**

</Scenario>

<LevelIntro
  items={[
    "Build's real cost, priced line by line",
    "Buy's real cost, against this company's actual growth",
    "What the fee number leaves out entirely",
  ]}
/>

## Pricing build, line by line

A "two engineers, six months" pitch prices the six months. It doesn't
price the years after, which is where most of build's real cost actually
lives:

| Line item                                   | Year 1       | Year 2       | Year 3       |
| ------------------------------------------- | ------------ | ------------ | ------------ |
| 2 senior engineers, fully loaded            | $460,000     | $460,000     | $460,000     |
| PCI-DSS Level 1 audit + penetration testing | $180,000     | $150,000     | $150,000     |
| Fraud/chargeback tooling (build own)        | $120,000     | $40,000      | $40,000      |
| On-call/incident allowance                  | $60,000      | $60,000      | $60,000      |
| **Year total**                              | **$820,000** | **$710,000** | **$710,000** |

Three-year total: **$2,240,000** — close to, but not identical to, L2's
simplified $2.05M estimate, because L2 held costs flat across years for
readability; a real model front-loads compliance setup cost into year one
and lets it drop once the program exists.

<Checkpoint question="Year 1's $820,000 is higher than years 2 and 3's $710,000, even though headcount is flat all three years. What's driving that gap, and would you expect it to keep shrinking in year 4?">

The gap is almost entirely the compliance line: $180,000 in year 1 (initial
PCI-DSS Level 1 certification, including the first full penetration test)
drops to $150,000 once the program exists and years 2-3 are recertification
audits rather than a first-time build-out. The fraud/chargeback tooling
line drops similarly once the initial tooling is built and years 2-3 are
just maintaining it. Year 4 would likely look close to year 3 — most of the
"first year is expensive" effect is a one-time setup cost, not a trend that
keeps shrinking indefinitely.

</Checkpoint>

## Pricing buy, against this company's actual trajectory

Stripe's blended effective rate — 2.9% + $0.30 per transaction, averaged
against a realistic transaction-size mix — lands close to 3.3% of volume
for a typical mid-market checkout. Applied to the real revenue path from
the scenario above, not a flat number:

| Year | Volume | Buy cost (≈3.3%) |
| ---- | ------ | ---------------- |
| 1    | $12M   | $396,000         |
| 2    | $20M   | $660,000         |
| 3    | $30M   | $990,000         |

Three-year total: **$2,046,000** — almost exactly tied with build's
$2,240,000. This specific company, on this specific growth path, is sitting
right on top of L2's crossover point, not comfortably on one side of it.
That's not a modeling failure — it's the realistic case. Most real
build-vs-buy decisions happen near the crossover, which is exactly why
"just compare the two totals" undersells how close and reversible the call
usually is.

## What the fee number leaves out entirely

Both totals above are close enough that the real decision hinges on costs
neither line item captures:

- **Stripe's fee bundles fraud detection (Radar) and dispute handling that
  the build column priced separately, but likely under-priced.** A homegrown
  fraud model trained on one company's transaction volume will, for years,
  be worse than a system trained across millions of merchants — the build
  column's $40,000/year "maintain fraud tooling" line assumes the tooling
  works about as well, which is optimistic.
- **The buy column assumes Stripe's rate stays flat.** Large-volume
  customers routinely renegotiate down from list price — the real buy
  number at $30M volume is very likely lower than $990,000, which tips the
  comparison further toward buy than the table shows.
- **Neither column prices the cost of being wrong.** A processing outage
  built in-house is this company's incident, at 2am, with two engineers who
  also have other jobs. A processing outage at Stripe is Stripe's incident,
  with Stripe's on-call team, and Stripe's status page absorbing the
  reputational cost of the postmortem.

<Checkpoint question="If Stripe would realistically renegotiate this company's rate down at $30M volume, and homegrown fraud tooling is realistically worse than Stripe's Radar for the first few years, which direction does that push the comparison above — further toward build, or further toward buy?">

Further toward buy. Both real-world adjustments cut against the build
column's favor: a renegotiated Stripe rate shrinks the already-close buy
total even further below $2,046,000, while worse in-house fraud detection
means the build column's real cost is higher than $2,240,000 shows (more
chargebacks, more manual review time, more customer friction) — not lower.
The two numbers looked like a near-tie on paper; accounting for what the
line items leave out moves the tie toward a real preference for buy, for
this specific company at this specific volume.

</Checkpoint>

## Vendor lock-in, priced as an exit cost

Buying doesn't remove the build-vs-buy question — it defers it, and adds
one more cost if the answer ever flips: **what does leaving cost?** If this
company grows to $200M in volume and the crossover math tips clearly toward
build, migrating off Stripe isn't just an integration project. PCI scope
that Stripe carried now has to be brought in-house from zero; years of
checkout logic, retry behavior, and fraud rules tuned around Stripe's
specific API now have to be re-built around a different (or no) vendor;
and the migration itself has to happen without a checkout outage, on a
system now processing $200M/year instead of $12M. That exit cost is real,
grows with how deeply the product has grown around the vendor's specific
model (not just how long you've used it), and belongs in the buy column
from day one — not as a surprise the day someone proposes leaving.

**Extend it:** everything above assumed a fairly ordinary SaaS company.
What would change in this same 3-year model if the company were in a
regulated industry with its own compliance regime on top of PCI (say,
healthcare payments, which layer HIPAA requirements on top) — does that
push the crossover point higher or lower than $20M, and why? And what
would change if this were an early-stage company evaluating this question
at $500,000 in volume instead of $12M — is the framework from L2 still the
right one to apply, or does something about very early stage change which
question should be asked first?
