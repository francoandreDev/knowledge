---
title: "L1 — How do I name and structure things so the code explains itself?"
---

import Scenario from "../../../components/Scenario.astro";

<Scenario label="One generic field name, two teams, one silent bug">
  <Fragment slot="facts">
    <div class="not-prose flex flex-col gap-2 text-sm text-slate-700 dark:text-slate-300">
      <div class="flex items-center gap-1.5"><span>💳</span> <strong>Billing team, 2019</strong> — adds `order.flag`, meaning "this invoice is overdue"</div>
      <div class="flex items-center gap-1.5"><span>🔔</span> <strong>Notifications team, 2022</strong> — reuses `order.flag`, assuming it means "customer muted notifications"</div>
      <div class="flex items-center gap-1.5"><span>🐛</span> <strong>The bug</strong> — <code>if (order.flag) skipNotification()</code> silently skips reminders for the customers who most need them: the overdue ones</div>
    </div>
  </Fragment>

A field called `flag` gets added to the `order` object by the Billing
team in 2019 to mean "this invoice is overdue." Three years later, an
engineer on the Notifications team, building a "let customers mute
reminders" feature, sees `order.flag` sitting right there, assumes it's
for exactly this purpose, and writes `if (order.flag) return;` to skip
sending a reminder. **The code compiles. Tests that only check "does
skipping work when flag is true" pass. What actually breaks — and who
notices first?**

Every overdue customer, the ones the reminder system exists specifically
to reach, silently stops receiving reminders. Nothing crashes. No error
is logged. The bug isn't discovered for months, until someone in Billing
asks why overdue collections dropped.

</Scenario>

- **A name is a promise about what a piece of code means — when the name
  is generic enough to support two different interpretations, both
  readers can be completely confident and completely wrong at the same
  time.** `flag` said nothing about what it actually represented, so
  nothing stopped a second team from filling in their own plausible
  guess.
- **The cost of a bad name isn't paid by whoever wrote it — it's paid by
  whoever reads it next**, often years later, with no memory of the
  original intent and no way to ask the original author. Billing's
  engineer knew exactly what `flag` meant when they wrote it; that
  knowledge left the building with them.
- **A specific name doesn't just read better — it actively prevents this
  exact class of bug.** `order.isOverdue` and `order.notificationsMuted`
  as two separate, explicitly named fields couldn't have collided the way
  `flag` did; the name itself would have made the Notifications engineer's
  incorrect assumption impossible to make.
- **Boolean names deserve particular care, because a negation on top of an
  ambiguous name compounds the confusion** — `!order.flag` requires first
  resolving what `flag` means and then mentally flipping it, doubling the
  chance of a reader getting it backwards.
- The practical skill this unit builds: **treating a name as load-bearing
  documentation**, not decoration — asking "does this name make the wrong
  interpretation impossible, or just less likely" before committing it to
  a shared piece of code.

## What the generic name actually cost

|                                                      | `order.flag` (one shared, generic field) | `order.isOverdue` + `order.notificationsMuted` (two explicit fields) |
| ---------------------------------------------------- | ---------------------------------------- | -------------------------------------------------------------------- |
| Can two teams silently disagree about its meaning?   | Yes — and did                            | No — each name states its own meaning                                |
| Time to discover the bug                             | Months, and only by accident             | The bug can't occur in the same shape                                |
| What a new engineer needs to know before touching it | Tribal knowledge, or a risky guess       | Exactly what the name says                                           |
