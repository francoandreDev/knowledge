---
title: "L1 — Why does the same bug come back after you already fixed it?"
---

import Scenario from "../../../components/Scenario.astro";

<Scenario label="Fixed once, broken twice more">
  <Fragment slot="facts">
    <div class="not-prose flex flex-col gap-2 text-sm text-slate-700 dark:text-slate-300">
      <div class="flex items-center gap-1.5"><span>🐛</span> <strong>The bug</strong> — a rounding error in a discount calculation</div>
      <div class="flex items-center gap-1.5"><span>🔧</span> <strong>Fixed in</strong> — the checkout page's copy of the discount logic</div>
      <div class="flex items-center gap-1.5"><span>😬</span> <strong>Still broken in</strong> — the cart preview and the admin refund tool, both with their own pasted copy</div>
    </div>
  </Fragment>

A customer reports a discount that's off by a cent. An engineer finds the
bug in the checkout page's discount calculation and fixes it there.
Two weeks later, a nearly identical bug report comes in — this time from
the cart preview. The same rounding mistake, in a second copy of nearly
the same code that nobody remembered existed. **The fix already
happened once. Why did the exact same bug need fixing twice more?**

</Scenario>

## The shape of the problem

- Copy-pasting a working piece of logic feels harmless in the moment —
  it's fast, and the pasted copy works immediately. The cost shows up
  later, whenever that logic needs to change and every pasted copy has
  to be found and changed too, correctly, every time.
- Not all duplication is the same kind of problem — two pieces of code
  that happen to look alike today, but change for unrelated reasons,
  aren't the same bug waiting to happen twice. Treating them as if they
  were is where the _next_ problem, a forced and awkward abstraction,
  comes from.

## Key terms

- **DRY (Don't Repeat Yourself)** — every piece of knowledge or logic
  should have a single, unambiguous place it lives, so a change to that
  logic only ever needs to happen in one place.
- **True duplication** — two pieces of code that express the _same
  underlying rule_, which should always change together — this is what
  DRY is actually about avoiding.
- **Coincidental duplication** — two pieces of code that happen to look
  similar right now, but exist for different reasons and will likely
  need to change independently later — collapsing these into one shared
  function is a trap, not a fix.
- **Premature abstraction** — extracting a shared function or class
  before there's real evidence the duplication is the true kind, often
  producing something more tangled than the duplication it replaced.

## What this unit covers

L2 works through how to actually tell true duplication apart from
coincidental duplication, and what a forced abstraction looks like when
that distinction gets missed. L3 works a full example: three
near-identical discount functions, consolidated into one shared
function, and what happens when a new requirement reveals the
consolidation was premature.
