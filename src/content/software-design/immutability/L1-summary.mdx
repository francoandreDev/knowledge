---
title: "L1 — What does functional programming actually buy me? (immutability, pure functions, managing side effects)"
---

import Scenario from "../../../components/Scenario.astro";

<Scenario label="The user clicked “preview discount,” decided not to apply it, and checked out anyway getting 10% off.">
  <Fragment slot="facts">
    <div class="not-prose flex flex-col gap-2 text-sm text-slate-700 dark:text-slate-300">
      <div class="flex items-center gap-1.5"><span>🛒</span> <strong>Real cart total</strong> — $50, no discount applied</div>
      <div class="flex items-center gap-1.5"><span>👀</span> <strong>User action</strong> — clicks "preview SAVE10" to see the discounted total</div>
      <div class="flex items-center gap-1.5"><span>❌</span> <strong>What actually happened</strong> — the preview function mutated the same cart object checkout uses — the discount stuck, whether the user wanted it or not</div>
    </div>
  </Fragment>

A checkout page has an `applyPromoCode(cart, code)` function. One
button calls it to show a live "preview" of the discounted total; a
separate step calls the same function again at actual checkout. A
user tries `SAVE10` just to see the preview, decides against it, and
checks out — but the total still reflects the discount. **The preview
button and checkout never shared any code path that should have
carried the discount forward. So how did clicking "preview" change
what checkout actually charged?**

</Scenario>

## The shape of the problem

- `applyPromoCode(cart, code)` didn't return a new, discounted cart —
  it reached into the _same_ cart object the preview button and
  checkout both hold a reference to, and changed it in place. Once
  the preview function ran, there was no longer a version of the cart
  without the discount to go back to.
- This is a **mutation** bug, not a logic bug — the discount math
  itself was correct. The problem is that two different parts of the
  code assumed they had their own independent copy of the data, when
  they actually shared one mutable object.
- **Immutability** means treating data as unchangeable once created —
  instead of modifying an object in place, a function that needs to
  "change" something returns a brand-new object with the change
  applied, leaving the original untouched. This is the core idea
  functional programming actually buys you: not a new syntax, but a
  guarantee that calling a function can't quietly corrupt data
  something else still depends on.

## Key terms

- **Mutation** — modifying an existing object or array in place,
  rather than creating a new one with the change applied.
- **Aliasing** — two different variables (or two different parts of a
  program) holding a reference to the _same_ underlying object, so a
  change made through one is visible through the other.
- **Pure function** — a function whose output depends only on its
  inputs, and which produces no observable side effects (no mutation,
  no I/O, no reliance on external state) — the same input always
  produces the same output.
- **Side effect** — anything a function does besides computing and
  returning a value: mutating shared state, writing to a database,
  making a network call, logging.

## What this unit covers

L2 works through why aliasing turns an innocent-looking function call
into action-at-a-distance, contrasts mutating and pure versions of
the same function, and introduces the "functional core, imperative
shell" pattern for isolating the side effects a real program can't
avoid. L3 walks the promo-code bug through a full fix, and covers what
immutability costs (not just what it buys) so the trade-off is
honest, not one-sided.
