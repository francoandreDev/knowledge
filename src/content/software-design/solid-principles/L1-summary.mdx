---
title: "L1 — How do I design so a change in one place doesn't ripple everywhere? (SOLID principles, dependency inversion)"
---

import Scenario from "../../../components/Scenario.astro";

<Scenario label="One provider switch, twelve files touched">
  <Fragment slot="facts">
    <div class="not-prose flex flex-col gap-2 text-sm text-slate-700 dark:text-slate-300">
      <div class="flex items-center gap-1.5"><span>📧</span> <strong>The change</strong> — switching email providers from Sendgrid to Postmark</div>
      <div class="flex items-center gap-1.5"><span>🔍</span> <strong>What that touched</strong> — 12 different classes, each with <code>new SendgridEmailClient()</code> written directly inside them</div>
      <div class="flex items-center gap-1.5"><span>😬</span> <strong>The risk</strong> — missing even one of the 12 leaves it silently still using the old provider</div>
    </div>
  </Fragment>

The company decides to switch email providers, from Sendgrid to
Postmark — a decision that, on its face, is about one vendor
relationship. Making the change in code means finding and editing 12
different classes across the codebase, each of which has
`new SendgridEmailClient()` written directly inside it. **The business
decision was simple. Why did implementing it require touching code in
12 unrelated places?**

</Scenario>

## The shape of the problem

- Each of those 12 classes needed to _use_ email sending, but each one
  also took on the job of _deciding which concrete email provider_ to
  construct — two different responsibilities baked into the same
  place, in every single one of them.
- A change that should have been a business decision (which vendor to
  use) became a code change scattered across every place that decision
  happened to be hardcoded — the ripple isn't caused by the vendor
  switch itself, it's caused by _where_ that decision was allowed to live.
- The fix isn't "be more careful when searching for every place a
  class gets constructed" — it's restructuring so there's exactly one
  place that decision lives, and everything else depends on an
  abstraction instead of a specific vendor.

## Key terms

- **SOLID** — five design principles (Single responsibility, Open/closed,
  Liskov substitution, Interface segregation, Dependency inversion) aimed
  at keeping a codebase changeable without each change rippling outward;
  this unit focuses on the last one, the one most directly responsible
  for the scenario above.
- **Dependency inversion principle (DIP)** — high-level code should
  depend on an abstraction (an interface, a contract), not on a
  specific concrete implementation — and the concrete implementation
  should be handed in from outside, not constructed internally.
- **Abstraction** — a description of _what_ something does (like "can
  send a message") without committing to _how_ — a contract that
  multiple different concrete implementations can satisfy.
- **Dependency injection** — the practical mechanism for DIP: instead of
  a class constructing its own dependency internally, that dependency
  is passed in (injected) from outside, usually through a constructor.

## What this unit covers

L2 works through what depending on an abstraction actually changes
structurally, and how DIP relates to the other four SOLID principles
without needing to cover all five in equal depth. L3 rebuilds the
email-provider scenario with real code — the tightly coupled version,
the version after applying DIP, and what happens when a third provider
(and a fake, for testing) gets added with zero changes to the code that
uses it.
