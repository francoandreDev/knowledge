{
  "items": [
    {
      "id": "files-touched-switching-providers",
      "level": 3,
      "title": "How many files does switching providers touch?",
      "description": "Drag the number of classes that directly construct the concrete email client. In the tightly coupled version, every one of them has to be edited to switch providers. After dependency inversion, only the one place that assembles the object graph (the composition root) needs to change, regardless of how many consumers there are. At 12 classes, this reproduces L1's actual scenario.",
      "params": [
        {
          "name": "numberOfConsumerClasses",
          "label": "Classes that need email sending",
          "min": 1,
          "max": 20,
          "step": 1,
          "default": 12,
          "unit": ""
        }
      ],
      "compute": "return { filesToTouchTight: params.numberOfConsumerClasses, filesToTouchInverted: 1 };",
      "outputs": [
        {
          "key": "filesToTouchTight",
          "label": "Files to touch (tightly coupled)",
          "unit": "",
          "color": "#f87171"
        },
        {
          "key": "filesToTouchInverted",
          "label": "Files to touch (dependency inverted)",
          "unit": "",
          "color": "#34d399"
        }
      ],
      "chartParam": "numberOfConsumerClasses"
    }
  ]
}
