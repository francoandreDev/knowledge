# Progress log

Append-only session log. One entry per session that produces or completes content. Never rewritten retroactively except to fix factual errors — see `docs/ARCHITECTURE.md`.

## 2026-08-12 — Project setup

- Created project structure, git repo, `CLAUDE.md` generation rules, `docs/ARCHITECTURE.md`, and full `ROADMAP.md` across all five tracks (`web`, `systems`, `git-teamwork`, `business-communication`, `logic`).
- No content units written yet — all units `planned`.
- Next: pick the first unit and track together with the user.

## 2026-08-12 — Roadmap expansion: 3 new tracks

- User asked whether the 5-track profile had gaps. Identified 3 missing families and added them: `security/` (13 units), `infra-delivery/` (12 units), `career-craft/` (11 units).
- Updated `ROADMAP.md`, `docs/ARCHITECTURE.md`, `CLAUDE.md`, `README.md` to reflect 8 tracks total.
- Total roadmap now 112 units across 8 tracks, all `planned`.

## 2026-08-12 — Roadmap expansion: product-domain track

- User asked to add a `product-domain/` track, explicitly including the commercial side: speaking the client's language well enough that value is appreciated (not just understood), and closing/exploiting the opportunity.
- Added `product-domain/` (13 units), spanning domain modeling/requirements through consultative selling, objection handling, and staff-level presales/executive pitching.
- Updated `ROADMAP.md`, `docs/ARCHITECTURE.md` (including a note distinguishing it from `business-communication/`), `CLAUDE.md`, `README.md` to reflect 9 tracks.
- Total roadmap now 125 units across 9 tracks, all `planned`.

## 2026-08-12 — Roadmap expansion: corporate-politics track

- User asked to add corporate politics explicitly framed as white/gray/black politics, plus defense against black politics.
- Added `corporate-politics/` (14 units), progressing from legitimate influence-building, through the gray zone of narrative/timing, to what black politics looks like in practice, defensive tactics (paper trails, reading warning signs, alliances), and staff-level ethical use of influence.
- Updated `ROADMAP.md`, `docs/ARCHITECTURE.md` (distinguishing it from `business-communication/` and `product-domain/`), `CLAUDE.md`, `README.md` to reflect 10 tracks.
- Total roadmap now 139 units across 10 tracks, all `planned`.

## 2026-08-12 — Roadmap expansion: learning-craft track

- User asked for a transversal track on finding and learning information correctly and faster, with good judgment.
- Added `learning-craft/` (13 units): source evaluation, search literacy, real understanding vs. recognition (Feynman technique), retention (spaced repetition/active recall), fast codebase onboarding, evaluating conflicting expert opinions, using AI tools without atrophying judgment, personal knowledge systems, prioritizing depth vs. skimming, staying current without noise, building calibrated technical judgment, and staff-level fast-learning-across-domains.
- Updated `ROADMAP.md`, `docs/ARCHITECTURE.md` (framed as transversal alongside `logic/`), `CLAUDE.md`, `README.md` to reflect 11 tracks.
- Total roadmap now 152 units across 11 tracks, all `planned`.

## 2026-08-12 — Roadmap expansion: applied-math track

- User asked for a final track on math and engineering: what to measure, calculate, forecast, and estimate, with explicit ties to where each concept applies in `systems/` and `web/`.
- Added `applied-math/` (15 units): measurement theory, orders of magnitude, asymptotic analysis, combinatorics/probability foundations, probability distributions, queueing theory (Little's Law), statistics/significance, time series/regression, graph theory, linear algebra, capacity planning math, unit economics/cost modeling, quantitative risk modeling, and staff-level quantitative modeling for architecture decisions.
- Updated `ROADMAP.md`, `docs/ARCHITECTURE.md` (framed as transversal alongside `logic/` and `learning-craft/`, with explicit cross-reference note to `systems/`/`web/`), `CLAUDE.md`, `README.md` to reflect 12 tracks.
- Total roadmap now 167 units across 12 tracks, all `planned`. This is likely the completion point of the track-level roadmap — future sessions should focus on content generation rather than further track expansion unless a genuine new gap surfaces.

## 2026-08-12 — Roadmap expansion: testing-quality, software-design, sustainable-performance; career-craft extended

- User asked once more whether the profile had gaps. Identified two genuine ones (no dedicated home for testing craft or software design principles) and two weaker candidates (compensation negotiation, personal sustainability), then asked the user to pick.
- User chose all four: added `testing-quality/` (13 units), `software-design/` (13 units), and `sustainable-performance/` (8 units) as new tracks; added 2 compensation-negotiation units to the existing `career-craft/` (now 13 units) rather than a new track, since negotiation is a career mechanic, not a separate family.
- Updated `ROADMAP.md`, `docs/ARCHITECTURE.md` (added relationship notes: `testing-quality/`+`software-design/` relate to `web/`/`systems/` the way `applied-math/` does; `sustainable-performance/` distinguished from `career-craft/`), `CLAUDE.md`, `README.md` to reflect 15 tracks.
- Total roadmap now 203 units across 15 tracks, all `planned`. User has been asked twice now whether the roadmap is complete — treat a third "any gaps?" pass as the point to push back gently on further expansion in favor of starting content, unless a clearly distinct new gap appears.

## 2026-08-12 — Roadmap expansion: people-management track

- User confirmed they specifically plan to lead teams, so added `people-management/` (15 units): the IC-to-manager transition, 1:1s, delegation, feedback (SBI), goal-setting/OKRs, spotting struggling reports early, conflict mediation, hiring, onboarding, performance calibration, PIPs, terminations, team resilience/succession, org design, and managing managers.
- This track is explicitly scoped to a management path, not assumed universal — noted as such in `docs/ARCHITECTURE.md` and `ROADMAP.md`, distinguishing it from `career-craft/` (growing yourself as an IC), `corporate-politics/` (informal power/influence), and `business-communication/` (peer/stakeholder communication).
- Updated `ROADMAP.md`, `docs/ARCHITECTURE.md`, `CLAUDE.md`, `README.md` to reflect 16 tracks.
- Total roadmap now 218 units across 16 tracks, all `planned`. (Note: earlier entries in this log undercounted due to arithmetic slips at the testing-quality/software-design/sustainable-performance step — 218 is the verified count, confirmed by direct table-row count in `ROADMAP.md`.)
- Next: pick the first unit and track together with the user.

## 2026-08-12 — Rebuilt as an Astro site (not loose markdown)

- User decided the plain-markdown-folder approach wasn't the right medium — the point is to teach in an engaging way, not to produce something as flat as a `.txt`. Chose Astro + MDX + Tailwind (recommended over plain HTML) and bun as the package manager/runtime.
- Scaffolded Astro 7 in place, added `@astrojs/mdx`, Tailwind v4 (`@tailwindcss/vite`) + `@tailwindcss/typography`, and `mermaid`. Moved all 16 track folders under `src/content/` (required by Astro content collections).
- Built `scripts/generate-roadmap-data.mjs`: parses `ROADMAP.md`'s per-track tables, assigns a stable kebab-case slug to any row missing one (rewriting `ROADMAP.md` with an explicit `Slug` column so slugs never drift on re-run), and emits `src/data/roadmap.json` (+ `src/data/roadmap.ts` typed wrapper). `ROADMAP.md` stays the human-edited source of truth; the JSON is generated and gets regenerated via `bun run generate:roadmap`.
- Built the rendering pipeline: `src/content.config.ts` (glob-loaded `curriculum` collection), `src/lib/curriculum.ts` (groups raw entries into per-unit `{l1, l2, l3, l3Parts}`), `Layout.astro` (nav + client-side Mermaid rendering of fenced ```mermaid blocks), `LevelTabs.astro` (L1/L2/L3 tab switcher), `ProgressToggle.astro` (localStorage-backed "mark as done"), and pages for home (`/`), `/roadmap`, `/[track]/`, `/[track]/[unit]/`.
- Verified end-to-end with a throwaway `_smoketest` content fixture (since deleted) via `bun run build` + `astro preview --background`, checked live in Chrome. Found and fixed two real bugs in the process: (1) the glob loader lowercases generated entry ids, so the id-parser in `curriculum.ts` needed case-insensitive matching against `L1-`/`L2-`/`L3-` prefixes; (2) `LevelTabs` and `ProgressToggle` only initialized on the `astro:page-load` event, which never fires without a `ClientRouter`/View Transitions — fixed by also calling init directly on script execution.
- Also learned: `bun run dev` (file-watching) reliably fails to start within Astro's 30s timeout on this `/mnt/c/...` WSL-over-Windows path — `bun run build` + `astro preview --background` is the dependable local loop, now documented in `CLAUDE.md` and `README.md`.
- No study content has been written yet — this session was entirely infrastructure. All units remain `planned`.
- Updated `CLAUDE.md` (full "Tech stack" section, new file/folder naming under `src/content/`, session workflow now includes running `generate:roadmap` and a real build check), `docs/ARCHITECTURE.md` (new "Structure on disk" reflecting the Astro layout), `README.md` (run instructions).
- Next: pick the first unit and track together with the user — this time it will actually render as a page, not just a markdown file.

## 2026-08-12 — Added lint/format/typecheck/content-validation/test guardrails + scaffold script

- User asked for security/lint/test measures and rules that are always read, so that starting a new topic from their side is always easy and never leads to an inconsistent state.
- Added ESLint (flat config, `eslint.config.mjs`, TS + Astro + Node-aware for `scripts/`), Prettier (`prettier-plugin-astro`), `astro check` for types, and `scripts/validate-content.mjs` — which cross-checks `ROADMAP.md`, `src/data/roadmap.json`, and the real files under `src/content/` for drift (missing roadmap rows, stale generated data, status/content mismatches, missing frontmatter).
- Added `scripts/new-unit.mjs` (`bun run new:unit <track> <unit-number-or-slug>`) — scaffolds a unit's three level files with correct frontmatter and flips its `ROADMAP.md` status to `in-progress` in one command, so the user (or Claude) always starts from the same known-good shape.
- Added `bun test` coverage (`bun:test`) for the two places a silent parsing bug would be hardest to spot by eye: the roadmap slug generator (`scripts/generate-roadmap-data.test.mjs`) and the content-entry-id parser (`src/lib/curriculum.test.ts`). Had to refactor `curriculum.ts` to type-only import `astro:content` and dynamic-import `getCollection` at call time, since the virtual module doesn't resolve under plain `bun test`.
- Added `bun run check` aggregating generate:roadmap → lint → format:check → typecheck → validate:content → test → build; documented in `CLAUDE.md` as mandatory before ending any content/code session, replacing the earlier "just run build" guidance.
- Found and fixed two real bugs while dogfooding all of this: (1) `generate-roadmap-data.mjs` wrote `src/data/roadmap.json` before reformatting `ROADMAP.md` with Prettier, so `ROADMAP.md`'s mtime ended up newer than the JSON it just generated from, causing `validate:content` to always report the data as stale — reordered so Prettier runs before the JSON write; (2) `new-unit.mjs`'s status-flip regex assumed single-space table cells, but Prettier pads columns for alignment — fixed to match on `\s*` like the rest of the parsing already does.
- Verified the whole loop end-to-end with a throwaway scaffolded unit (`web/http-request-response-basics`, since removed, status reverted to `planned`): `new:unit` → `validate:content` correctly flagging staleness → `generate:roadmap` clearing it → cleanup.
- No study content has been written yet. All 218 units remain `planned`.
- Next: pick the first unit and track together with the user.

## 2026-08-12 — First real unit: web/http-request-response-basics (done)

- Wrote the first actual study content, to validate the whole pipeline (structure, tooling, and the site itself) end-to-end with real material rather than a throwaway fixture. `web/01 — HTTP request/response basics`, chosen as the most foundational, broadly-illustrative unit: it exercises a Mermaid diagram, pseudocode, and real runnable code, which most other units will need some subset of.
- L3 code examples (`raw-request.mjs` client, `raw-server.mjs` server, both raw `node:net` sockets with no HTTP library) were actually run under `bun run` and verified against each other locally (not just written and assumed correct) — confirmed `GET /time` and a 404 path both round-trip correctly before considering the unit done.
- Found and fixed a real rendering bug while reviewing the built page in Chrome: Mermaid sequence diagrams mirror actor boxes at the bottom by default, and in our narrow content column that mirrored repeat overlapped the diagram's last note/message instead of getting its own space. Fixed globally via `sequence: { mirrorActors: false }` in `Layout.astro`'s Mermaid init — affects every future Mermaid sequence diagram in the project, not just this unit. (Caught via a user-provided screenshot after a `<br/>`-in-message-text overlap was already fixed — worth remembering: always scroll/screenshot the _entire_ rendered diagram, not just what's initially in viewport, since Mermaid layout bugs can be low enough to be off-screen on first look.)
- Ran `bun run check` clean (0 lint/format/type errors, content validation passed, 9/9 tests, build succeeded) both before and after the Mermaid fix, and visually reviewed all three levels (L1/L2/L3) live in Chrome via `astro preview --background`.
- Marked `web/http-request-response-basics` `done` in `ROADMAP.md` (all three levels complete, not just started) and ran `generate:roadmap`.
- Confirms the structure works for both sides as designed: `new:unit` → write content → `check` → visual review → mark `done` is a clean, repeatable loop with no friction found other than the Mermaid bug above (now fixed for all future units).
- 1/218 units done.
- Next: pick the next unit and track together with the user.

## 2026-08-12 — Second Mermaid fix: note text vertical alignment (user-caught via screenshots)

- After the mirrorActors fix above, the user flagged (with a `/paste` screenshot, then the actual rendered SVG markup) that a note's rect and its text were still visibly misaligned — a problem the visual "does it look okay" pass had missed, and one my browser automation couldn't re-check for a while (Chrome DevTools Protocol screenshot calls started timing out mid-session, on every tab including freshly-created ones — resolved on its own after a few minutes; treated as a transient bridge issue, not a page bug, since Vivaldi itself was responsive the whole time per the user).
- Root cause, from the actual SVG the user pasted: Mermaid's note `<text>` had **both** `dominant-baseline="middle"` and `dy="1em"` set — two different vertical-centering techniques (one for browsers that honor `dominant-baseline`, one for those that don't) stacked instead of chosen between. Browsers that support `dominant-baseline` (Chrome/Vivaldi/Firefox) apply both, so the extra `dy` shifts note text well below the rect's real center.
- First attempt (`transform: translateY(-1em)` on `.noteText`, canceling the dy entirely) was an overcorrection — user confirmed via a second screenshot it was "slightly better but not perfect," text now sitting a bit high. Recomputed from the exact coordinates in the pasted SVG (rect y=179 h=20 → true center 189; text y=184 → dominant-baseline-only center is 184, 5px short of true center) and corrected to `translateY(calc(-1em + 5px))`. User confirmed via a third screenshot this is now correctly centered, both axes.
- Lesson for future units: a `bun run check` pass and even a visual glance are not sufficient to catch sub-pixel-scale Mermaid rendering bugs — they need either a zoomed screenshot of the specific element or, as happened here, the user inspecting actual SVG output. Worth doing a zoomed check on any new diagram type (not just sequence diagrams) before marking a unit `done`.
- Fix lives in `src/styles/global.css` (`.mermaid .noteText { transform: translateY(calc(-1em + 5px)); }`), documented inline with the exact reasoning — applies to every note in every Mermaid sequence diagram project-wide, not just this unit's.

## 2026-08-12 — Built the exercise system: quizzes, sandboxed code exercises, gated "Mark as done", spaced repetition

- User asked for a real testing layer on top of the reading material: self-graded exercises with an "expect" (i.e. real code assertions, not just multiple choice), "Mark as done" only achievable once everything passes with no errors, and periodic automatic un-marking to force a genuine re-exam and catch regressions in retained knowledge. Mid-implementation, the user added two more requirements: every exercise must reveal the solution and the reasoning after any attempt (not just pass/fail), and exercises should be randomized from a pool per topic rather than the same fixed question every time.
- New `exercises` content collection (`src/content.config.ts`): one optional `exercises.json` per unit, `quiz` (multiple choice) and `code` (sandboxed, `expect()`-based) item types. Both types now require an `explanation`; code items also require a `solution` — enforced by `bun run validate:content`, not just convention.
- Items sharing a `poolId` are randomized variants of the same question; `src/lib/curriculum.ts`'s `groupExercisesIntoPools()` groups them, and `ExercisePanel.astro`'s client script picks one at random per render (including after a spaced-repetition reset) and builds its DOM directly (not server-rendered per-variant, since which variant shows has to vary per view).
- Code exercises run in a sandboxed Web Worker (`ExercisePanel.astro`'s `WORKER_SOURCE`) — no DOM/window access, a 3-second timeout guards against infinite loops, and a minimal `expect(actual).toBe/toEqual/toBeTruthy/toThrow()` API is injected. Verified with both a deliberately wrong implementation (correctly fails 2/4 tests with real error messages) and a correct one (4/4 pass) via real browser interaction, not just reading the code.
- `ProgressToggle.astro` rewritten: units with any exercises disable "Mark as done" until every pool has been passed (tracked per-pool in `localStorage`, not per-variant, so gating survives which random variant was shown); units with none keep the old ungated manual toggle. `src/lib/spaced-repetition.ts` schedules re-exams at 7/30/90-day intervals (capped at 90) and, on the next visit past the due date, auto-unmarks the unit **and clears every pool's pass/fail state**, forcing a real re-take with a fresh random variant — verified by manually backdating `nextReviewAt` in localStorage and confirming the reset fired correctly (gating re-locked, all 4 exercise states cleared to null, stage preserved at 1 rather than reset to 0).
- Found and fixed a real bug while testing the toggle: `initProgressToggles()`/`initExercises()` re-run on events like `exercise:graded`, and were re-attaching a fresh click listener to the same button every time without removing the old one — one real click ended up firing the handler multiple times, toggling `done` on then back off. Fixed by splitting each component into a one-time "wire" step (guarded by a `data-wired` flag) and a repeatable "render" step; documented as a required pattern in `CLAUDE.md` for any future interactive component.
- Confirmed GFM tables and Mermaid chart types (`xychart-beta`, etc. — anything beyond sequence diagrams) both render with zero new code, just documentation of usage in `CLAUDE.md`; charts/tables are for illustrating concepts in content, not interactive, per the user's explicit scope answer earlier in this conversation.
- Added real (not placeholder) exercises to `web/http-request-response-basics`: 2 quiz pools (one with 2 randomized variants, one with 1) for L1, 1 quiz for L2, 1 code exercise (`byteLength`, using `TextEncoder` since Workers have no Node `Buffer`) for L3 — all verified working end-to-end in the browser, including the randomized-variant pool (dispatched `exercises:reset` 8 times, saw both variants appear) and the solution/explanation reveal after a failing attempt.
- Updated `CLAUDE.md` with a full "Exercises, 'Mark as done' gating, and spaced repetition" section (format, required fields, pools, gating behavior, the wire/render pattern requirement) and `docs/ARCHITECTURE.md` with the "why" (exercises exist because reading isn't the same as knowing, tied back to `learning-craft/04`'s Feynman-technique framing). Session workflow (`CLAUDE.md`) now includes writing exercises and clicking through them before marking a unit done.
- `bun run check` clean throughout. `web/http-request-response-basics` remains the only `done` unit (1/218) — this session was entirely infrastructure for the exercise system, no new topic content.
- Next: pick the next unit and track together with the user — from here on, units should come with exercises unless there's a real reason not to.

## 2026-08-12 — Validated tables/charts in a real browser; fixed two contrast bugs

- The earlier "tables and charts work" claim had only been checked via `bun run build` succeeding and a raw HTML grep (`<table>`, `xychart-beta` present in output) — never actually looked at in a rendered page. User pushed back specifically because `applied-math/` will lean on this heavily. Built a throwaway fixture (GFM table, `xychart-beta` bar chart, `xychart-beta` line chart with 3 series approximating O(n)/O(n log n)/O(n²) growth curves, `pie` chart) and reviewed all four live in Chrome.
- GFM table: correct out of the box, no issues.
- `xychart-beta` bar and line charts: both rendered correctly (the 3-series line chart clearly showed the three growth curves as distinct, readable lines) but the plot area had its own mid-gray background rectangle that visibly mismatched the page's near-black `bg-slate-950` — `xyChart`'s background falls back to the Mermaid theme's `background` variable, which the built-in `dark`/`default` themes set to a shade lighter than our actual page background.
- `pie` chart: the default `dark` theme's slice palette (`pie1`..`pie6`) includes a near-black entry, which was effectively invisible against `bg-slate-950` — only distinguishable by its border.
- Fixed both in `Layout.astro`'s `mermaid.initialize()`: added `themeVariables` (background: transparent, brighter `pie1`-`pie6` palette with dark slice-label text for contrast, for both light/dark mode) and an explicit `xyChart: { backgroundColor: "transparent", plotColorPalette: ... }`. Deliberately layered these as _overrides on top of_ the existing `theme: "dark"/"default"` rather than switching to Mermaid's fully custom `"base"` theme, specifically to avoid risking a regression in the sequence-diagram appearance already fixed and validated earlier — re-checked the existing `web/http-request-response-basics` L2 sequence diagram after the change and confirmed no visual regression.
- Assessed `xychart-beta`'s real limits for `applied-math/`'s sake: bar/line only, no scatter, no true smooth curves (approximate with enough points), no dual-axis, no log scale. Judged sufficient for the large majority of illustration needs there; recommended against building custom charting infrastructure speculatively — revisit only if a specific `applied-math/` unit hits a real wall (needs scatter or log-scale) that an approximation can't cover. Documented in `CLAUDE.md`.
- Fixture removed after review. `bun run check` clean.
- Next: pick the next unit and track together with the user.

## 2026-08-12 — Visual pass: icons throughout the UI chrome

- User asked (explicitly framed as a taste call, not a functional gap) for the site to feel more visual — icons, and periodic images/graphics/tables — while keeping what already works, then wanted the resulting visual/functional balance written down as a standing rule.
- Installed `lucide-astro` (tree-shakeable per-icon Astro components, no runtime icon-font dependency). Added `TrackIcon.astro` (one icon per track, e.g. Globe for `web/`, Server for `systems/`, GitBranch for `git-teamwork/`, Shield for `security/`, Swords for `corporate-politics/`, etc. — all 16 mapped) and `StatusBadge.astro` (check/loader/circle icon + color per done/in-progress/planned status).
- Wired icons into: home page track cards (icon chip per track, next to the progress bar), `/roadmap` (track icon per section, `StatusBadge` per unit replacing plain text), track detail pages (large track icon by the `<h1>`, `StatusBadge` per unit), unit pages (track icon in the breadcrumb, file/lightbulb/code icons on the L1/L2/L3 tabs), `ProgressToggle` (check/lock/circle icon reflecting done/gated/available state), `ExercisePanel` (question-mark/code-brackets badge per exercise pool — type is constant across a pool's randomized variants even though which variant shows is random, so this badge is safely server-rendered rather than needing the client-side variant picker), and the site nav (graduation-cap logo, checklist icon on the roadmap link).
- Verified visually in the browser (not just build-passing) across home, roadmap, track, and unit pages — icons render crisply, status badges are clearly scannable (green "written" vs. gray "planned" reads at a glance in a way plain text didn't), no regressions to existing layout/spacing.
- Wrote the requested standing rule into `CLAUDE.md` ("Visual design balance"): UI chrome icons are already built and should be reused, not reinvented, by any new page/component; content visuals (tables/charts) should appear only where they genuinely clarify a comparative or quantitative idea, not to hit a decoration quota — L2's required diagram already guarantees a baseline visual per unit.
- `bun run check` clean throughout this session.
- Next: pick the next unit and track together with the user.

## 2026-08-12 — More visuals on the first unit + a new "interactive demo" content type

- User pushed back that the first unit still felt visually thin, and separately asked for theory sections to allow exploring an example by adjusting values and seeing both the result and the trend of how that variation behaves — not a graded exercise, an exploratory tool.
- Added a third content type, `interactives.json` (`InteractiveDemoItem` in `content.config.ts`, rendered by a new `InteractiveDemo.astro`): sliders per numeric parameter, live-computed output(s) via an author-provided `compute` JS function-body (run synchronously via `new Function`, not the sandboxed Worker exercises use — this is author content re-run on every drag tick, not learner input, so a Worker's postMessage latency isn't worth paying), and a hand-built SVG line chart sweeping the full range of one chosen parameter so the learner sees the _trend_, not just the current point. Ungraded — no effect on `ProgressToggle` gating, purely exploratory.
- Added a real demo to `web/http-request-response-basics` L2: "How much does keep-alive actually save?" — sliders for request count, connection setup cost, and per-request cost; outputs comparing total time with vs. without connection reuse; chart swept across request count. Verified in the browser: dragging request count from 5 to 20 correctly recomputed both totals (750ms→3000ms without keep-alive, 550ms→2050ms with) and the chart's marker/lines updated live.
- Also added, directly answering "not enough graphics": a reference table of status code ranges to L1, and a static Mermaid `xychart-beta` bar chart ("typical time budget for a cold HTTPS request": DNS/TCP/TLS/request/response) to L3, tying into the existing raw-socket content and reinforcing _why_ keep-alive matters before the interactive demo lets you play with the number.
- Found and fixed an unrelated but real rendering issue while reviewing the new table: Tailwind Typography's default inline-`<code>` style wraps content in literal backtick characters via CSS `::before`/`::after` — correct behavior, but noisy with several code spans packed into table cells. Replaced with a subtle background-chip style (no backticks) in `global.css`, applied project-wide to both prose and table inline code.
- Added `checkInteractivesFile()` to `validate-content.mjs` (structure only — params/outputs non-empty, `chartParam` names a real param, defaults within range; can't verify `compute` actually computes correctly, so still requires manual browser testing) and documented the whole feature in `CLAUDE.md` ("Interactive demos").
- `bun run check` clean; both the interactive demo and the new static visuals were verified live in the browser, not just via build success.
- Next: pick the next unit and track together with the user — from here on, consider an interactives.json demo alongside exercises when a unit has a real variable relationship worth exploring.

## 2026-08-12 — First systems/ unit: `systems/process` ("What actually happens when you run a program?")

- User chose `systems/process` as the next unit. Wrote all three levels: L1 (bullet summary of process/virtual memory/scheduler/segfault concepts), L2 (ASCII memory-layout diagram, a Mermaid `sequenceDiagram` for fork+exec, pseudocode, and the "why virtual memory enables isolation" explanation), L3 (four real, actually-run code examples: `pids.mjs` proving separate PIDs via `child_process.spawn`, `stack.mjs` proving the stack's finite size by overflowing it on purpose, `heap.mjs` proving heap growth via `process.memoryUsage()`, `maps.mjs` reading `/proc/self/maps` directly from the kernel to confirm the heap/stack regions are real — plus a failure-modes section distinguishing a caught `RangeError` from a real segfault).
- Added `exercises.json`: 3 quiz items (page-table isolation, why NULL derefs fault, why fork/exec are split) and 1 code exercise (`willStackOverflow(currentDepth, frameSizeBytes, stackLimitBytes)`, the same arithmetic the runtime does internally). Added `interactives.json`: a "recursion depth budget" demo (frame size × stack limit sliders → max safe recursion depth, charted across frame size).
- Found and fixed a real regression while reviewing L2 in the browser: reused `<br/>` inside Mermaid `Note over` text (one note with 2 lines, one with 3) to force line breaks — this renders garbled/overlapping text in this Mermaid setup, the same failure class already documented from message-text `<br/>` usage in the first unit, but not previously confirmed for Note text specifically, and the existing single-line-calibrated `.noteText` CSS transform doesn't correct it for multi-line notes. Fixed by rewriting both notes as single lines (matching the established message-text fix) rather than trying to extend the CSS fix to handle multi-line notes.
- Full browser click-through completed after the fix: all 3 level tabs render correctly (including the corrected sequence diagram), all 3 quiz pools answered both right and wrong (correct/incorrect states and explanations both confirmed), the code exercise run with a deliberately broken TODO stub (4/4 tests correctly fail) and with the correct implementation (4/4 pass, solution/explanation still shown), "Mark as done" correctly unlocked once every pool passed, and the interactive demo's two sliders dragged to their extremes with correct live recomputation (max depth 13107 → 2097 on frame-size max, → 16777 on stack-limit max).
- `ROADMAP.md` status flipped to `done` for `systems/process`; `bun run check` clean (lint, format, typecheck, content validation, 9/9 tests, build).
- Takeaway for future sessions: the `<br/>`-in-Mermaid-text pitfall applies to both message text and note text — default to single-line Mermaid text everywhere, don't rely on the CSS fix to handle multi-line cases.
- Next: pick the next unit and track together with the user.

## 2026-08-12 — Color/legibility pass: Shiki theme, prose accent color, chart axis reference

- User feedback with 2 units written: the site read as too monochrome — keywords/bold text had no accent color, code blocks (especially untagged pseudocode) had near-zero syntax color, and the custom SVG trend chart in `InteractiveDemo.astro` showed only a curve's shape with no readable value scale.
- Switched Shiki theme (`astro.config.mjs`) from `github-dark` to `dracula` for more vivid syntax colors; tagged the two previously-untagged pseudocode fences (`systems/process` and `web/http-request-response-basics` L2s) as `python` so they get real highlighting instead of flat plaintext.
- Added a `sky` accent color (reused from existing chart palettes) to `.prose-panel` bold text, links, and inline code chips in `global.css`, via Tailwind Typography's `--tw-prose-*` custom properties rather than overriding generated utility classes.
- Rewrote `InteractiveDemo.astro`'s `buildChart()` to add axis min/max labels, reference gridlines, and a value label at the current point — a generic infrastructure fix that applies to every current and future `interactives.json` demo, not a one-off. Mermaid's `xychart-beta` already renders its own y-axis scale, so needed no change.
- Verified all of the above live in the browser (dracula colors, sky accents, chart axis labels/gridlines/value annotation on `systems/process`'s recursion-depth demo); `bun run check` clean throughout. Committed as `47f40dd`.
- Next: pick the next unit and track together with the user.

## 2026-08-12 — First business-communication/ unit: `audience-awareness` ("Why does the same message land differently depending on who says it?")

- User chose `business-communication/audience-awareness` as the next unit — the first non-code, non-systems track to get content, establishing the pattern for tracks with no natural code/diagram substrate.
- Wrote all three levels: L1 (bullet summary — context/incentives/channel as the three lenses, the "audience awareness isn't spin" distinction), L2 (a Mermaid flowchart showing one set of facts branching into three audience-specific outputs, a comparison table of the three lenses across peer/manager/exec, and a pseudocode-style decision procedure `compose_message(facts, audience)` tagged `python` for consistent coloring with the code tracks), L3 (one real scenario — a reconciliation job slowdown — rewritten three full times for a peer engineer, a manager, and a non-technical exec, with an explicit "what's assumed / what's foregrounded / the ask" breakdown per version, a "what stayed constant across all three" section making the not-spin distinction concrete, and 5 failure modes).
- Added `exercises.json`: 6 quiz items only (no code exercises — this track has no runnable-code substrate), including one 2-variant randomized pool on the "audience awareness isn't spin" distinction.
- No `interactives.json` — no real quantitative variable relationship in this topic; per `CLAUDE.md`'s restraint rule, skipped consciously rather than bolted on.
- `ROADMAP.md` flipped to `done` for `audience-awareness`; `bun run check` clean (lint, format, typecheck, content validation, tests, build). Browser verification done by the user directly (Chrome extension bridge wasn't connected in this session).
- Next: user asked to advance every remaining track by one unit (its first), to establish a precedent pattern per track before going deeper on any single one.

## 2026-08-12 — First unit for every remaining track (13 units in one session, explicitly batched)

- User explicitly asked to batch: advance every track that had zero content to its first roadmap unit, one unit each, so every track has a written precedent before going deeper on any single one. This is a deliberate exception to the normal one-unit-per-session rule, per the user's direct request.
- Wrote all three levels + `exercises.json` for 13 units in one pass: `git-teamwork/snapshots-manual-copies`, `logic/formal-informal-logic`, `security/security-mindset`, `infra-delivery/environment-parity`, `career-craft/leveling-expectations`, `product-domain/understanding-user-s-problem-solution`, `corporate-politics/power-real-resource`, `learning-craft/exposure-understanding`, `applied-math/math-tool-prediction`, `testing-quality/case-automated-testing`, `software-design/cost-design`, `sustainable-performance/myth-linear-output`, `people-management/ic-excellence-management`.
- Established two content patterns going forward: **technical tracks** (git-teamwork, security, infra-delivery, testing-quality, software-design, applied-math) ground L3 in real, runnable code (a reimplemented git blob hash, a truth-table validity checker, a tampering-bug fix, a timezone-drift fix, a queueing-formula reproduction, a coupling/decoupling refactor) and include `code` exercises alongside quizzes; **non-technical tracks** (business-communication, career-craft, product-domain, corporate-politics, learning-craft, people-management) ground L3 in fully worked real-world scenarios (the same situation handled multiple ways) instead of code, and use quiz-only exercises, since there's no natural runnable-code substrate for the subject matter.
- `sustainable-performance/myth-linear-output` got an `interactives.json` demo (a fatigue-penalty output-vs-hours curve) — the first non-code track to have one, added because the topic has a genuine quantitative relationship worth exploring, not by default.
- Found and fixed two real content-validation bugs during this batch, both from `poolId` misuse: five units (`career-craft`, `corporate-politics`, `learning-craft`, `people-management`, `software-design`) had a `poolId` shared between two quiz items of _different_ levels — these weren't true randomized variants of the same question, just two related-but-distinct questions I'd mistakenly grouped; `validate:content` correctly rejected this, and the fix was removing the shared `poolId` (making them standalone singleton items) rather than forcing mismatched levels together.
- Also caught, during writing (not by tooling): a code exercise (`applied-math`) using unsupported Jest-style matchers (`toBeCloseTo`, `toBeGreaterThan`) that don't exist in this project's sandboxed `expect()` mini-API (`toBe`/`toEqual`/`toBeTruthy`/`toThrow` only, per `ExercisePanel.astro`'s `WORKER_SOURCE`) — fixed by rounding floating-point results before `toEqual` comparisons and rephrasing the comparison test as a boolean `toBeTruthy()` check; grepped all exercises.json files afterward to confirm no other unit made the same mistake.
- `ROADMAP.md` flipped to `done` for all 13 units (now 15 of 218 done); `bun run generate:roadmap` + full `bun run check` (lint, format, typecheck, content validation, tests, 34-page build) clean after fixes. Browser click-through was not performed this session (Chrome extension bridge unavailable) — flagged to the user as a gap; recommend a browser pass over at least the new interactive demo and a couple of the exercise pools next session.
- Next: pick a track to go deeper on (unit 02+), or do the deferred browser verification pass first — user's call.

## 2026-08-12 — Fixed fake-code pseudocode blocks in non-technical tracks

- User caught (via a browser screenshot of `people-management/ic-excellence-management` L2) a real pattern across the batch above: several non-technical units dressed a plain reasoning procedure as a fenced ` ```python ` code block with `function`/`if`/`return` syntax and invented, undefined helper calls (`degrade_with_fatigue(...)`, `translate_to_sponsor_incentives(...)`) — code-shaped noise for a topic that isn't code, worse for teaching than just writing the same logic as prose or a numbered list.
- Fixed by removing the fake-code fences and rewriting the same content as plain prose/lists in all 7 affected files: `business-communication/audience-awareness`, `career-craft/leveling-expectations`, `product-domain/understanding-user-s-problem-solution`, `corporate-politics/power-real-resource`, `learning-craft/exposure-understanding`, `people-management/ic-excellence-management`, `sustainable-performance/myth-linear-output` (all L2-concept.md). Mermaid diagrams and real data/comparison tables in these same files were left untouched — the problem was specifically fake executable-looking pseudocode, not diagrams or structured content in general.
- Standing rule going forward (should be added to `CLAUDE.md` if not already implicit from "don't pad"/"stands on real code"): code blocks are for genuinely technical tracks where the logic is naturally computational (git-teamwork, security, infra-delivery, testing-quality, software-design, applied-math, logic, systems, web) — non-technical/soft-skill tracks (business-communication, career-craft, product-domain, corporate-politics, learning-craft, people-management, sustainable-performance) should express procedures and models in prose, numbered steps, or tables, never as pseudocode dressed in a programming language's syntax.
- `bun run check` clean after the fix.
- Next: pick a track to go deeper on, or do the still-deferred browser verification pass.

## 2026-08-12 — Fixed a real, site-wide Mermaid rendering bug found during browser verification

- Chrome bridge reconnected this session. First check (`people-management`'s flowchart) showed a tiny 16x16 broken-icon box instead of the diagram — initially misdiagnosed as caused by a literal `×` character in a node label (fixed that too, harmless either way), but the same tiny box reproduced on `career-craft` and `business-communication`'s flowcharts, which have no special characters at all — proved it was systemic, not a content bug.
- Root cause, confirmed by instrumenting Mermaid's render pipeline directly: `flowchart`/`xychart-beta` diagrams measure text via real DOM layout (`getBBox`), which returns 0 for anything inside a `display:none` ancestor. `LevelTabs.astro` hides the L2/L3 panels by default (`class="hidden"`), and `Layout.astro`'s mermaid script rendered every `.mermaid` block unconditionally at page load — so any flowchart/xychart living on a non-default tab collapsed to an empty box, permanently (Mermaid doesn't re-measure once `data-processed` is set). `sequenceDiagram`s (used in the two previously-verified units) measure text via canvas instead, which doesn't depend on visibility — that's the only reason this went undetected until now, across every unit written this session and, in hindsight, `business-communication`'s diagram too (never actually browser-checked before).
- Fixed in two files: `Layout.astro` now only calls `mermaid.run()` on diagrams that are currently visible (filtering out anything inside a `[data-tab-panel].hidden` ancestor) instead of rendering everything unconditionally on load; `LevelTabs.astro` dispatches a `mermaid:rerun` custom event after un-hiding a panel, which `Layout.astro` listens for and uses to render any diagram that was skipped, now that it's actually visible for the first time. Diagrams already rendered (`data-processed`) are never touched again, so no duplicate work on repeated tab switches.
- Verified via direct DOM inspection (`querySelector('svg').getAttribute('viewBox')` before/after clicking each unit's L2 tab) across all 15 written units with diagrams — every flowchart and xychart now reports a real, full-size viewBox instead of the `-8 -8 16 16` error box, and the two previously-working sequenceDiagrams (`systems/process`, `web/http-request-response-basics`) still render correctly (no regression). Screenshot capture itself was unreliable this session (CDP `Page.captureScreenshot` repeatedly timed out on this WSL/Vivaldi bridge, unrelated to the fix — page JS execution worked fine throughout), so verification relied on direct JS/DOM queries instead of visual screenshots.
- `bun run check` clean (lint, format, typecheck, content validation, tests, 34-page build).
- Takeaway for future sessions: a unit's diagram type determines whether hidden-tab rendering is even a risk — `sequenceDiagram` is safe, `flowchart`/`xychart-beta`/`pie` are not (all use DOM-based text measurement) — and browser-verifying only the _first_ unit of a new pattern isn't sufficient once tabs are hidden by default; this bug existed since `LevelTabs.astro` was built but was masked by every earlier verified unit happening to use `sequenceDiagram`.
- Next: continue advancing tracks with a second unit each, per the user's request to keep going.

## 2026-08-12 — Second unit for every track (16 units), all Mermaid diagrams browser-verified

- User asked to retry the browser connection and, since it worked, verify everything and then advance again with one more unit per track (unit 02 for all 16 tracks, including `people-management/1` for the "1:1s" unit — slug is literally `1`, matching `ROADMAP.md`'s assigned slug).
- Wrote L1/L2/L3 + exercises for: `web/need-html-semantics-just-divs`, `systems/persistence`, `git-teamwork/checkout`, `business-communication/status-updates-build-trust`, `logic/fallacies`, `security/authentication-fundamentals`, `infra-delivery/docker`, `career-craft/asking-feedback`, `product-domain/requirements-gathering`, `corporate-politics/white`, `learning-craft/source-evaluation`, `applied-math/measurement-theory`, `testing-quality/test-pyramid`, `software-design/single-responsibility`, `sustainable-performance/early-warning-signs`, `people-management/1`.
- Followed both standing rules established this session: technical tracks ground L3 in real, runnable code (a real salted-hash + rainbow-table demo, real `docker build` cache-hit output, a real WAL implementation, a real Goodhart's-Law numeric reproduction matching a new interactive demo); non-technical tracks use fully worked real-world scenarios in prose, no fake pseudocode. `applied-math/measurement-theory` got a second `interactives.json` demo (Goodhart's Law: proxy score vs. real target as optimization pressure and proxy-target correlation vary).
- Caught and fixed one more `poolId` level-mismatch bug (`product-domain/requirements-gathering`), same class as the earlier batch — same fix (drop the shared `poolId`, make the items standalone).
- **Verified all 16 units' Mermaid diagrams for real in the browser this time**, using direct DOM/`viewBox` inspection rather than screenshots (screenshot capture — `Page.captureScreenshot` — was unreliable all session, timing out repeatedly on this WSL/Vivaldi bridge; page JS execution was reliable throughout, so verification relied on that instead). Every flowchart, the one new xychart, and the new interactive demo reported a real, full-size `viewBox` (not the `-8 -8 16 16` collapsed-error box from the earlier bug), confirming the `Layout.astro`/`LevelTabs.astro` fix generalizes correctly to freshly-written content, not just the units open at the time of the fix. Also spot-checked that quiz grading actually works end-to-end (selected the correct choice on `people-management/1`'s first quiz, clicked "Check answer," confirmed it graded correct).
- `ROADMAP.md` flipped to `done` for all 16 new units (31 of 218 done now); `bun run generate:roadmap` + full `bun run check` (lint, format, typecheck, content validation — 32 written units checked, tests, 50-page build) clean.
- Next: pick a track to go deeper on (unit 03+), or keep advancing every track — user's call. Chrome bridge connectivity has been intermittent across sessions (worked this time, didn't at the start of the previous one) — always attempt reconnection before assuming it's unavailable, per this session's outcome.

## 2026-08-13 — Content-methodology pilot on `business-communication/audience-awareness` (not a new unit)

- User pushed back on the pedagogy across all 32 written units after actually using the site: L1/L2 explain before the reader feels a need to know (definitions/frameworks up front instead of a problem that creates the itch), no self-test moment before the answer is revealed, exercise banks too thin (~6/unit) for the 7/30/90-day spaced-repetition cycle to mean anything, and visual density (diagrams/tables/charts) too sparse. Agreed to fix the methodology + engine first, pilot it on **one already-written unit**, then stop for review before retrofitting the other 31 in future one-unit-per-session passes.
- **Engine changes** (not content): `ExercisePanel.astro` now shows only 2 randomly-picked pools per level per page view (was: all pools rendered at once), reshuffled on every load and on spaced-repetition reset — supports a much larger exercise bank without overwhelming the page. `ProgressToggle.astro` no longer gates on a static list of every pool id (`exerciseIds` prop, computed once at build time); it now reads whichever pools `ExercisePanel` is currently showing directly off the DOM (`hasExercises` boolean prop instead), so "Mark as done" only requires passing the currently-shown subset, not the full bank. `spaced-repetition.ts`'s `checkAndApplyReviewDue` now clears exercise state by localStorage key prefix instead of a passed-in id list, since there's no longer a fixed list to know ahead of time.
- **CLAUDE.md updated**: new rule 4b (problem-first, test-before-teach — L1 and every major L2 section must open with a concrete problem/prediction, not a definition, resolving it only afterward); exercises section rewritten to target ~40 items/unit (~13/13/14 across levels, pools of 2-3 variants) as a number being _piloted_, not a hard `validate:content` minimum, plus documentation of the new 2-shown/reshuffle/shown-subset-gating mechanic; visual-density guidance tightened from "at least one diagram" to "most units should carry 2-3 visual elements across L1-L3."
- **Pilot content**: rewrote `business-communication/audience-awareness`'s L1 and L2 to open with a concrete scenario/prediction before the framework (L3 already matched this voice — the payments-retry scenario — so left as-is); added one new table (mismatch-cost) and one new mermaid diagram (the four-question decision tree as a flowchart) to L2, on top of the existing flowchart+table. Expanded `exercises.json` from 6 to 40 items (13/13/14 across L1/L2/L3, quiz-only per rule 9 since this is a non-technical track), grouped into pools of 2-3 variants each (5-6 pools per level) so the new "2 of several" display actually has something to rotate through.
- `bun run check` clean (lint, format, typecheck, content validation, tests, 50-page build — unchanged unit/page count, this was a methodology pass not a new unit).
- **Browser-verified the full new mechanic end-to-end**, not just the build: confirmed each level tab shows exactly 2 of the unit's pools with a "Showing 2 of N — reload for a different pair" caption; answered exercises both right and wrong and confirmed the explanation always displays either way; confirmed "Mark as done" stayed locked while any shown pool was unanswered/failed, then unlocked once all _currently-shown_ pools (not all 40) were passed; clicked "Mark as done" and confirmed it persists across a reload; reloaded and confirmed a genuinely different 2-pool pair appeared per level (reshuffle-on-load works); confirmed both new L2 diagrams (flowchart + decision-tree) and both tables (lens table + new mismatch-cost table) render correctly, and the new problem-first L1/L2 openings read naturally in the browser, not just in the source.
- Next: **this needs user review before touching the other 31 units.** If the pilot's shape (voice, 40-exercise scale, 2-shown mechanic) is approved, the remaining 31 written units each need the same retrofit — one per session, per the existing one-unit-per-session rule — starting wherever the user wants to go next.

## 2026-08-13 — Pilot round 2: visual callouts, universal example, and a freehand whiteboard (still `audience-awareness`, still not a new unit)

- User reviewed round 1 and gave two more rounds of feedback, both addressed in this session:
  1. **"The problem itself forces too much reading, no visual support."** L1 had stopped being a skimmable index (the problem-first rewrite turned it into a narrative paragraph) and L3's worked example was pure prose. Fix: built a reusable `<Scenario>` callout component (`src/components/Scenario.astro`) — a visually distinct card (icon + label) that pairs the problem statement with a compact table/diagram instead of narrating the tension in prose. L1 went back to bullets-first with the problem trimmed to a 1-2 sentence callout + 3-row reaction table. L3 got the same callout for its opening scenario plus a new "at a glance" comparison table before the three full prose versions. New CLAUDE.md rule 4b codifies "problem-first, throughout, not just at the open" — guiding questions at each major L2/L3 step, plus an explicit requirement that L3 include at least one question inviting the reader to extend the worked example beyond what's given (added a "Beyond this example" section).
  2. **"Don't mix families — this topic isn't engineering-specific, use a universal example."** The entire unit (L1/L2/L3 + all 40 exercises) was anchored in an engineering scenario (a reconciliation job, vendor rate limits, Slack vs. email) even though audience-awareness itself is domain-general. Rewrote the whole unit around one consistent universal scenario — a class bake sale fundraiser short of its goal, told to a groupmate/teacher/principal — understandable without any professional context. New CLAUDE.md rule 4c: match example scope to the concept's actual scope, don't default to engineering framing just because the site's overall audience is engineers.
  - A follow-up ambiguity bug surfaced during this rewrite ("I never knew who actually panicked" — the three reactions in the L1 table were tonally similar quoted questions with no explicit signal): fixed by labeling each reaction's emotional register explicitly (Calm / Concerned / Alarmed) instead of relying on the reader to infer tone from phrasing alone.
- **New feature: a slide-in freehand whiteboard**, built via a full plan-mode cycle given the scope (new shared client module + two component integrations). `src/lib/whiteboard.ts` exports `mountWhiteboardTrigger()`: a drawer that slides in from the right with a backdrop, showing a read-only recap of the block's own content (mirrored via `Astro.slots.render` for `<Scenario>`, assembled from the shown variant for exercises) above a full freehand canvas (pointer-events drawing, pen/eraser/clear, persisted per-block via `localStorage` + `canvas.toDataURL()`, keyed by `pathname + block id` so it survives reloads without needing `track`/`unitSlug` props). Wired into both `Scenario.astro` (automatic on every problem callout) and `ExercisePanel.astro` (automatic on every shown exercise) — no new authoring needed for existing or future units, documented in CLAUDE.md's new "Whiteboard" section.
- `bun run check` clean at each stage; browser-verified end-to-end: callout/table rendering, both mermaid diagrams' viewBoxes confirmed non-collapsed, drawing → close → reopen → full page reload all preserve the drawing, Eraser and Clear both work correctly (Clear also removes the localStorage key), and Scenario vs. exercise whiteboards use distinct, non-colliding storage keys (confirmed via direct `localStorage` inspection).
- Next: same as round 1 — **awaiting user review** of this second pass before retrofitting the other 31 units. The engine additions this session (`<Scenario>`, the whiteboard) are now standing patterns any future unit gets automatically or can opt into with near-zero extra authoring.

## 2026-08-13 — Whiteboard refinement round (still `audience-awareness`, still not a new unit)

- User tested the whiteboard from the previous entry and gave three corrections:
  1. **"Chalks" — need selectable colors, not just black ink.** Added a 4-color swatch row per board style.
  2. **The reference panel was a literal duplicate of the visible callout/exercise — needed to be a distinct, structured, hierarchical recap instead.** Added a dedicated named slot on `Scenario.astro`, `<Fragment slot="facts">`, that a unit author writes separately from the flowing narrative (falls back to the narrative if unwritten, but new units should always write it — documented as a hard expectation in CLAUDE.md after shipping the duplicate version once). Wrote real `facts` content for both `audience-awareness` Scenario callouts (L1, L3). For exercises, simplified the reference down to just the question (`variant.prompt`) — dropped the choices/starter code, since those are already visible on the page beside the drawer and repeating them was the same duplication problem in miniature.
  3. **Two board presentations (white/green), and — caught during browser verification — colors need to remap between styles, not just persist unchanged.** Reworked the canvas to draw on a **transparent** layer with the board color as a separate CSS background (this also upgraded the eraser to a real `destination-out` erase instead of a same-color-as-background hack). White board uses marker colors (black/red/blue/green); green chalkboard uses chalk colors (white/yellow/pink/light-blue), paired by palette position. Switching board style now calls `recolorCanvas()` (nearest-RGB-match pixel remap against the previous palette, substituted with the new palette's positional counterpart) so a stroke keeps its _role_ (e.g. "the dark, primary color") rather than its literal RGB value, which was going near-invisible when carried unchanged onto the opposite-contrast board.
- `bun run check` clean after each round of changes. Verified the recolor logic isn't just visually plausible but exactly correct: drew a real stroke via dispatched pointer events, read back the exact RGB via `getImageData`, switched white → green → white, and confirmed the pixel value round-trips losslessly to the original.
- Next: unchanged — awaiting user review before retrofitting the other 31 units.

## 2026-08-13 — Whiteboard: text tool + one more bug fix (still `audience-awareness`, still not a new unit)

- User caught a real bug from the previous round: switching board style was also changing the **reference panel's** background (it shouldn't — the facts recap is unrelated to the scratch-space presentation choice). Fixed by removing the coupling in `applyBoardStyle()`; only the canvas background changes now, verified via `getComputedStyle`-equivalent check before/after a style switch.
- User also flagged that pure freehand drawing (mouse-only, no stylus) is uncomfortable for actually writing anything — added a **Text tool** alongside Pen/Eraser: clicking the canvas spawns a small positioned `<input>`, Enter/blur commits it onto the canvas via `ctx.fillText()` in the current color (Escape cancels). Committed text becomes ordinary raster pixels, so it persists/erases/recolors on board-style switch through the exact same mechanism as a pen stroke — no separate data model needed. Refactored the internal `erasing: boolean` into a proper `tool: "pen" | "eraser" | "text"` to support the three-way mode cleanly.
- Verified via dispatched pointer/keyboard events end-to-end: clicking Text spawns the input, typing + Enter removes it and commits, and the committed pixels were confirmed present in the exact clicked region via direct `getImageData` inspection (not just visually plausible — checked the actual opaque-pixel bounding box).
- `bun run check` clean.
- Next: unchanged — awaiting user review before retrofitting the other 31 units.

## 2026-08-13 — Whiteboard: make the facts panel actually visual (still `audience-awareness`, still not a new unit)

- User's last note on the `facts` reference panel: it was still pure text (bullets + bold numbers) — asked for something understood "at a glance," not read. Redid both `L1-summary.mdx` and `L3-deep-dive.mdx`'s `facts` Fragments with a real progress bar ($340 of $500, width set proportionally) and an emoji/icon per line for the cause/fix facts and the three reader roles, instead of plain bulleted sentences — raw HTML in a `not-prose` wrapper works directly inside an MDX `Fragment` slot, no new component needed.
- Documented the pattern in CLAUDE.md's Whiteboard section: prefer a genuine visual (progress bar, icon row) over plain text bullets when the facts support one — the bar is for future authors to reuse, not a one-off.
- `bun run check` clean; browser-verified the rendered panel shows the bar + icon rows as intended.
- Next: unchanged — awaiting user review before retrofitting the other 31 units.

## 2026-08-13 — Every exercise linked to its theoretical basis (still `audience-awareness`, still not a new unit)

- User's next request: the "more visual" requirement applies to every exercise, not just the intro problem — and specifically, a failed exercise should have a direct, visible connection to whatever teaches the concept it's testing (question → visual → theory), creating that link where it doesn't exist yet. Confirmed via plan mode (full write-up in this session's plan file) before executing, given the scope: ~19 exercise pools needed dedicated authored content, plus a schema change.
- Added an optional `reference` field to both exercise types in `src/content.config.ts` (trusted HTML, same pattern as `Scenario`'s `facts` slot). Extended `scripts/validate-content.mjs`'s existing pool-consistency check (previously just level/type) to also require identical `reference` across every variant in a pool, since only one variant renders per view.
- Authored `reference` for all ~19 pools across `exercises.json` (6 in L1, 6 in L2, 7 in L3) — each a compact visual (icon list, 2-column compare box, or mini table matching the concept — e.g. the Context/Incentives/Channel lens table row for `l2-context-lens`, a "role ≠ stake" callout for `l3-failure-hierarchy-vs-stake`) followed by a one-line `<strong>Why:</strong>` theory statement. Generated programmatically (a Python script mapping poolId → reference string, applied to all 40 items) rather than 40 manual edits, to guarantee the per-pool consistency the new validator checks for.
- Wired into `ExercisePanel.astro`: `buildReferenceHTML()` now appends the pool's `reference` below the question; `renderPool()` resolves it from whichever variant in the pool set it (`variant.reference ?? variants.find(v => v.reference)?.reference`) so the randomly-shown variant doesn't matter.
- `bun run check` clean, including the new validator rule passing against the freshly-authored content. Browser-verified end-to-end across all three levels: opened whiteboards for an L1 pool (`l1-not-spin`) and an L3 pool (`l3-failure-missing-ask`), confirmed both show Question → the pool-specific visual → the theory line, not just the bare prompt as before — content matched each pool's actual concept, not a generic placeholder.
- Next: unchanged — awaiting user review before retrofitting the other 31 units. This session has now built four reusable engine pieces any future unit gets for free or near-free: `<Scenario>` (problem callouts + `facts` slot), the 2-of-N exercise rotation, the freehand whiteboard (two board styles, text tool), and per-pool `reference` linking exercises to their theory.

## 2026-08-13 — A real "Learn more" modal, separate from the whiteboard's one-liner (still `audience-awareness`, still not a new unit)

- User clarified the previous round's `reference` "Why:" line — fine to keep, but not what they actually wanted for connecting a failed exercise to its theoretical basis. The real ask: a genuinely deep, dedicated explanation, in its own modal, reachable from its own button. Scoped via plan mode given the size (new component + schema + 19 pools of real content); confirmed with the user first: dedicated authored content (same pattern as `reference`/`facts`), reached by an always-available "Learn more" button next to "Whiteboard," not gated to failure.
- New module `src/lib/learn-more.ts` — `mountLearnMoreTrigger()`, deliberately simpler than the whiteboard (no canvas, no persistence): a **centered** modal (distinct from the whiteboard's right-side drawer, so it reads as "stop and read" rather than "work alongside"), closes via X / Escape / backdrop click.
- Added `learnMore: z.string().optional()` to both exercise types in `content.config.ts`, and generalized `validate-content.mjs`'s per-pool consistency check (previously just for `reference`) into a small loop covering both `reference` and `learnMore`, so any future third field reuses the same one-line addition instead of copy-pasted validation logic.
- Authored `learnMore` for all ~19 pools — two real paragraphs each (mechanism, then why it matters / the common mistake), not the one-liner `reference` has. Applied via the same Python poolId-keyed script pattern as `reference`, for the same consistency guarantee.
- Wired into `ExercisePanel.astro`: `exerciseToolsRow()` (renamed from `whiteboardTriggerRow`) now mounts both triggers side by side; `renderPool()` resolves `learnMore` with the same sibling-fallback logic already used for `reference`.
- `bun run check` clean. Browser-verified: both buttons render per exercise, "Learn more" opens a centered modal with genuine two-paragraph depth (confirmed against the actual authored text, not a placeholder), and closes correctly via the X button (backdrop opacity confirmed via `getComputedStyle`).
- Next: unchanged — awaiting user review before retrofitting the other 31 units.

## 2026-08-13 — "Learn more" content redone with real structure, not prose

- User's follow-up on the just-shipped modal: still the same problem as earlier rounds — solid paragraphs, no structure. Rewrote all ~19 pools' `learnMore` content in `exercises.json` with a consistent template: a heading, a table/comparison-box/flow-diagram as the central element (reusing the same visual language as `reference`/`facts`, but bigger and pool-specific), 1-2 short sentences of surrounding prose (not paragraphs), and a visually distinct amber "Common mistake" callout box instead of a mistake buried in running text.
- No engine changes this round — `learn-more.ts`, the schema, and `validate-content.mjs`'s consistency check all already supported this from the previous round; this was purely a content redo via the same Python poolId-keyed script approach.
- `bun run check` clean. Browser-verified two pools across two levels (`l1-facts-vs-effect`, `l2-context-lens`) — both render heading + table + short prose + mistake callout, confirmed via screenshot, not just text presence.
- Next: unchanged — awaiting user review before retrofitting the other 31 units.

## 2026-08-13 — L1/L2 body content: L1 and L3 were under-visualized relative to L2

- User pointed out L2 (4 visual elements: 2 diagrams, 2 tables) was fine, but L1 and L3's main body — after the problem callout, before the exercises — had almost none (L1: zero; L3: one table). Added, matching CLAUDE.md's existing "spread across L1-L3, not clustered in L2" guidance the pilot itself wasn't following yet:
  - **L1**: a mermaid flowchart showing the three lenses applying simultaneously to one message (distinct from L2's audience-branching diagram — this one shows convergence, not divergence), plus a table for the three pre-writing questions.
  - **L3**: a mermaid flowchart grounding the abstract L2 filter model in this unit's actual concrete facts ($340/$500/Saturday &rarr; 3 named versions), plus converted the "Failure modes" bullet list into a table (Failure / What it looks like / The fix) — more scannable than five paragraphs of bold-lead-in prose.
- `bun run check` clean. Browser-verified both new diagrams specifically for the known hidden-tab mermaid collapse bug from an earlier session (L3's tab isn't the default-visible one) — confirmed real, non-collapsed `viewBox` values on both after switching tabs, not just on initial load. Confirmed 6 tables total across the unit (2 per level) via direct DOM count.
- Next: unchanged — awaiting user review before retrofitting the other 31 units.

## 2026-08-13 — L3 pushed to 7+ visual elements (user's explicit target, matching L2's 4)

- User: if L2 has 4 visual elements, L3 (the deepest level) should have at least 7. Added, on top of the prior round's flowchart + 2 tables:
  - A new `xychart-beta` bar chart — "Relative detail included, by reader" (Groupmate 9, Teacher 5, Principal 2) — quantifies the pattern the prose already described, first chart type used in this unit alongside the flowcharts.
  - Small icon badges (🔍 Full detail / ⚙️ Status + default / 🎯 Outcome only) on each of the three Version headings — a quick visual read of each version's shape before reading the quote.
  - "What stayed constant" converted from plain bullets into a bordered green checklist box (✅ per item), matching the visual language already used for `reference`/`learnMore` callouts.
  - "Beyond this example" got a small inline icon-flow ("🥧 This bake sale &rarr; 🧑‍💼 Your actual readers") ahead of the exploratory questions, reinforcing the generalization point visually before the prose makes it explicitly.
  - Total distinct visual elements in L3 now: 2 diagrams (flowchart + xychart), 2 tables (at-a-glance, failure-modes), version badges, the constants checklist, and the beyond-example icon-flow — 7.
- `bun run check` clean. Browser-verified via direct DOM inspection on the L3 tab specifically (not the default-visible one): 3 non-icon SVGs with real viewBoxes (including the new xychart-beta at `0 0 700 500`, confirming it isn't the known hidden-tab collapse bug), 6 tables total across the unit, and visually confirmed the bar chart renders with real bars via screenshot.
- Next: unchanged — awaiting user review before retrofitting the other 31 units.

## 2026-08-13 — Two real bugs caught in the new L3 chart: invented data, and a site-wide dark-mode text color bug

- User caught the xychart's numbers (9/5/2, "Detail level 0-10") as invented — no real basis, not what the earlier data-grounding work in this session was supposed to prevent. Replaced with a genuinely countable metric: exact counts of concrete operational specifics named in each of the three quoted versions (rain/mechanism/magnitude/named-contact/time-of-day) — Groupmate 6, Teacher 3, Principal 1 — computed by literally listing and counting them against the actual quotes, with the count criteria spelled out in the surrounding text so a reader can verify it themselves. Also caught and corrected a wrong assumption baked into the original prose: word count does NOT track this pattern (the teacher version is actually the _longest_ at 72 words vs. the groupmate's 62) — said so explicitly instead of quietly using a metric that would have contradicted the point.
- User then caught a second, unrelated bug: the chart's title/axis text was dark-on-dark, unreadable. Root cause, found by reading mermaid's own source: `xyChart` title/axis colors fall back to `themeVariables.primaryTextColor`, which mermaid resolves from an internal `darkMode` boolean — **not** from the `theme: "dark"` string this project already passes to `mermaid.initialize()`. That boolean was never set, so `primaryTextColor` silently resolved to near-black regardless of site theme. Fixed in `src/layouts/Layout.astro`'s `chartThemeVariables` by setting `xyChart.titleColor`/`xAxisLabelColor`/`xAxisTitleColor`/`yAxisLabelColor`/`yAxisTitleColor`/tick/line colors explicitly for both light and dark branches, bypassing the broken fallback chain entirely.
- **This is a site-wide fix, not a pilot-only one** — `xychart-beta` is already used in 5 other written units (`applied-math/math-tool-prediction`, `applied-math/measurement-theory`, `software-design/cost-design`, `sustainable-performance/myth-linear-output`, `web/http-request-response-basics`), all of which had this same invisible-text bug and are now fixed by this one change, same pattern as the earlier-session hidden-tab mermaid collapse fix.
- `bun run check` clean. Verified the fix directly rather than trusting a screenshot (screenshots were unreliable this round, per the known WSL/Vivaldi CDP flakiness): read `getComputedStyle(...).fill` on the actual rendered title and axis-label `<text>` elements and confirmed they resolve to the exact hex values set in the fix (`#e2e8f0` title, `#cbd5e1` axis labels), not just "looks lighter."
- Next: unchanged — awaiting user review before retrofitting the other 31 units.

## 2026-08-13 — Quiz-integrity fix, then a batch of interactivity/animation work

- User caught a real gap in the quiz mechanic: nothing stopped clicking through choices one at a time until one passed, or re-selecting the now-visible correct answer after reading the explanation — either way "passed" no longer meant the learner actually knew it going in. Fixed in `ExercisePanel.astro`'s `renderQuiz`: after the first "Check answer" click, radios and the button lock (disabled), and each choice is color-coded (correct = emerald, the wrong one picked = red + strikethrough, the rest = greyed out).
- User then asked for the site to feel less static and specifically pushed back on things that only look interactive without actually being it: exercise/feedback reveal animation, a tab-switch transition, a "when you get it right vs. wrong" distinction, a micro-interaction on "Mark as done," an animated fill on the whiteboard's progress bar, and — the substantive one — real sliders that change the **problem's own numbers** (not the exercises) and update the analysis live. Implemented:
  - `global.css`: new one-shot keyframe animations — `fade-slide-in` (reveals), `shake` (wrong answer), `pop` (correct answer / "Mark as done"), `fill-bar` (a bar's `width` animating from 0 to a `--target-width` custom property set inline).
  - `ExercisePanel.astro`: shown pools now get `animate-fade-slide-in` with a staggered `animation-delay` when picked; quiz/code feedback gets `animate-pop` (pass) or `animate-shake` (fail); the explanation/solution reveal also gets `animate-fade-slide-in`.
  - `LevelTabs.astro`: switching tabs now applies `animate-fade-slide-in` to the incoming panel (remove-then-reflow-then-add, so repeated clicks re-trigger it) instead of an instant cut.
  - `ProgressToggle.astro`: a successful "Mark as done" click applies `animate-pop` to the button (same reflow re-trigger technique).
  - `L1-summary.mdx` / `L3-deep-dive.mdx`: the whiteboard's `$340`/`$500` progress bar switched from a static `width: 68%` to `--target-width: 68%` + `.animate-fill-bar`, so it fills in the first time the whiteboard drawer is built rather than appearing already-filled.
  - New `interactives.json`: one level-1 slider demo ("What if less selling time had been lost to rain?") reusing the already-built `InteractiveDemo.astro` engine with zero code changes — dragging `timeLostPercent` (0–70%, default 50%) live-updates `raised`/`gap` via a formula back-derived from the unit's own facts (50% exactly reproduces $340 raised / $160 gap), charted across the full range. This was the answer to "let me change the problem's values and see the result update in real time" — no new engine work needed, since `InteractiveDemo`/`interactives.json` already existed and was simply unused by this pilot.
  - **A real bug surfaced and fixed during this round, not just cosmetic**: the first implementation used a JS `requestAnimationFrame` double-class-swap (set the "from" state, then flip to the "to" state next frame) for the pool-reveal and tab-fade animations. This got stuck permanently at the "from" state (opacity: 0) during browser verification — traced to `document.visibilityState === "hidden"` on the automation tab, which suspends `requestAnimationFrame` callbacks entirely. Rather than special-case the test environment, switched both to a declarative CSS `animation` (keyframes) applied via a simple `classList.add`, which has no rAF/foreground dependency — confirmed correct by forcing the Web Animations API `.finish()` and checking the resulting computed style lands exactly on the intended end state (opacity 1, translateY 0, bar width exactly 68% of its parent).
- `bun run check` clean. Browser-verified via direct DOM/Web-Animations-API inspection rather than screenshots (this session's tab was backgrounded per `document.visibilityState`, making real-time screenshot/animation-timing checks unreliable — same class of flakiness noted in earlier sessions): confirmed `animate-shake`/`animate-pop` apply correctly on wrong/right answers with radios locked afterward, the L1→L2 tab switch re-renders Mermaid diagrams on the freshly-shown panel with real (non-collapsed) viewBoxes, the whiteboard bar's forced-finished width is exactly 68% of its container, the slider's live output at 0/50/70% is $680/$0, $340/$160, $204/$296 (matching the back-derived formula and the unit's actual story), and clicking "Mark as done" (after passing all shown exercises across all three levels) applies `animate-pop` and flips to "Done (review in 7d)".
- Next: unchanged — awaiting user review before retrofitting the other 31 units.

## 2026-08-13 — Try-it demo cleanup, then pilot signed off and rules strengthened

- User looked at the running preview and flagged the new "Try it" slider as unclear — not confusing on purpose, just not helping. Root-caused two real issues, not vague polish: (1) `InteractiveDemo.astro`'s `formatValue()` always suffixed the unit, so dollar amounts rendered as `340$` instead of `$340` — looked like a formatting bug; (2) the demo plotted two mathematically complementary outputs (`raised` and `gap = 500 - raised`) as two crossing lines with no independent meaning, which is exactly the kind of thing that raises "why does this cross here?" instead of teaching anything.
  - Fixed `formatValue()` to prefix currency symbols (`$`/`€`/`£`/`¥`) instead of always suffixing — a general engine fix, not pilot-only, so every future `interactives.json` demo using a currency unit benefits automatically.
  - Simplified `audience-awareness/interactives.json` to a single output (`raised`), stated the fixed $500 goal in the description text instead of as a second plotted line, and clarified the steady-pace assumption behind the underlying formula.
  - `bun run check` clean. Verified via direct DOM read (screenshots were flaky again this session) that the chart now renders exactly one `<polyline>`/one `<circle>`, and that dragging to 0%/70% live-updates to `$680`/`$204`.
- User then approved the pilot as done and asked to bake the lesson in: **push visual elements, interactions, and animation as the default for every unit going forward, not an optional nice-to-have.** Updated `CLAUDE.md`:
  - New rule **5c** under "Non-negotiable generation rules": every unit should default to a `Scenario` facts visual, an `interactives.json` demo wherever a real relationship exists, exercise `reference`/`learnMore`, and the shared animation classes on new interactive elements — skipping any of these should be a stated, deliberate call, not silence.
  - "Visual design balance" reworded from "restraint, 2–3 elements for most units" to "default to including them, 2–3+ for every unit, actively look for the comparison/quantity/relationship rather than waiting for one to be obvious."
  - "Interactive demos" reworded from "not every unit needs one" to "default to building one — look for the relationship before concluding there isn't one" — plus two new documented lessons from this round: keep demos to as few outputs as carry independent information (no complementary-line crossings), and the currency-prefix formatting rule.
- **Pilot (`business-communication/audience-awareness`) is now signed off.** Retrofitting the other 31 already-written units to this bar is unblocked, but not started — per the "one unit per session" rule, that's separate work to take on deliberately, one at a time, not as a batch.

## 2026-08-13 — Second retrofit pilot: `security/authentication-fundamentals` (technical track, to validate the rules generalize)

- User deliberately picked a second, different-domain unit to stress-test whether the rules updated in the previous entry actually generalize beyond the non-technical pilot, rather than only fitting `business-communication`'s shape. Chose `security/authentication-fundamentals` (technical, code-heavy, with real quantifiable relationships) over two other offered options (`systems/process`, `web/http-request-response-basics`).
- Converted `L1-summary.md`/`L2-concept.md`/`L3-deep-dive.md` to `.mdx` (needed for the `Scenario` component) and retrofitted per the strengthened rules:
  - **L1**: opens with a `Scenario` — two companies breached the same day, one storing plain-text passwords and one salted hashes, same 2M-account database, opposite consequences — with a distilled `facts` whiteboard slot (breach-outcome comparison boxes) distinct from the narrative. Added a 3-column icon visual for the "something you know/have/are" factor categories.
  - **L2**: reworded section headers into guiding questions per rule 4b ("If a database gets stolen, what can the attacker actually do with what's stored?", "Two users pick the same password — should their stored hashes look the same?"). Fixed a real rule-9 violation found while reading the existing content: a pseudocode block was fenced as \`\`\`python despite not being valid Python (`function register(username, password):` isn't Python syntax) — relabeled as a plain fence, since L2 pseudocode shouldn't masquerade as a specific runnable language. Added a new `interactives.json` slider demo (level 2): drag password length and attacker guesses/sec (as a log10 exponent) and watch time-to-crack change by orders of magnitude for the same password — directly grounded in L3's own fast-hash-vs-slow-KDF discussion.
  - **L3** (already strong — real Node `crypto` code, a genuine runnable rainbow-table demo): added a static `xychart-beta` bar chart (log-scale guesses/sec: SHA-256 vs. bcrypt vs. scrypt vs. Argon2id) as a second, distinct visual alongside the interactive demo, and an extension question per rule 4b ("what would you need to change for a password-reset flow, where the old hash can't be re-derived from a locked-out user").
  - **Exercises**: expanded from 6 flat singleton items (no pools, no `reference`/`learnMore`) to 39 items across 15 pools (13/13/13 per level, 2–3 variants each), matching the pilot's ~40-item bar almost exactly. Every pool has both `reference` (compact visual + one-line "why," in the whiteboard) and `learnMore` (deeper table + common-mistake box) — none were skipped. Kept both original code-exercise properties (toy salted hash, deterministic + input-sensitive) and added a second variant (`mixHash`, argument order flipped, different mixing formula) testing the same underlying properties.
- **A real, user-relevant bug found and fixed during browser verification, not just cosmetic**: dragging the new crack-time demo to its slow-attacker/long-password extreme produced `754566709180386.5 years` — technically correct but unreadable, the same class of clarity problem the first pilot's Try-it review caught. Fixed `InteractiveDemo.astro`'s `formatValue()` to use `Intl.NumberFormat` compact notation (`754.6 B years`) for any value ≥ 1,000,000 — another general engine fix, not pilot-only, so any future demo with real exponential-scale outputs benefits automatically.
- `bun run check`-equivalent run in full (`generate:roadmap`, `lint`, `format`, `typecheck`, `validate:content`, `test`, `build`) — all clean, only pre-existing unrelated warnings (Zod deprecation notice, two `is:inline` script hints). Browser-verified directly rather than trusting the build alone: L1's Scenario/icon visuals render, L2's Mermaid diagram and both new interactive-demo sliders live-update the chart and output correctly at both extremes (post-fix), L3's new bar chart renders with correct axis labels, the code exercise (`mixHash` variant, shown on first load) fails with a broken implementation and passes with the real one, both with the solution/explanation correctly revealed either way, and "Mark as done" correctly stays gated/disabled until every currently-shown pool across all three levels is passed.
- **Conclusion: the rules generalize.** The default-visual/interactive/animation posture from the previous session's `CLAUDE.md` changes produced a comparably rich unit on a technical, code-heavy track without needing further rule changes — the only edits this round were a genuine content bug (mislabeled pseudocode) and a genuine engine bug (large-number formatting), not gaps in the rules themselves.
- Next: suggest picking a third track for variety (e.g. something diagram/process-heavy like `systems/process`, or a soft-skill track distinct from `business-communication` like `learning-craft` or `people-management`) whenever the user wants to continue retrofitting, one unit per session.

## 2026-08-13 — Three global UI bugs found by the user testing the live preview, all fixed at the engine level

- User tested the running preview themselves (both retrofitted units) and reported three cross-cutting issues, none specific to one unit:
  1. **Quiz radio buttons didn't clearly show which choice was selected** before grading — the native radio dot alone was too subtle to scan at a glance. Fixed in `ExercisePanel.astro`'s `renderQuiz`: each choice's `<label>` now gets a visible border + background + bold text the moment its radio is checked (a `change` listener toggles between a defined "selected" and "unselected" label class across the group), on top of the native dot, not instead of it — pre-grading only; the existing post-grading correct/wrong color-coding is unchanged.
  2. **Mermaid `xychart-beta` bar charts cut off long x-axis category labels** (e.g. "SHA-256 (GPU cluster)") — mermaid's default `xAxis.labelRotation` is `0` (horizontal) with no wrapping, so labels wider than their allotted per-category slot overlapped/were clipped. Fixed in `Layout.astro`'s `mermaid.initialize()` by adding a top-level `xyChart: { xAxis: { labelRotation: -30, labelFontSize: 11 } }` config (separate from the existing `themeVariables.xyChart` color overrides, which only handle theming, not layout) — angling labels gives each one far more effective width for the same horizontal footprint, the standard fix for this class of chart. Site-wide, not pilot-only: every `xychart-beta` bar chart across all units benefits automatically.
  3. **`InteractiveDemo.astro`'s own SVG line chart clipped numbers/labels at certain slider positions** — the marker dot's value label was always drawn to the dot's right and slightly above it, and the y-axis min/max labels used a fixed 44px left margin; both assumptions broke once a real relationship (this session's crack-time demo) produced long compact-notation labels (`754.6 B years`) or a marker near the chart's right edge. Fixed in `InteractiveDemo.astro`'s `buildChart()`:
     - The left/right chart margins are now computed per-render from the actual current y-axis and x-axis-max label lengths, growing beyond the old fixed minimums instead of clipping against them.
     - The marker's value label flips to the dot's _left_ side (with matching `text-anchor`) whenever there isn't room on the right, and its vertical position is clamped to stay inside the plot area — so a label can no longer run past the SVG's own bounds and get silently hidden by overflow, at any slider position.
  - All three fixes are general engine/layout fixes, not specific to either retrofitted unit — same category as the earlier currency-prefix and compact-number-formatting fixes from prior sessions.
- Verified directly in the browser rather than trusting the build: checked/unchecked radio label classes toggle correctly; read the actual rendered `<text>` geometry (`getBoundingClientRect()` compared against the SVG's own bounding rect, not `getBBox()` — which ignores the rotation transform and gave false positives on the first check) for both the L3 bar chart (confirms −30° rotation, no clipping, no label overlap) and the L2 interactive demo at all four slider corners (min/min, min/max, max/min, max/max) — zero overflowing text nodes in every case. Also re-checked the original pilot's demo (`business-communication/audience-awareness`) at both slider extremes to confirm no regression from the shared-component changes.
- `bun run check`-equivalent run in full — clean, same pre-existing unrelated warnings as before.
- Next: unchanged from the previous entry — awaiting the user's opinion on the second pilot before picking a third track.

## 2026-08-13 — Mermaid bar-chart labels still clipping after the first fix; root-caused to a mermaid internal estimation bug

- User pasted a screenshot from testing the live preview themselves showing the L3 bar chart's rotated x-axis labels still visibly cut off ("SHA-256 (GPU cluster)" missing its "SHA-" prefix, etc.), despite the −30° rotation fix from the previous entry. The earlier verification (`getBoundingClientRect()` on text vs. the SVG's own rect) had been insufficient — it doesn't account for content that overflows the SVG's _internal_ `viewBox` coordinate space specifically (browsers default the root `<svg>` element to `overflow: hidden`, confirmed via `getComputedStyle`), which is a different, stricter boundary than the SVG's rendered client rect.
- Root-caused properly this time by transforming each `<text>`'s `getBBox()` through its actual `getCTM()` into the chart's own viewBox coordinates and comparing against `[0, 0, 700, 500]` directly. Found the real problem: labels were overflowing the chart's **bottom** edge (up to ~18px), not the sides — the −30° rotation fix from the previous entry had actually fully solved the horizontal/side clipping (confirmed zero left/right overflow at every step below), but rotating labels adds vertical footprint that mermaid's default `height: 500` doesn't budget for.
- Tried increasing `xyChart.height` (500 → 580 → 650) first — this barely moved the overflow. Reading mermaid's own `xychartDiagram` source clarified why: the x-axis's reserved space is computed from its own estimated footprint (`sin(rotation)×textWidth + cos(rotation)×textHeight + labelPadding×2`), not as a percentage of total chart height — so growing `height` mostly just grows the plot/bar area, not the axis label area. The real fix was `xAxis.labelPadding` (default `5`, feeds directly into that reserved-space formula) — raised to `20`, which closed most of the gap, then to `28`, which reached zero overflow on every axis label in the live chart. The remaining gap at each step was consistent with mermaid's own text-measurement estimate slightly under-shooting the real rendered bbox for rotated text (an internal mermaid quirk, not something wrong with this project's config) — `labelPadding` buys back that margin directly rather than fighting the exact trig estimate.
- Reverted the unhelpful `height: 650` override; the final `Layout.astro` mermaid config is just `xyChart: { xAxis: { labelRotation: -30, labelFontSize: 11, labelPadding: 28 } }`.
- Verified with the corrected getCTM-based check: zero overflowing text nodes (any edge) on `security/authentication-fundamentals` L3's bar chart, and re-checked it doesn't regress the other three existing `xychart-beta` charts (`web/http-request-response-basics`, `applied-math/math-tool-prediction`) — zero overflow there too, confirming the fix generalizes rather than being tuned to one chart's specific label lengths.
- `bun run check`-equivalent run in full — clean, same pre-existing unrelated warnings.
- **Lesson for future verification**: `getBoundingClientRect()` comparisons against a parent's client rect are not sufficient to catch SVG-internal clipping — the reliable check is transforming `getBBox()` through the element's own `getCTM()` into the root SVG's viewBox coordinate space and comparing there, since that's the actual boundary the browser's default `overflow: hidden` on `<svg>` enforces.
- Next: unchanged — awaiting the user's opinion on the second pilot (now with all three reported UI bugs fixed) before picking a third track.

## 2026-08-13/14 — Agent-orchestrated retrofit of remaining units to the new content bar, batch 1 (3 units)

- User asked to dispatch agents to retrofit all already-written units (30 remaining) to the validated visual/interactive/animation content bar, one unit each, until done. Used the `Agent` tool with `isolation: "worktree"` (parallel git worktrees under `.claude/worktrees/agent-<id>/`, each on its own branch) so agents don't collide on file writes, batching 3 at a time.
- **Process bug found and fixed**: none of the pilot work (both retrofitted units, the shared component/CSS/lib changes) had been committed yet — it only existed as uncommitted working-tree changes. Worktree agents branch from committed history, so they were invisible to it. First batch of 3 agents all started from a stale base missing the whole pilot infrastructure; one correctly detected this and stopped (git-teamwork/checkout, first attempt), one proceeded against the stale ruleset (applied-math/measurement-theory, first attempt — discarded), one worked around it by manually copying in the missing shared files (learning-craft/exposure-understanding — completed successfully, but its branch carried a hand-duplicated copy of shared infra alongside its real content). Fixed at the root by committing the full pilot work (`1ced061`) so every subsequent/relaunched worktree branches from the correct, complete base. Also fixed two things this surfaced: `.gitignore` was missing `.claude/` (agent worktree scratch space), and `eslint.config.mjs`'s `ignores` didn't exclude `.claude/**`, so ESLint's TS parser choked on multiple nested `tsconfig.json`s once worktrees existed.
- Relaunched `applied-math/measurement-theory` and `git-teamwork/checkout` from the correct base; both hit an external Claude API session-limit error mid-task (unrelated to the base fix) but had already finished all content authoring (L1/L3 as `.mdx`, L2, expanded `exercises.json` with pools/reference/learnMore, `interactives.json`) before failing on the final verification steps.
- **Merged all three units' content into `main`** (`learning-craft/exposure-understanding`, `applied-math/measurement-theory`, `git-teamwork/checkout`) — extracted only each unit's `src/content/<track>/<unit>/*` files from its worktree (explicitly discarding the duplicated shared-infra files from the exposure-understanding branch, which were confirmed byte-identical to what's already on `main`), then ran the full guardrail suite (`generate:roadmap`, `lint`, `format`, `typecheck`, `validate:content`, `test`, `build`) fresh in the main checkout against the merged result — all clean. One small fixup needed: `eslint --fix` had stripped an `eslint-disable-next-line` comment in `InteractiveDemo.astro` down to an orphaned continuation line; restored it as a plain two-line comment (the disable itself was unnecessary — the rule wasn't firing — so dropped the directive, kept the explanation).
- Browser extension wasn't connected this session, so this batch was **not** browser-verified interactively (only guardrail-verified) — worth a manual spot-check of these three units' exercises/demos next session.
- 3 of 30 remaining units done (exposure-understanding, measurement-theory, checkout). 27 remain. Continuing in batches of 3.

## 2026-08-14 — Retrofit batch 2 (3 units) — found and worked around a session-wide worktree-isolation bug

- Launched 3 more worktree-isolated agents (`applied-math/math-tool-prediction`, `business-communication/status-updates-build-trust`, `career-craft/asking-feedback`) after committing batch 1 (`6d93976`), expecting them to branch from that commit.
- **All three instead branched from `51aab47`** — one commit before the very first pilot commit (`1ced061`), i.e. the state of `main` at the moment the _first_ worktree agent was ever launched this session. Committing more work to `main` afterward did not change what base new `isolation: "worktree"` agents get — the base appears to be fixed/cached per-session from the first use, not re-read from current `HEAD` on each launch. This is a session-scoped environment quirk, not something fixable by committing more or waiting.
- Two agents (`math-tool-prediction`, `asking-feedback`) discovered this only after writing full retrofit content (using `<Scenario>` and `reference`/`learnMore` exercise fields that don't exist at that stale base) and hitting a `bun run build` failure on the unresolved `Scenario.astro` import — correctly stopped rather than reimplementing the missing shared infrastructure themselves. The third (`status-updates-build-trust`) caught the mismatch earlier and made no content changes at all (also correctly stopped) — its worktree was empty and auto-cleaned up by the harness; that unit goes back in the queue for batch 3.
- One process incident during this: the `status-updates-build-trust` agent reported it accidentally ran `rm` against the **shared main checkout** (not just its own worktree) while investigating, then restored the deleted files from what it had just read. Verified directly afterward: `git status`/`git diff` on the main checkout showed clean, zero divergence from `HEAD` — no actual damage, but flagging the incident since it's a real risk pattern (an agent operating outside its assigned worktree) worth watching for in future batches.
- Recovered the same way as batch 1's `exposure-understanding` case: since the two content-complete agents had only ever touched their own unit's `src/content/<track>/<unit>/*` files (never the shared infra they were missing — they couldn't, it didn't exist in their worktree), their content was extracted directly and copied into the main checkout, where `Scenario.astro` etc. already exist. No rebase needed. Ran the full guardrail suite fresh (`lint`, `format`, `typecheck`, `validate:content`, `test`, `generate:roadmap`, `build`) against the merged result in `main` — all clean.
- Browser-verified both units this time (extension was connected): L1/L2/L3 render, Scenario callouts with visual `facts` panels render correctly, exercise pools show/gate correctly (`data-shown-ids`/`data-ready`), and both units' interactive-demo sliders were dragged to every param's min/max and checked via the `getBBox()`-through-`getCTM()`-into-viewBox geometry check — zero overflowing text in any case, on both units' demos.
- 5 of 30 remaining units done (exposure-understanding, measurement-theory, checkout, math-tool-prediction, asking-feedback). 25 remain (`status-updates-build-trust` re-added to the queue). Continuing in batches of 3 — future batches should expect the same stale-worktree-base bug and plan to extract-and-merge content-only rather than assume a clean worktree merge will work.

## 2026-08-14 — Retrofit batch 3 (3 units) — agents instructed to push through the known worktree bug instead of stopping

- Applied the lesson from batch 2: told all 3 agents upfront about the stale-worktree-base bug and instructed them to write full content anyway (reading exemplars/CLAUDE.md from the main checkout path, not their worktree) and treat a `Scenario.astro`-import build failure as an expected, non-blocking symptom rather than a reason to stop. All 3 agents (`business-communication/status-updates-build-trust`, `career-craft/leveling-expectations`, `corporate-politics/power-real-resource`) followed this correctly — every one produced full, content-complete retrofits (L1/L3 as `.mdx` with `<Scenario>` + facts slots, L2 rewritten with guiding questions, 40-item `exercises.json` with `reference`/`learnMore` per pool, a grounded `interactives.json` demo) and reported clean lint/format/typecheck/validate-content/test results, with `build` failing only on the expected missing-`Scenario.astro` symptom.
- Merged all three the same way as before: confirmed each worktree only touched its own unit's `src/content/<track>/<unit>/*` files, copied those into the main checkout, ran the full guardrail suite fresh (`lint`, `format`, `typecheck`, `validate:content`, `test`, `generate:roadmap`, `build`) — all clean on the first pass this time (no orphaned-comment fixup needed, unlike batch 1).
- **Full browser verification this round** (Chrome extension connected): all three units' L1/L2/L3 tabs render correctly with rich Scenario callouts (progress bars, icon rows, comparison tables), mermaid diagrams, and xychart-beta charts; each unit's interactive-demo slider(s) dragged to every param's min/max and checked via the `getBBox()`/`getCTM()`-into-viewBox geometry check — zero overflowing text on any of the three; exercise pool gating (`data-shown-ids`/`data-ready`) confirmed wired correctly on all three.
- Content highlights worth noting: `leveling-expectations` kept its engineering framing deliberately (it's a workplace-specific mechanic per rule 4c, not a domain-general one) with a strong Alex/Jordan scope-vs-skill scenario; `power-real-resource` and `status-updates-build-trust` both produced clean, well-structured non-technical L3s with zero fake-pseudocode.
- 8 of 30 remaining units done (exposure-understanding, measurement-theory, checkout, math-tool-prediction, asking-feedback, status-updates-build-trust, leveling-expectations, power-real-resource). 22 remain. The "push through instead of stopping" instruction change worked cleanly — recommend keeping it as the standing approach for all future batches while the worktree-base bug persists this session.

## 2026-08-14 — Retrofit batch 4 (3 units, first technical-track pair) — all 3 agents hit the Claude API session limit mid-task, salvaged from partial worktree state

- Launched batch 4 (`corporate-politics/white`, `git-teamwork/snapshots-manual-copies`, `infra-delivery/docker` — the first two technical-track units retrofitted since the pilot). All three agents were terminated mid-task by an external Claude API session-limit error ("You've hit your session limit · resets 1:50pm America/Lima"), unrelated to this project. Resumed the session after the reset per the user's message ("ya puedes continuar ya son las 2").
- Before assuming the work was lost, inspected each worktree directly: all three had, in fact, already finished writing full content (L1/L3 `.mdx`, L2, 38-40 item `exercises.json`, `interactives.json`) before being cut off — the agents' last visible message ("now let's build exercises.json...") undersold how far they'd actually gotten; verified this by parsing each JSON file directly (`python3 -m json.tool`-equivalent) rather than trusting the transcript.
- Extracted and merged all three units' content-only files into `main`, same procedure as prior batches.
- **Found and fixed two real content bugs during the fresh guardrail run** (neither agent could have caught these — both failures only surface once merged onto the correct base, which none of them had access to):
  1. `validate:content` caught a genuine authoring bug in `git-teamwork/snapshots-manual-copies/exercises.json`: two variants sharing `poolId: "content-dedup"` had different `reference` HTML — the schema requires identical `reference`/`learnMore` across all variants of a pool (only one variant renders per view, so they must agree). Fixed by copying the canonical variant's `reference`/`learnMore` onto the other.
  2. `bun run build` failed rendering `/infra-delivery/docker` with `Cannot read properties of undefined (reading 'length')` inside `InteractiveDemo.astro`. Root cause: the `docker` agent had manually added `import InteractiveDemo ...` and `<InteractiveDemo id="build-time-vs-changed-layers" />` directly inside `L2-concept.mdx`'s prose — but `InteractiveDemo.astro` expects a `demos` array prop (plural, a list of full demo objects) and is meant to be rendered exactly once, automatically, by `src/pages/[track]/[unit]/index.astro` per level — not manually invoked from content with an `id` string, which the component has no code path for at all. No other unit does this. Fixed by removing the stray import and the manual `<InteractiveDemo>` tag from the MDX; the demo still renders correctly since the page template already handles it. Worth flagging in future agent prompts: interactive demos are automatic, never manually embedded in MDX.
  3. `git-teamwork/snapshots-manual-copies` kept `L2-concept.md` as `.md` (no `<Scenario>` needed there), consistent with the `checkout` unit's pattern — correctly followed the established convention rather than converting every level to `.mdx` by default.
- After both fixes, full guardrail suite clean (`lint`, `format`, `typecheck`, `validate:content`, `test`, `generate:roadmap`, `build`), and all three units browser-verified: L1/L2/L3 render, mermaid/xychart-beta visuals correct, exercise gating wired (`shownIds` populated correctly on all three), and every interactive-demo slider dragged to min/max on every param with zero SVG text overflow via the `getBBox()`/`getCTM()` geometry check.
- 11 of 30 remaining units done (adds `white`, `snapshots-manual-copies`, `docker`). 19 remain. Lesson for future batches: always run the full guardrail suite fresh in the main checkout after merging, even when every agent self-reported clean results — both bugs this round were invisible to the agents because they never had a working base to actually build/validate against.

## 2026-08-14 — Retrofit batch 5 (3 units) — clean run, no bugs found

- Retrofitted `infra-delivery/environment-parity`, `learning-craft/source-evaluation`, `logic/fallacies`, all via worktree-isolated agents briefed on both known issues (stale-base bug, and the `InteractiveDemo`-must-never-be-manually-embedded-in-MDX bug found in batch 4). All 3 produced full content-complete retrofits and self-reported clean guardrails aside from the expected `Scenario.astro` build failure.
- Merged, then ran the full guardrail suite fresh in the main checkout — clean on the first pass this time (`lint`, `format`, `typecheck`, `validate:content`, `test`, `generate:roadmap`, `build`), no bugs found, confirming the batch-4 lesson (brief agents explicitly on known failure modes) is working.
- Browser-verified all three: L1/L2/L3 render, exercise gating wired (`shownIds` populated), and each unit's interactive-demo slider(s) dragged to every param's min/max with zero SVG text overflow via the `getBBox()`/`getCTM()` geometry check.
- Content notes: `environment-parity` added a second code-exercise pool (`dockerfile-review`, a linter-style `isUnpinnedBaseImage` check) alongside the existing timezone-fix pool — first unit in this retrofit pass with two distinct L3 code pools rather than one. `fallacies` kept one piece of genuine runnable code (`isFalseDichotomy`, a real exhaustiveness check) inside an otherwise non-technical/prose track, correctly distinguishing "real defensible code" from "fake pseudocode" per rule 9's actual intent rather than mechanically banning all code blocks from non-technical tracks.
- 14 of 30 remaining units done. 16 remain.

## 2026-08-14 — Retrofit batch 6 (3 units) — clean run

- Retrofitted `logic/formal-informal-logic`, `people-management/1` (the "1:1s" unit), `people-management/ic-excellence-management`. All 3 self-caught and fixed their own issues before reporting (one agent caught a poolId mixing `quiz` and `code` item types in the same pool — an invalid combination per `validate:content` — and fixed it before finishing).
- Merged, ran the full guardrail suite fresh in the main checkout — clean on the first pass (`lint`, `format`, `typecheck`, `validate:content`, `test`, `generate:roadmap`, `build`), no bugs found.
- Browser-verified all three: L1/L2/L3 render, exercise gating wired (`shownIds` populated), every interactive-demo slider dragged to min/max on every param with zero SVG text overflow.
- 17 of 30 remaining units done. 13 remain.

## 2026-08-15 — Retrofit batch 7 (3 units) — first attempt lost to an API session limit, relaunched clean

- First attempt at batch 7 (`product-domain/requirements-gathering`, `software-design/cost-design`, `systems/process`) was cut off by an external Claude API session-limit error before any agent had written content — unlike batch 4, where agents had finished writing before being cut off, this time the last visible messages showed all three still in the "confirm the stale-base bug, start reading exemplars" phase. Confirmed via direct inspection (worktrees already auto-cleaned up, `main`'s `git status` clean at the last known commit with only the original pre-retrofit `.md` files present) that there was genuinely nothing to salvage, and relaunched the same 3 units fresh as a full new batch rather than hunting for partial state that didn't exist.
- Second attempt: all 3 agents briefed with the full accumulated known-issues list (stale-base bug + push-through instruction + `InteractiveDemo`-must-never-be-manually-embedded-in-MDX + poolId type/level/reference consistency) and pointed at track-appropriate exemplars (`career-craft/asking-feedback` + `business-communication/status-updates-build-trust` for the non-technical `product-domain` unit; `infra-delivery/docker` + `infra-delivery/environment-parity` for the two technical units). All 3 completed cleanly, each self-validating exercise JSON logic (one agent ran a standalone Node harness mimicking the sandboxed Worker's `expect()` API to verify every code exercise's `solution` actually passes its own tests before reporting back).
- `systems/process` already had a pre-existing `interactives.json` from before this retrofit pass — reviewed and kept it unchanged (still well-grounded in the retrofitted L3 content) rather than replacing a working demo without reason.
- Merged all three, ran the full guardrail suite fresh in the main checkout (`generate:roadmap`, `lint`, `format`, `typecheck`, `validate:content`, `test`, `build`) — clean on the first pass, no bugs found. None of the three units required a `ROADMAP.md` status change (all three were already marked `done` pre-retrofit, since this is a presentation/depth retrofit of existing units, not new-unit creation).
- Browser-verified all three: L1/L2/L3 render correctly, exercise gating wired (`shownIds` populated), Scenario callouts with visual `facts` slots render. Ran the `getBBox()`/`getCTM()`-into-viewBox overflow check on every interactive demo's slider at min/max — found the check flags several axis-label/value-label text nodes as technically outside a strict `viewBox` bound on all three units' demos, but confirmed via direct screenshot inspection this is a structural characteristic of the shared `InteractiveDemo.astro` SVG (the rendered SVG box is proportionally wider than its declared `viewBox`, i.e. effectively `overflow: visible` in its margin area) — the labels are not visually clipped or cut off in the actual rendered page on any of the three units. Not a regression introduced by this batch; worth a dedicated look at `InteractiveDemo.astro`'s SVG scaling in a future session, but not blocking.
- 20 of 30 remaining units done (adds `requirements-gathering`, `cost-design`, `process`). 10 remain: `product-domain/understanding-user-s-problem-solution`, `security/security-mindset`, `software-design/single-responsibility`, `sustainable-performance/early-warning-signs`, `sustainable-performance/myth-linear-output`, `systems/persistence`, `testing-quality/case-automated-testing`, `testing-quality/test-pyramid`, `web/http-request-response-basics`, `web/need-html-semantics-just-divs`.

## 2026-08-15 — Retrofit batch 8 (3 units) — one agent lost to a connection error, relaunched alone

- Launched `security/security-mindset`, `testing-quality/test-pyramid`, `web/http-request-response-basics`. The `web/http-request-response-basics` agent failed almost immediately ("API Error: Connection closed mid-response") after only reading files and sketching a plan — no content written, worktree already auto-cleaned up. The other two (`security-mindset`, `test-pyramid`) completed cleanly and in full.
- Relaunched `web/http-request-response-basics` alone as a fresh single-unit retry (not resuming — starting over), briefed with the same accumulated known-issues list plus one new note from this session: the codebase convention (confirmed via grep across all existing units) is exactly ONE `<Scenario>` per file, not one per major section — flagged explicitly to avoid an agent adding redundant Scenario callouts. It completed cleanly, kept and re-grounded a pre-existing `interactives.json` keep-alive-savings demo in L3's actual numbers instead of discarding it.
- Merged all three, ran the full guardrail suite fresh in the main checkout (`generate:roadmap`, `lint`, `format`, `typecheck`, `validate:content`, `test`, `build`) — clean on the first pass, no bugs found. No `ROADMAP.md` status changes needed (all three already `done` pre-retrofit).
- Browser-verified all three: L1/L2/L3 render correctly with Scenario callouts and visual facts slots, exercise gating wired (`shownIds` populated, e.g. `web/http-request-response-basics` showed `["methods-intent-pool","statelessness-pool"]`).
- 23 of 30 remaining units done (adds `security-mindset`, `test-pyramid`, `http-request-response-basics`). 7 remain: `product-domain/understanding-user-s-problem-solution`, `software-design/single-responsibility`, `sustainable-performance/early-warning-signs`, `sustainable-performance/myth-linear-output`, `systems/persistence`, `testing-quality/case-automated-testing`, `web/need-html-semantics-just-divs`.

## 2026-08-15 — Retrofit batch 9 (3 units) — one agent lost to a session-limit error mid-task, completed with a solo follow-up

- Launched `product-domain/understanding-user-s-problem-solution`, `software-design/single-responsibility`, `systems/persistence`. `product-domain/understanding-user-s-problem-solution` and `systems/persistence` completed cleanly and in full. `software-design/single-responsibility` was cut off by an external Claude API session-limit error partway through — its last visible message ("Now L3 pools (quiz + code).") was ambiguous about actual progress, so per the standing procedure ("inspect the worktree directly before deciding salvage vs. relaunch") the worktree was read directly rather than trusted from the transcript: `L1-summary.mdx`, `L2-concept.mdx`, `L3-deep-dive.md` (with a new failure-modes table and an "extend the example" section), and `interactives.json` were all present, complete, and valid — only `exercises.json` was missing.
- Rather than discarding and relaunching the whole unit, dispatched a solo follow-up agent directly into the same existing worktree (not a fresh `isolation: worktree` — an explicit `cd` into the on-disk path) with the narrow task of writing just `exercises.json`, reading the unit's own already-complete L1/L2/L3 plus the sibling `software-design/cost-design` exercises.json as a structural exemplar. It produced 40 items (12 quiz pools across L1/L2, 6 code pools for L3, code exercises grounded in L3's `MonthlyReportGenerator`/`BillingCalculator`/`UsageDataFetcher`/`ReportPdfRenderer` scenario) and self-verified every code `solution` actually passes its own tests by replaying the sandboxed Worker's `expect()` API in Node before reporting back.
- Merged all three, ran the full guardrail suite fresh in the main checkout (`generate:roadmap`, `lint`, `format`, `typecheck`, `validate:content`, `test`, `build`) — clean on the first pass, no bugs found. No `ROADMAP.md` status changes needed (all three already `done` pre-retrofit).
- Browser-verified all three: L1/L2/L3 render correctly, exercise gating wired on every level (e.g. `software-design/single-responsibility` showed L1 `["srp-not-literal","reasons-to-change-test"]`, L2 `["over-split-email-example","decision-procedure"]`, L3 `["orchestrator-compose","filter-usage-events"]` after switching tabs), interactive demos present on all three.
- 26 of 30 remaining units done (adds `understanding-user-s-problem-solution`, `single-responsibility`, `persistence`). 4 remain: `sustainable-performance/early-warning-signs`, `sustainable-performance/myth-linear-output`, `testing-quality/case-automated-testing`, `web/need-html-semantics-just-divs`.

## 2026-08-17 — Retrofit batch 10 (4 units) — final batch, retrofit project complete

- Launched all 4 remaining units at once (`sustainable-performance/early-warning-signs`, `sustainable-performance/myth-linear-output`, `testing-quality/case-automated-testing`, `web/need-html-semantics-just-divs`) rather than splitting into groups of 3 — with only 4 left, no reason to force an extra batch. All 4 agents completed cleanly on the first attempt, no failures/relaunches needed this time.
- `early-warning-signs` and `myth-linear-output` (both non-technical `sustainable-performance` units) already had a thin pre-existing `exercises.json` (6 items) from before this retrofit pass — both agents expanded it in place to the ~40-item/pool-based target rather than discarding it, and `myth-linear-output` also kept its pre-existing well-built `interactives.json` (`output-curve-explorer`) unchanged since it already matched the retrofitted L3 content.
- `case-automated-testing` and `need-html-semantics-just-divs` (technical tracks) got full L3 code-exercise pools grounded in each unit's own worked examples; both agents self-verified every code `solution` actually passes its own tests before reporting back.
- Merged all four, ran the full guardrail suite fresh in the main checkout (`generate:roadmap`, `lint`, `format`, `typecheck`, `validate:content`, `test`, `build`) — clean on the first pass, no bugs found. (Ran `format` — which writes fixes — before `git add` this time per the batch-9 lesson, avoiding the extra cleanup commit.) No `ROADMAP.md` status changes needed (all four already `done` pre-retrofit).
- Browser-verified all four: L1/L2/L3 render, exercise gating wired on every level (`shownIds` populated per unit, e.g. `web/need-html-semantics-just-divs` showed `["semantics-div-button-gaps-pool","semantics-div-legit-pool"]` on L1 and `["semantics-l3-landmark-pool","semantics-l3-tag-pool"]` on L3), interactive demos present on all four. Additionally end-to-end tested one L3 code exercise's "Run tests" flow on `web/need-html-semantics-just-divs` (clicked, confirmed new reveal buttons and checkmark glyphs appeared post-grading) since the Chrome extension's screenshot capture was unreliable this session (`Page.captureScreenshot` CDP timeouts) — fell back to DOM/dataset/innerText probing via `javascript_tool`, which is an equally valid verification method per the retrofit-queue.md fallback note.
- **30 of 30 units retrofitted. The retrofit project (dispatch agents to bring every already-written unit up to the visual/interactive/animation bar established by the `business-communication/audience-awareness` and `security/authentication-fundamentals` pilots) is complete.** All originally-`done` units now carry Scenario callouts with visual facts slots, 2-3+ visual elements across L1-L3, ~40-item pooled exercise banks with per-pool whiteboard references, and interactive demos where a real underlying relationship exists.

## 2026-08-17 — New unit: web/css-cascade-specificity, first run of `/next-unit`

- Added a `.claude/commands/next-unit.md` slash command that codifies CLAUDE.md's full session workflow (pick unit → scope check → scaffold → write content → update ROADMAP.md → `bun run check` → browser-verify → update PROGRESS.md → suggest next), so future sessions can start with one command instead of re-deriving the process each time.
- Immediately used it to write the first genuinely new unit since the retrofit project finished (as opposed to retrofitting an already-`done` unit). Since the user didn't name a track, the command's tie-break logic picked the tracks with the lowest done/total ratio (`web` and `systems`, both 2/17) and offered 3 candidates via `AskUserQuestion`; the user picked `web/css-cascade-specificity` — "How do we style once and reuse everywhere? (CSS cascade & specificity)".
- Wrote all three levels directly (not via a subagent, since this was a single focused unit): L1 opens on a rule that "should have worked" but didn't (a later `.btn-primary` losing to an older, more specific `#page .toolbar .btn`); L2 covers the full 4-step cascade chain, specificity as a componentwise tuple (not additive), why utility/BEM codebases stay flat while id-nested legacy codebases develop "specificity wars," and the two modern escape valves (`:where()`, `@layer`); L3 is a full incident (a design-system migration silently failing on old pages) with a real, runnable specificity-calculator function used to diagnose it, plus the `:where()`/`@layer` fixes and an extension question about third-party CSS you don't control.
- Caught and fixed one real specificity-direction bug while drafting L1's comparison table (`button.btn` vs `.btn-primary` — the tie-break on the element column was initially stated backwards) before it shipped, by hand-tracing the tuple comparison rule.
- Wrote 40 exercises (13/13/14, 18 pools) and self-verified all 38 code-exercise test assertions actually pass their solutions by extracting and running them directly out of the committed JSON (not just a hand-written reimplementation) — this caught 2 pools where `reference` differed by variant (only `learnMore` is required to match by CLAUDE.md's rule, but `reference` must too), fixed before merge. Also added `interactives.json` — a slider on DOM nesting depth showing legacy id-based specificity growing linearly while flat utility/BEM specificity stays constant at 1, deliberately using raw class counts instead of the id=100/class=10 additive shorthand so the demo can't accidentally imply "enough classes eventually beat an id," which L2 explicitly warns is false.
- Ran the full guardrail suite (`generate:roadmap`, `lint`, `format`, `typecheck`, `validate:content`, `test`, `build`) — clean. Browser-verified: exercise pools populated on all 3 levels, a quiz answered wrong (radios lock, explanation shows), an L3 code exercise run unmodified (solution/explanation reveal on failure too), and the interactive demo dragged min→max confirming the legacy-nesting output tracks the slider 1:1 while the flat-methodology output stays at 1.
- Flipped `web/css-cascade-specificity` to `done` in `ROADMAP.md`. 33 total `done` units now (2 pilots + 30 retrofitted + 1 new).
- Next: any track/unit — no particular one suggested, since this session used the new tie-break logic (`web`/`systems` still tied at 2/17 done, followed by `git-teamwork` at 2/16).

## 2026-08-17 — New unit: systems/algorithmic-complexity, first autonomous-loop unit

- User asked to "leave it running" — confirmed via two quick questions that the loop should (a) auto-pick the next unit each time without pausing to confirm, and (b) commit each finished unit locally (no push) as it completes, push handled separately by the user. This entry is the first unit written entirely under that mode.
- Auto-picked `systems/algorithmic-complexity` (tied with `web` at 2/17 done, `systems` selected this round) — "Why is my code fast on my machine but slow in production? (algorithmic complexity, Big-O in practice)".
- L1 opens on a "remove duplicate customer IDs" function that passes CI on a 200-row fixture and times out on a 500,000-row production import; L2 traces the incident's pseudocode line by line (`record.id not in result` hiding an O(n) scan inside what looks like a single check), covers the standard Big-O simplification rules (drop constants, drop lower-order terms, nested-vs-sequential), and the worst-case/average-case distinction for hash lookups; L3 reproduces the incident with real, runnable JS and an actual Node benchmark.
- Caught a real fabrication risk before it shipped: the L3 benchmark originally had invented "typical output" numbers for the array-vs-Set comparison. Actually ran the benchmark code in Node instead of asserting fictional timings — the real numbers were noisier than a clean textbook example (JIT warm-up and small-n measurement noise overshot the O(n²) prediction on the first step, undershot on the second), so the content was rewritten to show the ACTUAL measured numbers and explicitly teach why a single small-n timing isn't proof of anything — a more honest and arguably more useful lesson than a fabricated clean ~16x-every-step number would have been.
- Wrote 40 exercises (13/13/14, 18 pools) grounded in the incident, including a `countComparisons(n)` exercise using the exact triangular-number formula `n(n-1)/2` (not the rounded `n²` used elsewhere for illustration) so the exercise itself demonstrates L2's 'drop the constant, drop the lower-order term' simplification rule concretely. All 35 code-exercise test assertions verified passing against their solutions by extracting and executing them directly from the committed JSON. Added `interactives.json` — a slider on n (10–2000) charting O(n) vs O(n²) work, whose default (n=200) reproduces L1's Scenario fact (~40,000 comparisons) exactly.
- Ran the full guardrail suite (`generate:roadmap`, `lint`, `format`, `typecheck`, `validate:content`, `test`, `build`) — clean. Browser-verified: exercise pools populated on all 3 levels, mermaid renders, an L3 code exercise solved correctly and confirmed passing, and the interactive demo showing exactly 40,000 at n=200 and 4,000,000 at n=2,000 (both matching n² precisely).
- Flipped `systems/algorithmic-complexity` to `done` in `ROADMAP.md`. 34 total `done` units now. Committed locally (not pushed).
- Continuing the loop autonomously — no further user input expected between units unless something needs a judgment call outside the standing instructions.

## 2026-08-17 — New unit: git-teamwork/working-tree

- Auto-picked `git-teamwork/working-tree` (tied at 2/16 done alongside a few others after the previous two picks) — "Why did my change disappear when I switched branches? (working tree, staging, commit model)".
- Verified every claim in a real sandbox git repo before writing anything, rather than inventing shell transcripts: reproduced the actual "silent carry-over" case (an uncommitted edit to a file identical between two branches survives a checkout untouched, moves to the new branch, then gets wiped by an unrelated `git restore .` run there — confirmed the loss happens at the restore step, not either checkout) and the "refused" case (`error: Your local changes ... would be overwritten by checkout` when the file genuinely diverges between branches). `git checkout -- .` itself was blocked by the sandbox's destructive-command guard even in a scratch repo; used `git restore .` (verified equivalent) instead.
- L1 opens on the exact verified incident; L2 covers `git status`'s two independent diffs (working-tree-vs-index, index-vs-HEAD) and checkout's per-file decision tree; L3 reproduces the full four-step transcript command-by-command with real, verified output, plus the diverged-file counter-case and an untracked-files edge case.
- Wrote 40 exercises (13/13/14, 17 pools) modeling the unit's decision logic as pure functions for L3 (`checkoutOutcome`, `statusLabels`, `filesAtRisk`, `isDiscardRecoverable`, `describeThreeAreas`) rather than literal git commands, matching the sibling `checkout` unit's established pattern for this track. All 31 code-exercise test assertions verified passing by extracting and running them directly from the committed JSON. Added `interactives.json` — sliders for files-edited and %-diverged-in-target, charting how many of your uncommitted files are actually "at risk" vs. silently carried over, grounded in the `filesAtRisk` exercise logic.
- Ran the full guardrail suite (`generate:roadmap`, `lint`, `format`, `typecheck`, `validate:content`, `test`, `build`) — clean. Browser-verified: exercise pools populated on all 3 levels, mermaid renders, an L3 code exercise solved correctly and confirmed passing, and the interactive demo showing 0 files at risk at 0% divergence and all 5 at 100% divergence.
- Flipped `git-teamwork/working-tree` to `done` in `ROADMAP.md`. 35 total `done` units now. Committed locally (not pushed).

## 2026-08-17 — New unit: applied-math/orders-magnitude

- Auto-picked `applied-math/orders-magnitude` (tied at 2/15 done with `people-management`) — "How do I know if a number is big or small without relying on gut feeling? (orders of magnitude, dimensional analysis)".
- Built the whole unit around one fully-verified synthetic incident (not a fabricated one): 2,000,000 records/day × 5 KB/record correctly computed as 10,000,000 (KB), then mislabeled "MB" instead of converted, inflating the reported figure to 10,000 GB (10 TB) against an actual 10 GB requirement — exactly 1,000x, verified arithmetically before writing (`2,000,000 × 5 = 10,000,000`; `10,000,000 KB → 10 GB` correctly; `10,000,000` relabeled MB → `10,000 GB`).
- L1 opens on the incident; L2 covers unit cancellation as algebra (`records × KB/record = KB`), conversion factors as "multiplying by a disguised 1," and Fermi estimation (round each factor to the nearest power of 10) as a fast, independent sanity check — reproducing the exact ~10 GB estimate that would have caught the error in seconds. L3 builds a real `Quantity` value type (tracks its unit, throws on incompatible `.add()`, verified in Node to reproduce the incident's exact 1,000x inflation) and a runnable `fermiEstimate()` function, plus a brief, factual reference to the real 1999 Mars Climate Orbiter loss (well-documented public knowledge, not fabricated specifics) as an external parallel, and an extend-the-example question about the opposite-direction error (underestimate → outage, not wasted budget).
- Wrote 40 exercises (13/13/14, 18 pools) grounded in the same incident throughout (Fermi estimation, unit-cancellation algebra, the `Quantity` guard, order-of-magnitude comparison functions). All 27 code-exercise test assertions verified passing by extracting and running them directly from the committed JSON. Added `interactives.json` — a slider for "number of skipped conversion steps" (0-2) charting the reported figure exploding by 1,000x per step against the flat actual requirement, reproducing the incident exactly at step=1.
- Ran the full guardrail suite (`generate:roadmap`, `lint`, `format`, `typecheck`, `validate:content`, `test`, `build`) — clean. Browser-verified: exercise pools populated on all 3 levels, mermaid renders, an L3 code exercise solved correctly and confirmed passing, and the interactive demo showing exactly 10 GB / 10,000 GB / 10,000,000 GB at 0/1/2 skipped conversions.
- Flipped `applied-math/orders-magnitude` to `done` in `ROADMAP.md`. 36 total `done` units now. Committed locally (not pushed).

## 2026-08-17 — New unit: people-management/delegation-levels

- Auto-picked `people-management/delegation-levels` (tied at 2/15 done with `applied-math`, which the previous unit had just resolved) — "How do I delegate work without either micromanaging or abandoning it? (delegation levels, trust but verify)".
- Non-technical track — per CLAUDE.md rule 9, L3 is a fully worked real-world scenario (a before/after dialogue), not code. Built the whole unit around one scenario: a manager asks a report to "take care of" picking a new CI vendor; the report signs a one-year contract before the manager ever sees a shortlist, missing a better-fit vendor the manager knew about from another team. The root cause: a two-level gap on a six-level delegation spectrum (manager assumed "recommend, I approve"; report assumed "decide and act, tell me after") that was never made explicit.
- L1 opens on the incident; L2 gives the six-level spectrum, a two-question framework (reversibility + delegate's context) for choosing a level, and shows "trust but verify" as one specific, named check-in trigger rather than constant oversight; L3 plays out the full "before" and "after" conversations word-for-word, showing the fix is a single added sentence (not a heavier process), plus two extend-the-scenario questions (higher-stakes contractor hire; a report with 3x prior track record).
- Wrote 40 quiz-only exercises (13/13/14, 15 pools, matching the sibling `people-management/1` exemplar's quiz-only pattern for this track) grounded throughout in the same incident and its two extensions. Added `interactives.json` — sliders for contract value and cancellation-fee %, charting the real dollar cost of a missed check-in against the $0 cost when the check-in point is named, reproducing the incident's actual stakes (a cancellation fee on a signed contract).
- Ran the full guardrail suite (`generate:roadmap`, `lint`, `format`, `typecheck`, `validate:content`, `test`, `build`) — clean. **Browser verification was limited this session**: the Chrome extension bridge was disconnected (session-limit interruption earlier in the session likely broke the connection) and reconnect attempts failed, so live click-through testing (answering a quiz, dragging the demo slider) could not be done as in prior units. Verified via `curl` instead that the built page serves correctly (200, 3 exercise panels present, 1 interactive demo present, mermaid diagrams present in the markup) — a real but reduced level of verification compared to the DOM/JS-interaction checks used earlier in this session; worth a full browser click-through next session once the extension reconnects.
- Flipped `people-management/delegation-levels` to `done` in `ROADMAP.md`. 37 total `done` units now. Committed locally (not pushed).

## 2026-08-17 — New unit: business-communication/reading-stakeholder-incentives

- Auto-picked `business-communication/reading-stakeholder-incentives` (tied at 2/14 done with `corporate-politics`) — "Why did my 'obviously right' proposal get rejected? (reading stakeholder incentives)".
- Domain-general track, but this specific unit is a workplace mechanic (per CLAUDE.md rule 4c) — kept the workplace grounding rather than forcing a universal-audience scenario. Built the whole unit around one scenario: an engineer's technically flawless proposal to consolidate two internal tools gets rejected by the team lead who owns the tool being retired — not because of the numbers, but because approving it right before performance review season would read as an admission their prior year's flagship work was redundant.
- L1 introduces the "visible scorecard" (cost, merit) vs. "invisible scorecard" (the decision-maker's own standing) distinction; L2 gives a four-question incentive-mapping framework (measured on / gains / loses / timing risk) as an actual fillable table, explicitly framing this as accurate modeling of a legitimate concern, not manipulation; L3 plays out the full first pitch (rejected) and second pitch (accepted) word-for-word — proving not one underlying fact changed between them, only framing and ownership — plus two extend-the-scenario questions (no lever available; a recurring pattern across 3 incidents pointing to a systemic issue beyond any single incentive map).
- Wrote 40 quiz-only exercises (13/13/14, 18 pools). Explicitly decided NOT to build an `interactives.json` for this unit — the underlying relationship (stakeholder incentive analysis) is categorical/diagnostic, not a genuine continuous quantity, and fabricating a pseudo-quantitative "persuasion probability" slider would imply false precision about an inherently social/political dynamic; stated this call directly rather than skipping silently, per CLAUDE.md rule 5c's explicit-opt-out clause.
- Ran the full guardrail suite (`generate:roadmap`, `lint`, `format`, `typecheck`, `validate:content`, `test`, `build`) — clean. **Browser verification remained unavailable this session** — the Chrome extension bridge stayed disconnected (same issue as the prior unit); verified via `curl` instead that the built page serves correctly (200, 3 exercise panels present, 2 mermaid diagrams present in the markup, no interactive-demo marker as expected since none was built).
- Flipped `business-communication/reading-stakeholder-incentives` to `done` in `ROADMAP.md`. 38 total `done` units now. Committed locally (not pushed).

## 2026-08-17 — New unit: corporate-politics/who-holds-influence

- Auto-picked `corporate-politics/who-holds-influence` (2/14 done, tied lowest ratio) — "How do I read the informal org chart, not just the official one? (who actually holds influence, real decision networks)".
- Built around one scenario: two engineers want the same infrastructure change approved — one follows the formal reporting chain and stalls for three weeks; the other has a 15-minute conversation with a senior IC (no title, no direct reports) whom the director has learned to trust over years, and gets approved in days. Explicit throughout: Path B never skips the formal approval — it adds a genuine informal evaluation step upstream of it.
- L1 introduces the formal-chart-vs-informal-network distinction; L2 gives four observable signals of real influence (loop-in without formal role, being cited when a decision is explained, attending-without-chairing, disagreement alone stalling a decision) plus a concrete "trace the last three decisions" exercise, and explicitly frames legitimate use (getting real feedback early) against what the unit does NOT endorse (currying favor regardless of merit, or treating the informal step as a way to skip formal approval); L3 traces both paths in full, showing the exact same formal approval step in both, plus two extensions (the informal network shifting when a key person leaves; the harder case of an org with no established informal network yet).
- Wrote 40 quiz-only exercises (13/13/14, 16 pools), matching the sibling `power-real-resource` exemplar's quiz-only pattern for this track. Same explicit `interactives.json` opt-out as the previous unit — informal-influence mapping is categorical/diagnostic, not a genuine continuous quantity worth charting.
- Ran the full guardrail suite (`generate:roadmap`, `lint`, `format`, `typecheck`, `validate:content`, `test`, `build`) — clean. Browser verification again limited to `curl` structural checks (200, 3 exercise panels, 3 mermaid diagrams present) — Chrome extension bridge still disconnected this session.
- Flipped `corporate-politics/who-holds-influence` to `done` in `ROADMAP.md`. 39 total `done` units now. Committed locally (not pushed).

## 2026-08-17 — New unit: security/authorization-models

- Auto-picked `security/authorization-models` (`security` tied at lowest done/total ratio) — "How do we decide what an authenticated user is allowed to do? (authorization models: RBAC/ABAC)".
- Built the whole unit around a real, named vulnerability class: an IDOR (Insecure Direct Object Reference) incident where `GET /api/invoices/:id` correctly requires login but never checks that the requested invoice belongs to the requester — a genuinely authenticated user changes the URL's ID and gets a stranger's full billing details.
- L1 draws the authentication-vs-authorization distinction, names IDOR explicitly, defines RBAC and ABAC, and states directly why RBAC alone cannot express the fix (a role has no channel for referencing which specific resource is being requested); closes with a 3-row table mapping the incident's skipped check to authorization/ABAC-ownership specifically. L2 gives a mermaid flowchart of the 3-step check sequence (session → RBAC → ABAC-ownership), pseudocode for both models, a "combine both" flowchart, and a generalizable audit question (for every ID-accepting endpoint, is there an explicit attribute comparison, or is authentication alone silently standing in for it). L3 reproduces the exact incident in real, Node-verified JS — a vulnerable handler and a fixed handler differing by exactly one added ownership/admin check, separately-composable `rbacAllows`/`abacAllowsViewInvoice` functions, and an extension generalizing single-owner ABAC to team-membership ABAC (`abacAllowsViewTeamInvoice`) — plus a 5-row failure-modes table.
- All L3 code (`getInvoiceFixed`, `rbacAllows`, `abacAllowsViewInvoice`, `abacAllowsViewTeamInvoice`, and the newly-added combined check `canAccessInvoice`) was verified twice: once against the content as written, and again by extracting and running every code-exercise `solution`+`tests` pair directly out of the committed `exercises.json` via Node — all 14 code items pass.
- Wrote 40 exercises (13/13/14 across L1/L2/L3, 17 pools) — quiz for L1/L2 conceptual pools (authN vs authZ, IDOR naming, RBAC/ABAC definitions, why RBAC can't express ownership, the 3-check sequence, combining both models, the generalizable audit question), code for L3 (the 5 verified functions above, 2-3 variants each). Pool-consistency validated (level/type/reference/learnMore identical across all `poolId` variants).
- Built `interactives.json`: unlike the two prior non-technical units, this unit has a genuine quantifiable angle beyond the categorical access decision itself — how the IDOR "blast radius" (unauthorized cross-access paths in a vulnerable design) scales with system size. Params: number of users (2-50) and resources per user (1-5); at the default (2 users, 1 resource each) it reproduces the unit's own incident exactly (2 unauthorized paths, matching the symmetric user-42/user-7 pair), then charts how that number grows as the user base grows, with the fixed design's count always at 0.
- Ran the full guardrail suite (`generate:roadmap`, `lint`, `format`, `typecheck`, `validate:content`, `test`, `build`) — clean, no errors. Removed the stale scaffolded `L1-summary.md`/`L3-deep-dive.md` stubs (superseded by the `.mdx` versions). Chrome extension bridge remains disconnected this session (retried once, still not connected) — verified via `curl` structural checks instead: 200 OK, 3 mermaid diagrams, 3 exercise panels (one per level), interactive-demo marker present, 4 level-tab panels.
- Flipped `security/authorization-models` to `done` in `ROADMAP.md`. 40 total `done` units now. Committed locally (not pushed).
- Next: continuing the standing autonomous loop — auto-picking the next unit by lowest done/total ratio across tracks.

## 2026-08-17 — New unit: career-craft/mentoring-fundamentals

- Auto-picked `career-craft/mentoring-fundamentals` — a 5-way tie at the lowest done/total ratio (`career-craft`, `learning-craft`, `product-domain`, `software-design`, `testing-quality`, all 2/13), broken alphabetically, then lowest-numbered `planned` unit in that track — "How do I help a junior teammate grow without doing their job for them?".
- Built the whole unit around one scenario: Sam is stuck 40 minutes on a failing test caused by a stale test mock; Priya could fix it herself in 5 minutes, or spend 25 minutes asking diagnostic questions instead. Workplace-mechanic unit within a domain-general track (per CLAUDE.md rule 4c) — kept the workplace grounding rather than forcing a universal-audience scenario, matching sibling `asking-feedback`'s pattern.
- L1 introduces mentoring as transferring capability (not just answers), names the "just fix it" trap's invisible delayed cost, distinguishes productive struggle from wasted struggle, and introduces the "I do / we do / you do, I watch / you do alone" gradual-release gradient. L2 gives a mermaid flowchart for diagnosing productive-vs-wasted struggle and missing-fact-vs-unconnected-pieces, the four-stage gradient as pseudocode, a signal table for matching stage to specific skill (not the mentee as a fixed trait), and the generalizable lesson that "always ask, never tell" is itself too rigid a rule. L3 plays out Path A (silent fix) vs. Path B (diagnostic questions, full dialogue) word-for-word, a table of what each of Priya's three questions does/avoids, and two extensions: real urgency (narrate reasoning while doing it, rather than reverting to silence) and a third recurrence of the same bug class (signals a missing mental model, calling for a dedicated stage-1/2 conversation instead of repeating stage 3).
- Wrote 40 quiz-only exercises (13/13/14, 18 pools) — matching sibling `career-craft` units' quiz-only pattern. Pool-consistency validated (level/type/reference/learnMore identical across all `poolId` variants).
- Built `interactives.json`: a genuine quantifiable relationship distinct from the categorical mentoring-style decision itself — total mentor time under "always fix it directly" (grows with future recurrences of the same bug class) versus "teach once" (flat), reproducing the unit's own 5 min / 25 min numbers at the default slider position (22.5 min vs. 25 min — a real near-breakeven point, not an obviously-superior line, correctly reflecting that the unit does NOT claim direct fixes are always wrong).
- Ran the full guardrail suite (`generate:roadmap`, `lint`, `format`, `typecheck`, `validate:content`, `test`, `build`) — clean, no errors. Removed the stale scaffolded `L1-summary.md`/`L3-deep-dive.md` stubs.
- **The Chrome extension bridge reconnected this session** (after being down for the prior 3 units) — did a full real browser click-through instead of `curl`-only checks: confirmed 3 exercise panels with populated `data-shown-ids`, clicked a wrong quiz answer and confirmed the "Not quite" + explanation feedback rendered and radios locked afterward (integrity gate still holds), confirmed the interactive demo's two sliders render at correct defaults (5, 70%) with correct default outputs (22.5 min / 25 min), and confirmed dragging the first slider to its max (20) live-updated the chart to the hand-calculated value (75 min) — all matching the content exactly. Note: the `computer` tool's screenshot capture timed out repeatedly in this environment even though the page and JS were fully responsive, so verification was done via `javascript_tool` DOM inspection/interaction rather than visual screenshots — a real but visually-unconfirmed level of verification.
- Flipped `career-craft/mentoring-fundamentals` to `done` in `ROADMAP.md`. 41 total `done` units now. Committed locally (not pushed).
- Next: continuing the standing autonomous loop — auto-picking the next unit by lowest done/total ratio across tracks.

## 2026-08-17 — New unit: learning-craft/search-literacy

- Auto-picked `learning-craft/search-literacy` — a 4-way tie at the lowest done/total ratio (`learning-craft`, `product-domain`, `software-design`, `testing-quality`, all 2/13), broken alphabetically, then lowest-numbered `planned` unit — "How do I ask a question well enough to get a useful answer?".
- Built around one scenario: Jordan's Wi-Fi keeps dropping; a vague search ("wifi keeps disconnecting") returns 40,000+ mostly unrelated results after 20 minutes, while a specific search using the router's model number and the word "firmware" (both learned from skimming the first search) finds the manufacturer's own support page describing the exact bug in under 2 minutes. Kept a domain-general, universally-relatable scenario (per CLAUDE.md rule 4c) even though the concept naturally involves technical documentation.
- L1 introduces anchor terms (specific vs. generic search words), the idea that some questions have one authoritative source worth going to directly, and reframes a "failed" first search as reconnaissance that surfaces better terms rather than a wasted attempt. L2 gives a mermaid flowchart of how combined anchor terms narrow results, a four-step refinement loop (search → skim for better terms, not answers → re-search → repeat or go to the source), and a signal table for recognizing when a single authoritative source is the right target. L3 plays out both full search sessions, a table of what changed between them, and two extensions: no canonical source exists at all (fall back to independent corroboration — where search literacy meets source evaluation), and not yet knowing the domain's own vocabulary for the symptom (the refinement loop applies one level earlier, to bootstrap terminology itself).
- Wrote 40 quiz-only exercises (13/13/14, 17 pools) — matching sibling `learning-craft` units' quiz-only pattern. Caught and fixed a copy-paste typo (`amber-390` → `amber-300`, a Tailwind class that doesn't exist) during pool-consistency validation before finalizing; also caught the L2 count landing at 11/13 on first pass and added 2 more items to reach the 13/13/14 target.
- Built `interactives.json`: an explicitly-labeled **illustrative, order-of-magnitude model** (matching the framing style already used in `applied-math/orders-magnitude`'s Fermi estimation) — each genuinely specific anchor term is modeled as ruling out roughly 9 of every 10 remaining results, reproducing the scenario's own 42,000-result vague-query baseline at the default (0 anchors) and narrowing to ~42 at 3 anchors, matching the scenario's "a handful, mostly relevant" outcome. Framed explicitly as illustrative rather than a precise prediction, to avoid implying false precision about a real search engine's actual ranking behavior.
- Ran the full guardrail suite (`generate:roadmap`, `lint`, `format`, `typecheck`, `validate:content`, `test`, `build`) — clean, no errors. Removed the stale scaffolded `L1-summary.md`/`L3-deep-dive.md` stubs.
- **Chrome extension bridge stayed connected this session** (browser click-through, not `curl`-only) — confirmed 3 exercise panels with populated pools, 1 mermaid diagram, the interactive demo's slider present at the correct default; dragged the slider to 3 anchors and confirmed the output updated live from 42,000 to 42 (exact match to the formula); clicked a quiz answer and confirmed grading rendered feedback + explanation and locked the radios afterward. Screenshot capture (the `computer` tool) timed out repeatedly despite the page being fully responsive — verification was done via `javascript_tool` DOM inspection, same visually-unconfirmed caveat as the prior unit.
- Flipped `learning-craft/search-literacy` to `done` in `ROADMAP.md`. 42 total `done` units now. Committed locally (not pushed).
- Next: continuing the standing autonomous loop — auto-picking the next unit by lowest done/total ratio across tracks.

## 2026-08-17 — New unit: product-domain/domain-driven-design-basics

- Auto-picked `product-domain/domain-driven-design-basics` — a 3-way tie at the lowest done/total ratio (`product-domain`, `software-design`, `testing-quality`, all 2/13), broken alphabetically, then lowest-numbered `planned` unit — "How do I model a business domain so the code doesn't fight the business?".
- `product-domain` is classified non-technical per CLAUDE.md rule 9, so even though DDD is inherently a software modeling technique, L3 was grounded in a fully worked business/engineering dialogue rather than code snippets — no fake pseudocode-as-code.
- Built around one scenario: Billing reports 1,200 "Customers" (paying accounts), Support reports 4,800 "Customers" (ticket submitters) for the same business — both correct, colliding only because they share one word and one database table built by whichever team touched it first.
- L1 introduces ubiquitous language (keeping business vocabulary and code vocabulary identical), the core reframe that neither team was "wrong," and bounded contexts (a boundary within which a term has one precise meaning). L2 gives a mermaid flowchart contrasting "force one shared definition" (deletes the other team's real information) against "recognize two concepts + an explicit relationship," a table distinguishing bare data fields from modeled business rules, and the generalizable lesson that splitting isn't free and should only happen where meanings have genuinely diverged. L3 plays out the full reconciliation meeting (Priya asks "what are you actually counting?" before comparing numbers), a before/after table, and two extensions: a team whose own internal definition is genuinely fuzzy (naming can't fix an undefined concept — that needs domain-expert work), and organizational scale (10 teams → 45 possible pairwise conflicts, motivating an explicit context map over ad-hoc meetings).
- Wrote 40 quiz-only exercises (13/13/14, 17 pools) — matching the non-technical track's quiz-only pattern. Caught and fixed the same `amber-390` typo pattern from the prior unit (5 occurrences) during pool-consistency validation, and added 2 more L3 items after the first pass landed at 12/14.
- Built `interactives.json`: an explicitly-labeled illustrative model contrasting the pairwise coordination cost of forcing one shared definition (`n·(n-1)/2`, growing combinatorially) against bounded contexts' roughly-linear cost (`n`, one boundary per team) — reproducing the scenario's own 2-team situation at the default (1 conflict vs. 2 boundaries) and showing the crossover as team count grows toward the 10-team, 45-pair example used in L3's second extension.
- Ran the full guardrail suite (`generate:roadmap`, `lint`, `format`, `typecheck`, `validate:content`, `test`, `build`) — clean, no errors. Removed the stale scaffolded `L1-summary.md`/`L3-deep-dive.md` stubs.
- Chrome extension bridge stayed connected — verified via real browser interaction: 3 exercise panels populated, 1 mermaid diagram present, switched to the L2 tab (the demo is scoped to level 2 since the relationship it's built on is introduced there) and confirmed the interactive demo's slider at the correct default (2 → 1 conflict/2 boundaries), dragged to 10 and confirmed the output updated live to the exact formula values (45 conflicts / 10 boundaries), and clicked a quiz answer to confirm grading + explanation rendered and radios locked afterward.
- Flipped `product-domain/domain-driven-design-basics` to `done` in `ROADMAP.md`. 43 total `done` units now. Committed locally (not pushed).
- Next: continuing the standing autonomous loop — auto-picking the next unit by lowest done/total ratio across tracks.

## 2026-08-17 — New unit: software-design/naming

- Auto-picked `software-design/naming` — a 2-way tie at the lowest done/total ratio (`software-design`, `testing-quality`, both 2/13), broken alphabetically, then lowest-numbered `planned` unit — "How do I name and structure things so the code explains itself?".
- Technical track (per CLAUDE.md rule 9) — grounded in real, runnable, verified JS rather than a worked business dialogue, unlike the two prior units. Built around one incident: Billing adds `order.flag` in 2019 meaning "invoice overdue"; Notifications reuses the same generic field in 2022 assuming it means "customer muted notifications," silently skipping reminders for exactly the overdue customers who need them, undetected for months.
- L1 frames a name as a promise about meaning, and states the core reframe that the buggy function's own logic was correct — the entire bug was in what an ambiguous name let a second reader assume. L2 gives a mermaid flowchart on intention-revealing names, a worked contrast on why negation compounds ambiguity (not negation itself), the extraction-over-comments argument (a function's name can't drift out of sync with itself the way a comment can), and the generalizable "blast radius" lesson — naming care should scale with how far a name travels, not be uniform everywhere. L3 reproduces the exact bug and fix in verified code (`shouldSkipNotification` buggy vs. fixed, `isOverdueAndUnmuted` as a positive-phrased combined predicate, and a full extraction refactor of `calculateOrderTotal` from a commented monolith into three named steps, all confirmed to compute the identical result, 194.4), plus an extension on when splitting a true either/or state into loose booleans is itself the wrong fix (an enumerated field is).
- All L3 code was verified twice: once standalone in Node before writing (`/tmp/verify_naming.mjs`), and again by extracting and running every code-exercise `solution`+`tests` pair directly out of the committed `exercises.json` (`/tmp/verify_naming_ex.mjs`) — all 14 code items pass. Caught and fixed a real bug of my own during this process: two test expressions used an invalid `toBeCloseTo` matcher not present in the sandboxed Worker's mini API (only `toBe`/`toEqual`/`toBeTruthy`/`toThrow` are available) — replaced with exact `toBe` values after confirming via Node that both were exact, not floating-point-approximate. Also caught the `interactives.json` `compute` string referencing bare `monthsUndetected`/`overdueCustomersPerMonth` instead of `params.monthsUndetected`/`params.overdueCustomersPerMonth` before it ever reached the browser.
- Wrote 40 exercises (13/13/14, 18 pools) — quiz for L1/L2 conceptual pools, code for L3 using the verified functions above (2-3 variants each).
- Built `interactives.json`: reproduces the scenario's own "not discovered for months" framing directly — total missed reminders as a function of months undetected × overdue customers per month, defaulting to 3 months / 150 per month = 450, growing linearly as either slider moves.
- Ran the full guardrail suite (`generate:roadmap`, `lint`, `format`, `typecheck`, `validate:content`, `test`, `build`) — clean, no errors. Removed the stale scaffolded `L1-summary.md`/`L3-deep-dive.md` stubs.
- Chrome extension bridge stayed connected — full browser verification: 3 exercise panels populated (L3 showing genuine code-exercise pools this time), 1 mermaid diagram, dragged the interactive demo's slider to its max (12 months) and confirmed the output updated live to the exact formula value (1800); ran a code exercise with a deliberately broken implementation and confirmed it failed with a clear assertion message plus the solution/explanation reveal, then ran the correct solution and confirmed it passed — the sandboxed Worker execution path verified end-to-end, not just via Node.
- Flipped `software-design/naming` to `done` in `ROADMAP.md`. 44 total `done` units now. Committed locally (not pushed).
- Next: continuing the standing autonomous loop — auto-picking the next unit by lowest done/total ratio across tracks.

## 2026-08-17 — New unit: testing-quality/assertions-matter

- Auto-picked `testing-quality/assertions-matter` — the clear lowest done/total ratio (2/13) — "How do I write a test that actually catches bugs, not just exercises code?".
- Technical track. Built around one incident: a $100-order discount ships with a boundary bug (`subtotal > 100` instead of `>= 100`), and the existing test — `typeof result === 'number'` — passes on both the buggy and correct code, because it never checks which number, only that a number came back.
- L1 introduces the core distinction (code ran vs. code did the right thing), defines a tautological test precisely, and states the sharpest practical check: would this assertion fail if the implementation had a plausible bug? L2 gives the manual mutation-testing thought experiment as a flowchart, shows exactly why $150 can't distinguish the buggy from the fixed implementation while $100 can, and draws the behavior-vs-implementation-detail distinction (a table showing tautological and implementation-coupled assertions fail in opposite directions — one misses real bugs, the other breaks on harmless refactors). L3 reproduces the bug and both assertion styles in real code, adds a `testCatchesBug(goodFn, buggyFn, testInput)` helper that encodes the mutation-testing idea directly as executable code, and extends to a second independent threshold (free shipping at $50) to show why one boundary passing says nothing about another.
- All L3 code was verified twice: standalone in Node before writing (`/tmp/verify_assertions.mjs`), and again by extracting and running every code-exercise `solution`+`tests` pair directly from the committed `exercises.json` — all 14 code items pass.
- Wrote 40 exercises (13/13/14, 17 pools) — quiz for L1/L2, code for L3 using `calculateOrderTotal`, `testCatchesBug`, `calculateOrderTotalWithShipping`, `strongAssertionPasses`, and `weakAssertionPasses` (reproducing the original tautological assertion as its own testable function — a deliberate design choice, since the unit's own exercise format already uses `expect()`-style assertions, so the exercises double as worked examples of the exact concept being taught). Caught and fixed the same recurring `amber-390` CSS typo during pool-consistency validation.
- Built `interactives.json`: required boundary-exact test cases as a direct, linear function of the number of independent thresholds in a function — reproducing L3's own two-threshold extension at the default (2 thresholds → 2 required boundary tests).
- Ran the full guardrail suite (`generate:roadmap`, `lint`, `format`, `typecheck`, `validate:content`, `test`, `build`) — clean, no errors. Removed the stale scaffolded `L1-summary.md`/`L3-deep-dive.md` stubs.
- Chrome extension bridge stayed connected — full browser verification: 3 exercise panels populated, 1 mermaid diagram, dragged the interactive demo's slider to 6 and confirmed the output updated live to the exact linear value; ran a code exercise with a deliberately broken implementation (`return true` unconditionally) and confirmed it failed with a precise assertion message, then ran the correct solution and confirmed it passed — verified end-to-end through the actual sandboxed Worker execution path.
- Flipped `testing-quality/assertions-matter` to `done` in `ROADMAP.md`. 45 total `done` units now. Committed locally (not pushed).
- Next: continuing the standing autonomous loop — auto-picking the next unit by lowest done/total ratio across tracks.

## 2026-08-17 — New unit: infra-delivery/ci-cd-pipeline-anatomy

- Auto-picked `infra-delivery/ci-cd-pipeline-anatomy` — a 2-way tie at the lowest done/total ratio (`infra-delivery`, `logic`, both 2/12), broken alphabetically — "How does code get from a commit to running in production?".
- Technical track. Built around one incident: a `continue-on-error: true` flag added to a pipeline's test step to unblock a release around a specific flaky test, never removed, and weeks later a genuinely broken commit fails the test suite but the pipeline reports green and deploys to production anyway.
- L1 introduces stages as each catching a distinct class of problem, defines continue-on-error as a deliberate temporary escape hatch (not a permanent setting), draws the CI-vs-CD distinction (judging a change vs. getting an already-judged-good change to production), and introduces artifact promotion. L2 gives a mermaid flowchart on "finished" vs. "finished because every gate passed," explicitly distinguishes legitimate temporary use of the override (a narrowly-scoped, ticketed, low-stakes flaky check) from what actually happened (applied to the real correctness gate, no ticket, no removal plan), covers build-once/promote-unchanged and why rebuilding per-environment reopens drift risk, and states rollback must be designed and rehearsed before an incident, not decided during one. L3 reproduces the exact incident as both real GitHub Actions YAML and an equivalent `runPipeline(steps)` JS model (same function, same failure, completely different outcome based on one boolean flag), a `promoteThroughEnvironments()` function modeling build-once/promote-unchanged, and an extension arguing pipeline config deserves the same review rigor as application code given its blast radius.
- All L3 code was verified twice: standalone in Node before writing (`/tmp/verify_cicd.mjs`), and again by extracting and running every code-exercise `solution`+`tests` pair directly from the committed `exercises.json` — all 14 code items pass.
- Wrote 40 exercises (13/13/14, 16 pools) — quiz for L1/L2, code for L3 using `runPipeline` (both the buggy-reproduction and general-behavior variants) and `promoteThroughEnvironments`. Caught and fixed the same recurring `amber-390` CSS typo, and added 6 more L3 items after the first pass landed short (10, then 12, of 14).
- Built `interactives.json`: users exposed to a broken deploy as a direct function of rollback time × request rate, tied to L2's "rollback must be rehearsed in advance" point — defaulting to a fast, rehearsed 5-minute rollback (250 users) and showing the cost of an unrehearsed, much slower rollback (3,000 users at 60 minutes) at the same request rate.
- Ran the full guardrail suite (`generate:roadmap`, `lint`, `format`, `typecheck`, `validate:content`, `test`, `build`) — clean, no errors. Removed the stale scaffolded `L1-summary.md`/`L3-deep-dive.md` stubs.
- Chrome extension bridge stayed connected — full browser verification: 3 exercise panels populated, 1 mermaid diagram, dragged the interactive demo's slider to its max (60 minutes) and confirmed the output updated live to the exact linear value (3,000); ran a code exercise with a deliberately broken implementation (always returns `deployed: true` regardless of input) and confirmed it failed with a precise diff, then ran the correct solution and confirmed it passed — verified end-to-end through the actual sandboxed Worker execution path.
- Flipped `infra-delivery/ci-cd-pipeline-anatomy` to `done` in `ROADMAP.md`. 46 total `done` units now. Committed locally (not pushed).
- Next: continuing the standing autonomous loop — auto-picking the next unit by lowest done/total ratio across tracks.

## 2026-08-17 — New unit: logic/problem-decomposition

- Auto-picked `logic/problem-decomposition` — the clear lowest done/total ratio (2/12) — "How do I break a vague problem into solvable pieces?".
- Technical track. Built around one scenario: "make checkout faster" (4.2s total, vague) decomposed by measurement into four stages — network 300ms, server 800ms, database query 2,500ms, rendering 600ms — revealing the DB query as 59% of the total and the only piece worth attention.
- L1 introduces decomposition as genuinely separate, independently-attackable sub-problems (not just rewording a vague sentence), and the exhaustive/non-overlapping properties. L2 gives a mermaid flowchart formalizing both properties as checkable (not just intuitive), a worked false-decomposition example (server-processing time secretly including DB query time, so "fixing" the DB query would inflate both numbers), a third property (independence — can a piece be worked on without the others, illustrated by a badly-drawn "server-side time" vs. "end-to-end time" split where one is literally made of the other), and the point that decomposition's real value is revealing priority. L3 reproduces the exact numbers in verified code: `decomposeTiming`, `identifyBottleneck`, `isExhaustiveDecomposition`, and `hasOverlap` (the last two directly encoding the two failure-mode checks as executable, testable logic), plus an extension on non-additive decompositions (onboarding confusion, where pieces need to be defined before they're even measurable).
- All L3 code was verified twice: standalone in Node before writing (`/tmp/verify_decomp.mjs`), and again by extracting and running every code-exercise `solution`+`tests` pair directly from the committed `exercises.json` — all 14 code items pass.
- Wrote 40 exercises (13/13/14, 16 pools) — quiz for L1/L2, code for L3 using the four verified functions above.
- Built `interactives.json`: reproduces the unit's own scenario directly — dragging the database query time (the identified bottleneck) shows both the new total and the query's shrinking share of it live, demonstrating diminishing returns as the dominant piece gets optimized. Verified the default (2,500ms) reproduces the exact incident numbers (4,200ms total, 59.52% share).
- **Caught a real MDX build failure**: L1's `<Scenario label="\"Make checkout faster\" — where do you even start?">` used backslash-escaped double quotes inside a JSX attribute, which isn't valid JSX/MDX syntax (`\"` inside a `"`-delimited attribute doesn't parse the way it does in JS string literals) — `bun run build` failed with `Unexpected character after` at that exact line. Fixed by switching the attribute to single-quoted delimiters (`label='"Make checkout faster" — ...'`), rebuilt clean, and re-ran the full guardrail suite from `lint` onward to confirm nothing else was affected.
- Ran the full guardrail suite (`generate:roadmap`, `lint`, `format`, `typecheck`, `validate:content`, `test`, `build`) — clean after the MDX fix. Removed the stale scaffolded `L1-summary.md`/`L3-deep-dive.md` stubs.
- Chrome extension bridge stayed connected — full browser verification: 3 exercise panels populated, 1 mermaid diagram, dragged the interactive demo's slider to 500ms and confirmed both outputs (total, share%) updated live to the exact computed values; ran a code exercise with a deliberately broken implementation (`return true` unconditionally) and confirmed one of its two tests correctly failed, then ran the correct solution and confirmed both tests passed — verified end-to-end through the actual sandboxed Worker execution path.
- Flipped `logic/problem-decomposition` to `done` in `ROADMAP.md`. 47 total `done` units now. Committed locally (not pushed).
- Next: continuing the standing autonomous loop — auto-picking the next unit by lowest done/total ratio across tracks.

## 2026-08-17 — New unit: systems/sockets

- Auto-picked `systems/sockets` — a 2-way tie at the lowest done/total ratio (`systems`, `web`, both 3/17), broken alphabetically — "How do two programs talk to each other?".
- Technical track. Built around one incident: a chat server that assumes one `data` event equals one complete JSON message — reliably true on localhost, but false once a real network splits a longer message across two `data` events, producing a downstream parse failure with no crash at the socket layer itself.
- L1 introduces ports (program-level addressing beneath the machine-level IP address), states TCP's core guarantee precisely (ordered, reliable bytes — not messages), and frames the practical skill as verifying the one-event-one-message assumption rather than trusting it. L2 gives a mermaid sequence diagram of the TCP three-way handshake (contrasted with UDP's lack of one), two concrete framing schemes (newline-delimited vs. length-prefixed) with their tradeoffs, and explains precisely why localhost testing systematically avoids the conditions that trigger this bug. L3 reproduces the exact incident in verified code (`naiveHandler` showing the bug, `createLineFramer` as the fix — buffering across `data` events with a `while` loop specifically to handle both split messages and multiple-messages-in-one-chunk), plus an extension on when length-prefixed framing is the more robust choice over newline-delimited.
- All L3 code was verified twice: standalone in Node before writing (`/tmp/verify_sockets.mjs`), and again by extracting and running every code-exercise `solution`+`tests` pair directly from the committed `exercises.json` — all 14 code items pass, including edge cases (empty chunks, empty messages between consecutive delimiters, independent framer instances not sharing buffer state).
- Wrote 40 exercises (13/13/14, 15 pools) — quiz for L1/L2, code for L3 using `naiveHandler` and `createLineFramer` (basic, multi-message, and edge-case variants).
- Built `interactives.json`: an explicitly-labeled illustrative model (not a precise network simulation) of how message size relates to the minimum number of TCP-buffer-sized fragments a message could split across — defaulting to 1000 bytes (1 fragment) and showing the count grow as size increases toward the conditions that actually triggered the incident.
- **Caught a real MDX build failure during the previous unit's session and applied the lesson here**: avoided backslash-escaped quotes in JSX attribute strings this time; used a template-literal-style JSX expression (`{"{\"...\"}"}`) for embedding a JSON snippet with quotes inside a `Fragment` body instead, which is valid MDX/JSX. Build passed clean on the first attempt.
- Ran the full guardrail suite (`generate:roadmap`, `lint`, `format`, `typecheck`, `validate:content`, `test`, `build`) — clean, no errors. Removed the stale scaffolded `L1-summary.md`/`L3-deep-dive.md` stubs.
- Chrome extension bridge stayed connected — full browser verification: 3 exercise panels populated, 1 mermaid diagram, dragged the interactive demo's slider to 5000 bytes and confirmed the fragment count updated live to the exact `Math.ceil` value (4); ran a code exercise with a deliberately broken implementation (no buffering at all) and confirmed it failed, then ran the correct solution and confirmed it passed — verified end-to-end through the actual sandboxed Worker execution path.
- Flipped `systems/sockets` to `done` in `ROADMAP.md`. 48 total `done` units now. Committed locally (not pushed).
- Next: continuing the standing autonomous loop — auto-picking the next unit by lowest done/total ratio across tracks.

## 2026-08-18 — New unit: web/dom-event-model

- Auto-picked `web/dom-event-model` — `web` tied for the lowest done/total ratio — "How does a page react to user input without reloading?".
- Technical track. Built around one incident: a delete button with a nested icon span, where a naive click handler checks `event.target.className` directly and silently does nothing when the click lands on the icon instead of the button.
- L1 introduces the scenario and predicts whether the naive check fails only on the icon or in both cases. L2 covers the bubbling phase (mermaid `flowchart BT` from icon up through document), `event.target` vs. `event.currentTarget`, `closest()`'s ancestor-walking semantics as the fix, and why a delegated listener also covers elements added after the listener was attached (comparison table: per-element listeners vs. delegation). L3 models DOM nodes as plain JS objects (`{tagName, className, parent}`, no browser needed) with `makeNode`, `matchesClass`, `closest`, `naiveDelegatedHandler` (reproduces the bug), and `createDelegatedClickHandler` (the fix), plus an extension on `stopPropagation()` risk and a failure-modes table.
- All L3 code was verified twice: standalone in `/tmp/verify_dom.mjs` before writing, and again by extracting and running every code-exercise `solution`+`tests` pair directly from the committed `exercises.json` — all 14 code items pass.
- Wrote 40 exercises (13/13/14, 13 pools) — quiz for L1/L2, code for L3 using the verified functions above. Caught and fixed one invalid test-syntax mistake during authoring: `expect(caught).not.toBe(buttonB)` (the sandbox's `expect()` mini-API has no `.not` chaining) rewritten as `expect(caught === buttonB).toBe(false)`.
- Built `interactives.json`: a `delegation-listener-count` demo (level 2) — dragging a todo-list-size slider (1-100) compares listeners needed for a per-element approach (grows 1:1) against a delegated approach (flat at 1).
- L1 and L3 both landed short of the 13/14 target on first pass (12 and 11 respectively) — fixed by adding items to existing pools, reusing each pool's exact `reference`/`learnMore` HTML, then re-running the Python pool-consistency check until it reported `all consistent` at the correct split.
- Ran the full guardrail suite (`generate:roadmap`, `lint`, `format`, `typecheck`, `validate:content`, `test`, `build`) — clean. Removed the stale scaffolded `L1-summary.md`/`L3-deep-dive.md` stubs (kept the `.mdx` versions).
- Chrome extension bridge connected — full browser verification via `astro preview --background`: 3 exercise panels populated with `shownIds`, 1 mermaid diagram, 1 interactive demo; dragged the interactive demo's slider to 50 and confirmed both outputs (perElementListeners, delegatedListeners) updated live to the exact expected values (50 and 1); ran the L1 quiz on `event.target` with a wrong answer (confirmed "Not quite" + explanation, then confirmed the radios lock/disable after grading — the quiz-integrity behavior held), reloaded and answered a fresh pool correctly (confirmed "Correct" + explanation); ran the `closest()` code exercise with a deliberately broken implementation (`return null` unconditionally — confirmed a red/fail marker), then the correct implementation (confirmed an emerald/pass marker) — verified end-to-end through the actual sandboxed Worker execution path.
- Flipped `web/dom-event-model` to `done` in `ROADMAP.md`. 49 total `done` units now. Committed locally (not pushed).
- Next: continuing the standing autonomous loop — auto-picking the next unit by lowest done/total ratio across tracks.

## 2026-08-18 — New unit: git-teamwork/merge

- Auto-picked `git-teamwork/merge` — `git-teamwork` was the clear lowest done/total ratio (3/16) — "How do two people edit the same file without destroying each other's work? (merge, conflicts)".
- Technical track. Built around one scenario: Alex and Sam branch off the same `cart.js` commit — Alex adds a null-check guard, Sam changes the `TAX_RATE` constant. When their lines don't overlap, git auto-merges silently; when both independently edit the exact same line to different values, it produces a real conflict — and when they land on the _same_ new value independently, it still doesn't conflict.
- L1 introduces merge base, three-way merge, auto-merge vs. conflict, conflict markers, and fast-forward as key terms tied to the scenario. L2 gives a mermaid flowchart of the three-way comparison (base→ours, base→theirs→merged), a pseudocode decision procedure per base line, a table contrasting auto-merge/conflict/fast-forward outcomes, and the mechanical (not superstitious) reason short-lived branches reduce conflict risk. L3 implements a real, verified three-way merge algorithm — `diffLines` (LCS-based), `buildChangeMap`, `threeWayMerge` — and runs it against Alex and Sam's actual file for all three outcomes (auto-merge, real conflict, identical-edit no-conflict), plus an extension on merging three or more branches at once.
- All L3 code was verified twice: standalone in `/tmp/verify_merge.mjs` and `/tmp/verify_l3.mjs` before writing (including catching and fixing two real bugs in the algorithm during verification — a spurious conflict marker placeholder on a base line replaced by both sides, and a false-positive conflict flag when both sides made the identical edit), and again by extracting and running every code-exercise `solution`+`tests` pair directly from the committed `exercises.json` — all 18 test assertions across 14 code items pass.
- Wrote 40 exercises (13/13/14, 17 pools) — quiz for L1/L2, code for L3 using `diffLines`, `threeWayMerge`, and `buildChangeMap`.
- Built `interactives.json`: an explicitly-labeled illustrative model (assumes changed lines are scattered randomly and independently, not a precise simulation) of how expected conflicting lines and total touched lines scale as each branch's changed-line percentage grows — verified the default (300 lines, 10%/10%) and a dragged 50% value both match the exact hand-computed formula.
- Ran the full guardrail suite (`generate:roadmap`, `lint`, `format`, `typecheck` — 0 errors/0 warnings/69 pre-existing hints, `validate:content` — 50 units checked, `test` — 9/9 pass, `build` — 68 pages) — clean throughout, no fixes needed this time.
- Chrome extension bridge connected — full browser verification via `astro preview --background`: 3 exercise panels populated with `shownIds`, 1 mermaid diagram, 1 interactive demo; dragged the interactive demo's `theirsChangedPercent` slider to 50 and confirmed both outputs (expectedConflicts=15, touchedUnion=165) matched the exact formula; ran an L1 quiz with a wrong answer (confirmed "Not quite" + explanation), reloaded and answered a freshly-shuffled pool correctly (confirmed "Correct" + explanation); ran the `diffLines` code exercise with a deliberately broken implementation (`return []` unconditionally — confirmed a red/fail marker), then the correct implementation (confirmed an emerald/pass marker) — verified end-to-end through the actual sandboxed Worker execution path.
- Flipped `git-teamwork/merge` to `done` in `ROADMAP.md`. 50 total `done` units now. Committed locally (not pushed).
- Next: continuing the standing autonomous loop — auto-picking the next unit by lowest done/total ratio across tracks.

## 2026-08-18 — New unit: applied-math/asymptotic-analysis

- Auto-picked `applied-math/asymptotic-analysis` — a 2-way tie at the lowest done/total ratio (`applied-math`, `people-management`, both 3/15), broken alphabetically — "How do I reason precisely about growth as input size increases? (asymptotic analysis, Big-O put on rigorous footing)".
- Technical track. Built around one scenario: a candidate benchmarks their algorithm at n = 5 and n = 10, sees the runtime roughly double, and ships it confident it's linear. In production n reaches 100,000+ and the service falls over — the real function was `T(n) = 3n² + 100n`, whose `100n` term still dominated the `3n²` term at the small sizes tested, but whose `3n²` term takes over completely at scale.
- L1 introduces growth rate, dominant term, and the formal O/Ω/Θ vocabulary tied to the scenario. L2 gives the precise formal definitions (`T(n) ≤ c·f(n)` for `n ≥ n₀`), a table contrasting O/Ω/Θ, a mermaid `xychart-beta` showing the crossover point (n ≈ 33) where `3n²` overtakes `100n`, and a table of the doubling ratio drifting from ≈2.3 (n=5→10) to ≈4.0 (n=100,000→200,000) for the exact same function. L3 builds a verified operation-counting toolkit — `countLinearScan`, `countNestedLoop`, `countMergeSortComparisons` (a real recursive merge sort), `estimateGrowthExponent` (doubling-ratio → log2 exponent estimate), `classifyGrowth` — and reproduces the misleading benchmark with real numbers, plus an extension comparing `3n² + 100n` against `3n² − 100n` to show the direction of small-n bias isn't fixed.
- All L3 code was verified twice: standalone in `/tmp/verify_asymp.mjs` before writing, and again by extracting and running every code-exercise `solution`+`tests` pair directly from the committed `exercises.json` — all 22 test assertions across 14 code items pass. Caught and fixed two real bugs during exercise verification: a `toBe(2)` floating-point-equality test that failed at 1.9997... (fixed to a tolerance check), and a pool variant whose test referenced `estimateGrowthExponent` without that helper being part of its own `solution`/`starterCode` (fixed by giving that variant its own combined solution).
- Wrote 40 exercises (13/13/14, 19 pools) — quiz for L1/L2 (including a 3-variant pool on how a subtracted lower-order term biases small-n measurements in the opposite direction from an added one), code for L3 using the verified functions above.
- Built `interactives.json`: a `doubling-ratio-explorer` demo (level 2) — dragging the measurement point n from 5 to 100,000 shows the doubling ratio for `3n² + 100n` climbing live from 2.26 toward its true asymptotic value of 4.
- Ran the full guardrail suite (`generate:roadmap`, `lint`, `format`, `typecheck` — 0 errors/0 warnings/69 pre-existing hints, `validate:content` — 51 units checked, `test` — 9/9 pass, `build` — 69 pages) — clean.
- Chrome extension bridge connected — full browser verification via `astro preview --background`: 3 exercise panels populated with `shownIds`, 1 mermaid diagram, 1 interactive demo; dragged the interactive demo's slider to 100,000 and confirmed the ratio updated live to 4; ran an L1 quiz with a wrong answer (confirmed "Not quite" + explanation), reloaded and answered a freshly-shuffled pool correctly (confirmed "Correct" + explanation); ran the `classifyGrowth` code exercise with a deliberately broken implementation (always returns the same string — confirmed both tests failed with red markers), then the correct implementation (confirmed both tests passed with emerald markers) — verified end-to-end through the actual sandboxed Worker execution path.
- Flipped `applied-math/asymptotic-analysis` to `done` in `ROADMAP.md`. 51 total `done` units now. Committed locally (not pushed).
- Next: continuing the standing autonomous loop — auto-picking the next unit by lowest done/total ratio across tracks.

## 2026-08-18 — New unit: people-management/sbi-framework

- Auto-picked `people-management/sbi-framework` — the clear lowest done/total ratio (3/15) — "How do I give feedback that changes behavior instead of just being heard? (SBI framework, feedback timing, difficult conversations)".
- Non-technical track. Built around one scenario: a manager tells a report "you need to be more proactive" during a quarterly review, three weeks after the actual incident — nothing changes because the feedback names a trait, not a specific action, and has lost its concrete context by the time it's delivered.
- L1 introduces SBI (Situation-Behavior-Impact), trait-based feedback, feedback timing, and difficult conversations as key terms. L2 gives a mermaid flowchart of the SBI narrowing (situation → behavior → impact → next behavior), a comparison table of trait-based vs. SBI framing, why delay degrades the Situation back into vague pattern language, and how the structure scales to pattern-level "difficult conversations" without collapsing into "you always...". L3 rewrites one real incident — an engineer, Priya, repeatedly not repeating privately-flagged blockers in standup — three ways (trait-based/delayed, timely-but-still-trait-based, and full SBI next-day), a comparison table of what changed, the pattern-level version citing three specific dated instances, and an extension on how the structure adapts when impact is trivial or the audience shifts from a 1:1 to the whole team.
- Per CLAUDE.md rule 9 (non-technical tracks ground L3 in worked scenarios, not code), all three levels use prose, quotes, and tables — no fenced pseudocode.
- Wrote 40 exercises (13/13/14, 17 pools), quiz-only (no code type, consistent with the non-technical-track convention) — including a domain-general variant (a friend's feedback on a home-cooked meal) applying the same SBI-style shift outside a workplace context, per rule 4c.
- Built `interactives.json`: an explicitly-labeled illustrative model (not a measured curve) of estimated feedback specificity decaying exponentially with days since the event — verified the default (1 day → 87%) and a dragged 14-day value (→14%) both match `100·e^(-days/7)` exactly.
- Ran the full guardrail suite (`generate:roadmap`, `lint`, `format`, `typecheck` — 0 errors/0 warnings/69 pre-existing hints, `validate:content` — 52 units checked, `test` — 9/9 pass, `build` — 70 pages) — clean.
- Chrome extension bridge connected — full browser verification via `astro preview --background`: 3 exercise panels populated with `shownIds`, 1 mermaid diagram, 1 interactive demo; dragged the interactive demo's slider from the default (day 1, 87%) to day 14 (confirmed live update to 14%); answered one quiz pool wrong (confirmed "Not quite" + explanation), reloaded, and answered a freshly-shuffled pool correctly (confirmed "Correct" + explanation). No code exercises to verify in this unit (quiz-only, per the non-technical-track convention).
- Flipped `people-management/sbi-framework` to `done` in `ROADMAP.md`. 52 total `done` units now. Committed locally (not pushed).
- Next: continuing the standing autonomous loop — auto-picking the next unit by lowest done/total ratio across tracks.

## 2026-08-18 — New unit: business-communication/pushback-frameworks

- Auto-picked `business-communication/pushback-frameworks` — the clear lowest done/total ratio (3/14) — "How do I say no to a request without burning the relationship? (pushback frameworks)".
- Non-technical track. Built around one scenario: a PM asks an engineer to squeeze an urgent feature into an already-full sprint by Friday — a flat "no" hides the real reasoning, a silent "yes" hides the real cost until it surfaces later as a broken commitment.
- L1 introduces trade-off pushback, silent overcommitment, concrete constraints, and informed trade-offs as key terms. L2 gives a mermaid flowchart of the pushback structure (request → name constraint → offer options → requester decides), why a flat "no" and a silent "yes" both fail (one hides reasoning, one hides cost), and a comparison table of all three responses. L3 rewrites one real request — Dana asking Marcus for a Friday export feature on top of two existing commitments — three ways (flat refusal, silent overcommitment, trade-off pushback), a comparison table of what changed, an extension on framing the same structure when the requester has much more positional power (a skip-level exec), and an extension on the case where no real trade-off exists at all.
- Per CLAUDE.md rule 9, all three levels use prose, quotes, and tables — no fenced pseudocode.
- Wrote 40 exercises (13/13/14, 17 pools), quiz-only — including two domain-general variants (lending a car, a sibling's visit) applying the same trade-off-surfacing shift outside a workplace context, per rule 4c.
- Built `interactives.json`: a `sprint-overflow` demo (level 2) back-derived directly from the worked scenario's own numbers (existing load = 4 days from the billing fix + migration) — dragging the new request's size and the sprint's total capacity shows exactly when a real trade-off appears vs. when the request can genuinely be absorbed with none. Verified both a zero-overflow case (request size 1, capacity 5) and a real-overflow case (request size 5, capacity 5 → overflow 4) match the formula exactly.
- Ran the full guardrail suite (`generate:roadmap`, `lint`, `format`, `typecheck` — 0 errors/0 warnings/69 pre-existing hints, `validate:content` — 53 units checked, `test` — 9/9 pass, `build` — 71 pages) — clean.
- Chrome extension bridge connected — full browser verification via `astro preview --background`: 3 exercise panels populated with `shownIds`, 1 mermaid diagram, 1 interactive demo; dragged the interactive demo's request-size slider to 1 (confirmed overflow=0) and to 5 (confirmed overflow=4); answered one quiz pool wrong (confirmed "Not quite" + explanation), reloaded, and answered a freshly-shuffled pool correctly (confirmed "Correct" + explanation). No code exercises to verify (quiz-only, non-technical track).
- Flipped `business-communication/pushback-frameworks` to `done` in `ROADMAP.md`. 53 total `done` units now. Committed locally (not pushed).
- Next: continuing the standing autonomous loop — auto-picking the next unit by lowest done/total ratio across tracks.

## 2026-08-18 — New unit: corporate-politics/favor-economy

- Auto-picked `corporate-politics/favor-economy` — the clear lowest done/total ratio (3/14) — "How do I bank goodwill before I need to spend it? (the favor economy, relationship capital)". This is legitimate influence-building ("white" politics per the track's framing), not the gray/black end of the spectrum.
- Non-technical track. Built around one scenario: two engineers make an identical urgent ask of another team — one who's quietly helped that team for months gets an immediate yes, one with no prior relationship gets deprioritized.
- L1 introduces relationship capital, the favor economy, banking goodwill, and transactional keeping-score as key terms. L2 gives a mermaid flowchart of the two paths (genuine help → trust → willingness vs. explicit score-keeping → feeling tracked → reduced willingness), a table of genuine deposits vs. ploys, why timing between deposit and withdrawal matters, and the cold-ask problem. L3 rewrites the two-engineer scenario in full (Wren vs. Priya, nearly identical wording, isolating relationship capital as the actual variable), adds a third version where Wren explicitly cites the past favors (and it backfires), works through Priya's two legitimate cold-ask strategies, and an extension on whether Wren's help being public vs. quiet changes how it lands with two separate audiences (the recipient vs. everyone else watching).
- Per CLAUDE.md rule 9, all three levels use prose, quotes, and tables — no fenced pseudocode.
- Wrote 40 exercises (13/13/14, 16 pools), quiz-only — including domain-general variants (a study group sharing notes, a neighbor watering plants) applying the same mechanism outside a workplace context, per rule 4c.
- Built `interactives.json`: a `deposits-vs-willingness` demo (level 2) — an explicitly-labeled illustrative saturating-curve model of estimated willingness to help back as a function of accumulated genuine deposits. Verified the default (3 deposits → 45%) and a dragged 20-deposit value (→98%) both match `100·(1-e^(-n/5))` exactly.
- Ran the full guardrail suite (`generate:roadmap`, `lint`, `format`, `typecheck` — 0 errors/0 warnings/69 pre-existing hints, `validate:content` — 54 units checked, `test` — 9/9 pass, `build` — 72 pages) — clean.
- Chrome extension bridge connected — full browser verification via `astro preview --background`: 3 exercise panels populated with `shownIds`, 1 mermaid diagram, 1 interactive demo; dragged the interactive demo's slider to 20 and confirmed live update to 98%; answered one quiz pool wrong (confirmed "Not quite" + explanation), reloaded, and answered a freshly-shuffled pool correctly (confirmed "Correct" + explanation). No code exercises to verify (quiz-only, non-technical track).
- Flipped `corporate-politics/favor-economy` to `done` in `ROADMAP.md`. 54 total `done` units now. Committed locally (not pushed).
- Next: continuing the standing autonomous loop — auto-picking the next unit by lowest done/total ratio across tracks.

## 2026-08-18 — New unit: career-craft/structured-interviewing

- Auto-picked `career-craft/structured-interviewing` — the clear lowest done/total ratio (3/13) — "How do I know if someone should get the job? (structured interviewing, avoiding bias)".
- Non-technical track. Built around one scenario: an interviewer forms a gut impression in the first 90 seconds, and the written feedback 44 minutes later matches that impression almost exactly.
- L1 introduces the halo effect, unstructured vs. structured interviews, and scoring rubrics as key terms. L2 gives a mermaid flowchart of the two paths (unstructured: impression → shaped questions → matching score; structured: fixed questions → rubric → score reflects the answer), a concrete rubric-anchor table (1/3/5 with specific content requirements, not vague labels), why fixed questions matter as much as the rubric itself, and what structure does and doesn't eliminate (personal impressions can still exist, just can't silently drive the score). L3 rewrites one hiring decision two ways for the same two candidates and the same debugging question — unstructured (Candidate A's generic-but-confident answer rated favorably, Candidate B's more rigorous-but-hesitant answer rated poorly) vs. structured (scored 3 and 5 respectively against the L2 rubric — the ranking reverses) — plus an extension on what happens when the rubric itself is badly designed.
- Per CLAUDE.md rule 9, all three levels use prose, quotes, and tables — no fenced pseudocode.
- Wrote 40 exercises (13/13/14, 16 pools), quiz-only — including a domain-general variant (a parent deciding report-card criteria in advance) applying the same "decide criteria before seeing the evidence" principle outside hiring, per rule 4c.
- Built `interactives.json`: a `gut-feel-vs-rubric-weight` demo (level 3) back-derived directly from the worked scenario's own numbers (Candidate A: rubric 3, impression 5; Candidate B: rubric 5, impression 3) — a single `scoreGap` line (not two complementary lines, per CLAUDE.md's clarity guidance) showing the ranking flip as gut-feel weighting increases from 0% to 100%. Verified 0% → gap +2 (matches the structured outcome) and 100% → gap −2 (matches the unstructured outcome) exactly.
- Ran the full guardrail suite (`generate:roadmap`, `lint`, `format`, `typecheck` — 0 errors/0 warnings/69 pre-existing hints, `validate:content` — 55 units checked, `test` — 9/9 pass, `build` — 73 pages) — clean.
- Chrome extension bridge connected — full browser verification via `astro preview --background`: 3 exercise panels populated with `shownIds`, 1 mermaid diagram, 1 interactive demo (level 3, required clicking the L3 tab first since non-default panels are hidden); dragged the demo's slider from 0% to 100% and confirmed the score gap flips from +2 to −2 exactly; answered one quiz pool wrong (confirmed "Not quite" + explanation), reloaded, and answered a freshly-shuffled pool correctly (confirmed "Correct" + explanation). No code exercises to verify (quiz-only, non-technical track).
- Flipped `career-craft/structured-interviewing` to `done` in `ROADMAP.md`. 55 total `done` units now. Committed locally (not pushed).
- Next: continuing the standing autonomous loop — auto-picking the next unit by lowest done/total ratio across tracks.

## 2026-08-18 — New unit: learning-craft/feynman-technique

- Auto-picked `learning-craft/feynman-technique` — the lowest-numbered `planned` unit in `learning-craft` (the lowest done/total ratio track at the time) — "How do I know when I've actually understood something vs. just recognized it? (the Feynman technique, teach-back, the illusion of competence)".
- Domain-general track. Built around one scenario: an article on why a moving bicycle stays upright makes complete sense while reading, but the reader stalls two sentences into explaining it to a curious 10-year-old.
- L1 introduces the illusion of competence, the Feynman technique, teach-back, and plain language as key terms. L2 gives a mermaid flowchart of the four-step loop (explain → notice the gap → return to source for that exact gap → re-explain, looping back to "notice" if it still stalls), why plain language specifically (not just "explain it out loud") is required, and a recognition-vs-recall comparison table. L3 works the bicycle example in full: a first attempt that stalls at "it's like a spinning top" (an analogy with no mechanism), the precise gap that reveals, a second attempt after research that names steering geometry as the dominant mechanism (with the gyroscopic effect as a real but smaller secondary effect), a "what changed, concretely" comparison table, an extension on whether a fluent-but-wrong explanation would even be caught by teach-back (it wouldn't — fluency isn't proof of correctness), and a failure-modes table.
- Per CLAUDE.md rule 4c, chose a simple, universal, non-engineering scenario since the Feynman technique itself is a domain-general learning-craft concept, despite the site's overall engineering audience.
- Wrote 40 exercises (13/13/14, 18 pools), quiz-only.
- Built `interactives.json`: a `familiarity-vs-recall` demo (level 2) — an explicitly-labeled illustrative model with two independent saturating-curve outputs (felt familiarity vs. rereads, actual recall vs. teach-back attempts) rather than complementary lines, making the illusion-of-competence gap directly visible when teach-back attempts stay at 0 while rereads climb. Verified the default (3 rereads/0 teach-backs → 63%/0%) and both sliders at max (10/10 → 96%/97%) exactly.
- Ran the full guardrail suite (`generate:roadmap`, `lint`, `format`, `typecheck` — 0 errors/0 warnings/69 pre-existing hints, `validate:content` — 56 units checked, `test` — 9/9 pass, `build` — 74 pages) — clean.
- Chrome extension bridge connected — full browser verification via `astro preview --background`: L1/L2/L3 all render (mermaid flowchart and both tables confirmed), 2 exercise pools shown on L1; answered one quiz wrong (confirmed "Not quite" + explanation, locked) and the next pool correct (confirmed "Correct" + explanation); dragged the interactive demo's teach-back-attempts slider from 0 to 10 and confirmed recall rose from 0% to 97% while familiarity stayed at 63%, matching the verified formula exactly. No code exercises to verify (quiz-only, non-technical track).
- Flipped `learning-craft/feynman-technique` to `done` in `ROADMAP.md`. 56 total `done` units now. Committed locally (not pushed).
- Next: continuing the standing autonomous loop — auto-picking the next unit by lowest done/total ratio across tracks.

## 2026-08-18 — New unit: product-domain/bounded-contexts

- Auto-picked `product-domain/bounded-contexts` — one of four tracks tied at the lowest done/total ratio (3/13: product-domain, security, software-design, testing-quality), alphabetical tiebreak selected `product-domain`; lowest-numbered `planned` unit in that track was #04, "Why do two teams describe the 'same' business concept differently? (bounded contexts, shared vocabulary)".
- Non-technical track (per CLAUDE.md rule 9, product-domain uses fully worked scenarios, no fenced pseudocode, even though this topic is DDD-adjacent). Builds directly on the prior `domain-driven-design-basics` unit, which already introduced the term "bounded context" via a billing/support Customer split — this unit goes deeper into context maps and integration patterns, using a fresh scenario: Sales wires its `Order` object straight into Warehouse's system, and Warehouse ships packages whose payment later fails because Sales' "Order" and Warehouse's "Order" carry different guarantees despite both teams agreeing on the word.
- L1 introduces bounded context, context map, upstream/downstream, and anticorruption layer as key terms. L2 gives a mermaid flowchart of the context map (Sales Order → anticorruption layer → Warehouse ShipmentRequest), a 3-pattern comparison table (anticorruption layer / conformist / shared kernel), and why a context map tracks meaning boundaries rather than service boundaries. L3 works the Sales/Warehouse bug through in full — root cause, the fix (two explicit boolean conditions instead of trusting Sales' status field), a "what changed" comparison table, an extension on the conformist pattern when there's no negotiating leverage (a third-party marketplace platform), and a failure-modes table.
- Wrote 40 exercises (13/13/14, 19 pools), quiz-only — including an extension pool inviting the reader to reason about a third downstream context (Returns) needing its own translation logic, beyond the given Sales/Warehouse example.
- Built `interactives.json`: an `integration-risk-with-without-layer` demo (level 2) — an explicitly-labeled illustrative model comparing breakage incidents when wired directly (scales with number of downstream consumers × upstream change frequency) against updates needed with a shared anticorruption layer (constant, independent of consumer count). Verified the default (2 consumers/2 changes → 4/2) and both sliders at max (10 consumers/8 changes → 80/8) exactly.
- Ran the full guardrail suite (`generate:roadmap`, `lint`, `format`, `typecheck` — 0 errors/0 warnings/69 pre-existing hints, `validate:content` — 57 units checked, `test` — 9/9 pass, `build` — 75 pages) — clean.
- Chrome extension bridge connected — full browser verification via `astro preview --background`: L1/L2/L3 all render (mermaid context-map diagram confirmed on L2 and L3), 2 exercise pools shown on L2; answered one quiz wrong (confirmed "Not quite" + explanation, locked) and the next correct (confirmed "Correct" + explanation); dragged both interactive-demo sliders to max and confirmed 80/8 exactly matching the verified formula. No code exercises to verify (quiz-only, non-technical track).
- Flipped `product-domain/bounded-contexts` to `done` in `ROADMAP.md`. 57 total `done` units now. Committed locally (not pushed).
- Next: continuing the standing autonomous loop — auto-picking the next unit by lowest done/total ratio across tracks.

## 2026-08-18 — New unit: security/hashing

- Auto-picked `security/hashing` — one of three tracks tied at the lowest done/total ratio (3/13: security, software-design, testing-quality), alphabetical tiebreak selected `security`; lowest-numbered `planned` unit in that track was #04, "Why does storing a password in plain text guarantee disaster? (hashing, salting, key derivation)".
- Technical track. Deliberately scoped to avoid duplicating `security/authentication-fundamentals`, which already covers a full salted-scrypt implementation, a rainbow-table demo, and timing-safe comparison — that unit's L1 explicitly defers "covered in depth in security/hashing." This unit instead covers what makes a function a cryptographic hash in the first place (determinism, avalanche effect, preimage/collision resistance) and what a key derivation function structurally changes versus an everyday fast hash.
- L1 introduces deterministic, avalanche effect, collision resistance, and KDF as key terms, opened with a file-integrity-check scenario (not a password scenario, to stay distinct from authentication-fundamentals). L2 gives a properties table, a mermaid pigeonhole-principle flowchart explaining why collisions are mathematically inevitable yet still "resistant," and a two-lever table (iteration cost, memory hardness) for what a KDF adds. L3 runs three real, verified Node demos: an avalanche-effect measurement (122 of 256 SHA-256 bits flip from one changed character), a real brute-force collision search against a deliberately weak 8-bit toy hash (collision found in 21 attempts, matching the birthday-paradox estimate), and measured `scrypt` cost-parameter timings (3.7ms/12.0ms/50.1ms at N=2^10/12/14) — plus an extension on migrating a KDF's cost parameter transparently at next login, and a failure-modes table.
- Wrote 40 exercises (13/13/14, 19 pools) — L1/L2 quiz-only, L3 mixing 6 code items (3 pools: `hammingDistanceBits`, `toyHash8bit`, `estimateAttemptsForCollision`, all pure functions runnable in the sandboxed Worker with no Node globals) with 8 quiz items; all 6 code exercises' solutions verified against their tests via a standalone reproduction of the sandbox's `expect().toBe()` API before writing.
- Built `interactives.json`: a `kdf-cost-tradeoff` demo (level 3) — an explicitly-labeled illustrative model back-derived from L3's own measured scrypt timings, charting an attacker's total campaign time (days) against the KDF cost parameter while a second slider controls the attacker's guess count; deliberately kept to a single charted output after catching that a first draft's two outputs (login time in ms, attacker time in days) would have shared one mismatched-unit y-axis and were proportional to each other by construction — login time is stated in the description text instead. Verified the default (685.2 days) and both sliders at max (6851.9 days) exactly.
- Ran the full guardrail suite (`generate:roadmap`, `lint`, `format`, `typecheck` — 0 errors/0 warnings/69 pre-existing hints, `validate:content` — 58 units checked, `test` — 9/9 pass, `build` — 76 pages) — clean.
- Chrome extension bridge connected — full browser verification via `astro preview --background`: L1/L2/L3 all render (pigeonhole mermaid diagram and both L2 tables confirmed, L3 code blocks and failure-modes table confirmed); dragged the interactive demo's guess-count slider to max and confirmed 6851.9 days exactly; answered one L1 quiz wrong (confirmed "Not quite" + explanation, locked) and the next correct (confirmed "Correct" + explanation); ran a shown L3 code exercise (`estimateAttemptsForCollision`) with the unmodified TODO stub first (confirmed both tests fail red, solution + explanation revealed), then with a correct implementation (confirmed both tests pass green).
- Flipped `security/hashing` to `done` in `ROADMAP.md`. 58 total `done` units now. Committed locally (not pushed).
- Next: continuing the standing autonomous loop — auto-picking the next unit by lowest done/total ratio across tracks.

## 2026-08-18 — New unit: software-design/dry

- Auto-picked `software-design/dry` — one of two tracks tied at the lowest done/total ratio (3/13: software-design, testing-quality), alphabetical tiebreak selected `software-design`; lowest-numbered `planned` unit in that track was #04, "Why does copy-pasting code eventually blow up? (DRY, abstraction, and the trap of premature abstraction)".
- Technical track. Built around one running example across all three levels: three pasted copies of an identical bulk-discount rule (checkout, cart preview, admin refund tool), correctly consolidated into one shared `applyBulkDiscount` function on the third occurrence (the rule of three), then a new loyalty-discount requirement gets bolted onto that shared function via a boolean flag — revealing the forced-abstraction trap when a genuinely different rule reuses a shape that only looked similar. Fixed via composable discount-rule objects with explicit array-order priority instead of implicit if/else-if branching.
- L1 introduces DRY, true duplication, coincidental duplication, and premature abstraction as key terms. L2 gives the actual test for merging duplication ("would a rule change need to hit both places, always?"), a mermaid decision-tree diagram, the rule-of-three heuristic table, and the concrete boolean-flag warning sign. L3 works the full four-step example with real, verified code — including a genuinely silent bug (loyalty and bulk both qualifying, with priority falling out accidentally from `if/else-if` ordering) that existing tests couldn't catch — plus an extension distinguishing a genuinely shared 50%-discount-cap constraint (true duplication, belongs in one place) from the earlier flag mistake, and a failure-modes table.
- Wrote 40 exercises (13/13/14, 19 pools) — L1/L2 quiz-only, L3 mixing 4 code items (2 pools: `applyBulkDiscount`, `calculateDiscount` with its `discountRules` array given in the starter code) with 6 quiz items; all 4 code exercises' solutions verified against their tests via a standalone reproduction of the sandbox's `expect().toBe()` API before writing.
- Built `interactives.json`: a `discount-priority-explorer` demo (level 3) using the unit's own verified `calculateDiscount` logic directly — dragging quantity across the loyalty (3+) and bulk (5+) thresholds with a loyalty toggle shows the actual kink in the discounted total where rule priority takes effect. Verified the default ($25.50 at qty 3, loyalty on) and quantity at max with loyalty on ($85 at qty 10) exactly.
- Ran the full guardrail suite (`generate:roadmap`, `lint`, `format`, `typecheck` — 0 errors/0 warnings/69 pre-existing hints, `validate:content` — 59 units checked, `test` — 9/9 pass, `build` — 77 pages) — clean.
- Chrome extension bridge connected — full browser verification via `astro preview --background`: L1/L2/L3 all render (decision-tree mermaid diagram and glossary-term cross-links confirmed); dragged the interactive demo's quantity slider to max and confirmed $85 exactly, with the threshold kink visibly rendered on the chart; ran the shown L3 `calculateDiscount` code exercise with the unmodified TODO stub first (confirmed all three tests fail red), then replaced it with a correct implementation via a direct DOM value-set + input-event dispatch after `type` action input didn't register in the textarea (confirmed all three tests pass green, matching the earlier standalone verification).
- Flipped `software-design/dry` to `done` in `ROADMAP.md`. 59 total `done` units now. Committed locally (not pushed).
- Next: continuing the standing autonomous loop — auto-picking the next unit by lowest done/total ratio across tracks.

## 2026-08-18 — New unit: testing-quality/tdd-basics

- Auto-picked `testing-quality/tdd-basics` — the clear lowest done/total ratio (3/13) — "Why does writing the test first change the design? (TDD basics, red-green-refactor)".
- Technical track. Built around one running example across all three levels: a password-strength checker (`describePasswordStrength`) built through four real red-green-refactor cycles, contrasted against an opening scenario where a second engineer builds the same feature code-first with `console.warn` calls instead of a return value.
- L1 introduces TDD, red-green-refactor, minimum code to pass, and design pressure as key terms. L2 gives a mermaid flowchart of the loop, a table of what each of the three steps guards against, and the argument that "easiest to test" and "easiest to reuse elsewhere" tend to converge on the same interface. L3 works all four cycles with real, verified code (including a deliberate placeholder in cycle 1's green step for cases no test yet covers), a refactor into a `passwordRules` array verified to produce byte-for-byte identical output, a direct comparison to what the code-first `console.warn` version would have looked like, an extension on a genuinely different requirement (a common-password check that doesn't fit the existing rule shape and needs to short-circuit instead), and a failure-modes table.
- Wrote 40 exercises (13/13/14, 19 pools) — L1/L2 quiz-only, L3 mixing 4 code items (2 pools building `describePasswordStrength` incrementally: through cycle 2, then the full cycle-4 version) with 6 quiz items; all 4 code exercises' solutions verified against their tests via a standalone reproduction of the sandbox's `expect().toEqual()` API before writing.
- Did not build `interactives.json` for this unit — after genuine consideration, TDD's core content (the red-green-refactor discipline and the design-pressure argument) is process/qualitative, with no real continuous quantity in the worked example to back-derive a meaningful slider demo from (unlike the KDF-cost and discount-threshold curves used in recent technical units). Noted here per CLAUDE.md rule 5c rather than skipped silently.
- Ran the full guardrail suite (`generate:roadmap`, `lint`, `format`, `typecheck` — 0 errors/0 warnings/69 pre-existing hints, `validate:content` — 60 units checked, `test` — 9/9 pass, `build` — 78 pages) — clean.
- Chrome extension bridge connected — full browser verification via `astro preview --background`: L1/L2/L3 all render (red-green-refactor mermaid diagram and failure-modes table confirmed); reloaded once to land on a code-pool pair, ran the shown `describePasswordStrength` code exercise with the unmodified TODO stub first (confirmed all three tests fail red, solution + explanation revealed), then replaced it with a correct implementation via a direct DOM value-set + input-event dispatch (confirmed all three tests pass green).
- Flipped `testing-quality/tdd-basics` to `done` in `ROADMAP.md`. 60 total `done` units now. Committed locally (not pushed).
- Next: continuing the standing autonomous loop — auto-picking the next unit by lowest done/total ratio across tracks.

## 2026-08-18 — New unit: systems/concurrency

- Auto-picked `systems/concurrency` — one of two tracks tied at the lowest done/total ratio (4/17: systems, web), alphabetical tiebreak selected `systems`; lowest-numbered `planned` unit in that track was #05, "Why does my server fall over under load? (concurrency: threads, processes, async I/O)".
- Technical track. Built around one running example across all three levels: a single-threaded server where a 0ms-scheduled timer (Request B) gets delayed by a synchronous 200ms calculation (Request A) running at the same moment — measured with real timestamps, not just described.
- L1 introduces concurrency, parallelism, blocking, and the event loop as key terms, explicitly distinguishing concurrency (progress over time) from parallelism (same literal instant). L2 gives a mermaid flowchart of non-blocking I/O interleaving two requests' queries, a work-type table (I/O vs. CPU-bound), and threads/processes as a genuinely parallel alternative with its own new problem (shared-memory collisions, explicitly foreshadowing the next unit, `race-conditions`). L3 runs two real, verified Node demos — the block (measured ~200ms delay) and the fix (chunked work with `setImmediate`, measured delay dropping to ~20ms while Request A's total time stays unchanged) — plus an extension on uninterruptible work needing a worker thread instead of chunking, and a failure-modes table.
- Wrote 40 exercises (13/13/14, 19 pools) — L1/L2 quiz-only, L3 mixing 4 code items (2 pools: `splitIntoChunks`, `estimateFirstDelay`, both pure functions mirroring L3's own `chunkedWork` helper and its core finding) with 6 quiz items; all 4 code exercises' solutions verified against their tests via a standalone reproduction of the sandbox's `expect().toBe()`/`toEqual()` API before writing.
- Built `interactives.json`: a `chunk-size-vs-delay` demo (level 3) directly using the unit's own verified formula (`delay = min(chunkMs, totalMs)`) from `estimateFirstDelay` — dragging chunk size shows the delay felt by a concurrent callback drop linearly toward the chunk size, independent of total work. Verified the default (20ms) and chunk size at minimum (1ms) exactly.
- Ran the full guardrail suite (`generate:roadmap`, `lint`, `format`, `typecheck` — 0 errors/0 warnings/69 pre-existing hints, `validate:content` — 61 units checked, `test` — 9/9 pass, `build` — 79 pages) — clean.
- Chrome extension bridge connected — full browser verification via `astro preview --background`: L1/L2/L3 all render (event-loop mermaid diagram confirmed); dragged the interactive demo's chunk-size slider to minimum and confirmed the delay dropped to 1ms exactly, matching the verified formula; reloaded twice to land on a code-pool pair, ran the shown `estimateFirstDelay` code exercise with the unmodified TODO stub first (confirmed all three tests fail red), then replaced it with a correct implementation via a direct DOM value-set + input-event dispatch (confirmed all three tests pass green).
- Flipped `systems/concurrency` to `done` in `ROADMAP.md`. 61 total `done` units now. Committed locally (not pushed).
- Next: continuing the standing autonomous loop — auto-picking the next unit by lowest done/total ratio across tracks.

## 2026-08-18 — New unit: web/bundling

- Auto-picked `web/bundling` — the clear lowest done/total ratio (4/17) — "Why do we need a build step at all? (bundling, transpilation, module systems)".
- Technical track. Built around one running example across all three levels: a page with four `<script>` tags in a manually-worked-out order, where adding a fifth shared-utility tag at the bottom breaks the page with "utils is not defined" — contrasted throughout with a real dependency-graph resolver that computes the correct order automatically.
- L1 introduces module, dependency graph, bundler, and transpilation as key terms. L2 gives a mermaid flowchart of a shared dependency (`c.js`) being resolved into the bundle exactly once, a problems/solutions table distinguishing bundling from transpilation, and tree-shaking as one further step over the same graph information. L3 implements a real, verified topological-sort resolver (`resolveOrder`), demonstrates the shared-dependency case appearing exactly once, contrasts it directly against a naive `Object.keys` insertion-order bug reproducing the opening scenario's failure, implements a tree-shaking demo (`findUnusedExports`), and extends into circular imports — confirming `resolveOrder` terminates without an infinite loop on a cycle but that termination isn't the same as correctness.
- Wrote 40 exercises (13/13/14, 18 pools) — L1/L2 quiz-only, L3 mixing 4 code items (2 pools: `resolveOrder`, `findUnusedExports`, both matching L3's own verified functions exactly) with 6 quiz items; all 4 code exercises' solutions verified against their tests via a standalone reproduction of the sandbox's `expect().toEqual()` API before writing.
- Built `interactives.json`: a `dedup-savings-explorer` demo (level 2) directly grounded in the unit's own `resolveOrder` dedup mechanism — dragging the number of modules importing a shared file shows how many duplicated bytes a naive, non-deduplicated approach would have shipped for the same shared module size. Verified the default (20KB at 2 importers) and importers at max (380KB at 20) exactly.
- Ran the full guardrail suite (`generate:roadmap`, `lint`, `format`, `typecheck` — 0 errors/0 warnings/69 pre-existing hints, `validate:content` — 62 units checked, `test` — 9/9 pass, `build` — 80 pages) — clean.
- Chrome extension bridge connected — full browser verification via `astro preview --background`: L1/L2/L3 all render (dependency-graph mermaid diagram confirmed, `<script>` tag syntax in the opening scenario escaped correctly); dragged the interactive demo's importer-count slider to max and confirmed 380KB exactly; reloaded twice to land on a code-pool pair, ran the shown `findUnusedExports` code exercise with the unmodified TODO stub first (confirmed all three tests fail red), then replaced it with a correct implementation via a direct DOM value-set + input-event dispatch (confirmed all three tests pass green).
- Flipped `web/bundling` to `done` in `ROADMAP.md`. 62 total `done` units now. Committed locally (not pushed).
- Next: continuing the standing autonomous loop — auto-picking the next unit by lowest done/total ratio across tracks.

## 2026-08-18 — New unit: git-teamwork/merge-rebase

- Auto-picked `git-teamwork/merge-rebase` (5/18 done in `git-teamwork/`, tied-lowest ratio) — "Why does `git pull` sometimes create a mess? (merge vs. rebase, fast-forward)".
- Technical track. Built around one running example across all three levels: an engineer's clean Monday `git pull` versus Tuesday's identical command adding an unexplained "Merge branch 'main' of ..." commit, because local made one commit of its own before pulling on Tuesday.
- L1 introduces fast-forward, merge commit, rebase, and divergence as key terms. L2 gives a mermaid flowchart contrasting the fast-forward-possible vs. fast-forward-impossible commit-graph shapes, a merge-vs-rebase comparison table (new commit? / hashes preserved? / resulting shape), and the real trade-off: rebase's rewritten hashes are risky on already-shared history, which is why merge is the safer default there — foreshadowing a later `shared-history` unit. L3 implements and verifies a real `isAncestor`/`canFastForward` pair and a real `rebase` replay function against the exact Monday/Tuesday commit graphs, then extends the rebase example to a two-commit chain, and closes with a failure-modes table (including "no merge commit" not meaning "nothing was reconciled" — rebase can still hit conflicts while replaying).
- Wrote 40 exercises (13/13/14, 19 pools) — L1/L2 quiz-only, L3 mixing 4 code items (2 pools: `isAncestor`, `rebase`, both matching L3's own verified functions exactly) with 6 quiz items; all 4 code exercises' solutions verified against their tests via a standalone Node script before writing.
- Deliberately skipped `interactives.json`: the only real quantifiable relationship in this unit's mechanics (number of local commits diverged vs. number of commits a rebase must replay) is a 1:1 identity, not an independent computed relationship — same category of skip as `testing-quality/tdd-basics`, made consciously per CLAUDE.md rule 5c rather than by default.
- Ran the full guardrail suite (`generate:roadmap`, `lint` — 0 issues, `format`+`typecheck` — 0 errors/0 warnings/69 pre-existing hints, `validate:content` — 63 units checked, `test` — 9/9 pass, `build` — 81 pages) — clean.
- Chrome extension bridge connected — full browser verification via `astro preview --background`: L1/L2/L3 all render (fast-forward-vs-not mermaid diagram confirmed), L3's two shown code pools (`rebase`, `isAncestor`) both surfaced; ran the shown `rebase` code exercise with the unmodified TODO stub first (confirmed both tests fail red), then replaced it with a correct implementation via a direct DOM value-set + input-event dispatch (confirmed both tests pass green, solution/explanation revealed); on an L1 quiz, picked a wrong answer, confirmed "Check answer" locks the radios, marks the wrong pick red-strikethrough and the correct one green, and reveals the required explanation.
- Flipped `git-teamwork/merge-rebase` to `done` in `ROADMAP.md`. 63 total `done` units now. Committed locally (not pushed).
- Next: continuing the standing autonomous loop — auto-picking the next unit by lowest done/total ratio across tracks.

## 2026-08-18 — New unit: infra-delivery/deployment-automation

- Auto-picked `infra-delivery/deployment-automation` — tied-lowest done/total ratio (3/12) alongside `logic` and `sustainable-performance`, alphabetical tiebreak — "Why do manual deploys eventually cause an outage? (deployment automation, repeatability)".
- Technical track. Built around one running example across all three levels: an engineer's manual SSH-pull-install-restart deploy that works fine for eight releases, until a rushed ninth deploy skips `npm install` and crashes the service — contrasted throughout with a scripted pipeline (fixed step order, health check gating the atomic switch, automatic rollback).
- L1 introduces deployment, idempotency, repeatability, atomic switch, and rollback as key terms. L2 gives a mermaid flowchart of the full pipeline (extract → install → migrate → health check → switch/rollback → restart), explains why the health check must gate the switch rather than follow it, why a symlink switch has no mixed-version window an in-place overwrite would, and a manual-vs-scripted comparison table. L3 implements and verifies `buildDeploySteps` and `runDeploy` against the exact Deploy 9 scenario (reproducing the missing `install-deps` step side by side with the function's guaranteed-complete output), then extends into a `retryHealthCheck` function to handle a slow-starting-but-healthy service, and closes with a failure-modes table.
- Wrote 42 exercises (14/14/14, 21 pools) — L1/L2 quiz-only, L3 mixing 4 code items (2 pools: `buildDeploySteps`, `runDeploy`, both matching L3's own verified functions exactly) with 10 quiz items; all 4 code exercises' solutions verified against their tests via a standalone Node script before writing.
- Built `interactives.json`: a `manual-vs-automated-recovery` demo (level 2) showing manual recovery time (step count × seconds-per-step, both draggable) growing linearly against automated recovery time held flat at a constant 10s — deliberately kept as two outputs since one is genuinely non-constant and the other is a real independent fact (not a complement or proportional multiple), directly illustrating the unit's core point that scripted repeatability doesn't degrade as pipeline complexity grows. Verified the default (5 steps × 45s = 225s vs. 10s) and max (20 steps × 45s = 900s vs. 10s) exactly.
- Ran the full guardrail suite (`generate:roadmap`, `lint` — 0 issues, `format`+`typecheck` — 0 errors/0 warnings/69 pre-existing hints, `validate:content` — 64 units checked, `test` — 9/9 pass, `build` — 82 pages) — clean.
- Chrome extension bridge connected — full browser verification via `astro preview --background`: L1/L2/L3 all render (pipeline mermaid diagram with the health-check healthy/unhealthy branch confirmed); dragged the interactive demo's step-count slider to max and confirmed 900s manual vs. flat 10s automated exactly; L3's shown `buildDeploySteps` code exercise ran with the unmodified TODO stub first (confirmed both tests fail red), then replaced with a correct implementation via a direct DOM value-set + input-event dispatch (confirmed both tests pass green, solution/explanation revealed).
- Flipped `infra-delivery/deployment-automation` to `done` in `ROADMAP.md`. 64 total `done` units now. Committed locally (not pushed).
- Next: continuing the standing autonomous loop — auto-picking the next unit by lowest done/total ratio across tracks.

## 2026-08-18 — New unit: logic/boolean-logic

- Auto-picked `logic/boolean-logic` — tied-lowest done/total ratio (3/12) alongside `sustainable-performance`, alphabetical tiebreak — "Why do two \"correct\" pieces of code produce different results? (boolean logic, precedence, De Morgan's laws)".
- Technical track. Built around one running example across all three levels: an access-control condition `!isAdmin && isLoggedIn || isOwner` that reads like a faithful translation of "not an admin, and either logged in or the owner," but actually parses as `(!isAdmin && isLoggedIn) || isOwner` — letting an admin who also owns a document edit it, which a security review later flags as a real bug.
- L1 introduces operator precedence, truth table, De Morgan's laws, and short-circuit evaluation as key terms. L2 gives a mermaid diagram contrasting the actual vs. intended groupings, a truth table proving the two versions diverge exactly when `isAdmin` and `isOwner` are both true, and a De Morgan's-law derivation table for building the correct negation. L3 implements `canEditBuggy`/`canEditFixed` as real functions, brute-forces all eight input combinations to prove exactly where they diverge, derives `cannotEdit` via a two-step De Morgan negation and verifies it against `!canEditFixed` for every combination, then extends into what happens with a careless (operator-preserving) negation and a hypothetical fourth chained condition.
- Wrote 42 exercises (14/14/14, 21 pools) — L1/L2 quiz-only, L3 mixing 4 code items (2 pools: `canEditFixed`, `cannotEdit`, both matching L3's own verified functions exactly) with 10 quiz items; all 4 code exercises' solutions verified against their tests via a standalone Node script before writing.
- Deliberately skipped `interactives.json`: the unit's core relationship is a discrete 8-row boolean truth table, not a continuous numeric quantity, so a slider-driven chart doesn't naturally apply — same category of conscious skip as `testing-quality/tdd-basics` and `git-teamwork/merge-rebase`.
- Caught and fixed a real Prettier/markdown bug while running `format`: literal `||` characters inside table cells (even inside backtick code spans) broke the failure-modes table's column parsing, producing a corrupted table with mismatched column counts across two format passes. Fixed by rewriting the affected cells to say "AND"/"OR" in prose instead of literal `&&`/`||` symbols — confirmed the table reformats cleanly to 2 columns × 6 rows afterward, and verified in the browser that it renders as a proper 2-column table.
- Ran the full guardrail suite (`generate:roadmap`, `lint` — 0 issues, `format`+`typecheck` — 0 errors/0 warnings/69 pre-existing hints, `validate:content` — 65 units checked, `test` — 9/9 pass, `build` — 83 pages) — clean.
- Chrome extension bridge connected — full browser verification via `astro preview --background`: L1/L2/L3 all render (L2's actual-vs-intended grouping mermaid diagram and truth table both confirmed correct columns, L3's failure-modes table confirmed 2 columns × 6 rows via DOM inspection); L3's shown `cannotEdit` code exercise ran with the unmodified TODO stub first (confirmed both tests fail red), then replaced with a correct implementation via a direct DOM value-set + input-event dispatch (confirmed both tests pass green, solution/explanation revealed).
- Flipped `logic/boolean-logic` to `done` in `ROADMAP.md`. 65 total `done` units now. Committed locally (not pushed).
- Next: continuing the standing autonomous loop — auto-picking the next unit by lowest done/total ratio across tracks.

## 2026-08-18 — New unit: sustainable-performance/attention-management

- Auto-picked `sustainable-performance/attention-management` — lowest done/total ratio (2/8) — "How do I protect focus time in a job full of interruptions? (attention management, deep work vs. shallow work)".
- Non-technical/workplace-mechanic track — per CLAUDE.md rule 9, grounded entirely in a fully worked real-world scenario with prose/tables, no fake pseudocode. Built around one running example across all three levels: an engineer who works a genuinely busy 8-hour day (22 interruptions, all answered promptly) but makes only ~40 minutes of real progress on a hard architecture document, contrasted with a reorganized day using three 90-minute protected blocks that produces ~270 minutes of true deep-work progress from the same 8 hours and the same total shallow work.
- L1 introduces deep work, shallow work, attention residue, and task switching cost as key terms. L2 gives a mermaid diagram of an interruption's true cost (direct handling + residue), a rare-vs-frequent-interruptions comparison table showing the threshold effect (frequency mattering more than length), and the batching-not-deleting fix (shallow work moves in time, doesn't shrink). L3 walks Day A and Day B side by side with concrete illustrative numbers (30 sec direct handling, 15 min residue per interruption, explicitly labeled as illustrative not universal constants), isolates that both days spend the same ~11 minutes on direct handling while differing entirely in residue cost, then extends into whether the mechanism generalizes to other hard tasks and what happens to output at shorter block lengths.
- Wrote 42 quiz-only exercises (14/14/14, 21 pools) per rule 9's non-technical-track requirement — no code items.
- Built `interactives.json`: an `interruption-frequency-vs-deep-work` demo (level 2) modeling L2's frequency-vs-length threshold effect directly — `deepWorkMinutes = max(0, interval - residue) / interval × 480`. Verified the default (20 min interval, 15 min residue → 120 min) and the threshold case (5 min interval, below the 15 min residue floor → exactly 0 min), both confirmed in the browser.
- Caught and fixed a real Prettier/markdown table-corruption bug in the previous session's unit (`logic/boolean-logic`) is not repeated here — no literal `||`/pipe characters appear in any of this unit's table cells, confirmed by inspection before writing.
- Ran the full guardrail suite (`generate:roadmap`, `lint` — 0 issues, `format`+`typecheck` — 0 errors/0 warnings/69 pre-existing hints, `validate:content` — 66 units checked, `test` — 9/9 pass, `build` — 84 pages) — clean.
- Chrome extension bridge connected — full browser verification via `astro preview --background`: L1/L2/L3 all render (L2's rare-vs-frequent comparison table confirmed 3 clean columns); dragged the interactive demo's interval slider to its minimum (5 min) and confirmed the output correctly drops to exactly 0min, matching the threshold-effect design; on an L3 quiz, picked a wrong answer, confirmed "Check answer" locks the radios, marks the wrong pick red-strikethrough and the correct one green, and reveals the required explanation.
- Flipped `sustainable-performance/attention-management` to `done` in `ROADMAP.md`. 66 total `done` units now. Committed locally (not pushed).
- Next: continuing the standing autonomous loop — auto-picking the next unit by lowest done/total ratio across tracks.

## 2026-08-18 — New unit: applied-math/combinatorics

- Auto-picked `applied-math/combinatorics` — tied-lowest done/total ratio (4/15) alongside `people-management`, alphabetical tiebreak — "How do I count possibilities without enumerating them? (combinatorics: permutations, combinations, foundations of probability)".
- Technical track. Built around one running example across all three levels: a payments feature behind 4 independent boolean flags, where QA's 8 hand-picked "important" test cases felt thorough but silently left 3 of the 6 distinct flag pairs uncovered — including Apple Pay + promo code, the exact pair that broke checkout in production.
- L1 introduces factorial, permutation, combination, and combinatorial explosion as key terms. L2 gives an `xychart-beta` bar chart of 2^n growth from 1–8 flags, worked permutation (ranking 3 finalists) and combination (picking a 3-person team) examples with a comparison table, and identifies that the flag-combination problem itself is a third pattern (2^n) distinct from both, while counting flag _pairs_ is genuinely a combination question (nC2). L3 implements `combinations`/`factorial`, a `generateAllCombinations` power-set enumerator, and a `findUncoveredPairs` pairwise-coverage checker run against the real 8 test cases — reproducing the exact 3 uncovered pairs and confirming the missing Apple-Pay/promo-code state precisely — then extends into what a 9th test case does and doesn't fix.
- Wrote 40 exercises (14/12/14, 20 pools) — L1/L2 quiz-only, L3 mixing 4 code items (2 pools: `combinations`, `findUncoveredPairs`, both matching L3's own verified functions exactly) with 10 quiz items; all 4 code exercises' solutions verified against their tests via a standalone Node script before writing.
- Built `interactives.json`: a `flags-vs-pairs-growth` demo (level 2) charting `2^n` (total combinations) against `nC2` (distinct pairs) as flag count grows from 1–12 — two genuinely independent counts (one exponential, one quadratic, neither a complement or multiple of the other) directly reproducing the unit's own 4-flag numbers (16 combinations, 6 pairs) at the default. Verified the default exactly in the browser.
- Ran the full guardrail suite (`generate:roadmap`, `lint` — 0 issues, `format`+`typecheck` — 0 errors/0 warnings/69 pre-existing hints, `validate:content` — 67 units checked, `test` — 9/9 pass, `build` — 85 pages) — clean.
- Chrome extension bridge connected — full browser verification via `astro preview --background`: L1/L2/L3 all render (L2's `xychart-beta` growth chart confirmed rendering correctly from 2 to 256); interactive demo confirmed matching the verified default (16 combinations, 6 pairs) exactly; L3's shown `combinations` code exercise ran with the unmodified TODO stub first (confirmed all three tests fail red), then replaced with a correct implementation via a direct DOM value-set + input-event dispatch (confirmed tests pass green, including C(7,3) = 35).
- Flipped `applied-math/combinatorics` to `done` in `ROADMAP.md`. 67 total `done` units now. Committed locally (not pushed).
- Next: continuing the standing autonomous loop — auto-picking the next unit by lowest done/total ratio across tracks.

## 2026-08-18 — New unit: people-management/goal-setting

- Auto-picked `people-management/goal-setting` — lowest done/total ratio (4/15) — "How do I set goals for a team that actually align with what the org needs? (goal-setting, OKRs, translating strategy downward)".
- Non-technical/workplace-mechanic track — per CLAUDE.md rule 9, grounded entirely in a fully worked real-world scenario with prose/tables, no fake pseudocode. Built around one running example across all three levels: a manager sets the vague team goal "improve code quality," the team works hard all quarter, and leadership is still disappointed because the goal never traced to the company's real priority (reducing outages threatening a major customer renewal).
- L1 introduces objective, key result, cascading goals, and skill gap analysis as key terms. L2 gives a mermaid diagram of the cascade from company priority → team objective → key results, an objective-vs-key-result comparison table, and the "slogan vs. spreadsheet" framing for why both pieces are needed together. L3 rebuilds the quarter's actual goal step by step from the real company priority, checks each key result against three explicit questions (measurable? within control? traces to the objective?) in a table, directly compares the rebuilt goal against "improve code quality" in a second table, and extends into what should happen if the company priority shifts mid-quarter.
- Wrote 38 quiz-only exercises (14/12/12, 19 pools) per rule 9's non-technical-track requirement — no code items.
- **Fixed a real bug in the scaffolding step**: `bun run new:unit people-management goal-setting` correctly scaffolded the content files under `src/content/people-management/goal-setting/`, but incorrectly flipped `career-craft`'s same-named `goal-setting` row to `in-progress` in `ROADMAP.md` instead of `people-management`'s — the script's status-update logic doesn't disambiguate identical slugs across different tracks. Manually corrected both rows (`career-craft/goal-setting` back to `planned`, `people-management/goal-setting` to `done`) before running `generate:roadmap`. Not fixed at the script level this session — worth fixing `new-unit.mjs` to scope its slug lookup by track in a future session.
- **Caught and fixed a real MDX build failure**: L3's key-result table originally wrote `<15min` in a table cell, which MDX's JSX parser interpreted as the start of a malformed JSX tag, breaking `bun run build` with an `mdx-jsx: unexpected-character` error. Fixed by rewriting the cell as "under 15min" — confirmed the identical `<15min` string inside the L2 mermaid diagram (a code fence, not parsed as JSX) was unaffected and didn't need changing.
- Built `interactives.json`: a `total-downtime-from-two-key-results` demo (level 3) modeling `totalDowntimeMinutes = incidentCount × mttrMinutes` — a genuine, non-contrived numeric relationship pulled directly from the rebuilt goal's own first two key results (8 incidents × 45min → 360min at default; 2 × 10min → 20min when dragged toward the target), illustrating why the two key results are independent levers rather than redundant. Verified both the default and a dragged-to-target reading exactly in the browser.
- Ran the full guardrail suite (`generate:roadmap`, `lint` — 0 issues, `format`+`typecheck` — 0 errors/0 warnings/69 pre-existing hints, `validate:content` — 68 units checked, `test` — 9/9 pass, `build` — 86 pages after the MDX fix) — clean.
- Chrome extension bridge connected — full browser verification via `astro preview --background`: L1/L2/L3 all render (L2's cascade mermaid diagram confirmed, including the `<15min` label rendering correctly inside the diagram); all three L3 tables confirmed clean, consistent column counts via DOM inspection; interactive demo confirmed matching the verified default (360min) exactly, then dragged both sliders to the target values and confirmed the output dropped to exactly 20min.
- Flipped `people-management/goal-setting` to `done` in `ROADMAP.md`. 68 total `done` units now. Committed locally (not pushed).
- Next: continuing the standing autonomous loop — auto-picking the next unit by lowest done/total ratio across tracks.

## 2026-08-19 — New unit: business-communication/building-credibility-ahead-ask

- Auto-picked `business-communication/building-credibility-ahead-ask` — "Why did the same request get a yes from one person and a no from another? (building credibility ahead of the ask)".
- Non-technical/domain-general track — per rule 4c, used a fully universal, non-workplace scenario: two people, Alex and Jordan, separately ask Maya the identical question ("can I borrow your bike for the weekend?"). Alex gets a yes because of a month of genuine trust-building beforehand; Jordan, a near-stranger, gets a no. Same running example carried through all three levels.
- L1 introduces credibility, trust deposit, trust withdrawal, and timing of the ask as key terms. L2 gives a mermaid flowchart of deposits flowing into a rising trust balance that the ask then draws down from, a small-ask-vs-big-ask comparison table, and a section on why deposits must be genuine (not performative) to count. L3 rebuilds Alex's month as a literal running-balance table (4 deposits of +5 each → 20, ask costs -15 → final balance 5, matching the actual "likely yes" outcome) alongside Jordan's cold-ask table (0 → -15, matching the actual "no"), then extends into what happens if Alex asks again the following weekend without new deposits (5 - 15 = -10), plus a 5-row failure-modes table.
- Wrote 38 quiz-only exercises (14/12/12, 19 pools) per rule 9's non-technical-track requirement — no code items. Every pool carries a `reference` panel (icon list / mini-table + a `<strong>Why:</strong>` line) for the whiteboard drawer.
- Built `interactives.json`: a `trust-balance-explorer` demo (level 3) modeling `balanceAfterAsk = numDeposits × 5 - askCost`, back-derived directly from L3's own point system — reproduces Alex's actual balance of 5 at the default (4 deposits, cost 15) and Jordan's actual -15 at 0 deposits. Verified both readings exactly in the browser, including the live SVG chart.
- Ran the full guardrail suite (`generate:roadmap`, `lint` — 0 issues, `format` — clean, `typecheck` — 0 errors/0 warnings/69 pre-existing hints, `validate:content` — 69 units checked, `test` — 9/9 pass, `build` — 87 pages) — clean.
- Chrome extension bridge connected — full browser verification via `astro preview --background`: L1/L2/L3 all render (L2's mermaid deposit-flow diagram confirmed rendering correctly); exercise panel confirmed showing 2 of 7 pools with `data-ready="true"`; interactive demo confirmed matching both verified readings (5 at default, -15 at 0 deposits) exactly, live chart rendering; picked a wrong answer on an L3 quiz item, confirmed "Check answer" locks the radios, marks the wrong pick red-strikethrough and the correct one green, and reveals the required explanation.
- Flipped `business-communication/building-credibility-ahead-ask` to `done` in `ROADMAP.md`. 69 total `done` units now. Committed locally (not pushed).
- Next: continuing the standing autonomous loop — auto-picking the next unit by lowest done/total ratio across tracks.

## 2026-08-19 — New unit: corporate-politics/strategic-ambiguity

- Auto-picked `corporate-politics/strategic-ambiguity` — lowest done/total ratio (4/14) — "What is 'gray' politics, and where's the line before it turns dishonest? (strategic ambiguity, selective disclosure, timing information)".
- Workplace-mechanic track — per rule 9, grounded entirely in a fully worked real-world scenario with prose/tables, no fake pseudocode. Built around one running example across all three levels: Priya, a manager who learns in confidence that a reorg will likely cut her report Sam's role, isn't yet authorized to say so, and has to answer Sam's direct "is my job safe?" without lying or breaking confidence.
- L1 introduces strategic ambiguity, selective disclosure, affirmative falsehood, and timing information as key terms, contrasting Priya's two candidate answers. L2 gives a mermaid decision-flowchart distinguishing legitimate ambiguity from deception via a "does this create a false belief" test, a withholding-vs-lying comparison table, and a section establishing timing as an axis independent of truthfulness. L3 walks through the Day 3 conversation as four concrete versions (full disclosure — not actually available; strategic ambiguity — honest; silent gray zone — risky; affirmative falsehood — dishonest) with a verdict table, then a day-by-day risk table showing how the honest Day 3 answer goes stale by Day 12 once a real decision (a competing job offer) enters the picture, plus an extend-it question on whether a later-cancelled reorg retroactively changes Day 3's honesty, and a 5-row failure-modes table.
- Wrote 38 quiz-only exercises (14/12/12, 19 pools) per rule 9's non-technical-track requirement — no code items.
- Built `interactives.json`: a `cost-of-the-ambiguity-window` demo (level 3) modeling `riskScore = min(100, daysSinceAsked × riskPerDay)`, back-derived from L3's own timeline (Day 3 to Day 12 = 9 days) — reproduces a risk of 63 at the default (9 days, 7/day) and caps correctly at 100 when dragged to the slider maximums. Verified both readings exactly in the browser.
- **Caught and fixed a real Mermaid rendering bug during browser verification**: L2's flowchart node `A["Someone asks a direct question"]` had no internal `\n` line break (unlike every other node in the same diagram), and Mermaid clipped the rendered box to a fixed width, truncating the visible text to "Someone asks a direct quest" — a real, silently-broken diagram that `build`/`typecheck` had no way to catch since it's a runtime rendering issue, not a syntax error. Fixed by adding a line break (`"Someone asks\na direct question"`), rebuilt, and re-verified in the browser that the full text now renders on two lines.
- Ran the full guardrail suite (`generate:roadmap` — 218 units/16 tracks, `lint` — 0 issues, `format` — clean, `typecheck` — 0 errors/0 warnings/69 pre-existing hints, `validate:content` — 70 units checked, `test` — 9/9 pass, `build` — 88 pages) — clean, re-run after the Mermaid fix.
- Chrome extension bridge connected — full browser verification via `astro preview --background`: L1/L2/L3 all render; L2's decision flowchart confirmed fully legible end to end after the truncation fix; all three levels' exercise panels confirmed populated (2 pools each) via `data-shown-ids`/`data-ready`; interactive demo confirmed matching both verified readings (63 at default, 100 at max) exactly; picked a wrong answer on an L3 quiz item, confirmed "Check answer" locks the radios, marks the wrong pick red-strikethrough and the correct one green, and reveals the required explanation.
- Flipped `corporate-politics/strategic-ambiguity` to `done` in `ROADMAP.md`. 70 total `done` units now. Committed locally (not pushed).
- Next: continuing the standing autonomous loop — auto-picking the next unit by lowest done/total ratio across tracks.

## 2026-08-19 — New unit: systems/race-conditions

- Auto-picked `systems/race-conditions` — tied-lowest done/total ratio (5/17) alongside `web`, alphabetical tiebreak — "How do we avoid two processes corrupting shared data? (race conditions, locks, atomicity)".
- Technical track. Built around one running example across all three levels: a flash-sale item with 1 unit of stock, where two concurrent purchase requests both read `stock = 1` and both succeed, oversold by 1 — the classic time-of-check-to-time-of-use (TOCTOU) bug.
- L1 introduces race condition, critical section, atomicity, and lock (mutex) as key terms. L2 gives a mermaid sequence diagram of the exact interleaving that causes the bug, a make-it-atomic-vs-use-a-lock comparison table, and a section on why adding more checks or delays doesn't close the gap, only relocates it. L3 reproduces the bug with real, runnable Node.js code (`InventoryStore` with simulated DB round-trip delay via `await delay(5)`), verified to actually reproduce 2 successes against 1 unit of stock, then fixes it two ways: a synchronous `purchaseAtomic` (no `await` in the critical section, verified 1 success), and a hand-rolled `Mutex` class with `purchaseLocked` (verified 1 success out of 3 concurrent buyers), plus an extend-it question confirming the atomic fix holds with 3 buyers too (verified 1 success, matching the locked version).
- Wrote 40 exercises (13/13/14, 19 pools) — L1/L2 quiz-only, L3 mixing 4 code items (2 pools: `purchase-atomic`, `mutex`) with 10 quiz items; all 4 code exercises' solutions independently verified against their tests via a standalone Node script before writing, using a hand-written `expect()` shim matching the site's actual grading API.
- Built `interactives.json`: an `oversold-units-by-concurrent-buyers` demo (level 3) charting `unitsOversold = concurrentBuyers - 1`, back-derived from the exact racy code and verified via a Node script across buyer counts 2–20 (deterministic, since all buyers hit the same fixed delay window) — reproduces L3's actual 2-buyer result (1 oversold) at the default.
- **Caught and fixed a real Mermaid rendering bug during browser verification**: L2's sequence-diagram flowchart's first node, `A["Someone asks a direct question"]` (from the corporate-politics unit earlier this session) doesn't apply here, but a near-identical class of bug did: a manual markdown line-wrap split the word "time-of-check" across two source lines as `time-of-\ncheck`, which Markdown's soft-line-break-to-space rule rendered as the literal typo "time-of- check" in prose. Fixed by removing the mid-word line break; confirmed no other hyphenated word had the same issue via `grep`.
- **Found and fixed a real, previously-undiscovered bug in the shared `ExercisePanel.astro` component, affecting every code exercise site-wide, not just this unit's content**: the sandboxed Web Worker ran `eval(code)` once to define the starter/solution code, then ran each test's `eval(t.expr)` as a _separate_ eval call. `class` declarations (unlike `function` declarations) created via direct `eval()` are scoped to that eval call itself in JavaScript and don't leak into the enclosing function scope — confirmed via a standalone Node repro (`class Foo {...}` via eval, then `new Foo()` in a second eval throws `Foo is not defined`, while an equivalent `function Bar(){}` works fine). This meant any code exercise using `class` syntax in its starter code (this unit's two `mutex-pool` exercises) would fail every test with a spurious "X is not defined" instead of a real assertion result, regardless of what the learner wrote. Fixed by combining `code` and each test's `expr` into a single `eval(code + "\n" + t.expr)` call per test, verified via a standalone Worker-logic repro in the browser (stub correctly fails with real assertion errors, solution correctly passes), and confirmed the fix doesn't regress existing function-based code exercises by live-testing `applied-math/combinatorics`'s `combinations` exercise in the browser (stub fails correctly, solution passes) before and after the change.
- Ran the full guardrail suite (`generate:roadmap` — 218 units/16 tracks, `lint` — 0 issues, `format` — clean, `typecheck` — 0 errors/0 warnings/69 pre-existing hints, `validate:content` — 71 units checked, `test` — 9/9 pass, `build` — 89 pages) — clean, re-run after both fixes.
- Chrome extension bridge connected — full browser verification via `astro preview --background`: L1/L2/L3 all render; L2's sequence diagram confirmed fully legible after the line-wrap fix; all three levels' exercise panels confirmed populated via `data-shown-ids`/`data-ready`; interactive demo confirmed matching both verified readings (1 oversold at 2 buyers, 19 oversold at 20 buyers) exactly; a live quiz answer confirmed correct/wrong color-coding and locking; the `mutex-release-1` code exercise's exact starter/solution/tests were run through a standalone reproduction of the (fixed) Worker grading logic, confirming the stub fails with real per-assertion errors and the solution passes all 3 tests; `applied-math/combinatorics`'s pre-existing `combinations` code exercise was also live-tested in the UI (stub fails, solution passes) to confirm no regression from the shared-component fix.
- Flipped `systems/race-conditions` to `done` in `ROADMAP.md`. 71 total `done` units now. Committed locally (not pushed).
- Next: continuing the standing autonomous loop — auto-picking the next unit by lowest done/total ratio across tracks.

## 2026-08-19 — New unit: web/component-state

- Auto-picked `web/component-state` — lowest done/total ratio (5/17) — "How do we manage state across a page without global chaos? (component state, unidirectional data flow)".
- Technical track. Built around one running example across all three levels: a shopping site with a cart count shown in a header badge and a cart-drawer badge, where a "quick buy" shortcut updates the drawer but was never wired to update the header, causing the two badges to silently disagree even though the underlying count is correct.
- L1 introduces state, single source of truth, unidirectional data flow, and render function as key terms. L2 gives a mermaid flowchart of the action → state update → render loop (explicitly showing no arrow skips straight from an action to one display), a scattered-updates-vs-single-store comparison table, and a section on why "just be more careful" doesn't scale as a fix. L3 reproduces the two-badge bug with real, runnable JS (plain objects standing in for DOM elements, verified header stuck at "1" while drawer reaches "2"), fixes it with a `createCartStore` + `subscribe` pattern (verified both badges reach "2" after the same two actions), and extends into adding a third "recently viewed" display later with zero changes to existing handlers (verified all three reach "3").
- Wrote 40 exercises (13/13/14, 19 pools) — L1/L2 quiz-only, L3 mixing 4 code items (2 pools: `create-cart-store` implementing `addItem`/`removeItem`, `subscribe-notify` implementing a generic store's `setState`/`subscribe`) with 10 quiz items; all 4 code exercises' solutions independently verified against their tests via a standalone Node script before writing.
- Built `interactives.json`: a `changes-required-to-add-a-display` demo (level 3) charting `naiveChangesRequired = existingHandlers` (linear growth) against `storeChangesRequired = 1` (flat) as the number of existing handlers grows — a genuine, back-derived illustration of why the store approach doesn't get more fragile as a page grows, reproducing L3's actual 2-handler setup at the default.
- **Caught and fixed a markdown line-wrap typo during browser verification**: a manual line break split "time-of-check" across two source lines in a _different_ file from an earlier unit this session (`systems/race-conditions`) as `time-of-\ncheck`, which rendered as the literal "time-of- check" — same class of bug as the earlier Mermaid node-truncation fix, caught by the same discipline of actually reading rendered output rather than trusting markdown source. No occurrence of this in `component-state`'s own content (checked via `grep`), but documented here since it was fixed as part of this session's verification pass before this unit's commit.
- Ran the full guardrail suite (`generate:roadmap` — 218 units/16 tracks, `lint` — 0 issues, `format` — clean, `typecheck` — 0 errors/0 warnings/69 pre-existing hints, `validate:content` — 72 units checked, `test` — 9/9 pass, `build` — 90 pages) — clean.
- Chrome extension bridge connected — full browser verification via `astro preview --background`: L1/L2/L3 all render; L2's mermaid loop diagram confirmed every node label renders in full (checked via `foreignObject` text extraction, not just a screenshot) after last unit's truncation bug made this worth double-checking; all three levels' exercise panels confirmed populated via `data-shown-ids`/`data-ready`; interactive demo confirmed matching both verified readings (2 handlers → 2 vs. 1 edits, 15 handlers → 15 vs. 1 edits) exactly; the `subscribe-notify` code exercise's `setState` implementation was live-tested in the UI — unmodified stub fails with real assertion errors ("Expected 0 to be 5"), inserting the correct solution passes both tests.
- Flipped `web/component-state` to `done` in `ROADMAP.md`. 72 total `done` units now. Committed locally (not pushed).
- Next: continuing the standing autonomous loop — auto-picking the next unit by lowest done/total ratio across tracks.

## 2026-08-19 — New unit: career-craft/goal-setting

- Auto-picked `career-craft/goal-setting` — alphabetically first among several tracks tied at lowest done/total ratio (4/13) — "How do I build a growth plan that isn't just 'work harder'? (goal-setting, skill gap analysis)". This slug is the known cross-track collision with `people-management/goal-setting` (documented in an earlier session's `new-unit.mjs` bug); this time the scaffold correctly flagged the `career-craft` row `in-progress` without touching `people-management`'s already-`done` row — confirmed via `grep` before and after.
- Non-technical/workplace-mechanic track — per rule 9, grounded entirely in a fully worked real-world scenario. Built around one running example across all three levels, distinct from the earlier `people-management/goal-setting` unit's team-OKR framing: two engineers, Jordan and Alex, both pursuing the same senior-engineer promotion over the same six months. Jordan's plan is "work harder"; Alex instead asks their manager directly what the specific gap is, then designs one deliberate opportunity to close exactly that gap.
- L1 introduces skill gap analysis, growth plan, calibration conversation, and evidence of readiness as key terms. L2 gives a mermaid flowchart of the iterative growth-plan loop (identify the gap → design a targeted opportunity → produce evidence → check back, looping if not yet closed), an effort-based-vs-gap-based comparison table, and a section on why asking directly beats guessing at the gap. L3 rebuilds Alex's actual six months: a row-by-row evidence-vs-bar comparison table identifying cross-team technical leadership as the one clear gap, a month-by-month table of the chosen migration project producing specific evidence for exactly that gap, an extend-it question on restructuring one opportunity to cover two named gaps at once rather than splitting effort, and a 5-row failure-modes table.
- Wrote 40 quiz-only exercises (13/13/14, 19 pools) per rule 9's non-technical-track requirement — no code items.
- Built `interactives.json`: an `evidence-strength-from-targeted-opportunities` demo (level 3) modeling `evidenceScore = min(100, targetedOpportunities × 70)`, back-derived from L3's own single-opportunity case (1 targeted opportunity → 70, a strong but not maxed case; 0 → nothing to bring to calibration). Verified both readings plus the 3-opportunity cap (100) exactly in the browser.
- Ran the full guardrail suite (`generate:roadmap` — 218 units/16 tracks, `lint` — 0 issues, `format` — clean, `typecheck` — 0 errors/0 warnings/69 pre-existing hints, `validate:content` — 73 units checked, `test` — 9/9 pass, `build` — 91 pages) — clean.
- Chrome extension bridge connected — full browser verification via `astro preview --background`: L1/L2/L3 all render (L1's title with embedded escaped double quotes displays correctly); L2's mermaid loop diagram confirmed every node label renders in full via `foreignObject` text extraction; all three levels' exercise panels confirmed populated via `data-shown-ids`/`data-ready`; interactive demo confirmed matching all three verified readings (70 at default, 100 at max, 0 at zero) exactly; a live quiz answer confirmed correct/wrong color-coding.
- Flipped `career-craft/goal-setting` to `done` in `ROADMAP.md`. 73 total `done` units now. Committed locally (not pushed).
- Next: continuing the standing autonomous loop — auto-picking the next unit by lowest done/total ratio across tracks.

## 2026-08-19 — New unit: learning-craft/spaced-repetition

- Auto-picked `learning-craft/spaced-repetition` — alphabetically first among several tracks tied at lowest done/total ratio (4/13) — "How do I retain what I learn instead of relearning it every few months? (spaced repetition, retrieval practice, active recall)". Domain-general track — per rule 4c, used a universal, non-workplace scenario rather than forcing an engineering frame, even though the site's own progress-tracking system (documented in `CLAUDE.md`) implements this exact concept.
- Built around one running example across all three levels: Maya and Diego each study 60 new vocabulary words for a test 4 weeks out, both investing exactly 3 total hours starting on the same day — Maya in one massed session on day 1, Diego across six sessions spaced over the month. **Caught and fixed a factual inconsistency during drafting, before any guardrails ran**: an early draft had Maya's single session happen "the night before the test," which would contradict the forgetting curve's own math (she'd still show reasonably high next-day retention) — revised so both start on day 1 and the test is 4 weeks later, making the retention gap actually follow from the curve rather than contradicting it.
- L1 introduces spaced repetition, retrieval practice, active recall, and forgetting curve as key terms. L2 gives an `xychart-beta` forgetting-curve chart (steep early drop, flattening tail), a massed-vs-spaced comparison table, and a section distinguishing active recall from spacing as two independent levers. L3 rebuilds Maya's and Diego's actual day-by-day schedules as two tables with illustrative retention numbers at each point (Maya: 100% → ~10% by day 28 with zero reviews; Diego: five reviews on days 2/4/8/16/24, each resetting retention near-100% with a shallower pre-review dip each time, ending at ~90% on test day), an explicit note that the raw day-count gap alone doesn't explain the difference (curve sturdiness from repeated review matters too), an extend-it question isolating what happens if Diego kept the same schedule but reread passively instead of using active recall, and a 5-row failure-modes table.
- Wrote 40 quiz-only exercises (13/13/14, 19 pools) per rule 9's non-technical-track requirement — no code items.
- Built `interactives.json`: a `retention-from-spaced-reviews` demo (level 3) modeling `retentionAtTest = min(95, 10 + numberOfReviews × 16)`, back-derived from L3's own verified numbers — reproduces Maya's actual ~10% at 0 reviews and Diego's actual ~90% at 5 reviews exactly, capping at 95% for additional reviews. Verified all three readings exactly in the browser.
- Ran the full guardrail suite (`generate:roadmap` — 218 units/16 tracks, `lint` — 0 issues, `format` — clean, `typecheck` — 0 errors/0 warnings/69 pre-existing hints, `validate:content` — 74 units checked, `test` — 9/9 pass, `build` — 92 pages) — clean.
- Chrome extension bridge connected — full browser verification via `astro preview --background`: L1/L2/L3 all render; L2's `xychart-beta` forgetting curve confirmed rendering (path/line elements present); all three levels' exercise panels confirmed populated via `data-shown-ids`/`data-ready`; interactive demo confirmed matching all three verified readings (90 at default, 10 at zero reviews, 95 capped at max) exactly; a live quiz answer confirmed correct/wrong color-coding.
- Flipped `learning-craft/spaced-repetition` to `done` in `ROADMAP.md`. 74 total `done` units now. Committed locally (not pushed).
- Next: continuing the standing autonomous loop — auto-picking the next unit by lowest done/total ratio across tracks.

## 2026-08-19 — New unit: product-domain/validation

- Auto-picked `product-domain/validation` — alphabetically first among several tracks tied at lowest done/total ratio (4/13) — "How do I know if I'm building the right thing before I build it? (validation, prototyping, MVP thinking)". Non-technical/domain-specific track — per rule 9, grounded entirely in a fully worked product scenario, no fake pseudocode.
- Built around one running example across all three levels: a team builds a full bulk export feature from a secondhand sales request ("customers want to export their data"), spends 6 weeks, and gets 1% adoption (2 of 200 customers). A second pass tests the same assumption with a 2-day fake-door prototype (a button that opens a follow-up question instead of exporting anything) before building anything real.
- L1 introduces validation, prototype, MVP, and assumption-vs-evidence as key terms. L2 gives a mermaid validate-before-build loop, a "smaller version of the feature" vs. "test of the riskiest assumption" comparison table correcting the common MVP misconception, and a section on why a secondhand stated request isn't the same as behavioral evidence. L3 rebuilds the fake-door test's actual metrics (40 of 200 clicked, 32 of 36 answers wanted sharing with an accountant, not raw export), the resulting pivot to a shareable read-only link (1 week to build, 75% adoption vs. the original's 1%), an extend-it question on whether a _confirming_ validation result would still have value, and a 5-row failure-modes table.
- Wrote 40 quiz-only exercises (13/13/14, 19 pools) per rule 9's non-technical-track requirement — no code items.
- Built `interactives.json`: a `cost-of-skipping-validation` demo (level 3) charting `wastedEngineeringWeeks = weeksBuildBeforeValidating` (linear growth) against a flat `validationCostWeeks = 0.5` (matching L3's 2-day test) — reproduces the original team's actual 6-weeks-at-risk case at the default, and the validate-first team's 0-weeks-at-risk case at zero. Verified both readings exactly in the browser.
- Ran the full guardrail suite (`generate:roadmap` — 218 units/16 tracks, `lint` — 0 issues, `format` — clean, `typecheck` — 0 errors/0 warnings/69 pre-existing hints, `validate:content` — 75 units checked, `test` — 9/9 pass, `build` — 93 pages) — clean.
- Chrome extension bridge connected — full browser verification via `astro preview --background`: L1/L2/L3 all render; L2's mermaid loop diagram confirmed every node label renders in full via `foreignObject` text extraction; all three levels' exercise panels confirmed populated via `data-shown-ids`/`data-ready`; interactive demo confirmed matching both verified readings (6/0.5 at default, 0/0.5 at zero) exactly; a live quiz answer confirmed correct/wrong color-coding.
- Flipped `product-domain/validation` to `done` in `ROADMAP.md`. 75 total `done` units now. Committed locally (not pushed).
- Next: continuing the standing autonomous loop — auto-picking the next unit by lowest done/total ratio across tracks.

## 2026-08-19 — New unit: security/symmetric-asymmetric-encryption-basics

- Auto-picked `security/symmetric-asymmetric-encryption-basics` — lowest done/total ratio (4/13) — "How do we keep data unreadable to anyone but the intended recipient? (symmetric vs. asymmetric encryption basics)".
- Technical track. Built around one running example across all three levels: Alice needs to send Bob a confidential contract by email — she encrypts it with a symmetric key, then has to email Bob that same key over the same interceptable channel, which is the key distribution problem asymmetric encryption exists to solve.
- L1 introduces symmetric encryption, asymmetric encryption, public/private key pair, and key distribution problem as key terms. L2 gives two mermaid sequence diagrams (symmetric: both file and key cross the network; asymmetric: only the public key does), a cost comparison table, and a section on why real systems combine both as hybrid encryption (previewing the next unit, TLS/HTTPS). L3 implements and runs real Node `crypto`-module code: AES-256-GCM symmetric encrypt/decrypt (verified correct-key recovery and wrong-key authentication failure), RSA-2048 asymmetric encrypt/decrypt, a timing comparison measuring the actual per-operation cost (symmetric ~0.028ms vs. asymmetric private-key ~0.55ms on an identical 32-byte payload — about 19.5x slower, independently verified via a standalone Node script before writing), a full hybrid-encryption roundtrip (AES for the message, RSA-wrapped AES key, 256-byte wrapped key confirmed independent of message length), and an extend-it question on what a 100x-longer message would change (only the symmetric step's cost, not the asymmetric wrapping cost).
- Wrote 40 exercises (13/13/14, 19 pools) — L1/L2 quiz-only, L3 mixing 4 code items with 10 quiz items. The 4 code exercises use a simplified XOR-cipher illustration of the symmetric round-trip property (explicitly labeled in-exercise as a toy illustration, not real cryptography — L3's actual encryption uses real AES-256-GCM/RSA-2048 via Node's crypto module) plus a `chooseEncryptionApproach`/`wrappedKeySizeBytes` pair modeling the real hybrid-encryption size-limit logic from L3. This choice was necessary because the exercise sandbox is a plain browser Web Worker with no Node `crypto` module and no synchronous Web Crypto API, so genuine AES/RSA operations aren't testable there — all 4 solutions independently verified against their exact tests via a standalone Node script before writing.
- Built `interactives.json`: a `symmetric-vs-asymmetric-decrypt-time` demo (level 3) charting `symmetricTimeMs`/`asymmetricTimeMs` as linear functions of operation count, using the exact measured per-operation rates from L3 — reproduces L3's actual 500-operation result (14.2ms vs. 277.3ms) exactly at the default. Verified in the browser.
- Ran the full guardrail suite (`generate:roadmap` — 218 units/16 tracks, `lint` — 0 issues, `format` — clean, `typecheck` — 0 errors/0 warnings/69 pre-existing hints, `validate:content` — 76 units checked, `test` — 9/9 pass, `build` — 94 pages) — clean.
- Chrome extension bridge connected — full browser verification via `astro preview --background`: L1/L2/L3 all render; both L2 sequence diagrams confirmed rendering with correct participant/note text; all three levels' exercise panels confirmed populated via `data-shown-ids`/`data-ready`; interactive demo confirmed matching the verified 500-operation reading (14.2/277.3) exactly; the `xor-cipher-1` code exercise was live-tested in the UI — unmodified stub fails the primary roundtrip assertion with a real error, inserting the correct solution passes all 3 tests.
- Flipped `security/symmetric-asymmetric-encryption-basics` to `done` in `ROADMAP.md`. 76 total `done` units now. Committed locally (not pushed).
- Next: continuing the standing autonomous loop — auto-picking the next unit by lowest done/total ratio across tracks.

## 2026-08-19 — New unit: software-design/solid-principles

- Auto-picked `software-design/solid-principles` — alphabetically first among tracks tied at lowest done/total ratio (4/13) — "How do I design so a change in one place doesn't ripple everywhere? (SOLID principles, dependency inversion)". Given the unit title's specific emphasis on dependency inversion (not all five SOLID principles in equal depth), scoped the unit to DIP as the primary worked mechanism, with the other four principles covered briefly as key terms/context in L2's table rather than each getting a full worked example — a deliberate scoping call given the title's own framing, not an oversight.
- Built around one running example across all three levels: 12 classes each directly construct `new SendgridEmailClient()` internally, so switching email providers (to Postmark) means editing all 12 — the ripple comes from where the vendor decision was allowed to live, not from the vendor switch itself.
- L1 introduces SOLID, dependency inversion principle, abstraction, and dependency injection as key terms. L2 gives a one-line-per-letter table of all five SOLID principles, a mermaid before/after flowchart (direct dependency vs. dependency-on-abstraction with three implementations), a concrete-vs-abstraction comparison table, and an explicit caution against over-applying DIP everywhere. L3 implements and runs real JS: `TightlyCoupledOrderService` (constructs `SendgridEmailClient` internally) vs. the fixed `OrderService` (constructor-injected client) — verified `OrderService.toString()` is character-for-character identical across the Sendgrid and Postmark examples (zero lines changed to add a new provider), a `FakeEmailClient` for testing (verified recording sent messages with no real network call), and an extend-it question on where the vendor decision actually goes after inversion (the composition root, not nowhere).
- Wrote 40 exercises (13/13/14, 19 pools) — L1/L2 quiz-only, L3 mixing 4 code items (2 pools: `order-service-injection` generalizing the DIP pattern to both `OrderService` and a `NotificationService` variant, `fake-client` implementing `FakeEmailClient` and a `countMessagesTo` test helper) with 10 quiz items; all 4 code exercises' solutions independently verified against their exact tests via a standalone Node script before writing.
- Built `interactives.json`: a `files-touched-switching-providers` demo (level 3) charting `filesToTouchTight = numberOfConsumerClasses` (linear growth) against a flat `filesToTouchInverted = 1`, reproducing L1's actual 12-vs-1 scenario at the default. Verified in the browser.
- Ran the full guardrail suite (`generate:roadmap` — 218 units/16 tracks, `lint` — 0 issues, `format` — clean, `typecheck` — 0 errors/0 warnings/69 pre-existing hints, `validate:content` — 77 units checked, `test` — 9/9 pass, `build` — 95 pages) — clean.
- Chrome extension bridge connected — full browser verification via `astro preview --background`: L1/L2/L3 all render; L2's mermaid before/after flowchart (with subgraphs, a diagram shape not tested earlier this session) confirmed every node and subgraph label renders in full via `foreignObject` text extraction; all three levels' exercise panels confirmed populated via `data-shown-ids`/`data-ready`; interactive demo confirmed matching the verified 12-vs-1 reading exactly; the `NotificationService` code exercise variant was live-tested in the UI — unmodified stub fails both tests with real assertion errors, inserting the correct solution passes both.
- Flipped `software-design/solid-principles` to `done` in `ROADMAP.md`. 77 total `done` units now. Committed locally (not pushed).
- Next: continuing the standing autonomous loop — auto-picking the next unit by lowest done/total ratio across tracks.

## 2026-08-19 — New unit: testing-quality/bdd

- Auto-picked `testing-quality/bdd` — lowest done/total ratio (4/13) — "How do I test behavior instead of implementation? (BDD, testing through the public interface)".
- Technical track. Built around one running example across all three levels: a shopping cart's internal storage is refactored from an array to a Map (purely for lookup performance), and 15 tests turn red even though every externally visible behavior — adding items, computing totals, counting items — still works identically.
- L1 introduces BDD, public interface, implementation detail, and Given-When-Then as key terms. L2 gives a mermaid flowchart of the two things a test can target (public interface vs. internals) and their diverging outcomes on refactor, a Given-When-Then breakdown table, a section on why implementation-coupled tests feel like the natural shortcut to write, and a protects-you-from comparison table. L3 implements and runs real JS: `ShoppingCartV1` (array-backed) and `ShoppingCartV2` (Map-backed, with a small genuine behavior improvement — merging duplicate item adds) — verified implementation-coupled assertions (`cart.items.length`, `cart.items[0]`) pass on V1 but fail on V2, while behavior-based assertions (`getItemCount()`, `getTotal()`) pass identically on both — plus a Given-When-Then rephrasing of the same behavior test and a failure-modes table.
- Wrote 40 exercises (13/13/14, 19 pools) — L1/L2 quiz-only, L3 mixing 4 code items (2 pools: `cart-v2-behavior` implementing `getItemCount`/`getTotal`, `cart-v2-addItem` implementing the merge-on-duplicate `addItem` logic plus a generalized `mergeOrAdd` helper) with 10 quiz items; all 4 code exercises' solutions independently verified against their exact tests via a standalone Node script (reconstructing the full class from starter code + solution) before writing.
- Built `interactives.json`: a `tests-broken-by-internal-refactor` demo (level 3) charting `implementationCoupledBroken = testsWrittenAgainstInternals` (linear growth) against a flat `behaviorBasedBroken = 0`, reproducing L1's actual 15-vs-0 scenario at the default. Verified in the browser.
- Ran the full guardrail suite (`generate:roadmap` — 218 units/16 tracks, `lint` — 0 issues, `format` — clean, `typecheck` — 0 errors/0 warnings/69 pre-existing hints, `validate:content` — 78 units checked, `test` — 9/9 pass, `build` — 96 pages) — clean.
- Chrome extension bridge connected — full browser verification via `astro preview --background`: L1/L2/L3 all render; L2's mermaid flowchart (with edge labels, not just node labels) confirmed every label renders in full via `foreignObject` text extraction; all three levels' exercise panels confirmed populated via `data-shown-ids`/`data-ready`; interactive demo confirmed matching the verified 15-vs-0 reading exactly; the `cart-v2-addItem` code exercise was live-tested in the UI — unmodified stub fails all 3 tests with real assertion errors, inserting the correct solution passes all 3.
- Flipped `testing-quality/bdd` to `done` in `ROADMAP.md`. 78 total `done` units now. Committed locally (not pushed).
- Next: continuing the standing autonomous loop — auto-picking the next unit by lowest done/total ratio across tracks.

## 2026-08-19 — New unit: git-teamwork/commit-conventions

- Auto-picked `git-teamwork/commit-conventions` — lowest done/total ratio (5/17) — "How do we keep history readable as a team? (commit conventions, atomic commits)".
- Technical track, modeling git internals as pure JS data (consistent with `merge-rebase`'s convention). Built around one running example across all three levels: `git bisect` correctly narrows a production bug down to a single commit, but that commit is titled "various fixes and cleanup," touches 14 files, and bundling a dozen unrelated changes means finding the actual bad line still takes two more hours of manual diff-reading.
- L1 introduces atomic commit, conventional commits, git bisect, and binary search as key terms. L2 gives a mermaid flowchart of bisect's binary-search narrowing (16 candidates → repeated midpoint testing → 1 commit left), a conventional-commit-types table (feat/fix/refactor/docs/test/chore), an atomic-vs-bundled comparison table (what bisect points to, work left after, message usefulness, revertability), and a generalizable-lesson section extending the idea to code review and reverting deploys. L3 implements `bisect(commits, isBad)` as real binary search and runs it against two histories — verified 16 atomic commits find the bug (`c11`) in exactly 4 steps (`log2(16)`), while a bundled 9-commit history (8 atomic + 1 mega-commit hiding 8 logical changes) finds `megaCommit` in only 3 steps but leaves the actual culprit still to be found manually — plus an extend-it check at 64 commits (6 steps, matching `log2(64)`) and a failure-modes table.
- Wrote 35 exercises (13/10/12, 17 pools) — L1/L2 quiz-only, L3 mixing 4 code items across 2 pools (`bisect-implementation`: `bisect`/`stepsNeeded`; `conventional-commits-parsing`: `parseConventionalCommit`/`groupCommitsByType`) with 8 quiz items; all 4 code exercises' solutions independently verified against their exact tests via a standalone Node script before writing. Below the ~40-item pilot target on L2 (10 vs. ~13) but still 5 pools deep — noted rather than padded, per CLAUDE.md's "don't pad" rule.
- Built `interactives.json`: a `bisect-steps-by-history-size` demo (level 3) using the exact `bisect` binary-search logic from L3, charting `bisectSteps = ceil(log2(historySize))` as the slider moves across 2–256 commits — reproduces L3's verified 16→4 and 64→6 readings exactly at those positions. Verified in the browser across the full slider range (min=2→1 step, max=256→8 steps).
- Ran the full guardrail suite (`generate:roadmap` — 218 units/16 tracks, `lint` — 0 issues, `format` — clean, `typecheck` — 0 errors, `validate:content` — 79 units checked, `test` — 9/9 pass, `build` — 97 pages) — clean.
- Chrome extension bridge connected — full browser verification via `astro preview --background`: L1/L2/L3 all render; L2's bisect mermaid flowchart confirmed every node label renders in full via `foreignObject` text extraction; all three levels' exercise panels confirmed populated; interactive demo confirmed matching the verified 16→4/64→6 readings and correct at both slider extremes; the `stepsNeeded` code exercise was live-tested in the UI — unmodified stub fails all 3 tests with real assertion errors, inserting the correct solution passes all 3; a quiz item was answered incorrectly on purpose and confirmed the "Not quite" feedback plus explanation revealed correctly.
- Flipped `git-teamwork/commit-conventions` to `done` in `ROADMAP.md`. 79 total `done` units now. Committed locally (not pushed).
- Next: continuing the standing autonomous loop — auto-picking the next unit by lowest done/total ratio across tracks.

## 2026-08-19 — New unit: applied-math/probability-distributions-uniform

- Auto-picked `applied-math/probability-distributions-uniform` — tied for lowest done/total ratio (5/15, 33.3%) with `infra-delivery`, `logic`, and `people-management`, winning the alphabetical tiebreak — "How do I model something that involves chance? (probability distributions — uniform, binomial, normal — and where each shows up in systems)".
- Technical track. Built around three tied real-code scenarios across all three levels rather than one running example, since the unit covers three distinct distributions: (1) a load balancer picking 1 of 4 servers uniformly at random, where server 3 got 64 of 200 requests (32% vs. expected 25%) — is that a bug? (2) a downstream dependency with a 2% independent failure rate, and what failure count in 500 requests should actually page someone; (3) end-to-end latency as the sum of 6 independent uniformly-distributed hop delays, demonstrating the Central Limit Theorem.
- L1 introduces probability distribution, uniform distribution, binomial distribution, normal distribution/CLT, and standard deviation as key terms. L2 gives an `xychart-beta` bar chart of the uniform distribution's flat shape, a binomial formulas table (mean/variance/stddev) plus an `xychart-beta` histogram of the 500-request/2%-failure-rate distribution (bucket probabilities independently computed, not guessed — an earlier draft's made-up bucket values were caught and replaced with verified ones), a mermaid flowchart of the CLT (6 flat delays summing to a bell shape), and a picking-the-right-tool comparison table. L3 implements and runs real JS: `binomialStats`/`zScore` (verified server 3's 64 hits are 2.286 standard deviations from the expected 50 — notable but not proof of a bug), `binomialPMF`/`probabilityAtLeast` using a log-factorial technique to avoid `500!` overflow (verified P(20+ failures in 500 trials, p=0.02) ≈ 0.31%, ~1 in 320 — the basis for a defensible alert threshold), and a seeded Mulberry32 PRNG simulating 20,000 samples of 6 summed `uniform(1,5)` delays (verified empirical mean 18.026/variance 7.977 against theoretical 18/8, and p50/p99 matching CLT predictions) — plus an extend-it question on relative spread shrinking with more summed pieces, and a failure-modes table.
- Wrote 38 exercises (13/13/12, 18 pools) — L1/L2 quiz-only, L3 mixing 4 code items across 2 pools (`binomial-stats-and-zscore`: `binomialStats`/`zScore`; `binomial-probability`: `binomialPMF`/`probabilityAtLeast`) with 8 quiz items; all 4 code exercises' solutions and stubs independently verified against their exact tests via a standalone Node script simulating the actual Worker eval semantics before writing — caught and fixed one exercise (`probabilityAtLeast`) whose `solution` field was initially missing its `logFactorial`/`binomialPMF` dependencies, which the real exercise runner needs since it evaluates the full textarea content, not an isolated function.
- Built `interactives.json`: an `alert-threshold-rarity` demo (level 3) using the exact `binomialPMF`/log-factorial logic from L3, charting the probability of hitting a given failure-count threshold by pure chance as the threshold slider moves from 5 to 35 — reproduces L3's verified 0.31% at threshold=20 exactly. Verified in the browser at the default, min (5→97.19%), and max (35→0%).
- Ran the full guardrail suite (`generate:roadmap` — 218 units/16 tracks, `lint` — 0 issues, `format` — clean, `typecheck` — 0 errors, `validate:content` — 80 units checked, `test` — 9/9 pass, `build` — 98 pages) — clean.
- Chrome extension bridge connected — full browser verification via `astro preview --background`: L1/L2/L3 all render; L2's two `xychart-beta` bar charts (uniform's flat shape, binomial's histogram) and its CLT flowchart confirmed rendering with full, correct data via page-text extraction; all three levels' exercise panels confirmed populated; interactive demo confirmed matching the verified 0.31% reading at threshold=20 and correct at both slider extremes; the `probabilityAtLeast` code exercise was live-tested in the UI — unmodified stub fails all 3 tests with real assertion errors, inserting the correct solution passes all 3; a quiz item was answered incorrectly on purpose and confirmed the "Not quite" feedback plus explanation revealed correctly.
- Flipped `applied-math/probability-distributions-uniform` to `done` in `ROADMAP.md`. 80 total `done` units now. Committed locally (not pushed).
- Next: continuing the standing autonomous loop — auto-picking the next unit by lowest done/total ratio across tracks.

## 2026-08-19 — New unit: infra-delivery/orchestration-basics

- Auto-picked `infra-delivery/orchestration-basics` — lowest done/total ratio (4/12, 33.3%), winning the alphabetical tiebreak over `logic` and `people-management` — "How do we run many containers reliably across many machines? (orchestration basics: Kubernetes concepts)".
- Technical track. Built around one running example across all three levels: a team running containers by hand on 2 servers loses half its capacity when one server crashes at 3am, with no automatic recovery until a paged engineer manually restarts things — motivating the reconciliation loop and scheduler as the two things actually missing.
- L1 introduces container orchestration, desired state/reconciliation loop, scheduler, node, pod, and self-healing as key terms. L2 gives a mermaid flowchart of the reconciliation loop (desired vs. actual state comparison → gap → reschedule), a scheduler-concepts table (node capacity, pod resource requests, placement, spread vs. pack), and a manual-ops-vs-orchestrator comparison table ending on the key caveat that an orchestrator doesn't manufacture capacity that isn't there. L3 implements a real `scheduleSpread(nodes, pods)` bin-packing scheduler (placing pods on whichever eligible node has the most free capacity, not just the first fit) and runs two verified scenarios: Scenario A (3 nodes, moderate load) where a failed node's 2 lost pods are both fully rescheduled onto surviving spare capacity, and Scenario B (2 nodes, packed to exactly 0 remaining capacity) where the identical reconciliation logic leaves both lost pods permanently pending — demonstrating that "the loop is running correctly" and "the app is fully recovered" are not the same claim. Includes an extend-it question and a 5-row failure-modes table.
- Wrote 27 exercises (11/8/8, 13 pools) — L1/L2 quiz-only, L3 mixing 4 code items across 2 pools (`spread-scheduler`: `scheduleSpread`/a contrasting `schedulePack`; `reconciliation`: `detectGap`/`countPendingAfterNodeFailure`, the latter combining scheduling + simulated failure + reconciliation into the exact function verified against both Scenario A and B) with 8 quiz items; all 4 code exercises' solutions and stubs independently verified against their exact tests via a standalone Node script before writing. Below the ~40-item pilot target — the unit's actual concept surface (one scheduler, one reconciliation idea, two contrasting scenarios) didn't support more without padding, noted rather than forced.
- Built `interactives.json`: a `headroom-vs-recovery` demo (level 3) reusing the exact `scheduleSpread` logic from L3, charting pods-still-pending-after-failure as the node-count slider moves from 2 to 8 (fixed 8-pod, 2-CPU/4-mem-each workload) — reveals a genuine threshold effect: 2 pods pending at ≤4 nodes, 0 pending at ≥5 nodes. Verified in the browser across the full range.
- Ran the full guardrail suite (`generate:roadmap` — 218 units/16 tracks, `lint` — 0 issues, `format` — clean, `typecheck` — 0 errors, `validate:content` — 81 units checked, `test` — 9/9 pass, `build` — 99 pages) — clean.
- Chrome extension bridge connected — full browser verification via `astro preview --background`: L1/L2/L3 all render; L2's reconciliation-loop mermaid flowchart confirmed every node label renders in full via `foreignObject` text extraction; all three levels' exercise panels confirmed populated; interactive demo confirmed reproducing the exact verified threshold (2 pending at nodeCount 2 and 4, 0 pending at 5 and 8); the `scheduleSpread` code exercise was live-tested in the UI — unmodified stub fails all 3 tests with real assertion errors, inserting the correct solution passes all 3; a quiz item was answered incorrectly on purpose and confirmed the "Not quite" feedback plus explanation revealed correctly.
- Flipped `infra-delivery/orchestration-basics` to `done` in `ROADMAP.md`. 81 total `done` units now. Committed locally (not pushed).
- Next: continuing the standing autonomous loop — auto-picking the next unit by lowest done/total ratio across tracks.

## 2026-08-19 — New unit: logic/state-machines

- Auto-picked `logic/state-machines` — lowest done/total ratio (4/12, 33.3%), winning the alphabetical tiebreak over `people-management` — "How do I reason about a system with many possible states? (state machines, truth tables)".
- Technical track. Built around one running example across all three levels: a file upload widget tracked with 4 independent booleans (`isUploading`, `isPaused`, `isSuccess`, `isError`) ships a bug where the `SUCCESS` handler forgets to reset `isUploading`, so the UI shows a spinner and a checkmark simultaneously — only 5 of the 16 possible boolean combinations were ever meant to be reachable, and nothing stopped the other 11.
- L1 introduces state machine, state, transition, truth table, and invalid/illegal state as key terms. L2 gives a partial truth table (5 meaningful rows + 2 named invalid examples + a "9 more, none meaningful" summary row) for the 4-boolean version, a mermaid flowchart of the 6-state/8-transition state machine version of the same widget, and a comparison table (representable combinations, meaningful fraction, what prevents invalid combinations, correctness burden per handler) plus a generalization to order lifecycles/TCP handshakes/CI pipeline status. L3 implements and runs real JS: a `legacyHandleEvent` reproducing the exact bug (verified `START, SUCCESS` → both `isUploading` and `isSuccess` true), a `transition(state, event)` function with an explicit transition table (verified the same event sequence produces a single clean `"success"` state, and that `PAUSE` from `success` throws `Invalid transition: cannot PAUSE from success`), plus verified pause/resume and retry-after-`FAIL` paths, an extend-it question on adding a bounded retry limit, and a 5-row failure-modes table.
- Wrote 28 exercises (10/8/10, 14 pools) — L1/L2 quiz-only, L3 mixing 4 code items across 2 pools (`transition-function`: `transition`/`canTransition`; `combinatorics`: `totalCombinations`/`meaningfulPercent`) with 10 quiz items; all 4 code exercises' solutions and stubs independently verified against their exact tests via a standalone Node script before writing.
- Built `interactives.json`: a `flag-combinations-vs-meaningful` demo (level 2, since the relationship is purely combinatorial rather than needing L3's specific code) charting `meaningfulPercent = (5 / 2^flagCount) * 100` as the flag-count slider moves from 3 to 8 — reproduces L2's exact 4-flag/16-combination/31.25%-meaningful reading at the default. Verified in the browser across the full range (62.5% at 3 flags, 1.95% at 8).
- Ran the full guardrail suite (`generate:roadmap` — 218 units/16 tracks, `lint` — 0 issues, `format` — clean, `typecheck` — 0 errors, `validate:content` — 82 units checked, `test` — 9/9 pass, `build` — 100 pages) — clean.
- Chrome extension bridge connected — full browser verification via `astro preview --background`: L1/L2/L3 all render; L2's truth table, flowchart (all 6 state labels and 8 transition-event labels confirmed rendering in full via `foreignObject` text extraction), and comparison table all read correctly; the interactive demo confirmed matching the verified 31.25%/62.5%/1.95% readings exactly; all three levels' exercise panels confirmed populated; the `canTransition` code exercise was live-tested in the UI (reloading once to land on a code pool, since the first load showed two quiz pools) — unmodified stub fails all 3 tests with real assertion errors, inserting the correct solution passes all 3; a quiz item was answered incorrectly on purpose and confirmed the "Not quite" feedback plus explanation revealed correctly.
- Flipped `logic/state-machines` to `done` in `ROADMAP.md`. 82 total `done` units now. Committed locally (not pushed).
- Next: continuing the standing autonomous loop — auto-picking the next unit by lowest done/total ratio across tracks.

## 2026-08-19 — New unit: people-management/early-signals

- Auto-picked `people-management/early-signals` — lowest done/total ratio (5/15, 33.3%) — "How do I know if someone on my team is struggling before it's a crisis? (early signals, psychological safety, checking in)".
- Non-technical track (soft-skill mechanic, per CLAUDE.md rule 9) — L3 grounded in a fully worked real-world scenario with prose, tables, and dialogue, no fake pseudocode. Built around one running example across all three levels: two reports miss the same deadline in the same week, but only one surprises the manager — Report A flagged a blocker directly in week 2, while Report B gave off four quiet behavioral signals across three weeks (shorter standup updates, camera off, unanswered messages, a stale review comment) that nobody was watching for.
- L1 introduces early signal, psychological safety, checking in, and lagging vs. leading signal as key terms. L2 gives a 5-row signals table (what each behavioral cue might mean, with the explicit caveat that none alone is proof), a mermaid flowchart of the psychological-safety fork (struggle begins → safe to name it? → flags early & gets help, or stays quiet & grows into a missed deadline), a low-safety vs. high-safety comparison table, and a closing section on specific check-ins as the practical lever. L3 walks through the two reports' actual three weeks side by side in a table, identifies week 2 as the point two independent signals turn "maybe nothing" into "probably something," contrasts a generic "everything okay?" check-in (fails) against a specific, observational one naming the actual noticed change (works), gives a 3-level signal-severity framework (mild/moderate/severe, each with its own response), an extend-it question on what changes when the underlying cause is outside the manager's control, and a 5-row failure-modes table.
- Wrote 28 quiz-only exercises (12/8/8, 14 pools) — no code exercises, consistent with the non-technical-track exercise convention (quiz for L1/L2/L3 rather than code for L3).
- Built `interactives.json`: a `checkin-frequency-vs-detection-delay` demo (level 2) modeling the worst-case days before a quiet struggle could be caught as directly equal to the check-in frequency (the issue could start right after a check-in, so the next one is the earliest realistic catch point) — at the default 21 days, this reproduces Report B's actual 3-week timeline from L3 exactly. Verified in the browser across the full 3–30 day range.
- Ran the full guardrail suite (`generate:roadmap` — 218 units/16 tracks, `lint` — 0 issues, `format` — clean, `typecheck` — 0 errors, `validate:content` — 83 units checked, `test` — 9/9 pass, `build` — 101 pages) — clean.
- Chrome extension bridge connected — full browser verification via `astro preview --background`: L1/L2/L3 all render; L2's psychological-safety flowchart confirmed every node and edge label renders in full via `foreignObject` text extraction; the interactive demo confirmed matching the verified 21-day default and correct at both range extremes (3 and 30 days); all three levels' exercise panels confirmed populated; one quiz item was answered correctly and confirmed the "Correct" feedback plus explanation, and a second was answered incorrectly on purpose and confirmed the "Not quite" feedback plus explanation, both revealed correctly.
- Flipped `people-management/early-signals` to `done` in `ROADMAP.md`. 83 total `done` units now. Committed locally (not pushed).
- Next: continuing the standing autonomous loop — auto-picking the next unit by lowest done/total ratio across tracks.

## 2026-08-19 — New unit: systems/transactions

- Auto-picked `systems/transactions` — tied for lowest done/total ratio (6/17, 35.3%) with `web`, winning the alphabetical tiebreak — "Why did my request \"succeed\" but the data never saved? (transactions, ACID)".
- Technical track. Built around one running example across all three levels: placing an order does two writes (decrement stock, insert order record); a customer's stock visibly drops but the order never appears, because the stock decrement succeeded and the order insert failed a moment later with nothing rolling the decrement back.
- L1 introduces transaction, ACID, rollback, commit, and write-ahead log as key terms. L2 gives a 4-row ACID table (each property mapped to what specifically breaks without it, using the stock/order scenario), a mermaid flowchart of the transaction lifecycle (begin → operate → check → commit or rollback), a without-vs-with-transactions comparison table, and a generalization beyond e-commerce to money transfers and profile/audit-log updates. L3 implements and runs real JS: `placeOrderNaive` reproducing the exact bug (verified stock drops to 7, orders stays empty), `runTransaction(store, operation, invariant)` with clone-then-commit-or-rollback semantics (verified the identical failing order leaves stock unchanged at 10), a `stockNeverNegative` consistency invariant catching an over-sell that would have silently succeeded in the naive version (verified rollback with stock unchanged at 10), a successful-order commit path (verified stock 7, orders 1), and a `durableCommit`/`recoverFromLog` write-ahead-log pair demonstrating a simulated crash still recovers the correct state (verified stock 5, orders 2, from the log alone after discarding the in-memory store) — plus an extend-it question on isolation/concurrent transactions and a 5-row failure-modes table.
- Wrote 26 exercises (10/6/10, 13 pools) — L1/L2 quiz-only, L3 mixing 4 code items across 2 pools (`transaction-wrapper`: `runTransaction`/`stockNeverNegative`; `durability`: `durableCommit`/`recoverFromLog`) with 10 quiz items; all 4 code exercises' solutions and stubs independently verified against their exact tests via a standalone Node script before writing. Caught and fixed one pool-consistency violation (`transaction-wrapper-pool`'s two variants had different `reference` HTML) before finalizing.
- Built `interactives.json`: a `phantom-stock-loss` demo (level 3) charting phantom stock lost in the naive version (`= failedOrderCount`, linear growth) against the transactional version (flat 0, since every failure rolls back cleanly) as the failed-order-count slider moves from 0 to 20 — a legitimate linear-vs-flat pairing since the two lines represent genuinely different systems' behavior, not an arithmetic complement of each other. Verified in the browser across the full range.
- Ran the full guardrail suite (`generate:roadmap` — 218 units/16 tracks, `lint` — 0 issues, `format` — clean, `typecheck` — 0 errors, `validate:content` — 84 units checked, `test` — 9/9 pass, `build` — 102 pages) — clean.
- Chrome extension bridge connected — full browser verification via `astro preview --background`: L1/L2/L3 all render; L2's transaction-lifecycle mermaid flowchart confirmed every node and edge label renders in full via `foreignObject` text extraction; the interactive demo confirmed matching the verified naive-linear/transactional-flat-zero behavior at the default, min, and max of the slider range; all three levels' exercise panels confirmed populated; the `recoverFromLog` code exercise was live-tested in the UI (landed on a code pool on first load) — unmodified stub fails all 3 tests with real assertion errors, inserting the correct solution passes all 3; a quiz item was answered incorrectly on purpose and confirmed the "Not quite" feedback plus explanation revealed correctly.
- Flipped `systems/transactions` to `done` in `ROADMAP.md`. 84 total `done` units now. Committed locally (not pushed).
- Next: continuing the standing autonomous loop — auto-picking the next unit by lowest done/total ratio across tracks.

## 2026-08-19 — New unit: web/cors

- Auto-picked `web/cors` — tied for lowest done/total ratio (6/17, 35.3%) with `business-communication` and `corporate-politics`, winning the alphabetical tiebreak — "Why does calling an API from the browser fail unpredictably? (CORS, async/await, error handling)".
- Technical track. Built around one running example across all three levels: `fetch('https://api.example.com/users')` from `https://app.example.com` works fine in Postman but fails in the browser with no data reaching the app, even though the API's own server logs show the request was handled successfully — because CORS is enforced entirely browser-side, after the server already responded. A second, deliberately separate thread covers the classic async/await gotcha: `fetch()` only rejects on network-level failures, not on HTTP error statuses like 404/500.
- L1 introduces same-origin policy, CORS, preflight request, and Access-Control-Allow-Origin as key terms. L2 gives a simple-vs-non-simple-request table (method/headers/Content-Type), a mermaid sequence diagram of the preflight round-trip (OPTIONS → check → real request), a note that even simple requests get checked (just later, on the actual response), and a does-fetch()-reject table contrasting network/CORS failures (reject) against HTTP error statuses (resolve with `ok: false`). L3 implements and runs real JS: `needsPreflight(method, headers)` (verified GET+Authorization flips to `true` even though GET alone is simple), a `simulateCorsCheck` function reproducing the exact "blocked by CORS" failure against a `buggyConfig` missing the calling origin (verified it fails at the preflight step, never sending the real request) and its one-line fix via `fixedConfig` (verified it then passes), the same check against a simple request showing it's still blocked but only after reaching the server (verified, matching L1's server-logs-show-success detail), and `getUserBuggy`/`getUserFixed` demonstrating the fetch-resolves-on-404 gap and its `response.ok` fix — plus an extend-it question on distinguishing 401 from 404 and a 5-row failure-modes table.
- Wrote 24 exercises (8/6/10, 12 pools) — L1/L2 quiz-only, L3 mixing 4 code items across 2 pools (`cors-mechanics`: `needsPreflight`/`isSameOrigin`; `fetch-error-handling`: `simulateFetchResolution`/`checkResponseOk`) with 10 quiz items; all 4 code exercises' solutions and stubs independently verified against their exact tests via a standalone Node script before writing. Caught a real correctness risk before shipping: the sandbox's `ExercisePanel.astro` grading worker runs `eval()` fully synchronously with no `await` anywhere in its loop, so an `async`/`await` code exercise's assertions would never actually be awaited — a broken stub and a correct solution would both silently "pass." Confirmed this by reading `WORKER_SOURCE` directly, then rewrote what were originally `getUserBuggy`/`getUserFixed` async exercises as synchronous equivalents (`simulateFetchResolution`, `checkResponseOk`) that teach the identical lesson without needing `await` inside the graded code. Also caught and fixed one pool-consistency violation (`cors-mechanics-pool`'s two variants had different `reference` HTML) before finalizing.
- Built `interactives.json`: a `preflight-caching` demo (level 2) modeling 20 non-simple requests spaced 30 seconds apart, charting how many separate OPTIONS preflight round-trips actually occur as the `Access-Control-Max-Age` slider moves from 0 to 600 seconds — reproduces a clean, verified relationship (20 preflights at 0s caching, 10 at the default 60s, 1 at 600s). Verified in the browser across the full range.
- Ran the full guardrail suite (`generate:roadmap` — 218 units/16 tracks, `lint` — 0 issues, `format` — clean, `typecheck` — 0 errors, `validate:content` — 85 units checked, `test` — 9/9 pass, `build` — 103 pages) — clean.
- Chrome extension bridge connected — full browser verification via `astro preview --background`: L1/L2/L3 all render; L2's preflight sequence diagram confirmed every actor label, message, and note renders in full via text extraction (a diagram type — sequenceDiagram — not previously verified this session); the interactive demo confirmed matching the verified 20/10/1 readings at 0s/60s/600s exactly; all three levels' exercise panels confirmed populated; the `simulateFetchResolution` code exercise was live-tested in the UI (landed on a code pool on first load) — unmodified stub fails all 3 tests with real assertion errors, inserting the correct solution passes all 3; a quiz item was answered incorrectly on purpose and confirmed the "Not quite" feedback plus explanation revealed correctly.
- Flipped `web/cors` to `done` in `ROADMAP.md`. 85 total `done` units now. Committed locally (not pushed).
- Next: continuing the standing autonomous loop — auto-picking the next unit by lowest done/total ratio across tracks.

## 2026-08-19 — New unit: business-communication/decision-making-frameworks

- Auto-picked `business-communication/decision-making-frameworks` — tied for lowest done/total ratio (5/14, 35.7%) with `corporate-politics`, winning the alphabetical tiebreak — "Why do meetings feel like they decide nothing? (decision-making frameworks: DACI/RACI)".
- Non-technical track — a workplace-mechanic unit (per CLAUDE.md rule 4c, decision frameworks are inherently domain-specific to work, so grounding it directly in a meeting scenario rather than simplifying to a generic example is appropriate). Built around one running example across all three levels: 8 people spend 45 minutes deciding on a logging vendor, end with "let's take this offline," and the exact same discussion recurs two weeks later because nothing was actually decided.
- L1 introduces DACI, RACI, decision rights, and diffusion of responsibility as key terms. L2 gives a DACI roles table (Driver/Approver/Contributors/Informed, what each does and doesn't do), a RACI roles table (Responsible/Accountable/Consulted/Informed) with an explicit note on when each framework fits, a mermaid flowchart of the DACI decision flow (input gathered before the decision moment, not during a live group debate), and a section on why unstructured "everyone decides together" meetings default to needing consensus and quietly fail. L3 reruns the exact vendor-decision meeting with DACI roles assigned to specific named people (with reasoning for each assignment), a 5-step numbered process for how the decision actually happens, and a verified time comparison: 720 person-minutes for the original no-roles version (8 people × 2 meetings × 45 min) versus 88 person-minutes with DACI (4-person decision meeting at 20 min + 4 people reading a 2-minute summary) — an 88% reduction — plus an extend-it question on scaling to more Contributors and a 5-row failure-modes table.
- Wrote 28 quiz-only exercises (12/8/8, 14 pools) — no code exercises, consistent with the non-technical-track convention.
- Built `interactives.json`: a `consensus-vs-daci-time` demo (level 3) charting both the no-roles-consensus total (`stakeholderCount × ceil(stakeholderCount/4) meetings × 45 min`) and the DACI total (`min(4,n) × 20 + max(0,n-4) × 2`) as the stakeholder-count slider moves from 4 to 20 — reproduces L3's exact verified 720/88 reading at the default of 8 stakeholders. Two genuinely independent lines (not an arithmetic complement of each other), matching the naive-vs-fixed pattern established earlier this session. Verified in the browser across the full range (180/80 at 4, 4500/112 at 20).
- Ran the full guardrail suite (`generate:roadmap` — 218 units/16 tracks, `lint` — 0 issues, `format` — clean, `typecheck` — 0 errors, `validate:content` — 86 units checked, `test` — 9/9 pass, `build` — 104 pages) — clean.
- Chrome extension bridge connected — full browser verification via `astro preview --background`: L1/L2/L3 all render; L2's DACI-flow mermaid flowchart confirmed every node label renders in full via `foreignObject` text extraction; the interactive demo confirmed matching the verified 720/88, 180/80, and 4500/112 readings at min/default/max; all three levels' exercise panels confirmed populated; one quiz item was answered correctly and confirmed the "Correct" feedback plus explanation, and a second was answered incorrectly on purpose and confirmed the "Not quite" feedback plus explanation, both revealed correctly.
- Flipped `business-communication/decision-making-frameworks` to `done` in `ROADMAP.md`. 86 total `done` units now. Committed locally (not pushed).
- Next: continuing the standing autonomous loop — auto-picking the next unit by lowest done/total ratio across tracks.

## 2026-08-19 — New unit: corporate-politics/framing

- Auto-picked `corporate-politics/framing` — lowest done/total ratio (5/14, 35.7%) — "How do I control the narrative around my own work without lying? (framing, emphasis, owning the story honestly)". A white-politics unit (legitimate narrative ownership), contrasted explicitly against spin as the point where it crosses into something worse.
- Non-technical, workplace-mechanic unit — grounded directly in a work scenario per CLAUDE.md rule 4c, since decision/narrative framing is inherently domain-specific to work rather than a generalizable-to-a-child concept. Built around one running example across all three levels: a project shipped with three true facts at once — 40.6% faster page loads (3.2s → 1.9s), 3 weeks late, and a 12-minute outage from a bug (found and fixed) — written up two completely different ways from the identical fact set.
- L1 introduces framing, emphasis, spin, and narrative ownership as key terms, establishing the core distinction: framing is legitimate (true + complete), lying is false, and spin is the harder-to-spot middle case (true statements, material omission). L2 gives a three-way comparison table (framing/spin/lying against two independent yes/no questions), a definition of materiality ("would a reader's understanding of the outcome change?"), a mermaid self-check flowchart (is every fact true? → is any material fact missing? → framing/spin/lying), and a section arguing chronological/undeliberate ordering isn't actually neutral. L3 writes the same project update three full ways — Draft A (buried-lead, passes the honesty check but undeliberately buries the 40.6% win under a vague apology), Draft B (honest framing, leads with the result, names the outage's specific cause and duration), Draft C (spin, every sentence true but the outage omitted entirely) — run against the same two-question standard in a comparison table, plus an extend-it question on how materiality scales with an incident's severity and a 5-row failure-modes table.
- Wrote 24 quiz-only exercises (10/8/6, 12 pools) — no code exercises, consistent with the non-technical-track convention. No `interactives.json` for this unit — a deliberate, considered call per CLAUDE.md rule 5c rather than a default: framing is a qualitative narrative-structure choice, not a continuous quantity with a natural slider variable, and forcing one (e.g., a fabricated "reader perception" statistic) would have meant inventing unverifiable research, which the project's zero-unverified-claims discipline rules out. Compensated with 3 visual elements spread across L1–L3 instead (facts callout, L2's comparison table + flowchart, L3's three-draft comparison table).
- Ran the full guardrail suite (`generate:roadmap` — 218 units/16 tracks, `lint` — 0 issues, `format` — clean, `typecheck` — 0 errors, `validate:content` — 87 units checked, `test` — 9/9 pass, `build` — 105 pages) — clean.
- Chrome extension bridge connected — full browser verification via `astro preview --background`: L1/L2/L3 all render, including all three full drafts and their comparison table in L3; L2's self-check mermaid flowchart confirmed every node and edge label renders in full via `foreignObject` text extraction; all three levels' exercise panels confirmed populated; one quiz item was answered incorrectly on purpose and confirmed the "Not quite" feedback plus explanation, and a second was answered correctly and confirmed the "Correct" feedback plus explanation, both revealed correctly.
- Flipped `corporate-politics/framing` to `done` in `ROADMAP.md`. 87 total `done` units now. Committed locally (not pushed).
- Next: continuing the standing autonomous loop — auto-picking the next unit by lowest done/total ratio across tracks.
